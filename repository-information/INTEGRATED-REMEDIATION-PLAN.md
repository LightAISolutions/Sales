# Integrated Remediation Plan — Profiler and Classroom, Phase 0 through Phase 6

**Agreed 2026-09-07 in the X3 close-out session (Opus 5 xhigh, v05.01r) and persisted at Phase 0 (Fable 5.1 High, v05.02r).** Until this file existed the plan lived only in a chat transcript and in the *Latest Session* of `SESSION-CONTEXT.md`; Phase 3's curriculum-skeleton review had nothing to edit against. This is now the **live run order for the remaining program**. **§7 (agreed 2026-09-07 PM, persisted at v05.08r) extends that order from Phase 3's close to completion and supersedes §2's rows 4–6 and G6 where they differ**; §2–§4 are kept as the v05.01r record. `PROFILER-COVERAGE-PLAN.md` §9.5 rows 5–6 and §6 (Phase D), and `CLASSROOM-CURRICULUM-PLAN.md` §7 (the cut line), map onto Phases 0–4 below and are kept as their own records, not as competing schedules.

**The model rule is unchanged** — `PROFILER-COVERAGE-PLAN.md` §2: Medium buys comparison of records the repo already holds; High buys reading depth; Opus 5 xhigh buys research and authoring; Fable 5.1 xhigh buys design and evaluation against a corpus. Every phase below names its model and effort for that reason, and the reason is written down.

**Two rules that shaped the order, both learned the hard way at X3:**

1. **Cheap-and-certain before slow-and-uncertain.** The first recommendation out of X3 named only a `sungrow` refresh, when five findings needed no research at all. The single-recommendation rule picked the highest-value item instead of the cheapest. Phase 1 (zero-research fixes) therefore precedes Phase 2a (research).
2. **The session that writes a criterion does not close against it.** X3 was marked Done against an exit criterion the same session wrote in the same commit. Phase 0 exists to reverse that, and every "done when" below is closed by the developer or by a later session, never by the one that authored it.

## 1 · The finding set this plan remediates

All of these were produced by X3 (v05.01r) reading the 21 over-cap cross-reference scopes and the 38 aged report pins. **None was fixed there, deliberately** — step 7 of the Profiler Command requires two figures that may describe the same thing to be *stated*, not silently reconciled, and X3 was a read-and-report item. Each finding is assigned to exactly one phase.

| # | Finding | Where it lives | Needs research? | Phase |
|---|---------|----------------|-----------------|-------|
| F1 | **X3's exit criterion overstated.** The "0 warnings" clause of X3's "done when" was not met and was permanently unreachable as written; the v05.01r session flipped the ledger to Done against a criterion it wrote itself | `PROFILER-COVERAGE-PLAN.md` §9.3–9.5, `scripts/check-profiler-reports.py` | No | **0** |
| F2 | **Missing `hithium` → `lightsource-bp` customer edge.** `lightsource-bp` carries the supplier edge and a 222 MW / 640 MWh, 128-container project; `hithium`'s own prospectus roster names Lightsource bp but its `relationships[]` does not | `hithium.profile.json` | No | **1** |
| F3 | **`google` behind `intersect-power` on the same acquisition** — ~$4.75B against the USD 5,868m final purchase-price allocation already read from Alphabet's Q2 10-Q into `intersect-power` (goodwill USD 2,174m, PP&E USD 5,129m) | `google.profile.json` | No — the figure is already in the corpus | **1** |
| F4 | **Stale figure in `compass-datacenters`** — the Brookfield × Bloom framework at USD 5bn, since expanded fivefold to USD 25bn (on record in `brookfield` and `bloom-energy`) | `compass-datacenters.profile.json` | No | **1** |
| F5 | **Internal inconsistency in `arevon`** — "five operating utility-scale California BESS by mid-2025" against its own list of three | `arevon.profile.json` | No | **1** |
| F6 | **Sungrow portfolio gap, huawei side.** `huawei-digital-power` attributes to Sungrow a ~CNY 100B market-value loss after the FCC Covered List action and a public argument that its products fall outside the rule's connectivity prong (wired links); `sungrow` v8 carries neither. The Phase 0 brief names `huawei-digital-power` among the five Phase 1 dossiers; the Phase 1 brief scopes the edit, and whatever needs Sungrow's own record moves to F7 | `huawei-digital-power.profile.json` | No for the attribution wording; yes for anything that asserts Sungrow's record | **1** (residue → 2a) |
| F7 | **Sungrow portfolio gap, subject side.** `hithium` and `huawei-digital-power` credit Sungrow with an SST product, "the most architecturally complete grid-to-chip offer (PCS + storage + SST)", the CNY 100B loss and the FCC wired-prong argument; `sungrow` v8 contains none of `SST`, "solid-state transformer", "grid-to-chip", "wired" or that figure | `sungrow.profile.json` | **Yes** | **2a** |
| F8 | **`catl` omits its own RMB 4.1B purchase of 49% of Zhonhen's controlling holdco**, which `zhonhen` headlines and the AIDC report makes a key judgment | `catl.profile.json` | **Yes** | **2a** |
| F9 | **`tract` ↔ `prime-data-centers` figure disagreement.** `tract` asserts a "USD 4 billion" Prime Lockhart campus four times; Prime's own dossier gives USD 400m for Austin phase 1 (down from USD 1.3bn in 2024). The USD 4bn may be a total-campus figure against a phase-1 figure — deleting it would be the exact failure step 7 prevents, so this is a **citation read**, not a rewrite | `tract.profile.json` | Source reading, not new research | **2b** |
| F10 | **Three report pins record that the corpus moved under a report.** `vantage` v9 puts battery storage on the record at Lighthouse and `stack-infrastructure` v7 at Project Jupiter — two of the three campuses the opportunity report calls storage whitespace; `meta` v9 adds the Enbridge Cowboy 200 MW / 1,600 MWh tolled-BESS lane the report predates | `named-project-bess-attach--opportunity--2026-08-30` | No — reports are immutable; this is a regeneration question | **5 → 6** |
| F11 | **The four reports are under-scoped, not wrong.** Built at ~85–89 covered companies against today's 154: `aidc-power-conversion` covers 8 where ~15 covered companies now map to NVIDIA's 800 VDC ecosystem; `named-project` was built on 8 projects and there are now 9 (`river-bend-campus` missing) | all four reports | No | **5 → 6** |
| F12 | **`CLASSROOM-CURRICULUM-PLAN.md` §1 is badly stale.** Verified 2026-09-02 against 89 dossiers / 62 guides / 44 concepts / 490 edges / 7 modules / 8 projects; today 154 / 154 / 1,210 / 1,257 / 8 / 9. At least three of §2.2's exclusions are now contradicted by the §6 closures (nuclear/SMR — `x-energy` and `oklo` have dossier and guide; cooling — `coolit` exists; `utility-procurement-meets-ai-load` is guidance-gated although six `utility` dossiers now exist) | `CLASSROOM-CURRICULUM-PLAN.md` | No | **3** |

**Recorded, not scheduled** (see §5): the `microsoft` omission of the 1.35 GW Monarch LOI (defensible — never confirmed, both counterparties mark it press-sourced) and the `check-profiler-crossrefs.py` detector recommendation (an `A-on-B versus B-on-B` figure comparison behind a flag; `--no-cap` as an audit switch; leave `OQ_SCOPE_MAX` at 900).

## 2 · The nine phases

| Phase | Model · effort | Sessions | What | Done when |
|-------|----------------|----------|------|-----------|
| **0** | **Fable 5.1 High** | 1 | **Record and criterion.** Split §9.4's X3 row into X3a (done) / X3b (open); amend §9.3's defective "0 warnings" clause visibly; seed `report-pins-verified.json` with the 38 pins X3a read; change `check-profiler-reports.py` so the warning count is actionable (skip superseded reports for drift; consult the verified list; re-warn when a dossier moves past its verification); persist this plan | The ledger no longer claims X3 is finished; the checker reads 0 errors / 0 warnings with every aged pin explained line-by-line; this file exists. **Corrects a record that overstated progress and unblocks nothing else — done first because every later phase reads this record** |
| **1** | **Fable 5.1 Medium** | 1 | **Five zero-research dossier fixes** — F2 `hithium`, F3 `google`, F4 `compass-datacenters`, F5 `arevon`, F6 `huawei-digital-power` (the attribution side only). Every correcting figure is already in the corpus, so the work is comparison of records the repo holds — §2's definition of Medium | The five dossiers revised and archived per the Profiler Command; all six checkers exit 0; `check-profiler-reports.py` shows exactly the `hithium` pins re-warning (three current reports) and nothing else new |
| **2a** | **Opus 5 xhigh** | 1–2 | **`sungrow` and `catl` research refreshes** — F7 (SST / grid-to-chip / CNY 100B / FCC wired-prong, sourced from Sungrow's own record or stated as unconfirmed) and F8 (the RMB 4.1B / 49% Zhonhen holdco purchase). Research, so Opus | Both dossiers revised with first-party or disclosure sources for every new claim; the `hithium`/`huawei` attributions either corroborated in `sungrow` or explicitly marked one-sided; checkers exit 0 |
| **2b** | **Fable 5.1 High** | 1 | **`tract` citation read** — F9. Read the sources behind tract's USD 4bn and Prime's USD 400m; if both are real figures for different scopes, *state both with their scopes*; if one is wrong, correct that one. Reading depth, not research — High | The two dossiers agree or disagree on the record with the reason written in the dossier text; `check-profiler-crossrefs.py --pair tract prime-data-centers` still 0 candidates |
| **3** | **Fable 5.1 xhigh** | 1 | **Curriculum re-plan and skeleton review** — F12. Re-verify `CLASSROOM-CURRICULUM-PLAN.md` §1 against the 154-company corpus; re-test every §2.2 exclusion against the §6 closures; review the skeleton in §4 below and revise the second-wave order. Design against a grown corpus, so Fable at xhigh | §1 re-dated; each exclusion kept or reversed with a written reason; the 20-lesson remaining list re-ordered and recorded in the curriculum plan's §7 |
| **4** | **Opus 5 xhigh** | 20 (one lesson per session) | **Author the 20 remaining lessons** in the order Phase 3 sets, under the standing one-lesson-per-session decision and the unattended-committer contract (`CLASSROOM-COMMITTER-CONTRACT.md`) | 30 of 30 lessons built; `check-classroom-content.py` and `check-classroom-pipeline.py` clean after each |
| **5** | **Fable 5.1 xhigh** | 1 | **Report-strategy evaluation** — F10, F11. Decide per report: regenerate as-is, re-scope, or retire; decide whether the drift-gated monthly Routine's gate is right for a 154-company corpus. Evaluation, so Fable at xhigh. **Runs before Phase 6 so the reports are not authored twice** | A written decision per report with scope and the reason; the interim caveat in §5 lifted or restated |
| **6** | **Opus 5 xhigh** | 1–2 | **Regenerate the reports** Phase 5 chose, as new files carrying `supersedes` (the old files stay — the library is its own archive). Report-Command authoring, so Opus | New reports pass `check-profiler-reports.py` with fresh pins; superseded editions marked in the index; the old editions' pins no longer counted |
| **G6** | Developer-blocked; any model once the input exists | in parallel | The one open row of `CLASSROOM-CURRICULUM-PLAN.md` §6: a large-load-interconnection guidance module, which needs a developer-supplied industry document run through the `industry guidance:` command. Unlocks an *additional* contributor-gated lesson behind `the-fence-line`; blocks none of the 20 | The module exists and §6's G6 row closes |

**~31 sessions in total, 20 of them lesson authoring.** Phases 0, 1, 2b, 3 and 5 are one session each; 2a and 6 are one to two; Phase 4 is twenty; G6 is outside the count.

## 3 · Per-phase detail and the reason for each model

**Phase 0 — Fable 5.1 High.** The work is reading a checker and a schema carefully enough to state *why* a clause is unreachable, then changing the checker without breaking its other jobs. That is reading depth (High), not research (Opus) and not design (xhigh). Done at v05.02r; the evidence is in `PROFILER-COVERAGE-PLAN.md` §9.3 "Phase 0 evaluation" and §9.4.

**Phase 1 — Fable 5.1 Medium.** Every correcting fact is already in another dossier: Intersect's 10-Q allocation, Brookfield/Bloom's USD 25bn, Arevon's own project list, Lightsource's supplier edge. §2 of the coverage plan defines Medium as comparison of records the repo already holds, and this is that. It goes before 2a because it is cheap and certain; the X3 session's first recommendation got this order wrong. **Side-effect to expect and leave alone:** revising `hithium` moves it past v11, so its three verified pins (`grid-scale-bess`, `named-project-bess-attach`, `s154-listed-bess-suppliers`) re-warn. That is the Phase 0 mechanism working; the pins are read again at Phase 6 (or by whichever session next reads them), not silenced.

**Phase 2a — Opus 5 xhigh.** Two dossiers need facts the corpus does not hold: Sungrow's own SST / grid-to-chip product record and its FCC-rule position, and CATL's Zhonhen holdco purchase from CATL's side. New sourcing is research, and the model rule puts research on Opus. It follows Phase 1 so the cheap fixes are not held hostage to the slow ones.

**Phase 2b — Fable 5.1 High.** The tract/Prime disagreement is the one finding where the wrong fix is worse than no fix: a USD 4bn campus total and a USD 400m phase-1 figure can both be true. The task is to read the cited sources and state the scopes, which is reading depth. It is split from 2a because it needs no new sources and a different model.

**Phase 3 — Fable 5.1 xhigh.** The curriculum was cut on an 89-dossier corpus and the corpus is now 154 with three of the plan's own exclusions contradicted. Re-planning a curriculum against a corpus is design and evaluation, which is what xhigh on Fable buys. Phase 3 edits against §4 below; without this file it had no skeleton to edit.

**Phase 4 — Opus 5 xhigh, one lesson per session.** The standing decision from the Classroom build: lesson authoring is corpus synthesis with provenance stamps and a contract to satisfy, and it has never been done more than one per session without a pipeline. Twenty sessions.

**Phase 5 — Fable 5.1 xhigh.** Whether to regenerate, re-scope or retire a report is a strategy question, and the reports' defect is scope rather than fact (F11). Evaluating four reports against a corpus that nearly doubled is design work. It runs before Phase 6 so the four reports are authored once, not twice — the X3 session's explicit reason for *not* regenerating at v05.01r.

**Phase 6 — Opus 5 xhigh.** Report-Command authoring: synthesis from dossiers with verbatim citations and a coverage block, on the writing style in `PROFILER-STYLES.md`. Opus per the model rule. The superseded editions stay on disk and, since Phase 0, are no longer drift-checked, so the checker's count after Phase 6 reflects only the new editions.

**G6 — blocked on the developer.** Eight guidance analyses exist and none is a large-load-interconnection subject; the `industry guidance:` command needs a document to ingest. No model choice is pending.

## 4 · The curriculum skeleton as it stands (Phase 3 edits against this)

Recorded at the X3 close (2026-09-07, v05.01r) from `CLASSROOM-CURRICULUM-PLAN.md` §2.2, §3 and §7 and the live `Classroom.gs` content (`check-classroom-content.py`: 10 lessons, 3 tracks, 134 gate cases). **This is the input Phase 3 reviews; Phase 3 writes its revisions into the curriculum plan itself, not here.**

**3 lanes · 5 tracks · 30 lessons · 10 built · 20 remaining.**

| Track | Lane | Lessons | Built | Remaining | Notes |
|-------|------|---------|-------|-----------|-------|
| `bess-foundations` | Technology Foundations | 6 | 4 | 2 | Existing track, extended; 5 public · 1 guidance |
| `electrical-foundations` | Technology Foundations | 5 | **0** | 5 | New track; all public |
| `aidc-grid-to-chip` | The AI Data-Center Wave | 8 | 4 | 4 | Replaces `aidc-power-primer`; 7 public · 1 guidance |
| `aidc-campus` | The AI Data-Center Wave | 4 | 2 | 2 | New track; 3 public · 1 guidance |
| `market-access` | Market Access & Bankability | 7 | **0** | 7 | New track; 3 public · 4 guidance |

- **The "first five" cut line (`CLASSROOM-CURRICULUM-PLAN.md` §7) is complete**: `the-fence-line`, `bridge-power`, `the-800-vdc-shift`, `the-control-stack`, `where-bess-plugs-in`. The second wave is next.
- **The corpus now stamps lessons it could not stamp at planning time.** `PROFILER-COVERAGE-PLAN.md` §6 step 2 lists the closures' unlocks: `backup-generation`, `the-ups-room`, `how-a-utility-buys` (the first public utility lesson), the battery side of `inside-the-rack` / `the-800-vdc-shift`, `the-transformer-and-the-substation`, and the rest of `electrical-foundations`.
- **Three of §2.2's exclusions are contradicted by the §6 closures** (F12): nuclear/SMR was excluded pending a vendor dossier and `x-energy` + `oklo` now carry dossier and guide; cooling was capped partly for want of a CDU specialist and `coolit` exists; `utility-procurement-meets-ai-load` is specified guidance-gated because utility machinery was guidance-only, and six `utility` dossiers now exist, so it could be public.
- **G6 staying open blocks none of the 20** — it unlocks one *additional* contributor-gated lesson behind `the-fence-line`.
- **Track edits are a developer-session write** (curriculum plan §8 item 9): creating the three new tracks, retiring `aidc-power-primer` and re-sequencing `clLessons_()` happen inside the content fence in one commit with `gateDigest` unchanged.

## 5 · Dependencies, standing caveats, and what is recorded but not scheduled

**Dependencies.**
- Phase 0 first; nothing depends on it, but every later phase reads the record it corrects.
- Phase 1 before 2a (cheap-and-certain first). 2a and 2b are independent of each other and of Phase 3.
- Phase 3 before Phase 4; Phase 4's twenty sessions can interleave with 2a/2b/5/6 — they touch different files.
- Phase 5 before Phase 6, always. Regenerating before the evaluation authors the reports twice.
- G6 runs whenever the developer supplies the document; it never blocks a phase.

**Standing caveats while the plan runs.**
- **~~Do not act on the `named-project` report's whitespace call as a *storage* whitespace call.~~ LIFTED at Phase 6 session 1 (v05.13r, 2026-09-08)** — `named-project-bess-attach--opportunity--2026-09-08` supersedes the 2026-08-30 edition and states the call in its own words: the whitespace is **supplier** whitespace, storage is in the plan at four of the nine projects with no supplier named at any of them, and the party to court is the storage-scope holder rather than the hyperscaler. The record the caveat carried, kept for provenance: Storage is in the plan at Lighthouse (`vantage` v9: 70% of power from new solar, wind and battery storage built for the project) and at Project Jupiter (`stack-infrastructure` v7: a microgrid with battery energy storage), and Meta's BESS exposure runs through a utility-tolled lane (`meta` v9: Enbridge Cowboy, 200 MW / 1,600 MWh); no BESS supplier is named at any of the nine registered projects except Trimount (Hithium) and Colossus (Tesla). The whitespace that survives is **supplier** whitespace, and the party to court at Lighthouse and Jupiter is whoever holds the storage scope in those plans, not the hyperscaler. That edition has now landed, so this caveat no longer constrains any phase; the 2026-09-08 edition is the statement of record.
- **Reports are immutable.** No phase edits a published report; the only route to a fresh pin is a new edition carrying `supersedes`. A pin that re-warns after a dossier bump is read again and recorded in `report-pins-verified.json`, never re-pinned.
- **X3b closed at v05.03r** — the developer accepted the amended criterion in `PROFILER-COVERAGE-PLAN.md` §9.3 on 2026-09-07, and Phase X is closed. The pins that re-warn after Phase 1 are read again and recorded, never re-pinned.
- **CHANGELOG rotation** is decided by the non-exempt count on the day a push lands (`CHANGELOG-archive.md` step 1, fixed at v05.01r). When it fires, `git fetch --unshallow` runs before any SHA lookup.

**Recorded, not scheduled.**
- `microsoft` v4 omits the 1.35 GW Monarch LOI that `anthropic` and `nscale` attribute to it. Defensible: never confirmed, both counterparties mark it press-sourced. Revisit only if Microsoft confirms.
- The `check-profiler-crossrefs.py` detector recommendation from X3 (§9.3 of the coverage plan): an `A-on-B versus B-on-B` figure comparison behind a flag with calibration first; `--no-cap` as an audit switch; `OQ_SCOPE_MAX` stays at 900. Needs its own calibration session; not on this plan's critical path.
- Standing, unassigned housekeeping carried in `SESSION-CONTEXT.md`: the README archive listing lags; `archive/nvidia.profile.v2.json` is missing and unreconstructable; OSHA IMIS and SEC EDGAR are network-blocked from the remote environment.

## 6 · Status ledger

| Phase | Model · effort | Status |
|-------|----------------|--------|
| 0 · record and criterion | Fable 5.1 High | **Done — v05.02r (2026-09-07).** §9.4 split into X3a Done / X3b Open; §9.3 criterion amended with the defect noted; `report-pins-verified.json` seeded with 38 pins after re-confirming the triples against a live checker run; `check-profiler-reports.py` skips superseded reports for drift and consults the verified list — 0 errors / 0 warnings / 38 quiet at v05.02r; this file persisted. **X3b accepted by the developer and closed at v05.03r** |
| 1 · five zero-research dossier fixes | Fable 5.1 Medium | **Done — v05.04r (2026-09-07).** `hithium` v12 (F2: `lightsource-bp` customer edge), `google` v9 (F3: USD 5,868m 10-Q allocation, announced ~$4.75B kept as the announcement), `compass-datacenters` v5 (F4: Bloom framework USD 5bn → 25bn), `arevon` v4 (F5: both BESS counts stated with their definitions), `huawei-digital-power` v7 (F6: Sungrow attributions marked one-sided and traced to the registered tier; the CNY 100B and wired-links claims carried unconfirmed to 2a); all five archived. Six checkers exit 0; `check-profiler-reports.py` 0 errors / exactly the three `hithium` pins re-warning, left loud; two new crossref candidates (lightsource-bp × hithium, both Lightsource-side open questions Hithium's record cannot answer) adjudicated and accepted with reasons |
| 2a · `sungrow` + `catl` research | Opus 5 xhigh | **Done — v05.05r (2026-09-07).** `sungrow` v9 (F7): the SST is real and first-party (EnerNeo, launched 9 July 2026, described in the H1 2026 SZSE interim report as supplying the market — 10–13.8 kV MVAC to 800 V DC, 1.5–4.5 MW, 98.5%, 312 kW/m²), "grid-to-chip" is Sungrow's own English-market framing from 19 January 2026, and the two remaining attributions are **refuted**: the ~CNY 100B loss belongs to the 30 June–late July window that preceded the FCC action (aggregator-sourced; the month after 28 July was about RMB 29.8B) and the wired-links argument was never Sungrow's — Sungrow declined it when a retail investor proposed it and stated the opposite on 4 September. New `policyExposure[]` (FCC, EO 14420, EU), H1 2026 financials, the AIDC product line and EnerNeo specs. `catl` v7 (F8): the RMB 4,099,858,823.53 / 49.00% Zhonhen holdco **capital increase** added with the CATL-side null documented — CATL has published nothing naming Zhonhen anywhere. Step 7: `huawei-digital-power` v8 and `hithium` v13 corrected, `zhonhen` v7's unsourced "~43.5% premium" replaced with what the filings say |
| 2b · `tract` citation read | Fable 5.1 High | **Done — v05.06r (2026-09-07).** Both figures are real and describe different scopes: the USD 4bn is the Austin Business Journal's rounding (8 May 2025, reprinted by Muskin Elam — the page is dated, not undated) of the county's **USD 4.2 billion 'at full buildout'** for nine buildings in three phases (Caldwell County abatement announcement, March 2024 — already cited by `prime-data-centers` as Hays Caldwell EDP / Post Register but never stated in its prose), while USD 400 million is the two-shell phase 1 of Texas filing TABS2026019473 (May 2026) that replaced the USD 1.3 billion three-building phase 1 of 2024. Neither figure was deleted; both dossiers now state both with their scopes and say no source restates a total for the current eight-building / 384 MW plan. `tract` v3 (v2 archived; Muskin Elam dated 2025-05-08 and re-sorted; TDLR, Post Register, Hays Caldwell EDP and Baxtel Prime sources added), `prime-data-centers` v3 (v2 archived; USD 4.2bn stated in the campus record, the `tract` edge and the capex-claims row; Muskin Elam source added). `check-profiler-crossrefs.py --pair tract prime-data-centers` 0 candidates |
| 3 · curriculum re-plan + skeleton review | Fable 5.1 xhigh | **Done — v05.07r (2026-09-07).** `CLASSROOM-CURRICULUM-PLAN.md` §1 re-verified from the filesystem and re-dated (154 dossiers / 154 guides / 1,210 concepts / 1,260 edges / 8 modules / 9 projects / 4 reports; Classroom 10 lessons / 3 tracks / 134 gate cases; 26 of the 56 built-lesson pins have moved). All eight §2.2 exclusions re-tested with a written reason each: **nuclear reversed** as a public `clean-firm-power` in `aidc-campus`; **utility reversed as a split** — a public `how-a-utility-buys` carries the machinery the six utility guides now cover section by section, `utility-procurement-meets-ai-load` stays guidance-gated as the seller's playbook; **cooling's one-lesson cap kept** with `the-cooling-plant-and-water` deepened on `coolit` / `trane-technologies`; solar PV, hydrogen, wind, EV charging, roadmap chemistries, flywheels/syncons and HVDC kept, three with reasons rewritten where the corpus moved. The §4 skeleton's four unlock ids that the thirty never contained (`backup-generation`, `the-ups-room`, `how-a-utility-buys`, `clean-firm-power`) reconciled — all four admitted (curriculum plan §2.3), so the curriculum is **34 lessons / 10 built / 24 remaining**, every track still under ten lessons. The remaining list re-ranked in Phase 4 order in the curriculum plan's §7 with stamps and a reason per row (`four-machines` first — its session creates `electrical-foundations`; `how-a-utility-buys` second — creates `market-access`; the four guidance lessons last, rows 23–24 gated on the bankability module's review); §4 and §8 updated; the dated revision log is the curriculum plan's §9. No lesson authored, `Classroom.gs` untouched, no dossier or report touched; checkers identical before and after (content 10 / 3 / 134, 0 / 0; pipeline "nothing changed"; reports 0 errors / 7 warnings). §2 and §4 of this file are kept as the v05.01r record |
| **S0** · segment-layer design gate | **Fable 5.1 xhigh** | **Done — v05.09r (2026-09-07).** `live-site-pages/profiler-data/profiler-segments.json` written: nineteen segments in chain order with definition, buying criteria and **283 memberships across all 154 dossiers**, every one verified against the dossier's own `ecosystemRole` / `productsAndServices` with the supporting line recorded as `basis` (none unassigned). Sixteen segments at the floor; **three below it — assurance and insurance as §7.2 predicted, and `compute-and-the-rack`, which §7.2 counted above the floor and the dossiers do not support** (NVIDIA plus Flex as a system builder; no AMD, no rack/server OEM covered) — recorded in the registry's `notes` for the developer's taxonomy review; `software-and-optimization` is not at zero (FlexGen incumbent, Fluence's Mosaic/Nispera challenger) but its landscape waits for S3 batch 1. Schema in `PROFILER-SCHEMA.md` ("Segments registry" + the `companies[].segments[]` mirror, sync-script change left to S1); Profiler Command step 5 gains the segment assignment; content contract amended in `CLASSROOM-SCHEMA.md` and `classroom-app.md`; curriculum plan **§10** (five layers, floor rule, segment-lesson template, generator spec, three Value Chain tracks under a fourth lane, landscape spec, K1/K2 specs, health-script spec, two role paths), rows 25–26 (`reading-the-numbers`, `interconnection-for-large-loads`), §4 re-numbered, G6 row corrected, §9 row; coverage plan **Phase E** (§10) and seventeen §8 rows with model, group, segment and the §7.2 hypotheses verbatim. No code, no lesson, no dossier; checkers identical before and after. **Taxonomy reviewed by the developer 2026-09-07 PM (v05.11r):** `compute-and-the-rack` is filled, not folded — AMD (challenger) and Supermicro (rack/server OEM) added as Phase E group E7 (coverage plan §8/§10); the fold was rejected because both current members already sit in `power-conversion-and-rack-power-silicon`, so it would only have deleted the load's own buying criteria. S0's "done when" is closed |
| **K1** · study-pool fix | **Fable 5.1 High** | **Done — v05.10r (2026-09-07).** `clDrillStudyItems_()` reads the top-level `flashcards[]` and every `flashcards`-kind section under the new `ss:<slug>:<sectionId>:<n>` item kind; `CL_DRILL_INV_CAP` 1200 → 2400; the cached pool is the compact `{ id: hash }` map (the full shape measured ~146 KB, past the `CacheService` cap §10.7 assumed it would clear); `Classroom.html` resolves `ss:` from the fetched guide and names the section on the source line; the schema's identity table gains the `ss` row. No guide touched, no `sf:` id or `study:` pin moved. Verified: the real builder under Node shims → **1,920 items (766 `sf:` + 1,154 `ss:`)**; both checkers clean, no P3; Playwright read of the landing card at 1,920 and an `ss:` card rendering with its answer. Classroom GAS v01.17g · page v01.08w |
| **S1** · generator, Value Chain tracks, health checker, `reading-the-graph` | **Fable 5.1 High** (code) · **Opus 5 xhigh** (lesson) | **Code half Done — v05.17r (2026-09-08, Fable 5.1 High).** `scripts/build-classroom-segments.py` generated all nineteen `segment-*` lessons into `Classroom.gs` (deterministic — a forced `--all` rerun rewrote nothing; `--check` at 0 due); the three Value Chain tracks registered under the fourth lane `The Value Chain` (3 · 7 · 9 lessons, appended at the end of `clTracks_()` — `market-access` does not exist yet, so the plan's "after `market-access`" position is recorded, not applied); `scripts/check-classroom-curriculum.py` reports (lanes, floor, stale pins, drill pool, review dates; `--strict` clean); the content checker gained the segment assertion and the lane; `sync-profiler-registry.py` now mirrors `segments[]` into the roster registry (154/154). Content checker 0 / 0; pipeline checker P1 (developer files) + P10 (nineteen modules, the expected developer-run cap breach) only — **no P3, no P5**; `--selftest` 13 / 0. Classroom GAS v01.18g. **Lesson half Done — v05.18r (2026-09-08, Opus 5 xhigh).** `reading-the-graph` authored inside the content fence — a public `module` in the lane `The Value Chain`, nine fixed sections (`what-an-edge-is` · `seven-types-two-directions` · `reading-the-chips` · `one-chain-three-typings` · `the-explorer` · `segments-and-roles` · `where-it-fails` · `drill` · `check-yourself`), a ten-entry lesson glossary for the graph vocabulary the concepts registry does not carry, and a six-input all-public stamp (`graph:profiler-graph`@2026-09-08 · `concepts:profiler-concepts`@2026-09-07 · `profile:fluidstack` / `profile:hut-8` / `profile:terawulf`@2026-09-06 · `profile:google`@2026-09-07, every date read off the fetched file and every slug identity-matched). The worked example is the credit-substitution chain — one tenant of record, two landlords, one backstop — which the graph types **supplier**, **customer** and **competitor** at different ends, and whose backstop Google types **partner** where TeraWulf types **investor**; `where-it-fails` is built on the same records (five undated Hut 8 → TeraWulf cross-mentions that are all benchmark sentences; the partner/investor split; two `customer` typings at one campus — tenant of record against end user; `announced` against `active` and an edge whose substance moved under an unchanged type). Inserted at **position 1** of `value-chain-makers` (4 lessons) and appended to the end of `clLessons_()`. Classroom GAS v01.19g; page unchanged at v01.08w. Content checker **30 lessons / 6 tracks / 134 gate cases, 0 / 0**; curriculum checker `--strict` clean with **The Value Chain at 20 lessons, 20/20 analyst-visible**; pipeline checker P1 (developer files) + **P5 on the `value-chain-makers` insert — expected and accepted for a developer session, since the track is a position-ordered reading list and no account holds progress on it** — and **no P3**, so `gateDigest` was left untouched; `--selftest` 13 / 0. **S1 is closed.** |
| **S3** · coverage fill, nineteen dossiers + guides (E7 added 2026-09-07 PM) | **Opus 5 xhigh** (public deep records) · **Fable 5.1 High** (private/opaque), three per session | **Done — v05.41r (2026-09-13). Ten of ten sessions landed** (P1 v05.20r, V1 v05.21r, P2 v05.22r, P3 v05.26r, P4 v05.27r, P5 v05.28r, P6 v05.29r, V2 v05.39r, V3 v05.40r, V4 v05.41r — nineteen dossiers and guides, every §8 Phase E row of `PROFILER-COVERAGE-PLAN.md` carrying a version in both columns). Done-when met and verified by the V4 session, not by the author of the criterion: every segment at the floor (`check-classroom-curriculum.py --strict` no structural findings); registry synced (`sync-profiler-registry.py --check` 0 of 174 out of sync); graph rebuilt (1,429 edges); and **the §6 register in `CLASSROOM-CURRICULUM-PLAN.md` re-run in full and dated 2026-09-13 with G10's residual recorded closed** — the condition §7.3 names. S2 may proceed on any landscape. *Trail as it stood at v05.22r:* In progress — **3 of 10 sessions** (P1 `E7` AMD · Supermicro landed 2026-09-08 at v05.20r, clearing `compute-and-the-rack` to 4 members / 1 incumbent / 2 challengers; V1 `E2b+E3b` kWh Analytics · CSA Group landed 2026-09-08 at v05.21r, clearing `assurance` to 5 / 2 / 1 / 2; **P2 `E2a` Marsh McLennan · Aon landed 2026-09-09 at v05.22r, clearing `insurance-and-risk-transfer` to 3 / 2 / 1 — so THREE OF THE FOUR BLOCKED LANDSCAPES ARE NOW WRITABLE and only `software-and-optimization` (P3 + V2) is held**). The action plan was split by model on 2026-09-08 (v05.19r) in `PROFILER-COVERAGE-PLAN.md` §10.1–10.4: six public sessions on **Opus 5 xhigh** (P1 `E7` AMD · Supermicro → P2 `E2a` Marsh · Aon → P3 `E1a` Stem → P4 `E3a` UL Solutions · Intertek → P5 `E4a` Albemarle · Novonix → P6 `E6a` CALB · Great Power, 11 companies) and four private on **Fable 5.1 High** (V1 `E2b+E3b` kWh Analytics · CSA Group → V2 `E1b` Habitat · Gridmatic → V3 `E5` Grid United · Pattern Energy → V4 `E4b+E6b` Mitra Chem · Cornex, 8 companies); ledger rows in §8 now carry the model-half id. **The floor arithmetic reorders the groups (§10.3):** `compute-and-the-rack` is cleared by P1 alone; `insurance-and-risk-transfer` needs P2 **and** V1; `assurance` is blocked on a challenger that only **V1** carries, so P4 deepens it without unblocking it; `software-and-optimization` needs P3 and V2. **§10.4 was reordered on 2026-09-09 (v05.25r): the lanes no longer interleave — the four remaining Opus sessions run first (P3 → P4 → P5 → P6), then the three Fable ones (V2 → V3 → V4).** The trigger was budget — the weekly Fable allowance was 96% consumed against 13% of all-models — and the dependency check cleared it: **V2 is the only remaining session that gates anything**, because the software landscape is held for both halves of E1. The cost is that the fourth landscape stays held three sessions longer; S2 is not stalled, since compute, assurance and insurance are writable today. P1, V1 and P2 have run. **Of the remainder, if only one more S3 session runs next it is P3 (`E1a`, Stem) followed by V2 (`E1b`, Habitat Energy · Gridmatic)** — together they complete the last blocked landscape, `software-and-optimization`, after which S2 may start on any of the nineteen segments. P2 also produced two corrections the plan should carry forward: the E2a **subject question's premise had expired** before the session asked it (the January 2026 rebrand collapsed 'the broker Marsh' and 'the listed parent Marsh McLennan' into one entity, and NYSE: MMC became MRSH on 2026-01-14), and **Aon agreed on 2026-08-30 to acquire USI for USD 17.0bn** — a fact nine days old at authoring. Both are the same failure mode V1 recorded for kWh Analytics and CSA Group: **the ownership and identity premises in §8 age faster than the coverage plan does, and every remaining row's identity fields should be re-verified at authoring rather than trusted.** S0 done, so each session assigns its segments; interleaves with S2 |
| **C3** · guidance homecoming | **Opus 5 xhigh** | **Done — v05.48r (2026-09-13), three sessions** (v05.44r, v05.45r, v05.48r; fixes at v05.46r/v05.47r). Session 1: the nine modules and the read ops in `Classroom.gs` below the `// CONTENT END` fence, plus the guidance surface in `Classroom.html`. Session 2: the Admin lens re-hosted under `clCan('reports')` off public Pages data, and `guidanceMentions_()` moved to Classroom behind a token-gated peer route that `Profiler.gs` proxies. Session 3: the progress ticks migrated in one shot and **verified against a pre-migration export** (`scripts/check-guidance-migration.js`, 44 assertions, idempotent on a second run), guidance ticks and the C4 drill wired (**decision 6 at its target state** — 139 guidance items in the pool), Profiler's masthead slot and dossier chips re-pointed to `Classroom.html#guidance/…` with **no access-matrix row moved**, Profiler's twelve functions and dead page engine deleted with `check-guidance-parity.py`, `.claude/rules/industry-guidance.md` re-targeted, and the quarterly Routine's prompt updated **in place with the developer's in-session approval**. `GUIDANCE_PEER_TOKEN` is set in both projects and the chips were confirmed live at v05.47r — **no operator step outstanding** |
| **Q** · quarterly guidance review — `bess-bankability-2026-08` | **Opus 5 xhigh** | **First cycle done — v05.50r (2026-09-13), one session, order 3 of the §7.3 run table.** Eleven section ids, the module id and the three `counterparty` report anchors all unchanged, as §7.12 required. `updated` → 2026-09-13; `reviewBy` → **2027-01-01**, set from the module's own next dated gate (UL 9540A Ed. 6 effective, and the non-BES IBR date under PRC-029) rather than a cadence — `check-classroom-curriculum.py --strict` now reports **0 items due** inside the 30-day horizon. Two of the five moved claims were **corrections**: the FCC's inverter Covered List entry reads on **place of production, not nationality**, so the module's 'pair with a non-Chinese PCS' play was wrong; and **AB 303 died 2026-01-31**, not in committee in April 2025. Two were **staleness**: UL 9540A Ed. 6 (effective 2027-01-01, unit-level dropped for non-residential BESS without an active TRPPS, pass/fail on the installation test) and ANSI/CSA **C800:25 §9.7** superseding TS-800:24 while NFPA 855-2026 as issued names no method. One was a **tightening**: BNEF's 2026-08-15 two-commercial-bank criterion. PRC-029-1's 2026-10-01 date **confirmed** against NERC's own Aug 31–Sep 7, 2026 bulletin, with PRC-029-2 and the phased dates added. Rendered at admin in Playwright with **zero page errors**, both Admin-lens panels above the Mark-as-understood button. **The review also found and fixed a C3 leftover the brief's own step 4 exposed:** `check-profiler-reports.py` parsed guidance modules from `Profiler.gs`, which C3 emptied, so it returned `[]` and errored on every overlay anchor in every report — pre-existing and identical on a pristine `HEAD`; repointed at `Classroom.gs` and back to 0/0. No report is materially out of date; **no re-run recommended.** Next fire: the Routine's own cron, or `reviewBy` 2027-01-01, whichever comes first |
| **S2** · landscape modules, one per segment | **Opus 5 xhigh** | In progress — **17 of ~19. **Session 16 landed v06.36r (2026-09-17): `landscape-compute-and-the-rack-2026-09`**, the twenty-fifth guidance module and the sixteenth landscape. Corpus synthesis over **4** member dossiers — **1 · 2 · 1**, **exactly the floor and the smallest roster S2 has covered** — at the versions its **35-row** claims ledger pins. Ten findings, **(ii1)–(ii10)**. **THE COVERAGE BOUNDARY IS THE OPENING SECTION AND IT IS A MEASUREMENT, NOT A CAVEAT.** The only named independent series the segment has is IDC's worldwide server revenue share, and it names **FOUR parties of which the corpus covers ONE** — ODM Direct 50.2 %, Dell 16.5, Supermicro 7.6, HPE 3.0, so **7.6 of the 77.3 points named, 9.8 %** — with every figure read out of the one covered member's own file. **The gap is visible in the curriculum's own vocabulary**: `Dell` appears ten times and **nine are Dell'Oro**, the research firm, while `Quanta` appears **forty-two times and all forty-two are Quanta Services**, the covered EPC contractor — **(hh3)**'s collision class fired twice and pointing at the boundary rather than at a teaching hazard. **Five of six possible pairs carry an edge — the densest ratio S2 has measured — and exactly ONE of seven curated typings reads `competitor`**, so the segment is a supply chain with a single rivalry in it; the one missing pair is the two members that both build racks to other people's designs. **Every external membership points UPSTREAM — all six, and none in the ten segments downstream — so the segment is a terminus**, and **(aa2)** and **(ee3)** fire together for the second time and complete rather than merely dominant. `reviewBy` **2027-03-17**, the six-month default and only the **second** landscape of sixteen to take it: **nineteen `policyExposure` entries, ten dated, NOT ONE in the future**, and the one future day-level date inside six months anywhere in the segment lives **only in a `sources[]` label** and was rejected on provenance — seven rejections in writing. Scraper seed `topic-landscape-compute-and-the-rack` added, twelve terms, with a new gap class: **the roster is biased in the same direction as the market it observes** — the incumbent's roadmap is seeded exactly and the challenger's is entirely absent. `--check` **6 → 5**, called before the run; content checker **0/0 at 50 lessons**, module assertion **24 → 25**; render 9/9 at contributor with zero page errors and analyst denied at the server. **The screenshots caught SEVEN defects for a tenth consecutive session** **Session 17 landed v06.40r (2026-09-17): `landscape-software-and-optimization-2026-09`**, the **twenty-sixth** guidance module and the **seventeenth** landscape. Corpus synthesis over **20** member dossiers — **2 · 3 · 15** — at the versions its **37-row** claims ledger pins. Findings **(kk1)–(kk8)**. **THE MOST ADJACENT-HEAVY ROSTER IN THE TAXONOMY, MEASURED RATHER THAN INHERITED: 75.0 % adjacent**, the highest of all nineteen segments, against `clean-firm-and-nuclear`'s 68.8 % — and **only three segments carry no adjacent member at all**, not five as first drafted. **Sixteen of the twenty are typed on a `productsAndServices` line rather than a business**; three on `ecosystemRole`, one on the registry tagline — and of the three, Stem's own basis line types it an incumbent of a **different** market, so **exactly two of twenty are here because this is the whole company**: `habitat-energy` and `gridmatic`, both private, both challengers, one in a sale process. **(aa2) AND (ee3) FIRED TOTAL ON BOTH SIDES FOR THE FIRST TIME** — all **15 of 15** bench members are ranked incumbent or challenger elsewhere (eleven of them incumbent) and **not one is adjacent everywhere**, while **4 of the 5** external memberships held by this segment's own ranked players **demote** them; `habitat-energy` is the only member belonging to no other segment. **THE TERMINUS CLAIM WAS DECLINED ON ITS OWN ARITHMETIC** — all 38 memberships point upstream, but position **18 of 19** leaves exactly one segment downstream, so the result carries almost no information and the module says so. **THE BRIEF'S RANKING CLAIM WAS UNDERSTATED AND THE MEASUREMENT CAUGHT IT**: **four** named third parties publish **six** measurements near this segment and **none ranks the segment** — and **coverage and relevance run in opposite directions**, the ranking the corpus covers completely (Wood Mackenzie's AC-block integrator table, which this segment's own incumbent deliberately sits out) measuring the wrong market while the two that measure the right one are covered at **44 %** and **20 %**. **(dd2)'s cross-attestation instrument fired SEVEN FOR SEVEN**, its widest positive. `reviewBy` **2027-03-17**, the six-month default and only the **third** of seventeen landscapes to take it, with **four rejections in writing** including a **new rejection class — the filer's own cadence**. **(jj5) at its most extreme: the two scans' in-horizon results are DISJOINT.** `--check` **7 → 7 → 6**, derived from the parsed generator and **exact on both halves**; **(aa5) holds for a twelfth session**. The recount pass caught **five** defects the 0/0 simulations could not see and the screenshots then caught **four more**, a **twelfth** consecutive session. Scraper seed **13 terms, 33 of 35 candidates scoring zero against 506 held**, and a new open item **(xiv)**: the matcher's word-boundary guard makes **447 of 451** singular held terms blind to their own plural. The literal is **100 % ASCII**. Brief was §7.49; §7.50 written in the same commit. |
| **K2** · roster deck (opt-in) | **Opus 5 xhigh** | Open — S1's code half landed (v05.17r): the `the-players` tables carry a `Dossier` slug column, 283 rows across nineteen lessons, ready to enumerate |
| **C5** · sales simulations | **Fable 5.1 xhigh** (design) · **Opus 5 xhigh** (authoring) | Open — after ~6 landscape modules |
| **C6** · team layer | — | Deferred — gated on a team existing |
| 4 · author the remaining lessons | Opus 5 xhigh, one per session | Open — **21 of 26** (§2's twenty, the four admitted at Phase 3, `reading-the-numbers` as row 25 and the G6 lesson `interconnection-for-large-loads` as row 26 — both specified at S0, curriculum plan §3.3 / §3.5 / §7; `reading-the-graph` is authored inside S1) — rows 1–10 built at v05.53r, v05.54r, v05.55r, v05.57r, v05.61r, v05.67r, v05.72r, v05.76r, **v05.80r (2026-09-15, `the-campus-as-a-power-project` — 39 lessons, 8 tracks; P1 + P5, no P3, no P8; all **thirty** stamps pinned off documents fetched that run — the widest stamp in the programme, and one input wider than §7.24 forecast — the instructed `the-named-projects` rename taken, a six-by-nine table that is the widest any mechanism lesson carries by column count, `reviewBy` on the six-month default with three candidates rejected in writing, and `--check` 5 due → 8 due exactly as predicted)**; and **v05.85r (2026-09-15, `how-a-storage-project-happens` — 40 lessons, 8 tracks; P1 + P5, no P3, no P8; all **sixteen** stamps pinned off documents fetched that run, the roster re-derived rather than copied and agreeing with §7 row 10 at sixteen; `reviewBy` on the six-month default with four candidates rejected in writing; `--check` 7 due → 8 due exactly as predicted, with `epc-and-construction` additionally deepening to two differing sections; the track's `short` deliberately unchanged)**; and **v05.91r (2026-09-15, `contracts-and-revenue` — 41 lessons, 8 tracks; P1 + P5, no P3, no P8; all **fourteen** stamps pinned off documents fetched that run, the roster re-derived rather than copied and coming out at fourteen against §7 row 11's thirteen — the amendment *overlaps* the base list on `study:core-scientific`, which is the trap §7.28 named and the first row where a straight addition over-counts; `reviewBy` on the six-month default with five candidates rejected in writing; `--check` **7 due → 10 due** exactly as predicted **on both halves**, with `storage-developers-and-ipps` deepening to two differing sections — the first firing of the row-10 deepening case since it was discovered)**; and **v05.95r (2026-09-15, `where-batteries-stop` — 42 lessons, 8 tracks; **P1 alone** — no P5 because this row *appends* at `bess-foundations` position 6, no P3, no P7, and **no P8, which cannot fire on a head-only lesson**; all **ten** stamps pinned off documents fetched that run, the roster re-derived and **agreeing with §7 row 12 for the first time in four rows**; **an eighth section taken** because §3.1's v05.07r amendment names `iron-air-honestly` while its outline table does not — the first row on which an amendment moved the *section contract* rather than the input roster; `reviewBy` on the six-month default with five candidates rejected in writing, none of them a date; and `--check` **9 due → 10 due** on the membership half as predicted but `read-next` **alone** on the section half, falsifying the brief's forecast and adding a third gate to §10.6 (w) — a lesson absent from `CRITERION_LEXICON` can never reach `what-is-bought-and-on-what`)**; and **v05.99r (2026-09-15, `breakers-relays-and-faults` — 43 lessons; the append signature broken by a **P7** on a segment regenerated twice in one EST day, and the first deepening-at-birth)**; and **v06.05r (2026-09-16, `grid-stability-and-the-generator` — 44 lessons, 8 tracks, **the lesson that closes the floor track**; **P1 alone** — no P5 on an append, no P3, no P8, and **no P7 because the EST day had rolled**; all **eight** stamps pinned off documents fetched that run; **the first Phase 4 row to regenerate TWO segments**, `--check` **10 → 12 → 10** with both halves called correctly; and **the first row to resolve a contradiction between two parts of the plan** — §3.2's outline put two `prose` sections back to back, which §8 item 6 forbids, so a `table` was inserted between them and the section count moved from seven to eight)**; **next: S2 session 10** — with Phase 4 now at **14** and S2 at **9**, the §7.4 item 6 interleave stands at **14:9** and hands the turn to S2 session 10 (`clean-firm-and-nuclear`); that is what §7.35 briefs. Each session takes the lowest unbuilt row and flips it there. *(This cell read "0 of 26, next row 1" from v05.53r to v05.60r — the sentence under this table asks a session to flip a row here **and** in the record that owns the work, and rows 1–4 flipped only the curriculum plan's. Corrected at v05.61r.)* Rows 23–24 wait for the bankability module's quarterly review **Row 15 done — v06.09r (2026-09-16, `string-versus-central`, *One Big Box or Many Small Ones* — 45 lessons, 8 tracks, 142 gate cases, 0/0; nine public stamps; the FIRST Phase 4 row whose correct write is a MID-ARRAY INSERT, at §4 position 2 of `electrical-foundations`, which fires P5 by construction and correctly — the track is now COMPLETE at five of five). §8 item 6's two-back-to-back-`prose` contradiction resolved on evidence for the second time: the corpus states the harvest and availability arguments as relationships with NO figures, so `bars` was impossible and a load-bearing `proscons` was inserted instead, seven sections → eight. Both halves of the due count measured (9 → 10, `read-next` alone, staying at 10) and row 14's three-part deepening test run programmatically. `reviewBy` 2027-03-16 on the ~6-month default, three rejections in writing.** **Row 16 done — v06.13r (2026-09-16, `clean-firm-power`, *When the Campus Signs a Reactor* — 46 lessons, 8 tracks, 142 gate cases, 0/0; ten public stamps; §4 position 4 of `aidc-campus`, the SECOND mid-array insert in a row and the second P5 by construction). **It is the row that makes an already-declared handoff real**: `contracts-and-revenue` had named *Clean Firm Power* as the owner of the physics before the lesson existed, and the search found exactly one such creditor across all twenty-seven hand-authored lessons. The split is the INVERSE of every S2 split — the contributor landscape owns the parties, this public lesson owns the physics, and no company is ranked in it. Both halves of the due count called exactly and **the deepening half fired for the first time in the programme** (9 → 10 → 10; `read-next` AND `what-is-bought-and-on-what`). `reviewBy` 2027-03-16 on the six-month default, the sort returning zero future day-level dates across 43 policy entries, three rejections in writing.** **Row 17 done — v06.22r (2026-09-16, `where-the-chain-breaks`, *The Failure-Point Map* — 47 lessons, 8 tracks, 142 gate cases, 0/0; THIRTY public stamps, the widest in the programme; §4 position 11 of `aidc-grid-to-chip`, an APPEND at 9 of 9 among the built lessons, which broke the two-row insert run and reported **P1 alone** — no P5, no P3, no P8). **It is the capstone the developer asked for by name, and the one row whose spec is §5 rather than §3.3** — fourteen rows behind a section id that says twelve. The id is kept, all fourteen carried, and the split made an explicit line in the table's `note`. **The four recurring causes were counted, not asserted: hands 6, current squared 5, no zero crossing 1, series heat 1, none of the four 3** — so the commonest cause is not physics and the most-discussed hazard is the rarest. **The DC-arc taxonomy was declined and the handoff named** (session 10's (aa1) posture, applied to a lesson). Two guides §5 does not name were pinned because they alone carry row 1's audit clause; three §5 names were dropped as unused. Both halves of the due count called exactly (8 → 8 → 8; the deepening test fails at part one and the membership half nets). **(bb1) measured and did not bind** — 21 modules grepped, zero hits. `reviewBy` 2027-03-16 on the six-month default.** **Row 18 done — v06.26r (2026-09-16, `the-cooling-plant-and-water`, *PUE, WUE, and the Plant Outside the Hall* — 48 lessons, 8 tracks, 142 gate cases, 0/0; thirteen public stamps, every date read off the fetched guide's own `lastUpdated`; §4 position 2 of `aidc-campus`, a MID-ARRAY INSERT, so **P1 and P5** — no P3, no P8). **THE DEEPENING TEST PASSED ON ALL THREE PARTS AND ON SIX OF SIX CRITERIA — the strongest pass the programme has measured.** `--check` **7 → 8 → 8**, `cooling` **entering** rather than clearing and differing in **read-next AND what-is-bought-and-on-what**, exactly as §7.42 forecast; measured against the generator before the write and again after, **all six `(planned)` renders cleared to a built title, none left**. **§2.3's one-lesson cap held and the equipment side went inside the eight sections** — the chiller arrives as temperature lift, part load and the compressor choice, the CDU as one number (the approach that sizes the plant); no ninth section. **§10.6 (t) FIRED INVERTED — an UNDECLARED PRE-EMPTION.** `heat-is-the-constraint`, one position upstream in the same track and the other lesson in `cooling`'s `READ_NEXT`, carries **no decline sentence anywhere** and had already taught six things §3.4 reads as this lesson's — CRAC/CRAH, the economiser, the CDU, the blanking panel, warm water, and `approach` as a glossary term — because it was authored before this row was scheduled beneath it. Six omissions written into the header comment; **§7.23's test held on the 40–45 °C band**, a *destination* upstream and a *ceiling* here. **The track's `short` was rewritten to the whole-track form**, the debt row 16 deliberately left for this session: `aidc-campus` is now **five of five**. `reviewBy` 2027-03-16 on the six-month default, with **five rejections in writing** — the sort found four real future day-level dates and every one is 27 months out or further. **The screenshots caught three defects every checker passed, for a fifth consecutive session** — a tile headline whose `↕` renders as a colon in the serif tile font (*PUE : WUE* said ratio where the tile meant trade), a two-preposition collision in a table `note`, and a glossary entry naming *blanking plates* against the lesson's own `blanking panel` term. **Not the rotator either: the push landed on 2026-09-16 EST for a tenth consecutive session.** Brief was §7.42; §7.43 written in the same commit. **Row 19 (`how-a-cell-is-made`) done — v06.30r, 2026-09-17**, the manufacturing half of the BESS floor and the row that **completes `bess-foundations` at six of six** — the **third of the five mechanism tracks to close**, after `electrical-foundations` (row 15) and `aidc-campus` (row 18). Seven sections at §3.1's ids and order — `the-line` (prose) · `three-formats` (**proscons**) · `yield-is-the-game` (table) · `scaling-physics` (prose) · `factories-are-the-product` (callout) · `drill` (6 cards) · `check-yourself` (5 items) — inserted at `bess-foundations` position 5, so **P1 and P5, no P3, no P8, no P12**. **The G2 trap was ten of ten and every one was avoided**: all ten `study:` guides carry a `lastUpdated` that differs from their `git log` date, and each pin is the fetched document's own value. §3.3's rule **cut in one direction** — `study:envision-energy` read and dropped (its material is `the-control-stack`'s), so **ten inputs, all public**, gate `tracks`. **`--check` held at 7 → 7, the predicted HOLD**, and there was **no deepening** because no `CRITERION_LEXICON` entry maps to this lesson at all — measured programmatically, not assumed. **§10.6 (t) fired three times in one track and one firing was a third class: a DECLARED handoff pointed at the WRONG sibling**, corrected in the same commit. `reviewBy` 2027-03-17 on the six-month default with the sort returning an **empty set** inside the lesson's own material and three rejections in writing. **THE ROTATOR AT LAST — the push landed 2026-09-17 EST**, ending a twelve-session deferral: two date groups out of `CHANGELOG.md` and two out of `Classroomgs.changelog.md`. Brief was §7.44; §7.45 written in the same commit.**; **Row 20 (`dc-fault-engineering`) done — v06.34r, 2026-09-17**, the **fiftieth** lesson and **the first gated lesson Phase 4 has authored**: every one of the forty-nine before it folds to `tracks`, and this one carries `guidance:nvidia-800vdc-2026-08` in its stamp, so `clGateForProvenance_()` returns `guidance` and **no field anywhere says so**. Eight sections at §3's ids and order — `four-deployment-options` (proscons) · `ac-versus-dc-faults` (table) · `grounding` (table) · `zones-and-the-interlock` (prose) · `sscb-versus-mccb` (bars) · `what-the-paper-does-not-say` (callout) · `drill` (6 cards) · `check-yourself` (5 items) — inserted at `aidc-grid-to-chip` index 8 of ten, so **P1 and P5, no P3, no P7, no P8, no P12**. **THE DENIAL IS THE ROW'S TEST AND IT WAS MEASURED BASE AGAINST HEAD**: an analyst's `aidc-grid-to-chip` went from **9 cards / `withheld` 0** to **9 cards / `withheld` 1**, `cop=lesson` answers `ROLE_DENIED` with **0 of 8 section nodes** rendered, the analyst index stays at 47 of 50 and the title never enters the analyst DOM; contributor and admin get all eight. **THE ROW INHERITED A DEBT AND CLOSED IT** — `the-800-vdc-shift` enumerated **five** things as belonging to the stricter gate and all five are carried one per section; `where-the-chain-breaks` declined the arc taxonomy twice, and **both of its sentences claiming the taxonomy was "held by nobody" became false on this commit**, so both were corrected under **one `revisions[]` entry naming both section ids**, which is why P8 stayed silent. **THE REVERSE (t) CHECK RETURNED EMPTY** across all forty-nine lessons — no silent neighbour, so session 15's fourth shape did not recur — but it surfaced a real collision that became teaching material: **`interlock` already means four other things in this curriculum and none is this one**. **The G2 trap fired on three of the four study guides** and every pin is the fetched document's own value. **`reviewBy` 2026-11-30 is INHERITED per §3**, confirmed on the day, with the nearest-gate sort returning an **empty set** and two rejections in writing. **`--check` held at 6 → 6, both halves derived programmatically before the run.** The screenshots caught **two** defects every checker had passed — a table `note` asserting a provenance the table beside it contradicted, and a callout saying *"the section above"* about a section three earlier — for a **ninth** consecutive session. **The literal is 100 % ASCII.** Brief was §7.46; §7.47 written in the same commit. **Row 21 (`utility-procurement-meets-ai-load`) done — v06.38r, 2026-09-17**, the **fifty-first** lesson, the **SECOND gated lesson Phase 4 has authored**, and the row that **closes `market-access` at five of its nine planned positions** — the first withheld count an analyst meets in the market track. Title *The Gatekeeper*, group Market Access & Bankability. Eight sections at §3.5's ids and order — `the-gatekeeper` (prose) · `the-four-moves` (table) · `five-channels` (**proscons**, 5 cards) · `who-signs-the-po` (callout) · `ercot-versus-regulated` (table) · `the-dated-gates` (**timeline**, 7 items) · `drill` (6 cards) · `check-yourself` (5 items at indices 1·3·0·2·3) — **APPENDED** to `clTrackMarketAccess_()` at position 9 of §4, so **P1 alone: no P5, no P3, no P7, no P8, no P12**, and `--base origin/main` reported **0 findings** on the code commit. **THE BRIEF'S DENIAL ARITHMETIC WAS WRONG AND THE MEASUREMENT CAUGHT IT.** §7.48 forecast the analyst `withheld` count moving **3 → 4** because `market-access` "already carries three gated lessons". It carries none: `the-certification-stack`, `what-bankable-means` and `the-china-policy-stack` are **§7 rows 22–24 and register zero times** in `Classroom.gs`. Measured base against head through the real `handleClassroomOp_`, the count moves **0 → 1**, exactly as row 20's did; `cop=lesson` answers `ROLE_DENIED`; **0 of 8 section nodes** render in the analyst DOM at 60 characters and the title never enters it; contributor and admin get all eight at 25,030 characters with **zero page errors**. The brief's "analyst index stays at 50 of 51" was wrong on both numbers too — **the analyst index holds at 47 while the corpus goes 50 → 51**. **THE SPLIT WAS CHECKED RATHER THAN INHERITED, AND TWO OF §2.2's FOUR RESERVED ITEMS HAD PARTLY MOVED** — the two-lane sales motion is already public in the Dominion and Southern guides, and row 2's `who-picks-the-battery` teaches three lanes inside one utility carrying the *same qualification-list sentence*. What no source holds is the five channels as **one comparable set** and the counterparties of channels four and five, so the lesson maps the set and leaves the lanes where they are. **And one item §2.2 did not anticipate**: the fifth channel's *product* is `where-bess-plugs-in`'s socket 2, already taught at length (NOGRR ×7, ride-through ×21, buffering ×8), so that card carries the procurement half only and its own `note` says so. **THE DATED GATES WERE HANDED OVER IN WRITING, AND THE HANDOFF WAS WRITTEN CORRECTLY** — `where-bess-plugs-in`'s timeline `note` declines "the utility procurement lane — certification orders, storage RFP cycles, new large-load tariffs taking effect" as belonging "to the market-access material rather than here". It is a **pointer, not a status report**, so **nothing in it became false when this lesson landed**: (hh2) applied correctly by a prior row rather than violated by one, and the first such instance the programme has recorded. **`--check` 5 → 7, DERIVED FROM THE PARSED GENERATOR BEFORE THE WRITE AND EXACT ON BOTH HALVES** — exactly two `READ_NEXT` memberships (`hyperscalers-and-ai-labs`, `utilities`), neither previously due, both differing in `read-next` **alone**; zero of the 27 `CRITERION_LEXICON` entries map to this lesson, so (ff2)'s first part fails and no `what-is-bought-and-on-what` moved. **(aa5) now holds for an eleventh session.** Nothing was regenerated. **`reviewBy` 2026-12-10 is INHERITED per §7 row 21**, re-read off the module on the day, and the sort ran anyway: **(ii7) fired harder than at S2 session 16** — an ISO regex finds **one** future day-level date in all seven inputs (the module's own `reviewBy`, a tautology) while a word-form scan finds **five it cannot see, two of them EARLIER than the inherited date**. **Five rejections in writing** and the first Phase 4 sort to return a candidate that would have beaten the inheritance. **TWO RENDERER FACTS FOUND PRE-SPLICE AND USED**: `CL_LANE_COLORS` is keyed on the literal lane names `gen`/`deploy`/`eco`, so any other key renders faint grey; and `clTimeline` prints `Math.floor(x)` in the left gutter, so on a **calendar** timeline `x` carries the real year and the gutter reads 2025 / 2026 / 2027. **All six pre-splice simulations returned 0/0** and **the recount pass then caught six defects they could not see**; **the screenshots then caught four more for an ELEVENTH consecutive session**, two of them the prose-contradicts-the-adjacent-content class — a table `note` calling a move a non-purchase when its own row is gas *procurement*, and a timeline item claiming to be "the one date that has not happened" beside a note saying **two** have. **The literal is 100 % ASCII.** Brief was §7.48; §7.49 written in the same commit. |
| 5 · report-strategy evaluation | Fable 5.1 xhigh | **Done — v05.12r (2026-09-08).** A written decision per report in §7.7: all four **re-scoped and regenerated**, none retired, none regenerated as-is — named-project to the live project web (24 pinning dossiers + `tesla`, 9 projects), grid-scale to the registry's integrator incumbents + challengers (18) + `flexgen`, §154 to the statutory six (`gotion` added), AIDC to the covered 800 VDC roster by layer (15); the seven warnings read again and left loud (none re-pinned); the drift-gated Routine's gate **replaced** in §7.7 (recorded, not applied); the §5 caveat restated as supplier whitespace; §7.8 carries the three Phase 6 briefs. Checkers identical before and after (reports 0 / 7; content 10 / 3 / 134, 0 / 0; pipeline no P3). Closed against the §2 criterion X3 wrote — not by the session that wrote it |
| 6 · regenerate reports | Opus 5 xhigh | **Done — v05.15r (2026-09-08).** Three sessions in the §7.7 order, briefs in §7.8: **(1) Done — v05.13r (2026-09-08).** `named-project-bess-attach--opportunity--2026-09-08` published, superseding the 2026-08-30 id: scope re-derived on the day to **25** (24 dossiers pinning the nine registered projects + `tesla`), River Bend and ten new participants in, fresh pins for all 25, 40 citations, overlays on `power-infra-aidc-2026-08` → `sockets` and `utility-aidc-procurement-2026-08` → `buyermap`; the old index entry flipped to `superseded` and its 13 rows removed from `report-pins-verified.json`; `check-profiler-reports.py` **0 errors / 6 warnings**, all six on the three untouched editions and none on the new one. Landed before the Routine's 2026-10-01 fire, which now reads this edition; **(2) Done — v05.14r (2026-09-08).** Both editions published in one session. **Preflight taken, not deferred:** the desk had still not landed `byd` (due 2026-08-29) or `eve-energy` (due 2026-08-20), so both were refreshed under the full Profiler Command first — `byd` v7→v8 and `eve-energy` v5→v6, archived, registry synced, graph rebuilt, calendar rows advanced to the 2026-10-31 CSRC backstop — because BYD's H1 was a **fired indicator of both reports** and a stale BYD next to a fresh Sungrow would have broken the head-to-head's like-for-like. `grid-scale-bess--competitive--2026-09-08`: scope re-derived on the day from the `storage-integrators-and-containers` roster to **19** (10 incumbents + 8 challengers + `flexgen`) — **eight added**, not the six the §7.8 brief said (§7.7 finding 5's own list names eight; the registry decides), `eve-energy` out; registry role as a column, a new **ranking-basis table** recording each claim's instrument, 36 citations, overlays on `bess-bankability-2026-08` → `counterparty` and `power-infra-aidc-2026-08` → `markets`. `s154-listed-bess-suppliers--risk--2026-09-08`: the statutory six with `gotion` added, 22 citations, `rept`'s not-named record in `limitations[]`, the CATL/Zhonhen RMB 4.1bn capital increase as the new line in the fence, four overlays re-authored plus a fifth. **The old edition's "EVE 1260H is reported, not established" limitation is retired** — the June 2026 listing is now citable. Both old index entries flipped to `superseded` and their **17** rows (12 + 5) removed from `report-pins-verified.json`; `check-profiler-reports.py` **0 errors / 1 warning**, the warning being `zhonhen` v7 on the untouched AIDC edition; all six checkers exit 0; **(3) Done — v05.15r (2026-09-08).** `aidc-power-conversion--competitive--2026-09-08` published, superseding the 2026-08-29 id. **Preflight taken, not deferred:** reachability and existence were established first — all three H1 2026 interims had published (`sinexcel` 2026-08-11, `zhonhen` 2026-08-27, `megmeet` 2026-08-28) and `cninfo.com.cn` answered while `szse.cn` did not — so all three were refreshed under the full Profiler Command (`megmeet` v6→v7, `zhonhen` v7→v8, `sinexcel` v7→v8; archived, registry synced, graph rebuilt 3,681→3,690 evidence items, calendar rows advanced). Scope re-derived on the day to the **15**-name 800 VDC roster **by layer** — silicon (`infineon`, `vicor`); the rack (`delta-electronics`, `liteon`, `megmeet`, `flex`); the sidecar and hall-edge block (`zhonhen`, `sungrow`, `sinexcel`, with `delta-electronics` again); facility and grid (`vertiv`, `eaton`, `schneider-electric`, `abb`, `ge-vernova`, `hitachi-energy`) — with the like-for-like rule met **within each layer's table**, four layer tables instead of one field table, and a normalized whole-company scale section the prior edition could not carry (it recorded the KPI overlay as unused; all 15 now carry one). 31 citations, seven sections, eight judgments, three overlays re-authored on `nvidia-800vdc-2026-08` (`options`, `trusst`, `suppliers`), anchors validated. **The prior edition's 'SST roster still forming' framing is retired** by `sungrow` v9's EnerNeo, described in its own interim as launched and supplying. **The three interims answered the two open indicators the same way — capability without disclosed commerce:** Zhonhen's 187-page interim contains no order, backlog, customer or NVIDIA/SST/TRU mention at all; Sinexcel's management states four times that AIDC products are samples with no formal sales and '800V' appears zero times in its 212 pages; Megmeet's interim claims only membership of NVIDIA's recommended-supplier set and names no competitor in 238 pages. Two corrections landed with them: `zhonhen` does **not** run late (H1 filed four days inside the deadline; the 2024–2026 record is consistent), and `sinexcel`'s OVERDUE row was a **desk false positive** — the report published on its scheduled date. Old index entry flipped to `superseded` and its **8** rows removed from `report-pins-verified.json` (8 → 0). `check-profiler-reports.py` **0 errors / 0 warnings** — the Phase 6 target; all six profiler checkers exit 0; content 10 / 3 / 134, 0 / 0; pipeline 14 findings, all P1 on out-of-write-set paths, no P3. Each new edition carries `supersedes`, fresh pins, overlays only on existing modules (§7.7 finding 8), and removes the superseded edition's rows from `report-pins-verified.json`; before S2 (§7.4) |
| G6 · guidance module | **Opus 5 xhigh** (research synthesis — the developer accepted it 2026-09-07 on the G4 precedent; a document-based revision stays possible later) | **Done — v05.43r (2026-09-13), one Opus 5 xhigh session, order 1 of the §7.3 run table.** `large-load-interconnection-2026-09` is the ninth module in `guidanceDocs_()` — *Large-Load Interconnection: The Federal Rulebook Above the Fence*, lane The AI Data-Center Wave, contributor tier, twelve sections (two prose, four table, one timeline, one proscons, one callout, flashcards, quiz, ledger), twenty-one glossary terms, `reviewBy` **2026-11-16** set from its own nearest dated gate (the 90-day abeyance filings in the six section 206 dockets, and the month FERC's Unified Agenda projects for RM26-4's next action). Public primary sources only, composed **against** the six utility guides rather than restating them; analysis + claims ledger in `repository-information/industry-guidance/large-load-interconnection-2026-09-analysis.md`; Scraper seed `topic-federal-interconnection`. Rendered in Playwright with **zero page errors**; `check-classroom-curriculum.py --strict` clean. **Closes `CLASSROOM-CURRICULUM-PLAN.md` §6's last open row.** The lesson it unlocks — Phase 4 row 26 `interconnection-for-large-loads` — is deliberately unbuilt |

Sessions flip a row here **and** in the record that owns the work (`PROFILER-COVERAGE-PLAN.md` §9.4 for X3b and Phase E / §8 for S3, `CLASSROOM-CURRICULUM-PLAN.md` §6/§7 for Phases 3–4 and G6 and §9/§10 for S0–S2 and K1–K2, the reports index for Phase 6). A row is closed by the developer or a later session against the "done when" in §2, never by the session that wrote the criterion.

## 7 · Program extension — from Phase 3's close to completion (agreed 2026-09-07 PM, persisted at v05.08r)

Phase 3 closed at v05.07r and the developer reviewed it the same day. Three things came out of that review and are recorded here so the next session edits against a file, not a transcript: **(a)** an evaluation of what landed against the plan as agreed at X3; **(b)** a set of developer decisions — the segment layer for Classroom, five coverage areas to fill, and G6 reclassified as research-based; **(c)** the run order from here to completion, with a model and effort per phase under the §2 model rule of `PROFILER-COVERAGE-PLAN.md`, and the paste-in brief for the first session (S0).

### 7.1 · What landed against the original plan

| Phase | Planned (X3 close) | Landed | Deviation worth recording |
|-------|--------------------|--------|---------------------------|
| 0 | Fable 5.1 High · 1 | v05.02r; X3b closed by the developer v05.03r | None |
| 1 | Fable 5.1 Medium · 1 | v05.04r | None; the `hithium` pins re-warned exactly as predicted and were left loud |
| 2a | Opus 5 xhigh · 1–2 | v05.05r · 1 session | Two of the four Sungrow attributions were **refuted**, not corroborated or marked one-sided as the plan expected — the CNY 100B loss belonged to an earlier window and the wired-prong argument was never Sungrow's. Step 7 then moved three more dossiers |
| 2b | Fable 5.1 High · 1 | v05.06r | Hypothesis held with a correction: the USD 4bn is a rounding of a nine-building 2024 total, not an eight-building one |
| 3 | Fable 5.1 xhigh · 1 | v05.07r | The §4 skeleton's "20 remaining" was **24**: four register unlocks (`backup-generation`, `the-ups-room`, `how-a-utility-buys`, `clean-firm-power`) were counted but never specified. Nuclear reversed, utility split, cooling capped; the curriculum is 34 lessons / 10 built. The developer's review of Phase 3 then found the whole plan short one layer (7.2) |
| 4 | Opus 5 xhigh · 20 | Not started | Now **26 sessions**: 24 from Phase 3 plus `reading-the-numbers` (row 25) and the G6 lesson (row 26, after the module); `reading-the-graph` is authored inside S1 |
| 5, 6 | Fable xhigh · 1; Opus xhigh · ~4 | **5 landed v05.12r; 6 at 2 of 3 (v05.13r, v05.14r) — session 3 (AIDC) remains** | **Moved earlier** — before S2, because the landscape modules cite the reports for their admin-tier extras and F10/F11 staleness would propagate into nineteen modules. Phase 5 found the extras cannot reach a landscape through a Phase 6 edition (the overlay lives in the report and must name a module that exists) — §7.7 finding 8 records how they arrive instead |
| G6 | "Blocked on a developer document" | Not started | **Reclassified.** The G4 closure at v04.55r recorded that six of the eight guidance modules are research-based synthesis with no ingested document; the developer accepted a research-synthesis G6 on 2026-09-07. It is one Opus 5 xhigh session and no longer blocked. A document-based revision remains possible later through the `industry guidance:` command, which is also the answer to "upload a document and have the app teach it" — the capability exists in Profiler today and moves to Classroom at C3 |
| — | — | Found in Phase 3 | **The drill's study pool reads top-level `flashcards[]` only**; the 95 guides authored since September carry 1,154 cards in a `flashcards` section the pool never sees. Fixed by K1 below |

### 7.2 · Decisions taken 2026-09-07 PM

**The segment layer (approved as proposed in chat).** Classroom gains a generated market-structure layer between the study guides and the reports: one public **segment lesson** per value-chain segment (players' stated positions, numbers, relationship map, what moved, the policy fence), generated by a script from the registry, dossiers and graph and regenerated when a member dossier moves; and one contributor-tier **landscape module** per segment (who dominates and on what basis, who threatens, each player's strategy bet, indicators, the seller's play), authored by Opus in the guidance-module shape. Three fences moved deliberately, all approved:

1. The content contract is amended: mechanism lessons keep "never company trivia"; segment lessons may drill *structure*; a separate **roster deck** of player cards is opt-in.
2. Landscape analyses sit at the **contributor** tier (guidance-module shape), not admin (report shape), so the future team reads them.
3. The **segments** become a registry layer — `profiler-segments.json`, segment → members with a role (incumbent / challenger / adjacent) — that the Profiler Command must populate on every new dossier, mirrored into the registry as `segments[]`.

**The taxonomy** is the seventeen segments proposed in chat (cells and chemistry; storage integrators and containers; power conversion and rack-power silicon; grid equipment; in-hall power; bridge and on-site generation; clean firm and nuclear; cooling; compute and the rack; EPC and construction with GCs; storage developers and IPPs; AIDC developers and landlords; hyperscalers and AI labs; neoclouds; utilities; capital; advisors) **plus two new ones and one merge**, decided with the five empty areas:

| Area | Decision | Companies approved (hypotheses — every session verifies against sources before writing, per `PROFILER-COVERAGE-PLAN.md` §5's rule) | Model under §2 |
|------|----------|------------------------------------------------------------------------------------------------------------------------------------|----------------|
| Software and optimization | **New segment** | Stem · Habitat Energy · Gridmatic | Stem public (NYSE: STEM) → Opus 5 xhigh; Habitat and Gridmatic private → Fable 5.1 High |
| Insurance and risk transfer | **New segment** (closes the G10 residual) | Marsh (Marsh McLennan) · Aon · kWh Analytics | Marsh, Aon public → Opus 5 xhigh; kWh Analytics private → Fable 5.1 High |
| Testing and certification | **Merged with advisors into "assurance"**, which lifts advisors above the floor | UL Solutions · CSA Group · Intertek | UL Solutions (NYSE: ULS) and Intertek (LSE: ITRK) → Opus 5 xhigh; CSA Group (private non-profit) → Fable 5.1 High |
| Upstream materials | **Adjacent role on cells and chemistry**, not a segment yet | Albemarle (lithium) · a US LFP-cathode entrant — **Mitra Chem** recommended, ICL (St. Louis LFP plant) as the public alternate, Nano One as the North-American fallback · a graphite/anode name — **Novonix** (Chattanooga synthetic graphite; ASX/Nasdaq) recommended, Syrah Resources' Vidalia plant as the operating-US alternate, Westwater Resources' Kellyton as the domestic natural-graphite alternate | Albemarle, Novonix, ICL, Syrah, Westwater public → Opus 5 xhigh; Mitra Chem private → Fable 5.1 High |
| Transmission developers | **Adjacent role on grid equipment and utilities** | Grid United · Pattern Energy | Both private (Pattern under CPP Investments) → Fable 5.1 High |
| Cell roster (C12's absentees, existing segment) | Coverage item | CALB · Cornex · Great Power | CALB (HKEX: 3931) and Great Power (SZSE: 300438) → Opus 5 xhigh; Cornex private → Fable 5.1 High |

That is **nineteen segments** and **seventeen dossiers with study guides** to fill *(nineteen after the developer's 2026-09-07 PM taxonomy review added AMD and Supermicro as group E7 for `compute-and-the-rack`)* (three per session where the research frame is shared → six to seven sessions). The **floor rule**: no landscape module is authored for a segment until it holds at least three dossiers including one incumbent and one challenger. Today only assurance (two advisors) is below it among the nineteen; the two new segments start at zero.

**G6 — research-synthesis accepted.** One Opus 5 xhigh guidance session on the federal interconnection layer (FERC Order 2023, large-load tariffs as a class, study-fee regimes), composed against the six utility guides rather than restating them; the module unlocks the contributor lesson behind `the-fence-line` (Phase 4 row 26). The register's "blocked on a developer-supplied document" wording is corrected by S0.

### 7.3 · The run order from here

| # | Phase | Model · effort | Sessions | What | Done when | Depends on |
|---|-------|----------------|----------|------|-----------|------------|
| **S0** | **Segment-layer design gate** | **Fable 5.1 xhigh** | 1 | `profiler-segments.json` (nineteen segments, every membership verified against the dossier's `ecosystemRole`); curriculum plan **§10** (the layer, the floor rule, the segment-lesson template, the generator spec, the roster-deck and study-pool-fix specs, the two implicit role paths); §3/§7 rows 25–26; the content-contract amendment in `CLASSROOM-SCHEMA.md` and `classroom-app.md`; the registry `segments[]` field in `PROFILER-SCHEMA.md` and the Profiler Command step in `profiler-app.md`; the G6 row corrected in the curriculum plan §6; a **Phase E** coverage ledger in `PROFILER-COVERAGE-PLAN.md` with the seventeen companies of 7.2 and their models. Paste-in brief in 7.5 | Files exist; the developer reviews the taxonomy; no code, no lesson, no dossier | Nothing — runs next |
| **K1** | **Study-pool fix** | **Fable 5.1 High** | 1 | The drill's study pool reads the `flashcards` section of a guide when the top-level array is empty (or the lift script moves the cards up); pool count ≈ 766 + 1,154. Classroom GAS + page bump; `check-classroom-pipeline.py --base origin/main` — refresh `gateDigest` if P3 reports | Both checkers clean; Playwright read of the drill | Nothing — can run before or after S0 |
| **5** | Report-strategy evaluation (F10, F11) | **Fable 5.1 xhigh** | 1 | Per report: regenerate as-is, re-scope, or retire; whether the monthly drift gate is right at 154+; lift or restate the §5 caveat | A written decision per report | Moved before S2 (7.1) |
| **S1** | **Generator, tracks, checker, lane opener** | code: **Fable 5.1 High** · lesson: **Opus 5 xhigh** | 2–3 | `scripts/build-classroom-segments.py` (stable ids `segment-<id>`, fixed section ids, `revisions[].changed[]` computed by diffing sections as P8 does); the registry sync mirrors `segments[]`; three Value Chain tracks under a fourth lane; `check-classroom-curriculum.py` (lessons per lane, gate distribution, segment coverage vs floor, stale pins, drill pool size) plus a segment check in the content checker; `reading-the-graph` authored as the lane's opener | Segment lessons generated for all nineteen; content checker 0 errors; analyst-view test passes; health script reports — **Done: code half v05.17r, lesson half v05.18r; The Value Chain lane at 20 lessons, 20/20 analyst-visible** | S0 |
| **6** | Regenerate the reports Phase 5 keeps | **Opus 5 xhigh** | 2–4 | New editions carrying `supersedes`; fresh pins; superseded editions marked | `check-profiler-reports.py` clean on the new editions | 5 |
| **S3** | **Coverage fill** | per 7.2: **Opus 5 xhigh** for public deep records, **Fable 5.1 High** for private/opaque | **10** (6 public · 4 private) | The nineteen dossiers + guides of 7.2, **split by model into ten sessions** (`PROFILER-COVERAGE-PLAN.md` §10.1–10.4, 2026-09-08): the group is not the session, the model half is (`Ea` public, `Eb` private), and the two pairs of one-company Fable halves run merged as `E2b+E3b` and `E4b+E6b`. Public — E7, E2a, E1a, E3a, E4a, E6a. Private — E2b+E3b, E1b, E5, E4b+E6b. Each assigns its segments and roles | Every segment at the floor; registry synced; graph rebuilt; register re-run for G10. **Critical path is sessions 1–5 of §10.4** — all four blocked landscapes writable after them. **Met 2026-09-13 (v05.41r)** — see the §6 ledger row | S0 (for the segment assignment); interleaves with S2 |
| **G6** | Large-load interconnection module | **Opus 5 xhigh** | 1 | Research synthesis in `guidanceDocs_()` with a claims ledger and a `**Provenance:**` line; Scraper seed per the guidance rules | Module exists; curriculum plan §6 G6 closes | S0 (row wording); before Phase 4 row 26 |
| **C3** | Guidance homecoming | **Opus 5 xhigh** | 2–3 | ~~Migrate guidance modules from `Profiler.gs` to `Classroom.gs` per the design's checklist; the `industry guidance:` command targets Classroom thereafter~~ — **done in 3 sessions, v05.48r** | ~~Modules render in Classroom; role matrix verified; Profiler deep-links redirect~~ — all three met, plus the migrated ticks verified against a pre-migration export | ~~Before S2 exceeds ~6 landscape modules~~ — met: S2 has authored none |
| **S2** | **Landscape modules** | **Opus 5 xhigh** | ~19 | **17 of ~19 built — integrators (v05.64r), cells (v05.70r), developers and IPPs (v05.74r), utilities (v05.78r), power conversion and rack-power silicon (v05.83r), AIDC landlords (v05.88r), in-hall power (v05.93r), grid equipment (v05.97r), bridge generation (v06.03r), clean firm and nuclear (v06.07r), hyperscalers and AI labs (v06.11r, 2026-09-16), neoclouds (v06.15r, 2026-09-16), capital (v06.24r, 2026-09-16), EPC and construction (v06.28r, 2026-09-16), cooling (v06.32r, 2026-09-17), compute and the rack (v06.36r, 2026-09-17) and software and optimization (v06.40r, 2026-09-17 — 20 members at 2 · 3 · 15, the most adjacent-heavy roster in the taxonomy at 75.0 %, and the first segment whose bench inverts upward 15 of 15 while its own ranked players invert downward 4 of 5).** *(This cell read "5 of ~19" from v05.83r to v05.96r and was two sessions stale; S2 session 8 flipped it, since the row is S2's own record. **It went two sessions stale a SECOND time** — it still read "13 of ~19" with `EPC/GC` unstruck when S2 session 15 opened, so session 14's landing never reached it. The row is S2's own record and each session must flip BOTH the count and the strike list; a session that flips only the §6 ledger leaves this one behind.)* One segment per session in the guidance-module shape at contributor tier, ordered by sales value: ~~integrators~~, ~~cells~~, ~~developers and IPPs~~, ~~utilities~~, ~~power conversion~~, ~~AIDC landlords~~, ~~in-hall power~~, ~~grid equipment~~, ~~bridge generation~~, ~~clean firm~~, ~~hyperscalers~~, ~~neoclouds~~, ~~capital~~, ~~EPC/GC~~, ~~cooling~~, ~~compute~~, ~~software~~, insurance, assurance | Each module passes the guidance checks; its segment is at the floor; `reviewBy` set from its nearest dated gate | S0; the segment's S3 fill; Phase 6 for report-derived extras |
| **4** | Mechanism lessons | **Opus 5 xhigh**, one per session | 26 | **21 of 26 built.** The curriculum plan §7 order, rows 1–24, then row 25 `reading-the-numbers`, then row 26 the G6 lesson. Rows 23–24 no longer wait — the bankability module's quarterly review landed at v05.50r and moved its gate to 2027-01-01. **Row 20 (`dc-fault-engineering`, v06.34r, 2026-09-17) is the FIRST GATED lesson Phase 4 has authored**: its stamp carries a `guidance:` ref, so the fold makes it contributor-only and `aidc-grid-to-chip` becomes the third track an analyst sees a withheld count on — 9 cards / `withheld` 0 → 9 cards / `withheld` 1, measured base against head through the real serving path. **Row 21 (`utility-procurement-meets-ai-load`, v06.38r, 2026-09-17) is the SECOND**, and it makes `market-access` the **fourth** such track — the first in the market walk. Its arithmetic is the same 0 → 1, *not* the 3 → 4 §7.48 forecast: the three other guidance market lessons named there are §7 rows 22–24 and **are not built**, which the measurement caught and the brief did not | 34 → 37 lessons built; both checkers clean after each | Row 1 creates `electrical-foundations`; row 26 needs G6; otherwise interleaves with everything |
| **K2** | Roster deck | **Opus 5 xhigh** | 1–2 | Opt-in second deck enumerated from segment lessons' player tables; separate queue; mechanism deck untouched | Drill serves both decks; analyst-view test | S1 |
| **C5** | Sales simulations | design **Fable 5.1 xhigh** · 1; authoring **Opus 5 xhigh** · ~5 | ~6 | Scenario modules per segment built from dossier facts, relationships, projects and policy exposure — the assessment layer for "evaluate" | Per the design's own criteria | After ~6 landscape modules |
| **C6** | Team layer | — | deferred | Dashboard, placement quiz, certification checkpoints | — | A team existing |
| **Q** | Quarterly clocks | Fable 5.1 xhigh (plan) · Opus 5 xhigh (guidance review) | recurring | Guidance review (bankability first, before Phase 4 rows 23–24); the plan clock: re-verify curriculum plan §1, re-test §2.2, re-rank §7, run the health script | — | — |

**About 75 sessions from here** (S3 re-counted from 7–8 to 10 when it was split by model on 2026-09-08), C6 excluded: roughly 50 on Opus 5 xhigh (26 mechanism lessons, ~19 landscapes, reports, G6, K2, C3, part of S3 and C5), about 12 on Fable 5.1 High (K1, S1's code, the private-company halves of S3), and 4 on Fable 5.1 xhigh (S0, Phase 5, C5 design, the plan clock). The mechanism lessons and the landscape modules are the two long lanes and they touch different files, so they interleave with each other and with S3.

**Progress against this table — updated 2026-09-13 (v05.44r).** **Order 2 (C3) is under way: session 1 of 2–3 is done.** One Opus 5 xhigh session copied the nine `guidanceDoc*_()` literals and `guidanceDocs_`/`guidanceIndex_`/`guidanceDoc_` verbatim into `Classroom.gs` **below `// CONTENT END`** — outside the unattended C2 pipeline's write set by construction — added `handleGuidanceOp_` behind `clRequire_(sess,'guidance')` on `action=guidance`, and built the library, module, search and unified-glossary surface in `Classroom.html` on the `cl*` primitives C1 slice 2 already provided. The **file-layout question is settled and recorded**: one `.gs`, not two, because `pullAndDeployFromGitHub()` PUTs a single `SERVER_JS` entry and replaces the project's whole file set — a second file would be deleted by the next deploy and carries no `VERSION` line for the deployer to diff. Profiler keeps serving the same nine modules until session 3 cuts it over, so they are **deliberately duplicated** and `scripts/check-guidance-parity.py` fails the build on any divergence; it and Profiler's copy go together in session 3. Guidance renders **read-only** in Classroom through session 2 so the one-shot progress import stays one-shot. **Session 2 (checklist items 2 and 3 — the Admin lens re-host and the token-gated mentions route) is next, with its brief in §7.10.** Nothing else in the table moved.

**Re-evaluated 2026-09-13 (v05.42r), the day S3 closed — where the programme stands and the order from here, each phase tied to a model.** Done: Phases 0, 1, 2a, 2b, 3, 5 and 6; S0, K1, S1 (code half; the lane opener `reading-the-graph` landed with it), S3 (ten of ten). Open: G6, C3, S2 (0 of 19), Phase 4 (0 of 26), K2, C5, the Q clocks. Deferred: C6. **The question the close of S3 raised — S2 next, or G6? — is answered G6, and it was already the plan's own answer** (§7.4 item 5 has read "G6, then C3 before S2 passes about six modules" since S0): G6 was never gated on S3 (its gate is S0, met 2026-09-07), it is one session, it closes the register's only open row and unlocks Phase 4 row 26, and it adds the ninth guidance module *before* C3 migrates them from `Profiler.gs` to `Classroom.gs`. C3 then runs before any S2 landscape exists, so the migration moves nine modules and zero landscapes rather than growing with S2. Only after that do the two long lanes start.

| Order | Phase | Model · effort | Sessions | Gate | Note |
|---|---|---|---|---|---|
| 1 | **G6** — large-load interconnection module | **Opus 5 xhigh** | 1 | none open (S0 met) | **Done — v05.43r, 2026-09-13.** Ninth module in `guidanceDocs_()`; register's last open row closed. Brief was in §7.9 |
| 2 | **C3** — guidance homecoming | **Opus 5 xhigh** | 2–3 | **DONE — three sessions, v05.44r / v05.45r / v05.48r, all 2026-09-13** | Nine modules and zero landscapes, as designed. Session 1 moved the content and the read ops into `Classroom.gs` below the `// CONTENT END` fence and built the guidance surface; session 2 re-hosted the Admin lens and moved the mentions scan behind Classroom's token-gated peer route; session 3 migrated and verified the progress ticks, wired the ticks and the drill, re-pointed Profiler's deep links, deleted Profiler's copy and `check-guidance-parity.py`, re-targeted `.claude/rules/industry-guidance.md` and updated the quarterly Routine in place. The slice plan under the C3 bullet of `PHASE6-CLASSROOM-DESIGN.md` records what each session landed and **closes C3**. **Next in the order: row 3, the bankability review — and it now edits only Classroom's copy** |
| 3 | **Q · bankability review** — `bess-bankability-2026-08` | **Opus 5 xhigh** | 1 | date | **DONE — v05.50r, 2026-09-13.** `updated` 2026-08-24 → 2026-09-13, `reviewBy` 2026-10-01 → **2027-01-01** set from the module's own next dated gate (UL 9540A Ed. 6's effective date, which is also when non-BES IBRs come inside PRC-029) rather than a fixed cadence; the curriculum checker's 30-day horizon now reports **0 items due**. **Five claims moved against primary sources, two of them corrections of things the module had wrong:** UL 9540A is a full edition behind (Ed. 6 exists, effective 2027-01-01, drops unit-level for non-residential BESS without an active TRPPS and puts pass/fail on the installation test — so 'cell + module + unit is the complete stack' was false); the LSFT procedure is ANSI/CSA **C800:25 §9.7**, not TS-800:24, and NFPA 855-2026 as issued names **no** method (Annex G.11 says only what it must show); **the FCC inverter rule is not a China rule** — the Covered List entry is 'foreign-produced power inverters', expressly regardless of the producer's nationality, with the exits being §45X eligibility, domestic-end-product status or a Conditional Approval, which kills the module's 'pair with a non-Chinese PCS' advice outright; **AB 303 died 2026-01-31, not April 2025**; and BNEF's Tier 1 criteria gained a two-commercial-bank test on 2026-08-15. PRC-029-1's 2026-10-01 date is **confirmed** and the successor PRC-029-2 plus the phased dates are now carried. **Section ids, the module id, and the three `counterparty` overlay anchors are untouched** — the two Admin-lens panels were re-rendered above the Mark-as-understood button with zero page errors. **One incidental fix, and it is the report half of the brief:** `check-profiler-reports.py` still parsed guidance modules out of `Profiler.gs` and so returned `[]`, erroring on all 20 overlay anchors in all 8 reports — a silent C3 leftover, identical on a pristine `HEAD`; repointed at `Classroom.gs`, the checker is back to **0 errors / 0 warnings**. **No report is materially out of date and none is recommended for re-run.** Brief was in **§7.12** |
| 4 | **Phase 4 row 1** — `four-machines` | **Opus 5 xhigh** | 1 | none | **DONE — v05.53r, 2026-09-13.** One lesson, seven sections at the ids curriculum plan §3.2 fixes, gate `tracks` on seven public stamps each dated off the fetched file. `electrical-foundations` created at **`clTracks_()` position 2**, not appended — §8 item 9's rule for a developer session, and the reason P5 fires and is accepted, exactly as S1's lesson half recorded at v05.18r. The `prereqs[]` §2.2 promised since v05.07r are written on both AIDC tracks. **31 lessons, 7 tracks;** `check-classroom-content.py`'s 24 errors are byte-identical to a pristine `HEAD` worktree — twelve stale `segment-*` lessons, S2's to regenerate, neither caused nor fixed here. Brief was the §7.13 that was never written to this file: the run table's row 3 close at v05.50r advanced to order 4 without persisting one, so the prompt lived only in the session that received it. Phase 4 is **1 of 26** |
| 5 | **S2** — landscape modules, one per segment, in the §7.3 sales-value order (integrators first, cells second — the two deepest at 32 and 22 members) | **Opus 5 xhigh** | ~19 — **17 done** | C3 | Each session regenerates its own `segment-*` lesson (the "regenerate only what moved" rule collapsed to all nineteen at v05.23r; ownership per curriculum plan §10.9). **The standing 19-due backlog was cleared ahead of S2 by the developer regeneration run of v05.62r (row `—` below), so an S2 session no longer inherits a stale roster** — but it **still regenerates its own segment after authoring the module**, because §10.6 requires that segment lesson's `read-next` to deep-link to the landscape module and the link cannot exist until the module does. That remains one `--segment <id>` invocation per session, well inside P10's caps. **Session 1 is DONE — v05.64r, 2026-09-14.** `landscape-storage-integrators-and-containers-2026-09`: nine §10.6 sections, eighteen `each-players-bet` rows, a 41-row claims ledger, `reviewBy` **2027-01-01**, Scraper seed `topic-bess-integrators`, and the one `--segment` regeneration that turned the lesson's `read-next` from "No landscape module yet" into the module's name. The segment re-measured on the day at **32 members (10 · 8 · 14)**, against §10.2's S0 figure of 29. **Neither predicted changelog rotation fired, and both predictions were arithmetic errors of the same kind** — they counted raw sections where the rule counts non-exempt ones: the repo CHANGELOG landed on 2026-09-14, so its five same-day sections plus this one stayed exempt at **98 non-exempt**, and `Scrapergs.changelog.md`'s new section was itself same-day, leaving **49 non-exempt** against a trigger of 50. The clone was deepened anyway, which is the right order regardless. Brief was §7.17. **Session 2 is DONE — v05.70r, 2026-09-14.** `landscape-cells-and-chemistry-2026-09`: nine §10.6 sections, the full **fifteen-row** `each-players-bet` table (4 incumbents + 11 challengers, none trimmed), a 50-row claims ledger in plain text, and the one `--segment` regeneration that turned the lesson's `read-next` from "No landscape module yet" into the module's name. The segment re-measured on the day at **22 members (4 · 11 · 7)** — §7.19's figure confirmed exactly, as was its correction that this is the **fifth**-deepest segment rather than the second. **Two findings the next seventeen inherit.** (i) **`reviewBy` is read, not sorted.** It resolved to **2027-01-01** on China's export-rebate cliff, which two members state inside a `policyExposure[].exposure` paragraph while the same entry's `effectiveDate` carries the earlier first step of the schedule — so a sort of the fence returns 2027-02 and the generator returns **2027-10-01**, a ten-month spread across three defensible readings. Three nearer candidates were rejected in writing (one company's execution milestone; a dated gate the module leans on no claim from; an undated "late 2026"). (ii) **The Scraper seed of `industry-guidance.md` step 9 was correctly NOT added**: `topic-bess-technology` already owns the cell and chemistry layer, which session 1's own seed comment states in as many words — so `Scraper.gs` was untouched and `Scrapergs.changelog.md` stays at **50 raw / 49 non-exempt**, still one short of its trigger. Two genuinely unscored sub-layers (the format ladder, the EU battery passport) are recorded in the analysis file and left for the next session that touches that file for its own reason. **The pipeline signature §7.19 predicted was wrong and is corrected here for session 3: the run reports P1 on the developer files, P2 on the guidance literal, one P7 when the segment was already regenerated the same day, and NEVER P3 — not "P1 and P10".** P2 is what a developer diff outside the content fence reports, and one regenerated lesson is well inside P10's cap of three; this is the identical signature session 1 recorded at v05.64r. Neither predicted changelog rotation fired: the push landed **2026-09-14**, so the repo CHANGELOG's twelve same-day sections stayed exempt at **98 non-exempt** against 110 raw. Brief was §7.19. **Session 3 is DONE — v05.74r, 2026-09-14** (this row was not appended to at the time; recorded here by session 4). `landscape-storage-developers-and-ipps-2026-09`: nine §10.6 sections, the full **twenty-seven-row** `each-players-bet` table, a 58-row claims ledger, `reviewBy` **2027-01-01** set from a cost ratio indexed to the calendar year when **a sort of the fence returned nothing at all** (142 policy entries, 96 dated, none in the future), and the Scraper seed `topic-storage-owners` added after the first draft was cut back against all 144 existing terms. The segment measured 34 members (19 · 8 · 7). Brief was §7.21. **Session 4 is DONE — v05.78r, 2026-09-14.** `landscape-utilities-2026-09`: nine §10.6 sections, an **eight-row** `each-players-bet` table — the shortest of the four and the segment reported honestly — a 52-row claims ledger, and the one `--segment` regeneration that took `--check` **5 → 6 → 5 due** exactly as §7.23 predicted. The segment re-measured on the day at **14 members (6 · 2 · 6)**, confirming §7.23 exactly. **Four findings the next fifteen inherit.** (i) **A landscape can land on a segment an existing module already covers, and the split has to be written before the module is.** `utility-aidc-procurement-2026-08` owns the procurement **process**; this one owns the **parties**, and it deliberately carries no tariff table, no channel list and no buyer map, states the rule in its opening paragraph, and closes `check-yourself` on it. (ii) **`reviewBy` read-don't-sort fired a third time and for the first time returned a date *nearer* than the sort** — 2026-10-01 out of `southern-company`'s policy prose (an Alabama 150 MW threshold statute) against 2027-01-01 from the only future `effectiveDate` in 76 entries — so this is **the first module to ship inside its own 30-day review horizon**, and `check-classroom-curriculum.py` now reports 1 item due while still exiting 0, because review dates never call `strict()`. (iii) **A role label can be a structural placeholder rather than a claim.** The registry's own `notes` says regulated franchises have no challenger by construction; the two carrying the role rank first and third by revenue and are, in ERCOT, the wires incumbent's two largest customers at 25 and 21 per cent of its 2025 revenue. Read `notes` before sizing a threat section. (iv) **The recorded-not-taken `read-next` link item is NOT S2's** — `clFmt` has no link token at all, so linking is a `Classroom.html` feature; and the string is emitted in every segment, so a generator edit would make all nineteen due while §10.9 lets a session regenerate only its own. Reassigned to a session that opens the page. **The brief carried two slips, both recorded so they stop here:** it named this table's row as order 6 (order 6 is Phase 4; S2 is order 5), and it named `SCRAPER_SEGMENT_SEEDS` as the array to check against when the rule and every guidance-derived precedent use `SCRAPER_INTEREST_TOPIC_SEEDS` — the check was run against both regardless, so it cost nothing. The signature was **P1 ×3, P2, P7 ×1, no P3**, and **no changelog rotation fired** because the push landed on the EST day it was written, with `Scrapergs.changelog.md` one short of its trigger for the second session running. Brief was §7.23; §7.24 written in the same commit. **Session 5 is DONE — v05.83r, 2026-09-15.** `landscape-power-conversion-and-rack-power-silicon-2026-09`: nine §10.6 sections, **twelve** `each-players-bet` rows, a **60-row** claims ledger, `reviewBy` **2026-11-27**, Scraper seed `topic-power-semiconductors`, and one `--segment` regeneration. The segment re-measured on the day at **20 members (9 · 3 · 8)**, confirming §7.25 member for member — the fourth consecutive session to do so. **Four findings the next S2 session inherits.** (v) **A landscape whose own segment is ALREADY due cannot raise the due count, and four briefs had carried the wrong model.** §7.25 predicted 8 → 9 → 8 on sessions 1–4's arithmetic; the measurement was **8 → 8 → 7**, because in those sessions the session's own `segment-*` lesson was *not* in the standing due set and here it was the first of eight. Registering the module changed its `read-next` without changing its membership; regenerating then cleared it. **Check whether your own segment is already due before predicting: if it is, the arithmetic is N → N → N−1.** (vi) **The split rule fired a second time and produced the sharpest overlap yet — total rather than partial.** `nvidia-800vdc-2026-08` covers the *architecture* of the exact layer this segment is defined as, so the segment's own one-sentence definition reads as that module's table of contents. The line: **that module owns the architecture and is deliberately vendor-blind; this one owns the parties.** §7.23's test applied cleanly — the shared number is **~2029**, a *roadmap gate* there and a *position clock* here. **Six things that module owns are enumerated as deliberate omissions** in the function's header comment so a later revision cannot import them. With `eo14420-bulk-power-2026-08` and `china-policy-stack-2026-08` owning the manufacturing-location criterion, this is the **first landscape with three modules bearing on its segment**. (vii) **The Scraper-seed prior was wrong, and running the check properly is what showed it.** §7.25 expected no; against **390 distinct terms across both arrays** the wide-bandgap silicon layer and the last centimetre scored **zero** — two of the five sub-layers the segment's own definition names. One seed added, re-scoped once before landing (`bus converter` and `power semiconductor` dropped as duplicates of `converter` and `semiconductor`). **State a prior in a brief as a prior, not as an answer.** (viii) **The armed `Scrapergs.changelog.md` rotation fired, and the EST roll is what armed it** — three sections dated 2026-09-14 stopped being exempt overnight, taking it from the 49 non-exempt three briefs had reported to **52 against a cap of 50** before this session touched anything. Twenty-six sections dated 2026-08-28 moved; all 26 SHAs resolved on the deepened clone. **The enrichment failed silently on the first attempt and only the mandatory post-rotation grep caught it.** (ix) **The v05.59r deploy poll fired for the first time, on the Scraper leg, and it was needed** — seven briefs had recorded it as armed and never used. Two re-reads resolved it at `Scraper deploy confirmed (POLL 2): Already up to date (v02.06g)`; without it that leg would have read as a failure on a deploy that had succeeded. **`Already up to date` consumes no deployment**, so Scraper stayed at 150/200 against a 151/200 forecast — forecast a counter from whether the running bundle is behind, not from whether the file changed. Classroom confirmed on the first GET leg at 51/200 with no POLL and no probe, the eighth consecutive clean use of the §7.19 order. The signature was **P1 ×4, P2, no P3**. Brief was §7.25; §7.26 written in the same commit and its counters refreshed in both halves at v05.84r. **Session 6 is DONE — v05.88r, 2026-09-15.** `landscape-aidc-developers-and-landlords-2026-09`: nine §10.6 sections, **twenty-two** `each-players-bet` rows — the second-largest bets table in the corpus, behind session 3's twenty-seven — a **75-row** claims ledger, `reviewBy` **2026-12-15**, Scraper seed `topic-aidc-landlords`, and one `--segment` regeneration. The segment re-measured on the day at **30 members (9 · 13 · 8)**, confirming §7.27 member for member — the **fifth consecutive** session to do so. **Four findings the next S2 session inherits.** (x) **A landscape can have to split against a public LESSON, and that split crosses an access tier rather than only a subject.** `the-campus-as-a-power-project` (Phase 4 row 9) is built on nine of this segment's own members and `how-a-storage-project-happens` touches two more, so this segment has **four** neighbours against session 5's three. The line taken is one sentence — **the lesson teaches the test, this module applies it to named parties** — and it matters more than a module split because the lesson is *analyst-visible* while the module is *contributor-only*: material drifting either way crosses a gate rather than a topic boundary. **The neighbour pre-declared the handoff**: one of that lesson's own tiles reads *0 league tables — no ranking of these landlords exists in this corpus and this lesson does not invent one*, which is layer 4's job by construction. §7.23's test held on **the pre-leased share falling 64 % → 54 % while capacity underway rose 82 %**: an *arithmetic exercise* there (convert a percentage into exposure) and a *solitary market gauge* here (the only speculative-build number the segment has). **Nine omissions across all four neighbours** are enumerated in the function's header comment. (xi) **The netting rule held on its second run, and the deepening did not fire.** `--check` went **8 → 8 → 7** exactly as §7.27 predicted, with this segment already the sixth of the eight; it differed in `read-next` alone before and after, so row 10's warning that an already-due segment can also *deepen* is a real possibility rather than a regular one — **predict both halves, and record when only one fires**. (xii) **The `reviewBy` rejection class session 5 established fired on the NEAREST candidate rather than a competing one, and it fired twice over.** The genuine nearest gate — **10 December 2026**, the ERCOT audit report to the PUCT, carried by three members' prose — is already the `reviewBy` of **two** registered modules, `utility-aidc-procurement-2026-08` (whose own Texas section names that completion target as the live wildcard) and `power-infra-aidc-2026-08`. It went to `the-indicators` as row 1. A **second** candidate, PJM's 16 September comment deadline, was rejected twice over: one day after `updated`, *and* squarely `large-load-interconnection-2026-09`'s subject. The date taken, **2026-12-15**, is Virginia's tax-policy study, read out of a member's policy **prose** while that entry's `effectiveDate` holds the consumption tax's 1 July start — and a sort of the fence would have returned **2027-12-31**, a tariff expiry, because exactly two of 84 dated entries across 107 are in the future and both are the same Section 232 schedule. (xiii) **The Scraper-seed prior was HALF right, which is a third outcome the rule had not recorded.** §7.27 named five candidates; run against all **392** terms, two were already covered — the co-location statutes (`co-location`, `behind-the-meter`, `large load`, `interconnection`, `substation`, `ratepayer` are all exact existing terms) and the buildout shape — while the segment's own **commercial** layer scored zero across the board: `powered land`, `land entitlement`, `build-to-suit`, `data center ABS`, `tenant credit`, `take-or-pay lease` and `bitcoin miner`. Two of its six buying criteria sat entirely unscored. Seven terms seeded; `special contract` and `energy service agreement` score zero and were **left out on split grounds rather than duplication grounds**, because they are `utility-aidc-procurement-2026-08`'s instruments and seeding them here would blur in the digest the line the module draws in the curriculum. The signature was **P1 ×7, P2 ×9, no P3 and no P7** — the regeneration's appended revision entry did not trip P7, which three briefs had expected it to. **(xiv) THE DEPLOY CORRECTED A RULE THREE BRIEFS HAD CARRIED, and arithmetic rather than log-reading is what caught it.** Both legs fired and both confirmed on the first GET with no POLL and no probe — `Scraper … Updated to v02.07g (deployment 152) | 152/200` and `Classroom … Updated to v01.42g (deployment 53) | 53/200`, the **tenth consecutive** clean use of the §7.19 order. Classroom's counter was exact; **Scraper's came in one ABOVE the 151/200 forecast.** Reading the chain back to the last measured anchor: **150/200 at v05.78r** → v05.83r's leg (poll-resolved `Already up to date`) → v05.85r's step never fired → **152/200 here**. One `Updated to` accounts for one; **one consumption is unaccounted for**, and the only Scraper deploy activity in between was v05.83r's. **So §7.26's middle case is wrong as stated**: `Already up to date` is the state the deployment reports at **read** time, not a statement that the call spent nothing — v05.83r's *unconfirmed first GET leg had already run `pullAndDeployFromGitHub()`* and consumed a deployment, and the poll then read back a bundle that was by then current. **The correction, stated as the inference it is**: `Updated to` consumes one; a step that never fires consumes none and logs nothing; **`Already up to date` is silent on consumption and must not be forecast as zero.** The v05.84r conclusion was inferred from wording and never measured across the event; this is the first measurement that spans it. Brief was §7.27; §7.28 written in the same commit and its counters refreshed in both halves at v05.89r. **Session 7 is DONE — v05.93r, 2026-09-15.** `landscape-in-hall-power-2026-09`, the **sixteenth guidance module** and the **seventh landscape**: nine §10.6 sections, **seventeen** `each-players-bet` rows, a **45-row** claims ledger, `reviewBy` **2026-10-01**, Scraper seed `topic-in-hall-power`, and one `--segment` regeneration. The segment re-measured on the day at **27 members (10 · 7 · 10)**, confirming §7.29 member for member — the **seventh consecutive** session to do so, and the third-deepest segment in the corpus. **Five findings the next S2 session inherits.** **(xv) ELEVEN neighbours — six of them public lessons — and the organising finding is that ALL SIX of the segment's buying criteria have their mechanism owned by one of them.** No landscape has met that before: criteria 1 and 3 are `the-ups-room`'s, 2 is that lesson's and `the-aidc-power-chain`'s, 4 is that lesson's and `bess-tech-fundamentals-2026-08`'s, 5 is `nvidia-800vdc-2026-08`'s and `the-800-vdc-shift`'s, 6 is `grid-equipment-shortage-2026-09`'s. So the module owns **none of the six tests** and only says who sits where on each — which is what a layer-4 read is, stated more sharply than any previous landscape has had to state it. **Nineteen omissions** across the eleven are enumerated in the function's header comment, against session 6's nine. **(xvi) THE FIRST LANDSCAPE-AGAINST-LANDSCAPE SPLIT, and the line is a ROLE INVERSION rather than a subject.** **Eight members sit in both this segment and `power-conversion-and-rack-power-silicon`, and five carry a different role in each** — `schneider-electric`, `eaton` and `abb` are incumbent here and adjacent there; `delta-electronics` is adjacent here and incumbent there; `heron-power` is adjacent here and challenger there. **The registry itself draws the split**, so it needed no arbitration: that module ranks the conversion layer, this one ranks the room. §7.23's test held on **`late 2027`**, `heron-power`'s mass production — an *arrival risk on a challenger's own business* there (its dossier calls a one-year slip fatal), a *reprieve clock on an installed product category* here. **(xvii) THREE public neighbours PRE-DECLARED the handoff, and reading them first is what made the split cheap.** §10.6 (t) paid three times over: `the-ups-room`'s `the-shelf-that-threatens-the-room` closes on a paragraph headed *"The honest limit, and where this hands off"* and states that what is in play is **the marginal megawatt in new AI halls** — which became the module's brief and a glossary term; `redundancy-by-the-numbers`'s `the-vanishing-ups` says *"This lesson deliberately does not describe the machine being deleted"*; and `where-bess-plugs-in`'s `the-honest-no` says the answer on socket 3 is **"No"** and then says **"Keep watching it anyway"** — so the module's storage-seller path is written as *what the watching looks like* rather than as competing advice. **(xviii) `reviewBy` read-don't-sort produced a FIFTH distinct failure mode and a NEW FIELD.** 81 policy entries, 29 dated, **exactly three in the future** — and the nearest, `2027-01`, is an adjacent member's **on-highway engine model year**, so a sort would have clocked an in-hall-power module on a truck. The date taken, **2026-10-01**, was read out of `samsung-sdi`'s `policyExposure[0].`**`mitigation`** — the first gate any session has found in that sub-field — on an entry carrying **no `effectiveDate` at all**, and its own `strategyRead` calls it *the pivotal execution item*. **Five rejections in writing**, including `caterpillar`'s CFO retirement **on the same calendar day** (the trap of taking the nearest *date* rather than the nearest *gate*) and `nvidia-800vdc-2026-08`'s own clock under (r). The module ships **inside its own 30-day horizon**, so the curriculum checker now reports **2 items due** rather than 1, still exiting 0 — session 4's (k) firing a second time. **(xix) The Scraper-seed prior resolved YES, and the gap it found was a whole HALF of the segment.** Run against all **399** terms across both arrays per (i), most of the vocabulary was already covered — and by **five different segment seeds** rather than one — but the **rotary machine, the store under the machine and the packaging axis scored zero**: `rotary ups`, `drups`, `flywheel ups`, `flywheel`, `modular ups`, `isolated parallel bus`, `eco-mode`, `vrla`, `transfer switch`, `critical power`, `grid-interactive ups`. **That gap is buying criteria 2, 3 and 4** — the three whose mechanism the public lesson owns and whose parties nobody was watching. Four terms dropped as superstrings of existing ones, and **three dropped on split grounds** per (v) (`supercapacitor`, `capacitor backup unit`, `panama architecture` — all `nvidia-800vdc-2026-08`'s). **The due-count arithmetic was 10 → 10 → 9 exactly as predicted, and BOTH halves were called correctly**: the netting rule of (p) held on its **third** run, and the row-10 deepening **did not** fire on any other segment — so the rule now reads 2-for-3 on deepening (row 11 fired, sessions 6 and 7 did not). The signature was **P1 ×4, P2, no P3 and no P7** — a second consecutive session in which the regeneration's appended revision entry did not trip P7, which briefs had expected twice. Content checker held at **0 errors / 0 warnings at 41 lessons**, module count moved 15 → 16 in the same commit. Brief was §7.29; §7.30 (Phase 4 row 12) written in the same commit. **Session 8 is DONE — v05.97r, 2026-09-15.** `landscape-grid-equipment-2026-09`, the **seventeenth guidance module** and the eighth landscape: nine §10.6 sections, a **six-row** `each-players-bet` table — the shortest in the corpus — a **50-row** claims ledger, `reviewBy` **2026-10-31**, Scraper seed `topic-landscape-grid-equipment`, and one `--segment` regeneration. The segment re-measured on the day at **15 members (5 · 1 · 9)**, confirming §7.31 member for member — the **eighth consecutive** session to do so. **Five findings the next S2 session inherits. (xx) THE FIRST SEGMENT WITH A BUILT LANDSCAPE ON BOTH SIDES OF IT, and both splits are role inversions pointing in OPPOSITE directions** — eight members shared upstream with five inverting (three of this segment's incumbents are *adjacent* in the conversion layer), six shared downstream with four inverting the other way. Each landscape ranks the members whose centre of gravity sits at its own chain position, so with a neighbour on each side the roles carry a **direction** and not only a line. **(xxi) §10.6 (t) FIRED ON A MODULE RATHER THAN A LESSON**: `grid-equipment-shortage-2026-09` is a module about this segment's own scarcity — its title *is* this segment's first buying criterion — and its own buyer callout pre-declares the handoff (*the queue position is the thing being bought, not the transformer*). §7.23's test held on the **transformer lead time**: a market condition there, one named vendor's own quoted wait travelling with that vendor's own reservation instrument here. **Thirty-five omissions** across six neighbours are enumerated, against session 7's nineteen across eleven — fewer neighbours, deeper overlap. **(xxii) THE FIRST LANDSCAPE WRITTEN AGAINST A HALF-UNBUILT CURRICULUM**: `READ_NEXT` names four lessons and only two are built, so **two of the six buying criteria have no complete owner** and the module names the gap rather than appointing itself to teach it. **(xxiii) A ONE-CHALLENGER THREAT SECTION IS WRITABLE, and the registry's `notes` is what makes it writable** — §10.6 (l) firing a second time. The note says the adjacent role was reserved and filled with **buyers**, so the nine adjacents split into six suppliers carrying the routes and three buyers who are the demand; the single challenger is **the only member of the fifteen that belongs to no other segment** and **buys its interrupting elements from four of the five incumbents it is listed against**. The module's judgment is that the threat is capacity entering from outside the segment rather than share moving inside it. **(xxiv) `reviewBy` produced session 3's (g) case a SECOND time — a sort returning NOTHING AT ALL** (41 policy entries, 25 dated, none in the future; five members carry no policy array), with the date read out of `grid-united`'s **`strategyRead[4]`**, a new sub-field, and **six** rejections in writing. **The due-count arithmetic was 10 → 11 → 10 with BOTH halves called correctly, and the section half was DERIVED rather than guessed** — `self.landscapes` is read at exactly one place in the generator, inside `sec_read_next`. The signature was **P1 ×4, P2, no P3 and no P7** — a third consecutive session in which the regeneration's appended revision entry did not trip P7. Content checker held at **0 errors / 0 warnings at 42 lessons**, module count moved 16 → 17. Brief was §7.31; §7.32 (Phase 4 row 13) written in the same commit. **Session 9 is DONE — v06.03r, 2026-09-15.** `landscape-bridge-and-on-site-generation-2026-09`, the **eighteenth guidance module** and the ninth landscape: nine §10.6 sections, a **thirteen-row** `each-players-bet` table, a **62-row** claims ledger, `reviewBy` **2026-12-31**, Scraper seed `topic-landscape-bridge-and-on-site-generation`, and one `--segment` regeneration. The segment re-measured on the day at **17 members (8 · 5 · 4)**, confirming §7.33 member for member. **(z1) §10.6 (t) fired on four whole sections of two public lessons**, which between them teach five of the six buying criteria and name not one vendor. **(z2) A public lesson's own classification mapped one-for-one onto the challenger bench** — *exactly three ways out of a factory queue* — so session 6's (s) generalises to **the lesson teaches the CATEGORIES and the module FILLS them**. **(z3) Six built landscape neighbours, and the inversion rate is bimodal** — 100 % where a shared member's business *crosses* the boundary, 0 % where it *straddles* it. **(z4) The sort returned a TRAP rather than nothing** — the one future date applies to on-highway truck platforms — and the date taken rested on a **convergence** of five members for the first time. **(z5) The first deliberate acceptance of a calendar day another module already carries** (a different bell, not the same one). **(z6)** `--check` went **10 → 11 → 10**, both halves called correctly, the section half derived. **(z7) `section 232` was DECIDED rather than deferred a third time** and stays out on a sharpened ground. Content checker held at **0/0 at 43 lessons**, module count **17 → 18**. Brief was §7.33; §7.34 written in the same commit. **Session 10 is DONE — v06.07r, 2026-09-16.** `landscape-clean-firm-and-nuclear-2026-09`, the **nineteenth guidance module** and the tenth landscape: nine §10.6 sections, a **five-row** `each-players-bet` table — the shortest in the corpus and honest, because three incumbents and two challengers is the entire ranked roster of a sixteen-member segment — a **47-row** claims ledger, `reviewBy` **2026-12-31**, Scraper seed `topic-landscape-clean-firm-and-nuclear` (thirteen terms), and one `--segment` regeneration. The segment re-measured on the day at **16 members (3 · 2 · 11)**, confirming §7.35 member for member — the **tenth consecutive** session to do so. **Five findings the next S2 session inherits. (aa1) THE SPLIT IS DRAWN BY A LESSON THAT DOES NOT EXIST YET, which no previous landscape has faced.** `contracts-and-revenue` — public, built, stamped on all three incumbents — carries a section `clean-firm-and-the-restart` whose opening sentence hands the physics off *by name* to *Clean Firm Power*, **which is §7 row 16 and unbuilt**. So a built public lesson pre-declared a handoff **to a lesson that does not exist** and the declined material is in nobody's hands. Session 6's (s) generalises again: from *the lesson teaches the test and the module applies it to named parties* to **a third posture — say the test is unowned and decline to own it.** §7.23's test held on **the uprate attached to a supply agreement**: an arithmetic worked example of additionality there, naming nobody, and a **position marker** here that sorts the three incumbents. **Twenty-two omissions** across **eight** neighbours are enumerated. **(aa2) THE EMPTY `notes` WAS ANSWERED BY MEASUREMENT, AND THE MEASUREMENT IS A NEW INSTRUMENT.** Session 8's (y4) route needs a registry note and this segment has none, so the 69 % adjacent share was read off the memberships instead: **eleven of the eleven adjacents are ranked — incumbent or challenger — in another segment, eleven out of eleven with no exception**, and **the two challengers are the segment's only pure plays**, the only two of sixteen belonging to no other segment. 19 shared memberships across seven neighbours, **14 inversions (74 %), the highest rate measured** — but the finding is that the direction is **UNIFORM** rather than position-dependent (session 8's (y1)) or bimodal (session 9's (z3)). **The new instrument: when a segment's adjacents are 100 % ranked elsewhere, the segment is a PRODUCT other industries make, not a LAYER other industries sell into.** **(aa3) `reviewBy` PRODUCED A SIXTH FAILURE MODE — THE TWO REJECTION CLASSES INTERSECTING TO EXHAUST THE FIELD.** 67 policy entries, 49 dated, **exactly one in the future** — a Virginia large-load rate class, another lane's subject and **already four modules' `reviewBy`**. Every genuinely on-subject gate is then either **month-level only** (X-energy's Long Mott final safety evaluation, November 2026 — itself a *tell* rather than a gate) or lands on **a day another module already holds** (its month-end on `nvidia-800vdc-2026-08`'s 2026-11-30; the Long Mott construction permit's quarter-end on `grid-equipment-shortage-2026-09`'s 2027-03-31). So **2026-12-31 is the THIRD module to carry that day**, taken on a four-way convergence in `fermi-america`'s `policyExposure[0].exposure` — an entry with **no `effectiveDate` at all** — and recorded rather than absorbed. **Eight** rejections in writing. **(aa4) §7.35's OWN P7 CLAIM WAS FALSE, AND THE INSTRUCTION TO VERIFY IS WHAT CAUGHT IT — the third consecutive brief to carry a false assertion.** The brief said this segment was last regenerated 2026-09-16 EST by row 14; the literal read **`updated: 2026-09-14`** (row 14 regenerated `grid-equipment` and `bridge-and-on-site-generation`, not this one). So **P7 could not fire** and did not. **Never take a measurement from a brief; read the literal.** **(aa5) THE NETTING CASE WAS TRACED TO ITS CAUSE FOR THE FIRST TIME.** `--check` went **10 → 10 → 9** with `read-next` alone at every step and the section half **derived** from the generator (`self.landscapes` is read at exactly one place, inside `sec_read_next`) — but the segment was already due for a reason unrelated to this module: `the-campus-as-a-power-project` was built *after* that segment's last regeneration, so `lesson_ref` had been rendering a **built** lesson as `(planned)`. The registration then added a second change to the same section. The three-part test of row 14 was run and passed cleanly (`clean-firm-power` ∈ `READ_NEXT` ∩ `CRITERION_LEXICON` ∩ a keyword hit on criterion 2), and it is irrelevant to a landscape because a landscape moves none of its three inputs. The signature was **P1, P2, no P3 and no P7**. Content checker held at **0 errors / 0 warnings at 44 lessons**, module count moved **18 → 19**. Brief was §7.35; §7.36 (Phase 4 row 15) written in the same commit. **FOURTEEN OF NINETEEN ARE BUILT AS OF v06.28r; the five remaining are `cooling`, `compute-and-the-rack`, `assurance`, `software-and-optimization` and `insurance-and-risk-transfer`** — all five in the standing `--check` due set, so each is a *clear* case whose session takes the count down rather than holding it. |
| — | **Segment regeneration** — a one-off developer run of `scripts/build-classroom-segments.py --all` | **Opus 5 xhigh** | 1 | S3 closed | **DONE — v05.62r, 2026-09-14.** Not a numbered order and not an authoring session: no lesson, no landscape, no guidance module, no dossier, no report, no track — the generator wrote all nineteen `segment-*` literals and nothing else was hand-edited inside the content fence. Run under curriculum plan §10.4's "cleared in a single developer run whenever convenient", and taken now because the backlog's **cost was in the checker, not the lessons**: twelve segment lessons were failing `check-classroom-content.py`'s membership assertion at two errors each, so **every Classroom commit was proving itself against a 24-error baseline** — exactly how a 25th error hides. **24 errors → 0 errors / 0 warnings**, `--check` 19 due → **0 due**, and **20 distinct companies restored** to the rosters that should have listed them; `insurance-and-risk-transfer` had carried no player rows at all and now carries three. §10.4's recorded split of "5 substantive, 14 bookkeeping" (measured 2026-09-09, before S3 closed) was re-measured at **12 substantive, 7 bookkeeping** and corrected in place. `check-classroom-pipeline.py --base origin/main` reported **P10 alone** — nineteen revised lessons against a cap of 3, breached by design on a developer regeneration run and accepted by §10.4 — and **no P3, no P5, no P7, no P8**: the generator touches no gate symbol (so `gateDigest` was correctly left alone), inserts nothing into a registry, moves no pin backwards, and writes its own `revisions[].changed[]` as the differs set. **This does not retire the per-segment regeneration S2 owes** — order 5 above states why |
| 6 | **Phase 4 rows 2–24** — mechanism lessons | **Opus 5 xhigh**, one per session | 23 | row 1 | Interleaves with S2 — different files. Rows 23–24 after the bankability review (order 3). **Row 2 (`how-a-utility-buys`) done — v05.54r, 2026-09-13**, the second-ever mechanism lesson and the last track-creating one: `market-access` registered at `clTracks_()` **position 5** with the `prereqs[]` §2.2 promises, so **every track the curriculum plan specifies now exists (32 lessons, 8 tracks)** and rows 3–24 are pure lesson authoring with no registry shape left to change. Brief was §7.13, written into this file in the same commit. **Row 3 (`backup-generation`) done — v05.55r, 2026-09-13**, the first Phase 4 row that authors *only* a lesson: no track created, no `prereqs[]` written, so the run reported **P1 and P5 alone** — P5 for the `lessons[]` insertion at the §4 position rather than an append, which is what a developer session does and what a pipeline run may not. Brief was §7.14, written in the same commit per the obligation §7.13 sets. **Row 4 (`the-ups-room`) done — v05.57r, 2026-09-13**, the second pure lesson row and the second mid-track insertion: nine sections on nine public stamps, inserted at `lessons[]` position 5 of `aidc-grid-to-chip` per §4, so the run again reported **P1 and P5 alone — no P3**. Its screenshot pass found a rendering defect no checker sees — a `proscons` card's `t` and `meta` are `textContent`, so a `{{term}}` in either prints its braces to the reader — fixed in this lesson, found already shipped in four others, and handed to the next run with the mirror check in §7.15 rather than bundled here, because fixing another row's literal would add a P8 finding and adding the check before those fixes would break the pristine-HEAD error baseline. Brief was §7.15, written in the same commit. **Row 5 (`the-transformer-and-the-substation`) done — v05.61r, 2026-09-14**, the third pure lesson row and the **first that appends**: `electrical-foundations` holds only `four-machines` today, so the §4 position 3 is an append among the built lessons and the run reported **P1 and P8 — no P5, no P3**, breaking the P1+P5 signature rows 3 and 4 set. Seven sections on nine public stamps, each date read off the document fetched that run. It also took the standing job §7.15 handed it: the **nine shipped plain-field markup occurrences** were fixed first and the **mirror check** added to `check-classroom-content.py` second — that order matters, because the check errors on them and the content checker's error count must stay byte-identical to a pristine `HEAD` (24 errors, 0 warnings, before and after). The check was implemented **wider than §7.15's list**, at every `textContent` call site the renderer actually has — `revisions[].note` and a track's `title`/`short` as well — after a corpus-wide scan confirmed the wider scope finds exactly the same nine and no more. Brief was §7.16, written in the same commit. **Row 6 (`who-buys-storage`) done — v05.67r, 2026-09-14**, the fourth pure lesson row and the **first to insert at the head of a track**: `market-access` held only `how-a-utility-buys`, so the §4 position 1 is an insert ahead of it and the run reported **P1 and P5 — no P3, no P8**, restoring the signature rows 3 and 4 set and that row 5's append broke. Seven sections on **nineteen of the thirty-one candidate stamps**, each date read off the document fetched that run; the **twelve fetched-and-not-pinned are recorded with one shared reason** — they teach an instrument or a lifecycle stage, which §3.5 assigns to rows 10 and 11, not a buyer class. It took the **id-permanence exception §3.5's own v05.07r scope line instructs** (*Eleven Buyer Classes*, `eight-classes` → `the-classes`) and read the eleven classes off the roster's `categories` array on the day. **Its `the-classes` table is five columns by eleven rows — the widest any mechanism lesson has carried — and it rendered cleanly at 1400px**, so the open `.cl-tbl` `min-width` question stays recorded-not-taken and gains a second data point against column count being the cause. **One finding no earlier row could surface:** registering a lesson named in a segment's `READ_NEXT` list makes that segment due, because the generator renders an unregistered lesson from `PLANNED_TITLES` with a *(planned)* suffix. `build-classroom-segments.py --check` went 0 due → **1 due** (`storage-developers-and-ipps`, one string in `read-next`, no pin moved) and was **not regenerated** — §10.9 gives `segment-*` literals to S2. Rows 1–5 created the same staleness invisibly because the whole roster was already due until the v05.62r sweep; this is the first Phase 4 row to run against a clean 0-due baseline. **1 due is row 6's expected residue, not a defect.** Phase 4 is **6 of 26**. **Row 7 (`inside-the-rack`) done — v05.72r, 2026-09-14**, the fifth pure lesson row and the **third mid-track insertion**: `aidc-grid-to-chip` held six built lessons and the §4 position 8 falls **between `the-ups-room` and `the-800-vdc-shift`**, so the run reported **P1 and P5 — no P3, no P8**, the signature rows 3, 4 and 6 set. Eight sections on **all ten** of its candidate stamps, each date read off a document fetched that run; nothing was dropped, because every one of §3.3's ten inputs is load-bearing in a named section — so the omission record moved **one level down**, to the candidate *sections* fetched and not drawn on (flex's last-hundred-metres pair, upstream of this lesson's fence; vicor's three topology sections, which no §3.3 section asks for; the two industry-structure sections, which belong to the segment lessons). **`reviewBy` took the six-month default with its two near-candidates rejected in writing** — a next-generation platform stated as *H2 2026* and megawatt racks *projected from ~2027* are windows, not dates, the same test S2 session 2 applied to "late 2026". **Row 6's `READ_NEXT` finding sharpened: `--check` went 1 due → 3 due**, because this lesson sits in **two** segments' `READ_NEXT` lists and the second of them, `compute-and-the-rack`, differs in a **second** section — `what_is_bought` calls the same `lesson_ref()` helper for its *Taught in* column. And because `PLANNED_TITLES["inside-the-rack"]` was already byte-identical to the authored title, **the whole diff is the disappearance of the " (planned)" marker**; no pin moved, nothing was regenerated. **`psu-to-die` is 5 × 6 and rendered clean at 1400px**, a fourth data point against column count causing the open `.cl-tbl` question. Phase 4 is **7 of 26**. **Row 8 (`redundancy-by-the-numbers`) done — v05.76r, 2026-09-14**, the sixth pure lesson row and the **fourth mid-track insertion**: inserted at `aidc-grid-to-chip` `lessons[]` **index 5, immediately before `inside-the-rack`** and after `the-ups-room`, so the run reported **P1 and P5 — no P3, no P8**, the signature rows 3, 4, 6 and 7 set. Eight sections at §3.3's ids in its order on **all eleven** stamps, each date read off a document fetched that run. It is the first Phase 4 row whose outline called for a **`bars`** section and the first to render `where-it-fails` as a **table** rather than a callout; both rendered clean at 1400px, taking the open `.cl-tbl` question to a sixth and seventh data point. **`reviewBy` took the six-month default (2027-03-14) with two candidates rejected in writing** — a nuclear milestone stated as *late 2027* on a topic no section teaches, and one campus's 2028 completion, which is a project schedule rather than a rule taking effect. **The brief's due-count prediction was right in range and wrong in mechanism, and the mechanism is the transferable half.** `--check` went **2 due → 5 due**, not the 7 the two-channel reading allowed: `sec_what_is_bought` guards its lexicon match with `if lid in eligible`, where `eligible` is that segment's own `READ_NEXT` list — so **the buying-criteria lexicon is not a second independent channel, it is subordinate to `READ_NEXT`.** A new lesson can only touch segments whose `READ_NEXT` already names it (`in-hall-power`, `aidc-developers-and-landlords`, `neoclouds`); the lexicon decides only whether such a segment differs in one section or two. `in-hall-power` differed in two because its criterion carries "redundancy topology"; `grid-equipment` and `hyperscalers-and-ai-labs` could never have gone due however well "availability" matched, because this lesson is not in their `READ_NEXT`. **The bound for every future Phase 4 row is therefore |READ_NEXT membership| + prior residue.** `PLANNED_TITLES` already held the authored title byte-identically, so the whole diff on each is a " (planned)" marker disappearing; nothing was regenerated. Phase 4 is **8 of 26**. **Row 9 (`the-campus-as-a-power-project`) done — v05.80r, 2026-09-15**, the seventh pure lesson row and the **fifth mid-track insertion**: inserted at `aidc-campus` `lessons[]` **position 2 of the built list — between `heat-is-the-constraint` and `where-bess-plugs-in`**, the §4 position 3, so the run reported **P1 and P5 — no P3, no P8**, the signature rows 3, 4, 6, 7 and 8 set. Seven sections at §3.4's ids in its order on **all thirty** of its candidate stamps, each date read off a document fetched that run. **The brief's input count was one short and the contract is the arithmetic**: §3.4's base list carries eight `study:` refs and its v05.07r amendment adds eleven, so the roster is **nineteen study guides and thirty inputs**, not eighteen and twenty-nine. It took the **instructed id/title rename** (`the-named-projects`, the title dropping the count, the lesson id unchanged) and read **nine** projects off the registry on the day. Its `the-named-projects` table is **six columns by nine rows — the widest any mechanism lesson has carried by column count** — and rendered clean at 1400px, a tenth data point against the open `.cl-tbl` question and the first at six columns. `reviewBy` took the six-month default (2027-03-15) with three candidates rejected in writing: a lease-closing condition, a lender's covenant date, and two project delivery schedules — the last of which the lesson's own claim predicts will slip. **The due-count prediction was measured and held exactly: `--check` went 5 due → 8 due**, seven of the eight differing in `read-next` alone, and the predicted title mismatch against `PLANNED_TITLES` appeared as described; nothing was regenerated. **The finding the next rows inherit is about the clone, not the curriculum.** A fresh container's shallow clone makes its boundary commit look like it introduced every file, so `git log -1 --format=%cs -- <path>` returns the boundary's date: `--check` read **19 due** until `git fetch --unshallow` restored the true pin, and authoring off the shallow clone would have written `concepts:profiler-concepts@2026-09-14` when the true pin is 2026-09-13 — a silent freshness bug no checker sees. **Unshallow before taking any pin, not just before a CHANGELOG rotation.** It also **reassigned a recorded item rather than deferring it**: §7.24 offered `the-800-vdc-shift`'s stale `three-chains` intro on the grounds it sits in this row's track, and it does not — it is in `aidc-grid-to-chip` while this lesson went into `aidc-campus`, and reading §4 forward **no remaining Phase 4 row acquires a claim on it either**, so it belongs to whichever session opens that lesson for its own reason. Phase 4 is **9 of 26**. **Row 10 (`how-a-storage-project-happens`) done — v05.85r, 2026-09-15**, the eighth pure lesson row and the **sixth mid-track insertion**: inserted at `market-access` `lessons[]` **position 2, between `who-buys-storage` and `how-a-utility-buys`**, the §4 position 2, so the run reported **P1 and P5 — no P3, no P8**, the signature rows 3, 4, 6, 7, 8 and 9 set. Seven sections at §3.5's ids in its order (`the-lifecycle` timeline of nineteen items across three lanes, `who-does-what-when` 4 × 8, `where-the-seller-enters` prose, two callouts, eight drill cards, five quiz items) on **all sixteen** of its stamps, each date read off a document fetched that run. **The brief's own instruction to re-derive the roster was the point and it paid: base eight plus amendment eight is sixteen, and §7 row 10 already listed exactly sixteen** — the two halves agreed, which is what row 9's did not. **Row 6's twelve-guide omission list became this row's input list**, precisely as §3.5 assigns it, so nothing was fetched-and-not-pinned at the input level and the omission record sits at the *section* level, where capital structure and revenue instruments belong to rows 11 and 25. `reviewBy` took the six-month default with **four** candidates rejected in writing. **Two findings the remaining rows inherit.** (i) **The due-count netting held exactly (7 → 8) and exposed a second channel the bound does not describe**: `storage-developers-and-ipps` joined, `epc-and-construction` was already due — and *also deepened*, from differing in `read-next` alone to differing in `read-next` **and** `what-is-bought-and-on-what`. Row 8's rule that the buying-criteria lexicon is subordinate to `READ_NEXT` **applies to already-due segments too**, so a prediction stated only as a membership delta under-describes the diff. (ii) **A track's `short` is not automatically extended by an insertion, and the test is what kind of sentence it is.** `market-access`'s is §3.5's verbatim finished-track outcome; `aidc-campus`'s is a running "So far: …" enumeration. Row 9 extended the latter and row 6 correctly left the former alone, so this row left it alone too and recorded the reasoning rather than repeating the judgement call. Its 4 × 8 table rendered clean at 1400px — a twelfth data point against the open `.cl-tbl` question and the first with a `{{term}}` span in the first column. **The deploy added a third case to the counter rule** (recorded at v05.86r): Classroom confirmed on the first GET leg at `Updated to v01.41g (deployment 52) | 52/200` with no POLL and no probe — the ninth consecutive clean use of the §7.19 order — while the **Scraper step never fired at all**, because each project's step opens with `git diff --name-only "$PRE" HEAD | grep -q "$GS" || exit 0` and `Scraper.gs` was not in the merge diff. So `Updated to` consumes one, `Already up to date` consumes none, and **a step that never fires consumes none and logs nothing** — forecast the counter from whether the bundle is behind, but forecast the log line from whether the file is in the diff. Phase 4 is **10 of 26**. Brief was §7.26; §7.27 written in the same commit and its counters refreshed in both halves at v05.86r. **Row 11 (`contracts-and-revenue`) done — v05.91r, 2026-09-15**, *Who Carries Which Risk* at `market-access` position 3, the seventh mid-track insertion of the phase. Seven §3.5 sections in its order; the `the-instruments` table carries **eleven rows against five columns** — the instrument, who pays for what, who carries price risk, who carries volume risk, who finances on it — and its closing note is the lesson's own argument: nine of the eleven rows trade one risk for the other, and the two that do not, the must-run and the reservation, are the two that finance nothing. **The input roster is FOURTEEN and the arithmetic had the trap §7.28 named**: §3.5's base list is seven and its v05.07r amendment lists eight, but `study:core-scientific` appears in both, so a straight addition over-counts by one; `profile:plus-power` and `study:plus-power` are two distinct refs at two layers and both belong. §7 row 11's own list carried **thirteen** — it holds `study:plus-power` but not `profile:plus-power` — and was corrected in the same commit, which is the third consecutive row on which a §7 roster and a §3.5 roster disagreed and the re-derivation settled it. All fourteen are public, so the stamp folds to `tracks`. **THE DUE-COUNT PREDICTION HELD ON BOTH HALVES, AND THE ROW-10 DEEPENING FIRED FOR THE FIRST TIME SINCE IT WAS DISCOVERED.** `--check` went **7 → 10** exactly as forecast: `capital` and `insurance-and-risk-transfer` joined differing in `read-next` **and** `what-is-bought-and-on-what`, `software-and-optimization` joined differing in `read-next` alone, and `storage-developers-and-ipps` — already due on `read-next` alone — **deepened to two**. The per-segment split was predicted from the buying-criteria lexicon before the run and measured against the registry afterwards: three criteria hit at `storage-developers-and-ipps`, one at `capital`, one at `insurance-and-risk-transfer` and **none** at `software-and-optimization`. Session 6's segment did not deepen; this one did, so §10.6 (w)'s obligation to predict *both* halves has now been tested in both directions and held in both. Phase 4 is **11 of 26**. Brief was §7.28; §7.29 written in the same commit. **Row 12 (`where-batteries-stop`) done — v05.95r, 2026-09-15**, *Long Duration, Sodium, and the Engine Boundary* at `bess-foundations` position 6, the ninth pure lesson row and the **second that appends** — position 5 (`how-a-cell-is-made`) is row 19 and unbuilt, so §4 position 6 is an append among the built four and the run reported **P1 alone — no P5, no P3, no P7 and no P8**. That corrects an expectation the brief carried: P8 iterates `set(base.lessons) & set(head.lessons)`, so it cannot fire on a lesson that exists only at head, and row 5's "P1 and P8" came from the *extra* job §7.15 handed it — fixing nine other lessons' shipped markup — not from appending. **The append signature is P1 alone.** **THE FIRST ROW ON WHICH AN AMENDMENT MOVED THE SECTION CONTRACT RATHER THAN THE INPUT ROSTER**: §3.1's v05.07r amendment names `iron-air-honestly`, an id its outline table does not contain, and the eighth section was **taken** — the amendment is §3.1's later statement, it names the kind by analogy to `sodium-honestly`, and refusing it would have left a pinned input with nothing to teach. Ten inputs, the **first roster in four rows on which §3.1 and §7 agree**, every pin read off the document fetched that run. **AND THE FIRST SECTION-HALF PREDICTION TO BE FALSIFIED — before the run, by reading the generator rather than the criteria.** §7.30 forecast `cells-and-chemistry` differing in both sections from its criteria's prose; `sec_what_is_bought` resolves only through `CRITERION_LEXICON`, whose entries name lesson ids, and **`where-batteries-stop` has no entry in it**. Measured: `--check` **9 → 10**, `read-next` **alone**, no deepening. So §10.6 (w) gains a third gate on top of rows 8 and 11: the membership half is bounded by `READ_NEXT`, **the section half by `READ_NEXT` ∩ `CRITERION_LEXICON`** — predict it from the lexicon's lesson-id column, never from the criteria's prose. **The EST/UTC trap fired for the first time in this run and decided the rotation**: EST read 2026-09-15 20:01 against UTC's 2026-09-16 00:01, so the sixteen same-day sections stayed exempt at 80 non-exempt and the brief's "very likely the rotator" did not hold. Phase 4 is **12 of 26**. Brief was §7.30; §7.31 written in the same commit. **Row 13 (`breakers-relays-and-faults`) done — v05.99r, 2026-09-15**, *Stopping Ten Thousand Amps* at `electrical-foundations` position 4, the tenth pure lesson row and the **third that appends** — position 2 (`string-versus-central`) is row 15 and unbuilt, so §4 position 4 lands after `the-transformer-and-the-substation` among the built two. **BOTH HALVES OF THE DUE COUNT WERE CALLED CORRECTLY AND THE SECTION HALF WAS A DEEPENING AT BIRTH — the first of its kind.** `--check` ran **10 → 11 → 10**: `grid-equipment` joined differing in **`read-next` AND `what-is-bought-and-on-what`** and the generator's own line confirmed it verbatim (`changed: read-next, what-is-bought-and-on-what; no pin moved`). Every prior deepening happened to a segment that was *already* due; this one arrived due on two sections at once, because `breakers-relays-and-faults` is the first authored lesson sitting in both a segment's `READ_NEXT` **and** in `CRITERION_LEXICON`, and criterion 4 names it. Row 12's gate held exactly as stated. **THE APPEND SIGNATURE WAS NOT P1 ALONE, AND THE REASON IS NEW**: `--base origin/main` reported **P7 on `segment-grid-equipment`** — *content changed but `updated` did not advance (2026-09-15 → 2026-09-15)* — because S2 session 8 regenerated that same segment **earlier the same EST day**. It is the first same-day double regeneration in the programme, it is unavoidable given the mandate to regenerate, and the only two ways to clear it were rejected in writing: writing 2026-09-16 would be the UTC date this repo explicitly refuses, and hand-editing a generated literal is forbidden by §10.4 and §10.9. **P7 on a `segment-*` lesson is pipeline-rule noise on a developer commit, the same class as P1's write-set findings** — the pipeline never writes a segment lesson by construction. No P5, no P3, no P8. **§8 item 8's boundary was honoured and the reverse pointer was DECLINED with a structural reason**: `the-aidc-power-chain` already teaches selective coordination in full, so the new lesson names the boundary and points forward at it twice, while the backward pointer is already carried by the track graph — `aidc-grid-to-chip` lists `electrical-foundations` in its `prereqs[]`, so a reader reaching that lesson has already been offered this one. Opening it to add a sentence would also have been a G4 wording change that P8 would correctly have failed. Six inputs, **re-derived rather than copied** (base four plus an amendment's two, overlap zero). **The EST/UTC trap fired for a THIRD consecutive session and decided the rotation again**: EST read 2026-09-15 21:42 against UTC's 2026-09-16 01:42, so the twenty same-day sections stayed exempt at **80 non-exempt** and the brief's "you ARE the rotator" did not hold. Phase 4 is **13 of 26**. Brief was §7.32; §7.33 written in the same commit. **Row 14 (`grid-stability-and-the-generator`) done — v06.05r, 2026-09-16**, *Inertia, Reactive Power, and Teaching Electronics to Lead* at `electrical-foundations` position 5, the eleventh pure lesson row, the **fourth that appends**, and **the lesson that closes the floor track** (position 2, `string-versus-central`, is row 15 and still unbuilt). **THE ROW THAT RESOLVED A CONTRADICTION BETWEEN TWO PARTS OF THE PLAN RATHER THAN OBEYING ONE OF THEM.** §3.2's outline is seven ids and its first two — `the-spinning-machine` and `frequency-is-a-meter` — are both `prose`, which **§8 item 6 forbids in as many words**. §7.34 wrote out three routes and preferred converting the second to `bars` or `timeline` **only if a source states the numbers**. It does not: the corpus carries no frequency-deviation series at all, and `study:power-electronics` says in terms that the quantification (*no inertia constant, no fault-current contribution, no RoCoF withstand*) **is published nowhere** — so `bars` could only have been invented, which §8 item 6 bars explicitly. Route (a) was taken: a **`table`** section, `two-knobs`, was **inserted between them**, the section count moved **seven → eight**, and the insert is pedagogically load-bearing rather than a spacer — `study:ge-vernova` states *one machine, two independent knobs* outright, and the callout two sections later names inertia, voltage support and fault current as three separate things a reader cannot keep apart without it. Its third row is the one the lesson turns on: **the mass is not a knob at all**. **THE FIRST PHASE 4 ROW TO REGENERATE TWO SEGMENTS**, because its id sits in both `grid-equipment`'s and `bridge-and-on-site-generation`'s `READ_NEXT`; `--check` ran **10 → 12 → 10** with **both halves called correctly**, `read-next` alone on each and no deepening. **But the brief's derivation of the section half was wrong and the correction matters**: §7.34 asserted the lesson is *not* in `CRITERION_LEXICON`, "checked entry by entry" — **it is**, keyed on `grid-forming`, `inertia`, `reactive`, `stability`, `black start`. `what-is-bought-and-on-what` held still for a different reason: **none of those five keywords appears in either segment's six buying criteria**, checked programmatically. So row 12's gate needs one more turn — the section half is bounded by `READ_NEXT` ∩ `CRITERION_LEXICON` **and then by an actual keyword hit against that segment's own `buyingCriteria` strings**, which is the only test that can be run without guessing. **The append signature was P1 alone and the P7 did not repeat**: both segments were last regenerated on 2026-09-15 EST and this push landed on 2026-09-16 EST, so both `updated` values advanced and row 13's finding stayed a one-off — checked by reading the two literals before the run rather than by waiting for the checker. Eight inputs, **re-derived and agreeing with §7 row 14** (base five plus the v05.07r amendment's three, overlap zero), every pin read off the document fetched that run. **§10.6 (t) paid a fifth time and this row pre-declared the handoff in BOTH a tile and a note** — *0 vendors named* — because two landscape modules point at this lesson the day it lands and both name machines and operators it must not; the claim was verified rather than asserted, by scanning all seven study guides for any sizing or ranking of the grid-stability machine and finding none. `reviewBy` **2027-03-16** on the six-month default with **four rejections in writing**, the nearest candidate being a vendor product-availability date (*January 2027*) rejected as a vendor fact in a vendor-blind lesson. **It was also the rotator, and this time the EST/UTC trap resolved the other way**: EST read 2026-09-16 00:07 against UTC's 04:07, so the twenty-six sections dated 2026-09-15 all stopped being exempt and the file stood at **106 non-exempt against a 100 trigger** — the 2026-09-07 group (13 sections) rotated, all thirteen SHA-resolved on a 1,269-commit clone. Phase 4 is **14 of 26**. Brief was §7.34; §7.35 written in the same commit. **Next by §7.4 item 6's interleave, now at 14:9, is S2 session 10 (`clean-firm-and-nuclear`) — its brief is §7.35** **Row 15 (`string-versus-central`) done — v06.09r, 2026-09-16**, the forty-fifth lesson and the one that COMPLETES `electrical-foundations` at five of five. **The first Phase 4 row whose correct write is a mid-array insert** — §4 position 2 into a track already holding positions 1, 3, 4 and 5 — so `check-classroom-pipeline.py`'s P5 fired by construction and correctly; appending would have put the lesson at position 5 and contradicted §4. Registry order in `clLessons_()` is appended and passed P5 separately; no P3 and no P7. §8 item 6's contradiction resolved against §7.36's stated preference because the corpus states NEITHER a harvest delta nor an availability figure — only relationships — so a `proscons` was inserted and the count moved seven → eight, the middle term (a central-class block of swappable sub-units) being what the record actually names and what `where-it-fails` later turns into *a monolith in disguise*. §10.6 (t) ran the unusual way round: three DOWNSTREAM lessons (`the-ups-room`, `inside-the-rack`, `redundancy-by-the-numbers`) already own the specifics, so this one keeps the test and names them. A second instance of session 10's unbuilt-handoff shape found and NAMED rather than filled — `the-800-vdc-shift` hands the DC arc taxonomy to `dc-fault-engineering`, §7 row 20, unbuilt. Phase 4 is **15 of 26**. Brief was §7.36; §7.37 written in the same commit. **Next by §7.4 item 6's interleave, now at 15:10, is S2 session 11 (`hyperscalers-and-ai-labs`) — its brief is §7.37** **Row 16 (`clean-firm-power`) done — v06.13r, 2026-09-16**, the forty-sixth lesson and **the one that pays off a debt a built public lesson named by title**: `contracts-and-revenue`'s `clean-firm-and-the-restart` had declined the physics to *Clean Firm Power* for a session while nobody held it. A search across every hand-authored lesson for the id, the title and the phrase *clean firm* found **exactly one creditor and no second**; the id string appears in no authored lesson at all, and the two other occurrences of the title are generator output rendering *(planned)*. §4 position 4 of five, a mid-array insert between `the-campus-as-a-power-project` and `where-bess-plugs-in`, so **P5 fired by construction and was the only finding besides P1** — no P3, no P7, no P8: exactly row 15's signature. **The split runs the inverse of every S2 split** — the built contributor landscape owns the parties and this public lesson owns the physics — so no company is ranked anywhere in it and the module's undisclosed price ladder is left where it is. Nine sections at §3.4's ids and order, unmoved. **Both halves of the due count called exactly, and the deepening half fired for the first time in the programme**: `--check` 9 → 10 → 10, the entrant `clean-firm-and-nuclear` differing in **`read-next` AND `what-is-bought-and-on-what`**, the three-part test run programmatically (lexicon keys `nuclear`/`smr`/`clean firm`/`reactor`; exactly one `READ_NEXT`; criterion 2 hitting `reactor`). `reviewBy` 2027-03-16 on the six-month default after the sort returned **nothing** — 43 `policyExposure` entries across the nine stamped dossiers, 24 dated, **zero future day-level dates** — with three rejections in writing. **Not the rotator: the push landed on 2026-09-16 EST for the third consecutive session**, so both changelogs stayed under their triggers again. Phase 4 is **16 of 26**. Brief was §7.38; §7.39 written in the same commit. **Next by §7.4 item 6's interleave, now at 16:11, is S2 session 12 (`neoclouds`) — its brief is §7.39** **Row 17 (`where-the-chain-breaks`) done — v06.22r, 2026-09-16**, the seventeenth and the **capstone of `aidc-grid-to-chip`**: *The Failure-Point Map*, gate `tracks`, six sections at §3.3's ids and order in six distinct kinds, **thirty public stamps** — the widest stamp any lesson carries — and a registry at **47 lessons, 8 tracks, 142 gate cases, 0 errors / 0 warnings**, module assertion still 21. **The first Phase 4 row in three that APPENDS rather than inserts**: §4 puts it last of eleven and the two guidance lessons at positions 2 and 10 are unbuilt, so the §4 position is an append at 9 of 9 and the prefix survives — **P1 alone**, the two-row P5 run broken exactly as §7.40 forecast. **Its specification is `CLASSROOM-CURRICULUM-PLAN.md` §5, not §3.3's outline, and §5 has fourteen rows while the section id says twelve** — decided deliberately because ids are permanent: id kept, fourteen rows carried, the 12-against-14 split written into the table's `note`, and the numbering matched to three guides that already cite these row numbers in their own `where-it-fails` sections. **The four causes were tallied across the rows rather than asserted** — hands 6, current squared 5, no zero crossing 1, series heat 1, none of the four 3 — which yields the lesson's sharpest fact: the commonest cause on the map is not a physical mechanism, and the DC arc, the most-discussed hazard in the chain, is its rarest. **Row 11 states the failure and declines the taxonomy** to the unbuilt `dc-fault-engineering` (§7 row 20), in the row cell, the drill card and the header comment. **Two guides §5 does not name were pinned with the reason** — `study:aep` is the only guide in the corpus carrying §5's *audit that pauses a batch*, and `study:dominion-energy` states row 1 almost verbatim — while three guides §5 does name were dropped as unused. **Both halves of the due count called exactly: 8 → 8 → 8**, the three-part deepening test run programmatically and failing at part one (zero `CRITERION_LEXICON` entries), the one `READ_NEXT` membership netting against a segment already due and already differing in both sections; **fifth consecutive row to leave a stale `lesson_ref` behind**. **(bb1) was measured on its first outing and did not bind** — all 21 modules grepped for the id, the title and any unbuilt claim, zero hits, and no landscape covers `epc-and-construction` at all. `reviewBy` 2027-03-16 on the six-month default. **Not the rotator either: the push landed on 2026-09-16 EST for a ninth consecutive session**, and §7.40's forecast that this row would rotate was wrong for the reason §7.40 itself recorded — the deferral is a function of the day of the push. Phase 4 is **18 of 26**. Brief was §7.40; §7.41 written in the same commit. **Next by §7.4 item 6's interleave, now at 17:12, is S2 session 13 (`capital`) — its brief is §7.41** **Row 18 (`the-cooling-plant-and-water`) done — v06.26r, 2026-09-16.** Eight sections at §3.4's ids in §3.4's order — `two-ratios` (table) · `chillers-economisers-and-the-climate-file` (prose) · `waterless` (proscons) · `containment` (prose) · `the-nervous-system` (callout) · `where-it-fails` (table) · `drill` (6 cards) · `check-yourself` (5 items) — six distinct kinds, no two prose sections adjacent, three `sales` lines of eight, four tiles and a seven-entry local glossary checked against the registry's terms **and** aliases first. **Thirteen public stamps**, so the fold is `tracks` and an analyst sees it; **all twelve §3.4 guides were used and none was dropped**, and every pin is the guide's own `lastUpdated` rather than a `git log` date — the G2 trap on this row, since four of the twelve last committed on a different day from the one they carry. **A mid-array insert at position 2**, confirmed against the four existing ids before the edit, and the anchor for the new function verified by counting unbalanced braces before the splice point and getting **0**. **Both halves of the due count called exactly: 7 → 8 → 8**, `cooling` entering with two sections differing and **all six criterion rows clearing `(planned)`** — the first row where the deepening rewrote every criterion row of its segment. **Sixth consecutive row to leave a stale `lesson_ref` behind** (open item (v)). **(bb1) measured and did not bind** — no `landscape-cooling` module exists, and the only prose asserting this lesson unbuilt is the generated segment lesson, which a Phase 4 row does not regenerate. Render at analyst: **8 of 8 section nodes, 29,232 characters, zero page errors**, zero unresolved `{{…}}`, zero literal asterisks, zero backticks; through the real serving path **viewer → `ROLE_DENIED`** and **a token under 32 characters → `SESSION_EXPIRED`**. Phase 4 is **18 of 26**. Brief was §7.42; §7.43 written in the same commit. **Row 19 (`how-a-cell-is-made`) done — v06.30r, 2026-09-17.** Seven sections at the ids §3.1 fixes, in its order, **inserted at `bess-foundations` position 5** between `the-control-stack` and `where-batteries-stop` — the fourth Phase 4 row in five whose correct write is a mid-array insert, reporting **P1 and P5** with no P3, no P8 and no P12. **It completes `bess-foundations` at six of six**, so the track's `short` moved to the whole-track form §3.1 plans — the **third of the five mechanism tracks to close** and the third closed by a Phase 4 row in five rows, after `electrical-foundations` (row 15) and `aidc-campus` (row 18); the two still open are `aidc-grid-to-chip` (rows 20 and 26) and `market-access` (rows 21–25). **Ten public inputs and the widest G2 trap yet measured, ten of ten**: every stamped guide's `lastUpdated` differs from its `git log` date, so a session taking the commit date would have written ten wrong pins and no checker would have seen one. §3.3's rule **cut in one direction** — `study:envision-energy` dropped. **Both numbers called before the write and both held**: `--check` **7 → 7**, `cells-and-chemistry` still differing in `read-next` alone (the second change to the same section netted), and **no deepening at all** because the parsed `CRITERION_LEXICON` carries no entry for this lesson — run programmatically. **Seventh consecutive row to leave a stale `lesson_ref` behind** (open item (v)). **§10.6 (t) fired three times inside the lesson's own track, and one firing is a new class — a DECLARED handoff naming the WRONG sibling**, which this row corrects in place under the same section id with one `revisions[]` entry. Render at analyst: **7 of 7 section nodes, 26,328 characters, zero page errors**, zero unresolved `{{…}}`, zero literal asterisks, zero backticks; through the real serving path **viewer → the page-level denial and 0 of 7 sections**, and **a token under 32 characters → `SESSION_EXPIRED`**. Phase 4 is **19 of 26**. Brief was §7.44; §7.45 written in the same commit. **Next by §7.4 item 6's interleave, now at 19:14, is S2 session 15 — its brief is §7.45** |
| 7 | **K2** — roster deck (opt-in) | **Opus 5 xhigh** | 1–2 | S1 (met) | Runnable any time; low priority until segment lessons have been regenerated by S2 |
| 8 | **Phase 4 rows 25–26** — `reading-the-numbers`, `interconnection-for-large-loads` | **Opus 5 xhigh** | 2 | row 26 needs G6 (order 1) | |
| 9 | **C5** — sales simulations | design **Fable 5.1 xhigh** · 1; authoring **Opus 5 xhigh** · ~5 | ~6 | ~6 landscapes | |
| 10 | **Q · plan clock** — re-verify curriculum plan §1, re-test §2.2, re-rank §7, run the health script | **Fable 5.1 xhigh** | recurring | quarterly | Next due ~2026-12 |
| — | **C6** — team layer | — | deferred | a team exists | |

**About 57 sessions remain**, C6 excluded: roughly 54 on **Opus 5 xhigh** (G6, C3, the bankability review, 26 mechanism lessons, ~19 landscapes, K2, C5 authoring), 2–3 on **Fable 5.1 xhigh** (C5 design, the plan clock), and **none on Fable 5.1 High** — that lane's work (K1, S1's code, the four private S3 halves) is finished; the Profiler earnings desk and the quarterly sweep continue outside this plan on their own Routine. The critical path is orders 1–2 (three to four Opus sessions) before either long lane starts; after that the lanes interleave freely.


**Progress against this table — updated 2026-09-13 (v05.43r).** **Order 1 (G6) is done.** One Opus 5 xhigh session authored `large-load-interconnection-2026-09`, the ninth module in `guidanceDocs_()`, from public primary sources only, and closed `CLASSROOM-CURRICULUM-PLAN.md` §6's last open row — **the gap register now has no open row at all.** The session held its fence: no lesson, no migration, no dossier, guide, registry, report or Routine touched. **Order 2 (C3, the guidance homecoming) is now the next session**, and it inherits exactly the condition the 2026-09-13 re-sequencing was designed to produce — **nine modules to migrate and zero landscapes**, because S2 has not started. The count is now fixed rather than forecast. About **56 sessions remain** on the same split (roughly 53 Opus 5 xhigh, 2–3 Fable 5.1 xhigh, none Fable 5.1 High). Phase 4 row 26 (`interconnection-for-large-loads`) is unblocked by this session but stays in its own lane at order 8; nothing else in the table moved.

**Progress against this table — updated 2026-09-13 (v05.48r).** **Order 2 (C3) is DONE, in three sessions.** Session 3 closed the Guidance Homecoming: Industry Guidance is now hosted, served, studied and drilled in exactly one app. The **progress ticks migrated in one shot** — Profiler's `exportGuidanceProgress()` filters `gd_progress:<email>` to the nine module ids over a frozen id literal (the registry it would otherwise have walked is deleted in the same commit, and module ids are permanent anyway), Classroom's import reads it from a Script Property, and the **done-when the design states is met**: `scripts/check-guidance-migration.js` runs all six real one-shot functions in two isolated VM contexts with one property store each, against a fixture built from the real registry, and **44 assertions pass** — every exported tick accounted for per account with zero missing, a second run writing 0 and leaving the store byte-identical, `study-`/`dossier-` ticks untouched, and a Classroom tick newer than Profiler's left standing. **The merge rule is "an existing Classroom tick always stands"**, which satisfies the brief and is correct in the other direction too: when Profiler's date is the newer, Classroom's earlier date is the true first completion. The **read-only window closed with it** — modules tick like lessons, the library cards carry a completion readout, and **139 guidance items joined the C4 drill pool**, which is **decision 6 at its target state**. Profiler's masthead slot and dossier chips became real `<a>` deep links into `Classroom.html#guidance/…` with **no access-matrix row moved** (`verify-profiler-roles.py` reports the identical 13 × 4 matrix), and only then did the twelve functions, ten page-side functions, five dead CSS blocks and `scripts/check-guidance-parity.py` go — `Profiler.gs` 760 KB → 449 KB, `Profiler.html` 8,099 → 7,818 lines. **The `gd-*` renderer deliberately stayed**: study guides, the report view and the sign-in log all render through it. `.claude/rules/industry-guidance.md` was re-targeted at `Classroom.gs` as a file-target and op-name change rather than a rewrite, and the **quarterly review Routine's prompt was updated in place after the developer was asked in-session and approved** — same trigger id, same cron, run history intact. **No operator step is outstanding.** **Next in the order: row 3, the bankability review** (`bess-bankability-2026-08`, `reviewBy` 2026-10-01) — and with the duplicate gone it now edits Classroom's copy only. Nothing else in the table moved.

**Superseded note (v05.45r).** **Order 2 (C3) session 2 of 2–3 is done; only session 3 remains.** One Opus 5 xhigh session landed the two federation pieces the migration needed before Profiler's copy can go. The **Admin lens** now re-hosts in `Classroom.html` behind `clCan('reports')` — admin only, unchanged from `OV_ROLE_CAPS`, so no access-matrix value moved — reading the same public `reports/reports-index.json` and `reports/<id>.report.json` Profiler reads, with **no new server route**: twelve overlay panels across five modules for an admin, none on the other four, none at all for a contributor, and no report request even issued at that tier. The **mentions scan moved with the content it scans**: `guidanceMentions_()` is byte-identical in `Classroom.gs` and answers only on `?action=guidancepeer&gpop=mentions`, token-gated on a **new** `GUIDANCE_PEER_TOKEN` Script Property (deliberately not the Scraper bridge's `CORPUS_TOKEN`) exactly as `scHandleCorpus_` is; `Profiler.gs` relays it through `guidanceMentionsProxy_()` on the same shape `handleNewsOp_` uses, with `guidanceAllowed_` still the only browser-facing boundary and the page's `ovGuideApi('mentions')` call untouched. **One operator step is outstanding and this commit cannot do it**: the token must be set to the same value in both projects' Script Properties, or the dossier's guidance chips render nothing (both sides report themselves unconfigured rather than erroring, by design). Nothing moved between sessions 2 and 3, and nothing else in the table moved. **Two follow-up fixes landed the same night** and are not new slices: **v05.46r** hardened the peer route after the developer set the token and saw nothing — both sides now trim the shared secret before comparing (a value pasted out of an execution log carries a trailing newline, invisible in the Script Properties UI and fatal under `!==`), and Profiler's proxy stopped reporting every upstream fault as a transport fault. **v05.47r** found the actual reported symptom: the dossier chips had been rendering the whole time at **y ≈ 1817px on a 2035px page**, because `ovGuidanceMentionsLine` appended the line below the tab panes *and* the field-note form. It now inserts above `.ov-tabs`, and the developer confirmed the chips on the Tesla and NVIDIA dossiers. That placement predated C3. **Session 3's brief is in §7.11.**

### 7.4 · Sequencing and the critical path

1. **S0** first — every other new item reads its files.
2. **K1** early — a live defect independent of everything else; it can run before S0.
3. **Phase 5, then S1, then Phase 6** — the evaluation is one session and the reports feed the landscape modules' admin extras, so they are fresh before S2 starts.
4. **S3's critical path — sessions 1–5 of `PROFILER-COVERAGE-PLAN.md` §10.4** (E7 → E2a → E2b+E3b → E1a → E1b) before the S2 sessions for compute, insurance, assurance and software; the remaining five deepen segments already at the floor and run whenever a Profiler session is free. Note the ordering the floor arithmetic forces: **assurance and insurance are both blocked on a private-company challenger**, so the Fable session `E2b+E3b` gates two landscapes that no Opus session can unblock.
5. **G6**, then **C3** before S2 passes about six modules. *(Re-sequenced 2026-09-13, v05.42r, at the close of S3: G6 and C3 now run **before any** S2 module — the table above — so that the homecoming migrates nine modules and no landscapes.)*
6. **S2** interleaved with **Phase 4** rows — the two long lanes — with Phase 4 row 1 (`four-machines`) early because it creates `electrical-foundations`.
7. **K2** once segment lessons exist; **C5** after about six landscapes; **C6** when the team exists.

**Standing caveats carried forward:** the `named-project` report's whitespace call was restated by Phase 5 as *supplier* whitespace (§5, §7.7) and lifts with Phase 6 session 1's edition; reports stay immutable; the CHANGELOG rotation is decided by count on the day a push lands; the register is re-run and dated, never remembered (S3 closes G10's residual only when a session re-runs the row).

### 7.5 · Paste-in brief for S0 (Fable 5.1 xhigh)

> Picking up from the Phase 3 session (v05.07r, Phase 3 Done) and the program extension agreed the same day (INTEGRATED-REMEDIATION-PLAN.md §7, v05.08r), run **S0 — the segment-layer design gate** on Fable 5.1 xhigh as a fresh session. S0 is DESIGN: it writes registries, specs and plan sections. It authors no lesson, writes no generator code, touches no dossier, study guide or report, and does not open the content fence in Classroom.gs.
>
> READ FIRST: repository-information/SESSION-CONTEXT.md (Latest Session), then INTEGRATED-REMEDIATION-PLAN.md §7 in full (7.1 what landed, 7.2 the decisions and the approved company list, 7.3 the run order, 7.4 sequencing), then CLASSROOM-CURRICULUM-PLAN.md §1, §2.2–2.3, §4, §7, §8 and §9 (as re-planned at v05.07r), then CLASSROOM-SCHEMA.md (the stamp, the content contract, the drill), .claude/rules/classroom-app.md, PROFILER-SCHEMA.md (registry schema, profile schema v7, registry revision signals), .claude/rules/profiler-app.md (the Profiler Command steps), PROFILER-COVERAGE-PLAN.md §2 (the model rule) and §8 (the ledger format), and PHASE6-CLASSROOM-DESIGN.md (C3–C6).
>
> THE WORK — nine deliverables, all design, one push commit:
> 1. `live-site-pages/profiler-data/profiler-segments.json` — nineteen segments per §7.2 (the seventeen proposed plus software-and-optimization and insurance-and-risk-transfer; testing-and-certification merged with advisors as assurance; upstream materials and transmission developers as adjacent roles). Each segment: id, name, chain position, definition, buying criteria, members with role ∈ incumbent / challenger / adjacent. **Verify every membership against the dossier's own `ecosystemRole` and `productsAndServices`, not against the registry category** — record any company whose dossier does not support the assignment as unassigned rather than guessing. Add the schema to PROFILER-SCHEMA.md (a new "Segments registry" section) and the `segments[]` mirror field to the registry schema; the sync script change itself is S1's.
> 2. CLASSROOM-CURRICULUM-PLAN.md **§10 — the segment layer**: the five-layer model, the floor rule (three dossiers, one incumbent, one challenger, before a landscape module), the segment-lesson template (section ids fixed: the-segment · where-it-sits · what-is-bought-and-on-what · the-players · the-numbers · who-is-connected · what-moved · the-fence · read-next · check-yourself; stamp = every member `profile:` + `graph:` + `concepts:` → public), the generator spec (`scripts/build-classroom-segments.py`: stable `segment-<id>` ids, section diff → `revisions[].changed[]`, regenerate only segments whose members moved, developer-session tool not pipeline), the three Value Chain tracks under a fourth lane "The Value Chain" with `reading-the-graph` as the opener, the landscape-module spec (contributor tier, guidance shape, claims ledger citing dossiers, `reviewBy`), the K1 and K2 specs, the health-script spec, and the two implicit role paths.
> 3. Curriculum plan §3 and §7: add `reading-the-numbers` (market-access, after `contracts-and-revenue`; inputs from siemens-energy `backlog-economics…`, primoris percentage-of-completion, hut-8 `four-treatments`, clearway HLBV, the normalized KPI overlay) as row 25 and the G6 lesson (guidance-gated, behind `the-fence-line`) as row 26; `reading-the-graph` is S1's, note it in §7 as authored there. Re-number §4 positions accordingly and keep every built lesson's id and position semantics.
> 4. Curriculum plan §6: rewrite the G6 row's blocker — research-synthesis accepted 2026-09-07 on the G4 precedent (six of eight modules have no ingested document); Opus 5 xhigh, one session, after S0. The row stays Open until the module lands.
> 5. Content-contract amendment in CLASSROOM-SCHEMA.md ("Content contract" paragraph) and classroom-app.md: mechanism lessons keep "never company trivia"; segment lessons may drill structure; the roster deck is opt-in and separate.
> 6. .claude/rules/profiler-app.md: a Profiler Command step — every new dossier is assigned to one or more segments with a role in `profiler-segments.json` in the same commit.
> 7. PROFILER-COVERAGE-PLAN.md: a **Phase E** section and §8 ledger rows for the seventeen companies in §7.2 with the model per company, the six session groupings, the segment each fills, and the "Why (hypothesis — unverified)" column carrying the chat's company notes verbatim so sessions verify them.
> 8. INTEGRATED-REMEDIATION-PLAN.md §6: flip S0 to Done with the version; K1 and Phase 5 marked next.
> 9. CLASSROOM-CURRICULUM-PLAN.md §9: one revision-log row for S0.
>
> DO NOT: write `build-classroom-segments.py` or any code; author any lesson or module; edit Classroom.gs or Profiler.gs; touch any dossier, guide, report or report-pins-verified.json; run a registry sync or graph rebuild (nothing in the corpus changes); re-run the §6 register (only the G6 wording changes).
>
> VERIFY: `python3 scripts/check-classroom-content.py` and `python3 scripts/check-classroom-pipeline.py --base origin/main` — content 10 / 3 / 134 with 0 / 0; pipeline: P1 findings on the edited plan files are expected, no P3; `check-profiler-reports.py` 0 errors / exactly 7 warnings (hithium v13 ×3, sungrow v9, catl v7 ×2, zhonhen v7) unless Phase 5/6 ran first — any other dossier is a finding, stop and say so; `python3 -c "import json; json.load(open('live-site-pages/profiler-data/profiler-segments.json'))"` parses; every member slug in it has a `<slug>.profile.json`.
>
> CHANGELOG — check, do not assume: the file holds 108 sections after v05.08r, ten dated 2026-09-07. Same EST day → 109 − 11 = 98 → no rotation. A later EST day → 109 non-exempt → rotation fires on the thirteen sections dated 2026-09-01 (v04.01r–v04.13r); run `git fetch --unshallow` before any SHA lookup and budget ~10 extra minutes. Verify with `grep -c '^## \[v[0-9]' repository-information/CHANGELOG.md` and `TZ=America/New_York date '+%Y-%m-%d'`.
>
> Normal Pre-Commit and Pre-Push checklists; one push commit on a claude/* branch (check `git ls-remote` first). No page is affected (documentation and data-file design only — `profiler-segments.json` is new data with no consumer until S1, so no Profiler page bump). End with "remember session".

### 7.6 · Paste-in brief for Phase 5 (Fable 5.1 xhigh) — written 2026-09-07 at v05.11r

> Run Phase 5 of INTEGRATED-REMEDIATION-PLAN.md — the report-strategy evaluation — on Fable 5.1 xhigh as a fresh session. Phase 5 is EVALUATION: it writes a decision per report and the plan sections that carry it. It authors no report (that is Phase 6, Opus 5 xhigh), edits no published report file (reports are immutable), touches no dossier, guide or lesson, and does not open Classroom.gs or Profiler.gs.
>
> READ FIRST: repository-information/SESSION-CONTEXT.md (Latest Session), then INTEGRATED-REMEDIATION-PLAN.md §1 (F10, F11), §2–§3 (the Phase 5 and Phase 6 rows and paragraphs), §5 (the standing caveat on the named-project report's whitespace call), §6 (the ledger), §7.3–7.4 (Phase 5 before S1 and Phase 6 — the reports feed the landscape modules' admin extras), then .claude/rules/profiler-app.md → the Profiler Command's "profiler report" section in full (report shape, the four types, `supersedes` and the index `status` field, immutability, and item 8 — the drift-gated monthly Routine, read its gate as written), PROFILER-SCHEMA.md → the report schema and `report-pins-verified.json`, PROFILER-STYLES.md (the active style), scripts/check-profiler-reports.py (what a pin, a warning and an error are), live-site-pages/profiler-data/reports/reports-index.json and all four report files, CLASSROOM-CURRICULUM-PLAN.md §10.6 (what a landscape module will cite from a report), and PROFILER-COVERAGE-PLAN.md §8 + §10 (the nineteen Phase E companies, E7's AMD and Supermicro included, that will land before Phase 6 could re-run).
>
> THE WORK — one push commit, evaluation only:
> 1. For each of the four reports (`named-project-bess-attach--opportunity--2026-08-30`, `aidc-power-conversion--competitive--2026-08-29`, `grid-scale-bess--competitive--2026-08-29`, `s154-listed-bess-suppliers--risk--2026-08-29`): read it against today's corpus (154 dossiers, 9 named projects, the segments registry) and write a decision — regenerate as-is, re-scope (state the new scope: the company set, the question, the type), or retire — with the reason, the evidence (which dossiers moved under it, per check-profiler-reports.py's 7 warnings and F10/F11), what an S2 landscape module would need from it, and whether it should wait for a Phase E group to land first. Name the Phase E rows, if any, that change the answer.
> 2. Decide the drift-gated monthly Routine's gate for a 154-company corpus: keep, re-tune (state the threshold), or replace — with the reason. Do NOT create, update or delete any Routine; record the decision only — the developer applies it.
> 3. Lift or restate the §5 caveat on the named-project report's whitespace call, in the plan's own words.
> 4. Write the decisions where they live: INTEGRATED-REMEDIATION-PLAN.md — a new §7.7 "Phase 5 decisions (date)" with one block per report (decision · scope · reason · evidence · Phase 6 order), the Routine decision and the caveat outcome; §6 Phase 5 → Done with the version and Phase 6 → next with the report list and order it inherits; §5 caveat updated; and §7.8, the Phase 6 paste-in brief on the §7.5 pattern (one per report or one for all), naming for each the `supersedes` id, the scope and the pins it must refresh.
> 5. reports-index.json: no change (status flips are Phase 6's, when the new edition exists). report-pins-verified.json: untouched. No report file edited.
>
> DO NOT: author or regenerate a report; edit any report file or report-pins-verified.json; flip an index status; create, update or delete a Routine; touch a dossier, guide, lesson, Classroom.gs, Profiler.gs or profiler-segments.json; run a registry sync or graph rebuild.
>
> VERIFY: `python3 scripts/check-profiler-reports.py` — 0 errors / exactly 7 warnings (hithium v13 ×3, sungrow v9, catl v7 ×2, zhonhen v7), identical before and after — any other output is a finding, stop and say so; `python3 scripts/check-classroom-content.py` 10 / 3 / 134 with 0 / 0, unchanged; `git fetch origin main` then `python3 scripts/check-classroom-pipeline.py --base origin/main` — P1 on the edited plan file is expected, no P3; every internal link in the edited plan files resolves.
>
> CHANGELOG — check, do not assume: at v05.11r the file holds 111 sections, 13 dated 2026-09-07. On a LATER EST day none is exempt, so with your section it reads 112 → rotation fires on the oldest whole date group, the thirteen 2026-09-01 sections (v04.01r–v04.13r) → 99; run `git fetch --unshallow` before any SHA lookup and budget ~10 extra minutes. Verify with `grep -c '^## \[v[0-9]' repository-information/CHANGELOG.md` and `TZ=America/New_York date '+%Y-%m-%d'`.
>
> Normal Pre-Commit and Pre-Push checklists; one push commit on a claude/* branch (`git ls-remote` first). No page is affected (plan files only — no Profiler page bump). End with "remember session".

### 7.7 · Phase 5 decisions (2026-09-08, v05.12r — Fable 5.1 xhigh)

Evaluated against the corpus as it stands: **154 dossiers, 9 registered named projects, the 19-segment registry (283 memberships)**, and `check-profiler-reports.py` at **0 errors / 7 warnings** before and after (identical output: `hithium` v13 ×3, `sungrow` v9, `catl` v7 ×2, `zhonhen` v7). No report file, `reports-index.json`, `report-pins-verified.json`, dossier, guide, lesson, registry or Routine was touched. Phase 5 closes here against the "done when" §2 wrote at X3 (v05.01r) — a criterion this session did not author, so the §6 flip below is the later-session close rule 2 requires.

**What the read established, across all four reports**

1. **The defect is scope, not fact (F11 confirmed).** Of the 45 coverage pins, 38 were read or compared at X3 and 35 held exactly; the three that did not (F10) recorded the corpus moving *under* a report, not a wrong figure. The seven warnings since are Phase 1 / 2a moves, and each was read again here against what the reports cite: `hithium` v12 added the `lightsource-bp` customer edge and v13 marked its "most architecturally complete grid-to-chip offer" as Hithium's own assessment — no report cites either, and every cited Hithium figure (FY2024 RMB 12.917B / USD 1,800m, Trimount 2.8 GWh, the Texas plant, the 27 Apr 2026 HKEX lapse, the ~10 CATL suits) is untouched; `sungrow` v9 folded the H1 2026 interim and the first-party **EnerNeo** SST — the grid-scale report's cited figures hold (Wood Mackenzie #1, USD 12,408m, PowerTitan 3.0, Masdar 7.5 GWh) while the AIDC report's "SST roster still forming" framing is now contradicted by a covered company it left out of scope; `catl` v7 added the RMB 4.1B / 49% Zhonhen holdco **capital increase** with the CATL-side null — the grid-scale KPI (USD 58,951m) and every §154 citation hold; `zhonhen` v7 replaced an unsourced "~43.5% premium" that no report cited, and the RMB 4.1B / 49% the AIDC report's Judgment 4 rests on survives as a capital increase rather than a purchase. **Every warning is a pin to re-read, not a judgment to withdraw; not one is re-pinned.**
2. **Version drift and substantive drift are different quantities, and the reports show it.** Scoped dossiers moved past their pins: named-project **13 of 15** (all but `oracle` v4 and `xai` v4), AIDC **8 of 8**, grid-scale **12 of 12**, §154 **5 of 5**. Scoped dossiers carrying a `recentDevelopments[]` entry dated after the edition's `generated`: **0 of 15, 0 of 8, 1 of 12 (`sungrow`, 4 Sep), 0 of 5.** The moves are the schema v7 backfill (v03.93r), the project-pin pass (v03.94r), X2's relationship curation (v04.72r) and the Phase 1 / 2a fixes — depth added to the record, not events. F10 itself lived in `technicalSpecs` and `productsAndServices` (Lighthouse's "solar, wind, battery storage" in `vantage` v9; Project Jupiter's "microgrid … with battery energy storage" in `stack-infrastructure` v7; Enbridge Cowboy's 200 MW / 1,600 MWh Tesla BESS in `meta` v9), which no count of versions or developments would have surfaced — X3's wide-gap *read* found it. This is the evidence behind the Routine decision below.
3. **The project web moved more than the pins did.** At generation the named-project report's scope was the 15 companies whose curated relationships pinned eight projects. Today **24 dossiers pin nine projects**: ten are outside the report's scope — `aep`, `caterpillar`, `crusoe`, `ge-vernova`, `samsung-ct` (Stargate / Abilene), `anthropic` and `caterpillar` (Colossus), `mccarthy` and `whiting-turner` (Lighthouse's four-way GC bench), `google` and `hut-8` (River Bend, registered at v04.83r and absent from the report) — and one scoped company, `tesla`, pins nothing (its Megapack sale is cited through `xai`, as X3 found). **No BESS supplier is named at any Stargate campus, at Hyperion or at Homer City in any of the 24 dossiers** — the report's "None named" cells survive literally; what has changed is that storage is now *in the plan* at two of them.
4. **The 800 VDC roster is roughly fifteen covered companies, not eight (F11's count confirmed).** NVIDIA's own dossier carries partner edges to `abb`, `ge-vernova` and `hitachi-energy` (grid tier), `vertiv` and `eaton` (founding), `delta-electronics`, `liteon`, `megmeet`, `infineon` and `flex` (component tier); dossier mentions of 800 VDC run `vicor` 79, `zhonhen` 56, `delta-electronics` 54, `flex` 44, `infineon` 41, `megmeet` 33, `liteon` 32, `abb` 23, `vertiv` 22, `eaton` 21, `hitachi-energy` 19, `sungrow` 19 (EnerNeo), `ge-vernova` 17, `schneider-electric` 13. Seven of those fifteen post-date the report or were excluded from it. The segment registry's `power-conversion-and-rack-power-silicon` (19 members) is the wider set; the 800 VDC roster is the peer family the report's question actually names.
5. **The integrator peer family is the registry's, and it is eighteen, not twelve.** `storage-integrators-and-containers` holds ten incumbents and eight challengers, all of which existed when the report was cut at twelve: `envision-energy` (Wood Mackenzie tied-#5), `crrc-zhuzhou`, `hyperstrong`, `trina-storage`, `jinko`, `sunwoda`, `ls-energy-solutions` and `prevalon` were left out; `flexgen` is now `adjacent` (software) and `eve-energy` is placed in cells and in-hall power, not in the integrator set. **S2's first landscape is this segment**, so this is the report whose scope matters most to S2.
6. **The §154 statutory six are now all covered.** `gotion` (v04.94r) closes the report's own stated gap; `rept` v1 records that it is *not* named — a useful negative the edition can state. No other dossier is §154(b)-listed.
7. **Two things the brief did not anticipate, recorded rather than fixed.** *(a)* **`s154-listed-bess-suppliers--risk--2026-08-29.report.json` was edited after publication** — commit `e1324c7` (v04.94r, session C12) rewrote its `scope.rationale` to say Gotion "is covered … this report predates it and its scope was not re-cut; X3 owns the re-pin". The schema forbids editing a published report; the checker cannot see a prose edit. This session did not revert it (that would be a second edit of an immutable file); Phase 6's supersession moots it, and whether to restore the published text is the developer's call. *(b)* **The earnings desk is leaving rows overdue.** The calendar shows `sinexcel` (due 2026-08-11), `eve-energy` (08-20), `iren` (08-27), `jinko` (08-27) and `byd` (08-29) past due; the desk's 7 September run (`SUCCEEDED`, 19 minutes) landed no commit. Three of those rows are indicators the reports name (BYD H1 2026 for grid-scale and §154; EVE's H1 and H-share outcome for §154; Sinexcel's H1 for AIDC), and `megmeet` / `zhonhen` H1 2026 interims (statutory deadline 31 Aug) are not in their dossiers either, with calendar rows pointing at 30 Oct. This is the same failure shape as the 7-for-7 the desk replaced; it is not Phase 5's to fix, but Phase 6's preflight (Report Command step 2: refresh first or proceed with staleness flagged) inherits it.
8. **A sequencing gap in §10.6, resolved here.** The curriculum plan says a landscape's admin extras "reach admins only through a report's `guidanceOverlays[]` anchored on the module's sections", and §7.4 puts Phase 6 before S2 so that landscapes do not anchor on a retired edition. Mechanically the anchor points the other way: an overlay lives in the *report* and names a `moduleId` / `sectionId` that `check-profiler-reports.py` validates against the live modules, so a Phase 6 edition **cannot** overlay a landscape S2 has not yet written, and an immutable edition cannot gain the overlay later. Decision: Phase 6 editions overlay the **existing** guidance modules (as the three overlay-bearing reports do today); an S2 landscape records the current edition's id in its `claims-ledger` / `read-next` as a `report:` reference; the overlays onto `landscape-<id>-YYYY-MM` sections arrive with the **next** edition of each report after that landscape exists (the monthly Routine for the opportunity series; the quarterly guidance review for the other three). Phase 6 before S2 still holds — for the *edition* the landscapes cite, not for the lens.
9. **No Phase E row changes any answer.** E6 (CALB · Cornex · Great Power) fills `cells-and-chemistry`, and no report is scoped on cells — the grid-scale scope is the integrator set, and none of the three is §154(b)-listed. E7 (AMD · Supermicro) fills `compute-and-the-rack`, downstream of the 800 VDC conversion layer the AIDC report ranks. E1–E5 touch software, insurance, assurance, materials and transmission. **None of the four editions waits for Phase E**; the registry on the day, not the ledger, decides membership.

**Decision per report**

**1 · `named-project-bess-attach--opportunity--2026-08-30` — RE-SCOPE and regenerate.**
- **Scope.** `kind: slugs`, derived on the day from every `relationships[].project` that resolves in `profiler-projects.json` — today **24 dossiers over 9 projects** (`aep`, `anthropic`, `bloom-energy`, `caterpillar`, `crusoe`, `dpr`, `ge-vernova`, `google`, `hithium`, `hut-8`, `jupiter-power`, `kiewit`, `mccarthy`, `meta`, `mortenson`, `openai`, `oracle`, `samsung-ct`, `stack-infrastructure`, `turner-construction`, `vantage`, `voltagrid`, `whiting-turner`, `xai`) **plus `tesla`** as the attached supplier on record, cited through `xai` — 25. **Question:** BESS attach across the nine registered named projects — where storage is in the plan, where a supplier is named, and who holds the storage scope where none is. **Type:** `opportunity`, unchanged. Keep the topic slug `named-project-bess-attach` in the id so the monthly Routine's prefix match still finds the newest edition.
- **Reason.** F10 and F11 both land here: two of its three whitespace campuses have storage in the plan, a ninth project (River Bend — 330 MW of Entergy capacity, Vertiv and Jacobs named, Google backstopping the lease) is missing, and the web it maps has ten more participants than its scope. The central call survives only in a narrower form (finding 3), and the drift-gated Routine is armed on this series — the edition that resets its baseline should be Phase 6's, not one authored on churn (see the Routine decision).
- **Evidence.** 13 of 15 pins moved, 12 re-verified at X3 and holding, `hithium` v13 re-warning on an untouched citation; 0 of 15 new developments; the F10 three; `meta` v9's Enbridge Cowboy lane (utility-tolled 200 MW / 1,600 MWh Tesla BESS — a fourth attach lane the report never had). Indicators: none fired — no named BESS award at Frontier, Lighthouse or Project Jupiter; Frontier's first-building delivery, Trimount's construction milestones, the Port Washington litigation outcome and Homer City's first power are all absent from the record.
- **S2 needs.** The attach map (project × storage in plan × supplier named × who holds the storage scope) for ~~`aidc-developers-and-landlords`~~, ~~`hyperscalers-and-ai-labs`~~, ~~`epc-and-construction`~~ and ~~`bridge-and-on-site-generation`~~ landscapes — **all four are now built (sessions 6, 11, 14 and 9), so this need is spent**; each shipped without an attach map and said so in `what-the-record-does-not-say`; overlays on `power-infra-aidc-2026-08` and `utility-aidc-procurement-2026-08` for now (finding 8).
- **Wait for.** Nothing. `oracle`'s Q1 FY2027 (row dated 2026-09-10, unconfirmed) would move the one unmoved public pin; not worth holding the edition for. No Phase E row.
- **Phase 6 order: first** — no refresh dependency, it lifts the §5 caveat, and it must land before the Routine's 2026-10-01 fire.

**2 · `grid-scale-bess--competitive--2026-08-29` — RE-SCOPE and regenerate.**
- **Scope.** The registry's `storage-integrators-and-containers` **incumbents and challengers as of the day** — today 18: `sungrow`, `tesla`, `fluence`, `byd`, `catl`, `wartsila`, `canadian-solar`, `envision-energy`, `crrc-zhuzhou`, `hyperstrong` (incumbent); `hithium`, `trina-storage`, `jinko`, `sunwoda`, `ls-energy-solutions`, `prevalon`, `lg-energy-solution`, `samsung-sdi` (challenger) — **plus `flexgen`** kept as the named `adjacent` comparator the pure-integrator consolidation judgment needs (19). `eve-energy` leaves: the registry places it in cells and in-hall power, and its Mr.Giant block is a cell maker's system line — the cells peer family has no report and S2's cells landscape reads the dossiers directly. Adjacent members (the gen-set OEMs, `vertiv`, `eaton`, `ge-vernova`, `huawei-digital-power`, `narada`, `on-energy`, `sinexcel`) stay out with the reason stated in `scope.rationale`. **Question and type unchanged** (`competitive`); the registry role becomes a column in the head-to-head so the incumbents' and challengers' tables stay like-for-like.
- **Reason.** F11: the head-to-head omits a Wood Mackenzie top-five integrator and the two Chinese-domestic incumbents, and S2's *first* landscape is this segment. Judgment 5 (integrator consolidation) and the block race are unchanged in direction; the field is wider than the report shows.
- **Evidence.** 12 of 12 pins moved, 10 verified at X3 and holding; `sungrow` v9 (H1 2026: revenue RMB 30.912B, −29%; ESS now carries the revenue; inverter margin 42.72% vs ESS 32.43% — the report's Judgment 1 premise confirmed on mix, and a −29% print the edition must carry), `catl` v7 and `hithium` v13 re-warning on untouched citations. Indicator "Sungrow and BYD H1 2026 reports" **fired**: Sungrow's is in the corpus, **BYD's is not** (row overdue since 08-29). Wärtsilä–RCT JV closing (Q3 2026), Samsung SDI SBB 2.0 (Q4), LGES Lansing (2027) and Fluence Q4 FY2026 (Nov) are open.
- **S2 needs.** The ranking basis per incumbent as the dossiers cite it (Wood Mackenzie, Benchmark, BNEF), each challenger's route, the per-player bet table drawn from `strategyRead[]`, the indicators and the seller's play for `landscape-storage-integrators-and-containers`; overlays on `bess-bankability-2026-08` (counterparty) and `power-infra-aidc-2026-08` (markets) for now.
- **Wait for.** The `byd` H1 2026 refresh (and `eve-energy`'s, shared with the §154 edition) — a desk matter, not a Phase E one: refresh first in preflight or proceed with the staleness stated in `coverage.gaps[]`. No Phase E row (E6 is cells).
- **Phase 6 order: second**, in one session with the §154 edition (five of six §154 dossiers are in this scope; the same preflight serves both).

**3 · `s154-listed-bess-suppliers--risk--2026-08-29` — RE-SCOPE and regenerate.**
- **Scope.** The statutory six: `catl`, `byd`, `envision-energy`, `eve-energy`, `gotion`, `hithium`. **Question and type unchanged** (`risk`). `rept`'s record that it is not named belongs in `limitations[]`, not in scope.
- **Reason.** The report's own `coverage.gaps[]` says it "speaks for five of the statutory six"; `gotion` v1 (v04.94r) closes that with two independent statutory grounds recorded in its `policyExposure[]` — the one dossier in the corpus that changes what the report can say. The frame also gains a line it could not have: a §154-listed maker's capital (`catl` v7) now sits one step above a Panama/TRU architecture NVIDIA's own paper names.
- **Evidence.** 5 of 5 pins moved, 3 verified at X3 and holding; `catl` v7 and `hithium` v13 re-warning on untouched citations; `envision-energy` v3 unchanged since 30 Aug. Indicators: "BYD H1 2026 report (armed post-earnings refresh)" **fired and did not land** (finding 7b); EVE's H-share outcome and CSRC probe, a Hithium HKEX re-filing, Treasury's year-end PFE regulations, DFARS detail and a second AESC-style divestiture are all open. The post-publication edit of this file (finding 7a) is history the new edition supersedes.
- **S2 needs.** The fence map per maker and the five lanes as used, for `landscape-cells-and-chemistry` and the market-access lessons (`the-china-policy-stack`, `what-bankable-means`); overlays on `china-policy-stack-2026-08`, `bess-bankability-2026-08`, `power-infra-aidc-2026-08` and `utility-aidc-procurement-2026-08` as today — all four anchors still validate.
- **Wait for.** The `byd` and `eve-energy` H1 2026 refreshes (shared with the grid-scale edition). No Phase E row — the list is exactly six and E6's three are not on it.
- **Phase 6 order: second**, same session as the grid-scale edition.

**4 · `aidc-power-conversion--competitive--2026-08-29` — RE-SCOPE and regenerate.**
- **Scope.** The covered NVIDIA 800 VDC roster **by layer** — today 15: silicon and the last centimetre `infineon`, `vicor`; the rack (shelves, PSUs, BBUs) `delta-electronics`, `liteon`, `megmeet`, `flex`; the sidecar and the hall-edge block (TRU / SST) `zhonhen`, `sungrow` (EnerNeo), `sinexcel`, with `delta-electronics` again; facility and grid tier `vertiv`, `eaton`, `schneider-electric`, `abb`, `ge-vernova`, `hitachi-energy`. `huawei-digital-power` stays out on the report's own reason (sanctions-fenced from the ecosystem assessed); `mitsubishi-electric` (800 V named as a development target, May 2026) and `power-electronics` (utility-scale PCS) are named in `coverage.gaps[]`, not scoped. **Question:** who is winning the 800 VDC transition, **layer by layer**; the Option A/B/C slot frame stays. **Type:** `competitive`. Fifteen across four layers is not one peer family — the like-for-like rule is met *within each layer's table*, and the report's "three contests, not one" becomes four. One report, not two: the buyer prescribing the architecture is one, and the reader wants one map.
- **Reason.** F11: eight of roughly fifteen. Two of the seven absentees hold the corpus's deepest 800 VDC records (`vicor`, `infineon`) and one (`sungrow`) holds the most complete SST claim in the corpus — launched and supplying per its H1 2026 interim — which the report's "SST roster for the ~2029 slot is still forming" cannot stand beside.
- **Evidence.** 8 of 8 pins moved, 7 verified at X3 and holding (`liteon`'s $919M McKinney plant and Q1 2027 mass production, `vertiv`'s $15.0B backlog, `schneider-electric`'s 1.2 MW sidecar, `eaton`'s supercapacitor OCP architecture); `zhonhen` v7 re-warning on a figure the report never cited. Judgment 4's RMB 4.1B / 49% survives as a capital increase (`catl` v7 corroborates from the buyer side with the CATL-side null). `huawei-digital-power` v8's FCC DA 26-870 (20 Aug 2026) narrows the FCC prong to UL 1741 utility-interactive inverters, so no covered rack-power vendor is reached — a fact the edition can state. Indicators: **Zhonhen H1 2026 (due 31 Aug) and Megmeet H1 2026 (~27 Aug) have passed and neither is in its dossier** (finding 7b); TrendForce's 3Q26 first shipments, LITEON's Q1 2027, the next partner roster and the 34.5 kV SST announcements are open.
- **S2 needs.** The layer map, roster membership and the SST roster for `landscape-power-conversion-and-rack-power-silicon`; overlays on `nvidia-800vdc-2026-08` (`options`, `trusst`, `suppliers`) as today.
- **Wait for.** The `megmeet`, `zhonhen` and `sinexcel` H1 2026 refreshes — desk rows, not Phase E (`sinexcel` overdue since 08-11; the other two rows point at 30 Oct and should be re-dated). No Phase E row: E7's AMD and Supermicro are the load, downstream of the conversion layer.
- **Phase 6 order: third** — the edition with the largest scope change and the most preflight refreshes; S2 reaches power conversion fifth.

**None of the four is retired.** Each answers a question the corpus still asks and S2 will cite; each fails only on scope. **Regenerate-as-is was rejected for all four** because regenerating the old scope would re-pin a known under-count.

**The drift-gated monthly Routine (`trig_01TvnXREVHsQ4QCrtveYjvZM`) — REPLACE the measure, keep the cadence.** *Recorded only; the developer applies it — nothing was created, updated or deleted.*
- **Keep:** monthly on the 1st, fresh session, the opportunity series only (the other three reports stay on the quarterly guidance review with the library's age chip — fresh ≤45d / aging ≤120d / stale — as `profiler-app.md` already says), stand down silently with a one-line count, and "What changed since the last edition" as the new edition's first section.
- **Replace the gate.** Today's gate — *fewer than 10 scoped dossiers revised past their pins and no fired indicator → stand down* — is a churn detector at 154 companies: **it is already crossed (13 of 15) while 0 of 15 scoped dossiers carry a development since the edition and 35 of 38 pins held exactly**, and it would have missed F10, which no version count reaches. The desk at three refreshes a day and a corpus that doubles by phases (v03.93r moved 83 dossiers in one commit) guarantee the count trips on housekeeping. New gate — author when **any** of: **(a)** an `indicators[]` event is confirmed (unchanged); **(b) scope drift** — `profiler-projects.json` holds a project the edition does not cover, **or** the set of dossiers pinning registered projects (`relationships[].project`) differs from the edition's `scope.value` by **three or more** companies; **(c) substantive pin drift** — **five or more** scoped dossiers carry a `recentDevelopments[]` entry absent from the archived pinned version (`archive/<slug>.profile.v<pinned>.json`; dated after `generated` where no archive exists). The raw count of dossiers past their pins is still reported in the stand-down line — for information, never as a gate. Thresholds: three companies is ~12% of a 25-company scope and would have fired on River Bend alone (two new pinning dossiers plus a new project); five developments is ~20%, chosen so the 2026-10-01 fire on Phase 6's fresh edition stands down unless something happened.
- **Timing.** Until the gate is changed, the **2026-10-01 fire authors an edition on churn** unless Phase 6's edition has superseded the 30 Aug one first (a fresh edition resets the count to 0). Either land Phase 6 session 1 before then or pause the Routine until it has; do not let the Routine author what Opus is about to author (§3's "authored once, not twice").
- **Not extended.** A second Routine over the three competitive/risk reports is not recommended: the quarterly review already reads them, and the same three-signal test is its supersession cue.

**The §5 caveat — RESTATED, not lifted.** The plan's words are now in §5: the whitespace is **supplier** whitespace, not storage whitespace, at Lighthouse and Project Jupiter; storage-in-the-plan is on the record at both (`vantage` v9, `stack-infrastructure` v7) and at Meta through a utility-tolled lane (`meta` v9); no BESS supplier is named at any of the nine projects except Trimount (Hithium) and Colossus (Tesla). The caveat lifts when Phase 6 session 1's edition supersedes `named-project-bess-attach--opportunity--2026-08-30`.

**Phase 6 inherits this order:** session 1 — the named-project edition; session 2 — the grid-scale and §154 editions together, after the `byd` / `eve-energy` H1 2026 refreshes; session 3 — the AIDC edition, after the `megmeet` / `zhonhen` / `sinexcel` H1 2026 refreshes. Three sessions, within §7.3's 2–4. The brief for each is §7.8.

### 7.8 · Paste-in briefs for Phase 6 (Opus 5 xhigh) — written 2026-09-08 at v05.12r

Three sessions in the §7.7 order, each on the §7.5 pattern. Every brief shares the same fence: **reports are immutable** — a new file per edition carrying `supersedes`, the old file untouched; the old index entry flips to `superseded` and the new entry goes in newest-first; **the superseded edition's rows in `report-pins-verified.json` are removed** (the checker errors on a verified entry for a superseded report); every citation copied verbatim from the cited dossier's `sources[]`; figures only from the normalized-KPI overlay; analysis labeled; field notes never in content; the active style `intel-briefing`; `guidanceOverlays[]` only on modules that exist in `Profiler.gs` today (§7.7 finding 8). Each session keeps its edition's **topic slug** so ids stay `<topic>--<type>--<date>`.

**Session 1 — the named-project edition**

> Run Phase 6, session 1 of INTEGRATED-REMEDIATION-PLAN.md — regenerate the named-project opportunity report — on Opus 5 xhigh as a fresh session. Phase 6 is AUTHORING under the Profiler Report Command; it edits no published report, touches no dossier, guide, lesson or registry, and creates, updates or deletes no Routine.
>
> READ FIRST: repository-information/SESSION-CONTEXT.md (Latest Session), then INTEGRATED-REMEDIATION-PLAN.md §7.7 (the decision block for this report, findings 2–3 and 8, the Routine decision, the caveat) and §5, then .claude/rules/profiler-app.md → "Profiler Report Command" in full, PROFILER-SCHEMA.md → Report schema + Reports index + Named-projects registry, PROFILER-STYLES.md (active style), scripts/check-profiler-reports.py, and the edition you supersede: live-site-pages/profiler-data/reports/named-project-bess-attach--opportunity--2026-08-30.report.json with reports-index.json and repository-information/report-pins-verified.json.
>
> THE WORK — one push commit: (1) Scope per §7.7 — every dossier whose `relationships[].project` resolves in profiler-projects.json (24 on 2026-09-08 over 9 projects) plus `tesla`; derive the set on the day and state the rule in `scope.rationale`. Question: BESS attach across the nine registered projects — storage in the plan, supplier named, who holds the storage scope where none is. Type `opportunity`; id `named-project-bess-attach--opportunity--<today>`; `supersedes: "named-project-bess-attach--opportunity--2026-08-30"`. (2) Preflight per the Command's step 2 — coverage table with freshness; `oracle` (v4, row 2026-09-10) may be refreshed first or carried with staleness stated; no other refresh is owed. (3) First body section "What changed since the last edition": River Bend registered; storage in the plan at Lighthouse (vantage) and Project Jupiter (stack-infrastructure) with no supplier named; the Enbridge Cowboy tolled lane (meta); the ten new participants; Tesla cited through xai. Restate the whitespace call as supplier whitespace (§5). (4) Fresh coverage pins for all 25 at their current profileVersion / lastUpdated; overlays on power-infra-aidc-2026-08 and utility-aidc-procurement-2026-08 only if a finding bears on a section, anchors validated. (5) Index: new entry first, old entry `status: superseded`; remove the 13 `named-project-bess-attach--opportunity--2026-08-30` rows from report-pins-verified.json. (6) Plan: §6 Phase 6 row → session 1 done with the version; §5 caveat lifted in the plan's words; SESSION-CONTEXT via "remember session".
>
> DO NOT: edit the 2026-08-30 file; touch any dossier, guide, lesson, Classroom.gs, Profiler.gs, profiler-segments.json or profiler-projects.json; create, update or delete a Routine (the 2026-10-01 fire reads the newest current edition — this one); run a registry sync or graph rebuild.
>
> VERIFY: `python3 scripts/check-profiler-reports.py` — 0 errors; warnings are exactly the six that remain on the three untouched reports (sungrow v9, catl v7 ×2, zhonhen v7, hithium v13 ×2) plus none on the new edition — any warning on the new edition is a finding, fix before committing; `python3 scripts/check-classroom-content.py` 10 / 3 / 134, 0 / 0; `python3 scripts/check-classroom-pipeline.py --base origin/main` — P1 on plan files expected, no P3; `python3 scripts/verify-profiler-roles.py` unchanged.
>
> CHANGELOG — check, do not assume: v05.12r left the file at 99 sections after rotating the thirteen 2026-09-01 sections, one dated 2026-09-08. On 2026-09-08 EST your section makes 100 with two exempt → 98 → no rotation. On a later EST day → 100 non-exempt → rotation fires on the twenty-one 2026-09-02 sections (v04.14r–v04.34r) → 79; `git fetch --unshallow` before any SHA lookup; budget ~10 extra minutes. Verify with `grep -c '^## \[v[0-9]' repository-information/CHANGELOG.md` and `TZ=America/New_York date '+%Y-%m-%d'`.
>
> Normal Pre-Commit and Pre-Push checklists; one push commit on a claude/* branch (`git ls-remote` first). The Profiler page is an indirect affect (data-only; no page bump). End with "remember session".

**Session 2 — the grid-scale and §154 editions**

> Run Phase 6, session 2 of INTEGRATED-REMEDIATION-PLAN.md — regenerate the grid-scale BESS competitive report and the §154-listed suppliers risk report — on Opus 5 xhigh as a fresh session. Same fence as session 1: authoring only, no published report edited, no dossier, guide, lesson, registry or Routine touched **except** the preflight refreshes named below, which run the full Profiler Command with archival if you take them.
>
> READ FIRST: SESSION-CONTEXT.md (Latest Session); INTEGRATED-REMEDIATION-PLAN.md §7.7 (blocks 2 and 3, findings 1, 5, 6, 7 and 8); profiler-app.md → "Profiler Report Command" and "Scheduled Refreshes"; PROFILER-SCHEMA.md → Report schema, Reports index, Segments registry; PROFILER-STYLES.md; check-profiler-reports.py; the two editions you supersede (`grid-scale-bess--competitive--2026-08-29`, `s154-listed-bess-suppliers--risk--2026-08-29`), reports-index.json, report-pins-verified.json; live-site-pages/profiler-data/profiler-segments.json (the `storage-integrators-and-containers` roster); repository-information/profiler-refresh-calendar.json (the `byd` and `eve-energy` rows).
>
> THE WORK — one push commit: (1) PREFLIGHT: `byd` (H1 2026 due 2026-08-29) and `eve-energy` (H1 2026 due 2026-08-20) are overdue calendar rows and named indicators of both reports. If the desk has not landed them by your start, refresh both first under the Profiler Command (archive, bump, registry sync, graph rebuild, calendar rows advanced) — or proceed and state the staleness in each edition's `coverage.gaps[]`; say which in the CHANGELOG. (2) GRID-SCALE: scope = the registry's integrator incumbents + challengers on the day (18 on 2026-09-08) plus `flexgen` as the adjacent comparator; `eve-energy` out with the reason; question and type unchanged; id `grid-scale-bess--competitive--<today>`, `supersedes` the 2026-08-29 id; registry role as a column; whole-company revenue only from the KPI overlay with the denominator stated; "What changed since the last edition" first (Sungrow H1 2026 −29% with ESS carrying revenue; the ~~six~~ **eight** added members — the brief undercounted; §7.7 finding 5's own list names eight and the registry on the day confirmed 11 kept + 8 added − 1 removed (`eve-energy`) = 19, which is what the edition states (corrected at Phase 6 session 3, v05.15r; the original wording kept struck through for provenance); EVE's move; BYD's H1 if landed). (3) §154: scope = the statutory six (`gotion` added); id `s154-listed-bess-suppliers--risk--<today>`, `supersedes` the 2026-08-29 id; `rept`'s not-named record in `limitations[]`; CATL's Zhonhen capital increase as the new line in the fence; overlays re-authored on china-policy-stack-2026-08, bess-bankability-2026-08, power-infra-aidc-2026-08, utility-aidc-procurement-2026-08 (anchors validated). (4) Index: two new entries first, two old entries `superseded`; remove the 12 grid-scale and 5 §154 rows from report-pins-verified.json. (5) Plan §6: Phase 6 row → sessions 1–2 done; SESSION-CONTEXT via "remember session".
>
> DO NOT: edit either 2026-08-29 file; touch Classroom.gs, Profiler.gs, profiler-segments.json; create, update or delete a Routine; refresh any dossier other than the two named (if the desk landed them, refresh none).
>
> VERIFY: check-profiler-reports.py — 0 errors; warnings only on the untouched AIDC edition (zhonhen v7) and none on the two new editions; if the preflight refreshed `byd` / `eve-energy`, all six checkers exit 0 (sync-profiler-registry.py --check, check-profiler-study.py, check-profiler-relationships.py, check-profiler-crossrefs.py, build-profiler-graph.py, check-profiler-reports.py); check-classroom-content.py 10 / 3 / 134, 0 / 0; check-classroom-pipeline.py --base origin/main — no P3.
>
> CHANGELOG — check, do not assume, from the state session 1 leaves (it records its count and date in its own section); the rule is `grep -c '^## \[v[0-9]'` minus today's EST sections, rotation at 100 non-exempt on the oldest whole date group, `git fetch --unshallow` before any SHA lookup.
>
> Normal Pre-Commit and Pre-Push checklists; one push commit on a claude/* branch (`git ls-remote` first). Profiler page indirect affect only. End with "remember session".

**Session 3 — the AIDC power-conversion edition**

> Run Phase 6, session 3 of INTEGRATED-REMEDIATION-PLAN.md — regenerate the AIDC power-conversion competitive report — on Opus 5 xhigh as a fresh session. Same fence as sessions 1–2; the preflight refreshes named below are the only dossier work allowed.
>
> READ FIRST: SESSION-CONTEXT.md (Latest Session); INTEGRATED-REMEDIATION-PLAN.md §7.7 (block 4, findings 1, 4, 7 and 8); profiler-app.md → "Profiler Report Command"; PROFILER-SCHEMA.md → Report schema, Reports index; PROFILER-STYLES.md; check-profiler-reports.py; the edition you supersede (`aidc-power-conversion--competitive--2026-08-29`), reports-index.json, report-pins-verified.json; the `nvidia` dossier's `relationships[]` (the roster) and the Industry Guidance NVIDIA module (`nvidia-800vdc-2026-08`) whose `options` / `trusst` / `suppliers` sections the overlays anchor on.
>
> THE WORK — one push commit: (1) PREFLIGHT: `megmeet` and `zhonhen` H1 2026 interims (statutory deadline 31 Aug) and `sinexcel`'s (due 2026-08-11, overdue row) are not in their dossiers and are named indicators. Refresh what the desk has not landed under the Profiler Command, or proceed with the staleness stated; re-date the `megmeet` / `zhonhen` calendar rows if you refresh them. (2) Scope per §7.7 block 4 — the covered 800 VDC roster by layer (15 on 2026-09-08: infineon, vicor; delta-electronics, liteon, megmeet, flex; zhonhen, sungrow, sinexcel; vertiv, eaton, schneider-electric, abb, ge-vernova, hitachi-energy), `huawei-digital-power` out on the stated reason, `mitsubishi-electric` and `power-electronics` named in `coverage.gaps[]`. Question: who is winning the 800 VDC transition, layer by layer; type `competitive`; like-for-like within each layer's table; id `aidc-power-conversion--competitive--<today>`, `supersedes` the 2026-08-29 id. (3) "What changed since the last edition" first: Sungrow's EnerNeo against the "SST roster still forming" framing; the silicon layer; CATL's capital increase corroborated from the buyer side; FCC DA 26-870 narrowing the prong; the Zhonhen / Megmeet H1 outcome or its absence. (4) Fresh pins for all 15; overlays re-authored on nvidia-800vdc-2026-08 with validated anchors. (5) Index and pins file as in sessions 1–2 (remove the 8 AIDC rows). (6) Plan §6: Phase 6 → Done with the version; §7.3 / §7.4 unchanged; SESSION-CONTEXT via "remember session".
>
> DO NOT: edit the 2026-08-29 file; touch Classroom.gs, Profiler.gs, profiler-segments.json; create, update or delete a Routine; refresh any dossier other than the three named.
>
> VERIFY: check-profiler-reports.py — 0 errors / **0 warnings** (every current edition freshly pinned; every superseded edition untracked); the six checkers exit 0 if any dossier was refreshed; check-classroom-content.py 10 / 3 / 134, 0 / 0; check-classroom-pipeline.py --base origin/main — no P3.
>
> CHANGELOG — check, do not assume, from the state session 2 leaves; same rule and same unshallow precondition.
>
> Normal Pre-Commit and Pre-Push checklists; one push commit on a claude/* branch (`git ls-remote` first). Profiler page indirect affect only. End with "remember session".

### 7.9 · Paste-in brief for G6 (Opus 5 xhigh) — written 2026-09-13 at v05.42r, the first session after S3

One session on the §7.5 pattern. The fence: G6 is **guidance-module authoring by research synthesis** under the Industry Guidance Command's steps 4–10 (steps 1–3 are document-ingest steps and do not apply — there is no uploaded document, by the developer's 2026-09-07 decision on the G4 precedent); it edits no dossier, guide, lesson, registry or report, and creates, updates or deletes no Routine. The lesson that consumes the module (`interconnection-for-large-loads`, curriculum plan §3.3 and §7 row 26) is **Phase 4's**, not this session's.

> Run G6 of INTEGRATED-REMEDIATION-PLAN.md — the large-load interconnection guidance module, research synthesis — on Opus 5 xhigh as a fresh session. G6 is guidance-module AUTHORING: it adds one module to `guidanceDocs_()` in googleAppsScripts/Profiler/Profiler.gs, writes its analysis markdown, seeds the Scraper interest topic, and closes register row G6; it edits no dossier, study guide, lesson, registry or report, and creates, updates or deletes no Routine.
>
> READ FIRST: repository-information/SESSION-CONTEXT.md (Latest Session); INTEGRATED-REMEDIATION-PLAN.md §7.2 (the G6 paragraph), §7.3 (the 2026-09-13 re-evaluated order — G6 is order 1) and §6 (the G6 ledger row); CLASSROOM-CURRICULUM-PLAN.md §6 row G6 and §3.3 `interconnection-for-large-loads` (the lesson's outline names the sections the module must be able to support — two jurisdictions, Order 2023 and what it left for load, the large-load tariff as a class, who pays the studies, the co-location docket); .claude/rules/industry-guidance.md steps 4–10 (module shape, `group` meta, access model, GAS version bump, verification, Scraper seed, `reviewBy`); the six utility study guides (dominion-energy, oncor, aep, southern-company, xcel-energy, entergy) and burns-mcdonnell — the module is composed AGAINST them and must not restate them; the existing `**Provenance:**` line of any research-synthesis module (grid-equipment-shortage-2026-09 is the precedent) for the form.
>
> THE WORK — one push commit: (1) Research the federal layer from public primary sources only — FERC Order 2023 and 2023-A (the generator interconnection reforms and what they did not cover), FERC's large-load and co-location dockets (the PJM co-location proceeding and the Commission's 2025–2026 orders), the RTO large-load interconnection rules that exist today (PJM, MISO, SPP, ERCOT's SB 6 process via the Oncor guide), study-fee and readiness-deposit regimes, and the large-load tariff as a class of instrument across the six utilities — every quantitative claim with its docket, order or page; run scripts/check-source-reachability.py first and record blocked hosts. (2) Write repository-information/industry-guidance/large-load-interconnection-2026-09-analysis.md on the standard analysis shape, with a claims ledger. (3) Author `guidanceDocLargeLoadInterconnection_()` on the guidance section-kind vocabulary with id `large-load-interconnection-2026-09`, `group` = The AI Data-Center Wave, a `**Provenance:**` line naming it research synthesis with no ingested document, `reviewBy` set from its nearest dated gate (the next FERC compliance or effective date you find), registered in `guidanceDocs_()` immediately after `utility-aidc-procurement-2026-08`; contributor tier per the role matrix. (4) Scraper seed per industry-guidance step 9 (Scraper GAS bump applies). (5) Bump the Profiler GAS version and gs.version.txt per [PC-GS-VERSION] #1, `node --check` a .js copy, run scripts/check-gas-inner-scripts.js, and render the module in Playwright via gdRenderDoc(). (6) Close register row G6 in CLASSROOM-CURRICULUM-PLAN.md §6 with a dated re-check note (the row closes on the module's existence; the lesson is Phase 4 row 26), flip the G6 ledger row in INTEGRATED-REMEDIATION-PLAN.md §6, and note in §7.3 that order 1 is done and C3 is next. Do NOT author the lesson; do NOT migrate any module (that is C3).
>
> VERIFY: the module renders with zero page errors; the guidance index lists nine modules; `check-classroom-curriculum.py --strict` clean; CHANGELOG — check the counter, do not assume. Normal Pre-Commit and Pre-Push checklists; one push commit on a claude/* branch (`git ls-remote` first).

### 7.10 · Paste-in brief for C3 session 2 (Opus 5 xhigh) — written 2026-09-13 at v05.44r, at the close of C3 session 1

Session 2 of the 2–3 C3 allots. Session 1 landed the content and the read ops (v05.44r); the slice plan under the C3 bullet of `PHASE6-CLASSROOM-DESIGN.md` assigns **checklist items 2 and 3** here — the Admin lens re-host and the token-gated mentions route — and leaves items 4, 6, 7 plus the Profiler deep-link redirects and the deletion of Profiler's copy to session 3. The fence is the same shape as session 1's: a migration, not an authoring session.

**Facts established by session 1 that this brief depends on** (re-verify, do not assume): `reports-index.json` and the eight `*.report.json` files live at `live-site-pages/profiler-data/reports/`, reached as `OV_BASE + 'reports/…'` in Profiler and `CL_DATA_BASE + 'reports/…'` in Classroom — the same `profiler-data/` base in both. **Four `current` reports carry `overlayModules`, producing twelve overlay panels across five modules** (`nvidia-800vdc-2026-08` 3, `power-infra-aidc-2026-08` 3, `bess-bankability-2026-08` 2, `china-policy-stack-2026-08` 2, `utility-aidc-procurement-2026-08` 2); two anchors — `bess-bankability/counterparty` and `power-infra-aidc/markets` — take a panel from each of two different reports, so the renderer must append rather than replace. The other four modules must show no lens at all. `CL_ROLE_CAPS` already grants `reports` to **admin only**, so no access-matrix value changes and `verify-profiler-roles.py` is not required unless one does.

**The trap in item 2.** `gdApplyLens` inserts its panel before the section's `.gd-done-btn` when there is one. Guidance in Classroom renders **read-only** through sessions 1–2, so there is no `.cl-done-btn` on a guidance section — the port must not silently drop the panel when that selector misses, and the stale-anchor path (module revised after the report) must keep behaving as Profiler's does: a note plus placement before the glossary section, never a dropped panel.

**The trap in item 3.** `guidanceMentions_()` scans the module JSON, so after session 1 it can only answer where the content now lives. It moves to Classroom and is exposed on a **narrow token-gated route** — the mirror of `scHandleCorpus_` in `Scraper.gs` (`?action=corpus&t=…`, refused flat while the Script Property is unset, no default token in code) — which `Profiler.gs` proxies server-to-server exactly as `handleNewsOp_` proxies Scraper's corpus route. `guidanceAllowed_` stays the browser-facing boundary on the Profiler side; the page's `ovGuidanceMentionsLine` and its `ovGuideApi('mentions')` call are unchanged, which is the test that the chips still work. The token is a **new** shared Script Property, not `CORPUS_TOKEN` — a different pair of peers, a different secret — and until it is set both sides must report themselves unconfigured and the chips must render nothing rather than erroring.

> Run C3 session 2 of INTEGRATED-REMEDIATION-PLAN.md — the Guidance Homecoming, session 2 of 2–3 — on Opus 5 xhigh as a fresh session. C3 MIGRATES Industry Guidance from Profiler to Classroom; session 1 landed at v05.44r. This session authors no guidance module, no lesson, no landscape and no dossier, edits no registry or report, and creates, updates or deletes no Routine (the quarterly-review Routine re-point is session 3 and is developer-approved before it happens). It does NOT delete Profiler's copy of the nine modules and does NOT migrate progress ticks — both are session 3.
>
> READ FIRST: repository-information/SESSION-CONTEXT.md (Latest Session); PHASE6-CLASSROOM-DESIGN.md — the C3 bullet under "Phase plan", where session 1 wrote the SLICE PLAN (the eight checklist items mapped to sessions, the file-layout decision and its reasoning, the read-only window) and "Verification expectations" is the done-when; INTEGRATED-REMEDIATION-PLAN.md §7.10 (this brief), §7.3 (run table row 2) and §6 (the C3 ledger row); .claude/rules/profiler-app.md (the Role + Access matrix — OV_ROLE_CAPS `reports` in Profiler vs CL_ROLE_CAPS `reports` in Classroom); .claude/rules/gas-scripts.md (the GAS Projects table and [PC-GAS-CONFIG] #14); and the v05.44r CHANGELOG section for exactly what session 1 did.
>
> THE WORK — session 2, one push commit, checklist items 2 and 3 only. (1) **The Admin lens re-hosts in Classroom.** Port `gdLoadAdminLens` / `gdApplyLens` from Profiler.html to Classroom.html as `cl*` equivalents on the light palette, gated by `clCan('reports')` (admin only), reading `CL_DATA_BASE + 'reports/reports-index.json'` and `CL_DATA_BASE + 'reports/<id>.report.json'` from public Pages — same data, no new server route. Filter `status === 'current'` and `overlayModules[]` containing the module id, then render each `guidanceOverlays[]` entry ({moduleId, sectionId, title, ps[]}) into `#cl-<sectionId>`. Guidance renders read-only here, so there is NO `.cl-done-btn` to insert before — do not let a missed selector drop the panel, and keep Profiler's stale-anchor behaviour (a note plus placement before the glossary section) rather than dropping content an admin is meant to see. Fetch failures degrade silently: the lens is an enhancement, never a blocker for the module. (2) **One narrow token-gated mentions route on Classroom that Profiler.gs proxies.** Move `guidanceMentions_()` to Classroom.gs (it scans module JSON, so it can only answer where the content now lives), expose it on a token-gated route mirroring `scHandleCorpus_` in Scraper.gs — refused flat while the Script Property is unset, no default token ever in code, read-only and bounded — and have Profiler's `handleGuidanceOp_` `gop=mentions` branch call it server-to-server, exactly as `handleNewsOp_` proxies Scraper's corpus route. Use a NEW shared Script Property, not CORPUS_TOKEN. `guidanceAllowed_` stays the browser-facing boundary on the Profiler side. The page's `ovGuidanceMentionsLine` / `ovGuideApi('mentions')` must not change — that they still work unchanged is the test. While the token is unset, both sides report themselves unconfigured and the dossier chips render nothing rather than erroring. (3) Record in the slice plan what actually landed, and whether anything moved between sessions 2 and 3.
>
> DO NOT: touch the nine module literals (scripts/check-guidance-parity.py will fail the build if the two copies diverge — that is its job and it stays until session 3); touch Scraper.gs; delete Profiler's guidance content or redirect its deep links; wire guidance reading ticks in Classroom (a second live progress store is exactly what session 3's one-shot import exists to avoid).
>
> VERIFY: node --check on a .js copy of every .gs touched, plus the page's inline script blocks; scripts/check-gas-inner-scripts.js; scripts/check-guidance-parity.py still clean. Playwright render + screenshot read of every new surface with zero page errors — the lens must render **twelve overlay panels across five modules** from the four current reports (nvidia-800vdc 3, power-infra-aidc 3, bess-bankability 2, china-policy-stack 2, utility-aidc-procurement 2; the `bess-bankability/counterparty` and `power-infra-aidc/markets` anchors each take a panel from two different reports, so append, do not replace), no lens on the other four modules, and **no lens at all for a contributor** (prove the `reports` gate page-side and, where a server route is involved, in Node per tier the way session 1 proved the guidance gate). For the mentions route, prove the token boundary directly: a wrong or missing token gets a flat refusal, and an unset property reports not-configured rather than throwing. scripts/verify-profiler-roles.py if any access-matrix value changed (none should). scripts/check-classroom-curriculum.py --strict clean. scripts/check-readme-tree.py after any version bump. CHANGELOG — read the counter, do not assume (97/100 at v05.44r, so rotation is close; Scrapergs.changelog.md is at 49/50 but this session must not touch Scraper). Known pre-existing failures, neither caused nor fixed here: check-classroom-content.py exits 1 on ten stale segment-* lessons (S2's work per curriculum plan §10.9) and verify-profiler-roles.py reports 2 Profiler progress-persistence failures — confirm against a pristine HEAD worktree before attributing either to your change. Normal Pre-Commit and Pre-Push checklists; one push commit on a claude/* branch (git ls-remote first).

### 7.11 · Paste-in brief for C3 session 3 (Opus 5 xhigh) — written 2026-09-13 at v05.47r, at the close of C3 session 2

The last C3 session. Sessions 1 and 2 landed the content, the read ops, the guidance surface, the Admin lens and the token-gated mentions route (v05.44r–v05.47r); the slice plan under the C3 bullet of `PHASE6-CLASSROOM-DESIGN.md` leaves **items 4, 6 and 7** here, plus the two the checklist implies but does not number — Profiler's guidance deep-links redirect to Classroom, and Profiler's own copy of the modules is deleted. This is the session that ends the duplicate window, so it is the one where a mistake is not recoverable by falling back to the other app.

**Facts established by sessions 1–2 that this brief depends on** (re-verify, do not assume): the shared `GUIDANCE_PEER_TOKEN` **is set and working in both projects** — the dossier chips are live and were confirmed by the developer on the Tesla and NVIDIA dossiers at v05.47r. `guidanceMentions_()` already lives **only** in `Classroom.gs`, so Profiler's copy is **twelve** functions, not thirteen, and `scripts/check-guidance-parity.py` watches exactly those twelve. Profiler's progress store is one Script Property per account, `gd_progress:<email>`, and it holds **three kinds of doc id**: module ids, `study-<slug>` and `dossier-<slug>`. Classroom's is `cl_progress:<email>` (`CL_PROGRESS_PROP_PREFIX`). Values are **completion dates** (`'YYYY-MM-DD'`), with legacy `true` accepted forever and never rewritten.

**The trap in item 4.** `gd_progress:` is not a guidance store — it is a shared store that guidance, study guides and dossiers all write into. A migration that moves the whole property moves study and dossier ticks into Classroom, where nothing reads them and where they will be silently lost the next time either side writes. **Move only the keys whose doc id is a registered module id**, leave everything else in Profiler untouched, and make the import idempotent: it must be safe to run twice, and a Classroom tick that is already newer than the Profiler one must win rather than being overwritten. "Developer study history is not resettable state" is the design's phrasing and it is the whole reason this is a one-shot admin operation rather than a lazy merge.

**The trap in item 6.** The quarterly review Routine is `Industry Guidance quarterly review` (`trig_01CrhxzfBV6uKQNKpUXLLMSZ`, cron `0 13 15 1,4,7,10 *`, fresh session per fire). Re-pointing it is **developer-approved before it happens** — do not update, delete or recreate it without an explicit yes in the session, and never delete-and-recreate (that loses its run history).

**The trap in the deletion.** Deleting the twelve functions from `Profiler.gs` breaks `handleGuidanceOp_`'s `gop=index|doc` and every page surface above them — the masthead `✦ Industry Guidance` button, `#ov-guide-overlay`, `gdRenderDoc`, the search, the unified glossary, and `ovGuideOpenDoc`, which is what the **dossier mention chips call when clicked**. Those chips must keep working: after this session they should deep-link into Classroom (`Classroom.html#guidance/<id>`) rather than open a Profiler overlay. Decide deliberately how much of Profiler's `gd*` renderer goes and say so in the record — the Admin lens's `gdApplyLens`/`gdLoadAdminLens` are part of that engine and Profiler's own report view still needs the section primitives.

> Run C3 session 3 of INTEGRATED-REMEDIATION-PLAN.md — the Guidance Homecoming, the final session — on Opus 5 xhigh as a fresh session. C3 MIGRATES Industry Guidance from Profiler to Classroom; sessions 1 and 2 landed at v05.44r and v05.45r, with follow-up fixes at v05.46r and v05.47r. This session authors no guidance module, no lesson, no landscape and no dossier, and edits no registry or report.
>
> READ FIRST: repository-information/SESSION-CONTEXT.md (Latest Session); PHASE6-CLASSROOM-DESIGN.md — the C3 bullet under "Phase plan", where the SLICE PLAN records what sessions 1 and 2 actually landed and what is left, and "Verification expectations" is the done-when (note it says the migration is complete only when "migrated progress ticks verify against a pre-migration export"); INTEGRATED-REMEDIATION-PLAN.md §7.11 (this brief), §7.10 (session 2's brief), §7.3 (run table order 2) and §6 (the C3 ledger row); .claude/rules/industry-guidance.md (steps 4, 5, 6, 7 and 10 — the file item 7 re-targets); .claude/rules/profiler-app.md (the Role + Access matrix); and the v05.44r–v05.47r CHANGELOG sections for exactly what sessions 1 and 2 did.
>
> THE WORK — session 3, checklist items 4, 6 and 7 plus the redirects and the deletion. Do them in THIS order and consider splitting the push if the diff gets large; the deletion is last because everything else must be proven working first. (1) MIGRATE THE PROGRESS TICKS — a one-shot admin operation moving each account's guidance section ticks from Profiler's `gd_progress:<email>` Script Property to Classroom's `cl_progress:<email>`. Move ONLY doc ids that are registered module ids: the same property also holds `study-<slug>` and `dossier-<slug>` ticks, which stay in Profiler. Values are completion dates with legacy `true` accepted; the import must be idempotent and must not overwrite a newer Classroom tick with an older Profiler one. Take a pre-migration export first and verify the migrated ticks against it — that comparison is the design's stated done-when. Then wire guidance reading ticks in Classroom (the read-only window closes here) and let guidance items enter the C4 drill, which is decision 6 reaching its target state. (2) RE-POINT THE PROFILER DEEP LINKS — the dossier mention chips currently call ovGuideOpenDoc, which opens Profiler's own overlay; they must deep-link to Classroom.html#guidance/<id>. Anything else in Profiler that opens a module goes the same way. (3) DELETE PROFILER'S COPY — the twelve functions check-guidance-parity.py watches, plus whatever page-side engine is now dead, decided deliberately and recorded (the Admin lens shares that engine and Profiler's report view still needs the section primitives). Delete scripts/check-guidance-parity.py in the same commit — its whole purpose was the duplicate window. (4) RE-TARGET .claude/rules/industry-guidance.md at Classroom.gs — the module JSON vocabulary and renderer contract are unchanged, so this should be a file-target and op-name change, not a rewrite; correct step 5's access-model paragraph (guidance is CL_ROLE_CAPS in Classroom now, and gop=mentions is a token-gated peer route that Profiler proxies, not a third browser op) and step 10's Routine paragraph. (5) THE QUARTERLY ROUTINE — `Industry Guidance quarterly review`, trig_01CrhxzfBV6uKQNKpUXLLMSZ. ASK THE DEVELOPER before touching it, update the prompt in place rather than delete-and-recreate, and if approval does not come, leave it and say so in the record. (6) Record in the slice plan what landed, and close the C3 bullet.
>
> DO NOT: change any module's content (the ids are permanent — Scraper's `guidance:<module-id>` seeds and every lesson's provenance stamp key on them); touch Scraper.gs; move study-guide or dossier ticks out of Profiler; delete Profiler's copy before the ticks are migrated and verified and the deep links re-pointed; create, delete or recreate any Routine without an explicit developer yes.
>
> VERIFY: node --check on a .js copy of every .gs touched, plus every page's inline script blocks; scripts/check-gas-inner-scripts.js. Playwright render + screenshot read of every changed surface with zero page errors — guidance ticks working in Classroom, a dossier's mention chips deep-linking out to Classroom, Profiler's guidance surfaces gone cleanly with no dead buttons or console errors, and the Admin lens still rendering its twelve panels across five modules. Prove the tick migration against a pre-migration export, per account, and prove it is idempotent by running it twice. scripts/verify-profiler-roles.py (the Profiler guidance surface changes, so an access-matrix row may legitimately move — if it does, update the checker in the same commit). scripts/check-classroom-content.py, scripts/check-classroom-curriculum.py --strict and scripts/check-classroom-pipeline.py --selftest. scripts/check-readme-tree.py after any version bump. CHANGELOG — READ THE COUNTER AND DO THE NON-EXEMPT ARITHMETIC, do not trust the raw number: it stands at 100/100 raw but 96 non-exempt at v05.47r because four sections are dated 2026-09-13, so ROTATION IS DUE on the first push dated after that (oldest whole date group: thirteen sections dated 2026-09-04). The clone was deepened at v05.47r but a fresh session gets a fresh shallow clone — run `git fetch --unshallow origin main` BEFORE any SHA lookup or all thirteen archive as [SHA unavailable] permanently. Known pre-existing failures, neither caused nor fixed here: check-classroom-content.py exits 1 on ten stale segment-* lessons (S2's work per curriculum plan §10.9) and verify-profiler-roles.py reports 2 Profiler progress-persistence failures — confirm against a pristine HEAD worktree before attributing either to your change. Normal Pre-Commit and Pre-Push checklists; claude/* branch, git ls-remote before each push.

### 7.12 · Paste-in brief for the bankability review (Opus 5 xhigh) — written 2026-09-13 at v05.49r, at the close of C3

Order **3** of the §7.3 run table, and the first session after C3. It was scheduled beside C3 precisely because the module it edits moved during C3; now that the migration is closed, **it edits one file instead of two** and `check-guidance-parity.py` is not there to fail the build if it gets that wrong — because there is nothing left to keep in parity.

**Facts established by C3 that this brief depends on** (re-verify, do not assume): `guidanceDocBankability_()` lives **only** in `googleAppsScripts/Classroom/Classroom.gs`, below the `// CONTENT END` fence. Its module id is `bess-bankability-2026-08`, `updated` 2026-08-24, `reviewBy` **2026-10-01** — the only module inside `check-classroom-curriculum.py`'s 30-day horizon. Its eleven section ids are `what`, `stack`, `ninefortya`, `fires`, `bankability`, `counterparty`, `checklist`, `sales`, `ledger`, `cards`, `quiz`. **Three reports anchor `guidanceOverlays[]` at `counterparty`** — `grid-scale-bess--competitive--2026-09-08`, `s154-listed-bess-suppliers--risk--2026-09-08` and the superseded `…2026-08-29` — and the two current ones render as the two Admin-lens panels an admin sees in that section.

**The trap C3 created, and it is new since the last time anyone edited a module.** A module's section ids are no longer just anchors. Since v01.23g they are **progress-tick keys** in `cl_progress:<email>` and they are **embedded in C4 drill item ids** (`gc:<moduleId>:<sectionId>:<n>`, `gq:…`). Renaming or removing a section id therefore (a) orphans every account's tick for it — `clProgressVisible_` drops ticks whose section no longer exists, deliberately, so a rename reads as lost history rather than moved history; (b) invalidates that section's drill items and their SM-2 schedules; and (c) breaks the three report overlay anchors above. **Do not rename or delete a section id.** Edit prose inside a section freely; add a new section if the review needs one.

**The second-order effect, which is by design and should not be "fixed".** Editing a flashcard's or quiz item's text changes `clDrillHash_`, and `clDrillQueue_` treats a hash change as NEW — the item loses its schedule and is re-introduced. That is the intended rule ("the text moved under the schedule, so the schedule is not evidence of anything"). Expect it, say so in the record, do not work around it.

> Run the bankability review — order 3 of INTEGRATED-REMEDIATION-PLAN.md §7.3 — on Opus 5 xhigh as a fresh session. This is the quarterly-freshness review of ONE Industry Guidance module, `bess-bankability-2026-08` (`reviewBy` 2026-10-01), now that C3 has moved guidance into Classroom. It authors no new module, no lesson, no landscape and no dossier, edits no registry, and publishes no report.
>
> READ FIRST: repository-information/SESSION-CONTEXT.md (Latest Session); INTEGRATED-REMEDIATION-PLAN.md §7.12 (this brief), §7.3 (run table order 3) and §6; .claude/rules/industry-guidance.md IN FULL — it was re-targeted at Classroom.gs at v05.48r, so steps 4, 5, 6, 7 and 10 now name different files and different ops than any earlier module session used; .claude/rules/classroom-app.md (the `// CONTENT END` fence and the gateDigest obligation); and the v05.48r and v05.49r CHANGELOG sections for what C3 left behind.
>
> THE WORK — one push commit. (1) Inventory `guidanceDocBankability_()`'s dated gates and load-bearing claims: UL 9540A and NFPA 855-2026 status, the EPRI failure-rate figures and their caveat, the FEOC/tariff overlay dates, BNEF Tier 1 criteria, the IE/LTSA and insurance items, and anything in the claims ledger with a date on it. (2) Re-verify every gate that has passed or is imminent against PRIMARY sources with targeted web research — regulator, standards body, docket, or the company's own filing; never a secondary summary. (3) Update what has gone stale, bump the module's `updated`, and set a NEW `reviewBy` from its own next dated gate rather than a fixed cadence. A module still accurate but past its date gets a refreshed `reviewBy` only. Add a `revisions[]` entry dated today with a one-line note, in the shape the existing 2026-08-29 entry uses. (4) Re-check the report side: run `python3 scripts/check-profiler-reports.py` and confirm the three `counterparty` overlay anchors still resolve; if any current report is now materially out of date, RECOMMEND a re-run in the record — reports are immutable, never edit a published one. (5) Record the verdict.
>
> DO NOT: rename or delete ANY section id — since C3 they are progress-tick keys in `cl_progress:<email>` AND are embedded in C4 drill ids (`gc:<moduleId>:<sectionId>:<n>`), so a rename silently orphans the developer's reading history and resets that section's drill schedules, and it breaks three report overlay anchors. Do not change the module id (Scraper's `guidance:<module-id>` seeds and every lesson's provenance stamp key on it). Do not touch `Profiler.gs` or `Profiler.html` — guidance content is not there any more; if a change makes you open either, stop and re-read what you are doing. Do not touch Scraper.gs. Do not violate the content-scope rule: guidance to supplier/buyer GROUPS only, never to a single named company (statutory lists naming companies stay as objective fact).
>
> VERIFY: node --check on a .js copy of Classroom.gs; scripts/check-gas-inner-scripts.js; scripts/check-classroom-content.py; scripts/check-classroom-curriculum.py --strict (it should stop listing this module as due once `reviewBy` moves); scripts/check-classroom-pipeline.py --selftest and --base origin/main (guidance sits outside the content fence, so a P2 finding on your own diff is expected and is not a failure — you are a developer session, not a pipeline run; refresh `gateDigest` ONLY if P3 reports the gate surface itself changed, which it should not); scripts/check-profiler-reports.py; scripts/check-readme-tree.py after the version bump. Playwright render + screenshot read of `Classroom.html#guidance/bess-bankability-2026-08` at admin with ZERO page errors — every edited section legible, the two Admin-lens panels still rendering in `counterparty` above the Mark-as-understood button, and the flashcards and self-test sections intact. Render recipe: step 7 of .claude/rules/industry-guidance.md (seed the session AFTER page load — STORAGE_TYPE is sessionStorage and single-tab enforcement clears it — and override window._gasPost AFTER load too, because it is a top-level function declaration). Bump Classroom's GAS version in BOTH `Classroom.gs` VERSION and `live-site-pages/gs-versions/Classroomgs.version.txt`, and add a GENERIC entry to `Classroomgs.changelog.md` per changelog-security — never name the analyzed document or a specific finding publicly. CHANGELOG — READ THE COUNTER AND DO THE NON-EXEMPT ARITHMETIC: it reads 102/100 raw at v05.49r but only 96 non-exempt, because six sections are dated 2026-09-13; the moment a push lands on a LATER EST day all six stop being exempt and rotation IS due. Run `git fetch --unshallow origin main` BEFORE any SHA lookup or the whole oldest date group archives as [SHA unavailable] permanently. Oldest whole date group: thirteen sections dated 2026-09-04. `Profilerhtml.changelog.md` is at 49/50 but this session must not touch Profiler. Known pre-existing failures, neither caused nor fixed here: check-classroom-content.py exits 1 on ten stale segment-* lessons (S2's work per curriculum plan §10.9) and verify-profiler-roles.py reports 2 Profiler progress-persistence failures — confirm against a pristine HEAD worktree before attributing either to your change. Normal Pre-Commit and Pre-Push checklists; claude/* branch, git ls-remote before each push.

### 7.13 · Paste-in brief for Phase 4 row 1 (Opus 5 xhigh) — reconstructed 2026-09-13 at v05.54r, one version after the session it briefed

**This section is a backfill, and the gap is the point.** Order 3 (the bankability review) closed at v05.50r and advanced the run table's pointer to order 4 without writing a brief for it, so the `four-machines` session of v05.53r was handed a prompt that named "§7.13 (this brief)" against a file that ended at §7.12. That session recorded the gap rather than inventing the section; the row-2 session reconstructs it here from the prompt as issued, so the next Phase 4 row has a worked example in the place the run table points at. **The obligation this establishes: the session that closes a run-table row writes the next row's brief into this file in the same commit** — the pointer and the brief move together, or the brief exists only in a chat transcript.

The brief below is the v05.53r prompt verbatim except for the two facts it got wrong, corrected in place and marked: the clone is **not** already unshallowed (that is per-container and does not persist — a fresh remote session starts shallow, at 55 commits on the day), and the stale `segment-*` lesson count is **twelve**, not the ten §7.12 stated. Both corrections were established by the run.

> Author four-machines — Phase 4 row 1, order 4 of INTEGRATED-REMEDIATION-PLAN.md §7.3 — on Opus 5 xhigh as a fresh session. This is ONE mechanism lesson plus the track it creates. It authors no guidance module, no landscape, no dossier and no report, edits no registry outside Classroom.gs, and regenerates no segment-* lesson.
>
> READ FIRST: repository-information/SESSION-CONTEXT.md (Latest Session); INTEGRATED-REMEDIATION-PLAN.md §7.13 (this brief), §7.3 (run table order 4) and §6; CLASSROOM-CURRICULUM-PLAN.md §3.2 (the track and the lesson spec, verbatim), §7 row 1, §2.2 (the prereqs[] promise), §4 (the position table) and §8 item 9 (the track-edit rules); repository-information/CLASSROOM-SCHEMA.md for the lesson and track shapes; and .claude/rules/classroom-app.md IN FULL — especially the // CONTENT END fence, the provenance stamp rule, and the "Authoring a pipeline lesson" decision procedure.
>
> THE WORK — one push commit. (1) Author the lesson literal as strict JSON INSIDE the // CONTENT START … // CONTENT END fence, at the fixed section ids and kinds the curriculum plan names, with the group, the gate and the short the spec gives. (2) Build the provenance stamp from the inputs §7 names — every date read off the fetched file, every slug identity-matched, NEVER a fabricated input. (3) Create the track with the plan's verbatim short and its lesson order, of which only this lesson exists now. (4) Register the lesson at the END of clLessons_() and the track at its §4 POSITION in clTracks_() per curriculum plan §8 item 9 — not appended. (5) Write the prereqs[] §2.2 has promised since v05.07r, and bump each edited track's updated. (6) Flip the curriculum plan's §7 row and this file's §7.3 run-table row, both dated.
>
> DO NOT: put the lesson literal outside the // CONTENT END fence — guidance goes there, lessons do not. Do not touch any guidanceDoc*_() literal, Profiler.gs, Profiler.html or Scraper.gs. Do not regenerate a segment-* lesson — that is S2's, per curriculum plan §10.9. Do not refresh gateDigest: no GATE_SYMBOLS member moves in this work, and P3 should be silent. Do not "fix" the P5 finding by appending the track — the §4 position is what §8 item 9 specifies and P5 is expected and accepted for a developer session, exactly as S1's lesson half recorded at v05.18r. Do not fabricate a provenance input or a pin date.
>
> VERIFY: node --check on a .js copy of Classroom.gs; scripts/check-gas-inner-scripts.js; scripts/check-classroom-content.py (its error count must be BYTE-IDENTICAL to a pristine HEAD worktree — there are 24 pre-existing errors across **TWELVE** stale segment-* lessons, which are S2's work, neither caused nor fixed here; the §7.12 brief's figure of "ten" was wrong); scripts/check-classroom-curriculum.py --strict; scripts/check-classroom-pipeline.py --selftest (13 fixtures, 0 failures) and --base origin/main (expect P1 and P5, expect NO P3); scripts/check-readme-tree.py after the version bump. Playwright render of the new lesson AND the track view at admin with ZERO page errors — every section legible, every table and callout rendering, the flashcards and self-test intact. Render recipe: step 7 of .claude/rules/industry-guidance.md — seed the session AFTER page load (STORAGE_TYPE is sessionStorage and single-tab enforcement clears it on load) and override window._gasPost AFTER load too (it is a top-level function declaration), hide #auth-wall / .splash / #gas-pill / #verify-overlay, then clHeaderShow(); clAppMount();. **Answer the ops from the real serving functions** — run the PROJECT region in Node and replay clTrackIndexFor_ / clLessonIndexFor_ / clTrackFor_ / clStudyNext_ through the stub, so the render is of what the server would actually send, and so clStudyNext_ proves the new track is on the teaching walk rather than merely registered. Bump Classroom's GAS version in BOTH Classroom.gs VERSION and live-site-pages/gs-versions/Classroomgs.version.txt, and add a GENERIC entry to Classroomgs.changelog.md per changelog-security.
>
> THE DEPLOY IS VERIFIED NOW — this is new since v05.51r and you should read the run rather than trust the green tick. Deploy Classroom should print "Classroom deploy confirmed (GET): Updated to <the new version>" with NO POST attempt. If it does not, the run goes RED at the final step Fail the run if any GAS deploy was unconfirmed, which is intended behaviour and means the deploy genuinely did not land — run pullAndDeployFromGitHub() in the Apps Script editor and check line 1. The GitHub Actions log API truncates before the Deploy step on a run this size, so the authoritative independent check is curl "https://script.google.com/macros/s/$(jq -r .DEPLOYMENT_ID googleAppsScripts/Classroom/Classroom.config.json)/exec?action=api&op=deploy". The mechanism is documented in .claude/rules/workflows.md → "GAS Self-Update Deploy Verification"; its failure is deferred on purpose so branch cleanup still runs, and that design must not be "tidied up" into failing in place.
>
> CHANGELOG — READ THE COUNTER AND DO THE NON-EXEMPT ARITHMETIC, and note that **the clone is shallow**: `git rev-list --count HEAD` on a fresh remote session reads about 55, because unshallowing is per-container and does not persist. Run `git fetch --unshallow origin main` BEFORE any SHA lookup, and before trusting `origin/main` for anything — a stale `origin/main` ref also makes `git log -1 --format=%cs origin/main -- <registry>` return an OLD date, which would silently produce a wrong `concepts:` pin. Sections dated today (EST) are exempt from rotation; rotation fires when the NON-EXEMPT count reaches 100. The oldest whole date group is thirteen sections dated 2026-09-04. Profilerhtml.changelog.md is at 49/50 but this session must not touch Profiler. Normal Pre-Commit and Pre-Push checklists; claude/* branch, git ls-remote before each push, and rebase onto origin/main first — the auto-merge workflow adds a [skip ci] SHA-tracker commit after every merge, so origin/main is always one commit ahead of your last push.

### 7.14 · Paste-in brief for Phase 4 row 4 (Opus 5 xhigh) — written 2026-09-13 at v05.55r, at the close of row 3

Phase 4 is now **three of twenty-six**, and the registry work is finished: every track the curriculum plan specifies exists, so rows 4–24 author a lesson and nothing else. That makes the checker signature predictable — **P1 on the plan files and, when the lesson lands mid-track, P5 on the `lessons[]` insertion; never P3.** A run that sees anything else should stop and read why.

**What the last three runs established, so this one does not rediscover it.** The render harness answers the ops from the **real serving functions** — the PROJECT region run in Node, `clTrackIndexFor_` / `clLessonIndexFor_` / `clTrackFor_` / `clStudyNext_` replayed through a `window._gasPost` stub — which is what makes the render evidence of what the server would send rather than of a fixture. `clStudyNext_` is the proof a lesson is on the teaching walk, and for a mid-track insertion it also shows whose pointer moved. The Actions log API truncates before the Deploy step on a run this size, so the authoritative deploy check is `curl` against `?action=api&op=deploy`. And the EST clock, not UTC, governs the CHANGELOG rotation rule — the two diverge for five hours every evening.

**One hazard found at row 3 — now a checker, so this paragraph is background rather than an instruction.** `clFmt` in `Classroom.html` resolves `**bold**` with `/\*\*([^*]+)\*\*/g`, which **cannot contain an asterisk**, so *italic nested inside bold* silently fails and renders literal asterisks to the reader. Use one level of emphasis per span. **`check-classroom-content.py` errors on it as of v05.56r** — across lessons, tracks and the nine guidance modules — so no manual simulation is needed; a clean checker run is the check. *(The manual recipe this paragraph carried at v05.55r named too wide a scope: it listed `tiles`, `glossary`, `short` and `title`, which reach the DOM through `textContent` and would have produced false positives. The implemented scope is narrower and kind-aware — see `CLASSROOM-SCHEMA.md` → Verification.)*

> Author the next unbuilt lesson of `CLASSROOM-CURRICULUM-PLAN.md` §7 — take the **lowest-numbered row without a "Built" note** — on Opus 5 xhigh as a fresh session. This is ONE mechanism lesson. It authors no guidance module, no landscape, no dossier and no report, creates no track, and regenerates no segment-* lesson.
>
> READ FIRST: repository-information/SESSION-CONTEXT.md (Latest Session); INTEGRATED-REMEDIATION-PLAN.md §7.14 (this brief), §7.3 (run table order 6) and §6; CLASSROOM-CURRICULUM-PLAN.md §3's spec for your row VERBATIM — its section ids, kinds and order are the contract — plus §7's row for the stamps, §4 for the lesson's position in its track, and §8 items 2, 4, 6 and 9; repository-information/CLASSROOM-SCHEMA.md; and .claude/rules/classroom-app.md IN FULL.
>
> THE WORK — one push commit. (1) Author the literal as strict JSON INSIDE the // CONTENT START … // CONTENT END fence, at the section ids and kinds §3 fixes, in its order. **The ids are permanent — they key progress and drill ids — so follow the spec exactly even where §8 item 6's kind-variety assertion disagrees with it; record the disagreement rather than deviating.** (2) Build the stamp from the inputs §7 names: fetch each one, match its `slug` to the ref, and read the date off the fetched file — a study guide's and a dossier's `lastUpdated`, and for `concepts:` the file's last commit date on `origin/main`. **Fetch origin/main first**, because a stale ref silently yields an old date. NEVER fabricate an input; if a listed source contributes nothing to the finished lesson, omit it and say so. (3) Register at the END of clLessons_() and insert into the track's lessons[] at its §4 position — among the lessons that actually exist. (4) Flip the §7 row and the §7.3 run-table row, both dated. (5) Write §7.15 — the next row's brief — in the same commit.
>
> DO NOT: put the literal outside the fence. Do not touch any guidanceDoc*_() literal, Profiler.gs, Profiler.html or Scraper.gs. Do not regenerate a segment-* lesson (S2's, per §10.9). Do not refresh gateDigest — no GATE_SYMBOLS member moves in lesson authoring, and P3 should be silent; if P3 fires, something is wrong and refreshing the digest would hide it. Do not "fix" P5 by appending instead of inserting — the §4 position is the teaching order the study-next pointer walks. Do not drill a company as an answer: a mechanism lesson uses a company only as a worked example (CLASSROOM-SCHEMA.md, Content contract).
>
> VERIFY: node --check on a .js copy of Classroom.gs; scripts/check-gas-inner-scripts.js; scripts/check-classroom-content.py — its error count must be BYTE-IDENTICAL to a pristine HEAD worktree captured BEFORE any edit (24 errors across twelve stale segment-* lessons, S2's work, neither caused nor fixed here) and warnings must stay at 0, which means the `short` must be ≤160 characters even when §3's spec text is longer — trim it as `four-machines`, `how-a-utility-buys` and `the-800-vdc-shift` all did; scripts/check-classroom-curriculum.py --strict; scripts/check-classroom-pipeline.py --selftest (13 fixtures, 0 failures) and --base origin/main (expect P1 + P5, expect NO P3); scripts/check-readme-tree.py after the version bump. The content checker now covers the clFmt markup hazard, so a clean run is the check. Playwright render at admin with ZERO page errors, ops answered by the real serving functions, and read the screenshots anyway — a checker sees strings, not layout. Bump Classroom's GAS version in BOTH Classroom.gs VERSION and live-site-pages/gs-versions/Classroomgs.version.txt, and add a GENERIC entry to Classroomgs.changelog.md per changelog-security.
>
> THE DEPLOY IS VERIFIED — read the run, not the tick, and **a red gate is not proof the deploy failed.** `Deploy Classroom` should report the merged version with no POST attempt; if it does not, the run goes RED at `Fail the run if any GAS deploy was unconfirmed`. That failure is deferred on purpose so the merge and branch cleanup still run, and **that design must not be "tidied up" into failing in place.** **Corrected 2026-09-13 at v05.58r, from run #568 — the wording this brief carried at v05.57r said a red gate "means the deploy did not land", and that is false in one direction.** Run #568 went red on Classroom and the deploy *had* landed: `.github/scripts/gas-deploy.sh` fires one GET (`--max-time 120`) and, failing that, one POST, with **no poll afterwards**, so when the GET leg outruns its budget the confirmation is lost even though Apps Script finishes the update seconds later. `Classroom.gs` is now ~1.9 MB and is the slowest project in the repo, so it is the one that hits this. **The consequence is the part that matters:** the `deploy` job requires `auto-merge.result == 'success'`, so a red gate **skips the GitHub Pages deploy entirely** — `Classroomgs.version.txt` and `Classroomgs.changelog.md` stay stale on the live site even though the GAS project is current. **So: always confirm independently** with `curl "https://script.google.com/macros/s/$(jq -r .DEPLOYMENT_ID googleAppsScripts/Classroom/Classroom.config.json)/exec?action=api&op=deploy"` — `Already up to date (<version>)` means it landed — and also check `https://lightaisolutions.github.io/Sales/gs-versions/Classroomgs.version.txt`. If the GAS deploy landed but Pages is stale, the documented recovery is a **`workflow_dispatch` on `main`**: `auto-merge` is gated `github.ref != 'refs/heads/main'` so it skips, and `deploy` then fires through its `auto-merge.result == 'skipped' && is-initialized` branch. That is what run #569 did, and it went green.
>
> CHANGELOG — **use the EST clock, not UTC.** `TZ=America/New_York date '+%Y-%m-%d'` is what the rotation rule means by "today"; the harness clock may already have rolled over while EST has not, and firing rotation early is a real error. Sections dated today (EST) are exempt; rotation fires when the NON-EXEMPT count reaches 100. **A remote session's clone is shallow** — check `git rev-list --count HEAD` and run `git fetch --unshallow origin main` before any SHA lookup, because unshallowing does not persist between containers. Oldest whole date group: thirteen sections dated 2026-09-04. `Profilerhtml.changelog.md` is at 49/50 but this session must not touch Profiler. Normal Pre-Commit and Pre-Push checklists; `claude/*` branch, `git ls-remote` before each push, and restart the branch from `origin/main` first — the prior push merged and the auto-merge workflow adds a `[skip ci]` SHA-tracker commit after every merge.

### 7.15 · Paste-in brief for Phase 4 row 5 (Opus 5 xhigh) — written 2026-09-13 at v05.57r, at the close of row 4

Phase 4 is **four of twenty-six**. Rows 1–4 have settled the shape of a run, so this brief only records what the last one added.

**The checker signature is predictable, and row 5 breaks the recent pattern.** Rows 3 and 4 both inserted mid-track and so reported **P1 + P5**. Row 5's lesson joins `electrical-foundations`, which holds only `four-machines` today, so its §4 position 3 *is* an append among the built lessons — expect **P1 alone, no P5 and no P3**. A P5 on row 5 means the insertion went somewhere it should not have; anything else means stop and read why.

**One standing job this row should take, because it is now blocking a checker.** A `proscons` card's `t` and `meta` reach the DOM through `clEl()`'s `textContent`, not `clFmt` — and so do a `timeline`'s `lanes` values, a `bars` item's `label` and `sub`, a section's `title` and `read`, and the lesson's own `title`, `short`, `tiles[]` and `glossary[]`. A `{{term}}`, `**bold**` or `*italic*` in any of them prints its literal punctuation to the reader. Row 4 found this in a screenshot of its own lesson, removed its nine occurrences before commit, and found **nine already shipped**: `bridge-power` (`ways-to-eat-the-same-gas` cards 0, 2, 3 — `meta`), `the-control-stack` (`following-or-forming` cards 0 and 1 — `t`, and card 1 — `meta`), `where-bess-plugs-in` (`three-sockets` cards 0 and 1 — `t`) and `the-800-vdc-shift` (`glossary[6].d`, a `*square*`). **Fix those nine and then add the check** — the mirror of the v05.56r markup check, over the plain-field set above — in that order, because the check errors on them and a Classroom run's content-checker error count must stay byte-identical to a pristine `HEAD`. The wording change owes **no** `revisions[].changed[]` entry (G4: meaning does not move, and these are not `sections[]` fields a completed tick can be reconciled against), which is exactly why row 4 did not bundle it: editing another row's literal on a lesson-authoring commit adds a P8 finding to a run whose signature is the thing the next run reads. Expect **P1 + P8** on the commit that does it, and say so in its own note.

**What the last four runs established, so this one does not rediscover it.** The render harness answers the ops from the **real serving functions** — the PROJECT region run in Node, `clTrackIndexFor_` / `clLessonIndexFor_` / `clTrackFor_` / `clStudyNext_` replayed through a `window._gasPost` stub — which is what makes the render evidence of what the server would send rather than of a fixture. `clStudyNext_` is the proof a lesson is on the teaching walk: run it three ways — fresh, with every *other* lesson completed (it must then point at the new one), and with the new lesson completed too (it must move on) — and a mid-track insertion also shows whose pointer moved. The Actions log API truncates before the Deploy step on a run this size, so the authoritative deploy check is `curl` against `?action=api&op=deploy`. The EST clock, not UTC, governs the CHANGELOG rotation rule. And **the screenshots are not a formality** — the two defects found in this corpus so far (nested emphasis at row 3, plain-field markup at row 4) were both invisible to every checker and both visible in the first screenshot of the section that carried them.

> Author the next unbuilt lesson of `CLASSROOM-CURRICULUM-PLAN.md` §7 — take the **lowest-numbered row without a "Built" note** — on Opus 5 xhigh as a fresh session. This is ONE mechanism lesson. It authors no guidance module, no landscape, no dossier and no report, creates no track, and regenerates no segment-* lesson.
>
> READ FIRST: repository-information/SESSION-CONTEXT.md (Latest Session); INTEGRATED-REMEDIATION-PLAN.md §7.15 (this brief), §7.3 (run table order 6) and §6; CLASSROOM-CURRICULUM-PLAN.md §3's spec for your row VERBATIM — its section ids, kinds and order are the contract — plus §7's row for the stamps, §4 for the lesson's position in its track, §5 for the failure-map row its `where-it-fails` draws from, and §8 items 2, 4, 6 and 9; repository-information/CLASSROOM-SCHEMA.md; and .claude/rules/classroom-app.md IN FULL.
>
> THE WORK — one push commit. (1) Author the literal as strict JSON INSIDE the // CONTENT START … // CONTENT END fence, at the section ids and kinds §3 fixes, in its order. **The ids are permanent — they key progress and drill ids — so follow the spec exactly even where §8 item 6's kind-variety assertion disagrees with it; record the disagreement rather than deviating.** (2) Build the stamp from the inputs §7 names: fetch each one, match its `slug` to the ref, and read the date off the fetched file — a study guide's and a dossier's `lastUpdated`, and for `concepts:` the file's last commit date on `origin/main`. **Fetch origin/main first**, because a stale ref silently yields an old date. NEVER fabricate an input; if a listed source contributes nothing to the finished lesson, omit it and say so. (3) Register at the END of clLessons_() and insert into the track's lessons[] at its §4 position — among the lessons that actually exist. (4) Fix the nine shipped plain-field markup occurrences named above and add the mirror check to scripts/check-classroom-content.py, in that order. (5) Flip the §7 row and the §7.3 run-table row, both dated. (6) Write §7.16 — the next row's brief — in the same commit.
>
> DO NOT: put the literal outside the fence. Do not touch any guidanceDoc*_() literal, Profiler.gs, Profiler.html or Scraper.gs. Do not regenerate a segment-* lesson (S2's, per §10.9). Do not refresh gateDigest — no GATE_SYMBOLS member moves in lesson authoring, and P3 should be silent; if P3 fires, something is wrong and refreshing the digest would hide it. Do not add a revisions[] entry for the plain-field markup fixes — the meaning does not move (G4). Do not drill a company as an answer: a mechanism lesson uses a company only as a worked example (CLASSROOM-SCHEMA.md, Content contract).
>
> VERIFY: node --check on a .js copy of Classroom.gs; scripts/check-gas-inner-scripts.js; scripts/check-classroom-content.py — capture a pristine HEAD baseline BEFORE any edit and diff against it; the 24 errors across twelve stale segment-* lessons are S2's work, neither caused nor fixed here, and warnings must stay at 0, which means the `short` must be ≤160 characters even when §3's spec text is longer — trim it as rows 1–4 and `the-800-vdc-shift` all did. Then scripts/check-classroom-curriculum.py --strict; scripts/check-classroom-pipeline.py --selftest (13 fixtures, 0 failures) and --base origin/main (expect P1, expect NO P5 for an appending row, expect NO P3, and P8 only from the markup fixes); scripts/check-readme-tree.py after the version bump. Playwright render at admin with ZERO page errors, ops answered by the real serving functions, and **read every section's screenshot** — a checker sees strings, not layout, and that is how both of this corpus's rendering defects were caught. Bump Classroom's GAS version in BOTH Classroom.gs VERSION and live-site-pages/gs-versions/Classroomgs.version.txt, and add a GENERIC entry to Classroomgs.changelog.md per changelog-security.
>
> THE DEPLOY IS VERIFIED — read the run, not the tick, and **a red gate is not proof the deploy failed.** `Deploy Classroom` should report the merged version; if it does not, the run goes RED at `Fail the run if any GAS deploy was unconfirmed`. That failure is deferred on purpose so the merge and branch cleanup still run, and **that design must not be "tidied up" into failing in place.** **Updated 2026-09-14 at v05.59r — this paragraph carried two wrong things and both are now fixed in the code.** Run #568 went red on Classroom and the deploy *had* landed. The v05.58r wording blamed the GET leg outrunning its 120s budget; that was wrong — **the whole step took 76 seconds**, so neither leg came near its ceiling. The exec endpoint simply answered with something that was not the function's return string: reproduced by hand minutes later, one GET returned Google's HTML shell and the very next GET returned `Already up to date (v01.29g)`. A non-answer, not a slow deploy. **So `.github/scripts/gas-deploy.sh` now polls** — after the GET and POST legs both fail it re-reads the GET route five more times behind 5/15/30/60/90s of backoff, reporting the confirming leg as `POLL <n>`. **Your run is the first that will actually exercise it**, because every `Deploy <Project>` step exits early unless that project's `.gs` is in the merge diff and v05.59r touched no `.gs`. Expect either a clean first-leg confirmation as usual, or a `POLL <n>` line — and if you see the latter, say so in your §7 note, because it is the evidence the poll earned its place. **Confirm independently either way**, since the Actions log API truncates before the Deploy step on a run this size: `curl "https://script.google.com/macros/s/$(jq -r .DEPLOYMENT_ID googleAppsScripts/Classroom/Classroom.config.json)/exec?action=api&op=deploy"` — `Already up to date (<version>)` means it landed — and `https://lightaisolutions.github.io/Sales/gs-versions/Classroomgs.version.txt` for the Pages side. **A red gate still skips the Pages deploy entirely** (`deploy` needs `auto-merge.result == 'success'`), so if GAS is current and Pages is stale, the recovery is a **`workflow_dispatch` on `main`**: `auto-merge` is gated `github.ref != 'refs/heads/main'` so it skips, and `deploy` fires through its `auto-merge.result == 'skipped' && is-initialized` branch. Run #569 did exactly that and went green.
>
> CHANGELOG — **use the EST clock, not UTC.** `TZ=America/New_York date '+%Y-%m-%d'` is what the rotation rule means by "today"; the harness clock may already have rolled over while EST has not, and firing rotation early is a real error. Sections dated today (EST) are exempt; rotation fires when the NON-EXEMPT count reaches 100. **Updated 2026-09-14 at v05.60r: the rotation this paragraph predicted has already fired.** EST rolled over, the fourteen sections dated 2026-09-13 stopped being exempt, the non-exempt count went 96 → 111, and v05.59r moved the oldest whole date group — thirteen sections dated 2026-09-04, `v04.48r` through `v04.60r` — into `CHANGELOG-archive.md` with SHA enrichment on every header. **The file now stands at 100 raw and 98 non-exempt (`Sections: 100/100`), so your push will not rotate** unless it lands on a later EST date than the sections already in the file; if it does, the next oldest whole group is eighteen sections dated 2026-09-05. **A remote session's clone is shallow** — check `git rev-list --count HEAD` and run `git fetch --unshallow origin main` before any SHA lookup, because unshallowing does not persist between containers. `Profilerhtml.changelog.md` is at 49/50 but this session must not touch Profiler. Normal Pre-Commit and Pre-Push checklists; `claude/*` branch, `git ls-remote` before each push, and restart the branch from `origin/main` first — the prior push merged and the auto-merge workflow adds a `[skip ci]` SHA-tracker commit after every merge.

### 7.16 · Paste-in brief for Phase 4 row 6 (Opus 5 xhigh) — written 2026-09-14 at v05.61r, at the close of row 5 — **SUPERSEDED BY §7.18**

> **Do not paste this one.** Its reasoning still holds and §7.18 cites it, but **every figure in it went stale** when the segment regeneration run (v05.62r) and S2 session 1 (v05.64r) landed between it and the row: the pristine content-checker baseline is **0 errors / 0 warnings**, not 24; the candidate stamp is **31 refs, seven of them dossiers**, not "roughly thirty, four dossiers"; the repo CHANGELOG is **105 raw / 98 non-exempt**, not 101/99; and the next brief to write is **§7.19**, not §7.17. §7.18 carries the full corrections table. Kept as written because it is the v05.61r record.

Phase 4 is **five of twenty-six**. Rows 1–5 have settled the shape of a run; this brief records only what row 5 added and what row 6 does differently.

**Row 6 goes back to inserting, so the signature flips back.** Row 5 was the first appending row and reported **P1 + P8, no P5**. Row 6's lesson is `who-buys-storage` at `market-access` **position 1**, and that track holds only `how-a-utility-buys` today — so position 1 is an **insert at the head**, not an append, and **P5 fires and is accepted**, exactly as rows 3 and 4's mid-track insertions did. Expect **P1 + P5, no P3**. **Expect no P8 at all**: row 5 discharged the one standing cross-lesson job (the nine plain-field markup fixes), so nothing should now edit another row's literal. A P8 on row 6 means a section you did not intend to touch moved — stop and read it rather than naming it in a `changed[]`.

**The id-permanence rule has an explicit, written exception on this row — take it.** The standing rule is that §3's section ids are the contract and are followed even where §8 disagrees. §3.5's **v05.07r scope line overrides its own outline for this lesson**: the title becomes *Eleven Buyer Classes* and the section `eight-classes` becomes **`the-classes`**, because the registry gained `utility`, `investor` and `advisor` after the plan was written and eight is simply wrong. That is the spec instructing a change, not a session deviating from it, and it is safe because the lesson does not exist yet — no progress tick and no drill id keys on `eight-classes`. Follow the outline's other five ids (`the-ipp`, `epc-versus-gc`, `the-landlord-tiers`, `where-it-fails`, `drill`, `check-yourself`) exactly.

**This row has by far the largest candidate stamp in Phase 4, and §3.5 tells you to prune it.** The §7 row and the §3.5 scope line together list roughly thirty refs — four `profile:` dossiers, the B3–B6 and C5–C6 buyer guides, the investor and advisor guides, `concepts:`. §3.5's own instruction is "**pin only the guides a row is drawn from**", and the stamp rule is "**never fabricate an input**". So: fetch every candidate, read it, and pin **only** those that actually put something in the finished lesson — then **say in the CHANGELOG and the §7 note which listed sources you fetched and did not pin, and why**. Row 5 used all nine of its candidates and said so; this row almost certainly will not use all thirty, and the omission is the interesting part of its record. Every pinned date is read off the body of the document fetched **that run**, against a **freshly fetched `origin/main`** — a stale ref silently yields an old date.

**The content contract is under the most pressure it has been under, because this row's material *is* companies.** `who-buys-storage` is a **mechanism lesson**, so the rule is unchanged and absolute: a company appears as a **worked example**, never as the answer. A card may ask *what an IPP buys on, and who signs* — it may never ask *which company is an IPP*, or *how many gigawatt-hours anyone shipped*. That drilling is the segment lessons' and the roster deck's, and neither is this row. The tell that a card has crossed the line is that its answer is a proper noun.

**The plain-field markup check now exists, so a whole class of defect is a checker error rather than a screenshot finding.** v05.61r added the mirror of the v05.56r nested-emphasis check: `{{term}}`, `**bold**` and `*italic*` are now **errors** in every field the renderer prints as text rather than sending to `clFmt` — a `proscons` card's `t` and `meta`, a `timeline`'s `lanes` values, a `bars` item's `label` and `sub`, a section's `title` and `read`, a `revisions[].note`, and a lesson's or track's own `title`, `short`, `tiles[]` and `glossary[]`. `provenance.inputs[].note` is in neither check — it is never rendered. **`epc-versus-gc` is a `proscons` section**, so this row writes exactly the fields that shipped the original defect four times: keep its `t` and `meta` plain and put the emphasis in `adv`/`dis`, which *are* formatted. The pristine `HEAD` baseline to diff against is **24 errors, 0 warnings** — the twenty-four are twelve stale `segment-*` lessons and belong to S2.

**What the last five runs established, so this one does not rediscover it.** The render harness answers the ops from the **real serving functions** — the PROJECT region run in Node, `clTrackIndexFor_` / `clLessonIndexFor_` / `clTrackFor_` / `clStudyNext_` replayed through a `window._gasPost` stub — which is what makes the render evidence of what the server would send rather than of a fixture. Two harness details row 5 had to work out and you should not: the page reads its session from **`sessionStorage`**, not `localStorage` (`HTML_CONFIG.STORAGE_TYPE`), so seed both; and **hide the overlays with CSS, never `remove()` them** — removing `#auth-wall` / `#gas-app` makes unrelated page code dereference a null element and raises a `pageerror` that has nothing to do with the lesson, which is exactly the false positive that wastes a cycle. `clStudyNext_` is the proof a lesson is on the teaching walk: run it three ways — fresh, with every *other* lesson completed, and with the new lesson completed too — and for an inserting row also show whose pointer moved. **Read every section's screenshot**: the three defects this corpus has found were all invisible to every checker and all visible in the first screenshot of the section carrying them, and the third — a four-column table whose first column wraps one word per line — is still open as a `.cl-tbl` `min-width` question recorded in the row 5 note.

**THE DEPLOY — row 5 was the first run that could exercise the poll added at v05.59r**, because every `Deploy <Project>` step exits early unless that project's `.gs` is in the merge diff and v05.59r touched none. Watch the `Deploy Classroom` step for a **`POLL <n>`** line and record it in your §7 note if you see one. **A RED gate is not proof the deploy failed** — the failure is deferred on purpose so the merge and branch cleanup still run, and that design must not be "tidied up" into failing in place. Confirm independently either way, since the Actions log API truncates before the Deploy step on a run this size: `curl "https://script.google.com/macros/s/$(jq -r .DEPLOYMENT_ID googleAppsScripts/Classroom/Classroom.config.json)/exec?action=api&op=deploy"` — `Already up to date (<version>)` means it landed — and `https://lightaisolutions.github.io/Sales/gs-versions/Classroomgs.version.txt` for the Pages side. A red gate skips the Pages deploy entirely (`deploy` needs `auto-merge.result == 'success'`), so if GAS is current and Pages is stale the recovery is a **`workflow_dispatch` on `main`**.

**CHANGELOG — use the EST clock, not UTC.** `TZ=America/New_York date '+%Y-%m-%d'` is what the rotation rule means by "today"; the harness clock may already have rolled over while EST has not, and firing rotation early is a real error. **After v05.61r the file stands at 101 raw / 99 non-exempt** — the v05.61r section is dated 2026-09-14 and is exempt while EST is still on that date. **So your push rotates if it lands on 2026-09-15 or later**: the 2026-09-14 group stops being exempt, the non-exempt count clears 100, and the oldest whole date group to move is **eighteen sections dated 2026-09-05**, archived with SHA enrichment on every header. **A remote session's clone is shallow** — check `git rev-list --count HEAD` and run `git fetch --unshallow origin main` before any SHA lookup, because unshallowing does not persist between containers. `Profilerhtml.changelog.md` is at 49/50 but this session must not touch Profiler. Normal Pre-Commit and Pre-Push checklists; `claude/*` branch, `git ls-remote` before each push, and restart the branch from `origin/main` first — the auto-merge workflow adds a `[skip ci]` SHA-tracker commit after every merge.

> Author the next unbuilt lesson of `CLASSROOM-CURRICULUM-PLAN.md` §7 — take the **lowest-numbered row without a "Built" note** — on Opus 5 xhigh as a fresh session. This is ONE mechanism lesson. It authors no guidance module, no landscape, no dossier and no report, creates no track, and regenerates no segment-* lesson.
>
> READ FIRST: repository-information/SESSION-CONTEXT.md (Latest Session); INTEGRATED-REMEDIATION-PLAN.md §7.16 (this brief), §7.3 (run table order 6) and §6; CLASSROOM-CURRICULUM-PLAN.md §3's spec for your row VERBATIM — its section ids, kinds and order are the contract **except where the spec's own later scope line overrides them, as §3.5's does for this row** — plus §7's row for the stamps, §4 for the lesson's position in its track, §5 for the failure-map row its `where-it-fails` draws from, and §8 items 2, 4, 6 and 9; repository-information/CLASSROOM-SCHEMA.md; and .claude/rules/classroom-app.md IN FULL.
>
> THE WORK — one push commit. (1) Author the literal as strict JSON INSIDE the // CONTENT START … // CONTENT END fence, at the section ids and kinds §3 fixes, in its order, taking §3.5's title and `the-classes` rename. (2) Build the stamp from the inputs §7 and §3.5 name: fetch each one, match its `slug` to the ref, and read the date off the fetched file. **Fetch origin/main first.** NEVER fabricate an input; **pin only the sources a row is actually drawn from, and say which listed ones you fetched and did not pin.** (3) Register at the END of clLessons_() and insert into `market-access`'s lessons[] at **position 1** — ahead of `how-a-utility-buys`, so **P5 fires and is accepted**. (4) Flip the §7 row, the §4 status cell, the §7.3 run-table row and the §6 ledger row, all dated. (5) Write §7.17 — the next row's brief — in the same commit.
>
> DO NOT: put the literal outside the fence. Do not touch any guidanceDoc*_() literal, Profiler.gs, Profiler.html or Scraper.gs. Do not regenerate a segment-* lesson (S2's, per §10.9). Do not refresh gateDigest — no GATE_SYMBOLS member moves in lesson authoring and P3 should be silent; if P3 fires, something is wrong and refreshing the digest would hide it. **Do not drill a company as an answer** — this row's material is dossiers, and a mechanism lesson uses a company only as a worked example; the tell that a card has crossed the line is that its answer is a proper noun.
>
> VERIFY: node --check on a .js copy of Classroom.gs; scripts/check-gas-inner-scripts.js; scripts/check-classroom-content.py — capture a pristine HEAD baseline BEFORE any edit and diff against it (**24 errors across twelve stale segment-\* lessons are S2's, neither caused nor fixed here**) and warnings must stay at 0, which means `short` must be ≤160 characters even when §3's spec text is longer — trim it as rows 1–5 all did. Then scripts/check-classroom-curriculum.py --strict; scripts/check-classroom-pipeline.py --selftest (13 fixtures, 0 failures) and --base origin/main — expect P1, **expect P5** (this row inserts at the head), expect NO P3, expect NO P8; scripts/check-readme-tree.py after the version bump. Playwright render at admin with ZERO page errors, ops answered by the REAL serving functions, and READ EVERY SECTION'S SCREENSHOT. Prove the lesson is on the walk with clStudyNext_ three ways: fresh, with every other lesson completed, and with this one completed. Bump Classroom's GAS version in BOTH Classroom.gs VERSION and live-site-pages/gs-versions/Classroomgs.version.txt, and add a GENERIC entry to Classroomgs.changelog.md per changelog-security.

### 7.17 · Paste-in brief for S2 session 1 — `storage-integrators-and-containers` (Opus 5 xhigh) — written 2026-09-14 at v05.63r, after the segment regeneration run

**S2 is 0 of 19 and this is its first session ever, so this brief carries the shape of the phase, not just the row.** Everything §7.9–§7.16 settled is about *lessons*; a landscape module is a different artefact with a different home, a different gate and a different checker set. Read `CLASSROOM-CURRICULUM-PLAN.md` §10.6 as the contract and `.claude/rules/industry-guidance.md` as the machinery — the module is authored by the `industry guidance:` pipeline even though no document is ingested, on the G4 research-synthesis precedent.

**The segment is measured, not remembered — and it is bigger than §10.2's S0 table says.** That table recorded 29 members (10 · 8 · 11) on 2026-09-07. Measured 2026-09-14 it is **32 members — 10 incumbent, 8 challenger, 14 adjacent**, the three added ones (`calb`, `cornex`, `great-power`) being the S3 cell makers that arrived as adjacent. It is the largest supply segment and it is far above the floor. **All 32 have a dossier, and all 32 carry `strategyRead`** — including all 18 incumbents and challengers, which is the `each-players-bet` row set. So the module has no thin-evidence excuse anywhere, and **eighteen rows is a large table**: §10.6 says one row per incumbent and challenger, and the honest reading is that all eighteen belong, not that the table should be trimmed to the interesting ones.

**The segment lesson is already correct, and that is new.** Until v05.62r an S2 session would have inherited a stale roster; `segment-storage-integrators-and-containers` was three members short. It was regenerated with the other eighteen, so this session starts from a lesson whose `the-players` matches the registry exactly. **It still owes the regeneration at the end** — §10.6 requires the segment lesson's `read-next` to deep-link to the landscape module, and today it renders "No landscape module yet". That is one `--segment storage-integrators-and-containers` invocation after the module exists, and it is the step that closes the loop; skipping it leaves the module unreachable from the lesson that should send readers to it.

**Two changelog traps, both measured today, both of which bite this session rather than the last one.**

- **`Scrapergs.changelog.md` stands at 49/50 and this session must touch Scraper.** Step 9 of `industry-guidance.md` requires a `SCRAPER_INTEREST_TOPIC_SEEDS` entry for every new module, which bumps the Scraper GAS version and therefore adds a changelog section — taking it to **50/50, which *reaches* the trigger and fires rotation**. The oldest whole date group is **26 sections dated 2026-08-28** (repo cross-refs `v03.21r`–`v03.47r`), leaving 24 after the move, and **every one of the 26 headers needs SHA enrichment**. Deepen the clone *first* (`git fetch --unshallow origin main`) or all 26 archive as `[SHA unavailable]` permanently. Check before assuming: if an existing seed already covers this topic, no Scraper edit is needed and neither is the rotation.
- **The repo CHANGELOG rotates on this session if it lands 2026-09-15 or later.** It stands at **103 raw / 98 non-exempt** after v05.63r, with five sections dated 2026-09-14 holding it under the line. The moment a push lands on a later EST day those five stop being exempt, the count is **103 non-exempt**, and rotation fires — moving **18 sections dated 2026-09-05** (`v04.61r`–`v04.78r`), which leaves 85. One group is enough; do not move a second.

**`reviewBy` has a real answer here and it is not the six-month default.** §10.6 sets it from the nearest dated gate among the members' `policyExposure[]` and the developments the module leans on. Across the 32 members there are exactly **five future-dated gates**: `2027-01` (Cummins, EPA 2027 on-highway emissions), `2027-04` (Sungrow, EU high-risk inverter phase-out), and three at `2027-10` (CATL, Envision, BYD — NDAA §154, BYD's stated precisely as `2027-10-01`). The nearest is **earlier than `updated` + 6 months**, so take it rather than the default — but note that two of the three nearest are **partial dates (`YYYY-MM`)** and the Cummins one sits on an *adjacent* member. Resolve that judgment explicitly in the analysis markdown rather than silently; if a development the module leans on carries a nearer date, that wins.

**The lane is new on the guidance side.** `guidanceDocs_()` holds nine modules across three lanes — Technology Foundations, The AI Data-Center Wave, Market Access & Bankability — ordered by lane with a comment saying so. This is the **tenth module and the first in a fourth lane, `The Value Chain`**, so it appends at the end of the registry and the lane's ordering comment needs extending. The lane string already exists in `Classroom.gs` (23 lessons and tracks use it); nothing new is being invented, but no *module* has carried it before.

**The brief:**

> Run S2 session 1 — the landscape module for `storage-integrators-and-containers` — on Opus 5 xhigh as a fresh session. ONE landscape module: no mechanism lesson, no new track, no dossier, no report, no second segment.
>
> READ FIRST: repository-information/SESSION-CONTEXT.md (Latest Session); INTEGRATED-REMEDIATION-PLAN.md §7.17 (your brief — it carries today's measured numbers and two changelog traps), §7.3 (run table order 5) and §7.2 decision 2 (why a landscape may name companies when no other guidance module may); CLASSROOM-CURRICULUM-PLAN.md §10.6 VERBATIM — its section list, tier, provenance line and claims-ledger rule are the contract — plus §10.2 (the floor), §10.3 (what the segment lesson already carries, so the module does not repeat it), §10.8 and §10.10; .claude/rules/industry-guidance.md IN FULL, especially steps 3, 4, 5, 9 and 10; repository-information/CLASSROOM-SCHEMA.md; and .claude/rules/classroom-app.md.
>
> THE SEGMENT, measured 2026-09-14 at v05.62r. 32 members — 10 incumbent, 8 challenger, 14 adjacent — the largest supply segment, far above the floor. All 32 have a dossier and all 32 carry strategyRead, including all 18 incumbents and challengers, which is the each-players-bet row set: eighteen rows, and all eighteen belong. §10.2's S0 table still says 29 (10·8·11) — that was 2026-09-07, before calb, cornex and great-power arrived. Re-measure from profiler-segments.json before you trust any count, including this one.
>
> THE WORK — one push commit. (1) `git fetch --unshallow origin main` first if the clone is shallow — you will need history for SHA enrichment, see the CHANGELOG paragraph. (2) Write repository-information/industry-guidance/landscape-storage-integrators-and-containers-analysis.md on the standard analysis shape with a claims ledger: every claim → `profile:<slug>` at its profileVersion and the dossier field it rests on. Corpus synthesis over the member dossiers ONLY — no new web research, no ingested document; the dossiers already cite their sources and the ledger cites the dossiers. (3) Author `guidanceDocLandscapeStorageIntegrators_()` in googleAppsScripts/Classroom/Classroom.gs BELOW the `// CONTENT END` fence (guidance is not pipeline write-set), id `landscape-storage-integrators-and-containers-2026-09`, group `The Value Chain`, contributor tier, a `**Provenance:**` line reading "Corpus synthesis over the segment's member dossiers at the versions in the claims ledger; no ingested document", the nine §10.6 sections in its order. Register it at the END of `guidanceDocs_()` and extend that function's lane-ordering comment — this is the tenth module and the first in a fourth lane. (4) Set `reviewBy` from the nearest dated gate, not a cadence: across the 32 members the future gates are 2027-01 (Cummins, EPA 2027 on-highway), 2027-04 (Sungrow, EU high-risk inverter phase-out) and three at 2027-10 (CATL, Envision, BYD — NDAA §154). The nearest is earlier than updated+6mo so take it, but TWO ARE PARTIAL DATES and the nearest sits on an adjacent member — resolve that judgment explicitly in the analysis markdown. A nearer date on a development the module leans on wins. (5) Scraper interest seed per industry-guidance step 9 — but CHECK FIRST whether an existing SCRAPER_INTEREST_TOPIC_SEEDS entry already covers this topic; if it does, no Scraper edit and no Scraper rotation. (6) THEN regenerate the segment lesson: `python3 scripts/build-classroom-segments.py --segment storage-integrators-and-containers`, which rewrites its `read-next` from "No landscape module yet" to the deep link. This is the step §10.6 requires and it must come AFTER the module exists. Never hand-edit the generated literal. (7) Bump Classroom's GAS version in BOTH Classroom.gs VERSION and live-site-pages/gs-versions/Classroomgs.version.txt (and Scraper's in both, if step 5 touched it), with GENERIC entries per changelog-security — "Guidance library updated" / "Curriculum updated", never naming the segment's companies or the analysis.
>
> DO NOT: author or revise any mechanism lesson. Do not regenerate any segment other than this one. Do not put the module inside the content fence. Do not touch Profiler.gs or Profiler.html — module authoring stopped touching Profiler at C3. Do not blend analysis with fact in `each-players-bet`: a bet drawn from strategyRead is labelled as analysis, per §10.6. Do not refresh gateDigest — guidance sits outside the fence and touches no gate symbol, so expect P2 on your own diff (you are a developer session, not a pipeline run) and NO P3; if P3 fires, something is wrong and refreshing the digest would hide it.
>
> VERIFY: node --check on a .js copy of Classroom.gs (and Scraper.gs if touched); scripts/check-gas-inner-scripts.js; scripts/check-classroom-content.py — capture a pristine HEAD baseline BEFORE any edit; v05.62r took it to 0 errors / 0 warnings, so YOUR BASELINE IS 0/0 AND IT MUST STILL BE 0/0 AFTER. That is the first Classroom session in months with a clean baseline to protect — any error you introduce is unambiguously yours. Then scripts/check-classroom-curriculum.py --strict (it should now report the segment's landscape module present); scripts/build-classroom-segments.py --check (expect 0 due after step 6 — it will read 19 due before it, because concepts and graph pins move on every dossier session; that is the known §10.4 degeneracy, not your defect); scripts/check-classroom-pipeline.py --selftest (13 fixtures, 0 failures) and --base origin/main — expect P1 on the developer files and P2 on the guidance literal, expect NO P3, and P7/P8/P10 only from the one regenerated segment. scripts/check-readme-tree.py after the version bumps. Playwright render at admin of BOTH Classroom.html#guidance/landscape-storage-integrators-and-containers-2026-09 AND the regenerated segment lesson's read-next, with ZERO page errors, ops answered by the REAL serving functions, and READ THE SCREENSHOTS — the eighteen-row each-players-bet table is the one most likely to render badly. Also prove the gate: a contributor sees the module, an ANALYST MUST NOT — that is the §7.2 decision that put judgment at contributor, and an analyst-visible landscape would be a real access defect. Render recipe: step 7 of .claude/rules/industry-guidance.md — serve a scratch copy over http://127.0.0.1 with `_e = ''` and AUTO_REFRESH = false, seed the session AFTER page load (sessionStorage, and single-tab enforcement clears it on load), override window._gasPost AFTER load too, and HIDE #auth-wall / .splash / #gas-pill / #verify-overlay with CSS rather than remove()ing them — removing raises a pageerror from unrelated page code.
>
> THE DEPLOY: watch the Deploy Classroom step (and Deploy Scraper if touched). At v05.62r Classroom confirmed on the first GET leg — "confirmed (GET): Updated to v01.31g (deployment 41) | 41/200" — with no "POLL <n>" line, so the v05.59r poll is armed and has still never been needed; report it if you see one. A RED gate is not proof the deploy failed. Confirm independently AFTER the Deploy step has finished, not while it is in flight, or you race it and burn a deployment version: curl -sL "https://script.google.com/macros/s/$(jq -r .DEPLOYMENT_ID googleAppsScripts/Classroom/Classroom.config.json)/exec?action=api&op=deploy" — NOTE THE -L, the endpoint redirects and without it you get an empty body that looks like a failure — and https://lightaisolutions.github.io/Sales/gs-versions/Classroomgs.version.txt. If GAS is current but Pages is stale, recover with a workflow_dispatch on main.
>
> CHANGELOGS — READ THE COUNTERS AND DO THE NON-EXEMPT ARITHMETIC, do not trust the raw numbers. Use the EST clock (TZ=America/New_York date '+%Y-%m-%d'). (a) The repo CHANGELOG stands at 103 raw / 98 non-exempt, held under the line only by five sections dated 2026-09-14; IF YOUR PUSH LANDS 2026-09-15 OR LATER those stop being exempt, the count is 103, and ROTATION FIRES — move the oldest whole group, eighteen sections dated 2026-09-05 (v04.61r–v04.78r), leaving 85. One group is enough. (b) Scrapergs.changelog.md stands at 49/50, so if step 5 touches Scraper its entry REACHES 50 AND ROTATION FIRES THERE TOO — oldest whole group is twenty-six sections dated 2026-08-28 (repo cross-refs v03.21r–v03.47r), leaving 24. SHA enrichment is mandatory on every moved header in both files, and the unshallow in step 1 is the precondition, not the remedy. Classroomgs.changelog.md is at 31/50 — no rotation there.
>
> Normal Pre-Commit and Pre-Push checklists; claude/* branch restarted from origin/main first, git ls-remote before each push.
>
> AFTER THIS SESSION the interleave is 5:1 and §7.4 item 6 says the lanes alternate, so the recommended next run is a Phase 4 row — row 6, `who-buys-storage`, whose brief is already written and standing at §7.16. *(Amended at v05.66r: that brief is superseded — row 6's live brief is **§7.18**, re-measured at the close of this session, because S2 session 1 itself moved the baseline and both changelog counters §7.16 records.)*

### 7.18 · Paste-in brief for Phase 4 row 6 — `who-buys-storage` (Opus 5 xhigh) — written 2026-09-14 at v05.66r, at the close of S2 session 1. **Supersedes §7.16.**

**Read this one, not §7.16.** §7.16 is row 6's brief as written at v05.61r and its *reasoning* is still right — the P1+P5 signature, the id-permanence exception, the content-contract pressure, the harness details. What went stale is every number in it, because **S2 session 1 and the segment regeneration run both landed between §7.16 and this row**, and they moved exactly the figures a session checks its own work against. The corrections are listed below rather than patched into §7.16, so the v05.61r record stands as written.

| §7.16 said | Measured 2026-09-14 at v05.65r | Why it matters |
|---|---|---|
| Pristine `HEAD` baseline **24 errors, 0 warnings** | **0 errors, 0 warnings** | The most consequential correction in this table. A session expecting 24 and seeing 1 would read it as progress. **Any error you see is yours.** v05.62r's regeneration cleared the twelve stale `segment-*` lessons and v05.64r held it |
| "the twenty-four are twelve stale `segment-*` lessons and belong to S2" | Cleared; `build-classroom-segments.py --check` reports **0 due** | Nothing in the checker output is inherited any more |
| "roughly thirty refs — **four** `profile:` dossiers" | **31 refs exactly: 7 `profile:`, 23 `study:`, 1 `concepts:`** | §7.16 counted only §3.5's base inputs line and missed `brookfield`, `blackstone` and `mgx` from the v05.07r scope line. All 31 verified to exist today |
| repo CHANGELOG **101 raw / 99 non-exempt** | **105 raw / 98 non-exempt** (7 dated 2026-09-14) | The rotation answer is unchanged in shape but not in arithmetic — see the CHANGELOG paragraph |
| `Classroomgs.changelog.md` unstated | **32/50, 29 non-exempt** | No rotation on this row either way |
| "§5 for the failure-map row its `where-it-fails` draws from" | **§5 carries no row for this lesson** | §5 says in its own first line that it is the checklist **every equipment lesson's** `where-it-fails` draws from. `who-buys-storage` is a market lesson; its `where-it-fails` is specified directly in §3.5's outline. Do not go looking for a §5 row that is not there |
| "Write §7.17 — the next row's brief" | **Write §7.19** | §7.17 is S2 session 1's brief and §7.18 is this one |
| Classroom at the v05.61r-era versions | **GAS `v01.32g`, page `v01.14w`, repo `v05.65r`** | Bump from these |

**Phase 4 is still 5 of 26 and row 6 is still the lowest unbuilt row.** S2 ran in between and touched a different artefact class entirely; nothing in Phase 4 moved. The corpus is **35 lessons, 8 tracks** — row 6 makes it 36.

**What §7.16 got right and this brief does not restate at length.** Row 6 inserts at `market-access` **position 1**, ahead of `how-a-utility-buys` — verified today, the track's `lessons[]` is `["how-a-utility-buys"]` and nothing else — so **P5 fires and is accepted**, and the signature flips back from row 5's P1+P8 to **P1 + P5, no P3, no P8**. The id-permanence exception is real and written: §3.5's **v05.07r scope line overrides its own outline** — the title becomes *Eleven Buyer Classes* and `eight-classes` becomes **`the-classes`**, because the registry gained `utility`, `investor` and `advisor` after the outline was written. That is the spec instructing the change, and it is safe because no progress tick or drill id keys on `eight-classes` yet. The other six ids — `the-ipp`, `epc-versus-gc`, `the-landlord-tiers`, `where-it-fails`, `drill`, `check-yourself` — are followed exactly.

**The stamp is the interesting part of this row's record, and the instruction is to prune it.** All 31 candidates exist and carry a live date today: seven dossiers (`plus-power` 2026-09-06 · `nextera-energy-resources` 2026-09-05 · `arevon` 2026-09-07 · `jupiter-power` 2026-09-06 · `brookfield` 2026-09-06 · `blackstone` 2026-09-06 · `mgx` 2026-09-06), twenty-three study guides (`constellation-energy` · `equinix` · `vantage` · `mortenson` · `turner-construction` all 2026-08-21 · `stack-infrastructure` 2026-08-22 · `dominion-energy` 2026-09-03 · `dnv` and `sargent-lundy` 2026-09-04 · `excelsior-energy-capital`, `aypa-power`, `spearmint-energy`, `intersect-power`, `invenergy`, `gridstor`, `available-power`, `esvolta`, `strata-clean-energy`, `hunt-energy-network` all 2026-09-05 · `engie-north-america`, `aes-clean-energy`, `rwe-clean-energy`, `clearway-energy` all 2026-09-06), and `concepts:profiler-concepts`, whose commit date on `origin/main` is **2026-09-13**. **These dates are this session's observation and are not the pin** — §8 item 2 binds: re-fetch `origin/main`, open each file you pin, and read the date off the fetched body. §3.5's own instruction is "pin only the guides a row is drawn from", and the stamp rule is "never fabricate an input". **Say in the CHANGELOG and in the §7 note which of the 31 you fetched and did not pin, and why** — row 5 used all nine of its candidates and said so; this row will not use 31, and the omission is the record worth keeping.

**The five-column table is the thing most likely to render badly, and there is a named open question behind it.** `the-classes` is `class · what they buy · what they buy on · who signs · how fast` — **five columns, eleven rows**, the widest table any mechanism lesson has carried. The curriculum plan §7's row-5 note records an open `.cl-tbl` `min-width` question from a **four**-column table whose first column wrapped one word per line, and confirms it is the renderer's normal auto-layout output rather than something that content introduced. That CSS still has no `min-width` today — verified. Two things follow: **(a)** read this section's screenshot first and at a realistic width, and **(b)** if it wraps badly, the fix is a **page** change (`Classroom.html`, a `w` bump) and is still recorded-not-taken, so report it rather than widening the brief. One new data point in its favour: S2 session 1's landscape module rendered a **four-column, eighteen-row** table cleanly at a 1400px viewport, so the defect is about column count and first-column word length rather than row count.

**Two renderer traps that no checker catches, both found by reading screenshots.** First, the plain-field markup check added at v05.61r is live and is an **error**, not a warning: `{{term}}`, `**bold**` and `*italic*` in any field the renderer prints as text — a `proscons` card's `t` and `meta`, a section's `title` and `read`, `tiles[]`, `glossary[]`, a `revisions[].note`, a lesson's own `title`/`short`. **`epc-versus-gc` is a `proscons` section**, so this row writes exactly the fields that shipped the original defect four times: keep `t` and `meta` plain and put emphasis in `adv`/`dis`, which *are* formatted. Second, and newly measured at v05.64r: **`clFmt` resolves `**bold**`, `*italic*` and `{{term}}` and nothing else — a backtick renders literally to the reader**, and no checker scans for one. S2 session 1 shipped 254 of them into a module literal and caught them only in the screenshot. If you want code-ish emphasis in a lesson, use `**bold**`.

**The content contract is under real pressure on this row, and the tell is a proper noun.** `who-buys-storage` is a **mechanism lesson**: a company is a worked example, never the answer. A card may ask *what an IPP buys on, and who signs* — never *which company is an IPP*, and never *how many gigawatt-hours anyone shipped*. That drilling belongs to the segment lessons and the roster deck, and this row is neither. The amended contract in `CLASSROOM-SCHEMA.md` says the same thing in one line; §7.2 decision 1 is what moved that fence, and it moved it for segment lessons only.

**The harness, so this row does not rediscover it.** Run the PROJECT region of `Classroom.gs` in Node with GAS shims and replay the **real** serving functions — `clRoleOf_`, `clTrackIndexFor_`, `clLessonIndexFor_`, `clTrackFor_`, `clLesson_`, `clStampKinds_`, `clLessonGate_`, `clStudyNext_` — through a `window._gasPost` stub, so the render is evidence of what the server would send. Serve a scratch copy over `http://127.0.0.1` with `_e = ''` and `AUTO_REFRESH = false`; seed the session **after** page load (`sessionStorage`, keys `Classroom_gas_session_token` / `_gas_user_email` / `_gas_user_role` / `_gas_user_permissions` — single-tab enforcement clears storage on load); override `window._gasPost` **after** load too; and **hide** `#auth-wall` / `.splash` / `#gas-pill` / `#verify-overlay` with CSS rather than `remove()`ing them, because removing makes unrelated page code dereference a null element and raises a `pageerror` that has nothing to do with your lesson. Copy `profiler-data/profiler-concepts.json`, `sounds/` and `images/` into the scratch site or the console fills with 404s. **One Playwright gotcha measured at v05.64r:** `page.evaluate(fn, arg)` passes exactly **one** argument — an arrow function written `(a, b) => …` silently receives the whole array as `a` and `undefined` as `b`, and the page then renders nothing with zero errors. Destructure: `([a, b]) => …`. Prove the lesson is on the teaching walk with `clStudyNext_` three ways — fresh, with every *other* lesson completed, and with this one completed — and, because this row inserts, show whose pointer moved.

> Author Phase 4 row 6 — `who-buys-storage` — on Opus 5 xhigh as a fresh session. This is ONE mechanism lesson. It authors no guidance module, no landscape, no dossier and no report, creates no track, and regenerates no `segment-*` lesson.
>
> READ FIRST: repository-information/SESSION-CONTEXT.md (Latest Session); INTEGRATED-REMEDIATION-PLAN.md §7.18 (your brief — it supersedes §7.16 and carries the corrections table; read §7.16 too, for the reasoning it still owns), §7.3 (run table order 6) and §6; CLASSROOM-CURRICULUM-PLAN.md §3.5's spec for `who-buys-storage` VERBATIM — its section ids, kinds and order are the contract EXCEPT where its own v05.07r scope line overrides them, which it does for this row — plus §7 row 6 for the stamp candidates, §4 for the position, and §8 items 2, 4, 6 and 9. §5 does NOT carry a row for this lesson and you should not go looking for one: it is the equipment lessons' checklist and this is a market lesson, so `where-it-fails` comes from §3.5's outline. Then repository-information/CLASSROOM-SCHEMA.md and .claude/rules/classroom-app.md IN FULL.
>
> THE WORK — one push commit. (1) Author the literal as strict JSON INSIDE the `// CONTENT START` … `// CONTENT END` fence, at the ids and kinds §3.5 fixes, in its order, taking the scope line's title *Eleven Buyer Classes* and the `eight-classes` → `the-classes` rename. `short` must be ≤160 characters — §3.5's own short line is longer, so trim it as rows 1–5 all did; warnings must stay at 0. (2) Build the stamp from the 31 candidates §7 and §3.5 name: **fetch `origin/main` first**, open each one you pin, match its `slug` to the ref and read the date off the fetched body. NEVER fabricate an input. **Pin only the sources a row is actually drawn from, and record which of the 31 you fetched and did not pin, and why.** (3) Register at the END of `clLessons_()` and insert into `market-access`'s `lessons[]` at **position 1**, ahead of `how-a-utility-buys` — so **P5 fires and is accepted**. (4) Flip the §7 row, the §4 status cell, the §7.3 order 6 row and the §6 ledger row, all dated, to **6 of 26**. (5) Write **§7.19** — the next row's brief — in the same commit.
>
> DO NOT: put the literal outside the fence. Do not touch any `guidanceDoc*_()` literal, `Profiler.gs`, `Profiler.html` or `Scraper.gs`. Do not regenerate a `segment-*` lesson — those are S2's and `--check` reports 0 due today, so any regeneration is yours to explain. Do not refresh `gateDigest`: no `GATE_SYMBOLS` member moves in lesson authoring, so **P3 must be silent**, and refreshing the digest would hide a real signal. **Do not drill a company as an answer** — the tell that a card has crossed the line is that its answer is a proper noun.
>
> VERIFY: `node --check` on a `.js` copy of `Classroom.gs`; `scripts/check-gas-inner-scripts.js`; `scripts/check-classroom-content.py` — **capture a pristine HEAD baseline BEFORE any edit. IT IS 0 ERRORS / 0 WARNINGS and it must still be 0/0 after.** §7.16's "24 errors are S2's" is obsolete; there is nothing inherited left in that output, so any error is unambiguously yours. The checker also asserts the registered guidance-module count is **10** — if that fires you touched `guidanceDocs_()` and should not have. Then `scripts/check-classroom-curriculum.py --strict`; `scripts/build-classroom-segments.py --check` (expect **0 due** — if it reports due segments you moved something you should not have); `scripts/check-classroom-pipeline.py --selftest` (13 fixtures, 0 failures) and `--base origin/main` — expect **P1 and P5**, expect **NO P3**, expect **NO P8**. A P8 means a section you did not intend to touch moved: stop and read it rather than naming it in a `changed[]`. `scripts/check-readme-tree.py` after the version bump. Playwright render at admin with **ZERO page errors**, ops answered by the REAL serving functions, and **READ EVERY SECTION'S SCREENSHOT — `the-classes` first**, because it is a five-column eleven-row table and the widest any mechanism lesson has carried. Prove the walk with `clStudyNext_` three ways and show whose pointer the insert moved. Bump Classroom's GAS version in BOTH `Classroom.gs` `VERSION` (from `v01.32g`) and `live-site-pages/gs-versions/Classroomgs.version.txt`, with a GENERIC entry in `Classroomgs.changelog.md` per changelog-security. **No page bump is expected** — if you find yourself editing `Classroom.html`, stop and ask whether it is the `.cl-tbl` question, which is recorded-not-taken.
>
> THE DEPLOY: watch the `Deploy Classroom` step. At v05.64r it confirmed on the first GET leg — `Classroom deploy confirmed (GET): Updated to v01.32g (deployment 42) | 42/200` — with **no `POLL <n>` line**, so the v05.59r poll is armed and **has still never been needed**; report it if you see one. A RED gate is not proof the deploy failed. Confirm independently AFTER the Deploy step has finished, not while it is in flight, or you race it and burn a deployment version: `curl -sL "https://script.google.com/macros/s/$(jq -r .DEPLOYMENT_ID googleAppsScripts/Classroom/Classroom.config.json)/exec?action=api&op=deploy"` — **note the `-L`**, the endpoint redirects and without it you get an empty body that reads as a failure — and `https://lightaisolutions.github.io/Sales/gs-versions/Classroomgs.version.txt`. If GAS is current but Pages is stale, recover with a `workflow_dispatch` on `main`.
>
> CHANGELOGS — READ THE COUNTERS AND DO THE NON-EXEMPT ARITHMETIC. Use the EST clock (`TZ=America/New_York date '+%Y-%m-%d'`). The rule counts **non-exempt** sections — total minus today's — not the raw counter, and §7.17 got this wrong twice in a row by reading the raw number. (a) The repo CHANGELOG stands at **105 raw / 98 non-exempt**, seven sections dated 2026-09-14. **If your push lands 2026-09-14 it does not rotate** (98 non-exempt with yours). **If it lands 2026-09-15 or later, ROTATION FIRES** — the 2026-09-14 group stops being exempt, the count is 106, and the oldest whole group to move is **eighteen sections dated 2026-09-05 (`v04.61r`–`v04.78r`)**, leaving 88. One group is enough. (b) `Classroomgs.changelog.md` is at **32/50, 29 non-exempt** — no rotation either way. (c) **Two standing traps that are not yours but will be someone's:** `Scrapergs.changelog.md` now reads **50/50 with 49 non-exempt**, so the next session that touches `Scraper.gs` on a later EST day rotates it (oldest group: twenty-six sections dated 2026-08-28), and `Profilerhtml.changelog.md` sits at **49/50**. This row must not touch either file. SHA enrichment is mandatory on every moved header, and `git fetch --unshallow origin main` is the precondition, not the remedy.
>
> Normal Pre-Commit and Pre-Push checklists; `claude/*` branch restarted from `origin/main` first, `git ls-remote` before each push — the auto-merge workflow adds a `[skip ci]` SHA-tracker commit after every merge.
>
> AFTER THIS SESSION Phase 4 is 6 of 26 and the interleave is 6:1, so by §7.4 item 6 the recommended next run is **S2 session 2 — `cells-and-chemistry`**, the second-deepest segment at 22 members (4 incumbent · 11 challenger · 7 adjacent), for which §7.19 should be written.

### 7.19 · Paste-in brief for S2 session 2 — `cells-and-chemistry` (Opus 5 xhigh) — written 2026-09-14 at v05.67r, at the close of Phase 4 row 6

**Why this and not Phase 4 row 7.** Phase 4 is **6 of 26** and S2 is **1 of ~19**, so the interleave stands at 6:1 and §7.4 item 6 puts the long lanes in alternation. Row 7 (`inside-the-rack`) is the lowest unbuilt mechanism row and is not going anywhere; the landscape lane has run once. This session is S2's second, and its segment is fixed by §7.3's sales-value order, which reads *integrators, cells, developers and IPPs, …*.

**A measurement correction to carry, because two places in this file get it wrong.** §7.3 describes cells as "the two deepest at 32 and 22 members" and §7.18's closing line calls it "the second-deepest segment". **Re-measured 2026-09-14 from `profiler-segments.json`: `cells-and-chemistry` holds 22 members and is the FIFTH-deepest segment**, behind `storage-developers-and-ipps` (34), `storage-integrators-and-containers` (32), `aidc-developers-and-landlords` (30) and `in-hall-power` (27). It is **second in the S2 order**, which is a sales-value ranking, not a depth ranking — the two were the same number at S0 and S3 has since deepened three other segments past it. The roster is **4 incumbent · 11 challenger · 7 adjacent**: incumbents `byd` · `catl` · `hithium` · `lg-energy-solution`; challengers `calb` · `cornex` · `envision-energy` · `eve-energy` · `form-energy` · `gotion` · `great-power` · `narada` · `rept` · `samsung-sdi` · `sunwoda`; adjacent `albemarle` · `canadian-solar` · `mitra-chem` · `novonix` · `panasonic` · `tesla` · `trina-storage`. **Re-measure it yourself on the day** — these are this session's numbers, not the pin, and S3's fills moved them once already.

**It is the only segment whose challengers outnumber its incumbents nearly three to one**, and that shape is the module's subject rather than a statistic about it: the `each-players-bet` table will run **fifteen rows** of incumbent-plus-challenger, and the dominance section has to explain how four incumbents hold a segment that eleven challengers are contesting. The segment's own `buyingCriteria` in the registry names six, and the fourth — *prohibited-foreign-entity status and the factory's location, status following ownership rather than site* — is the one that does the most work in this segment and the one a landscape module can get wrong by restating a headline.

**What S2 session 1 established that this session should not rediscover.** The module is authored **below the `// CONTENT END` fence** in `guidanceDocs_()`, not inside it — it is a guidance module, not a lesson, and the fence is the lesson pipeline's write set. It is corpus synthesis over member dossiers at the `profileVersion` its claims ledger pins, with the analysis file in `repository-information/industry-guidance/` as the source of truth; no ingested document and no new web research. **`clFmt` resolves only `**bold**`, `*italic*` and `{{term}}` — a backtick renders literally to the reader and no checker scans for one.** S2 session 1 shipped 254 of them into a ledger's Source column and caught them only in the screenshot; the backticked form belongs in the analysis markdown and the module gets plain text. **`reviewBy` is a written judgment, not a sort** — session 1 wrote its reasoning out in §10 of its analysis file because the generator's `review_by()` only accepts a full `YYYY-MM-DD` and cannot see a partial date, so a hand-authored module may resolve one and must say why.

**The regeneration this session owes, and the one it does not.** §10.6 requires the session to run `scripts/build-classroom-segments.py --segment cells-and-chemistry` **after** the module exists, so the segment lesson's `read-next` stops saying "No landscape module yet". That is one `--segment` invocation, well inside P10's caps. **It does not clear the other due segment, and should not try.** `--check` today reports **1 due: `storage-developers-and-ipps`, one string in `read-next`, no pin moved** — row 6's expected residue, because registering `who-buys-storage` turned a `PLANNED_TITLES` entry rendered as *"Eight Buyer Classes" (planned)* into the live title. That segment's own S2 session clears it. **So expect `--check` to read 1 due before you start, 2 due after the module lands, and 1 due after your one regeneration** — and say so in the record, because a session that reads 1 due as damage will regenerate a lesson that is not its to write.

**The baselines to capture BEFORE any edit, all measured at the close of v05.67r.** `check-classroom-content.py` is **0 errors / 0 warnings at 36 lessons, 8 tracks, 142 gate cases** — there is nothing inherited in that output, so any error is unambiguously yours. The checker also asserts the **registered guidance-module count is 10**; your module makes it **11**, so that assertion must be updated in the same commit — this is the first S2 session for which it is not already correct, since session 1 moved it from 9 to 10. `check-classroom-curriculum.py --strict` reports no structural findings. `check-classroom-pipeline.py --selftest` is 13 fixtures / 0 failures. **Expect P1 and P10 on `--base origin/main` and no P3**: the generator touches no `GATE_SYMBOLS` member, so `gateDigest` stays untouched, and a P3 would mean something else moved.

**THE DEPLOY — an instruction this brief had to correct on its own first use, at a measured cost of one deployment version.** The standing advice has been "confirm independently **after** the Deploy step has finished, or you race it and burn a deployment version". **That condition is necessary and not sufficient, and following it exactly still burned one at v05.67r:** the Deploy Classroom step reported `Classroom deploy confirmed (GET): Updated to v01.33g (deployment 43) | 43/200`, and the independent probe issued several minutes *after* that step completed answered `Updated to v01.33g (deployment 44) | 44/200` — a second deploy, not a confirmation. **The reason is in `pullAndDeployFromGitHub()` and is worth knowing exactly:** it fetches `Classroom.gs` from GitHub, reads the pulled `VERSION`, and compares it against the `VERSION` constant **of the code currently executing**; equal returns `Already up to date (…)` and does nothing, unequal *deploys*. Between a deployment being repointed and `/exec` actually executing the new bundle there is a propagation lag, so a probe inside that window runs under the **old** `VERSION`, sees a mismatch against GitHub's new one, and deploys again. **The correct order, which costs nothing:** check Pages first — `https://lightaisolutions.github.io/Sales/gs-versions/Classroomgs.version.txt` is a static file and can be read as often as you like — and read the workflow's own `Classroom deploy confirmed (GET)` line out of the job log, which is the authoritative statement that the deploy landed. **Probe `?op=deploy` only if one of those two disagrees**, accept that it may deploy once, and re-probe after: a second probe returning `Already up to date (v01.33g)` is how v05.67r established that the state had settled and that the endpoint is idempotent once `/exec` is running the new code. Use `curl -sL` when you do probe — the endpoint redirects and without `-L` the body is empty, which reads as a failure.

**Versions and counters to bump from.** Classroom GAS **v01.33g** (both `Classroom.gs` `VERSION` and `live-site-pages/gs-versions/Classroomgs.version.txt`); page **v01.14w**, and no page bump is expected. Repo **v05.69r**. Deployment counter **44/200** — 43 from the workflow's own deploy and 44 from the confirmation probe described above. v05.64r and v05.67r both confirmed on the **first GET leg with no `POLL <n>` line**, so the v05.59r poll is armed and **has still never been needed**; report it if you see one.

**CHANGELOGS — read the counters and do the non-exempt arithmetic.** Use `TZ=America/New_York date '+%Y-%m-%d'`. The rule counts **non-exempt** sections (total minus today's), not the raw counter; §7.17 read the raw number twice and was wrong twice, and §7.18 got it right by spelling out both branches. **(a) The repo CHANGELOG stands at 109 raw / 98 non-exempt, eleven sections dated 2026-09-14** — the count this brief's own commit leaves behind, not the count when the brief was first written. A push landing 2026-09-14 does **not** rotate. A push landing **2026-09-15 or later ROTATES** — the 2026-09-14 group stops being exempt, the count clears 100, and the oldest whole group to move is **eighteen sections dated 2026-09-05 (`v04.61r`–`v04.78r`)**, leaving 91. One group is enough. **Re-count rather than trusting this line**: `grep -c '^## \[v' repository-information/CHANGELOG.md` minus the same grep narrowed to today's EST date is the whole rule, and three briefs in a row have now had to correct a written-down counter. **(b) `Classroomgs.changelog.md` is at 33/50, 29 non-exempt** — no rotation either way. **(c) `Scrapergs.changelog.md` reads 50/50 with 49 non-exempt**, so the next session to touch `Scraper.gs` on a later EST day fires rotation there (oldest group: twenty-six sections dated 2026-08-28); `Profilerhtml.changelog.md` sits at 49/50. **If this session adds a Scraper seed as session 1 did, that trap is yours.** SHA enrichment is mandatory on every moved header, and `git fetch --unshallow origin main` is the precondition, not the remedy — a remote session's clone is shallow and unshallowing does not persist between containers.

**Two open items recorded and not taken, neither of them this session's to fix.** The `.cl-tbl` `min-width` question is still open but has lost most of its force: row 6's **five-column, eleven-row** table rendered cleanly at 1400px, as did session 1's four-column, eighteen-row landscape table, so the defect looks to be about a first column holding one long word rather than about column or row count. And the generated segment lesson's `read-next` still **names** the landscape module without linking to it — the generator emits the id as italic text and the `cl*` renderer has no anchor syntax. §10.6 calls it a deep link; today it is a stated id. That is a generator-template question, and §10.4 forbids hand-editing a generated literal.

> Author S2 session 2 — the `cells-and-chemistry` landscape module — on Opus 5 xhigh as a fresh session. This is ONE guidance module plus ONE segment regeneration. It authors no mechanism lesson, no dossier and no report, creates no track, and regenerates no segment other than its own.
>
> READ FIRST: repository-information/SESSION-CONTEXT.md (Latest Session); INTEGRATED-REMEDIATION-PLAN.md §7.19 (your brief), §7.17 (S2 session 1's brief, for the reasoning it still owns), §7.3 (run table order 5) and §6; CLASSROOM-CURRICULUM-PLAN.md §10.6 VERBATIM — its nine section ids and their order are the contract — plus §10.2 for the floor, §10.3 for what the segment lesson already teaches so the module does not repeat it, §10.4 and §10.9 for the generator's ownership rules, and §8 items 2, 4 and 6. Then repository-information/CLASSROOM-SCHEMA.md, .claude/rules/classroom-app.md and .claude/rules/industry-guidance.md IN FULL, and .claude/rules/changelog-security.md before you write any public changelog line.
>
> THE WORK — one push commit. (1) Re-measure the segment from `profiler-segments.json` on the day and record the figure against this brief's 22 (4 · 11 · 7). (2) Write the analysis file in `repository-information/industry-guidance/` first — every claim traced to a member dossier at its `profileVersion`, with the `reviewBy` reasoning written out rather than sorted. (3) Author the module in `guidanceDocs_()` **below the `// CONTENT END` fence**, at §10.6's nine ids in its order, with **all fifteen incumbent-plus-challenger rows** in `each-players-bet` — none trimmed — and a claims ledger citing dossiers in **plain text, never backticks**. (4) Register it at the end of `guidanceDocs_()` and **update the content checker's registered-module assertion from 10 to 11** in the same commit. (5) Run `scripts/build-classroom-segments.py --segment cells-and-chemistry` AFTER the module exists, so the lesson's `read-next` can name it. (6) Add a Scraper seed only if you first read the existing seeds and establish that none scores this layer — and if you touch `Scraper.gs`, the changelog trap in (c) above is yours. (7) Flip the §7.3 order 5 row, the §6 S2 ledger row and curriculum plan §10's built-so-far note, all dated. (8) Write §7.20 — the next run's brief — in the same commit.
>
> DO NOT: put the module inside the `// CONTENT START … // CONTENT END` fence — it is not a lesson. Do not touch `Profiler.gs`, `Profiler.html` or any `clLesson*`/`clTrack*` literal. Do not regenerate any segment but your own — `--check` reports **1 due (`storage-developers-and-ipps`) before you start and that one is not yours**; expect 2 due after your module lands and 1 due after your regeneration. Do not refresh `gateDigest`: neither a guidance module nor the generator moves a `GATE_SYMBOLS` member, so **P3 must be silent**. Do not use a backtick anywhere in the module — `clFmt` renders it literally and no checker will tell you.
>
> VERIFY: `node --check` on a `.js` copy of `Classroom.gs`; `scripts/check-gas-inner-scripts.js`; `scripts/check-classroom-content.py` — **capture a pristine HEAD baseline BEFORE any edit. IT IS 0 ERRORS / 0 WARNINGS at 36 lessons / 8 tracks / 142 gate cases** and must still be 0/0 after, with the module count assertion moved 10 → 11 as part of your own diff. Then `scripts/check-classroom-curriculum.py --strict`; `scripts/check-classroom-pipeline.py --selftest` (13 fixtures, 0 failures) and `--base origin/main` — expect **P1 and P10**, expect **NO P3**. `scripts/check-readme-tree.py` after the version bump. Playwright render at **admin and contributor** with ZERO page errors, ops answered by the REAL serving functions, and **an analyst proven `ROLE_DENIED` on both `gop=index` and `gop=doc`** as session 1 did — then READ EVERY SECTION'S SCREENSHOT, the `each-players-bet` table and the claims ledger first, because those are where a backtick or a broken emphasis hides. Bump Classroom's GAS version in BOTH `Classroom.gs` `VERSION` (from `v01.33g`) and `live-site-pages/gs-versions/Classroomgs.version.txt`, with a GENERIC entry in `Classroomgs.changelog.md`.
>
> THE DEPLOY and THE CHANGELOG ARITHMETIC: exactly as the paragraphs above this prompt set out. **Confirm the deploy from Pages and the job log, NOT by probing `?op=deploy` first** — the probe deploys whenever the running `/exec` bundle has not yet caught up, which is how v05.67r took the counter from 43 to 44 while doing nothing but confirming. Deployment counter **44/200**; the v05.59r poll is armed and has still never been needed. Repo CHANGELOG 109 raw / 98 non-exempt with eleven sections dated 2026-09-14, so **2026-09-15 or later rotates the eighteen sections dated 2026-09-05** — but re-count it yourself rather than trusting the number.
>
> Normal Pre-Commit and Pre-Push checklists; `claude/*` branch restarted from `origin/main` first, `git ls-remote` before each push — the auto-merge workflow adds a `[skip ci]` SHA-tracker commit after every merge.
>
> AFTER THIS SESSION S2 is 2 of ~19 and Phase 4 is 6 of 26, so the interleave returns to Phase 4 and the recommended next run is **row 7, `inside-the-rack`**, for which §7.20 should be written.

### 7.20 · Paste-in brief for Phase 4 row 7 — `inside-the-rack` (Opus 5 xhigh) — written 2026-09-14 at v05.70r, at the close of S2 session 2

**Why this and not S2 session 3.** Phase 4 is **6 of 26** and S2 is now **2 of ~19**, so the interleave stands at 6:2 and §7.4 item 6 alternates the two long lanes. Row 7 (`inside-the-rack`) has been the lowest unbuilt mechanism row since row 6 landed, and S2 session 3 (`storage-developers-and-ipps`, 34 members — the deepest segment in the registry) is not going anywhere. One lesson, one session.

**This row is a mid-track insertion, and that is the run signature to expect.** `aidc-grid-to-chip` holds six built lessons today in this order: `the-fence-line` · `bridge-power` · `the-aidc-power-chain` · `backup-generation` · `the-ups-room` · `the-800-vdc-shift`. Curriculum plan §4 puts `inside-the-rack` at **position 8, immediately before `the-800-vdc-shift` at position 9** — so it is inserted at index 5 of the built list, not appended. Expect **P1 and P5, no P3, no P8** — the signature rows 3, 4 and 6 set, which only row 5's append broke. §8 item 9's rule applies: a positional insert into a track's `lessons[]` is a developer-session write and P5 reporting it is the accepted outcome, not a defect.

**The outline is fixed and the id is permanent.** Curriculum plan §3.3 fixes eight sections in this order: `the-rack-is-the-machine` (prose) · `psu-to-die` (table — stage · voltage · current · why that voltage · what it wastes) · `forty-eight-volts` (prose) · `hot-swap-is-engineered` (callout) · `the-periodic-swing` (prose) · `where-it-fails` (callout) · `drill` · `check-yourself`. Unlike §3.5's `who-buys-storage`, **this spec carries no id-permanence exception**, so take the ids verbatim. Title *The Last Ten Metres*. Mix at least four section kinds (§8 item 6) — the outline already does — and keep a `sales` line on roughly half the sections (§8 item 7), not all of them.

**The stamp: ten candidates, all public, and the fold is `tracks`.** §3.3 names `study:delta-electronics` · `study:liteon` · `study:lambda` · `study:nvidia` · `study:megmeet` · `concepts:profiler-concepts`, and the v05.07r line adds `study:vicor` · `study:infineon` · `study:flex` · `study:narada` with the note that **G9 is closed, so the lesson no longer has to state its own thinness**. All nine study guides exist and were confirmed present on 2026-09-14; their `lastUpdated` values then ranged **2026-08-07 (megmeet) to 2026-09-04 (infineon, flex, narada)** — **re-read every one off the fetched file and pin the date you read**, per §8 item 2 and G2. Do not carry those dates over from this brief. Every input is public, so the stamp folds to `tracks` and the lesson is analyst-visible; adding a `guidance:` or `report:` ref would change the gate and is not wanted here. Where a candidate is fetched and not pinned, **record it with its reason** as row 6 did with its twelve — that record is the most useful half of the stamp discipline.

**Two adjacency rules this row must honour, and one it inherits.** §8 item 8's pattern: `the-800-vdc-shift` immediately follows this lesson and owns the **thesis** — convert once, early, high, and the three chains side by side. `inside-the-rack` owns the chain **to the die** and must make that shift read as an inevitability rather than pre-empt it; §3.3's own "why" line says exactly that. `the-ups-room` at position 6 owns the room-level battery; the **supercapacitor and battery shelves inside the rack** are this lesson's, under `the-periodic-swing`. And it inherits the content contract unchanged: a mechanism lesson **never drills company trivia** — no flashcard or quiz answer may be a proper noun, which is the tell row 6 used and passed on material that was itself a roster of companies.

**`reviewBy`.** §8 item 3: the lesson's own nearest dated gate, else about six months from `updated`. Row 7's material is hardware fundamentals rather than policy, so a six-month default is the likely answer — but **check the study guides for a dated milestone before defaulting**, and say in the CHANGELOG which of the two you took and why. S2 session 2's finding is worth carrying across lanes: a dated gate can sit **inside a prose field rather than a date field**, so read the material rather than sorting it.

**The baselines to capture BEFORE any edit, all measured at the close of v05.70r and unchanged by the v05.71r handover, which touches no Classroom file.** `check-classroom-content.py` is **0 errors / 0 warnings at 36 lessons, 8 tracks, 142 gate cases** — nothing inherited is in that output, so any error is unambiguously yours; your lesson takes it to 37 lessons. The checker's **guidance-module count assertion is now 11** and you are not touching it — a Phase 4 row registers no module. `check-classroom-curriculum.py --strict` reports **no structural findings**. `check-classroom-pipeline.py --selftest` is **13 fixtures / 0 failures**. **`build-classroom-segments.py --check` reports 1 due (`storage-developers-and-ipps`) before you start, and it is not yours** — it has been that row's residue since v05.67r, `segment-*` literals belong to S2 under §10.9, and S2 session 3 is the session that clears it. **Registering `inside-the-rack` may make it 2 due**, exactly as row 6 and S2 session 2 both saw, if any segment's `READ_NEXT` names this lesson: check, record the number, and **regenerate nothing**.

**Do not refresh `gateDigest`.** A lesson authored inside the content fence touches no `GATE_SYMBOLS` member, so **P3 must be silent**; a P3 finding means something else moved and refreshing the digest would hide it.

**Versions and counters to bump from.** Classroom GAS **v01.34g** (both `Classroom.gs` `VERSION` and `live-site-pages/gs-versions/Classroomgs.version.txt`); page **v01.14w**, and no page bump is expected. Repo **v05.71r** (v05.70r plus this handover). Deployment counter **45/200, now measured rather than forecast**: v05.70r's workflow reported `Classroom deploy confirmed (GET): Updated to v01.34g (deployment 45) | 45/200` **on the first GET leg with no `POLL <n>` line**, and **no `?op=deploy` probe was issued at all** — Pages and the job log agreed, which is the §7.19 order working as intended and costing nothing. The v05.59r poll is armed and **has still never been needed**; report it if you see one.

**THE DEPLOY — the §7.19 procedure held on its second use and is now established.** Read **Pages** first (`https://lightaisolutions.github.io/Sales/gs-versions/Classroomgs.version.txt` is a static file, free to read as often as you like) and the workflow's own `Classroom deploy confirmed (GET)` line from the job log. **Probe `?op=deploy` only if those two disagree** — the probe *deploys* whenever the running `/exec` bundle has not yet caught up with GitHub, because `pullAndDeployFromGitHub()` compares the pulled `VERSION` against the `VERSION` of the code currently executing, and the propagation lag between repointing a deployment and `/exec` running the new bundle is exactly the window in which a "confirmation" costs a version. That is how v05.67r took the counter from 43 to 44 while doing nothing but confirming. Use `curl -sL` if you do probe — the endpoint redirects and without `-L` the body is empty, which reads as a failure.

**CHANGELOGS — re-count, do not trust this line.** `grep -c '^## \[v' repository-information/CHANGELOG.md` minus the same grep narrowed to today's EST date (`TZ=America/New_York date '+%Y-%m-%d'`) is the whole rule; **four briefs in a row have now had to correct a written-down counter**, and S2 session 2 re-counted and found §7.19's numbers exactly right for the first time. **(a) The repo CHANGELOG stands at 111 raw / 98 non-exempt, thirteen sections dated 2026-09-14** — the count this handover commit leaves behind, not the count when the brief was first written. A push landing 2026-09-14 does **not** rotate. A push landing **2026-09-15 or later ROTATES**: those thirteen stop being exempt, the count is 111, and the oldest whole group to move is **eighteen sections dated 2026-09-05 (`v04.61r`–`v04.78r`)**, leaving 93. One group is enough. **(b) `Classroomgs.changelog.md` is at 34/50, 29 non-exempt** — no rotation either way. **(c) `Scrapergs.changelog.md` still reads 50/50 with 49 non-exempt**: S2 session 2 did **not** touch `Scraper.gs`, because an existing seed already scored its layer, so that trap is still armed for the next session that touches that file on a later EST day (oldest group: twenty-six sections dated 2026-08-28). A Phase 4 row does not touch Scraper. `Profilerhtml.changelog.md` sits at 49/50. SHA enrichment is mandatory on every moved header and `git fetch --unshallow origin main` is the precondition, not the remedy.

**Two open items recorded and not taken, neither of them this session's to fix.** The `.cl-tbl` `min-width` question has now lost effectively all of its force: row 6's five-column, eleven-row table rendered cleanly at 1400px, and S2 session 2's **fifteen-row, four-column** bets table and **fifty-row** claims ledger both rendered cleanly at the same width. Three data points now say the defect is about a first column holding one long word, not about column or row count. Row 7's `psu-to-die` table is five columns and will be a fourth. And the generated segment lesson's `read-next` still **names** the landscape module without linking to it — verified again on the `cells-and-chemistry` regeneration, where it renders as italic text. §10.4 forbids hand-editing a generated literal, so it stays a generator-template question.

> Author Phase 4 row 7 — the `inside-the-rack` mechanism lesson — on Opus 5 xhigh as a fresh session. This is ONE lesson. It authors no guidance module, no dossier and no report, creates no track, and regenerates no segment.
>
> READ FIRST: repository-information/SESSION-CONTEXT.md (Latest Session); INTEGRATED-REMEDIATION-PLAN.md §7.20 (your brief), §7.18 (row 6's brief, for the mechanism-row shape it still owns), §7.3 (run table order 6) and §6; CLASSROOM-CURRICULUM-PLAN.md §3.3's `inside-the-rack` entry VERBATIM — its eight section ids and their order are the contract, and unlike §3.5 it carries NO id-permanence exception — plus §4 for the track position, §8 items 2, 3, 6, 7, 8 and 9, and §2.3. Then repository-information/CLASSROOM-SCHEMA.md and .claude/rules/classroom-app.md IN FULL, and .claude/rules/changelog-security.md before you write any public changelog line.
>
> THE WORK — one push commit. (1) Fetch all ten candidate inputs and pin each date off the fetched document, never off this brief. (2) Author `clLessonInsideTheRack_()` INSIDE the `// CONTENT START … // CONTENT END` fence, at §3.3's eight ids in its order, id `inside-the-rack`, title *The Last Ten Metres*, group The AI Data-Center Wave, gate `tracks` (all-public stamp). (3) Register it in `clLessons_()` and insert it into `clTrackAidcGridToChip_()`'s `lessons[]` at **index 5, immediately before `the-800-vdc-shift`** — a positional insert, not an append — and bump that track's `updated`. (4) Record every fetched-and-not-pinned candidate with its reason, as row 6 did with its twelve. (5) Flip the §7.3 order 6 row, the §6 Phase 4 row and curriculum plan §7's row-7 entry, all dated. (6) Write §7.21 — the next run's brief, which by the interleave is S2 session 3, `storage-developers-and-ipps` — in the same commit.
>
> DO NOT: author or revise any guidance module, and do not touch `guidanceDocs_()` or the content checker's module-count assertion (now 11) — a Phase 4 row registers no module. Do not regenerate any segment: `--check` reports 1 due (`storage-developers-and-ipps`) before you start and it is S2's, not yours; if registering this lesson makes it 2 due, record the number and regenerate nothing. Do not touch `Profiler.gs`, `Profiler.html` or `Scraper.gs`. Do not refresh `gateDigest` — a lesson inside the fence moves no `GATE_SYMBOLS` member, so P3 must be silent. Do not let any flashcard or quiz answer be a proper noun — the mechanism content contract is unchanged.
>
> VERIFY: `node --check` on a `.js` copy of `Classroom.gs`; `scripts/check-gas-inner-scripts.js`; `scripts/check-classroom-content.py` — **capture a pristine HEAD baseline BEFORE any edit. IT IS 0 ERRORS / 0 WARNINGS at 36 lessons / 8 tracks / 142 gate cases** and must still be 0/0 after, at 37 lessons. Then `scripts/check-classroom-curriculum.py --strict` (no structural findings today); `scripts/check-classroom-pipeline.py --selftest` (13 fixtures, 0 failures) and `--base origin/main` — expect **P1 and P5**, expect **NO P3 and NO P8**. `scripts/check-readme-tree.py` after the version bump. Playwright render of the new lesson with ZERO page errors, ops answered by the REAL serving functions, at **analyst** as well as contributor — this lesson is analyst-visible and that is the tier its gate is for — then READ EVERY SECTION'S SCREENSHOT, `psu-to-die` first, because it is a five-column table and the widest a mechanism lesson has carried since row 6's. Bump Classroom's GAS version in BOTH `Classroom.gs` `VERSION` (from `v01.34g`) and `live-site-pages/gs-versions/Classroomgs.version.txt`, with a GENERIC entry in `Classroomgs.changelog.md`.
>
> THE DEPLOY and THE CHANGELOG ARITHMETIC: exactly as the paragraphs above this prompt set out. **Confirm the deploy from Pages and the job log, NOT by probing `?op=deploy` first.** Deployment counter **45/200**, measured from v05.70r's own `Classroom deploy confirmed (GET)` line, which landed on the first GET leg with no `POLL` line and no probe issued. Repo CHANGELOG 111 raw / 98 non-exempt with thirteen sections dated 2026-09-14, so **2026-09-15 or later rotates the eighteen sections dated 2026-09-05, leaving 93** — but re-count it yourself rather than trusting the number.
>
> Normal Pre-Commit and Pre-Push checklists; `claude/*` branch restarted from `origin/main` first, `git ls-remote` before each push — the auto-merge workflow adds a `[skip ci]` SHA-tracker commit after every merge.
>
> AFTER THIS SESSION Phase 4 is 7 of 26 and S2 is 2 of ~19, so the interleave returns to S2 and the recommended next run is **S2 session 3, `storage-developers-and-ipps`** — the deepest segment at 34 members, and the one whose `segment-*` lesson has carried the standing 1-due since v05.67r — for which §7.21 should be written.

### 7.21 · Paste-in brief for S2 session 3 — `storage-developers-and-ipps` (Opus 5 xhigh) — written 2026-09-14 at v05.72r, at the close of Phase 4 row 7

**Why this and not Phase 4 row 8.** Phase 4 is now **7 of 26** and S2 is **2 of ~19**, so the interleave stands at 7:2 and §7.4 item 6 alternates the two long lanes. It is S2's turn, and S2's own sales-value order puts **developers and IPPs third**, after integrators (session 1) and cells (session 2). Row 8 (`redundancy-by-the-numbers`) is not going anywhere.

**The segment, measured on 2026-09-14 rather than remembered. It is the deepest in the registry, and this time that is not a correction.** `storage-developers-and-ipps` sits at **position 11** and holds **34 members — 19 incumbent · 8 challenger · 7 adjacent** — against 32 for the integrators, 30 for AIDC landlords, 27 for in-hall power and 22 for cells. §7.19 had to correct a "second-deepest" claim about `cells-and-chemistry`; there is nothing to correct here, because 34 is genuinely the largest membership of the nineteen. **Re-measure it yourself on the day** — that is the discipline two S2 sessions in a row have had to exercise — but expect the number to hold. Six `buyingCriteria` on the segment. **The incumbent-heavy shape is this segment's distinguishing fact and should shape the module**: 19 incumbents to 8 challengers is a ratio of roughly 2.4 *the other way* from `cells-and-chemistry`'s 11-to-4, so "who threatens" is a smaller and more specific story here than "who dominates", and the module should say so rather than padding the threat section to match the last one's shape.

**The shape is §10.6's, unchanged, and the first two sessions settled everything else.** Nine sections at §10.6's ids, used verbatim, in its order; the analysis markdown at `repository-information/industry-guidance/landscape-storage-developers-and-ipps-analysis.md` written **first**, with a claims ledger in which every load-bearing claim names a dossier field at its `profileVersion`; the module JSON appended at the end of `guidanceDocs_()` **below the `// CONTENT END` fence**; module id `landscape-storage-developers-and-ipps-2026-09` on the guidance id convention. Corpus synthesis over the member dossiers and `profiler-segments.json` only — no ingested document, no web research. **Do not trim the `each-players-bet` table**: session 2 carried all fifteen of its rows and the table is the module's spine; at 27 incumbents-and-challengers this one will be the longest yet, and the honest move is to carry them all.

**THE THING THAT STOPS A SESSION DEAD, and it is one line.** `scripts/check-classroom-content.py` **hard-asserts the registered guidance-module count**, and as of this commit the assertion reads **`if len(gids) != 11`** at line 839, with the comment above it recording the history (9 at C3 session 3, 10 at S2 session 1, 11 at S2 session 2). Your module is the **twelfth**, so **that assertion moves to 12 in the same commit** — and the comment gets its next line. S2 session 2 met this cold and was warned in time; you are being warned again because it is the one edit that is outside the fence, outside the module, and invisible until the checker fails.

**The standing 1-due is finally yours to clear, and you inherit two more that are not.** `build-classroom-segments.py --check` reports **3 due** as you start: **`storage-developers-and-ipps`** — row 6's residue since v05.67r, carried by every session since because `segment-*` literals belong to S2 under §10.9, and **it is your own segment**, so your regeneration clears it at last; plus **`power-conversion-and-rack-power-silicon`** and **`compute-and-the-rack`**, both created by row 7 registering `inside-the-rack` into two `READ_NEXT` lists. Those two are **not yours**: regenerate **only** `--segment storage-developers-and-ipps`, and expect the count to read **2 due** when you finish. Row 7's record in §7.3 explains the mechanism if the numbers look surprising — the whole diff on those two is a " (planned)" marker disappearing.

**`reviewBy` — and S2 session 2's finding is the one to carry.** §10.6 sets it from the nearest dated gate among the members' `policyExposure[]` and the developments the module leans on, else six months from `updated`. Session 2's finding was that **a dated gate can sit inside a `policyExposure[]` entry's `exposure` prose rather than in its `effectiveDate` field**, because one entry can carry a two-step schedule with only the first step in the field — so **read the registry, do not sort it**. On 34 members with heavy policy exposure (tax-credit rules, FEOC, interconnection dockets, safe-harbour windows) this segment is more likely than either predecessor to have a real gate; state which one you took, and name the nearer candidates you rejected and why, as session 2 did with its three.

**The Scraper seed question is live for this session, unlike the last one.** Session 1 added an integrator seed; session 2 declined, because `topic-bess-technology` already scored the cell and chemistry layer and session 1's own seed comment said so. Ask the question again from scratch here — a developer-and-IPP layer is a different beast from a technology layer — and **if the answer is yes, you walk into an armed changelog**: `Scrapergs.changelog.md` reads **50 raw / 49 non-exempt** today, so a push touching `Scraper.gs` on **2026-09-15 or later** fires rotation there (oldest whole group: twenty-six sections dated 2026-08-28), with SHA enrichment on every moved header and `git fetch --unshallow origin main` as the precondition rather than the remedy. `Profilerhtml.changelog.md` sits at **49/50** and you must not touch Profiler at all.

**The baselines to capture BEFORE any edit, all measured at the close of v05.72r.** `check-classroom-content.py` is **0 errors / 0 warnings at 37 lessons, 8 tracks, 142 gate cases** — row 7 held the 0/0 exactly while taking it from 36 to 37, so nothing inherited is in that output and any error is unambiguously yours. `check-classroom-curriculum.py --strict`: **no structural findings**. `check-classroom-pipeline.py --selftest`: **13 fixtures / 0 failures**. `build-classroom-segments.py --check`: **3 due**, as set out above. **Expected pipeline signature: P1 ×4, P2 ×9, P7 ×1, no P3** — identical for S2 sessions 1 and 2, so a third identical run would confirm it as the lane's signature. P2 is what a developer diff outside the content fence reports; one regenerated lesson is well inside P10's cap of three; the single P7 is the same-day `updated` on the regenerated segment. **Do not refresh `gateDigest`** — a module below the fence moves no `GATE_SYMBOLS` member, so P3 must be silent, and refreshing would hide a real signal.

**Versions and counters to bump from.** Classroom GAS **v01.35g** (both `Classroom.gs` `VERSION` and `live-site-pages/gs-versions/Classroomgs.version.txt`); page **v01.14w**, and no page bump is expected. Repo **v05.73r**. Scraper **v02.03g**, Profiler **v01.39g** — neither is yours unless the seed question says otherwise. **Deployment counter: 46/200, measured rather than forecast** — v05.72r's workflow reported `Classroom deploy confirmed (GET): Updated to v01.35g (deployment 46) | 46/200` **on the first GET leg with no `POLL <n>` line**, and **no `?op=deploy` probe was issued at all** because Pages and the job log agreed. The v05.59r poll is armed and has still never been needed; report it if you see one.

**THE DEPLOY — the §7.19 procedure is now established on its third use.** Read **Pages** first (`https://lightaisolutions.github.io/Sales/gs-versions/Classroomgs.version.txt` is a static file, free to read as often as you like) and the workflow's own `Classroom deploy confirmed (GET)` line from the job log. **Probe `?op=deploy` only if those two disagree** — the probe *deploys* whenever the running `/exec` bundle has not yet caught up with GitHub, because `pullAndDeployFromGitHub()` compares the pulled `VERSION` against the `VERSION` of the code currently executing, and the propagation lag between repointing a deployment and `/exec` serving the new bundle is exactly the window in which a "confirmation" costs a version. Use `curl -sL` if you do probe — the endpoint redirects and without `-L` the body is empty, which reads as a failure.

**CHANGELOGS — re-count, do not trust this line.** `grep -c '^## \[v' repository-information/CHANGELOG.md` minus the same grep narrowed to today's EST date (`TZ=America/New_York date '+%Y-%m-%d'`) is the whole rule, and note that the capacity counter string also appears **inside older entries**, so match the standalone line rather than the first hit. **The repo CHANGELOG stands at 113 raw / 98 non-exempt, fifteen sections dated 2026-09-14.** A push landing 2026-09-14 does **not** rotate. A push landing **2026-09-15 or later ROTATES**: those fifteen stop being exempt, the count is 113, and the oldest whole group to move is **eighteen sections dated 2026-09-05 (`v04.61r`–`v04.78r`)**, leaving **95**. One group is enough. `Classroomgs.changelog.md` is at **35/50, 29 non-exempt** — no rotation either way. Two briefs running have now found their predecessor's counters exactly right; keep the streak by re-counting rather than by trusting it.

**Three open items recorded and not taken, none of them this session's.** The `.cl-tbl` `min-width` question is now effectively retired as a column-count hypothesis: row 7's `psu-to-die` is **five columns by six rows and rendered clean at 1400px**, the fourth clean data point after row 6's 5 × 11 and session 2's 15-row and 50-row tables, so the v05.57r defect was about a first column holding one long word. The generated segment lesson's `read-next` still **names** the landscape module without linking to it — a generator-template question, and §10.4 forbids hand-editing a generated literal, so you will see it again on your own regeneration. And **new at v05.72r**: `the-800-vdc-shift`'s `three-chains` intro says "The previous lesson walked a megawatt from the service entrance to the rack" — true until row 7 inserted `inside-the-rack` between them. Correcting it costs a P8 finding on a literal that is not the session's, so it waits for a session with reason to open that lesson.

> Author S2 session 3 — the `storage-developers-and-ipps` landscape module — on Opus 5 xhigh as a fresh session. This is ONE guidance module. It authors no lesson, no dossier and no report, creates no track, and regenerates exactly one segment: its own.
>
> READ FIRST: repository-information/SESSION-CONTEXT.md (Latest Session); INTEGRATED-REMEDIATION-PLAN.md §7.21 (your brief), §7.19 (S2 session 2's brief, for the landscape shape and the `reviewBy` finding it still owns), §7.17 (session 1's, for what the first module settled), §7.3 order 5 and §6; CLASSROOM-CURRICULUM-PLAN.md §10.6 VERBATIM — its nine section ids and their order are the contract — plus §10.2 (the floor rule), §10.3–10.4 (what a segment lesson is and who may edit it) and §10.9. Then .claude/rules/industry-guidance.md IN FULL, repository-information/CLASSROOM-SCHEMA.md, .claude/rules/classroom-app.md, and .claude/rules/changelog-security.md before you write any public changelog line. If the Scraper-seed question comes out yes, also .claude/rules/scraper-sources.md IN FULL.
>
> THE WORK — one push commit. (1) Re-measure the segment on the day: it read **34 members — 19 incumbent · 8 challenger · 7 adjacent** at v05.72r, the deepest of the nineteen, and its **incumbent-heavy shape is the module's distinguishing fact** — do not pad "who threatens" to match the last module's proportions. (2) Write `repository-information/industry-guidance/landscape-storage-developers-and-ipps-analysis.md` FIRST, with a claims ledger in which every load-bearing claim names a dossier field at its `profileVersion`. (3) Author the module JSON at §10.6's nine ids in its order, id `landscape-storage-developers-and-ipps-2026-09`, appended to `guidanceDocs_()` **below the `// CONTENT END` fence**, and **carry every row of `each-players-bet`** — at 27 incumbents-and-challengers it will be the longest yet, and trimming it is the one thing session 2 refused to do. (4) **Move the content checker's guidance-module assertion from 11 to 12** at `scripts/check-classroom-content.py` line 839, and extend the comment above it — this is the single edit outside the fence and the one that stops a session dead if met cold. (5) Regenerate **only** `--segment storage-developers-and-ipps`. (6) Ask the Scraper-seed question from scratch and record the answer either way. (7) Flip the §7.3 order 5 row, the §6 S2 row and curriculum plan §10.6's "built so far" line, all dated. (8) Write §7.22 — the next run's brief, which by the interleave is Phase 4 row 8, `redundancy-by-the-numbers` — in the same commit.
>
> DO NOT: author or revise any lesson (`segment-*` literals are regenerated, never hand-edited — §10.4). Do not regenerate `power-conversion-and-rack-power-silicon` or `compute-and-the-rack`: both are due because Phase 4 row 7 registered `inside-the-rack` into their `READ_NEXT` lists, the whole diff on each is a " (planned)" marker disappearing, and they are not yours — expect `--check` to read **2 due** when you finish, not 0. Do not touch `Profiler.gs` or `Profiler.html`. Do not refresh `gateDigest` — a module below the fence moves no `GATE_SYMBOLS` member, so P3 must be silent.
>
> VERIFY: `node --check` on a `.js` copy of `Classroom.gs`; `scripts/check-gas-inner-scripts.js`; `scripts/check-classroom-content.py` — **capture a pristine HEAD baseline BEFORE any edit. IT IS 0 ERRORS / 0 WARNINGS at 37 lessons / 8 tracks / 142 gate cases** and must still be 0/0 after. Then `scripts/check-classroom-curriculum.py --strict` (no structural findings today); `scripts/check-classroom-pipeline.py --selftest` (13 fixtures, 0 failures) and `--base origin/main` — expect **P1 ×4, P2 ×9, P7 ×1 and NO P3**, the signature sessions 1 and 2 both reported. `scripts/check-readme-tree.py` after the version bump. Playwright render of the new module **and** of the regenerated segment lesson with ZERO page errors, ops answered by the REAL serving functions (shim only `validateSessionForData`), at **contributor** — a landscape module is contributor-gated and that is the tier its gate is for — then READ EVERY SECTION'S SCREENSHOT, `each-players-bet` first because it will be the longest table the corpus has carried. Bump Classroom's GAS version in BOTH `Classroom.gs` `VERSION` (from `v01.35g`) and `live-site-pages/gs-versions/Classroomgs.version.txt`, with a GENERIC entry in `Classroomgs.changelog.md`.
>
> THE DEPLOY and THE CHANGELOG ARITHMETIC: exactly as the paragraphs above this prompt set out. **Confirm the deploy from Pages and the job log, NOT by probing `?op=deploy` first.** Deployment counter **46/200**, measured from v05.72r's own `Classroom deploy confirmed (GET)` line, which landed on the first GET leg with no `POLL` line and no probe issued. Repo CHANGELOG 113 raw / 98 non-exempt with fifteen sections dated 2026-09-14, so **2026-09-15 or later rotates the eighteen sections dated 2026-09-05, leaving 95** — but re-count it yourself, and match the standalone counter line rather than the first `grep` hit, because that string also appears inside older entries.
>
> Normal Pre-Commit and Pre-Push checklists; `claude/*` branch restarted from `origin/main` first, `git ls-remote` before each push — the auto-merge workflow adds a `[skip ci]` SHA-tracker commit after every merge.
>
> AFTER THIS SESSION Phase 4 is 7 of 26 and S2 is 3 of ~19, so the interleave returns to Phase 4 and the recommended next run is **Phase 4 row 8, `redundancy-by-the-numbers`** — `aidc-grid-to-chip` position 7, which lands **immediately before `inside-the-rack`** and is therefore another mid-track insertion with the same P1 + P5 signature — for which §7.22 should be written.

### 7.22 · Paste-in brief for Phase 4 row 8 — `redundancy-by-the-numbers` (Opus 5 xhigh) — written 2026-09-14 at v05.74r, at the close of S2 session 3

**Why this and not another landscape.** Phase 4 is **7 of 26** and S2 is now **3 of ~19**, so the interleave stands at 7:3 and §7.4 item 6 alternates the two long lanes. It is Phase 4's turn, and row 8 is the lowest unbuilt mechanism row. S2's next segment by the §7.3 sales-value order is `utilities`, and it is not going anywhere.

**The lesson, and the one thing about it that is different from rows 5, 6 and 7.** `redundancy-by-the-numbers` — *N+1, 2N, and Six Nines* — is the **eighth** mechanism lesson and the **fourth mid-track insertion**. §3.3 fixes eight sections, their ids, their kinds and their order: `the-vocabulary` (table) · `pricing-a-nine` (**bars**) · `the-tiers` (prose) · `commissioning` (**timeline**) · `the-vanishing-ups` (callout) · `where-it-fails` (**table**) · `drill` · `check-yourself`. **Two of those kinds are the unusual part**: this is the first §3.3 outline in the Phase 4 queue to call for `bars`, and `where-it-fails` is specified as a **table** here rather than the callout every previous row used. Do not quietly normalise either — `bars` takes `unit` and the CVD-validated palette applies if any new chart colour is introduced (`industry-guidance.md` step 4), and a `where-it-fails` table needs columns the callout never needed. §8 item 6's kind-variety bar is met comfortably.

**Eleven stamps, and all eleven are `study:` refs plus the concepts registry — so the fold is `tracks` and the lesson is analyst-visible.** `study:liteon` · `study:huawei-digital-power` · `study:switch` · `study:microsoft` · `study:hitt` · `study:rosendin` · `study:turner-construction` · `study:dpr` · `study:whiting-turner` · `study:mccarthy` · `concepts:profiler-concepts`. Read each date **off the fetched document** (G2), not off this brief. §3.3's `v05.07r scope` note is load-bearing and narrows the lesson: `the-ups-room` (built v05.57r) now owns the UPS ladder and the battery under each, and `backup-generation` owns the generator side — **so this lesson keeps only the vocabulary, the nines, the tiers, commissioning and `the-vanishing-ups`, and the last of those refers to `the-ups-room` rather than teaching the machine.** A draft that re-teaches the UPS is the predictable failure here.

**The insertion, and the signature to expect.** §4 puts it at **position 7** of `aidc-grid-to-chip`; in the **live** track, which does not carry `interconnection-for-large-loads`, that is **`lessons[]` index 5, immediately before `inside-the-rack`** and after `the-ups-room`. A mid-track insertion into a position-ordered reading list is the **P1 + P5** signature rows 5, 6 and 7 all produced — P1 on the developer files, P5 on the registry reorder — with **no P3** (a mechanism lesson moves no `GATE_SYMBOLS` member, so `gateDigest` stays untouched) and **no P8** (a first authoring appends no revision). Bump the track's `updated` and give its running `short` whatever clause it is missing.

**THE DUE-COUNT PREDICTION, measured rather than guessed — and it is bigger than any previous row's.** `--check` reads **2 due** as you start (`power-conversion-and-rack-power-silicon` and `compute-and-the-rack`, row 7's residue, **not yours** — leave them). Registering this lesson will push it up, through **two** channels rather than one:

- **`READ_NEXT` names `redundancy-by-the-numbers` in three segments** — `in-hall-power`, `aidc-developers-and-landlords` and `neoclouds` — so all three go due on `read-next`.
- **The generator's buying-criteria lexicon maps it as well** (`("redundan", "n+1", "2n", "uptime", "availability", "concurrent")`), and three segments carry a criterion that can match: `in-hall-power` ("installed MW per protected MW under the chosen redundancy topology"), `grid-equipment` ("test-bay availability") and `hyperscalers-and-ai-labs` ("firm power availability"). Where it wins the match, `what-is-bought-and-on-what` changes too — which is exactly how row 6's `compute-and-the-rack` came out differing in **two** sections instead of one.

**And `PLANNED_TITLES["redundancy-by-the-numbers"]` is already `"N+1, 2N, and Six Nines"` — byte-identical to §3.3's own title** — so, as at row 7, the entire diff on every affected segment is a `" (planned)"` marker disappearing. **Do not regenerate any of them**: `segment-*` literals belong to S2 under §10.9, and each segment's own S2 session clears its own. **Expect `--check` to read somewhere around 5 to 7 due when you finish, and read its `sections differing` list rather than trusting that range** — the count is the least reliable thing about that script, as §10.4's re-measure note says in as many words. Say the number you actually see in the record.

**The baselines to capture BEFORE any edit, all measured at the close of v05.74r.** `check-classroom-content.py` is **0 errors / 0 warnings at 37 lessons, 8 tracks, 142 gate cases** — held unbroken across S2 session 3, so nothing inherited is in that output. **Its hard-coded guidance-module assertion now reads 12** and a Phase 4 row does **not** touch it — if you find yourself editing `scripts/check-classroom-content.py`, stop and re-read what you are doing. `check-classroom-curriculum.py --strict`: **no structural findings**, and **0 items due for review** — no row is gated by a review date today, the nearest being `large-load-interconnection-2026-09` at 2026-11-16. `check-classroom-pipeline.py --selftest`: **13 fixtures / 0 failures**.

**Versions and counters to bump from.** Classroom GAS **v01.36g** (both `Classroom.gs` `VERSION` and `live-site-pages/gs-versions/Classroomgs.version.txt`); page **v01.14w**, and no page bump is expected. Repo **v05.75r**. Scraper **v02.04g** and Profiler **v01.39g** — **neither is yours**: a Phase 4 row touches no Scraper seed and never touches Profiler. **Deployment counter: 47/200, measured rather than forecast** — v05.74r's workflow reported `Classroom deploy confirmed (GET): Updated to v01.36g (deployment 47) | 47/200` **on the first GET leg with no `POLL` line**, and **no `?op=deploy` probe was issued at all** because Pages and the job log agreed. Scraper landed at **149/200** in the same run. The v05.59r poll is armed and has still never been needed; report it if you see one. *(Refreshed at v05.75r once the deploy had landed — this paragraph read "46, expect 47" when §7.22 was first written in the v05.74r commit.)*

**THE DEPLOY — the §7.19 order is now established on its fourth use.** Read **Pages** first (`https://lightaisolutions.github.io/Sales/gs-versions/Classroomgs.version.txt` is a static file, free to read as often as you like) and the workflow's own `Classroom deploy confirmed (GET)` line from the job log. **Probe `?op=deploy` only if those two disagree** — the probe *deploys* whenever the running `/exec` bundle has not yet caught up with GitHub, because `pullAndDeployFromGitHub()` compares the pulled `VERSION` against the `VERSION` of the code currently executing. Use `curl -sL` if you do probe; without `-L` the body is empty and reads as a failure. The v05.59r poll is armed and has still never been needed.

**CHANGELOGS — and this session's finding is that the EST/UTC distinction can decide the outcome.** Use `TZ=America/New_York date '+%Y-%m-%d'`; the rule counts **non-exempt** sections (total minus today's) and fires when that reaches the cap, and **the harness clock can be a day ahead of the EST clock** — UTC rolled to 2026-09-15 mid-session here while EST was still 2026-09-14, and reading the wrong one would have forced three rotations that were not due. **The repo CHANGELOG stands at 115 raw / 98 non-exempt, seventeen sections dated 2026-09-14.** A push landing 2026-09-14 does not rotate; a push landing **2026-09-15 or later ROTATES** — those sixteen stop being exempt, the count is 115, and the oldest whole group to move is **eighteen sections dated 2026-09-05 (`v04.61r`–`v04.78r`)**, leaving **97**. One group is enough, `git fetch --unshallow origin main` is the precondition for SHA enrichment rather than the remedy, and the capacity-counter string **also appears inside older entries**, so match the standalone line rather than the first `grep` hit. `Classroomgs.changelog.md` is at **36/50, 29 non-exempt** — no rotation either way. **`Scrapergs.changelog.md` is now 51 raw / 49 non-exempt and remains armed** — S2 session 3 added a seed and still landed under the line on the EST clock; the next session to touch `Scraper.gs` on a later EST day fires it (oldest group: twenty-six sections dated 2026-08-28). **A Phase 4 row does not touch Scraper**, so that trap is not yours. `Profilerhtml.changelog.md` sits at 49/50 and you must not touch Profiler at all.

**Three open items recorded and not taken, one of which is now finally in your path.** The `.cl-tbl` `min-width` question has a **fifth** clean data point — S2 session 3's twenty-seven-row, four-column bets table rendered clean at 1400px — so the v05.57r defect is settled as a first column holding one long word, not column or row count; this row's `where-it-fails` **table** is the next chance to test it. The generated segment lesson's `read-next` still **names** the landscape module without linking to it — a generator-template question, and §10.4 forbids hand-editing a generated literal. And **`the-800-vdc-shift`'s `three-chains` intro still says "The previous lesson walked a megawatt from the service entrance to the rack"** — untrue since row 7 inserted `inside-the-rack` between them, and **your insertion moves that boundary again**. It is still not your lesson to open, and correcting it would cost a P8 finding on a literal that is not this session's; but it is now two inserts stale, so say so in the record and let a session with reason to open that lesson take it.

> Author Phase 4 row 8 — the `redundancy-by-the-numbers` mechanism lesson — on Opus 5 xhigh as a fresh session. This is ONE lesson. It authors no guidance module, no dossier and no report, creates no track, and regenerates NO segment.
>
> READ FIRST: repository-information/SESSION-CONTEXT.md (Latest Session); INTEGRATED-REMEDIATION-PLAN.md §7.22 (your brief), §7.20 (Phase 4 row 7's brief, for the mid-track-insertion shape it settled), §7.3 order 6 and §6; CLASSROOM-CURRICULUM-PLAN.md §3.3's `redundancy-by-the-numbers` spec VERBATIM — its eight section ids, their kinds and their order are the contract — plus its `v05.07r scope` note (which narrows the lesson), §4 for the track order and §8 items 2, 4 and 6. Then repository-information/CLASSROOM-SCHEMA.md, .claude/rules/classroom-app.md IN FULL, and .claude/rules/changelog-security.md before you write any public changelog line.
>
> THE WORK — one push commit. (1) Fetch all eleven inputs and read each date OFF the fetched document (G2); the stamp is ten `study:` refs plus `concepts:profiler-concepts`, all public, so the fold is `tracks` and the lesson is analyst-visible. (2) Author `clLessonRedundancyByTheNumbers_()` INSIDE the `// CONTENT START … // CONTENT END` fence at §3.3's eight ids in its order — note that `pricing-a-nine` is a **bars** section and `where-it-fails` is a **table** here, not the callout every previous row used. (3) Register it in `clLessons_()` and insert it into `aidc-grid-to-chip` `lessons[]` at **index 5, immediately before `inside-the-rack`**, bumping the track's `updated` and its running `short`. (4) Keep the lesson to the vocabulary, the nines, the tiers, commissioning and `the-vanishing-ups` — `the-ups-room` owns the UPS ladder and `backup-generation` owns the generator side, and re-teaching either is the predictable failure here. (5) Set `reviewBy` from the nearest dated gate in the fetched material, else six months from `updated`, and write the reasoning out rather than defaulting silently. (6) Flip the §7.3 order 6 row, the §6 Phase 4 row and curriculum plan §7 row 8, all dated. (7) Write §7.23 — the next run's brief, which by the interleave is S2 session 4, `utilities` — in the same commit.
>
> DO NOT: regenerate any segment. Registering this lesson will make several go due — `READ_NEXT` names it in `in-hall-power`, `aidc-developers-and-landlords` and `neoclouds`, and the buying-criteria lexicon can add `grid-equipment` and `hyperscalers-and-ai-labs` — and `PLANNED_TITLES` already holds the exact title you will author, so the whole diff on each is a " (planned)" marker disappearing. Those belong to their own S2 sessions under §10.9. **Expect `--check` to read roughly 5 to 7 due when you finish, not 0, and report the number you actually see.** Do not touch `scripts/check-classroom-content.py` — its module-count assertion is 12 and correct. Do not touch `Scraper.gs`, `Profiler.gs` or `Profiler.html`. Do not refresh `gateDigest` — a mechanism lesson moves no `GATE_SYMBOLS` member, so P3 must be silent, and refreshing would hide a real signal.
>
> VERIFY: `node --check` on a `.js` copy of `Classroom.gs`; `scripts/check-gas-inner-scripts.js`; `scripts/check-classroom-content.py` — **capture a pristine HEAD baseline BEFORE any edit. IT IS 0 ERRORS / 0 WARNINGS at 37 lessons / 8 tracks / 142 gate cases** and must still be 0/0 after, at **38 lessons**. Then `scripts/check-classroom-curriculum.py --strict` (no structural findings, 0 due for review); `scripts/check-classroom-pipeline.py --selftest` (13 fixtures, 0 failures) and `--base origin/main` — expect **P1 + P5**, expect **NO P3 and NO P8**. `scripts/check-readme-tree.py` after the version bump. Playwright render of the new lesson at **analyst AND contributor** with ZERO page errors, ops answered by the REAL serving functions (shim only `validateSessionForData`; the entrypoint is `clShowLesson(id)`, and `_gasPost` returns a **Promise**, not a callback; the session keys are `Classroom_gas_session_token` / `_gas_user_email` / `_gas_user_role` / `_gas_user_permissions`) — then READ EVERY SECTION'S SCREENSHOT, `pricing-a-nine` first because it is the corpus's first `bars` section in this lane and `where-it-fails` second because it is a table where every predecessor used a callout. Bump Classroom's GAS version in BOTH `Classroom.gs` `VERSION` (from `v01.36g`) and `live-site-pages/gs-versions/Classroomgs.version.txt`, with a GENERIC entry in `Classroomgs.changelog.md`.
>
> THE DEPLOY and THE CHANGELOG ARITHMETIC: exactly as the paragraphs above this prompt set out. **Confirm the deploy from Pages and the job log, NOT by probing `?op=deploy` first.** Deployment counter **47/200, measured** — v05.74r confirmed on the first GET leg with no POLL line and no probe issued; Scraper is at 149/200. Repo CHANGELOG **115 raw / 98 non-exempt with seventeen sections dated 2026-09-14**, so **2026-09-15 or later rotates the eighteen sections dated 2026-09-05, leaving 97** — but re-count it yourself **on the EST clock**, because the harness clock can be a day ahead and reading the wrong one forces rotations that are not due.
>
> Normal Pre-Commit and Pre-Push checklists; `claude/*` branch restarted from `origin/main` first, `git ls-remote` before each push — the auto-merge workflow adds a `[skip ci]` SHA-tracker commit after every merge.
>
> AFTER THIS SESSION Phase 4 is 8 of 26 and S2 is 3 of ~19, so the interleave returns to S2 and the recommended next run is **S2 session 4 — the `utilities` landscape module** (14 members, 6 · 2 · 6 at v05.74r; re-measure on the day) — for which §7.23 should be written.

### 7.23 · Paste-in brief for S2 session 4 — `utilities` (Opus 5 xhigh) — written 2026-09-14 at v05.76r, at the close of Phase 4 row 8

**Why this and not another Phase 4 row.** Phase 4 is now **8 of 26** and S2 is **3 of ~19**, so the interleave stands at 8:3 and §7.4 item 6 alternates the two long lanes. It is S2's turn, and `utilities` is the next segment by the §7.3 sales-value order. Phase 4's row 9 (`the-campus-as-a-power-project`) is not going anywhere.

**The segment, measured on 2026-09-14 rather than forecast: 14 members, 6 · 2 · 6.** Six incumbents (`dominion-energy`, `southern-company`, `entergy`, `oncor`, `aep`, `xcel-energy`), two challengers (`nrg-energy`, `vistra` — both competitive **retail** suppliers rather than wires companies) and six adjacents (`engie-north-america`, `tesla`, `nextera-energy-resources`, `invenergy`, `grid-united`, `pattern-energy`). **Re-measure on the day** — three of the last four S2 sessions found the registry had moved under a written-down figure, and this one was exact, which is the less common outcome. The `notes` field records that the reserved adjacent role was filled at V3 (v05.40r) by the two transmission developers, so the adjacency story here is *ownership slices and build-own-transfer*, not generation.

**The one structural thing that makes this segment different from the first three landscapes: a guidance module already covers part of it.** `utility-aidc-procurement-2026-08` is registered and is the **playbook** — how an AI load is procured through a utility. `landscape-utilities-2026-09` is the **market-structure judgment layer** over the same franchises: who dominates and on what basis, who threatens, each player's bet. Read the existing module first and write the landscape so the two are explicitly non-overlapping, the way `vertiv` and `schneider-electric`'s UPS spines were made to state their split in each other's opening callout. A landscape that re-tells the procurement playbook is the predictable failure here, exactly as re-teaching the UPS was row 8's.

**The second structural thing: the challengers are not a smaller version of the incumbents.** The two challengers are competitive retail suppliers and four of the six adjacents sell *to* or *through* the incumbents rather than against them. The `storage-developers-and-ipps` finding — that depth is not concentration, and that a threat section padded to the previous module's proportions would have missed it — applies here with a different shape: the threat to a regulated wires franchise is not another wires franchise, it is disintermediation of the load. Let the section proportions follow the segment rather than the last module.

**The Scraper-seed question must be asked, and the answer is not obviously yes.** S2 session 1 and session 3 each added one seed; session 2 did not, because an existing seed already scored its layer. Check every candidate term against **all** the seeds already in `SCRAPER_SEGMENT_SEEDS` before proposing one — session 3's first draft proposed two terms that `topic-storage-offtake` and `topic-capacity-markets` already carried. The utility layer is well covered by existing rate-case and procurement seeds; a new seed needs a scoring gap it can name, and a separate ranking table or filing class behind it. **A `tv` marker is needed only when an existing seed's terms are edited, never for a new key.**

**THE DUE-COUNT PREDICTION, and row 8 corrected the mechanism the last three briefs described.** `--check` reads **5 due** as you start — `power-conversion-and-rack-power-silicon` and `compute-and-the-rack` (row 7's residue) plus `in-hall-power`, `aidc-developers-and-landlords` and `neoclouds` (row 8's). **None of them is yours and none of them is `utilities`.** Registering `landscape-utilities-2026-09` will make `segment-utilities` go due on `read-next` — the generator's `sec_read_next` renders "**No landscape module yet.**" until a `landscape-<segment-id>-YYYY-MM` id exists in `guidanceDocs_()`, then names it — so expect **5 → 6 due**, regenerate `--segment utilities` only, and **end back at 5**. Leave the other five alone: each belongs to its own S2 session under §10.9.

**And the correction row 8 made, which every future Phase 4 and S2 brief should inherit.** §7.22 described two independent channels by which registering a lesson makes segments due — `READ_NEXT` membership and the buying-criteria lexicon — and predicted 5 to 7. The count came in at 5, and the reason is that **the two channels are not independent: the lexicon is subordinate to `READ_NEXT`.** `sec_what_is_bought` guards its match with `if lid in eligible`, where `eligible` is `set(READ_NEXT.get(seg["id"], []))`, so a lesson can only alter `what-is-bought-and-on-what` in a segment whose `READ_NEXT` already names it. `grid-equipment` and `hyperscalers-and-ai-labs` both carry a criterion containing the word "availability" and neither could ever have gone due, because `redundancy-by-the-numbers` is not in their `READ_NEXT`. **The bound is |READ_NEXT membership| + prior residue**, and the lexicon only decides whether an eligible segment differs in one section or two.

**The baselines to capture BEFORE any edit, all measured at the close of v05.76r.** `check-classroom-content.py` is **0 errors / 0 warnings at 38 lessons, 8 tracks, 142 gate cases** — held unbroken through row 8, so nothing inherited is in that output. **Its hard-coded guidance-module assertion reads 12 and S2 session 4 must move it to 13** in the same commit — the one edit outside the fence and the one that stops a session dead if met cold. `check-classroom-curriculum.py --strict`: **no structural findings, 0 items due for review** — the nearest is `large-load-interconnection-2026-09` at 2026-11-16. `check-classroom-pipeline.py --selftest`: **13 fixtures / 0 failures**.

**Versions and counters to bump from.** Classroom GAS **v01.37g** (both `Classroom.gs` `VERSION` and `live-site-pages/gs-versions/Classroomgs.version.txt`); page **v01.14w**, and no page bump is expected. Repo **v05.77r**. Scraper **v02.04g** and Profiler **v01.39g** — **Profiler is never yours**; Scraper is only yours if the seed question answers yes. **Deployment counter: 48/200, measured rather than forecast** — v05.76r's workflow reported `Classroom deploy confirmed (GET): Updated to v01.37g (deployment 48) | 48/200` **on the first GET leg with no `POLL` line**, and **no `?op=deploy` probe was issued at all** because Pages and the job log agreed; that is the §7.19 order working correctly on its fifth consecutive use. Scraper stayed at **149/200**. The v05.59r poll is armed and has still never been needed; report it if you see one. *(Refreshed at v05.77r once the deploy had landed — this paragraph read "expect 48/200" when §7.23 was first written in the v05.76r commit.)*

**THE DEPLOY — the §7.19 order, now on its fifth use.** Read **Pages** first (`https://lightaisolutions.github.io/Sales/gs-versions/Classroomgs.version.txt` is a static file, free to read as often as you like) and the workflow's own `Classroom deploy confirmed (GET)` line from the job log. **Probe `?op=deploy` only if those two disagree** — the probe *deploys* whenever the running `/exec` bundle has not caught up with GitHub, because `pullAndDeployFromGitHub()` compares the pulled `VERSION` against the `VERSION` of the code currently executing. Use `curl -sL` if you do probe; without `-L` the body is empty and reads as a failure. The v05.59r poll is armed and has still never been needed.

**CHANGELOGS — and row 8 hit the EST/UTC trap head-on, which is why it is stated first here.** Use `TZ=America/New_York date '+%Y-%m-%d'`. Row 8 ran with the harness clock reading **2026-09-15 UTC** while EST was still **2026-09-14**, and reading the wrong one would have forced a rotation that was not due. The rule counts **non-exempt** sections (total minus today's EST) and fires when that **reaches** the cap. **The repo CHANGELOG stands at 117 raw / 98 non-exempt, nineteen sections dated 2026-09-14.** A push landing 2026-09-14 does not rotate; a push landing **2026-09-15 or later ROTATES** — those nineteen stop being exempt, the count is 117, and the oldest whole group to move is **eighteen sections dated 2026-09-05 (`v04.61r`–`v04.78r`)**, leaving **99**. One group is enough; `git fetch --unshallow origin main` is the precondition for SHA enrichment rather than the remedy; and the capacity-counter string **also appears inside older entries**, so match the standalone line rather than the first `grep` hit. `Classroomgs.changelog.md` is **37/50 with 29 non-exempt** — no rotation either way. **`Scrapergs.changelog.md` is 51 raw / 49 non-exempt and remains armed**: if the seed question answers yes on a later EST day, that push fires rotation (oldest group: twenty-six sections dated 2026-08-28). `Profilerhtml.changelog.md` sits at 49/50 and you must not touch Profiler at all.

**Four open items recorded and not taken.** The `.cl-tbl` `min-width` question now has **seven** clean data points — row 8's 4 × 6 vocabulary table and 4 × 5 `where-it-fails` table both rendered clean at 1400px — so the v05.57r defect is settled as a first column holding one long unbreakable word, and the question should be closed rather than carried the next time a session has reason to open `Classroom.html`. The generated segment lesson's `read-next` still **names** the landscape module without linking to it, and **S2 session 4 is the session with the best claim on it**: it is a generator-template question (§10.4 forbids hand-editing the generated literal), it lives in `sec_read_next`, and you will be running the generator anyway. **`the-800-vdc-shift`'s `three-chains` intro is now three inserts stale** — it says "The previous lesson walked a megawatt from the service entrance to the rack", untrue since row 7 and further off since row 8 — still a P8 cost on a literal that is not S2's, so it waits for a session with reason to open that lesson. And the render harness needs `favicon.ico` copied into the scratch site root alongside `profiler-data/`, `sounds/` and `images/`, or the render reports a 404 that is the harness's and not the page's.

> Author S2 session 4 — the `utilities` landscape module — on Opus 5 xhigh as a fresh session. This is ONE guidance module plus ONE segment regeneration. It authors no lesson, no dossier and no report, and creates no track.
>
> READ FIRST: repository-information/SESSION-CONTEXT.md (Latest Session); INTEGRATED-REMEDIATION-PLAN.md §7.23 (your brief), §7.21 (S2 session 3's brief, for the landscape shape it settled), §7.3 order 6 and §6; CLASSROOM-CURRICULUM-PLAN.md §10.6 VERBATIM (the landscape-module spec — its section ids and their order are the contract), §10.3–10.4 (the generated segment lesson and what may not be hand-edited), §10.9 and §8 items 2 and 4. Then repository-information/CLASSROOM-SCHEMA.md, .claude/rules/industry-guidance.md and .claude/rules/classroom-app.md IN FULL, and .claude/rules/changelog-security.md before you write any public changelog line.
>
> THE WORK — one push commit. (1) Write the analysis file first — `repository-information/industry-guidance/landscape-utilities-analysis.md` — with a claims ledger in which every load-bearing claim names a dossier field at its `profileVersion`. Corpus synthesis over the 14 member dossiers and `profiler-segments.json` only; no ingested document, no web research. (2) Author `guidanceDocLandscapeUtilities_()` BELOW the `// CONTENT END` fence at §10.6's section ids in its order, id `landscape-utilities-2026-09`, and register it in `guidanceDocs_()`. (3) **Move the content checker's guidance-module assertion from 12 to 13** and extend its comment — this is the one edit outside the fence and the one that stops the session dead if met cold. (4) Read `utility-aidc-procurement-2026-08` first and write the landscape so the two state their split explicitly; the playbook is that module's, the market-structure judgment is yours. (5) Ask the Scraper-seed question and check every candidate term against all the seeds already in `SCRAPER_SEGMENT_SEEDS` before proposing one. (6) Regenerate `--segment utilities` ONLY. (7) Flip the §7.3 order 6 row, the §6 S2 row and curriculum plan §10's roster, all dated. (8) Write §7.24 — the next run's brief, which by the interleave is Phase 4 row 9, `the-campus-as-a-power-project` — in the same commit.
>
> DO NOT: author or revise any lesson inside the content fence. Do not regenerate any segment but your own — `--check` reads 5 due before you start and all five belong to Phase 4's lane and to their own S2 sessions under §10.9. Do not touch `Profiler.gs` or `Profiler.html`. Do not refresh `gateDigest` — a module below the fence moves no `GATE_SYMBOLS` member, so P3 must be silent, and refreshing would hide a real signal. Do not hand-edit the generated `segment-utilities` literal (§10.4).
>
> VERIFY: `node --check` on a `.js` copy of `Classroom.gs`; `scripts/check-gas-inner-scripts.js`; `scripts/check-classroom-content.py` — **capture a pristine HEAD baseline BEFORE any edit. IT IS 0 ERRORS / 0 WARNINGS at 38 lessons / 8 tracks / 142 gate cases** and must still be 0/0 after. Then `scripts/check-classroom-curriculum.py --strict` (no structural findings, 0 due for review); `scripts/check-classroom-pipeline.py --selftest` (13 fixtures, 0 failures) and `--base origin/main`; `build-classroom-segments.py --check` — **expect 5 due before, 6 after registering, and 5 again after regenerating your own segment.** `scripts/check-readme-tree.py` after the version bump. Playwright render of the new module at CONTRIBUTOR (guidance is contributor+, so analyst is the negative test, not the positive one) with ZERO page errors, ops answered by the REAL serving functions — the guidance entrypoint is `clShowGuidanceDoc(id)`, `_gasPost` returns a PROMISE, the session keys are `Classroom_gas_session_token` / `_gas_user_email` / `_gas_user_role` / `_gas_user_permissions`, and the scratch site root needs `favicon.ico` copied in beside `profiler-data/`, `sounds/` and `images/`. Then READ EVERY SECTION'S SCREENSHOT. Bump Classroom's GAS version in BOTH `Classroom.gs` `VERSION` (from `v01.37g`) and `live-site-pages/gs-versions/Classroomgs.version.txt`, with a GENERIC entry in `Classroomgs.changelog.md`.
>
> THE DEPLOY and THE CHANGELOG ARITHMETIC: exactly as the paragraphs above this prompt set out. **Confirm the deploy from Pages and the job log, NOT by probing `?op=deploy` first.** Deployment counter **48/200, measured** — v05.76r confirmed on the first GET leg with no POLL line and no probe issued; Scraper is at 149/200. Repo CHANGELOG **117 raw / 98 non-exempt with nineteen sections dated 2026-09-14**, so **2026-09-15 or later rotates the eighteen sections dated 2026-09-05, leaving 99** — but re-count it yourself **on the EST clock** (`TZ=America/New_York date`), because row 8 ran with the harness clock a full day ahead and reading the wrong one forces rotations that are not due.
>
> Normal Pre-Commit and Pre-Push checklists; `claude/*` branch restarted from `origin/main` first, `git ls-remote` before each push — the auto-merge workflow adds a `[skip ci]` SHA-tracker commit after every merge, and after it merges use `git fetch --prune origin` rather than `git fetch origin main`, or the stale remote-tracking ref makes the stop hook report that SHA-tracker commit as unpushed.
>
> AFTER THIS SESSION Phase 4 is 8 of 26 and S2 is 4 of ~19, so the interleave returns to Phase 4 and the recommended next run is **Phase 4 row 9 — `the-campus-as-a-power-project`** (`aidc-campus` position 3, the nine `project:` slugs plus the graph and eighteen landlord and neocloud study guides — the widest stamp in the queue) — for which §7.24 should be written.

### 7.24 · Paste-in brief for Phase 4 row 9 — `the-campus-as-a-power-project` (Opus 5 xhigh) — written 2026-09-14 at v05.78r, at the close of S2 session 4

**Why this and not another S2 segment.** Phase 4 is **8 of 26** and S2 is now **4 of ~19**, so the interleave stands at 8:4 and §7.4 item 6 alternates the two long lanes. It is Phase 4's turn, and row 9 is the next unbuilt row by the §7 order. S2 session 5 (`power-conversion-and-rack-power-silicon`) is not going anywhere — and note that its segment is already one of the five standing due, so that session clears its own residue.

**This is the widest stamp in the queue, and that is the row's whole character.** §3.4's spec plus its v05.07r amendment name **nine `project:` slugs** — `colossus`, `frontier`, `homer-city`, `hyperion`, `jupiter-nm`, `lighthouse`, `river-bend-campus`, `stargate`, `trimount`, all nine confirmed present in `profiler-projects.json` on 2026-09-14 — plus `graph:profiler-graph`, `concepts:profiler-concepts`, and **eighteen `study:` guides** across the landlord and neocloud bench (vantage, stack-infrastructure, crusoe, xai, meta, applied-digital, nebius, oracle, hut-8, cipher-mining, galaxy-digital, fermi-america, tract, prime-data-centers, compass-datacenters, edgecore, powerhouse-data-centers, digital-realty, cyrusone). **Twenty-nine inputs is roughly three times any previous row.** Budget the fetch phase accordingly, and expect the omission record to be the longest any row has written: §8 item 2 requires every pin read off the fetched document, and G7 requires every ref resolved by opening the file and matching `slug`. **Do not pin a guide you did not draw on** — row 7's discipline (the omission record moved one level down, to the candidate *sections* fetched and not used) is the model, and at eighteen guides it matters more here than anywhere.

**The id and the title change at authoring, and §3.4 says so explicitly.** "Title and section become *Reading the Named Projects* / `the-named-projects` at authoring, so no count is baked into a permanent id." **The lesson id `the-campus-as-a-power-project` stays** — it is the §4 position and the §7 row — but the section id is `the-named-projects`, not `the-eight-projects`, and the title drops the count. That is the same class of id-permanence exception row 6 took for *Eleven Buyer Classes*, and it is instructed rather than discretionary. **Read the project count off the registry on the day** — it was nine on 2026-09-14, and the spec's own prose still says eight in places.

**Where it lands: `aidc-campus` position 3, a mid-track insertion.** The track holds exactly two built lessons today — `heat-is-the-constraint` (position 1) and `where-bess-plugs-in` (position 5) — so §4 position 3 falls **between** them, and `lessons[]` becomes three entries with the new id in the middle. Expect **P1 + P5, no P8, no P3** — the signature rows 3, 4, 6, 7 and 8 set. Bump the track's `updated` (it reads 2026-09-13) and give the track's running `short` whatever clause the new lesson earns.

**The content contract is the trap on this row, and §3.4 names it.** These are the projects a seller is asked about **by name**, and the contract forbids company trivia in a mechanism lesson. **Teach the lifecycle and use the projects as evidence** — after the lesson a reader should be able to take any new campus headline and place it on the same lifecycle with the same cast. A drill card that asks which project has how many megawatts is exactly the card the contract exists to prevent; a card that asks *why an islanded campus chose engines over turbines* is the model, and §3.4 supplies it. Note also C8's standing finding, restated in the v05.07r amendment: **no ranking of AIDC landlords exists — teach the lifecycle, never a league table.**

**Sections — seven at §3.4's ids, in its order, with one rename:** `from-land-to-fit-out` (timeline) · **`the-named-projects`** (table — name · kind · location · prime power · who builds · who is the tenant) · `who-is-in-the-room` (prose) · `reading-an-announcement` (callout — gross against critical MW, contracted against connected, "up to", and what a first phase actually energises) · `where-it-fails` (callout) · `drill` · `check-yourself`. §8 item 6's variety rule is satisfied by the timeline-table-prose-callout mix; the `sales` line belongs on roughly half.

**THE DUE-COUNT PREDICTION, on the bound row 8 established.** `--check` reads **5 due** as you start — `power-conversion-and-rack-power-silicon`, `in-hall-power`, `compute-and-the-rack`, `aidc-developers-and-landlords`, `neoclouds` — and **none of them is yours**; each belongs to its own S2 session under §10.9. The bound for a new lesson is **|`READ_NEXT` membership| + prior residue**: `sec_what_is_bought` guards its lexicon match with `if lid in eligible`, where `eligible` is that segment's own `READ_NEXT` list, so the lexicon is **subordinate to `READ_NEXT`** and cannot make a segment due on its own. **The membership was measured for you on 2026-09-14 and it is five** — `clean-firm-and-nuclear`, `epc-and-construction`, `aidc-developers-and-landlords`, `hyperscalers-and-ai-labs`, `neoclouds` all name `the-campus-as-a-power-project` in `READ_NEXT`. Two of those five (`aidc-developers-and-landlords`, `neoclouds`) are already in the standing 5-due, so they gain a second reason rather than becoming newly due; the other three are new. **Expect `--check` to go 5 due → 8 due.** Re-grep on the day in case the map moved. **And one thing differs from rows 7 and 8, which both saw the whole diff reduce to a " (planned)" marker disappearing:** `PLANNED_TITLES["the-campus-as-a-power-project"]` reads **"Reading the Eight Named Projects"**, and §3.4 instructs the authored title to drop the count. **The titles will not be byte-identical**, so each of the five differs by the title string as well as the marker. That is expected, not a defect. Regenerate **nothing** regardless: `segment-*` literals are S2's under §10.9.

**The baselines to capture BEFORE any edit, all measured at the close of v05.78r.** `check-classroom-content.py` is **0 errors / 0 warnings at 38 lessons, 8 tracks, 142 gate cases** — held unbroken through four consecutive sessions, so nothing inherited is in that output and any error is unambiguously yours. **Its guidance-module assertion now reads 13, and a Phase 4 row does not touch it.** `check-classroom-curriculum.py --strict`: **no structural findings, and 1 item due for review — `landscape-utilities-2026-09` at 2026-10-01.** That one is expected and is not yours: it is S2 session 4's module shipping inside its own 30-day horizon on a real gate, and §10.6 (k) records why. **If you run on or after 2 October it will read "⚠ review due" rather than "due soon", and that is still not a Phase 4 row's to fix** — the quarterly guidance Routine and a future S2 or guidance session own it. `check-classroom-pipeline.py --selftest`: **13 fixtures / 0 failures**.

**Versions and counters to bump from.** Classroom GAS **v01.38g** (both `Classroom.gs` `VERSION` and `live-site-pages/gs-versions/Classroomgs.version.txt`); page **v01.14w**, and no page bump is expected. Repo **v05.79r**. Scraper **v02.05g** and Profiler **v01.39g** — **neither is yours; a Phase 4 row touches neither.** **Deployment counters: Classroom 49/200 and Scraper 150/200, MEASURED rather than forecast** — v05.78r's workflow reported `Scraper deploy confirmed (GET): Updated to v02.05g (deployment 150) | 150/200` and `Classroom deploy confirmed (GET): Updated to v01.38g (deployment 49) | 49/200`, **both on the first GET leg with no POLL line**, and **no `?op=deploy` probe was issued at all** because Pages and the job log agreed. *(Refreshed at v05.79r once the deploy had landed — this paragraph read "expected" when §7.24 was first written in the v05.78r commit, and both forecasts proved exactly right.)* The v05.59r poll is armed and has still never been needed; report it if you see one.

**THE DEPLOY — the §7.19 order, now on its sixth use and its first two-project run.** Read **Pages** first (`https://lightaisolutions.github.io/Sales/gs-versions/Classroomgs.version.txt` and the Scraper equivalent are static files, free to read as often as you like) and the workflow's own `… deploy confirmed (GET)` lines from the job log. **Probe `?op=deploy` only if those two disagree** — the probe *deploys* whenever the running `/exec` bundle has not caught up with GitHub, because `pullAndDeployFromGitHub()` compares the pulled `VERSION` against the `VERSION` of the code currently executing. Use `curl -sL` if you do probe; without `-L` the body is empty and reads as a failure.

**CHANGELOGS — and the EST/UTC trap has now fired for three sessions running, so it is stated first.** Use `TZ=America/New_York date '+%Y-%m-%d'`. Rows 8, S2 session 3 and S2 session 4 all ran with the harness clock a full day ahead of EST; reading the wrong one would have forced rotations that were not due in all three. The rule counts **non-exempt** sections (total minus today's EST) and fires when that **reaches** the cap. **That rotation has since fired, so the arithmetic below is the post-rotation state rather than a forecast.** The v05.79r push landed on **2026-09-15**, the twenty sections dated 2026-09-14 stopped being exempt at 118 non-exempt, and **two date groups moved**: eighteen dated 2026-09-05 (`v04.61r`–`v04.78r`) took it to exactly 100, still at the trigger, and twenty dated 2026-09-06 (`v04.79r`–`v04.98r`) took it to 80. **All 38 headers resolved to a real commit SHA — none archived as unavailable** — because the clone was deepened at the start of the v05.78r session and stayed deep. **The repo CHANGELOG now stands at 81 raw / 80 non-exempt, one section dated 2026-09-15**, and the oldest remaining group is **thirteen sections dated 2026-09-07**. A push landing 2026-09-15 adds one and leaves 80 non-exempt; a push landing 2026-09-16 or later reaches 81 non-exempt — **well under the trigger either way, so row 9 will not rotate.** Re-count anyway on the EST clock. `git fetch --unshallow origin main` is the precondition for SHA enrichment rather than the remedy, and the capacity-counter string **also appears inside older entries**, so match the standalone line. `Classroomgs.changelog.md` is **38 raw / 29 non-exempt** against 50 — no rotation either way. **`Scrapergs.changelog.md` is 52 raw / 49 non-exempt and remains armed at one short of its trigger for the second session running** — but a Phase 4 row does not touch `Scraper.gs`, so it is not yours. `Profilerhtml.changelog.md` sits at 49/50 and Profiler is never yours.

**Four open items recorded and not taken.** The `.cl-tbl` `min-width` question now has **nine** clean data points — S2 session 4's 8 × 4 bets table and 16 × 4 indicators table both rendered clean at 1400px — and the v05.57r defect is settled as a first column holding one long unbreakable word; **close it** the next time a session has reason to open `Classroom.html`. The generated segment lesson's `read-next` **names** the landscape module without linking to it, and **this is now known not to be a generator question**: `clFmt` resolves `**bold**`, `*italic*` and `{{term}}` **and has no link token at all**, so linking is a `Classroom.html` renderer feature; and the string is emitted in every segment, so a generator edit would make all nineteen due while §10.9 lets a session regenerate only its own. It belongs with the `.cl-tbl` item, in a session that opens the page, plus a developer `--all` run. **`the-800-vdc-shift`'s `three-chains` intro is now three inserts stale** — "The previous lesson walked a megawatt from the service entrance to the rack" has been untrue since row 7 — and it is a P8 cost on a literal that is **in your own track**, so this row is the first with a plausible claim on it; take it only if you are willing to carry the extra P8 and name the section in `changed[]`, and say so either way. And the render harness needs `favicon.ico` copied into the scratch site root alongside `profiler-data/`, `sounds/` and `images/`.

> Author Phase 4 row 9 — `the-campus-as-a-power-project` — on Opus 5 xhigh as a fresh session. This is ONE mechanism lesson. It authors no guidance module, no dossier and no report, creates no track, and regenerates no segment.
>
> READ FIRST: repository-information/SESSION-CONTEXT.md (Latest Session); INTEGRATED-REMEDIATION-PLAN.md §7.24 (your brief), §7.22 (row 8's brief, for the `READ_NEXT` bound it corrected) and §7.3 order 6 and §6; CLASSROOM-CURRICULUM-PLAN.md **§3.4's `the-campus-as-a-power-project` entry VERBATIM including its v05.07r amendment** — its inputs, its section ids and their order are the contract — plus §4 (the position), §7 (the row) and §8 items 2, 6, 7 and 9. Then repository-information/CLASSROOM-SCHEMA.md, .claude/rules/classroom-app.md IN FULL, and .claude/rules/changelog-security.md before you write any public changelog line.
>
> THE WORK — one push commit. (1) Fetch **all twenty-nine inputs** — nine `project:` slugs (re-read `profiler-projects.json` on the day; it held nine on 2026-09-14, and the spec's prose still says eight in places), `graph:`, `concepts:`, and eighteen `study:` guides — and pin each date **off the fetched document**, resolving every ref by opening the file and matching its own identity. Record what you fetched and did not draw on, one level down as row 7 did. (2) Author the lesson at §3.4's ids in its order, with the instructed rename: section **`the-named-projects`**, title **Reading the Named Projects**, lesson id **unchanged**. (3) Insert at `aidc-campus` `lessons[]` **position 3 of the §4 walk — between `heat-is-the-constraint` and `where-bess-plugs-in`** — and bump that track's `updated` (2026-09-13) and its `short`. (4) Register in `clLessons_()`. (5) Teach the **lifecycle**, use the projects as evidence, and drill mechanism — never which project has how many megawatts, and never a landlord league table (C8).
>
> DO NOT: regenerate any segment — `--check` reads 5 due before you start and all five are S2's under §10.9. Do not touch `Profiler.gs`, `Profiler.html` or `Scraper.gs`. Do not refresh `gateDigest` — a lesson inside the fence moves no `GATE_SYMBOLS` member, so P3 must be silent, and refreshing would hide a real signal. Do not fix `landscape-utilities-2026-09`'s review date — it is due by design and is not a Phase 4 row's.
>
> VERIFY: `node --check` on a `.js` copy of `Classroom.gs`; `scripts/check-gas-inner-scripts.js`; `scripts/check-classroom-content.py` — **capture a pristine HEAD baseline BEFORE any edit. IT IS 0 ERRORS / 0 WARNINGS at 38 lessons / 8 tracks / 142 gate cases** and must still be 0/0 after, at 39 lessons. Then `scripts/check-classroom-curriculum.py --strict` (no structural findings; **1 item due for review is expected and is not yours**); `scripts/check-classroom-pipeline.py --selftest` (13 fixtures, 0 failures) and `--base origin/main` — expect **P1 + P5, no P8, no P3**. `scripts/check-readme-tree.py` after the version bump. Playwright render at **analyst** (the lesson's stamp is all-public and folds to `tracks`, so analyst is the positive test here, not the negative one) with ZERO page errors, ops answered by the REAL serving functions — the lesson entrypoint is `clShowLesson(id)`, `_gasPost` returns a PROMISE, the session keys are `Classroom_gas_session_token` / `_gas_user_email` / `_gas_user_role` / `_gas_user_permissions`, overlays to hide are `#auth-wall` / `#signing-out-wall` / `#auth-timers` / `#gas-layer-toggle`, and the scratch site root needs `favicon.ico` copied in beside `profiler-data/`, `sounds/` and `images/`. Then READ EVERY SECTION'S SCREENSHOT, `the-named-projects` first because it is a six-column table and the widest this row carries. Bump Classroom's GAS version in BOTH `Classroom.gs` `VERSION` (from `v01.38g`) and `live-site-pages/gs-versions/Classroomgs.version.txt`, with a GENERIC entry in `Classroomgs.changelog.md`.
>
> THE DEPLOY and THE CHANGELOG ARITHMETIC: exactly as the paragraphs above this prompt set out. **Confirm the deploy from Pages and the job log, NOT by probing `?op=deploy` first.** Deployment counters **Classroom 49/200 and Scraper 150/200 EXPECTED rather than measured** — v05.78r bumped both and its own confirmation is recorded in the v05.78r CHANGELOG notes; correct the figure if the job log differs. Repo CHANGELOG **81 raw / 80 non-exempt** after the v05.79r push rotated two date groups (thirty-eight sections, all thirty-eight SHA-enriched), so **you will not rotate** — but re-count it yourself **on the EST clock** (`TZ=America/New_York date`), because three sessions running have found the harness clock a full day ahead and reading the wrong one forces rotations that are not due.
>
> Normal Pre-Commit and Pre-Push checklists; `claude/*` branch restarted from `origin/main` first, `git ls-remote` before each push — the auto-merge workflow adds a `[skip ci]` SHA-tracker commit after every merge, and after it merges use `git fetch --prune origin` rather than `git fetch origin main`, or the stale remote-tracking ref makes the stop hook report that SHA-tracker commit as unpushed.
>
> AFTER THIS SESSION Phase 4 is 9 of 26 and S2 is 4 of ~19, so the interleave returns to S2 and the recommended next run is **S2 session 5 — `power-conversion-and-rack-power-silicon`** (20 members; its `segment-*` lesson is one of the five standing due, so that session clears its own residue) — for which §7.25 should be written.

### 7.25 · Paste-in brief for S2 session 5 — `power-conversion-and-rack-power-silicon` (Opus 5 xhigh) — written 2026-09-15 at v05.80r, at the close of Phase 4 row 9 *(counters refreshed at v05.81r)*

**Why this and not another Phase 4 row.** Phase 4 is now **9 of 26** and S2 is **4 of ~19**, so the interleave stands at 9:4 and §7.4 item 6 alternates the two long lanes. It is S2's turn, and `power-conversion-and-rack-power-silicon` is next by the §7.3 sales-value order. It also has a property none of the first four landscapes had: **its own `segment-*` lesson is one of the eight standing due**, so this session clears its own residue rather than adding to a backlog.

**READ THIS BEFORE YOU TAKE A SINGLE PIN — the clone finding of v05.80r, and it is new.** A fresh container clones **shallow**, and a shallow clone's boundary commit looks like it introduced every file in the repo. So `git log -1 --format=%cs -- <path>` — which §8 item 2 and G2 make the pin source for `project:` and `concepts:` refs — **stops at the boundary and returns the boundary's date instead of the file's**. Row 9 met this cold: `build-classroom-segments.py --check` read **19 due** against a measured 5, every extra one reporting `inputs moved: concepts:profiler-concepts 2026-09-13→2026-09-14` with `sections differing: none`. `git fetch --unshallow origin main` restored the true answer (commit `5eab070`, **2026-09-13**) and `--check` fell straight back to 5. **The due count is only the alarm; the real cost is the pin.** Authoring off the shallow clone would have written `concepts:profiler-concepts@2026-09-14` into every stamp when the true pin is 2026-09-13 — invisible to every checker, and a false baseline for the next C2 refresh. **Unshallow first, before any measurement and before any pin.** Earlier briefs named unshallowing only as the precondition for CHANGELOG SHA enrichment; it is equally the precondition for the pin reads. If `--check` reports a number wildly above the one below, suspect the clone before you suspect the corpus.

**The segment, measured 2026-09-15 at v05.80r.** `power-conversion-and-rack-power-silicon` holds **20 members — 9 incumbent · 3 challenger · 8 adjacent**: incumbents `sungrow`, `huawei-digital-power`, `delta-electronics`, `liteon`, `infineon`, `sinexcel`, `power-electronics`, `zhonhen`, `vicor`; challengers `megmeet`, `flex`, `heron-power`; adjacents `ge-vernova`, `abb`, `eaton`, `schneider-electric`, `hitachi-energy`, `nvidia`, `tesla`, `ls-energy-solutions`. **Re-measure on the day** — three consecutive S2 sessions have confirmed their brief's figure exactly, and the fourth would be the one that does not. Twenty members puts the `each-players-bet` table between the utilities landscape's eight and the cells landscape's fifteen; report the roster honestly rather than cutting it for length.

**The split rule fires for the second time, and this segment has the sharpest overlap yet.** §7.23's finding was that a landscape can land on a segment an existing guidance module already covers, and that **the split has to be written before the module is**. Here the overlapping module is **`nvidia-800vdc-2026-08`**, and the overlap is not partial: the segment's own definition names "the solid-state transformer and MV-to-800 VDC sidecar, the power shelf, PSU and BBU shelf in the rack, the bus converter and point-of-load regulator at the die", which is that module's entire subject. Two more modules touch a buying criterion rather than the layer — **`eo14420-bulk-power-2026-08`** and **`china-policy-stack-2026-08`** both bear on "manufacturing location under the FCC inverter rule and EO 14420", which is one of the five criteria the registry records. **Read all three before you write §2 of the analysis file.** The defensible split, on the utilities precedent: the 800 VDC module owns the **architecture** — what the voltage change is, why it happens, what it deletes — and this landscape owns the **parties**: who is positioned where along it, which incumbents are defending a conversion stage the architecture removes, and what the three challengers are attacking. The test §7.23 set still applies: **the split is genuine when the one number both documents cite does a different job in each.**

**The baselines to capture BEFORE any edit, all measured at the close of v05.80r.** `check-classroom-content.py` is **0 errors / 0 warnings at 39 lessons, 8 tracks, 142 gate cases** — held unbroken through five consecutive sessions, so nothing inherited is in that output and any error is unambiguously yours. **Its guidance-module assertion reads 13 and you WILL move it to 14** — that is the one edit outside the content fence, and the one that stops the session dead if met cold. `check-classroom-curriculum.py --strict`: **no structural findings, 28 stale pins on the hand-authored lessons** (pre-existing and not an S2 concern; row 9 added none), and **1 item due for review — `landscape-utilities-2026-09` at 2026-10-01**. That last one is S2's own work and **by 2 October it reads "⚠ review due" rather than "due soon"** — still not this session's to fix unless the developer says so; it belongs to the quarterly guidance Routine. `check-classroom-pipeline.py --selftest`: **13 fixtures / 0 failures**. `build-classroom-segments.py --check`: **8 due** — `power-conversion-and-rack-power-silicon`, `in-hall-power`, `clean-firm-and-nuclear`, `compute-and-the-rack`, `epc-and-construction`, `aidc-developers-and-landlords`, `hyperscalers-and-ai-labs`, `neoclouds`. **The first is yours and the other seven are not**; regenerate `--segment power-conversion-and-rack-power-silicon` only, and expect **8 due → 9 due → 8 due** across authoring the module and regenerating your own segment.

**Versions and counters to bump from.** Classroom GAS **v01.39g** (both `Classroom.gs` `VERSION` and `live-site-pages/gs-versions/Classroomgs.version.txt`); page **v01.14w**, and no page bump is expected. Repo **v05.81r**. Scraper **v02.05g** and Profiler **v01.39g** — Profiler is never yours; `Scraper.gs` is yours only if the seed question below answers yes.

**THE DEPLOY — the §7.19 order, on its seventh use.** Read **Pages** first (`https://lightaisolutions.github.io/Sales/gs-versions/Classroomgs.version.txt` is a static file, free to read as often as you like) and the workflow's own `Classroom deploy confirmed (GET)` line from the job log. **Probe `?op=deploy` only if those two disagree** — the probe *deploys* whenever the running `/exec` bundle has not caught up with GitHub, because `pullAndDeployFromGitHub()` compares the pulled `VERSION` against the `VERSION` of the code currently executing. Use `curl -sL` if you do probe; without `-L` the body is empty and reads as a failure. **Deployment counters: Classroom 50/200 and Scraper 150/200, MEASURED rather than forecast** — v05.80r's workflow reported `Classroom deploy confirmed (GET): Updated to v01.39g (deployment 50) | 50/200` **on the first GET leg with no POLL line**, and **no `?op=deploy` probe was issued at all** because Pages and the job log agreed; Scraper was not touched and stays at 150/200. That is the **seventh consecutive** clean use of the §7.19 order. *(Refreshed at v05.81r once the deploy had landed — this paragraph read "expected" when §7.25 was first written in the v05.80r commit, and the forecast proved exactly right.)* The v05.59r poll is armed and has still never been needed; report it if you see one.

**CHANGELOGS — and the EST/UTC trap did NOT fire at v05.80r, for the first time in four sessions.** `TZ=America/New_York date` and the harness both read 2026-09-15. **Do not conclude the trap is gone** — it is a clock convergence, not a fix, and it will diverge again the moment a session runs between 20:00 EST and midnight UTC. Keep using `TZ=America/New_York date '+%Y-%m-%d'`. The rule counts **non-exempt** sections (total minus today's EST) and fires when that **reaches** the cap. The repo CHANGELOG stands at **83 raw / 80 non-exempt**, three sections dated 2026-09-15, and the oldest remaining group is **thirteen sections dated 2026-09-07**. A push landing 2026-09-15 adds one and leaves 80 non-exempt; a push landing 2026-09-16 or later reaches 84 non-exempt — **well under the trigger either way, so this session will not rotate.** Re-count anyway. `git fetch --unshallow origin main` is the precondition for SHA enrichment **and for the pins** (see above), and the capacity-counter string **also appears inside older entries**, so match the standalone line. `Classroomgs.changelog.md` is **39 raw / 38 non-exempt** against 50 — no rotation either way. **`Scrapergs.changelog.md` is 52 raw / 49 non-exempt and remains armed at one short of its trigger for a third session running** — and unlike a Phase 4 row, **an S2 session can fire it**: if the seed question answers yes and the push lands on a later EST day than the file's newest section, rotate it (oldest group: twenty-six sections dated 2026-08-28). `Profilerhtml.changelog.md` sits at 49/50 and Profiler is never yours.

**The Scraper-seed question, with the array name stated correctly.** §10.6 / `industry-guidance.md` step 9 asks whether this segment's layer is scored by any existing term. Check every candidate against **`SCRAPER_INTEREST_TOPIC_SEEDS`** in `Scraper.gs` — that is the array the rule and every guidance-derived precedent use; §7.23 named `SCRAPER_SEGMENT_SEEDS` and was wrong, and §7.24 recorded the correction. Note the likely answer is **no**: `topic-bess-technology` and the 800 VDC coverage already score parts of this layer, and S2 session 2 correctly declined a seed on exactly that reasoning. Add one only if a genuinely unscored sub-layer survives the check against all existing terms — the SiC/GaN silicon layer and the point-of-load regulator are the two worth testing — and record the decision either way.

**Four open items recorded and not taken, and one of them has been reassigned rather than deferred.** (i) The `.cl-tbl` `min-width` question now has **ten** clean data points and, at v05.80r, its first at **six columns** — row 9's `the-named-projects` table is 6 × 9, the widest any mechanism lesson carries by column count, and it rendered clean at 1400px. Column count is about as firmly excluded as evidence can exclude it; the v05.57r defect stands as a first column holding one long unbreakable word. **Close it** the next time a session has reason to edit `Classroom.html`. (ii) The generated segment lesson's `read-next` **names** the landscape module without linking to it: `clFmt` resolves `**bold**`, `*italic*` and `{{term}}` and **has no link token at all**, so this is a `Classroom.html` renderer feature rather than a generator one, and the string is emitted in every segment, so a generator edit would make all nineteen due while §10.9 lets a session regenerate only its own. It belongs with (i), in a session that opens the page, plus a developer `--all` run. (iii) **`the-800-vdc-shift`'s `three-chains` intro is three inserts stale and is NOT waiting for a row with a claim.** §7.24 offered it to Phase 4 row 9 on the grounds that it sits in that row's track; it does not — it is in `aidc-grid-to-chip` while row 9's lesson went into `aidc-campus`, so row 9's insertion moved its predecessor by nothing. Reading §4 forward, **no remaining Phase 4 row acquires a claim either**: row 26 inserts at position 2, ahead of `bridge-power`, and rows 20 and 17 append after `the-800-vdc-shift`. The boundary was last moved by rows 7 and 8 and will not move again, so **stop offering it to the next row** — it belongs to whichever session opens that lesson for its own reason (a G3 revision when its inputs move) or to a developer sweep. **This session has a plausible claim on it for the first time on different grounds**: the intro's subject is the 800 VDC chain, which is this segment's own layer. Take it only if you are willing to carry the extra P8 and name the section in `changed[]`, and say so either way. (iv) The render harness needs `favicon.ico` copied into the scratch site root alongside `profiler-data/`, `sounds/` and `images/`.

> Author S2 session 5 — the `power-conversion-and-rack-power-silicon` landscape module — on Opus 5 xhigh as a fresh session. This is ONE guidance module plus ONE segment regeneration. It authors no lesson, no dossier and no report, and creates no track.
>
> READ FIRST: repository-information/SESSION-CONTEXT.md (Latest Session); INTEGRATED-REMEDIATION-PLAN.md §7.25 (your brief), §7.23 (S2 session 4's brief, for the module-split rule it established), §7.3 order 5 and §6; CLASSROOM-CURRICULUM-PLAN.md §10.6 VERBATIM (the landscape-module spec — its section ids and their order are the contract), §10.3–10.4 (the generated segment lesson and what may not be hand-edited), §10.9 and §8 items 2 and 4. Then repository-information/CLASSROOM-SCHEMA.md, .claude/rules/industry-guidance.md and .claude/rules/classroom-app.md IN FULL, and .claude/rules/changelog-security.md before you write any public changelog line.
>
> **UNSHALLOW THE CLONE FIRST — before any measurement and before any pin.** `git fetch --unshallow origin main`. A fresh container clones shallow, and a shallow clone's boundary commit looks like it introduced every file, so `git log -1 --format=%cs -- <path>` returns the boundary's date rather than the file's. Row 9 met this: `--check` read 19 due against a measured 5, and authoring off it would have written `concepts:profiler-concepts@2026-09-14` when the true pin is 2026-09-13 — a silent freshness bug no checker sees. If `--check` reports a number wildly above the one in this brief, suspect the clone before the corpus.
>
> THE WORK — one push commit. (1) Write the analysis file first — `repository-information/industry-guidance/landscape-power-conversion-analysis.md` — with a claims ledger in which every load-bearing claim names a dossier field at its `profileVersion`. Corpus synthesis over the 20 member dossiers and `profiler-segments.json` only; no ingested document, no web research. (2) **Write §2 — the split — before any other section.** `nvidia-800vdc-2026-08` owns the architecture; this landscape owns the parties. Read that module in full first, plus `eo14420-bulk-power-2026-08` and `china-policy-stack-2026-08`, which touch the manufacturing-location buying criterion. (3) Author `guidanceDocLandscapePowerConversion_()` BELOW the `// CONTENT END` fence at §10.6's section ids in its order, id `landscape-power-conversion-and-rack-power-silicon-2026-09`, and register it in `guidanceDocs_()`. (4) **Move the content checker's guidance-module assertion from 13 to 14** and extend its comment — the one edit outside the fence. (5) Ask the Scraper-seed question and check every candidate against **`SCRAPER_INTEREST_TOPIC_SEEDS`** (not `SCRAPER_SEGMENT_SEEDS`; §7.23 named it wrongly and §7.24 corrected it); the likely answer is no, because existing terms already score this layer. (6) Regenerate `--segment power-conversion-and-rack-power-silicon` ONLY. (7) Flip the §7.3 order 5 row, the §6 S2 row and curriculum plan §10's roster, all dated. (8) Write §7.26 — the next run's brief, which by the interleave is Phase 4 row 10, `how-a-storage-project-happens` — in the same commit.
>
> DO NOT: author or revise any lesson inside the content fence. Do not regenerate any segment but your own — `--check` reads 8 due before you start and the other seven belong to their own sessions under §10.9. Do not touch `Profiler.gs` or `Profiler.html`. Do not refresh `gateDigest` — a module below the fence moves no `GATE_SYMBOLS` member, so P3 must be silent, and refreshing would hide a real signal. Do not hand-edit the generated `segment-power-conversion-and-rack-power-silicon` literal (§10.4). Do not fix `landscape-utilities-2026-09`'s review date — it is due by design and, if you run on or after 2 October, it will read "⚠ review due" rather than "due soon"; that is still the quarterly Routine's, not yours.
>
> VERIFY: `node --check` on a `.js` copy of `Classroom.gs`; `scripts/check-gas-inner-scripts.js`; `scripts/check-classroom-content.py` — **capture a pristine HEAD baseline BEFORE any edit. IT IS 0 ERRORS / 0 WARNINGS at 39 lessons / 8 tracks / 142 gate cases** and must still be 0/0 after. Then `scripts/check-classroom-curriculum.py --strict` (no structural findings; 28 stale pins and 1 item due for review are both pre-existing and not yours); `scripts/check-classroom-pipeline.py --selftest` (13 fixtures, 0 failures) and `--base origin/main`; `build-classroom-segments.py --check` — expect **8 due → 9 due → 8 due** across authoring and regenerating your own segment. `scripts/check-readme-tree.py` after the version bump. Playwright render of the new module at **contributor** (guidance is contributor+, so analyst is the negative test, not the positive one) with ZERO page errors, ops answered by the REAL serving functions — the guidance entrypoint is `clShowGuidanceDoc(id)`, `_gasPost` returns a PROMISE, `sess.permissions` must be an ARRAY in the shim, the session keys are `Classroom_gas_session_token` / `_gas_user_email` / `_gas_user_role` / `_gas_user_permissions`, overlays to hide are `#auth-wall` / `#signing-out-wall` / `#auth-timers` / `#gas-layer-toggle`, and the scratch site root needs `favicon.ico` copied in beside `profiler-data/`, `sounds/` and `images/`. Then READ EVERY SECTION'S SCREENSHOT. Bump Classroom's GAS version in BOTH `Classroom.gs` `VERSION` (from `v01.39g`) and `live-site-pages/gs-versions/Classroomgs.version.txt`, with a GENERIC entry in `Classroomgs.changelog.md`.
>
> THE DEPLOY and THE CHANGELOG ARITHMETIC: exactly as §7.25's paragraphs set out. **Confirm the deploy from Pages and the job log, NOT by probing `?op=deploy` first.** Deployment counters **Classroom 50/200 and Scraper 150/200, MEASURED rather than forecast** — v05.80r's workflow reported `Classroom deploy confirmed (GET): Updated to v01.39g (deployment 50) | 50/200` on the first GET leg with no POLL line and no `?op=deploy` probe issued at all, the seventh consecutive clean use of the §7.19 order; Scraper was untouched. Repo CHANGELOG **83 raw / 80 non-exempt**, so **you will not rotate** — but re-count it yourself on the EST clock (`TZ=America/New_York date`). The EST/UTC trap did not fire at v05.80r because the clocks converged, which is not a fix; it diverges again for any session running between 20:00 EST and midnight UTC. **`Scrapergs.changelog.md` is armed at one short of its trigger for a third session running, and unlike a Phase 4 row you can fire it** — if you add a seed and the push lands on a later EST day than that file's newest section, rotate it.
>
> Normal Pre-Commit and Pre-Push checklists; `claude/*` branch restarted from `origin/main` first, `git ls-remote` before each push — the auto-merge workflow adds a `[skip ci]` SHA-tracker commit after every merge, and after it merges use `git fetch --prune origin` rather than `git fetch origin main`, or the stale remote-tracking ref makes the stop hook report that SHA-tracker commit as unpushed.
>
> AFTER THIS SESSION Phase 4 is 9 of 26 and S2 is 5 of ~19, so the interleave returns to Phase 4 and the recommended next run is **Phase 4 row 10 — `how-a-storage-project-happens`** (`market-access` position 2) — for which §7.26 should be written.
### 7.26 · Paste-in brief for Phase 4 row 10 — `how-a-storage-project-happens` (Opus 5 xhigh) — written 2026-09-15 at v05.83r, at the close of S2 session 5

**Why this and not another S2 segment.** Phase 4 is **9 of 26** and S2 is now **5 of ~19**, so the interleave stands at 9:5 and §7.4 item 6 hands the turn back to Phase 4. Row 10 is `how-a-storage-project-happens` — `market-access` **position 2**, gate `tracks`, the sixth mid-track insertion of the phase.

**UNSHALLOW FIRST — and as of v05.83r this is a checklist item, not a brief's reminder.** `git fetch --unshallow origin main` before any pin read, any `--check` run, any `git log -1 --format=%cs` date and any CHANGELOG rotation. The Session Start Checklist's Always Run section now carries a **"Deepen the clone"** bullet requiring exactly this, so it no longer depends on whichever brief happens to mention it. The reason is still the one row 9 measured: a shallow clone's boundary commit appears to introduce every file, so a per-file `git log` returns the boundary's date, and §8 item 2 and G2 make that command the **pin source** — a wrong pin is a silent freshness bug no checker can see. v05.83r confirmed the fix from the other direction: with the clone deepened first, all **26** rotation SHAs resolved and `--check` opened at the true number with `inputs moved: none` on every line.

**The lesson, as §3.5 and §7 row 10 specify it.** Title *From Site Control to Commercial Operation* — **this exact string is already in the generator's `PLANNED_TITLES`**, so if you change it, change it deliberately and know that both segments below will render the new one. `short`, `group` **Market Access & Bankability**, gate `tracks`. **Seven sections, in this order:** `the-lifecycle` (timeline — site control → interconnection → offtake → financing → EPC award → construction → commissioning → COD) · `who-does-what-when` (table — stage · owner · what is decided · what a supplier can influence) · `where-the-seller-enters` (prose — bid-stage BOM lock, approved-vendor lists, the IE's technical review) · `how-overruns-hide` (callout — percentage-of-completion, backlog that is not one number) · `where-it-fails` (callout — offtake that does not close; an interconnection date that moves the pro forma; a vendor qualified after the RFP cycle) · `drill` · `check-yourself`.

**The input roster is SIXTEEN, and the arithmetic is worth doing yourself before you trust it.** §3.5's base list carries **eight** — `study:fluence`, `study:tesla`, `study:mortenson`, `study:primoris`, `profile:solv-energy`, `profile:blattner`, `study:hitt`, `concepts:profiler-concepts` — and the v05.07r amendment adds **eight** more: `study:aypa-power`, `study:intersect-power`, `study:invenergy`, `study:excelsior-energy-capital`, `study:recurrent-energy`, `study:dnv`, `study:mastec`, `study:samsung-ct`. §7's row 10 lists all sixteen and the two halves agree — **but row 9's brief was one short on exactly this arithmetic and the lesson's own §3 entry settled it**, so re-derive rather than copy. All sixteen are `public`, so the stamp folds to **`tracks`** and the lesson is analyst-visible. Pin every one off the document fetched that run (G2), and resolve every `study:` ref by opening the file and matching its own `slug` (G7). Twelve of these guides were **fetched, read and deliberately not pinned by row 9** because they teach an instrument or a lifecycle stage rather than a buyer class — §3.5 assigns that material to **this row and row 11**, so most of row 9's omission list is now your input list. Read that record in §7 row 9 before deciding what to drop.

**THE DUE-COUNT PREDICTION, and read the correction S2 session 5 made before you use it.** `--check` reads **7 due** as you start — `in-hall-power`, `clean-firm-and-nuclear`, `compute-and-the-rack`, `epc-and-construction`, `aidc-developers-and-landlords`, `hyperscalers-and-ai-labs`, `neoclouds`. **None of them is yours; do not regenerate any of them** (§10.9 gives each to its own S2 session). Two segments name `how-a-storage-project-happens` in the generator's `READ_NEXT`: **`epc-and-construction`, which is already in that seven**, and **`storage-developers-and-ipps`, which is not**. So registering the lesson should take it to **8 due**, adding exactly one. That is the row-8 bound — |`READ_NEXT` membership| + prior residue — with the overlap netted out, and v05.83r is the reason the netting is stated explicitly: it predicted 8 → 9 → 8 from sessions 1–4's arithmetic and measured **8 → 8 → 7**, because a segment already in the due set cannot be added to it twice. **Count the membership against the standing list, not against the previous session's delta.**

**The track edit is a mid-array insert, so expect P5.** `market-access` currently holds `["who-buys-storage", "how-a-utility-buys"]`. Row 10 goes **between them**, at position 2. Bump the track's `updated` and add a clause to its `short` only if the outcome sentence genuinely changes. A reorder is a developer-session write and the pipeline checker reports it under P5 — expected, not a defect.

**The baselines to capture BEFORE any edit, all measured at the close of v05.83r.** `check-classroom-content.py` is **0 errors / 0 warnings at 39 lessons, 8 tracks, 142 gate cases** — held unbroken through six consecutive sessions. **Its guidance-module assertion now reads 14 and row 10 must NOT touch it**: that number rises once per S2 session and a Phase 4 row registers no module, so if you find yourself editing `scripts/check-classroom-content.py` you have taken a wrong turn. `check-classroom-curriculum.py --strict`: no structural findings, **28 stale pins** on the hand-authored lessons (pre-existing, not a Phase 4 concern), and **1 item due for review** — `landscape-utilities-2026-09` at 2026-10-01, by design, and reading **"⚠ review due"** rather than "due soon" from 2 October. That is the quarterly guidance Routine's, not yours. **A second will join it on 28 October**, when `landscape-power-conversion-and-rack-power-silicon-2026-09` (`reviewBy` 2026-11-27) enters the 30-day horizon; both still exit 0, because review dates never call `strict()`. `check-classroom-pipeline.py --selftest`: **13 fixtures / 0 failures**.

**Versions and counters to bump from.** Classroom GAS **v01.40g** (both `Classroom.gs` `VERSION` and `live-site-pages/gs-versions/Classroomgs.version.txt`); page **v01.14w**, and no page bump is expected. Repo **v05.83r**. Scraper **v02.06g** and Profiler **v01.39g** — **neither is a Phase 4 row's**, and unlike an S2 session you have no seed question, so `Scraper.gs` is not yours at all.

**THE DEPLOY — the §7.19 order, on its eighth use.** Read **Pages** first (`https://lightaisolutions.github.io/Sales/gs-versions/Classroomgs.version.txt` is a static file, free to read as often as you like) and the workflow's own `Classroom deploy confirmed (GET)` line from the job log. **Probe `?op=deploy` only if those two disagree** — the probe *deploys* whenever the running `/exec` bundle has not caught up with GitHub, because `pullAndDeployFromGitHub()` compares the pulled `VERSION` against the `VERSION` of the code currently executing. Use `curl -sL` if you do probe; without `-L` the body is empty and reads as a failure. **Deployment counters: Classroom 51/200 and Scraper 150/200, MEASURED** — v05.83r's workflow reported `Classroom deploy confirmed (GET): Updated to v01.40g (deployment 51) | 51/200` on the **first GET leg with no POLL line**, and no `?op=deploy` probe was issued at all, the **eighth consecutive** clean use of the §7.19 order for Classroom. **But the Scraper leg is the news, and it is a first: the v05.59r poll FIRED, and it was needed.** Seven briefs have recorded that poll as armed and never used; here the workflow reported `Scraper: neither leg confirmed v02.06g — re-reading in 5s (poll 1/5)`, then `… in 15s (poll 2/5)`, then `Scraper deploy confirmed (POLL 2): Already up to date (v02.06g)`. **The poll works, and without it that leg would have read as a failure on a deploy that had in fact succeeded.** Two consequences worth carrying. First, **`Already up to date` is not `Updated to` — it consumed no deployment**, so Scraper stays at **150/200** and this brief's own forecast of 151/200 was wrong in the safe direction: forecast the counter from whether the running bundle is actually behind, not from whether the file changed. Second, the poll resolves on a delay of seconds, so a session reading Pages and the job log in the §7.19 order sees the confirmed line and still never needs the probe. *(Refreshed at v05.84r once the workflow reported — this paragraph and the prompt block below were written as a forecast in the v05.83r commit and were corrected together, per the trap §7.25 recorded.)*

**CHANGELOGS — and one trigger has just fired, which changes the arithmetic you inherit.** Keep using `TZ=America/New_York date '+%Y-%m-%d'`; the EST/UTC clocks converged again at v05.83r (both 2026-09-15) and that is a convergence, not a fix. The rule counts **non-exempt** sections (total minus today's EST) and fires when that **reaches** the cap. The repo CHANGELOG stands at **86 raw / 80 non-exempt**, six sections dated 2026-09-15, oldest remaining group thirteen dated 2026-09-07 — **well under 100 either way, so this session will not rotate**; re-count anyway. `Classroomgs.changelog.md` is **40 raw / 39 non-exempt** against 50 — no rotation, and about ten sessions of headroom. **`Scrapergs.changelog.md` has just rotated and is no longer armed**: it stood at 52 non-exempt against a cap of 50 once the EST roll stripped its exemptions, twenty-six sections dated 2026-08-28 moved to the archive with all 26 SHAs resolved, and it now reads **27 raw / 26 non-exempt**. Three consecutive briefs described it as "one short"; the figure was one short only on the pre-roll count, and **an armed changelog arms further overnight without anybody touching it** — re-count from the file rather than from a brief. `Profilerhtml.changelog.md` sits at 49/50 and Profiler is never yours.

**Four open items recorded and not taken.** (i) The `.cl-tbl` `min-width` question now has **eleven** clean data points — v05.83r's twelve-row × three-column bets table and its sixty-row ledger both rendered clean at 1400px, adding a row-count extreme to row 9's six-column one. Column count and row count are both excluded about as firmly as evidence can exclude them; the v05.57r defect stands as a first column holding one long unbreakable word. **Close it** the next time a session has reason to edit `Classroom.html`. (ii) The generated segment lesson's `read-next` **names** the landscape module without linking to it: `clFmt` resolves `**bold**`, `*italic*` and `{{term}}` and **has no link token at all**, so this is a `Classroom.html` renderer feature, and the string is emitted in every segment, so a generator edit would make all nineteen due while §10.9 lets a session regenerate only its own. It belongs with (i), in a session that opens the page, plus a developer `--all` run. (iii) **`the-800-vdc-shift`'s `three-chains` intro is four inserts stale and is still NOT waiting for a Phase 4 row** — §7.25 reassigned it out of the queue entirely on the reading that no remaining row acquires a claim, and row 10 does not either: it inserts into `market-access` while that lesson sits in `aidc-grid-to-chip`. It belongs to whichever session opens that lesson for its own reason, or to a developer sweep. Do not re-offer it. (iv) The render harness needs `favicon.ico` copied into the scratch site root alongside `profiler-data/`, `sounds/` and `images/`, and `sess.permissions` must be an **array** in the shim — `clRoleOf_` calls `.indexOf` on it.

> Author Phase 4 row 10 — the `how-a-storage-project-happens` lesson — on Opus 5 xhigh as a fresh session. This is ONE lesson plus ONE track insertion. It authors no guidance module, no dossier and no report, creates no track, and regenerates no segment.
>
> READ FIRST: repository-information/SESSION-CONTEXT.md (Latest Session); INTEGRATED-REMEDIATION-PLAN.md §7.26 (your brief), §7.25 (S2 session 5's brief, for the due-count correction and the brief-refresh rule), §7.3 order 6 and §6; CLASSROOM-CURRICULUM-PLAN.md §3.5's `how-a-storage-project-happens` spec VERBATIM (its section ids and their order are the contract), §4's `market-access` position table, §7 row 10, and §8 items 2, 4 and 9. Then repository-information/CLASSROOM-SCHEMA.md and .claude/rules/classroom-app.md IN FULL, and .claude/rules/changelog-security.md before you write any public changelog line.
>
> **UNSHALLOW THE CLONE FIRST** — `git fetch --unshallow origin main`, before any measurement and before any pin. As of v05.83r the Session Start Checklist's Always Run section carries a "Deepen the clone" bullet requiring exactly this, so run it as a checklist item rather than as a favour to this brief. A fresh container clones shallow and a shallow clone's boundary commit looks like it introduced every file, so `git log -1 --format=%cs -- <path>` returns the boundary's date rather than the file's — and §8 item 2 and G2 make that command the pin source. If `--check` reports a number wildly above the one in this brief, suspect the clone before the corpus.
>
> THE WORK — one push commit. (1) Author `clLessonHowAStorageProjectHappens_()` **inside** the `// CONTENT START` … `// CONTENT END` fence, at §3.5's seven section ids in its order — `the-lifecycle` (timeline) · `who-does-what-when` (table) · `where-the-seller-enters` (prose) · `how-overruns-hide` (callout) · `where-it-fails` (callout) · `drill` · `check-yourself` — title *From Site Control to Commercial Operation*, group Market Access & Bankability, and register it in `clLessons_()`. (2) **Re-derive the input roster rather than copying it**: §3.5's base list is eight and the v05.07r amendment adds eight, so the roster is **sixteen** and §7 row 10 lists exactly that; row 9's brief was one short on the same arithmetic. All sixteen are public, so the stamp folds to `tracks` — pin each off the document fetched that run and resolve every `study:` ref by matching its own `slug`. (3) Insert at **position 2 of `market-access`**, between `who-buys-storage` and `how-a-utility-buys`, and bump that track's `updated`. (4) Do NOT regenerate any segment — `--check` reads 7 due before you start and all seven belong to their own S2 sessions under §10.9; registering this lesson should take it to **8 due** (`storage-developers-and-ipps` joins; `epc-and-construction` is already in the seven), and that residue is expected, not damage. (5) Flip the §7.3 order-6 row, the §6 Phase 4 row and curriculum plan §4's position table and §7 row 10, all dated. (6) Write §7.27 — the next run's brief, which by the interleave is S2 session 6, `aidc-developers-and-landlords` — in the same commit, and **edit BOTH halves of any §7.x brief you refresh**: the prose paragraphs and the quoted prompt block.
>
> DO NOT: author or revise any guidance module. **Do not touch `scripts/check-classroom-content.py`** — its guidance-module assertion reads 14 and rises only on an S2 session; a Phase 4 row that edits it has taken a wrong turn. Do not regenerate any segment lesson or hand-edit a generated `segment-*` literal (§10.4). Do not touch `Profiler.gs`, `Profiler.html` or `Scraper.gs` — there is no seed question on a Phase 4 row. Do not refresh `gateDigest` — a lesson inside the fence moves no `GATE_SYMBOLS` member, so P3 must be silent, and refreshing would hide a real signal. Do not fix `landscape-utilities-2026-09`'s review date, which is due by design and belongs to the quarterly Routine; from 28 October a second module joins it in the 30-day horizon and that is equally not yours.
>
> VERIFY: `node --check` on a `.js` copy of `Classroom.gs`; `scripts/check-gas-inner-scripts.js`; `scripts/check-classroom-content.py` — **capture a pristine HEAD baseline BEFORE any edit. IT IS 0 ERRORS / 0 WARNINGS at 39 lessons / 8 tracks / 142 gate cases** and must be 0/0 at **40 lessons** after. Then `scripts/check-classroom-curriculum.py --strict` (no structural findings; 28 stale pins and 1 item due for review are both pre-existing and not yours); `scripts/check-classroom-pipeline.py --selftest` (13 fixtures, 0 failures) and `--base origin/main` (expect P1 on the developer files and **P5 on the mid-array track insert**; expect **no P3**); `build-classroom-segments.py --check` — expect **7 due → 8 due**, and regenerate nothing. `scripts/check-readme-tree.py` after the version bump. Playwright render of the new lesson at **analyst** (the stamp is all-public, so analyst is the POSITIVE test here, not the negative one) with ZERO page errors, ops answered by the REAL serving functions — the lesson entrypoint is `clShowLesson(id)`, `_gasPost` returns a PROMISE, `sess.permissions` must be an ARRAY in the shim, the session token must be at least 32 characters or the real guard returns SESSION_EXPIRED, the session keys are `Classroom_gas_session_token` / `_gas_user_email` / `_gas_user_role` / `_gas_user_permissions`, overlays to hide are `#auth-wall` / `#signing-out-wall` / `#auth-timers` / `#gas-layer-toggle`, and the scratch site root needs `favicon.ico` copied in beside `profiler-data/`, `sounds/` and `images/`. Then READ EVERY SECTION'S SCREENSHOT. Bump Classroom's GAS version in BOTH `Classroom.gs` `VERSION` (from `v01.40g`) and `live-site-pages/gs-versions/Classroomgs.version.txt`, with a GENERIC entry in `Classroomgs.changelog.md`.
>
> THE DEPLOY and THE CHANGELOG ARITHMETIC: exactly as §7.26's paragraphs set out. **Confirm the deploy from Pages and the job log, NOT by probing `?op=deploy` first.** Deployment counters **Classroom 51/200 and Scraper 150/200, MEASURED** — v05.83r's workflow reported `Classroom deploy confirmed (GET): Updated to v01.40g (deployment 51) | 51/200` on the first GET leg with no POLL line and no probe issued, the eighth consecutive clean use of the §7.19 order. **The v05.59r poll fired for the FIRST time on the Scraper leg and was needed** — two re-reads, resolving at `Scraper deploy confirmed (POLL 2): Already up to date (v02.06g)`. `Already up to date` consumed **no deployment**, so Scraper stays at 150/200 and the 151/200 forecast was wrong: forecast from whether the running bundle is behind, not from whether the file changed. Repo CHANGELOG **86 raw / 80 non-exempt**, so **you will not rotate** — but re-count it yourself on the EST clock (`TZ=America/New_York date`). `Classroomgs.changelog.md` is 40 raw / 39 non-exempt against 50. **`Scrapergs.changelog.md` has just rotated to 27 raw / 26 non-exempt and is no longer armed** — and the lesson of that rotation is that an armed changelog arms further overnight when EST rolls past its newest section, so re-count from the file rather than from any brief.
>
> Normal Pre-Commit and Pre-Push checklists; `claude/*` branch restarted from `origin/main` first, `git ls-remote` before each push — the auto-merge workflow adds a `[skip ci]` SHA-tracker commit after every merge, and after it merges use `git fetch --prune origin` rather than `git fetch origin main`, or the stale remote-tracking ref makes the stop hook report that SHA-tracker commit as unpushed.
>
> AFTER THIS SESSION Phase 4 is 10 of 26 and S2 is 5 of ~19, so the interleave returns to S2 and the recommended next run is **S2 session 6 — `aidc-developers-and-landlords`** — for which §7.27 should be written.

### 7.27 · Paste-in brief for S2 session 6 — `aidc-developers-and-landlords` (Opus 5 xhigh) — written 2026-09-15 at v05.85r, at the close of Phase 4 row 10

**Why this and not another Phase 4 row.** Phase 4 is **10 of 26** and S2 is **5 of ~19**, so the interleave stands at 10:5 and §7.4 item 6 hands the turn back to S2. By §7.3's own sales-value order the next segment is **`aidc-developers-and-landlords`** — registry position 12, and on the day this brief was written it measured **30 members (9 incumbents · 13 challengers · 8 adjacent)**, the third-deepest segment S2 has reached after integrators (32) and storage developers and IPPs (34). Re-measure it on the day from `profiler-segments.json`; four consecutive sessions have confirmed the brief's figure member for member, which is a good record and not a licence to skip the count.

**UNSHALLOW FIRST — a checklist item since v05.83r, not a brief's favour.** `git fetch --unshallow origin main` before any pin read, any `--check` run, any `git log -1 --format=%cs` date and any CHANGELOG rotation. Row 10 ran it as the Session Start Checklist's "Deepen the clone" bullet and `--check` opened at the true 7 with `inputs moved: none` on every line; the `concepts:profiler-concepts` pin resolved to **2026-09-13**, which is the value a shallow clone silently gets wrong.

**The module, as §10.6 specifies it.** `landscape-aidc-developers-and-landlords-2026-09`, the **fifteenth guidance module** and the **sixth landscape**, authored **below the `// CONTENT END` fence** in `guidanceDocs_()`, lane `The Value Chain`, tier contributor, with the analysis markdown at `repository-information/industry-guidance/landscape-aidc-developers-and-landlords-analysis.md` as its source of truth. Nine sections at §10.6's ids in its order: `who-dominates-and-on-what-basis` · `who-threatens` · `each-players-bet` (**twenty-two rows** — nine incumbents plus thirteen challengers, and session 3 settled that the table scales to twenty-seven without a cap, so trim nothing) · `the-indicators` · `the-sellers-play` · `claims-ledger` · `what-the-record-does-not-say` · `drill` · `check-yourself`. **The content checker's guidance-module assertion moves 14 → 15 in the same commit** — that is the one edit outside the fence a landscape session makes to `scripts/check-classroom-content.py`, and it is mandatory (§10.6 (c)).

**THE SPLIT, and this segment has the most crowded neighbourhood yet — including a kind of overlap no landscape has faced.** Three registered modules bear on it: **`large-load-interconnection-2026-09`** owns the federal rulebook above the fence, which is the developer's own gate; **`utility-aidc-procurement-2026-08`** owns the procurement process on the other side of that fence; **`power-infra-aidc-2026-08`** owns the chain itself. Apply §7.23's test to each — *the one number both cite must do a different job in each* — and write the split **before** the module, per §10.6 (j). **But the new problem is the fourth neighbour and it is not a guidance module at all: `the-campus-as-a-power-project` (Phase 4 row 9, v05.80r) is a public mechanism lesson built on nine of this segment's own members**, and row 10's `how-a-storage-project-happens` touches two more. That lesson owns the **lifecycle and how to read an announcement**; this module owns **who stands where and what each is betting**. No previous landscape has had to split against a *lesson*, and the split matters more than usual because the lesson is analyst-visible while the module is contributor-only — material that drifts from the module into the lesson's territory is material an analyst can already read, and material that drifts the other way is a gate raised on something already public. **Enumerate the omissions in the function's header comment and in §2 of the analysis file**, as session 5 did for its six.

**`reviewBy` is read, not sorted — for the sixth consecutive S2 session, and every one of the five has found the fence unreliable in a different way.** Session 2: the gate sat inside a `policyExposure[]` entry's *prose* while its `effectiveDate` held an earlier step. Session 3: there was nothing to sort at all — 96 dated entries, none in the future. Session 4: the read returned a date **nearer** than the sort, shipping the module inside its own 30-day horizon. Session 5: exactly one of 17 dated entries was in the future, and the real gate came out of a member's policy prose. **Read the prose of all 30 members' `policyExposure[]` before you trust any sort, and reject your candidates in writing.** One rejection class is now established and worth reusing: a gate that is **already another module's own `reviewBy`** is that module's clock, not yours — it belongs in `the-indicators`.

**The Scraper seed question — and state your prior as a prior.** §10.6 (i) requires the check to run against **every term in both arrays**, not every seed label, and §10.6 (e)/(i) make the answer conditional both ways. Session 5's brief said no and the correct answer was yes, found only by running it properly across all 390 distinct terms. `topic-datacenter-buildout`-shaped coverage probably exists; **powered land, entitlement, the two utility instruments, the co-location statutes and the converted-miner route may not.** Run it, re-scope any draft against the existing terms before landing it (session 3 had to drop duplicates; session 5 dropped two), and if the answer is no, say so in the analysis file with the terms you tested.

**THE DUE-COUNT ARITHMETIC — and this is the second run of the rule session 5 discovered, so it should hold.** `--check` reads **8 due** as you start: `in-hall-power`, `clean-firm-and-nuclear`, `compute-and-the-rack`, `epc-and-construction`, `storage-developers-and-ipps`, `aidc-developers-and-landlords`, `hyperscalers-and-ai-labs`, `neoclouds`. **Your own segment is already the sixth of those**, so registering the module changes its `read-next` without changing its membership and the count does **not** rise: the arithmetic is **8 → 8 → 7**, exactly session 5's shape, not sessions 1–4's. Regenerate **only** `--segment aidc-developers-and-landlords`; the other seven belong to their own sessions under §10.9. **One refinement row 10 added and you should expect to see: an already-due segment can also *deepen*.** `epc-and-construction` went from differing in `read-next` alone to differing in `read-next` **and** `what-is-bought-and-on-what` when row 10 registered, because row 8's rule — the buying-criteria lexicon is subordinate to `READ_NEXT` — applies to segments already in the set. Predict the membership delta *and* the per-segment section delta.

**The baselines to capture BEFORE any edit, all measured at the close of v05.85r.** `check-classroom-content.py` is **0 errors / 0 warnings at 40 lessons, 8 tracks, 142 gate cases** — held unbroken through seven consecutive sessions, and your commit must return it to 0/0 at 40 lessons with the module assertion at **15**. `check-classroom-curriculum.py --strict`: no structural findings, **28 stale pins** on the hand-authored lessons (pre-existing; row 10's sixteen pins are all fresh and added none), and **1 item due for review** — `landscape-utilities-2026-09` at 2026-10-01, by design and the quarterly Routine's. **A second joins it on 28 October**, when `landscape-power-conversion-and-rack-power-silicon-2026-09` (`reviewBy` 2026-11-27) enters the 30-day horizon; both still exit 0, because review dates never call `strict()`. `check-classroom-pipeline.py --selftest`: **13 fixtures / 0 failures**. Expect **P1** on the developer files and **P2** on the guidance literal (a diff outside the content fence is what P2 reports on a developer commit), one **P7** if you regenerate the segment the same day, and **never P3** — a P3 mismatch means the gate surface genuinely moved and the digest needs refreshing.

**Versions and counters to bump from.** Classroom GAS **v01.41g** (both `Classroom.gs` `VERSION` and `live-site-pages/gs-versions/Classroomgs.version.txt`); page **v01.14w**, and no page bump is expected. Repo **v05.85r**. Scraper **v02.06g** — yours only if the seed question answers yes. Profiler **v01.39g** and never yours.

**THE DEPLOY — the §7.19 order, on its ninth use.** Read **Pages** first (`https://lightaisolutions.github.io/Sales/gs-versions/Classroomgs.version.txt` is a static file, free to read as often as you like) and the workflow's own `Classroom deploy confirmed (GET)` line from the job log. **Probe `?op=deploy` only if those two disagree** — the probe *deploys* whenever the running `/exec` bundle is behind GitHub, because `pullAndDeployFromGitHub()` compares the pulled `VERSION` against the `VERSION` currently executing. Use `curl -sL` if you do probe; without `-L` the body is empty and reads as a failure. **Deployment counters: Classroom 52/200 and Scraper 150/200, MEASURED.** v05.85r's workflow reported `Classroom deploy confirmed (GET): Updated to v01.41g (deployment 52) | 52/200` on the **first GET leg with no POLL line**, and **no `?op=deploy` probe was issued at all** — the **ninth consecutive** clean use of the §7.19 order for Classroom. **The Scraper leg is the news, and it is a third mechanism the counter rule had not yet seen.** §7.26 taught that `Already up to date` consumes no deployment where `Updated to` consumes one; this run produced neither, because **the Scraper deploy step never fired**. Each project's step opens with `git diff --name-only "$PRE" HEAD | grep -q "$GS" || exit 0`, and `Scraper.gs` was not in the merge diff, so the step exited 0 in under a second without contacting the deployment at all. So the counter rule now has three cases, and only the first spends anything: **`Updated to` consumes one; `Already up to date` consumes none because the leg ran and found the bundle current; and a step that never fires consumes none because the file was not in the diff.** Forecasting from "whether the running bundle is behind" gets the number right in all three, but it describes only the middle case — if you want the log line you are going to read, ask first whether the file is in the diff at all.

**CHANGELOGS — re-count from the files, never from this brief.** Keep using `TZ=America/New_York date '+%Y-%m-%d'`; the EST and UTC clocks converged again at v05.85r (both 2026-09-15) and that is a convergence, not a fix. The rule counts **non-exempt** sections (total minus today's EST) and fires when that **reaches** the cap. At the close of v05.85r the repo CHANGELOG stood at 88 raw / 80 non-exempt against 100 — **and by the time this brief was executed it read 89 raw / 80 non-exempt**, because the v05.87r handover commit added a section of its own after the v05.86r correction. No rotation either way. *(Corrected at v05.86r: this paragraph was written during the v05.85r push and the v05.86r handover commit then added a section of its own. The "edit BOTH halves" rule has a second edge — a handover commit that moves a number this brief quotes must refresh that number too, not only the counters it was opened to fix.)* `Classroomgs.changelog.md` is **41 raw / 38 non-exempt** against 50, and **41 after the roll** — about eight sessions of headroom, and the closest of the three to arming. `Scrapergs.changelog.md` rotated at v05.83r and sits at **27 raw / 26 non-exempt**, no longer armed. **The standing lesson from that rotation is that an armed changelog arms further overnight when EST rolls past its newest section**, which is why three consecutive briefs described it as "one short" while it was already two over.

**Four open items recorded and not taken.** (i) The `.cl-tbl` `min-width` question now has **twelve** clean data points — row 10's 4 × 8 table added the first case of a `{{term}}` span in the first column, which wraps at word boundaries like any other cell, and v05.83r's sixty-row ledger and twelve-row bets table covered the row-count extreme. Column count, row count and tooltip spans are all excluded; the v05.57r defect stands as a first column holding one long unbreakable word. **Close it** the next time a session has reason to edit `Classroom.html`. (ii) The generated segment lesson's `read-next` **names** the landscape module without linking to it: `clFmt` has no link token at all, so this is a `Classroom.html` renderer feature, and the string is emitted in every segment, so a generator edit would make all nineteen due while §10.9 lets a session regenerate only its own. It belongs with (i), plus a developer `--all` run. (iii) **`the-800-vdc-shift`'s `three-chains` intro is five inserts stale and is NOT waiting for a Phase 4 row** — §7.25 reassigned it out of the queue on the reading that no remaining row acquires a claim, and row 10 did not either (it inserted into `market-access` while that lesson sits in `aidc-grid-to-chip`). It belongs to whichever session opens that lesson for its own reason. Do not re-offer it. (iv) **§10.6's session-4 bullet block still sits out of chronological order** — session 4's appears before session 3's, because session 5 appended its own at the end rather than silently reordering another session's text. A future session can fix it deliberately; it is cosmetic.

> Author S2 session 6 — the `aidc-developers-and-landlords` landscape module — on Opus 5 xhigh as a fresh session. This is ONE guidance module plus ONE segment regeneration. It authors no lesson, no dossier and no report, creates no track, and inserts into no track.
>
> READ FIRST: repository-information/SESSION-CONTEXT.md (Latest Session); INTEGRATED-REMEDIATION-PLAN.md §7.27 (your brief), §7.25 and §7.26 (for the due-count netting rule and the brief-refresh rule), §7.3 order 5 and §6; CLASSROOM-CURRICULUM-PLAN.md §10.6 VERBATIM (its nine section ids and their order are the contract), §10.2's floor table, §10.9's ownership rule and §10.10's two seller paths. Then repository-information/CLASSROOM-SCHEMA.md and .claude/rules/classroom-app.md IN FULL, .claude/rules/industry-guidance.md IN FULL, .claude/rules/scraper-sources.md before any seed edit, and .claude/rules/changelog-security.md before you write any public changelog line.
>
> **UNSHALLOW THE CLONE FIRST** — `git fetch --unshallow origin main`, before any measurement and before any pin. This is the Session Start Checklist's "Deepen the clone" bullet, not a favour to this brief. A shallow clone's boundary commit looks like it introduced every file, so `git log -1 --format=%cs -- <path>` returns the boundary's date rather than the file's — and §8 item 2 and G2 make that command the pin source. If `--check` reports a number wildly above the 8 below, suspect the clone before the corpus.
>
> THE WORK — one push commit. (1) Author `guidanceDocLandscapeAidcDevelopersAndLandlords_()` **below** the `// CONTENT END` fence, at §10.6's nine section ids in its order — `who-dominates-and-on-what-basis` · `who-threatens` · `each-players-bet` (**twenty-two rows**, nine incumbents plus thirteen challengers, trim nothing) · `the-indicators` · `the-sellers-play` · `claims-ledger` · `what-the-record-does-not-say` · `drill` · `check-yourself` — id `landscape-aidc-developers-and-landlords-2026-09`, lane `The Value Chain`, tier contributor, and register it in `guidanceDocs_()`. (2) Write the analysis markdown at `repository-information/industry-guidance/landscape-aidc-developers-and-landlords-analysis.md` as its source of truth, and **write §2 — the split — before any other section**. (3) **Bump the guidance-module assertion in `scripts/check-classroom-content.py` from 14 to 15** — the one edit outside the fence a landscape session makes, and mandatory per §10.6 (c). (4) Re-measure the segment on the day (the registry says **30 members: 9 · 13 · 8**) and pin every claim in the ledger to `profile:<slug>` at its `profileVersion` plus the dossier field it rests on. (5) Regenerate **only** `--segment aidc-developers-and-landlords`. (6) Flip the §7.3 order-5 row, the §6 S2 row, and curriculum plan §10.6's "Built so far" line and its session-6 bullet block, all dated. (7) Write §7.28 — the next run's brief, which by the interleave is Phase 4 row 11, `contracts-and-revenue` — in the same commit, and **edit BOTH halves of any §7.x brief you refresh**: the prose paragraphs and the quoted prompt block.
>
> THE SPLIT IS THE HARD PART AND IT HAS FOUR NEIGHBOURS. Three registered modules bear on this segment — `large-load-interconnection-2026-09` (the federal rulebook above the fence), `utility-aidc-procurement-2026-08` (the procurement process) and `power-infra-aidc-2026-08` (the chain) — so apply §7.23's test to each: the one number both modules cite must do a **different job** in each. **The fourth neighbour is not a module: `the-campus-as-a-power-project` is a public mechanism lesson built on nine of this segment's own members, and `how-a-storage-project-happens` touches two more.** No landscape has split against a lesson before. The line: **the lesson owns the lifecycle and how to read an announcement; this module owns who stands where and what each is betting.** It matters more than usual because the lesson is analyst-visible and the module is contributor-only. **Enumerate every omission in the function's header comment and in §2 of the analysis file** so a later revision cannot quietly import them.
>
> DO NOT: author or revise any lesson, and do not hand-edit a generated `segment-*` literal beyond the one `--segment` regeneration (§10.4, §10.9). Do not regenerate any segment but your own — the other seven in the 8-due list belong to their own S2 sessions. Do not touch `Profiler.gs` or `Profiler.html`. Do not refresh `gateDigest` — a module below the fence moves no `GATE_SYMBOLS` member, so P3 must be silent, and refreshing would hide a real signal. Do not fix `landscape-utilities-2026-09`'s review date, which is due by design and belongs to the quarterly Routine; from 28 October a second module joins it in the 30-day horizon and that is equally not yours.
>
> VERIFY: `node --check` on a `.js` copy of `Classroom.gs`; `scripts/check-gas-inner-scripts.js`; `scripts/check-classroom-content.py` — **capture a pristine HEAD baseline BEFORE any edit. IT IS 0 ERRORS / 0 WARNINGS at 40 lessons / 8 tracks / 142 gate cases** and must be 0/0 at 40 lessons after, with the module assertion at 15. Then `scripts/check-classroom-curriculum.py --strict` (no structural findings; 28 stale pins and 1 item due for review are both pre-existing and not yours); `scripts/check-classroom-pipeline.py --selftest` (13 fixtures, 0 failures) and `--base origin/main` (expect P1 on the developer files, P2 on the guidance literal, one P7 if you regenerate the same day; expect **no P3**); `build-classroom-segments.py --check` — expect **8 due → 8 due → 7 due**, because your own segment is already the sixth of the eight and a segment already in the due set cannot be added to it twice. Watch for an already-due segment *deepening* as well as a new one joining. `scripts/check-readme-tree.py` after the version bump. Playwright render of the new module at **contributor** (the module is contributor-only, so contributor is the POSITIVE test and analyst is the NEGATIVE one) with ZERO page errors, ops answered by the REAL serving functions — the guidance entrypoint is `clShowGuidanceDoc(id)`, `_gasPost` returns a PROMISE, `sess.permissions` must be an ARRAY in the shim, the session token must be at least 32 characters or the real guard returns SESSION_EXPIRED, the session keys are `Classroom_gas_session_token` / `_gas_user_email` / `_gas_user_role` / `_gas_user_permissions`, overlays to hide are `#auth-wall` / `#signing-out-wall` / `#auth-timers` / `#gas-layer-toggle`, and the scratch site root needs `favicon.ico` copied in beside `profiler-data/`, `sounds/` and `images/`. Then READ EVERY SECTION'S SCREENSHOT. Bump Classroom's GAS version in BOTH `Classroom.gs` `VERSION` (from `v01.41g`) and `live-site-pages/gs-versions/Classroomgs.version.txt`, with a GENERIC entry in `Classroomgs.changelog.md`.
>
> THE DEPLOY and THE CHANGELOG ARITHMETIC: exactly as §7.27's paragraphs set out. **Confirm the deploy from Pages and the job log, NOT by probing `?op=deploy` first.** Deployment counters **Classroom 52/200 and Scraper 150/200, MEASURED** — v05.85r's workflow reported `Classroom deploy confirmed (GET): Updated to v01.41g (deployment 52) | 52/200` on the first GET leg with no POLL line and no probe issued, the ninth consecutive clean use of the §7.19 order. **The Scraper leg produced no line at all, and that is a third case of the counter rule**: its step opens with `git diff --name-only "$PRE" HEAD | grep -q "$GS" || exit 0` and `Scraper.gs` was not in the merge diff, so the step exited 0 without contacting the deployment. **`Updated to` consumes one; `Already up to date` consumes none; a step that never fires consumes none and logs nothing.** Forecast the number from whether the running bundle is behind, but forecast the log line from whether the file is in the diff. Repo CHANGELOG **89 raw / 80 non-exempt** (89 once EST rolls past 2026-09-15), so **you will not rotate** — but re-count it yourself on the EST clock (`TZ=America/New_York date`). `Classroomgs.changelog.md` is **41 raw / 38 non-exempt** against 50 and is the closest of the three to arming. `Scrapergs.changelog.md` sits at 27 raw / 26 non-exempt and is no longer armed — and the lesson of its rotation is that an armed changelog arms further overnight when EST rolls past its newest section, so re-count from the file rather than from any brief.
>
> Normal Pre-Commit and Pre-Push checklists; `claude/*` branch restarted from `origin/main` first, `git ls-remote` before each push — the auto-merge workflow adds a `[skip ci]` SHA-tracker commit after every merge, and after it merges use `git fetch --prune origin` rather than `git fetch origin main`, or the stale remote-tracking ref makes the stop hook report that SHA-tracker commit as unpushed.
>
> AFTER THIS SESSION Phase 4 is 10 of 26 and S2 is 6 of ~19, so the interleave returns to Phase 4 and the recommended next run is **Phase 4 row 11 — `contracts-and-revenue`** — for which §7.28 should be written.

### 7.28 · Paste-in brief for Phase 4 row 11 — `contracts-and-revenue` (Opus 5 xhigh) — written 2026-09-15 at v05.88r, at the close of S2 session 6

**Why this and not another S2 segment.** Phase 4 is **10 of 26** and S2 is now **6 of ~19**, so the interleave stands at 10:6 and §7.4 item 6 hands the turn back to Phase 4. Its lowest unbuilt row is **row 11, `contracts-and-revenue`** — *Who Carries Which Risk* — specified in curriculum plan §3.5 with a v05.07r amendment, and third in `market-access`'s stated order.

**UNSHALLOW FIRST — a checklist item since v05.83r, not a brief's favour.** `git fetch --unshallow origin main` before any pin read, any `--check` run, any `git log -1 --format=%cs` date and any CHANGELOG rotation. S2 session 6 ran it as the Session Start Checklist's "Deepen the clone" bullet and `--check` opened at the true 8 with `inputs moved: none` on every line.

**The lesson, as §3.5 specifies it.** `contracts-and-revenue`, title *Who Carries Which Risk* — **byte-identical to `PLANNED_TITLES["contracts-and-revenue"]` in the generator, so check it before you rename anything** — group `Market Access & Bankability`, gate `tracks`, authored **inside** the `// CONTENT START` … `// CONTENT END` fence and registered in `clLessons_()`. Seven sections at §3.5's ids in its order: `the-instruments` (table — instrument · who pays for what · who carries price risk · who carries volume risk · who finances on it) · `capacity-markets` (prose) · `take-or-pay` (prose) · `clean-firm-and-the-restart` (callout) · `where-it-fails` (callout) · `drill` · `check-yourself`.

**RE-DERIVE THE INPUT ROSTER RATHER THAN COPYING IT, and the arithmetic has a trap the previous two rows did not.** §3.5's base list is **seven** — `study:constellation-energy`, `study:coreweave`, `study:core-scientific`, `study:amazon`, `study:google`, `profile:plus-power`, `concepts:profiler-concepts` — and its **v05.07r amendment lists eight**, but **one of those eight (`study:core-scientific`) is already in the base list**, so the union is **fourteen**, not fifteen. Row 9's brief was one short by *under*-counting an amendment and row 10's was right by adding cleanly; this row is the first where the amendment **overlaps** the base and a straight addition over-counts. Note also that `plus-power` appears twice at **two different layers** — `profile:plus-power` in the base and `study:plus-power` in the amendment ("tolling — now a guide, not only a dossier") — and those are two distinct refs that both belong, not a duplicate. Count fourteen, and say so in the §8 record.

**All fourteen are public, so the stamp folds to `tracks` and the lesson is analyst-visible.** Pin each off the document fetched that run (G2) and resolve every `study:` / `profile:` ref by opening the file and matching its own `slug` (G7).

**Insert at position 3 of `market-access`.** The track's `lessons[]` today is `who-buys-storage` → `how-a-storage-project-happens` → `how-a-utility-buys`, and §3.5's order puts `contracts-and-revenue` third — **between `how-a-storage-project-happens` and `how-a-utility-buys`**. That is a mid-track insertion, so expect **P1 and P5, no P3, no P8** — the signature rows 3, 4, 6, 7, 8, 9 and 10 set. Bump that track's `updated`. **Leave the track's `short` alone**, and this time the reasoning is already written: row 10 established that a `short` which states the *finished-track outcome* is not extended by an insertion, and `market-access`'s is §3.5's verbatim finished-track outcome. Do not re-litigate it.

**THE DUE-COUNT ARITHMETIC — and this row will produce the largest single jump any Phase 4 row has.** `--check` reads **7 due** as you start: `in-hall-power`, `clean-firm-and-nuclear`, `compute-and-the-rack`, `epc-and-construction`, `storage-developers-and-ipps`, `hyperscalers-and-ai-labs`, `neoclouds`. `contracts-and-revenue` is named in **four** segments' `READ_NEXT` — `storage-developers-and-ipps`, `capital`, `software-and-optimization`, `insurance-and-risk-transfer` — and **one of those four is already in the seven**, so the netting rule of §10.6 (p) applies and the count goes **7 → 10**, not 7 → 11. **And predict the per-segment section delta as well as the membership delta**, which §10.6 (w) now requires: the buying-criteria lexicon for this lesson is `ppa · tolling · offtake · contract · revenue · merchant · capacity payment`, and against the registry it hits **three** of `storage-developers-and-ipps`'s criteria, **one** of `capital`'s ("Contracted cash flow") and **one** of `insurance-and-risk-transfer`'s ("revenue put") — but **none** of `software-and-optimization`'s. So expect `capital` and `insurance-and-risk-transfer` to join differing in **two** sections, `software-and-optimization` to join differing in **one**, and `storage-developers-and-ipps` — already due on `read-next` alone — to **deepen** to two. That deepening is the row-10 case firing; session 6's did not fire, so this is the second real test of it. **Regenerate nothing**: all ten belong to their own S2 sessions under §10.9, and 10 due is row 11's expected residue, not damage.

**The baselines to capture BEFORE any edit, all measured at the close of v05.89r.** `check-classroom-content.py` is **0 errors / 0 warnings at 40 lessons, 8 tracks, 142 gate cases** — held unbroken through eight consecutive sessions, and your commit must return it to 0/0 at **41 lessons**. **Its guidance-module assertion is now 15 and a Phase 4 row must NOT touch it** — that number rises only on an S2 session. `check-classroom-curriculum.py --strict`: no structural findings, **28 stale pins** on the hand-authored lessons (pre-existing; session 6 added none), and **1 item due for review** — `landscape-utilities-2026-09` at 2026-10-01, by design and the quarterly Routine's. **A second joins it on 28 October** when `landscape-power-conversion-and-rack-power-silicon-2026-09` (`reviewBy` 2026-11-27) enters the 30-day horizon; **session 6's own module is at 2026-12-15 and does not enter it until 15 November**. All still exit 0, because review dates never call `strict()`. `check-classroom-pipeline.py --selftest`: **13 fixtures / 0 failures**.

**Versions and counters to bump from.** Classroom GAS **v01.42g** (both `Classroom.gs` `VERSION` and `live-site-pages/gs-versions/Classroomgs.version.txt`); page **v01.14w**, and no page bump is expected. Repo **v05.89r**. Scraper **v02.07g** — **not yours**: a Phase 4 row authors no guidance module, so `industry-guidance.md` step 9 does not fire and `Scraper.gs` is untouched. Profiler **v01.39g** and never yours.

**THE DEPLOY — the §7.19 order, on its tenth use, and this one should log TWO lines.** Read **Pages** first (`https://lightaisolutions.github.io/Sales/gs-versions/Classroomgs.version.txt` is a static file, free to read as often as you like) and the workflow's own `Classroom deploy confirmed (GET)` line from the job log. **Probe `?op=deploy` only if those two disagree** — the probe *deploys* whenever the running `/exec` bundle is behind GitHub. Use `curl -sL` if you do probe; without `-L` the body is empty and reads as a failure. **Deployment counters: Classroom 53/200 and Scraper 152/200, MEASURED.** Both `Classroom.gs` and `Scraper.gs` were in v05.88r's merge diff, so both steps fired and both confirmed on the **first GET leg with no POLL line and no `?op=deploy` probe** — `Scraper deploy confirmed (GET): Updated to v02.07g (deployment 152) | 152/200` and `Classroom deploy confirmed (GET): Updated to v01.42g (deployment 53) | 53/200`. The **tenth consecutive** clean use of the §7.19 order, and the first two-line deploy since v05.83r. **THE SCRAPER COUNTER CAME IN ONE ABOVE FORECAST, AND THAT CORRECTS THE COUNTER RULE'S MIDDLE CASE — see below.** Classroom's was exact. **Forecast the number from whether the bundle is behind; forecast the log line from whether the file is in the diff — but treat `Already up to date` as saying nothing about consumption.** Row 11 touches only `Classroom.gs`, so on *your* push expect **one** deploy line, not two, and Scraper to stay wherever v05.88r left it.

**CHANGELOGS — re-count from the files, never from this brief.** Keep using `TZ=America/New_York date '+%Y-%m-%d'`; the EST and UTC clocks converged again at v05.88r (both 2026-09-15) and that remains a convergence, not a fix — it diverges for any session running between 20:00 EST and midnight UTC. The rule counts **non-exempt** sections (total minus today's EST) and fires when that **reaches** the cap. At the close of v05.89r the repo CHANGELOG stands at **91 raw / 80 non-exempt** against 100 — the eleven same-day sections stop being exempt the moment EST rolls to 2026-09-16, taking it to **91 non-exempt**, still clear, and it is now **nine** from its cap, the closest it has ever been. *(Refreshed at v05.90r: this paragraph was written during the v05.88r push and the v05.89r counter-correction commit then added a section of its own — the same recursion v05.87r recorded, firing for a third time on a third handover. A handover commit that moves a number this brief quotes must refresh that number too.)* `Classroomgs.changelog.md` is **42 raw / 38 non-exempt** against 50, and **42 after the roll** — still the closest of the three to arming, with about eight sessions of headroom. `Scrapergs.changelog.md` sits at **28 raw / 26 non-exempt**. **The standing lesson is that an armed changelog arms further overnight when EST rolls past its newest section** — re-count from the file, never from a brief.

**Four open items recorded and not taken.** (i) The `.cl-tbl` `min-width` question now has **fourteen** clean data points — session 6's twenty-two-row bets table and seventy-five-row ledger both rendered clean at 1400px, adding two more row-count extremes on top of v05.83r's sixty-row ledger. Column count, row count and tooltip spans are all excluded; the v05.57r defect stands as a first column holding one long unbreakable word. **Close it** the next time a session has reason to edit `Classroom.html`. (ii) The generated segment lesson's `read-next` **names** the landscape module without linking to it: `clFmt` has no link token at all, and the string is emitted in every segment, so it belongs with (i) plus a developer `--all` run. (iii) **`the-800-vdc-shift`'s `three-chains` intro is six inserts stale and is NOT waiting for a Phase 4 row** — §7.25 reassigned it out of the queue and rows 10 and S2 session 6 did not acquire a claim either. It belongs to whichever session opens that lesson for its own reason. **Do not re-offer it.** (iv) **§10.6's session-4 bullet block still sits out of chronological order** — session 4's appears before session 3's, and sessions 5 and 6 both appended at the end rather than silently reordering another session's text. A future session can fix it deliberately; it is cosmetic.

> Author Phase 4 row 11 — the `contracts-and-revenue` lesson — on Opus 5 xhigh as a fresh session. This is ONE lesson. It authors no guidance module, no dossier and no report, creates no track, and regenerates no segment.
>
> READ FIRST: repository-information/SESSION-CONTEXT.md (Latest Session); INTEGRATED-REMEDIATION-PLAN.md §7.28 (your brief), §7.26 and §7.27 (for the roster-arithmetic rule and the due-count netting rule), §7.3 order 6 and §6; CLASSROOM-CURRICULUM-PLAN.md §3.5's `contracts-and-revenue` entry VERBATIM **including its v05.07r amendment** (its seven section ids and their order are the contract), §4's position table, §7 row 11 and §10.9's ownership rule. Then repository-information/CLASSROOM-SCHEMA.md and .claude/rules/classroom-app.md IN FULL, and .claude/rules/changelog-security.md before you write any public changelog line.
>
> **UNSHALLOW THE CLONE FIRST** — `git fetch --unshallow origin main`, before any measurement and before any pin. This is the Session Start Checklist's "Deepen the clone" bullet, not a favour to this brief. A shallow clone's boundary commit looks like it introduced every file, so `git log -1 --format=%cs -- <path>` returns the boundary's date rather than the file's — and §8 item 2 and G2 make that command the pin source. If `--check` reports a number wildly above the 7 below, suspect the clone before the corpus.
>
> THE WORK — one push commit. (1) Author `clLessonContractsAndRevenue_()` **inside** the `// CONTENT START` … `// CONTENT END` fence, at §3.5's seven section ids in its order — `the-instruments` (table) · `capacity-markets` (prose) · `take-or-pay` (prose) · `clean-firm-and-the-restart` (callout) · `where-it-fails` (callout) · `drill` · `check-yourself` — title *Who Carries Which Risk*, group Market Access & Bankability, and register it in `clLessons_()`. **The title must stay byte-identical to `PLANNED_TITLES["contracts-and-revenue"]` in `scripts/build-classroom-segments.py`** — check it before renaming anything. (2) **Re-derive the input roster rather than copying it, and watch the overlap**: §3.5's base list is **seven** and its v05.07r amendment lists eight, but `study:core-scientific` appears in both, so the union is **fourteen**, not fifteen. `profile:plus-power` and `study:plus-power` are two distinct refs at two layers and both belong. All fourteen are public, so the stamp folds to `tracks` — pin each off the document fetched that run and resolve every ref by matching its own `slug`. (3) Insert at **position 3 of `market-access`**, between `how-a-storage-project-happens` and `how-a-utility-buys`, and bump that track's `updated`. **Leave the track's `short` alone** — row 10 already settled that a finished-track-outcome `short` is not extended by an insertion. (4) Do NOT regenerate any segment — `--check` reads **7 due** before you start and all seven belong to their own S2 sessions under §10.9; registering this lesson should take it to **10 due** (`capital`, `software-and-optimization` and `insurance-and-risk-transfer` join; `storage-developers-and-ipps` is already in the seven), and that residue is expected, not damage. (5) Flip the §7.3 order-6 row, the §6 Phase 4 row and curriculum plan §4's position table and §7 row 11, all dated. (6) Write §7.29 — the next run's brief, which by the interleave is S2 session 7, `in-hall-power` — in the same commit, and **edit BOTH halves of any §7.x brief you refresh**: the prose paragraphs and the quoted prompt block.
>
> PREDICT BOTH HALVES OF THE DUE COUNT, NOT JUST THE MEMBERSHIP. §10.6 (w) makes this an obligation rather than a courtesy. This lesson's buying-criteria lexicon is `ppa · tolling · offtake · contract · revenue · merchant · capacity payment`, and row 8's rule is that the lexicon is **subordinate to `READ_NEXT`** — it decides only whether an eligible segment differs in one section or two. Against the registry it hits three of `storage-developers-and-ipps`'s criteria, one of `capital`'s and one of `insurance-and-risk-transfer`'s, and **none** of `software-and-optimization`'s. So expect `capital` and `insurance-and-risk-transfer` to join differing in **read-next and what-is-bought-and-on-what**, `software-and-optimization` to join differing in **read-next alone**, and `storage-developers-and-ipps` — already due on `read-next` alone — to **deepen** to two. Session 6's segment did not deepen, so this is the second real test of the row-10 case; **record which halves actually fired.**
>
> DO NOT: author or revise any guidance module, and do not touch `scripts/check-classroom-content.py`'s guidance-module assertion — it is **15** and rises only on an S2 session. Do not hand-edit a generated `segment-*` literal and do not regenerate any segment (§10.4, §10.9). Do not touch `Scraper.gs` — a Phase 4 row authors no module, so `industry-guidance.md` step 9 does not fire. Do not touch `Profiler.gs` or `Profiler.html`. Do not refresh `gateDigest` — a lesson inside the fence moves no `GATE_SYMBOLS` member, so P3 must be silent, and refreshing would hide a real signal. Do not fix `landscape-utilities-2026-09`'s review date, which is due by design and belongs to the quarterly Routine.
>
> VERIFY: `node --check` on a `.js` copy of `Classroom.gs`; `scripts/check-gas-inner-scripts.js`; `scripts/check-classroom-content.py` — **capture a pristine HEAD baseline BEFORE any edit. IT IS 0 ERRORS / 0 WARNINGS at 40 lessons / 8 tracks / 142 gate cases** and must be 0/0 at **41 lessons** after. Then `scripts/check-classroom-curriculum.py --strict` (no structural findings; 28 stale pins and 1 item due for review are both pre-existing and not yours); `scripts/check-classroom-pipeline.py --selftest` (13 fixtures, 0 failures) and `--base origin/main` (expect **P1 and P5, no P3, no P8**); `build-classroom-segments.py --check` — expect **7 due → 10 due**, with the per-segment section deltas above. `scripts/check-readme-tree.py` after the version bump. Playwright render of the new lesson at **analyst** (all fourteen inputs are public, so analyst is the POSITIVE test) with ZERO page errors, ops answered by the REAL serving functions — the lesson entrypoint is `clShowLesson(id)`, `_gasPost` returns a PROMISE, `sess.permissions` must be an ARRAY in the shim, the session token must be at least 32 characters or the real guard returns SESSION_EXPIRED, the session keys are `Classroom_gas_session_token` / `_gas_user_email` / `_gas_user_role` / `_gas_user_permissions`, overlays to hide are `#auth-wall` / `#signing-out-wall` / `#auth-timers` / `#gas-layer-toggle`, and the scratch site root needs `favicon.ico` copied in beside `profiler-data/`, `sounds/` and `images/`. Then READ EVERY SECTION'S SCREENSHOT. Bump Classroom's GAS version in BOTH `Classroom.gs` `VERSION` (from `v01.42g`) and `live-site-pages/gs-versions/Classroomgs.version.txt`, with a GENERIC entry in `Classroomgs.changelog.md`.
>
> THE DEPLOY and THE CHANGELOG ARITHMETIC: exactly as §7.28's paragraphs set out. **Confirm the deploy from Pages and the job log, NOT by probing `?op=deploy` first.** Deployment counters **Classroom 53/200 and Scraper 152/200, MEASURED** — v05.88r put both `Classroom.gs` and `Scraper.gs` in the merge diff with both bundles behind, and both confirmed on the first GET leg with no POLL and no probe, the tenth consecutive clean use of the §7.19 order. **The counter rule's middle case is now CORRECTED and you should not repeat the old form**: `Updated to` consumes one; **`Already up to date` describes the state at READ time and says nothing about whether the call consumed a deployment** — the arithmetic from the last measured anchor (150/200 at v05.78r) to 152/200 here leaves exactly one consumption unaccounted for, and the only Scraper deploy activity in between was v05.83r's poll-resolved `Already up to date`, whose *unconfirmed first leg had already run the deploy handler*; a step that never fires consumes none and logs nothing. Your own push touches only `Classroom.gs`, so expect **one** deploy line. Repo CHANGELOG **91 raw / 80 non-exempt** (91 once EST rolls past 2026-09-15) — **nine** from its cap, the closest it has ever been, so **you will not rotate but the session after you may** — re-count it yourself on the EST clock (`TZ=America/New_York date`). `Classroomgs.changelog.md` is **42 raw / 38 non-exempt** against 50 and is still the closest of the three to arming. `Scrapergs.changelog.md` sits at 28 raw / 26 non-exempt — and the lesson of its v05.83r rotation is that an armed changelog arms further overnight when EST rolls past its newest section, so re-count from the file rather than from any brief.
>
> Normal Pre-Commit and Pre-Push checklists; `claude/*` branch restarted from `origin/main` first, `git ls-remote` before each push — the auto-merge workflow adds a `[skip ci]` SHA-tracker commit after every merge, and after it merges use `git fetch --prune origin` rather than `git fetch origin main`, or the stale remote-tracking ref makes the stop hook report that SHA-tracker commit as unpushed.
>
> AFTER THIS SESSION Phase 4 is 11 of 26 and S2 is 6 of ~19, so the interleave returns to S2 and the recommended next run is **S2 session 7 — `in-hall-power`** — for which §7.29 should be written.

### 7.29 · Paste-in brief for S2 session 7 — `in-hall-power` (Opus 5 xhigh) — written 2026-09-15 at v05.91r, at the close of Phase 4 row 11 *(counters refreshed at v05.92r)*

**Why this and not another Phase 4 row.** Phase 4 is now **11 of 26** and S2 is **6 of ~19**, so the interleave stands at 11:6 and §7.4 item 6 hands the turn back to S2. Its next segment in the §7.3 sales-value order is **`in-hall-power`**, the seventh of nineteen and **position 5** in the chain.

**UNSHALLOW FIRST — a checklist item since v05.83r, not a brief's favour.** `git fetch --unshallow origin main` before any pin read, any `--check` run, any `git log -1 --format=%cs` date and any CHANGELOG rotation. Row 11 ran it as the Session Start Checklist's "Deepen the clone" bullet (55 → 1,241 commits) and `--check` opened at the true 7 with `inputs moved: none` on every line.

**THE SEGMENT IS TWENTY-SEVEN MEMBERS AND IT IS THE THIRD-DEEPEST IN THE CORPUS — measure it yourself on the day.** As of v05.91r `profiler-segments.json` reports **27 members (10 incumbents · 7 challengers · 10 adjacent)**, behind only `storage-integrators-and-containers` at 32 and `storage-developers-and-ipps` at 34, and ahead of session 6's 30-member landlords segment on incumbent count. Six consecutive S2 sessions have re-measured their segment on the day and confirmed the brief member for member; be the seventh, and say so in §10.6's bullet block. The segment's own definition is unusually wide — *everything between the service entrance and the rack whip that keeps a hall up when the grid drops*: static, rotary and modular UPS, the switchgear, busway and PDUs inside the fence, the prefabricated power module, the facility-level DC bus, and the VRLA, lithium and flywheel stores under all of it, **including the BBU and UPS cells that make the UPS room the one place a battery sale happens inside the building**.

**THE SPLIT — AND THIS IS THE MOST CROWDED NEIGHBOURHOOD ANY LANDSCAPE HAS FACED, ON BOTH SIDES OF THE GATE.** Session 6 had four neighbours and called that the record. This segment has more. On the **guidance** side: `nvidia-800vdc-2026-08` owns the 800 VDC architecture and this segment's own fifth buying criterion is literally *reference-design alignment (NVIDIA blueprints) and 800 VDC readiness*; `power-infra-aidc-2026-08` owns the chain; `grid-equipment-shortage-2026-08` owns the switchgear and transformer lead times that its sixth criterion turns on; `bess-tech-fundamentals-2026-08` owns the cell layer sitting under the UPS; and **`landscape-power-conversion-and-rack-power-silicon-2026-09` is S2 session 5's own module on the adjacent position**, so this is the **first landscape to split against another landscape**. On the **public** side its `READ_NEXT` names **five** lessons — `the-aidc-power-chain`, `the-ups-room`, `backup-generation`, `redundancy-by-the-numbers`, `where-bess-plugs-in` — and `the-800-vdc-shift` teaches the architecture beside them. Apply §7.23's test to each (*the one number both cite must do a different job in each*) and write the split **before** the module, per §10.6 (j). Session 6's finding (x) is the one to lean on: **the lesson teaches the test, the module applies it to named parties** — and check each public neighbour's tiles and callouts for a **pre-declared** handoff before drafting, which is the cleanest possible statement of where the line runs. **Enumerate every omission in the function's header comment and in §2 of the analysis file**; with this many neighbours expect more than session 6's nine.

**THE DUE-COUNT ARITHMETIC — your own segment is already due, so this is the N → N → N−1 shape.** `--check` reads **10 due** as you start: `in-hall-power`, `clean-firm-and-nuclear`, `compute-and-the-rack`, `epc-and-construction`, `storage-developers-and-ipps`, `hyperscalers-and-ai-labs`, `neoclouds`, `capital`, `software-and-optimization`, `insurance-and-risk-transfer`. **Yours is the first of those ten**, differing in `read-next` **and** `what-is-bought-and-on-what`. So registering the module changes its `read-next` without changing its membership and the count does **not** rise: the arithmetic is **10 → 10 → 9**, sessions 5 and 6's shape, not sessions 1–4's. Regenerate **only** `--segment in-hall-power`; the other nine belong to their own sessions under §10.9. **And predict both halves** per §10.6 (w): row 11 is the first session in which the deepening half fired, so the rule is now tested in both directions — session 6 predicted a deepening that did not happen, row 11 predicted one that did. State the per-segment section delta as well as the membership delta, and record which fired.

**The baselines to capture BEFORE any edit, all measured at the close of v05.91r.** `check-classroom-content.py` is **0 errors / 0 warnings at 41 lessons, 8 tracks, 142 gate cases** — held unbroken through nine consecutive sessions, and your commit must return it to 0/0 at **41 lessons** (a landscape module authors no lesson). **Its guidance-module assertion is 15 and yours is the commit that moves it to 16** — that number rises only on an S2 session, and `guidanceDocs_()` currently returns fifteen. `check-classroom-curriculum.py --strict`: no structural findings, **28 stale pins** on the hand-authored lessons (pre-existing; row 11 added none, because all fourteen of its pins were read off documents fetched that run), and **1 item due for review** — `landscape-utilities-2026-09` at 2026-10-01, by design and the quarterly Routine's. **A second joins it on 28 October** when `landscape-power-conversion-and-rack-power-silicon-2026-09` (`reviewBy` 2026-11-27) enters the 30-day horizon; session 6's module at 2026-12-15 does not enter it until 15 November. All still exit 0, because review dates never call `strict()`. `check-classroom-pipeline.py --selftest`: **13 fixtures / 0 failures**.

**Versions and counters to bump from.** Classroom GAS **v01.43g** (both `Classroom.gs` `VERSION` and `live-site-pages/gs-versions/Classroomgs.version.txt`); page **v01.14w**, and no page bump is expected. Repo **v05.92r**. Scraper **v02.07g** — **possibly yours**: an S2 session authors a guidance module, so `industry-guidance.md` step 9 fires and the interest-seed check runs. **State the prior as a prior, not as an answer** (§7.25's finding vii, and session 6's finding xiii that it can come back *half* right): run the candidate terms against **all** of `SCRAPER_INTEREST_TOPIC_SEEDS` and the segment array — 392 distinct terms at session 6 — and seed only what scores zero, dropping anything that duplicates an existing term and anything that belongs to a neighbour module on split grounds rather than duplication grounds. Profiler **v01.39g** and never yours.

**THE DEPLOY — the §7.19 order, on its eleventh use.** Read **Pages** first (`https://lightaisolutions.github.io/Sales/gs-versions/Classroomgs.version.txt` is a static file, free to read as often as you like) and the workflow's own `Classroom deploy confirmed (GET)` line from the job log. **Probe `?op=deploy` only if those two disagree** — the probe *deploys* whenever the running `/exec` bundle is behind GitHub. Use `curl -sL` if you do probe; without `-L` the body is empty and reads as a failure. **Deployment counters: Classroom 54/200, MEASURED at v05.91r; Scraper 152/200, measured at v05.88r and unmoved since.** Row 11 put only `Classroom.gs` in the merge diff, so **one** step fired and **one** line logged — `Classroom deploy confirmed (GET): Updated to v01.43g (deployment 54) | 54/200`, confirmed on the first GET leg with no POLL and no probe, the **eleventh consecutive** clean use of the §7.19 order. Scraper's step never fired, so it consumed none and logged nothing; **its 152/200 is the v05.88r measurement carried forward, not a v05.91r one** — a counter's provenance is part of the figure. **The counter rule as v05.88r corrected it**: `Updated to` consumes one; a step that never fires consumes none and logs nothing; and **`Already up to date` describes the state at READ time and is silent on whether the call consumed a deployment** — do not forecast it as zero. Forecast the number from whether the bundle is behind; forecast the log line from whether the file is in the diff. If your session seeds Scraper expect **two** deploy lines, otherwise one.

**CHANGELOGS — re-count from the files, never from this brief.** Keep using `TZ=America/New_York date '+%Y-%m-%d'`; the EST and UTC clocks converged again at v05.91r (both 2026-09-15) and that remains a convergence, not a fix — it diverges for any session running between 20:00 EST and midnight UTC. The rule counts **non-exempt** sections (total minus today's EST) and fires when that **reaches** the cap. At the close of v05.92r the repo CHANGELOG stands at **94 raw / 80 non-exempt** against 100, with fourteen sections dated 2026-09-15; those stop being exempt the moment EST rolls to 2026-09-16, taking it to **94 non-exempt** — still clear, and **six** from its cap, the closest it has ever been. **You will very likely be the session that rotates it**: any push landing on 2026-09-16 or later starts from 94 non-exempt, so six further sections reach the trigger. *(Refreshed at v05.92r: this paragraph was written during the v05.91r push and the handover commit that reproduced this brief then added a CHANGELOG section of its own — the same recursion v05.87r, v05.89r and v05.90r each recorded, now firing on a **fourth** consecutive handover. It is a structural property of the handover, not an oversight: any commit that reproduces a brief also moves the numbers that brief quotes. The defence is the one three sessions have now stated — re-run every figure a brief quotes against the working tree immediately before the final push, not only the figures the commit was opened to fix.)* The oldest whole date group is what moves; deepen the clone first so every header enriches. `Classroomgs.changelog.md` is **43 raw / 38 non-exempt** against 50, and **43 after the roll** — still the closest of the three to arming, with about seven sessions of headroom. `Scrapergs.changelog.md` sits at **28 raw / 26 non-exempt**. **The standing lesson is that an armed changelog arms further overnight when EST rolls past its newest section** — re-count from the file, never from a brief.

**Four open items recorded and not taken.** (i) The `.cl-tbl` `min-width` question now has **fifteen** clean data points — row 11's eleven-row, five-column instruments table rendered clean at 1400px with no horizontal scroll, which is the widest *column* count any mechanism-lesson table carries after row 9's six. Column count, row count and tooltip spans are all excluded; the v05.57r defect stands as a first column holding one long unbreakable word. **Close it** the next time a session has reason to edit `Classroom.html`. (ii) The generated segment lesson's `read-next` **names** the landscape module without linking to it: `clFmt` has no link token at all, and the string is emitted in every segment, so it belongs with (i) plus a developer `--all` run. (iii) **`the-800-vdc-shift`'s `three-chains` intro is now seven inserts stale and is NOT waiting for a Phase 4 row** — §7.25 reassigned it out of the queue and rows 10 and 11 and S2 session 6 all declined to acquire a claim. It belongs to whichever session opens that lesson for its own reason — **and this session may well be that session**, because `the-800-vdc-shift` sits squarely in this segment's neighbourhood. If you open it, fix it. (iv) **§10.6's session-4 bullet block still sits out of chronological order** — session 4's appears before session 3's, and sessions 5 and 6 both appended at the end rather than silently reordering another session's text. A future session can fix it deliberately; it is cosmetic.

> Author S2 session 7 — the `in-hall-power` landscape module — on Opus 5 xhigh as a fresh session. This is ONE guidance module. It authors no lesson, no dossier and no report, creates no track, and regenerates exactly ONE segment: its own.
>
> READ FIRST: repository-information/SESSION-CONTEXT.md (Latest Session); INTEGRATED-REMEDIATION-PLAN.md §7.29 (your brief), §7.27 and §7.28 (for the split rule and the both-halves due-count rule), §7.3 order 5 and §6; CLASSROOM-CURRICULUM-PLAN.md §10.6 VERBATIM (its nine section ids and their order are the contract), §10.2's floor table, §10.9's ownership rule and §10.10's two seller paths. Then repository-information/CLASSROOM-SCHEMA.md and .claude/rules/classroom-app.md IN FULL, .claude/rules/industry-guidance.md IN FULL, .claude/rules/scraper-sources.md before any seed edit, and .claude/rules/changelog-security.md before you write any public changelog line.
>
> **UNSHALLOW THE CLONE FIRST** — `git fetch --unshallow origin main`, before any measurement and before any pin. This is the Session Start Checklist's "Deepen the clone" bullet, not a favour to this brief. A shallow clone's boundary commit looks like it introduced every file, so `git log -1 --format=%cs -- <path>` returns the boundary's date rather than the file's — and §8 item 2 and G2 make that command the pin source. If `--check` reports a number wildly above the 10 below, suspect the clone before the corpus.
>
> THE WORK — one push commit. (1) Author `guidanceDocLandscapeInHallPower_()` **below** the `// CONTENT END` fence at §10.6's nine section ids in its order, register it in `guidanceDocs_()` at the **end** of The Value Chain lane, and write the analysis file. (2) **Re-measure the segment on the day rather than trusting this brief**: `profiler-segments.json` reports **27 members — 10 incumbents · 7 challengers · 10 adjacent** — the third-deepest segment in the corpus, and six consecutive sessions have confirmed their brief member for member. (3) **WRITE THE SPLIT BEFORE THE MODULE** (§10.6 (j)) — this is the most crowded neighbourhood any landscape has faced. Five guidance modules bear on it (`nvidia-800vdc-2026-08` on the architecture and this segment's own fifth buying criterion, `power-infra-aidc-2026-08` on the chain, `grid-equipment-shortage-2026-08` on the lead times behind its sixth, `bess-tech-fundamentals-2026-08` on the cell layer under the UPS, and **`landscape-power-conversion-and-rack-power-silicon-2026-09` on the adjacent position — the first landscape-against-landscape split**), and its `READ_NEXT` names **five public lessons** with `the-800-vdc-shift` beside them. Apply §7.23's test to each, take session 6's line (*the lesson teaches the test, the module applies it to named parties*), check every public neighbour's tiles and callouts for a pre-declared handoff, and **enumerate every omission** in the function's header comment and in §2 of the analysis file. (4) Regenerate **only** `--segment in-hall-power`. (5) Flip the §7.3 order-5 row, the §6 S2 row and the curriculum plan's §10.6 bullet block, all dated. (6) Write §7.30 — the next run's brief, which by the interleave is Phase 4 row 12 — in the same commit, and **edit BOTH halves of any §7.x brief you refresh**: the prose paragraphs and the quoted prompt block.
>
> PREDICT BOTH HALVES OF THE DUE COUNT, NOT JUST THE MEMBERSHIP. §10.6 (w) requires it and the rule is now tested in both directions: session 6 predicted a deepening that did **not** fire, row 11 predicted one that **did** — `storage-developers-and-ipps` went from differing in `read-next` alone to `read-next` **and** `what-is-bought-and-on-what` the moment `contracts-and-revenue` registered, because three of its six buying criteria hit that lesson's lexicon. **Your own segment is already the first of the ten due**, differing in both sections, so registering the module changes its `read-next` without changing its membership: the arithmetic is **10 → 10 → 9**, sessions 5 and 6's shape, and regenerating is what clears it. Record which halves actually fired.
>
> DO NOT: author or revise any lesson — `check-classroom-content.py` must read **41 lessons** before and after. The one assertion you DO move is the guidance-module count, **15 → 16**, and it moves only on an S2 session. Do not hand-edit a generated `segment-*` literal — change the registry, a member dossier or the generator's template and regenerate (§10.4). Do not regenerate any segment but your own (§10.9). Do not touch `Profiler.gs` or `Profiler.html`. Do not refresh `gateDigest` — a module below the fence moves no `GATE_SYMBOLS` member, so P3 must be silent, and refreshing would hide a real signal. Do not fix `landscape-utilities-2026-09`'s review date, which is due by design and belongs to the quarterly Routine.
>
> VERIFY: `node --check` on a `.js` copy of `Classroom.gs`; `scripts/check-gas-inner-scripts.js`; `scripts/check-classroom-content.py` — **capture a pristine HEAD baseline BEFORE any edit. IT IS 0 ERRORS / 0 WARNINGS at 41 lessons / 8 tracks / 142 gate cases** and must be 0/0 at 41 lessons after, with the module assertion moved to 16 in the same commit. Then `scripts/check-classroom-curriculum.py --strict` (no structural findings; 28 stale pins and 1 item due for review are both pre-existing and not yours); `scripts/check-classroom-pipeline.py --selftest` (13 fixtures, 0 failures) and `--base origin/main` (expect P1 on the developer files, P2 on the guidance literal, one P7 if you regenerate the same day; expect **no P3**); `build-classroom-segments.py --check` — expect **10 due → 10 due → 9 due**, because your own segment is already the first of the ten and a segment already in the due set cannot be added to it twice. `scripts/check-readme-tree.py` after the version bump. Playwright render of the new module at **contributor** (the module is contributor-only, so contributor is the POSITIVE test and analyst is the NEGATIVE one) with ZERO page errors, ops answered by the REAL serving functions — the guidance entrypoint is `clShowGuidanceDoc(id)`, `_gasPost` returns a PROMISE, `sess.permissions` must be an ARRAY in the shim, the session token must be at least 32 characters or the real guard returns SESSION_EXPIRED, the session keys are `Classroom_gas_session_token` / `_gas_user_email` / `_gas_user_role` / `_gas_user_permissions`, overlays to hide are `#auth-wall` / `#signing-out-wall` / `#auth-timers` / `#gas-layer-toggle`, and the scratch site root needs `favicon.ico` copied in beside `profiler-data/`, `sounds/` and `images/`. **SERVE THE SCRATCH COPY AT `/Classroom.html`, NOT AT THE SITE ROOT** — the page derives its sessionStorage key prefix from the URL path, and serving at `/` silently sends the seeded session to `index_gas_*` keys and renders as a role denial. Then READ EVERY SECTION'S SCREENSHOT. Bump Classroom's GAS version in BOTH `Classroom.gs` `VERSION` (from `v01.43g`) and `live-site-pages/gs-versions/Classroomgs.version.txt`, with a GENERIC entry in `Classroomgs.changelog.md`.
>
> THE DEPLOY and THE CHANGELOG ARITHMETIC: exactly as §7.29's paragraphs set out. **Confirm the deploy from Pages and the job log, NOT by probing `?op=deploy` first.** Deployment counters **Classroom 54/200, measured at v05.91r; Scraper 152/200, measured at v05.88r and unmoved since** — v05.91r put only `Classroom.gs` in the merge diff, so one step fired and one line logged (`Updated to v01.43g (deployment 54) | 54/200`), confirmed on the first GET leg with no POLL and no probe, the eleventh consecutive clean use of the §7.19 order. Scraper's step never fired, so it consumed none and logged nothing — carry its number forward as a v05.88r measurement, not a v05.91r one. **The counter rule's middle case**: `Updated to` consumes one; a step that never fires consumes none and logs nothing; **`Already up to date` describes the state at READ time and says nothing about whether the call consumed a deployment** — never forecast it as zero. If your session seeds `Scraper.gs` expect **two** deploy lines, otherwise one. Repo CHANGELOG **94 raw / 80 non-exempt** (94 once EST rolls past 2026-09-15) — **six** from its cap, the closest it has ever been, so **you may well be the session that rotates it**; re-count it yourself on the EST clock (`TZ=America/New_York date`) and deepen the clone first so every rotated header enriches. `Classroomgs.changelog.md` is **43 raw / 38 non-exempt** against 50 and is still the closest of the three to arming. `Scrapergs.changelog.md` sits at 28 raw / 26 non-exempt — and the lesson of its v05.83r rotation is that an armed changelog arms further overnight when EST rolls past its newest section, so re-count from the file rather than from any brief.
>
> Normal Pre-Commit and Pre-Push checklists; `claude/*` branch restarted from `origin/main` first, `git ls-remote` before each push — the auto-merge workflow adds a `[skip ci]` SHA-tracker commit after every merge, and after it merges use `git fetch --prune origin` rather than `git fetch origin main`, or the stale remote-tracking ref makes the stop hook report that SHA-tracker commit as unpushed.
>
> AFTER THIS SESSION Phase 4 is 11 of 26 and S2 is 7 of ~19, so the interleave returns to Phase 4 and the recommended next run is **Phase 4 row 12** — for which §7.30 should be written.

### 7.30 · Paste-in brief for Phase 4 row 12 — `where-batteries-stop` (Opus 5 xhigh) — written 2026-09-15 at v05.93r, at the close of S2 session 7 *(counters refreshed at v05.94r)*

**Why this and not another S2 segment.** Phase 4 is **11 of 26** and S2 is now **7 of ~19**, so the interleave stands at 11:7 and §7.4 item 6 hands the turn back to Phase 4. Its lowest unbuilt row is **row 12, `where-batteries-stop`** — *Long Duration, Sodium, and the Engine Boundary* — specified in curriculum plan §3.1 with a v05.07r amendment, and last in `bess-foundations`'s stated order.

**UNSHALLOW FIRST — a checklist item since v05.83r, not a brief's favour.** `git fetch --unshallow origin main` before any pin read, any `--check` run, any `git log -1 --format=%cs` date and any CHANGELOG rotation. S2 session 7 ran it as the Session Start Checklist's "Deepen the clone" bullet (1,245 commits) and `--check` opened at the true 10 with `inputs moved: none` on every line.

**The lesson, as §3.1 specifies it.** `where-batteries-stop`, title *Long Duration, Sodium, and the Engine Boundary* — **verified byte-identical to `PLANNED_TITLES["where-batteries-stop"]` in the generator at v05.93r, so it needs confirming rather than choosing** — group `Technology Foundations`, gate `tracks`, authored **inside** the `// CONTENT START` … `// CONTENT END` fence and registered in `clLessons_()`. §3.1's outline fixes **seven** section ids in this order: `the-cost-of-an-hour` (bars) · `eight-hour-native` (prose) · `sodium-honestly` (proscons) · `engines-own-the-long-end` (table) · `where-it-fails` (callout) · `drill` (6 cards) · `check-yourself` (5 items).

**BUT THE AMENDMENT ADDS A SECTION, NOT ONLY INPUTS, AND NO PREVIOUS ROW HAS HAD THAT.** §3.1's v05.07r amendment says `study:form-energy` arrives *"for an `iron-air-honestly` counterpart to `sodium-honestly`"* — which is a **section id the outline table does not contain**. Rows 9, 10 and 11 each found the amendment moving the *input roster*; this is the first row where it moves the **section contract**. Decide it explicitly and record the decision: eight sections with `iron-air-honestly` after `sodium-honestly` is the reading the amendment's own words support, and §8 item 6's standing rule is that §3's ids and kinds are the contract, so **adding the id the amendment names is following the spec rather than departing from it**. Say which you did and why, in the §8 record.

**RE-DERIVE THE INPUT ROSTER RATHER THAN COPYING IT — and this is the first row in four where the two lists agree.** §3.1's base list is **five** — `study:wartsila`, `study:hithium`, `study:catl`, `study:sungrow`, `concepts:profiler-concepts` — and its v05.07r amendment adds **five** more: `study:form-energy`, `study:narada`, `study:mitsubishi-power`, `study:bloom-energy`, `study:rolls-royce-power-systems`. **No entry overlaps, so the union is ten**, and §7 row 12's list is ten. Row 9 under-counted, row 10 added cleanly, row 11's amendment overlapped its base — **this one simply agrees, and it was checked rather than assumed.** All ten study guides were confirmed present on disk at v05.93r. Count ten, say so in the §8 record, and resolve every ref by opening the file and matching its own `slug` (G7).

**All ten are public, so the stamp folds to `tracks` and the lesson is analyst-visible.** Pin each off the document fetched that run (G2), never off the registry line beside it.

**THIS ROW APPENDS RATHER THAN INSERTS, so expect P1 and P8 and NOT P5.** `bess-foundations` holds four lessons today — `cell-to-container`, `duration-and-degradation`, `spec-sheet-decoded`, `the-control-stack` — and §3.1's order puts `how-a-cell-is-made` at position 5 and this lesson at 6. Position 5 is **row 15 and unbuilt**, so §4 position 6 *is* an append among the built lessons, exactly as row 5 was. Bump that track's `updated` in the same write.

**THE DUE-COUNT ARITHMETIC — the count RISES, and both halves are predicted.** `--check` reads **9 due** as you start: `clean-firm-and-nuclear`, `compute-and-the-rack`, `epc-and-construction`, `storage-developers-and-ipps`, `hyperscalers-and-ai-labs`, `neoclouds`, `capital`, `software-and-optimization`, `insurance-and-risk-transfer`. **`where-batteries-stop` is named in exactly ONE segment's `READ_NEXT` — `cells-and-chemistry` — and that segment is NOT in the nine**, so §10.6 (p)'s netting rule does not apply and the arithmetic is **9 → 10**, with nothing regenerated (all ten belong to their own S2 sessions under §10.9). **And the section half, per §10.6 (w):** the lesson's lexicon is `duration · long duration · sodium · sodium-ion · iron-air · eight-hour-native · C-rate · round-trip efficiency · cold performance · container count`, and against `cells-and-chemistry`'s six buying criteria it hits **two** — *"Cost per kWh at the cell and the format (Ah class) that sets container count"* and *"Chemistry fit to duty — energy vs power cell, C-rate, cold performance"*. So **expect `cells-and-chemistry` to join differing in BOTH `read-next` and `what-is-bought-and-on-what`, and expect no deepening on any of the nine**, because none of them names this lesson and row 8's rule makes the criteria lexicon subordinate to `READ_NEXT`. Record which halves fired: the rule now reads **2-for-3** on deepening (row 11 fired, S2 sessions 6 and 7 did not).

**§8 item 8 names this lesson specifically, so write the boundary rather than assuming it.** *"`where-batteries-stop` (track 1) ends where `bridge-power` (track 3) begins; the engine table appears once, in track 1, and track 3 refers to it."* So `engines-own-the-long-end` is **this** lesson's table and must not be a second copy of `bridge-power`'s prime-mover table — open that lesson and check what it already teaches before drafting. Two further boundaries are live: revenue *shapes* stay in `duration-and-degradation` (§8 item 8 again), and the cell itself stays in `cell-to-container` and `bess-tech-fundamentals-2026-08`.

**The baselines to capture BEFORE any edit, all measured at the close of v05.93r.** `check-classroom-content.py` is **0 errors / 0 warnings at 41 lessons, 8 tracks, 142 gate cases** — held unbroken through ten consecutive sessions, and your commit must return it to 0/0 at **42 lessons**. **Its guidance-module assertion is 16 and it does NOT move on a Phase 4 row** — that number rises only on an S2 session. `check-classroom-curriculum.py --strict`: no structural findings, **28 stale pins** on the hand-authored lessons (pre-existing; S2 session 7 added none, because it authored no lesson), and **2 items due for review** — `landscape-utilities-2026-09` and `landscape-in-hall-power-2026-09`, **both at 2026-10-01 and both by design**, the second because session 7's gate genuinely sits sixteen days after its `updated`. A third joins them on 28 October. All exit 0, because review dates never call `strict()`. **Do not "fix" either review date.** `check-classroom-pipeline.py --selftest`: **13 fixtures / 0 failures**.

**Versions and counters to bump from.** Classroom GAS **v01.44g** (both `Classroom.gs` `VERSION` and `live-site-pages/gs-versions/Classroomgs.version.txt`); page **v01.14w**, and no page bump is expected. Repo **v05.94r**. Scraper **v02.08g** and **not yours** — a Phase 4 row authors no guidance module, so `industry-guidance.md` step 9 does not fire. Profiler **v01.39g** and never yours.

**THE DEPLOY — the §7.19 order, on its thirteenth use.** Read **Pages** first (`https://lightaisolutions.github.io/Sales/gs-versions/Classroomgs.version.txt` is a static file, free to read as often as you like) and the workflow's own `Classroom deploy confirmed (GET)` line from the job log. **Probe `?op=deploy` only if those two disagree** — the probe *deploys* whenever the running `/exec` bundle is behind GitHub. Use `curl -sL` if you do probe; without `-L` the body is empty and reads as a failure. **Deployment counters: Classroom 55/200 and Scraper 153/200, both MEASURED at v05.93r** — that session put both `Classroom.gs` and `Scraper.gs` in the merge diff, so **both** steps fired and two lines logged, the twelfth consecutive clean use of the §7.19 order. Your row touches `Classroom.gs` only, so **forecast one line, not two** — the Scraper step will not fire, will consume nothing and will log nothing, and its 153/200 carries forward as a v05.93r measurement. **The counter rule, unchanged**: `Updated to` consumes one; a step that never fires consumes none and logs nothing; and **`Already up to date` describes the state at READ time and is silent on whether the call consumed a deployment** — never forecast it as zero.

**CHANGELOGS — re-count from the files, never from this brief, and YOU ARE VERY LIKELY THE ROTATOR.** Keep using `TZ=America/New_York date '+%Y-%m-%d'`; the EST and UTC clocks converged again at v05.93r (both 2026-09-15, at 18:47 EST / 22:47 UTC) and that remains a convergence, not a fix — it diverges for any session running between 20:00 EST and midnight UTC. The rule counts **non-exempt** sections (total minus today's EST) and fires when that **reaches** the cap. At the close of v05.93r the repo CHANGELOG stands at **96 raw / 80 non-exempt** against 100, with **sixteen** sections dated 2026-09-15; those stop being exempt the moment EST rolls to 2026-09-16, taking it to **96 non-exempt** — **four from its cap, the closest it has ever been, and any push landing on 2026-09-16 or later starts from there.** *(Refreshed at v05.94r: this paragraph was written during the v05.93r push and the handover commit that reproduced this brief then added a CHANGELOG section of its own — the **fifth** consecutive handover on which that has happened, after v05.86r, v05.87r, v05.89r/v05.90r and v05.92r. It is structural, not an oversight: any commit that reproduces a brief also moves the numbers that brief quotes. v05.93r wrote §7.30 inside the WORK commit and every figure held through that push — which is the useful refinement: the recursion fires on the **second** push, so a session that writes its brief in the work commit and then hands over must re-run the figures **again** before the handover push.)* Deepen the clone first so every rotated header enriches, and run the mandatory post-rotation grep: v05.83r's enrichment failed silently on the first attempt and only that grep caught it. `Classroomgs.changelog.md` is **44 raw / 38 non-exempt** against 50, and **44 after the roll** — six from its cap and still the closest of the page/GAS three to arming. `Scrapergs.changelog.md` sits at **29 raw / 26 non-exempt**. **The standing lesson is that an armed changelog arms further overnight when EST rolls past its newest section** — re-count from the file, never from a brief.

**Four open items recorded and not taken.** (i) The `.cl-tbl` `min-width` question now has **seventeen** clean data points — S2 session 7's seventeen-row bets table and its forty-five-row ledger both rendered clean at 1400px, and its ten-row indicators table is three columns. Column count, row count and tooltip spans are all excluded; the v05.57r defect stands as a first column holding one long unbreakable word. **Close it** the next time a session has reason to edit `Classroom.html`. (ii) The generated segment lesson's `read-next` **names** the landscape module without linking to it: `clFmt` has no link token at all, and the string is emitted in every segment, so it belongs with (i) plus a developer `--all` run. (iii) **`the-800-vdc-shift`'s `three-chains` intro is now eight inserts stale.** §7.25 reassigned it out of the Phase 4 queue and rows 10 and 11 and S2 sessions 6 and 7 all declined to acquire a claim — **S2 session 7 read that lesson for its split and did not edit it**, because a landscape module authors no lesson and opening it would have broken the 41-lesson baseline the same commit had to protect. It belongs to whichever session opens that lesson **to write in it**. (iv) **§10.6's session-4 bullet block still sits out of chronological order** — session 4's appears before session 3's, and sessions 5, 6 and 7 all appended at the end rather than silently reordering another session's text. A future session can fix it deliberately; it is cosmetic.

> Author Phase 4 row 12 — the `where-batteries-stop` lesson — on Opus 5 xhigh as a fresh session. This is ONE mechanism lesson. It authors no guidance module, no dossier and no report, creates no track, and regenerates NO segment.
>
> READ FIRST: repository-information/SESSION-CONTEXT.md (Latest Session); INTEGRATED-REMEDIATION-PLAN.md §7.30 (your brief), §7.28 and §7.29 (for the roster-trap rule and the both-halves due-count rule), §7.3 order 6 and §6; CLASSROOM-CURRICULUM-PLAN.md §3.1 VERBATIM — **its outline table's section ids and their order are the contract, and its v05.07r amendment names an EIGHTH id the table does not contain** — plus §4's position column, §7 row 12, and §8 items 6, 8 and 9. Then repository-information/CLASSROOM-SCHEMA.md and .claude/rules/classroom-app.md IN FULL, and .claude/rules/changelog-security.md before you write any public changelog line.
>
> **UNSHALLOW THE CLONE FIRST** — `git fetch --unshallow origin main`, before any measurement and before any pin. This is the Session Start Checklist's "Deepen the clone" bullet, not a favour to this brief. A shallow clone's boundary commit looks like it introduced every file, so `git log -1 --format=%cs -- <path>` returns the boundary's date rather than the file's — and §8 item 2 and G2 make that command the pin source. If `--check` reports a number wildly above the 9 below, suspect the clone before the corpus.
>
> THE WORK — one push commit. (1) Author `clLessonWhereBatteriesStop_()` **inside** the `// CONTENT START` … `// CONTENT END` fence at §3.1's section ids in its order — `the-cost-of-an-hour` (bars) · `eight-hour-native` (prose) · `sodium-honestly` (proscons) · `engines-own-the-long-end` (table) · `where-it-fails` (callout) · `drill` (6 cards) · `check-yourself` (5 items) — and **decide explicitly whether `iron-air-honestly` is an eighth section**, because §3.1's v05.07r amendment names that id while the outline table does not. Rows 9–11 each found the amendment moving the *input roster*; this is the first row where it moves the **section contract**. Record the decision and its reason in the §8 record. (2) Title *Long Duration, Sodium, and the Engine Boundary* — **verified byte-identical to `PLANNED_TITLES["where-batteries-stop"]` at v05.93r**, so confirm it rather than choosing it. Group `Technology Foundations`, gate `tracks`. (3) **Re-derive the input roster: five in §3.1's base list plus five in its amendment, no overlap, union TEN** — and §7 row 12 also lists ten, so **this is the first row in four where the two agree**; all ten study guides were confirmed present on disk at v05.93r. Resolve every ref by opening the file and matching its own `slug` (G7) and pin each off the document fetched that run (G2). (4) Register in `clLessons_()` and **APPEND** to `clTrackBessFoundations_()`'s `lessons[]`, bumping that track's `updated` — §4 position 6 is an append among the built four because position 5 (`how-a-cell-is-made`) is row 15 and unbuilt, so expect **P1 and P8 and NOT P5**. (5) **Regenerate nothing.** (6) Flip the §7.3 order-6 row, the §6 Phase 4 row, and curriculum plan §7 row 12 and §4's status cell, all dated. (7) Write §7.31 — the next run's brief, which by the interleave is S2 session 8, `grid-equipment` — in the same commit, and **edit BOTH halves of any §7.x brief you refresh**: the prose paragraphs and the quoted prompt block.
>
> PREDICT BOTH HALVES OF THE DUE COUNT, NOT JUST THE MEMBERSHIP. §10.6 (w) requires it and the rule is now tested three times: row 11 predicted a deepening that fired, S2 sessions 6 and 7 predicted none and none fired. **`where-batteries-stop` is named in exactly ONE segment's `READ_NEXT` — `cells-and-chemistry` — and that segment is NOT in the standing nine**, so the netting rule does not apply and the count RISES: **9 → 10**, with nothing regenerated. **And the section half**: against `cells-and-chemistry`'s six buying criteria the lesson's lexicon hits **two** — *"Cost per kWh at the cell and the format (Ah class) that sets container count"* and *"Chemistry fit to duty — energy vs power cell, C-rate, cold performance"* — so expect it to join differing in **both** `read-next` and `what-is-bought-and-on-what`, and expect **no deepening** on any of the nine. Record which halves actually fired.
>
> DO NOT: author or revise any guidance module — `guidanceDocs_()` must return **16** before and after, and the content checker's module assertion moves only on an S2 session. Do not hand-edit a generated `segment-*` literal and do not run the generator at all (§10.4, §10.9). Do not touch `Profiler.gs`, `Profiler.html` or `Scraper.gs` — a Phase 4 row fires no interest seed. Do not refresh `gateDigest` — a lesson inside the fence moves no `GATE_SYMBOLS` member, so P3 must be silent, and refreshing would hide a real signal. Do not "fix" the two items the curriculum checker reports as due for review: `landscape-utilities-2026-09` and `landscape-in-hall-power-2026-09` are **both at 2026-10-01 and both by design**, and both belong to the quarterly Routine.
>
> VERIFY: `node --check` on a `.js` copy of `Classroom.gs`; `scripts/check-gas-inner-scripts.js`; `scripts/check-classroom-content.py` — **capture a pristine HEAD baseline BEFORE any edit. IT IS 0 ERRORS / 0 WARNINGS at 41 lessons / 8 tracks / 142 gate cases** and must be 0/0 at **42 lessons** after, with the module assertion left at 16. Then `scripts/check-classroom-curriculum.py --strict` (no structural findings; 28 stale pins and 2 items due for review are all pre-existing and not yours); `scripts/check-classroom-pipeline.py --selftest` (13 fixtures, 0 failures) and `--base origin/main` (expect **P1** on the developer files and **P8**; expect **no P5** because this row appends, and **no P3**); `build-classroom-segments.py --check` — expect **9 due → 10 due**, with `cells-and-chemistry` joining on two sections. `scripts/check-readme-tree.py` after the version bump. Playwright render of the new lesson at **analyst** (the lesson is `tracks`-gated, so analyst is the POSITIVE test) with ZERO page errors, ops answered by the REAL serving functions — the lesson entrypoint is `clShowLesson(id)`, `_gasPost` returns a PROMISE, `sess.permissions` must be an ARRAY in the shim, the session token must be at least 32 characters or the real guard returns SESSION_EXPIRED, the session keys are `Classroom_gas_session_token` / `_gas_user_email` / `_gas_user_role` / `_gas_user_permissions`, overlays to hide are `#auth-wall` / `#signing-out-wall` / `#auth-timers` / `#gas-layer-toggle`, and the scratch site root needs `favicon.ico` copied in beside `profiler-data/`, `sounds/` and `images/`. **SERVE THE SCRATCH COPY AT `/Classroom.html`, NOT AT THE SITE ROOT** — the page derives its sessionStorage key prefix from the URL path, and serving at `/` silently sends the seeded session to `index_gas_*` keys and renders as a role denial. Then READ EVERY SECTION'S SCREENSHOT — **`the-cost-of-an-hour` is a `bars` section and `sodium-honestly` is `proscons`, and a proscons card's `t` and `meta` are `textContent`, so a `{{term}}` in either prints its braces to the reader.** Bump Classroom's GAS version in BOTH `Classroom.gs` `VERSION` (from `v01.44g`) and `live-site-pages/gs-versions/Classroomgs.version.txt`, with a GENERIC entry in `Classroomgs.changelog.md`.
>
> THE DEPLOY and THE CHANGELOG ARITHMETIC: exactly as §7.30's paragraphs set out. **Confirm the deploy from Pages and the job log, NOT by probing `?op=deploy` first.** Deployment counters **Classroom 55/200 and Scraper 153/200, both measured at v05.93r** — that session put both files in the merge diff, so both steps fired and two lines logged, the twelfth consecutive clean use of the §7.19 order. Your row touches `Classroom.gs` only, so **forecast one line, not two**: the Scraper step will not fire, will consume nothing and will log nothing, and its 153/200 carries forward as a v05.93r measurement. Repo CHANGELOG **96 raw / 80 non-exempt** (96 once EST rolls past 2026-09-15) — **four from its cap, the closest it has ever been, so you are very likely the session that rotates it**; re-count it yourself on the EST clock (`TZ=America/New_York date`), deepen the clone first so every rotated header enriches, and **run the mandatory post-rotation grep** — v05.83r's enrichment failed silently on the first attempt and only that grep caught it. `Classroomgs.changelog.md` is 44 raw / 38 non-exempt against 50 and is still the closest of the page/GAS three to arming. `Scrapergs.changelog.md` sits at 29 raw / 26 non-exempt — and the lesson of its v05.83r rotation is that an armed changelog arms further overnight when EST rolls past its newest section, so re-count from the file rather than from any brief.
>
> Normal Pre-Commit and Pre-Push checklists; `claude/*` branch restarted from `origin/main` first, `git ls-remote` before each push — the auto-merge workflow adds a `[skip ci]` SHA-tracker commit after every merge, and after it merges use `git fetch --prune origin` rather than `git fetch origin main`, or the stale remote-tracking ref makes the stop hook report that SHA-tracker commit as unpushed.
>
> AFTER THIS SESSION Phase 4 is 12 of 26 and S2 is 7 of ~19, so the interleave returns to S2 and the recommended next run is **S2 session 8 — `grid-equipment`** — for which §7.31 should be written.

### 7.31 · Paste-in brief for S2 session 8 — `grid-equipment` (Opus 5 xhigh) — written 2026-09-15 at v05.95r, at the close of Phase 4 row 12 *(counters refreshed at v05.96r)*

**Why this and not another Phase 4 row.** Phase 4 is now **12 of 26** and S2 is **7 of ~19**, so the interleave stands at 12:7 and §7.4 item 6 hands the turn back to S2. Its next segment in the §7.3 sales-value order is **`grid-equipment`**, the eighth of nineteen and **position 4** in the chain.

**UNSHALLOW FIRST — a checklist item since v05.83r, not a brief's favour.** `git fetch --unshallow origin main` before any pin read, any `--check` run, any `git log -1 --format=%cs` date and any CHANGELOG rotation. Row 12 ran it as the Session Start Checklist's "Deepen the clone" bullet (1,249 commits) and `--check` opened at the true 9 with `inputs moved: none` on every line.

**THE SEGMENT IS FIFTEEN MEMBERS AND ITS CHALLENGER BENCH IS THE THINNEST ANY LANDSCAPE HAS FACED — measure it yourself on the day.** As of v05.95r `profiler-segments.json` reports **15 members (5 incumbents · 1 challenger · 9 adjacent)**: incumbents `hitachi-energy`, `siemens-energy`, `ge-vernova`, `abb`, `mitsubishi-electric`; the single challenger `powell-industries`; and nine adjacent, `eaton`, `schneider-electric`, `flex`, `quanta-services`, `ls-energy-solutions`, `zhonhen`, `invenergy`, `grid-united`, `pattern-energy`. The floor rule is met — three members including an incumbent and a challenger — but **only just, on the challenger axis**, and §10.6's `who-threatens` section has exactly one named party to work with. That is the session's central craft problem rather than a defect in the segment: seven consecutive S2 sessions have re-measured their segment on the day and confirmed the brief member for member; be the eighth, and say in §10.6's bullet block how a one-challenger field was written honestly. The definition is unusually consequential — *the transformers, medium- and high-voltage switchgear, HVDC converters, protection relays and grid-stability machines that connect a campus or a storage plant to the bulk grid and hold it up* — and it closes on the sentence that is the whole segment's leverage: **the delivery calendar here sets energization dates for everyone else.**

**THE SPLIT — AND THIS IS THE FIRST SEGMENT WITH A BUILT LANDSCAPE MODULE ON BOTH SIDES OF IT IN THE CHAIN.** Position 3 is `power-conversion-and-rack-power-silicon` (S2 session 5's module) and position 5 is `in-hall-power` (session 7's), so both neighbours are already ranked and this module has to hold a line in two directions at once — session 7 split against *one* landscape and called that a first. On the **guidance** side the split is harder still, because `grid-equipment-shortage-2026-09` is a module about **this exact segment's scarcity**, and this segment's first buying criterion *is* that shortage (*lead time and the manufacturing slot; transformers 30–40 months; test-bay availability*). Apply §7.23's test to it before anything else — *the one number both cite must do a different job in each* — and expect the answer to be that the shortage module owns **the calendar as a market condition** while the landscape owns **who holds which slot and on what basis**. Two further guidance neighbours are live and both map onto a named criterion: `eo14420-bulk-power-2026-08` owns criterion 3 (*domestic manufacture under EO 14420 and the tariff stack*) and `power-infra-aidc-2026-08` owns the chain. On the **public** side `READ_NEXT` names four lessons of which **two are built** — `the-transformer-and-the-substation` (§7 row 5) and `the-fence-line` — while `breakers-relays-and-faults` (row 13) and `grid-stability-and-the-generator` (row 14) are not; read the two built ones for their tiles *and* their callouts before drafting, per §10.6 (t), and write the split **before** the module, per §10.6 (j). Enumerate every omission in the function's header comment and in §2 of the analysis file.

**THE DUE-COUNT ARITHMETIC — your own segment is NOT due, so this is sessions 1–4's shape rather than 5–7's.** `--check` reads **10 due** as you start: `cells-and-chemistry`, `clean-firm-and-nuclear`, `compute-and-the-rack`, `epc-and-construction`, `storage-developers-and-ipps`, `hyperscalers-and-ai-labs`, `neoclouds`, `capital`, `software-and-optimization`, `insurance-and-risk-transfer`. **`grid-equipment` is not among them**, so registering the module makes it due and the arithmetic is **10 → 11 → regenerate your own → 10**, not the netting shape. Regenerate **only** `--segment grid-equipment`; the other ten belong to their own sessions under §10.9. **And the section half is `read-next` ALONE, and this one is derived rather than guessed** — `self.landscapes` is read at exactly one place in the generator, inside `sec_read_next`, so a landscape registration cannot touch any other section. **Carry row 12's finding when you predict**: §7.30 forecast a section half from the buying criteria's *prose* and was falsified, because `sec_what_is_bought` resolves only through `CRITERION_LEXICON` and a lesson absent from that table can never appear in it however well the words match. The membership half is bounded by `READ_NEXT`; **the section half is bounded by the mechanism, so read the generator.**

**The baselines to capture BEFORE any edit, all measured at the close of v05.95r.** `check-classroom-content.py` is **0 errors / 0 warnings at 42 lessons, 8 tracks, 142 gate cases** — held unbroken through eleven consecutive sessions, and your commit must return it to 0/0 at **42 lessons** (a landscape module authors no lesson). **Its guidance-module assertion is 16 and yours is the commit that moves it to 17** — that number rises only on an S2 session, and `guidanceDocs_()` currently returns sixteen. `check-classroom-curriculum.py --strict`: no structural findings, **28 stale pins** on the hand-authored lessons (pre-existing and unmoved — row 12 added none, because all ten of its pins were read off documents fetched that run), and **2 items due for review** — `landscape-utilities-2026-09` and `landscape-in-hall-power-2026-09`, **both at 2026-10-01, both by design** and both the quarterly Routine's. A third joins them on 28 October. All exit 0, because review dates never call `strict()`. **Do not "fix" either review date.** `check-classroom-pipeline.py --selftest`: **13 fixtures / 0 failures**.

**Versions and counters to bump from.** Classroom GAS **v01.45g** (both `Classroom.gs` `VERSION` and `live-site-pages/gs-versions/Classroomgs.version.txt`); page **v01.14w**, and no page bump is expected. Repo **v05.96r**. Scraper **v02.08g** — **possibly yours**: an S2 session authors a guidance module, so `industry-guidance.md` step 9 fires and the interest-seed check runs. **State the prior as a prior, not as an answer** (§7.25's finding vii, and session 6's finding that it can come back *half* right): run the candidate terms against **all** of `SCRAPER_INTEREST_TOPIC_SEEDS` and the segment array — 399 distinct terms at session 7 — and seed only what scores zero, dropping anything that duplicates an existing term and anything that belongs to a neighbour module on split grounds rather than duplication grounds. With `grid-equipment-shortage-2026-09` already seeded, expect the prior to lean **negative** on the calendar vocabulary and positive only on the machines themselves — but run it, because the label-level glance the rule forbids is exactly what would get this one wrong. Profiler **v01.39g** and never yours.

**THE DEPLOY — the §7.19 order, on its fourteenth use.** Read **Pages** first (`https://lightaisolutions.github.io/Sales/gs-versions/Classroomgs.version.txt` is a static file, free to read as often as you like) and the workflow's own `Classroom deploy confirmed (GET)` line from the job log. **Probe `?op=deploy` only if those two disagree** — the probe *deploys* whenever the running `/exec` bundle is behind GitHub. Use `curl -sL` if you do probe; without `-L` the body is empty and reads as a failure. **Deployment counters: Classroom 56/200, MEASURED at v05.95r; Scraper 153/200, measured at v05.93r and unmoved since.** Row 12 put **only `Classroom.gs`** in the merge diff, so exactly one step fired and one line logged — `Classroom deploy confirmed (GET): Updated to v01.45g (deployment 56) | 56/200`, confirmed on the **first GET leg** with no POLL and no `?op=deploy` probe, and Pages read `|v01.45g|` in agreement before the job log was closed. **The thirteenth consecutive clean use of the §7.19 order**, and the forecast of one line rather than two held exactly: Scraper's step opened with `git diff --name-only "$PRE" HEAD | grep -q "$GS" || exit 0`, never fired, consumed nothing and logged nothing, so **its 153/200 is a v05.93r measurement carried forward, not a v05.95r one** — a counter's provenance is part of the figure. **The counter rule, unchanged**: `Updated to` consumes one; a step that never fires consumes none and logs nothing; and **`Already up to date` describes the state at READ time and is silent on whether the call consumed a deployment** — never forecast it as zero. If your session seeds Scraper expect **two** deploy lines, otherwise one.

**CHANGELOGS — AND THE EST/UTC TRAP FIRED AT v05.95r, WHICH IS WHY THE ROTATION DID NOT HAPPEN.** Keep using `TZ=America/New_York date '+%Y-%m-%d'` and **trust it over the session harness's own date banner, which follows UTC.** At v05.95r the EST clock read **2026-09-15 20:01** while UTC read **2026-09-16 00:01** — the first divergence in this run, one minute into the 20:00-EST window §7.29 and §7.30 both named. §7.30 forecast that session as *"very likely the rotator"* on the premise of a push landing on 2026-09-16; by the clock the rule actually counts it did not, and a session that had trusted the banner would have rotated the thirteen sections dated 2026-09-07 **with no trigger at all**. The rule counts **non-exempt** sections (total minus today's EST) and fires when that **reaches** 100. At the close of v05.96r the repo CHANGELOG stands at **98 raw / 80 non-exempt**, with **eighteen** sections dated 2026-09-15; those stop being exempt the moment EST rolls to 2026-09-16, taking it to **98 non-exempt — two from its cap, and the closest it has ever been.** **So you are very probably the rotator — but check `TZ=America/New_York date` before you decide, because that is precisely the check that changed the answer last time.** Deepen the clone first so every rotated header enriches, and run the mandatory post-rotation grep: v05.83r's enrichment failed silently on the first attempt and only that grep caught it. `Classroomgs.changelog.md` is **45 raw / 38 non-exempt** against 50, and **45 after the roll** — five from its cap and still the closest of the page/GAS three to arming. `Scrapergs.changelog.md` sits at **29 raw / 26 non-exempt**. **The standing lesson is that an armed changelog arms further overnight when EST rolls past its newest section** — re-count from the file, never from a brief.

**Five open items recorded and not taken.** (i) The `.cl-tbl` `min-width` question now has **thirteen** clean data points — row 12's six-by-four boundary table rendered clean at 1400px, the second six-column table in the corpus after row 9's. Column count, row count and tooltip spans are all excluded and the v05.57r defect stands as a first column holding one long unbreakable word. **Close it** the next time a session has reason to edit `Classroom.html`. (ii) The generated segment lesson's `read-next` **names** the landscape module without linking to it: `clFmt` has no link token at all, and the string is emitted in every segment, so it belongs with (i) plus a developer `--all` run. (iii) **`the-800-vdc-shift`'s `three-chains` intro is now nine inserts stale** — §7.25 reassigned it out of the Phase 4 queue and rows 10, 11 and 12 and S2 sessions 6 and 7 all declined to acquire a claim. It belongs to whichever session opens that lesson **to write in it**. (iv) **§8 item 8's pointer runs only one way**: row 12's `engines-own-the-long-end` states its boundary against `bridge-power`, but `bridge-power` shipped among the first five and cannot refer back to a lesson that did not exist; editing its literal would put a section in the differs set with no contradiction to justify it. Same disposition as (iii) — it belongs to whichever session opens `bridge-power` to write in it. (v) **§7.3's own S2 row reads "5 of ~19 built" and is two sessions stale** (sessions 6 and 7 landed at v05.88r and v05.93r); the §6 ledger's S2 row is one behind as well. That row is S2's own record, so **flip both in your commit** rather than leaving them to a Phase 4 session. §10.6's session-4 bullet block also still sits out of chronological order — cosmetic, and a future session can fix it deliberately.

> Author S2 session 8 — the `grid-equipment` landscape module — on Opus 5 xhigh as a fresh session. This is ONE guidance module. It authors no lesson, no dossier and no report, creates no track, and regenerates exactly ONE segment: its own.
>
> READ FIRST: repository-information/SESSION-CONTEXT.md (Latest Session); INTEGRATED-REMEDIATION-PLAN.md §7.31 (your brief), §7.29 and §7.30 (for the split discipline and the both-halves due-count rule), §7.3 order 5 and §6; CLASSROOM-CURRICULUM-PLAN.md §10.6 VERBATIM — **its nine section ids and their order are the contract** — plus §10.2's floor rule, §10.5's lane and §10.9. Then repository-information/CLASSROOM-SCHEMA.md and .claude/rules/classroom-app.md IN FULL, .claude/rules/industry-guidance.md for the module shape and its step 9, and .claude/rules/changelog-security.md before you write any public changelog line.
>
> **UNSHALLOW THE CLONE FIRST** — `git fetch --unshallow origin main`, before any measurement and before any pin. This is the Session Start Checklist's "Deepen the clone" bullet, not a favour to this brief. A shallow clone's boundary commit looks like it introduced every file, so `git log -1 --format=%cs -- <path>` returns the boundary's date rather than the file's — and §8 item 2 and G2 make that command the pin source. If `--check` reports a number wildly above the 10 below, suspect the clone before the corpus.
>
> THE WORK — one push commit. (1) Author `guidanceDocLandscapeGridEquipment_()` **below** the `// CONTENT END` fence, at §10.6's nine section ids in its order, and register it at the **end** of `guidanceDocs_()`'s **The Value Chain** lane. (2) Write its analysis file in `repository-information/industry-guidance/`, with **§2 — the split — written before any other section**, per §10.6 (j). (3) **MEASURE THE SEGMENT ON THE DAY.** §7.31 says 15 members (5 incumbents · 1 challenger · 9 adjacent) — the **thinnest challenger bench any landscape has faced**, exactly one named party. Confirm it member for member and say in §10.6's bullet block how you wrote `who-threatens` honestly with one challenger. (4) **THE SPLIT IS THE HARDEST PART AND IT RUNS IN THREE DIRECTIONS.** This is the **first segment with a built landscape module on both sides of it in the chain** — position 3 `power-conversion-and-rack-power-silicon` and position 5 `in-hall-power` — and on the guidance side `grid-equipment-shortage-2026-09` is a module about **this exact segment's scarcity**, whose subject is this segment's own first buying criterion. Apply §7.23's test (*the one number both cite must do a different job in each*) to that module before anything else. `eo14420-bulk-power-2026-08` owns criterion 3 and `power-infra-aidc-2026-08` owns the chain. On the public side `READ_NEXT` names four lessons of which two are built — read their tiles **and** their callouts for a pre-declared handoff, per §10.6 (t). Enumerate every omission in the header comment and in §2. (5) Regenerate **only** `--segment grid-equipment`. (6) Flip the §7.3 order-5 row **and its S2 row, which reads "5 of ~19" and is two sessions stale**, the §6 S2 row, and curriculum plan §10.6's bullet block, all dated. (7) Write §7.32 — the next run's brief, which by the interleave is Phase 4 row 13, `breakers-relays-and-faults` — in the same commit, and **edit BOTH halves of any §7.x brief you refresh**: the prose paragraphs and the quoted prompt block.
>
> PREDICT BOTH HALVES OF THE DUE COUNT, NOT JUST THE MEMBERSHIP. §10.6 (w) requires it, and row 12 is the first session whose section half was **falsified** — it forecast from the buying criteria's prose when `sec_what_is_bought` resolves only through `CRITERION_LEXICON`, so read the generator, not the words. **Your own segment is NOT among the standing ten**, so registering the module makes it due and the arithmetic is **10 → 11 → regenerate your own → 10** — sessions 1–4's shape, not sessions 5–7's netting shape. **And the section half is `read-next` ALONE**, derived rather than guessed: `self.landscapes` is read at exactly one place in the generator, inside `sec_read_next`, so a landscape registration cannot touch any other section. The standing ten are `cells-and-chemistry`, `clean-firm-and-nuclear`, `compute-and-the-rack`, `epc-and-construction`, `storage-developers-and-ipps`, `hyperscalers-and-ai-labs`, `neoclouds`, `capital`, `software-and-optimization`, `insurance-and-risk-transfer`; none is yours under §10.9. Record which halves actually fired.
>
> DO NOT: author or revise any lesson — `check-classroom-content.py` must report **42 lessons** before and after, and a landscape module authors none. Do not hand-edit a generated `segment-*` literal for any segment but your own, and run the generator only with `--segment grid-equipment` (§10.4, §10.9). Do not touch `Profiler.gs` or `Profiler.html`. Do not refresh `gateDigest` — a module below the fence moves no `GATE_SYMBOLS` member, so P3 must be silent, and refreshing would hide a real signal. Do not "fix" the two items the curriculum checker reports as due for review: `landscape-utilities-2026-09` and `landscape-in-hall-power-2026-09` are **both at 2026-10-01 and both by design**, and both belong to the quarterly Routine.
>
> VERIFY: `node --check` on a `.js` copy of `Classroom.gs`; `scripts/check-gas-inner-scripts.js`; `scripts/check-classroom-content.py` — **capture a pristine HEAD baseline BEFORE any edit. IT IS 0 ERRORS / 0 WARNINGS at 42 lessons / 8 tracks / 142 gate cases** and must be 0/0 at **42 lessons** after, with the module assertion moved **16 → 17** (yours is the commit that moves it). Then `scripts/check-classroom-curriculum.py --strict` (no structural findings; 28 stale pins and 2 items due for review are all pre-existing and not yours); `scripts/check-classroom-pipeline.py --selftest` (13 fixtures, 0 failures) and `--base origin/main` (expect **P1** on the developer files; expect **no P3**, and expect **no P7** — three briefs have now expected P7 on a regeneration and been wrong, so the S2 signature is **P1 ×4, P2, no P3, no P7**); `build-classroom-segments.py --check` — expect **10 → 11 → 10**, your own segment joining and leaving on `read-next` alone. `scripts/check-readme-tree.py` after the version bump. Playwright render of the new module at **contributor** (the module is `guidance`-gated, so contributor is the POSITIVE test and analyst is the negative one) with ZERO page errors, ops answered by the REAL serving functions — the module entrypoint is `clShowGuidanceDoc(id)`, `_gasPost` returns a PROMISE, `sess.permissions` must be an ARRAY in the shim, the session token must be at least 32 characters or the real guard returns SESSION_EXPIRED, the session keys are `Classroom_gas_session_token` / `_gas_user_email` / `_gas_user_role` / `_gas_user_permissions`, overlays to hide are `#auth-wall` / `#signing-out-wall` / `#auth-timers` / `#gas-layer-toggle`, and the scratch site root needs `favicon.ico` copied in beside `profiler-data/`, `sounds/` and `images/`. **SERVE THE SCRATCH COPY AT `/Classroom.html`, NOT AT THE SITE ROOT** — the page derives its sessionStorage key prefix from the URL path, and serving at `/` silently sends the seeded session to `index_gas_*` keys and renders as a role denial. Then READ EVERY SECTION'S SCREENSHOT — a `proscons` card's `t` and `meta` are `textContent`, so a `{{term}}` in either prints its braces to the reader, and a `bars` item's `label` and `sub` are `textContent` too. Bump Classroom's GAS version in BOTH `Classroom.gs` `VERSION` (from `v01.45g`) and `live-site-pages/gs-versions/Classroomgs.version.txt`, with a GENERIC entry in `Classroomgs.changelog.md`.
>
> THE DEPLOY and THE CHANGELOG ARITHMETIC: exactly as §7.31's paragraphs set out. **Confirm the deploy from Pages and the job log, NOT by probing `?op=deploy` first.** Deployment counters **Classroom 56/200, measured at v05.95r, and Scraper 153/200, measured at v05.93r and unmoved since** — v05.95r put only `Classroom.gs` in the merge diff, so one step fired and one line logged, the thirteenth consecutive clean use of the §7.19 order, and a step that never fires consumes nothing and logs nothing. If your session seeds Scraper expect two deploy lines, otherwise one. **THE EST/UTC TRAP FIRED AT v05.95r AND IT DECIDED THE ROTATION**: EST read 2026-09-15 20:01 against UTC's 2026-09-16 00:01, the harness's own date banner follows UTC, and a session trusting it would have rotated thirteen sections with no trigger. Repo CHANGELOG **98 raw / 80 non-exempt** with eighteen sections dated 2026-09-15 EST — **98 non-exempt once EST rolls past that date, two from the cap and the closest it has ever been, so you are very probably the rotator.** Re-count it yourself on the EST clock (`TZ=America/New_York date`), deepen the clone first so every rotated header enriches, and **run the mandatory post-rotation grep** — v05.83r's enrichment failed silently on the first attempt and only that grep caught it. `Classroomgs.changelog.md` is 45 raw / 38 non-exempt against 50 and is still the closest of the page/GAS three to arming. `Scrapergs.changelog.md` sits at 29 raw / 26 non-exempt — and the lesson of its v05.83r rotation is that an armed changelog arms further overnight when EST rolls past its newest section, so re-count from the file rather than from any brief.
>
> Normal Pre-Commit and Pre-Push checklists; `claude/*` branch restarted from `origin/main` first, `git ls-remote` before each push — the auto-merge workflow adds a `[skip ci]` SHA-tracker commit after every merge, and after it merges use `git fetch --prune origin` rather than `git fetch origin main`, or the stale remote-tracking ref makes the stop hook report that SHA-tracker commit as unpushed.
>
> AFTER THIS SESSION Phase 4 is 12 of 26 and S2 is 8 of ~19, so the interleave returns to Phase 4 and the recommended next run is **Phase 4 row 13 — `breakers-relays-and-faults`** — for which §7.32 should be written.

### 7.32 · Paste-in brief for Phase 4 row 13 — `breakers-relays-and-faults` (Opus 5 xhigh) — written 2026-09-15 at v05.97r, at the close of S2 session 8 *(counters refreshed at v05.98r)*

**Why this and not another S2 segment.** Phase 4 is **12 of 26** and S2 is now **8 of ~19**, so the interleave stands at 12:8 and §7.4 item 6 hands the turn back to Phase 4. Its lowest unbuilt row is **13 — `breakers-relays-and-faults`, *Stopping Ten Thousand Amps*** — the forty-third lesson, and the one `dc-fault-engineering` is a stated prerequisite for.

**UNSHALLOW FIRST — a Session Start Checklist item since v05.83r, not a brief's favour.** `git fetch --unshallow origin main` before any pin read, any `--check` run, any `git log -1 --format=%cs` date and any CHANGELOG rotation. Session 8 ran it as the checklist's "Deepen the clone" bullet (1,253 commits) and `--check` opened at the true 10 with `inputs moved: none` on every line.

**THE ROSTER AGREES, AND THAT IS NOW TWO ROWS RUNNING — but re-derive it rather than trusting this sentence.** §3.1's outline names four inputs — `study:abb` (arc interruption: chutes, vacuum, SF6, solid-state; protection relays, IEDs, IEC 61850, reclosers and FLISR) · `study:eaton` (fault energy as what sizes each tier) · `study:zhonhen` (DC safety literacy: no zero crossing) · `concepts:profiler-concepts`. Its **v05.07r amendment adds two with no overlap** — `study:siemens-energy` (AIS against GIS against SF6-free) and `study:powell-industries` (`what-a-lineup-actually-is`, `arc-flash`, `how-a-protection-study-is-built`, `kilovolts-and-kiloamps` — *"the second deep source the spec lacked"*). Four plus two is six, and **§7 row 13 lists the same six.** Row 9 under-counted an amendment, row 10 added cleanly, row 11's amendment *overlapped* its base on `study:core-scientific`, row 12 agreed, and this one agrees. **Agreement is the current run, not the rule** — do the addition.

**THE SECTION CONTRACT IS SEVEN, AND THIS AMENDMENT MOVES NO SECTION.** §3.1's outline table fixes seven ids in this order: `what-a-fault-is` (prose — 10 to 50 times normal current, and why equipment upstream is heavier) · `killing-the-arc` (**proscons** — arc chutes · vacuum · SF6 · solid-state, each with what it buys and what it costs) · `the-brain-and-the-muscle` (prose — relays, instrument transformers, differential and distance protection, the networked substation and its cybersecurity debt) · `dc-has-no-zero` (**callout** — why a DC arc must be engineered out rather than waited out) · `where-it-fails` (**table** — miscoordination · nuisance trips · relay settings nobody re-checked after a load change · a sustained DC series arc · SF6 handling) · `drill` · `check-yourself`. **Row 12 was the first row whose amendment moved the *sections* rather than the input roster** (§3.1's v05.07r amendment named `iron-air-honestly`, an id the outline table did not contain). **This amendment names two inputs and no section id**, so seven is the contract — but read the amendment yourself before you conclude that, because row 12's session is the reason anyone would.

**§8 ITEM 8 IS EXPLICIT ABOUT THIS ROW AND NAMES THE BOUNDARY IN ADVANCE — the only Phase 4 row for which it does.** *"`breakers-relays-and-faults` (track 2) cites `study:eaton` for fault-energy sizing only — selective coordination stays in `the-aidc-power-chain`."* So the `study:eaton` pin is **taken for the sizing argument and nothing else**, and the coordination material a reader will expect belongs to a lesson that already exists. Open `the-aidc-power-chain` and read what it actually teaches before drafting — the boundary is pre-declared, and the trap row 12 recorded is the reverse case (a boundary stated in the new lesson with no reverse pointer possible). Here the neighbour exists and was built first, so **check whether the reverse pointer is worth acquiring and record the decision either way.** `the-transformer-and-the-substation` is this row's immediate predecessor in the track and carries a where-it-fails table naming bushings and tap changers; `dc-fault-engineering` is downstream and unbuilt, so this lesson **sets up** a depth it must not reach.

**THE TRACK INSERTION IS AN APPEND, SO P5 CANNOT FIRE.** `electrical-foundations` currently holds `["four-machines", "the-transformer-and-the-substation"]` — §3.1's order puts this row **fourth of five**, but `string-versus-central` (row 15) is unbuilt, so in the built list it lands **after `the-transformer-and-the-substation`, at the end**. That is an append, and row 12's finding applies unchanged: **the signature for an append is P1 alone** — no P5, no P3, no P7, and **no P8, because `p8_changed_names_what_moved` iterates `set(base.lessons) & set(head.lessons)` and a brand-new lesson exists only at head.** Bump the track's `updated`.

**PREDICT BOTH HALVES, AND THIS ROW'S SECTION HALF IS A DEEPENING AT BIRTH — the first of its kind.** `--check` reads **10 due** as you start: `cells-and-chemistry`, `clean-firm-and-nuclear`, `compute-and-the-rack`, `epc-and-construction`, `storage-developers-and-ipps`, `hyperscalers-and-ai-labs`, `neoclouds`, `capital`, `software-and-optimization`, `insurance-and-risk-transfer`. **`grid-equipment` is NOT among them** — session 8 regenerated it — and it is the **only** segment whose `READ_NEXT` names this lesson, so it is the only one that can move. **The membership half is 10 → 11 → regenerate → 10.** The section half is the interesting one and it is **derived, not guessed**: `lesson_ref()` returns `*Title*` for a registered id and `*Title* (planned)` otherwise, and `breakers-relays-and-faults` appears **twice** in that segment's generated literal — once in `read-next` and once in `what-is-bought-and-on-what`, because it **is** in `CRITERION_LEXICON` (`breaker`, `fault`, `protection`, `relay`, `arc`) *and* in that segment's `READ_NEXT`, and criterion 4 reads *"Protection and automation integration (IEC 61850) and the coordination study"*. So **both** section strings lose the `(planned)` marker and the segment joins differing in **`read-next` AND `what-is-bought-and-on-what`**. Row 12's gate is the one to apply: **the membership half is bounded by `READ_NEXT`; the section half by `READ_NEXT` ∩ `CRITERION_LEXICON`** — and this row is the first where that intersection is non-empty for the lesson being authored. **Regenerate only `--segment grid-equipment`**; the standing ten belong to their own sessions under §10.9.

**The baselines to capture BEFORE any edit, all measured at the close of v05.97r.** `check-classroom-content.py` is **0 errors / 0 warnings at 42 lessons, 8 tracks, 142 gate cases** — held unbroken through twelve consecutive sessions, and your commit must return it to 0/0 at **43 lessons**. **Its guidance-module assertion is 17 and does NOT move on a Phase 4 row** — that number rises only on an S2 session. `check-classroom-curriculum.py --strict`: no structural findings, **28 stale pins** on the hand-authored lessons (pre-existing and unmoved), and **2 items due for review** — `landscape-utilities-2026-09` and `landscape-in-hall-power-2026-09`, both at 2026-10-01, both by design and both the quarterly Routine's. Session 8's own module took **2026-10-31 rather than 2026-10-01 deliberately**, partly so this count would stay at 2. **Do not "fix" either.** `check-classroom-pipeline.py --selftest`: **13 fixtures / 0 failures**.

**Versions and counters to bump from.** Classroom GAS **v01.46g** (both `Classroom.gs` `VERSION` and `live-site-pages/gs-versions/Classroomgs.version.txt`); page **v01.14w**, and no page bump is expected. Repo **v05.98r**. Scraper **v02.09g** and **not yours** — a Phase 4 row authors no guidance module, so `industry-guidance.md` step 9 does not fire and `Scraper.gs` stays untouched, which means **one deploy line rather than two**. Profiler v01.39g and never yours.

**THE DEPLOY — the §7.19 order, on its fifteenth use.** Read **Pages** first (`https://lightaisolutions.github.io/Sales/gs-versions/Classroomgs.version.txt` is a static file, free to read as often as you like) and the workflow's own `Classroom deploy confirmed (GET)` line from the job log. **Probe `?op=deploy` only if those two disagree** — the probe *deploys* whenever the running `/exec` bundle is behind GitHub. Use `curl -sL` if you do probe; without `-L` the body is empty and reads as a failure. **Deployment counters: Classroom 57/200 and Scraper 154/200, both MEASURED at v05.97r and confirmed against the job log** — session 8 put both `.gs` files in the merge diff, so **two** steps fired and two lines logged: `Scraper deploy confirmed (GET): Updated to v02.09g (deployment 154) | 154/200` and `Classroom deploy confirmed (GET): Updated to v01.46g (deployment 57) | 57/200`, both on the **first GET leg** with no POLL and no `?op=deploy` probe, with Pages agreeing before the job log was closed. **The fourteenth consecutive clean use of the §7.19 order**, and the two-line forecast held exactly. **The counter rule, unchanged**: `Updated to` consumes one; a step that never fires consumes none and logs nothing; and **`Already up to date` describes the state at READ time and is silent on whether the call consumed a deployment** — never forecast it as zero.

**CHANGELOGS — AND YOU ARE ALMOST CERTAINLY THE ROTATOR, WHICH TWO CONSECUTIVE SESSIONS WERE FORECAST TO BE AND WERE NOT.** Keep using `TZ=America/New_York date '+%Y-%m-%d'` and **trust it over the session harness's own date banner, which follows UTC.** The trap fired for a **second** consecutive session at v05.97r: EST read **2026-09-15 20:44** against UTC's **2026-09-16 00:44**, so the nineteen sections dated 2026-09-15 EST were still exempt and the repo CHANGELOG stood at **99 raw / 80 non-exempt** — twenty clear of the cap, and no rotation. **The rule counts non-exempt sections (total minus today's EST) and fires when that *reaches* 100.** At the close of v05.98r the file stands at **100 raw / 80 non-exempt**, and those twenty stop being exempt the moment EST rolls to 2026-09-16 — taking it to **100 non-exempt, exactly at the cap.** So unless your push also lands on 2026-09-15 EST, **you rotate.** Deepen the clone first so every rotated header enriches, and run the **mandatory post-rotation grep**: v05.83r's enrichment failed silently on the first attempt and only that grep caught it. `Classroomgs.changelog.md` is **46 raw / 38 non-exempt** against 50 and **46 after the roll** — four from its cap and still the closest of the page/GAS three to arming. `Scrapergs.changelog.md` sits at **30 raw / 26 non-exempt**. `Profilerhtml.changelog.md` sits at **49 raw / 49 non-exempt**, one section from its own trigger, and nothing a Phase 4 row does touches Profiler. **Re-count every one of these from the files rather than from this brief.**

**Six open items recorded and not taken.** (i) The `.cl-tbl` `min-width` question now has **fourteen** clean data points — session 8's six-row, three-column bets table and its three-column ten-row indicators table both rendered clean at 1400px, and its fifty-row ledger did too. Column count, row count and tooltip spans are all excluded, and the v05.57r defect stands as a first column holding one long unbreakable word. **Close it** the next time a session has reason to edit `Classroom.html`. (ii) The generated segment lesson's `read-next` **names** the landscape module without linking to it: `clFmt` has no link token at all, and the string is emitted in every segment, so it belongs with (i) plus a developer `--all` run. (iii) **`the-800-vdc-shift`'s `three-chains` intro is now ten inserts stale** — §7.25 reassigned it out of the Phase 4 queue and rows 10, 11 and 12 and S2 sessions 6, 7 and 8 all declined to acquire a claim. It belongs to whichever session opens that lesson **to write in it**. (iv) **§8 item 8's pointer runs only one way on row 12's pair**: `where-batteries-stop`'s `engines-own-the-long-end` states its boundary against `bridge-power`, but `bridge-power` shipped among the first five and cannot refer back. Same disposition as (iii). **Note that this row's own §8 item 8 pair is different** — `the-aidc-power-chain` already exists, so a reverse pointer *is* acquirable and the decision should be recorded either way. (v) **§10.6's session-4 bullet block still sits out of chronological order** (session 4's appears before session 3's). Cosmetic; a future session can fix it deliberately. (vi) **`section 232` scores zero across all 410 Scraper terms and was left out on split grounds** by S2 session 8 — a tariff regime belongs with the policy seeds rather than on a landscape seed. Recorded as a genuine unscored gap for whichever session next opens `Scraper.gs` or the policy modules for its own reason.

> Author Phase 4 row 13 — the `breakers-relays-and-faults` lesson, *Stopping Ten Thousand Amps* — on Opus 5 xhigh as a fresh session. This is ONE lesson. It authors no guidance module, no dossier and no report, creates no track, and regenerates exactly ONE segment: `grid-equipment`.
>
> READ FIRST: repository-information/SESSION-CONTEXT.md (Latest Session); INTEGRATED-REMEDIATION-PLAN.md §7.32 (your brief), §7.30 and §7.31 (for the both-halves due-count rule and the roster-derivation trap), §7.3 order 6 and §6; CLASSROOM-CURRICULUM-PLAN.md §3.1's `breakers-relays-and-faults` block VERBATIM — **its seven section ids and their order are the contract, and its v05.07r amendment adds two inputs and NO section** — plus §7 row 13, §8 items 2, 6, 8 and 9, and §4's track table. Then repository-information/CLASSROOM-SCHEMA.md and .claude/rules/classroom-app.md IN FULL, and .claude/rules/changelog-security.md before you write any public changelog line.
>
> **UNSHALLOW THE CLONE FIRST** — `git fetch --unshallow origin main`, before any measurement and before any pin. This is the Session Start Checklist's "Deepen the clone" bullet, not a favour to this brief. A shallow clone's boundary commit looks like it introduced every file, so `git log -1 --format=%cs -- <path>` returns the boundary's date rather than the file's — and §8 item 2 and G2 make that command the pin source. If `--check` reports a number wildly above the 10 below, suspect the clone before the corpus.
>
> THE WORK — one push commit. (1) Author `clLessonBreakersRelaysAndFaults_()` **inside** the `// CONTENT START` … `// CONTENT END` fence at §3.1's seven section ids in its order, register it at the **end** of `clLessons_()` and **append** it to `electrical-foundations`'s `lessons[]` (the track currently holds `four-machines` and `the-transformer-and-the-substation`; §3.1's position 4 is an append in the built list because `string-versus-central` is row 15 and unbuilt), bumping that track's `updated`. (2) **RE-DERIVE THE INPUT ROSTER RATHER THAN COPYING IT.** §3.1's base four plus its v05.07r amendment's two with no overlap is six, and §7 row 13 lists the same six — but row 11's amendment *overlapped* its base and a straight addition over-counted, so do the arithmetic. (3) **§8 ITEM 8 NAMES THIS ROW'S BOUNDARY IN ADVANCE**, the only Phase 4 row for which it does: `study:eaton` is cited **for fault-energy sizing only** and selective coordination stays in `the-aidc-power-chain`. Open that lesson and read what it actually teaches before drafting; **unlike row 12's pair, the neighbour already exists, so a reverse pointer IS acquirable — decide and record it either way.** (4) Every pin read off the document fetched that run (G2) and every ref resolved by opening the file and matching its own `slug` (G7). (5) Regenerate **only** `--segment grid-equipment`. (6) Flip the §7.3 order-6 row and the curriculum plan's §7 row 13 and §4 track table, all dated. (7) Write §7.33 — the next run's brief, which by the interleave is **S2 session 9, `bridge-and-on-site-generation`** — in the same commit, and **edit BOTH halves of any §7.x brief you refresh**: the prose paragraphs and the quoted prompt block.
>
> PREDICT BOTH HALVES OF THE DUE COUNT, AND THIS ROW'S SECTION HALF IS A DEEPENING AT BIRTH — the first of its kind. §10.6 (w) requires both halves. **`grid-equipment` is NOT among the standing ten** (S2 session 8 regenerated it) and it is the **only** segment whose `READ_NEXT` names this lesson, so the membership half is **10 → 11 → regenerate → 10**. The section half is **`read-next` AND `what-is-bought-and-on-what`**, derived rather than guessed: `lesson_ref()` returns `*Title*` when an id is registered and `*Title* (planned)` otherwise, and this id appears in **both** strings because it is in `CRITERION_LEXICON` (`breaker`, `fault`, `protection`, `relay`, `arc`) **and** in that segment's `READ_NEXT`, with criterion 4 reading *"Protection and automation integration (IEC 61850) and the coordination study"*. Row 12's gate is what governs: **the membership half is bounded by `READ_NEXT`; the section half by `READ_NEXT` ∩ `CRITERION_LEXICON`** — and this is the first row for which that intersection is non-empty. The standing ten are `cells-and-chemistry`, `clean-firm-and-nuclear`, `compute-and-the-rack`, `epc-and-construction`, `storage-developers-and-ipps`, `hyperscalers-and-ai-labs`, `neoclouds`, `capital`, `software-and-optimization`, `insurance-and-risk-transfer`; none is yours under §10.9. Record which halves actually fired.
>
> DO NOT: author or revise any guidance module — `check-classroom-content.py`'s guidance-module assertion is **17 and must stay 17**; it rises only on an S2 session. Do not hand-edit a generated `segment-*` literal for any segment but `grid-equipment`, and run the generator only with `--segment grid-equipment` (§10.4, §10.9). Do not touch `Profiler.gs`, `Profiler.html` or `Scraper.gs` — a Phase 4 row fires no interest seed, so expect **one** deploy line rather than two. Do not refresh `gateDigest` — a lesson inside the fence moves no `GATE_SYMBOLS` member, so P3 must be silent, and refreshing would hide a real signal. Do not "fix" the two items the curriculum checker reports as due for review: `landscape-utilities-2026-09` and `landscape-in-hall-power-2026-09` are **both at 2026-10-01 and both by design**, and both belong to the quarterly Routine.
>
> VERIFY: `node --check` on a `.js` copy of `Classroom.gs`; `scripts/check-gas-inner-scripts.js`; `scripts/check-classroom-content.py` — **capture a pristine HEAD baseline BEFORE any edit. IT IS 0 ERRORS / 0 WARNINGS at 42 lessons / 8 tracks / 142 gate cases** and must be 0/0 at **43 lessons** after, with the module assertion **unmoved at 17**. Watch the micro-markup check: it has now caught a nested-emphasis defect in three separate sessions, most recently a `*both*` inside a bold span, and nothing else in the toolchain sees it. Then `scripts/check-classroom-curriculum.py --strict` (no structural findings; 28 stale pins and 2 items due for review are all pre-existing and not yours); `scripts/check-classroom-pipeline.py --selftest` (13 fixtures, 0 failures) and `--base origin/main` (expect **P1 alone** — no P5 because this row *appends*, no P3, no P7, and **no P8, which cannot fire on a head-only lesson**); `build-classroom-segments.py --check` — expect **10 → 11 → 10**, `grid-equipment` joining and leaving on **both** `read-next` and `what-is-bought-and-on-what`. `scripts/check-readme-tree.py` after the version bump. Playwright render of the new lesson at **analyst** (the stamp is all-public so it folds to `tracks`, which makes analyst the POSITIVE test) with ZERO page errors, ops answered by the REAL serving functions — the lesson entrypoint is `clShowLesson(id)`, `_gasPost` returns a PROMISE, `sess.permissions` must be an ARRAY in the shim, the session token must be at least 32 characters or the real guard returns SESSION_EXPIRED, the session keys are `Classroom_gas_session_token` / `_gas_user_email` / `_gas_user_role` / `_gas_user_permissions`, overlays to hide are `#auth-wall` / `#signing-out-wall` / `#auth-timers` / `#gas-layer-toggle`, and the scratch site root needs `favicon.ico` copied in beside `profiler-data/`, `sounds/` and `images/`. **SERVE THE SCRATCH COPY AT `/Classroom.html`, NOT AT THE SITE ROOT** — the page derives its sessionStorage key prefix from the URL path, and serving at `/` silently sends the seeded session to `index_gas_*` keys and renders as a role denial. Then READ EVERY SECTION'S SCREENSHOT — a `proscons` card's `t` and `meta` are `textContent`, so a `{{term}}` in either prints its braces to the reader, and `killing-the-arc` is a proscons section. Bump Classroom's GAS version in BOTH `Classroom.gs` `VERSION` (from `v01.46g`) and `live-site-pages/gs-versions/Classroomgs.version.txt`, with a GENERIC entry in `Classroomgs.changelog.md`.
>
> THE DEPLOY and THE CHANGELOG ARITHMETIC: exactly as §7.32's paragraphs set out. **Confirm the deploy from Pages and the job log, NOT by probing `?op=deploy` first.** Deployment counters **Classroom 57/200 and Scraper 154/200, both measured at v05.97r and confirmed against the job log** — that session put both `.gs` files in the merge diff, so two steps fired and two lines logged (`Updated to v02.09g (deployment 154) | 154/200` and `Updated to v01.46g (deployment 57) | 57/200`), both on the first GET leg with no POLL and no probe — the fourteenth consecutive clean use of the §7.19 order. Yours touches Classroom only, so expect **one** line. **THE EST/UTC TRAP HAS NOW FIRED ON TWO CONSECUTIVE SESSIONS AND DECIDED THE ROTATION BOTH TIMES**: at v05.97r EST read 2026-09-15 20:44 against UTC's 2026-09-16 00:44, the harness's own date banner follows UTC, and a session trusting it would have rotated with no trigger. Repo CHANGELOG **100 raw / 80 non-exempt** with twenty sections dated 2026-09-15 EST — **100 non-exempt the moment EST rolls past that date, exactly at the cap, so unless your push also lands on 2026-09-15 EST you ARE the rotator.** Re-count it yourself on the EST clock (`TZ=America/New_York date`), deepen the clone first so every rotated header enriches, and **run the mandatory post-rotation grep** — v05.83r's enrichment failed silently on the first attempt and only that grep caught it. `Classroomgs.changelog.md` is 46 raw / 38 non-exempt against 50 and is still the closest of the page/GAS three to arming. `Scrapergs.changelog.md` sits at 30 raw / 26 non-exempt, and `Profilerhtml.changelog.md` at 49 raw / 49 non-exempt — one section from its own trigger, and nothing a Phase 4 row does touches Profiler. **Re-count from the files rather than from any brief.**
>
> Normal Pre-Commit and Pre-Push checklists; `claude/*` branch restarted from `origin/main` first, `git ls-remote` before each push — the auto-merge workflow adds a `[skip ci]` SHA-tracker commit after every merge, and after it merges use `git fetch --prune origin` rather than `git fetch origin main`, or the stale remote-tracking ref makes the stop hook report that SHA-tracker commit as unpushed.
>
> AFTER THIS SESSION Phase 4 is 13 of 26 and S2 is 8 of ~19, so the interleave returns to S2 and the recommended next run is **S2 session 9 — `bridge-and-on-site-generation`** — for which §7.33 should be written.

### 7.33 · Paste-in brief for S2 session 9 — `bridge-and-on-site-generation` (Opus 5 xhigh) — written 2026-09-15 at v05.99r, at the close of Phase 4 row 13 *(counters refreshed at v06.00r)*

**Why this and not another Phase 4 row.** Phase 4 is now **13 of 26** and S2 is **8 of ~19**, so the interleave stands at 13:8 and §7.4 item 6 hands the turn back to S2. Its next segment in the §7.3 sales-value order is **`bridge-and-on-site-generation`** — the ninth landscape, the **eighteenth** guidance module, and the one segment whose mechanism lessons are the *most* built of any remaining: two of its three `READ_NEXT` entries already exist.

**UNSHALLOW FIRST — a Session Start Checklist item since v05.83r, not a brief's favour.** `git fetch --unshallow origin main` before any pin read, any `--check` run, any `git log -1 --format=%cs` date and any CHANGELOG rotation. Row 13 ran it as the checklist's "Deepen the clone" bullet (**1,257 commits**) and `--check` opened at the true 10 with `inputs moved: none` on every line.

**THE SEGMENT, MEASURED 2026-09-15 AT v05.99r — but re-measure it on the day.** `profiler-segments.json` gives `bridge-and-on-site-generation` **17 members: 8 incumbents · 5 challengers · 4 adjacents.** Incumbents: `ge-vernova`, `siemens-energy`, `mitsubishi-power`, `caterpillar`, `cummins`, `wartsila`, `voltagrid`, `enchanted-rock`. Challengers: `bloom-energy`, `rolls-royce-power-systems`, `rehlko`, `proenergy`, `mainspring-energy`. Adjacents: `piller`, `eaton`, `nrg-energy`, `engie-north-america`. **All seventeen carry a dossier** — checked file by file at v05.99r, zero missing — so the claims ledger has a `profile:<slug>` for every member. `each-players-bet` takes **one row per incumbent and challenger = 13 rows**, between session 8's six and session 6's twenty-two. The segment carries **no `notes` field**, so session 8's §10.6 (l) route — reading the registry's own note to split a role — is **not available here**; the roles have to be read from the dossiers.

**THE SECTION CONTRACT IS §10.6'S NINE, IN ITS ORDER.** `who-dominates-and-on-what-basis` (prose) · `who-threatens` (prose) · `each-players-bet` (table, **13 rows**, trim nothing) · `the-indicators` (table) · `the-sellers-play` (callout) · `claims-ledger` (kind `ledger`) · `what-the-record-does-not-say` (callout) · `drill` · `check-yourself`. Id `landscape-bridge-and-on-site-generation-2026-09`, lane **`The Value Chain`**, tier **contributor**, authored **below** the `// CONTENT END` fence and registered at the **end** of `guidanceDocs_()`'s Value Chain lane. Write §2 of the analysis markdown — **the split** — before any other section, per §10.6 (j); session 8 did it literally, as the first text in the file, with the front matter prepended afterwards.

**BUMP THE GUIDANCE-MODULE ASSERTION FROM 17 TO 18.** `scripts/check-classroom-content.py` line ~885 reads `expected 17`; it is the **one edit outside the fence** a landscape session makes and it is mandatory per §10.6 (c). Row 13 left it at 17 because a Phase 4 row authors no module — **that number rises only on an S2 session, and this is one.**

**PREDICT BOTH HALVES, AND THE SECTION HALF IS DERIVED FROM THE GENERATOR RATHER THAN THE PROSE.** `--check` should read **10 due** as you start — `cells-and-chemistry`, `clean-firm-and-nuclear`, `compute-and-the-rack`, `epc-and-construction`, `storage-developers-and-ipps`, `hyperscalers-and-ai-labs`, `neoclouds`, `capital`, `software-and-optimization`, `insurance-and-risk-transfer`. **`bridge-and-on-site-generation` is NOT among them**, so registering its landscape makes it due and the membership half is **10 → 11 → regenerate → 10**. The section half is **`read-next` alone**: `self.landscapes` is read at **exactly one place** in the generator, inside `sec_read_next` (line 542), so a landscape registration cannot touch any other section. That was session 8's finding and it was re-verified in the code at v05.99r. **Do not import row 13's deepening**: that was a *lesson* registration reaching `CRITERION_LEXICON`, a different mechanism entirely. Regenerate **only** `--segment bridge-and-on-site-generation`; the standing ten belong to their own sessions under §10.9.

**THE `reviewBy` SORT WILL PROBABLY RETURN NOTHING — SESSION 3'S (g) CASE, FOR A THIRD TIME, AND THE ARITHMETIC IS ALREADY DONE.** Across all seventeen members' `policyExposure[]` there are **11 dated entries and not one of them is in the future** (measured 2026-09-15 at v05.99r). So the nearest dated gate almost certainly is not in the policy arrays, and the answer will have to come from a member's own dossier prose — session 8 found **2026-10-31** inside `profile:grid-united`'s `strategyRead[4]`, a field no session had found a gate in before. **Look in `strategyRead[]`, in the developments the module actually leans on, and in the fuel-and-permitting material this segment is full of** — Tier 4 hours, non-attainment deadlines and firm-gas contract dates are exactly the kind of thing this roster states. **Re-measure rather than trust this paragraph**, write your rejections down, and if nothing dated survives, take six months from `updated` and say so. **One arithmetic constraint to respect**: the curriculum checker currently reports **2** items due for review (`landscape-utilities-2026-09` and `landscape-in-hall-power-2026-09`, both 2026-10-01). A `reviewBy` inside your own 30-day horizon makes it 3 — session 8 avoided that deliberately by choosing 31 October over 1 October. Choose knowingly either way.

**THE BOUNDARY THIS SEGMENT HAS THAT NO OTHER LANDSCAPE HAS HAD: two of its three `READ_NEXT` lessons are built, and one of them carries an OPEN DEBT to this exact material.** `READ_NEXT["bridge-and-on-site-generation"]` is `["bridge-power", "backup-generation", "grid-stability-and-the-generator"]` — `bridge-power` and `backup-generation` are both built; `grid-stability-and-the-generator` is Phase 4 **row 14** and unbuilt, so it will still render `(planned)`. **Open both built lessons and read them before drafting.** And note the standing open item this segment finally touches: **§7.32's item (iv) — `bridge-power` needs the reverse pointer to `where-batteries-stop`'s engine table** (§8 item 8 says the engine table appears once, in track 1, and track 3 refers to it; `bridge-power` shipped among the first five and cannot refer back). **That debt is a lesson edit, and an S2 session does not edit lessons** — so it is still not yours. But you are the session with the best reason to have read `bridge-power` closely, so **say in the §8 record whether the debt is still real after your read**, which is worth more than another session guessing.

**§10.6 (t) — the pre-declaration test — has a live candidate here, and it is the strongest yet.** `bridge-power` teaches the bridge-generation *mechanism*, and this landscape ranks the companies that sell it. Session 8's test held on the transformer lead time: a **market condition** taught vendor-blind belongs in the mechanism lesson, and **one named vendor's own instrument** belongs in the landscape. Apply the same line to this segment's first buying criterion — *"Time to power — the production slot, reservation fees, trailer-mounted mobility"* — which is a slot-reservation instrument almost word for word, and check whether `bridge-power` already pre-declares it.

**The six buying criteria, for the `the-indicators` and `the-sellers-play` sections** (read them off the registry on the day, they are quoted here for scope only): time to power and the production slot · cost per kW and heat rate at the real operating point · emissions permitting, NOx without SCR, Tier 4 hours, non-attainment · fuel logistics and firm gas supply · block-load and ride-through against a swinging AI hall · rating (standby / prime / continuous) and the O&M and dealer network. **Criterion 5 is the one this curriculum teaches best and the one a seller is least likely to raise**, and criterion 3 is where the challengers' whole case lives.

**THE SCRAPER SEED — step 9 of `industry-guidance.md`, and it DOES fire for an S2 session.** Add `topic-landscape-bridge-and-on-site-generation` to `Scraper.gs` and bump it (**v02.09g** today), which puts **two** `.gs` files in the merge diff and therefore **two** deploy steps and **two** logged lines. Session 8's prior was right in both halves for the first time — negative on the calendar vocabulary, positive on the machines — so **score your candidate terms against all of the existing seed arrays before adding any**, and drop superstrings, generic words and company names. **One term is already known unscored and waiting**: §7.32's item (vi) records that **`section 232` scores zero across all 410 Scraper terms** and was left out on split grounds by session 8 as a tariff regime belonging with the policy seeds. This segment is full of tariff-exposed engine imports — **decide it deliberately this time and record the decision either way.**

**The baselines to capture BEFORE any edit, all measured at the close of v05.99r.** `check-classroom-content.py` is **0 errors / 0 warnings at 43 lessons, 8 tracks, 142 gate cases** — held unbroken through thirteen consecutive sessions, and your commit must return it to 0/0 at **43 lessons** (an S2 session authors no lesson) with the **module assertion moved to 18**. `check-classroom-curriculum.py --strict`: no structural findings, **28 stale pins** on the hand-authored lessons (pre-existing and unmoved — row 13 added six fresh pins and the count did not move, so the 28 are all older lessons), and **2 items due for review**, both at 2026-10-01 and both by design. **Do not "fix" either.** `check-classroom-pipeline.py --selftest`: **13 fixtures / 0 failures**.

**EXPECT P1 *AND* P7, AND KNOW WHY BEFORE YOU SEE IT.** Row 13 found something new: `--base origin/main` reported **P7 on `segment-grid-equipment`** — *content changed but `updated` did not advance* — because S2 session 8 had regenerated that same segment **earlier on the same EST day**, so base and head both read `2026-09-15`. **The rule that generalises: any segment regenerated twice within one EST day fails P7, and there is no legitimate way to clear it** — the UTC date is the date this repo refuses, and hand-editing a generated literal is forbidden by §10.4 and §10.9. It is **pipeline-rule noise on a developer commit**, the same class as P1's write-set findings, because the pipeline by construction never writes a `segment-*` lesson. **You are at risk of it only if `bridge-and-on-site-generation` was regenerated earlier on your own EST day** — it was not regenerated on 2026-09-15, so a push on 2026-09-16 or later should see **P1 alone**. Check the literal's `updated` against your EST date before you worry about it.

**Versions and counters to bump from.** Classroom GAS **v01.47g** (both `Classroom.gs` `VERSION` and `live-site-pages/gs-versions/Classroomgs.version.txt`); page **v01.14w**, and no page bump is expected. **Repo version: read `repository-information/repository.version.txt` on the day and bump it by 0.01 — do not take the number from this brief.** This is the one counter a brief structurally cannot state: the handover commit that finishes writing the brief consumes the next version itself, so any number written here is stale before the brief is read. *(Written as `v06.00r` in the v05.99r work commit; the v06.00r handover took it. The claim is **removed** rather than corrected, because correcting it would consume another version and re-create the error — the first time this recursion has been closed rather than patched.)* Scraper **v02.09g** and **yours this time**. Profiler v01.39g and never yours.

**THE DEPLOY — the §7.19 order, on its sixteenth use.** Read **Pages** first (`https://lightaisolutions.github.io/Sales/gs-versions/Classroomgs.version.txt` is a static file, free to read as often as you like) and the workflow's own `Classroom deploy confirmed (GET)` line from the job log. **Probe `?op=deploy` only if those two disagree** — the probe *deploys* whenever the running `/exec` bundle is behind GitHub. Use `curl -sL` if you do probe; without `-L` the body is empty and reads as a failure. **Deployment counters: Classroom 58/200 and Scraper 154/200, Classroom's now MEASURED and confirmed against the job log.** Row 13 put only `Classroom.gs` in the merge diff, so exactly **one** step fired and exactly one line logged — `Classroom deploy confirmed (GET): Updated to v01.47g (deployment 58) | 58/200` — on the **first GET leg** with no POLL and no `?op=deploy` probe, with Pages reading `|v01.47g|` in agreement before the job log was closed. **The fifteenth consecutive clean use of the §7.19 order**, and the one-line forecast held exactly. Scraper's **154/200 is a measurement carried forward from v05.97r** and does not move until a session touches `Scraper.gs` — which yours does, so expect **two** lines. **Confirm both against the job log and refresh this paragraph in both halves if either differs.** **The counter rule, unchanged**: `Updated to` consumes one; a step that never fires consumes none and logs nothing; and **`Already up to date` describes the state at READ time and is silent on whether the call consumed a deployment** — never forecast it as zero.

**CHANGELOGS — AND YOU ARE THE ROTATOR UNLESS YOUR PUSH LANDS ON 2026-09-15 EST, WHICH IT ALMOST CERTAINLY WILL NOT.** Keep using `TZ=America/New_York date '+%Y-%m-%d'` and **trust it over the session harness's own date banner, which follows UTC.** The trap has now fired on **three consecutive sessions** and decided the rotation every time: at v05.99r EST read **2026-09-15 21:42** against UTC's **2026-09-16 01:42**, and the harness banner said 2026-09-16. §7.32 forecast row 13 as the rotator; the push landed on 2026-09-15 EST, the twenty-one same-day sections stayed exempt, and the work commit closed at **101 raw / 80 non-exempt** and the handover at **102 raw / 80 non-exempt** — twenty clear of the cap either way, with the counter legitimately reading `Sections: 102/100` (step 3 of `CHANGELOG-archive.md` allows the file to sit above 100 on a busy day and self-correct after midnight). **The moment EST rolls past 2026-09-15 those twenty-two stop being exempt and the file is at 102 non-exempt — past the 100 trigger — so unless your push also lands on 2026-09-15 EST, you rotate, and you rotate the oldest whole date group.** Deepen the clone first so every rotated header enriches, and run the **mandatory post-rotation grep** (`grep '^## \[v' CHANGELOG-archive.md | grep -v '— \[' | head -5`) — v05.83r's enrichment failed silently on the first attempt and only that grep caught it. `Classroomgs.changelog.md` is **47 raw / 38 non-exempt** against 50 and **47 after the roll** — three from its cap and still the closest of the page/GAS three to arming; **your push takes it to 48, so check it too.** `Scrapergs.changelog.md` sits at **30 raw / 26 non-exempt** and yours adds one. `Profilerhtml.changelog.md` sits at **49 raw / 49 non-exempt**, one section from its own trigger, and nothing an S2 session does touches Profiler. **Re-count every one of these from the files rather than from this brief.**

**Six open items recorded and not taken.** (i) The `.cl-tbl` `min-width` question now has **fifteen** clean data points — row 13's five-row, four-column `where-it-fails` table rendered clean at 1400px with no horizontal overflow, and the v05.57r defect still stands as a first column holding one long unbreakable word. **Close it** the next time a session has reason to edit `Classroom.html`. (ii) The generated segment lesson's `read-next` **names** the landscape module without linking to it: `clFmt` has no link token at all, so it belongs with (i) plus a developer `--all` run. (iii) **`the-800-vdc-shift`'s `three-chains` intro is now eleven inserts stale** — rows 10–13 and S2 sessions 6–8 all declined to acquire a claim. It belongs to whichever session opens that lesson **to write in it**, and row 13 is a near miss: its `dc-has-no-zero` section is the protection half of the 800 VDC argument and points at it in prose without editing it. (iv) **§8 item 8's `bridge-power` → `where-batteries-stop` pointer still runs one way** — see the paragraph above; your session is the one best placed to *report* on it. (v) **§10.6's session-4 bullet block still sits out of chronological order** (session 4's appears before session 3's) — cosmetic, and a future session can fix it deliberately. (vi) **`section 232` scores zero across all 410 Scraper terms** — see the Scraper paragraph; this is the segment where it finally has a natural home, so decide it rather than deferring it again.

> Author S2 session 9 — the `bridge-and-on-site-generation` landscape module — on Opus 5 xhigh as a fresh session. This is ONE guidance module. It authors no lesson, no dossier and no report, creates no track, and regenerates exactly ONE segment: `bridge-and-on-site-generation`.
>
> READ FIRST: repository-information/SESSION-CONTEXT.md (Latest Session); INTEGRATED-REMEDIATION-PLAN.md §7.33 (your brief), §7.31 and §7.32 (for the both-halves due-count rule, the `reviewBy` read-don't-sort case and the new P7 same-day rule), §7.3 order 5 and §6; CLASSROOM-CURRICULUM-PLAN.md §10.6 IN FULL — **its nine section ids and their order are the contract** — plus §10.2's floor table, §10.4, §10.9, §10.10's two role paths and §8 item 2. Then .claude/rules/industry-guidance.md and .claude/rules/classroom-app.md IN FULL, repository-information/CLASSROOM-SCHEMA.md, and .claude/rules/changelog-security.md before you write any public changelog line.
>
> **UNSHALLOW THE CLONE FIRST** — `git fetch --unshallow origin main`, before any measurement and before any pin. This is the Session Start Checklist's "Deepen the clone" bullet, not a favour to this brief. A shallow clone's boundary commit looks like it introduced every file, so `git log -1 --format=%cs -- <path>` returns the boundary's date rather than the file's. If `--check` reports a number wildly above the 10 below, suspect the clone before the corpus.
>
> THE WORK — one push commit. (1) Author `guidanceDocLandscapeBridgeAndOnSiteGeneration_()` **below** the `// CONTENT END` fence at §10.6's nine section ids in its order — `who-dominates-and-on-what-basis` · `who-threatens` · `each-players-bet` (**13 rows**, eight incumbents plus five challengers, trim nothing) · `the-indicators` · `the-sellers-play` · `claims-ledger` · `what-the-record-does-not-say` · `drill` · `check-yourself` — id `landscape-bridge-and-on-site-generation-2026-09`, lane `The Value Chain`, tier contributor, and register it at the **end** of `guidanceDocs_()`. (2) Write the analysis markdown at `repository-information/industry-guidance/landscape-bridge-and-on-site-generation-analysis.md` as its source of truth, and **write §2 — the split — before any other section**. (3) **Bump the guidance-module assertion in `scripts/check-classroom-content.py` from 17 to 18** — the one edit outside the fence a landscape session makes, mandatory per §10.6 (c), and the number row 13 deliberately left alone. (4) Re-measure the segment on the day (the registry says **17 members: 8 · 5 · 4**, and all seventeen carried a dossier at v05.99r) and pin every claim in the ledger to `profile:<slug>` at its `profileVersion` plus the dossier field it rests on. (5) Add the `topic-landscape-bridge-and-on-site-generation` seed to `Scraper.gs` per step 9, scoring candidate terms against every existing seed array first. (6) Regenerate **only** `--segment bridge-and-on-site-generation`. (7) Flip the §7.3 S2 row and §10.6's progress note, dated. (8) Write §7.34 — the next run's brief, which by the interleave is **Phase 4 row 14, `grid-stability-and-the-generator`** — in the same commit, and **edit BOTH halves of any §7.x brief you refresh**: the prose paragraphs and the quoted prompt block.
>
> PREDICT BOTH HALVES OF THE DUE COUNT. §10.6 (w) requires both halves. **`bridge-and-on-site-generation` is NOT among the standing ten**, so registering its landscape makes it due and the membership half is **10 → 11 → regenerate → 10**. The section half is **`read-next` alone**, derived from the generator rather than guessed: `self.landscapes` is read at **exactly one place**, inside `sec_read_next` (line 542), so a landscape registration cannot touch any other section — re-verified in the code at v05.99r. **Do not import row 13's deepening**: that fired because a *lesson* id sat in both a segment's `READ_NEXT` and in `CRITERION_LEXICON`, which a landscape registration never does. The standing ten are `cells-and-chemistry`, `clean-firm-and-nuclear`, `compute-and-the-rack`, `epc-and-construction`, `storage-developers-and-ipps`, `hyperscalers-and-ai-labs`, `neoclouds`, `capital`, `software-and-optimization`, `insurance-and-risk-transfer`; none is yours under §10.9. Record which halves actually fired.
>
> THE `reviewBy` SORT WILL PROBABLY RETURN NOTHING — session 3's (g) case for a third time. Across all seventeen members' `policyExposure[]` there are **11 dated entries and not one in the future**, measured 2026-09-15. Session 8 found its gate in `profile:grid-united`'s `strategyRead[4]`, a field no session had found one in before — so look in `strategyRead[]`, in the developments the module leans on, and in this segment's fuel-and-permitting material (Tier 4 hours, non-attainment deadlines, firm-gas contract dates). **Re-measure rather than trust this**, write your rejections down, and if nothing dated survives take six months from `updated` and say so. One constraint: the curriculum checker reports **2** items due for review today and a `reviewBy` inside your own 30-day horizon makes it 3 — session 8 avoided that deliberately. Choose knowingly.
>
> TWO OF THIS SEGMENT'S THREE `READ_NEXT` LESSONS ARE BUILT, which no previous landscape has had. `READ_NEXT` is `["bridge-power", "backup-generation", "grid-stability-and-the-generator"]`; the first two exist and the third is Phase 4 row 14 and will still render `(planned)`. **Open both built lessons and read them before drafting**, and apply §10.6 (t)'s pre-declaration test to criterion 1 — *"Time to power — the production slot, reservation fees, trailer-mounted mobility"* — which is a slot-reservation instrument almost word for word. Session 8's line holds: a **market condition** taught vendor-blind belongs in the mechanism lesson; **one named vendor's own instrument** belongs here. **Also report on §7.32's open item (iv)** — `bridge-power` still owes a reverse pointer to `where-batteries-stop`'s engine table under §8 item 8, and an S2 session does not edit lessons, so it is not yours to fix; but you are the session with the best reason to have read `bridge-power` closely, so say in the §8 record whether the debt is still real.
>
> DO NOT: author or revise any lesson — `check-classroom-content.py` must stay at **43 lessons / 8 tracks / 142 gate cases**, and only the module assertion moves (17 → 18). Do not hand-edit a generated `segment-*` literal for any segment but `bridge-and-on-site-generation`, and run the generator only with `--segment bridge-and-on-site-generation` (§10.4, §10.9). Do not touch `Profiler.gs` or `Profiler.html`. Do not refresh `gateDigest` — a module below the fence moves no `GATE_SYMBOLS` member, so P3 must be silent, and refreshing would hide a real signal. Do not "fix" the two items the curriculum checker reports as due for review: `landscape-utilities-2026-09` and `landscape-in-hall-power-2026-09` are **both at 2026-10-01 and both by design**.
>
> VERIFY: `node --check` on a `.js` copy of `Classroom.gs` and of `Scraper.gs`; `scripts/check-gas-inner-scripts.js`; `scripts/check-classroom-content.py` — **capture a pristine HEAD baseline BEFORE any edit. IT IS 0 ERRORS / 0 WARNINGS at 43 lessons / 8 tracks / 142 gate cases** and must be 0/0 at 43 lessons after, with the module assertion **moved to 18**. Watch the micro-markup check: it has caught a nested-emphasis defect in three separate sessions and nothing else in the toolchain sees it — and it covers the `guidanceDoc*_()` literals too. Then `scripts/check-classroom-curriculum.py --strict` (no structural findings; 28 stale pins and 2 items due for review are pre-existing and not yours); `scripts/check-classroom-pipeline.py --selftest` (13 fixtures, 0 failures) and `--base origin/main` — **expect P1 alone**, but read §7.33's P7 paragraph first: a segment regenerated twice within one EST day fails P7 and there is no legitimate way to clear it. `build-classroom-segments.py --check` — expect **10 → 11 → 10**, `bridge-and-on-site-generation` joining and leaving on `read-next` alone. `scripts/check-readme-tree.py` after the version bumps. Playwright render of the new module at **contributor** (§10.6's tier — and confirm an analyst is denied, which is the negative half) with ZERO page errors, ops answered by the REAL serving functions — the guidance entrypoint is `clShowGuidanceDoc(id)`, `_gasPost` returns a PROMISE, `sess.permissions` must be an ARRAY in the shim, the session token must be at least 32 characters or the real guard returns SESSION_EXPIRED, the session keys are `Classroom_gas_session_token` / `_gas_user_email` / `_gas_user_role` / `_gas_user_permissions`, overlays to hide are `#auth-wall` / `#signing-out-wall` / `#auth-timers` / `#gas-layer-toggle`, and the scratch site root needs `favicon.ico` copied in beside `profiler-data/`, `sounds/` and `images/`. **SERVE THE SCRATCH COPY AT `/Classroom.html`, NOT AT THE SITE ROOT** — the page derives its sessionStorage key prefix from the URL path, and serving at `/` silently sends the seeded session to `index_gas_*` keys and renders as a role denial. **Call `handleClassroomOp_` through the vm context object (`ctx.handleClassroomOp_`), not from Node's own scope** — row 13 lost a cycle to that. Then READ EVERY SECTION'S SCREENSHOT. Bump Classroom's GAS version in BOTH `Classroom.gs` `VERSION` (from `v01.47g`) and `Classroomgs.version.txt`, and Scraper's in BOTH `Scraper.gs` `VERSION` (from `v02.09g`) and `Scrapergs.version.txt`, each with a GENERIC entry in its changelog.
>
> THE DEPLOY and THE CHANGELOG ARITHMETIC: exactly as §7.33's paragraphs set out. **Confirm the deploy from Pages and the job log, NOT by probing `?op=deploy` first.** Deployment counters **Classroom 58/200 (MEASURED at v05.99r — one step fired and one line logged, `Updated to v01.47g (deployment 58) | 58/200`, on the first GET leg with no POLL and no probe) and Scraper 154/200 (measured at v05.97r)**; yours touches both `.gs` files, so expect **two** lines. **THE EST/UTC TRAP HAS NOW FIRED ON THREE CONSECUTIVE SESSIONS AND DECIDED THE ROTATION ALL THREE TIMES**: at v05.99r EST read 2026-09-15 21:42 against UTC's 2026-09-16 01:42, the harness's own date banner said 2026-09-16, and a session trusting it would have rotated with no trigger. Repo CHANGELOG **102 raw / 80 non-exempt** with twenty-two sections dated 2026-09-15 EST — **102 non-exempt the moment EST rolls past that date, past the cap, so unless your push also lands on 2026-09-15 EST you ARE the rotator and you rotate the oldest whole date group.** Re-count it yourself on the EST clock (`TZ=America/New_York date`), deepen the clone first so every rotated header enriches, and **run the mandatory post-rotation grep** — v05.83r's enrichment failed silently on the first attempt and only that grep caught it. `Classroomgs.changelog.md` is 47 raw / 38 non-exempt against 50 and yours takes it to 48 — three from its cap and the closest of the page/GAS three to arming. `Scrapergs.changelog.md` sits at 30 raw / 26 non-exempt and yours adds one, and `Profilerhtml.changelog.md` at 49 raw / 49 non-exempt — one section from its own trigger, and nothing an S2 session does touches Profiler. **Re-count from the files rather than from any brief.**
>
> Normal Pre-Commit and Pre-Push checklists; `claude/*` branch restarted from `origin/main` first, `git ls-remote` before each push — the auto-merge workflow adds a `[skip ci]` SHA-tracker commit after every merge, and after it merges use `git fetch --prune origin` rather than `git fetch origin main`, or the stale remote-tracking ref makes the stop hook report that SHA-tracker commit as unpushed. **Read `repository-information/repository.version.txt` for the version to bump from — do not take it from this brief**, whose own handover commit consumes the next number.
>
> AFTER THIS SESSION Phase 4 is 13 of 26 and S2 is 9 of ~19, so the interleave returns to Phase 4 and the recommended next run is **Phase 4 row 14 — `grid-stability-and-the-generator`** — for which §7.34 should be written.

### 7.34 · Paste-in brief for Phase 4 row 14 — `grid-stability-and-the-generator` (Opus 5 xhigh) — written 2026-09-15 at v06.03r, at the close of S2 session 9

**Why this and not another S2 segment.** S2 is now **9 of ~19** and Phase 4 is **13 of 26**, so the interleave stands at 13:9 and §7.4 item 6 hands the turn back to Phase 4. Its next unbuilt row is **row 14, `grid-stability-and-the-generator`** — *Inertia, Reactive Power, and Teaching Electronics to Lead* — the **forty-fourth** lesson, at §4 position **5** of `electrical-foundations`, and **the lesson that closes the floor track**. Positions 1, 3 and 4 are built (`four-machines`, `the-transformer-and-the-substation`, `breakers-relays-and-faults`); position 2, `string-versus-central`, is row 15 and still unbuilt.

**UNSHALLOW FIRST — a Session Start Checklist item, not a brief's favour.** `git fetch --unshallow origin main` before any pin read, any `--check` run, any `git log -1 --format=%cs` date and any CHANGELOG rotation. Session 9 ran it as the checklist's "Deepen the clone" bullet and took the clone to **1,265 commits**; `--check` then opened at the true 10 with `inputs moved: none` on every line, and the rotation's SHA lookups all resolved.

**THE ROSTER — eight inputs, base five plus the v05.07r amendment's three, overlap zero. Re-verify it anyway.** §3.1's spec names `study:ge-vernova` (generator physics — excitation, 3,600 rpm, torque angle against excitation as two independent knobs, hydrogen cooling) · `study:black-veatch` (inertia, reactive power, synchronous condensers, the afterlife of coal plants) · `study:hitachi-energy` (grid-following against grid-forming; the stability ceiling; forming as a tender requirement) · `study:sinexcel` (frequency as the live balance meter) · `concepts:profiler-concepts`; the amendment adds `study:siemens-energy` (`holding-the-grid-up` — inertia and system strength, the condenser against the {{STATCOM}}) · `study:piller` (`the-stiff-source` — a synchronous machine's fault contribution seen from the UPS room) · `study:power-electronics` (`following-and-forming`, `ride-through`, `two-rulebooks`). §7 row 14's own list is the same eight. **All seven study guides were confirmed present at v06.03r** with `lastUpdated` 2026-08-21 (GE Vernova, Black & Veatch, Hitachi Energy), 2026-08-08 (Sinexcel), 2026-09-04 (Siemens Energy), 2026-09-03 (Piller) and 2026-09-06 (Power Electronics); the concepts file's last commit date is 2026-09-13. **Read every pin off the fetched document (G2), not off this paragraph** — this is the **fifth consecutive row** whose roster a brief has stated and the fifth that must be re-read anyway.

**A GENUINE STRUCTURAL CONFLICT TO RESOLVE BEFORE DRAFTING, AND IT IS THE FIRST OF ITS KIND IN PHASE 4.** §3.1's outline for this lesson is `the-spinning-machine` (prose) · `frequency-is-a-meter` (prose) · `what-retires-with-coal` (callout) · `following-versus-forming` (proscons) · `where-it-fails` (callout) · `drill` · `check-yourself` — **two `prose` sections back to back**, which **§8 item 6 forbids in as many words**: *"no lesson runs two `prose` sections back to back without a `table`, `callout`, `proscons`, `timeline` or `bars` between them."* Every built lesson has honoured that rule. **This is a contradiction between two parts of the plan, not a licence to pick one** — so resolve it deliberately and write the resolution down. The three defensible routes: (a) keep both prose ids and insert a section between them, which changes the section count from seven to eight and needs a reason; (b) convert `frequency-is-a-meter` to a kind that carries its own argument — a `bars` of frequency deviation against event, or a `timeline` of the seconds after a generator trip — which §8 item 6 explicitly allows and which the Sinexcel guide's "frequency as the live balance meter" framing suits; or (c) reorder so `what-retires-with-coal` sits between them. **Route (b) is the one this brief would take** — `bars` needs numbers the source actually states, so check Sinexcel and Black & Veatch for a stated series first and fall back to (a) if none exists — but the decision is the authoring session's and it belongs in the §8 CHANGELOG record either way. **Section ids are permanent once shipped**, so this is the one moment it can be settled cheaply.

**THE SEGMENT ARITHMETIC IS NEW — YOU ARE THE FIRST PHASE 4 ROW WHOSE LESSON SITS IN *TWO* SEGMENTS' `READ_NEXT`, AND YOU MUST REGENERATE BOTH.** `grid-stability-and-the-generator` appears in `READ_NEXT["grid-equipment"]` **and** in `READ_NEXT["bridge-and-on-site-generation"]` — verified in `scripts/build-classroom-segments.py` at v06.03r. Row 13's lesson sat in exactly one. So registering this lesson makes **two** segments due and `--check` should read **10 → 12 → regenerate both → 10**. Run `--segment grid-equipment` and `--segment bridge-and-on-site-generation` and **nothing else** (§10.4, §10.9); the standing ten belong to their own sessions. **The section half is `read-next` alone for both, and it is derived rather than guessed**: `grid-stability-and-the-generator` is **not** in `CRITERION_LEXICON` — checked entry by entry at v06.03r — so row 13's deepening cannot repeat. Row 13's deepening fired only because `breakers-relays-and-faults` sat in **both** a segment's `READ_NEXT` **and** in `CRITERION_LEXICON`, with criterion 4 naming it. **Predict both halves and record which fired** (§10.6 (w)).

**EXPECT P1 *AND* P7 — AND THIS TIME THE P7 IS FORESEEABLE RATHER THAN A SURPRISE.** **both** `segment-grid-equipment` (row 13) and `segment-bridge-and-on-site-generation` (session 9) were regenerated on **2026-09-15 EST**, and yours are the same two. If your push were to land on 2026-09-15 EST, neither `updated` could advance and `check-classroom-pipeline.py --base origin/main` will report **P7 on it** — *content changed but `updated` did not advance*. §7.32's rule stands and there is **no legitimate way to clear it**: the UTC date is the date this repo refuses, and hand-editing a generated literal is forbidden by §10.4, §10.9 and `classroom-app.md`. **It is pipeline-rule noise on a developer commit**, the same class as P1's write-set findings, because the pipeline by construction never writes a `segment-*` lesson. **Any later EST day clears both**, which is the likely case — so unlike row 13 and session 9 you are probably *not* at risk, and the check is cheap: **read both literals' `updated` and compare against your EST date before you worry about it, and record the outcome either way.**

**§10.6 (t) RUNS THE OTHER WAY ON A PHASE 4 ROW, AND THIS ROW HAS THE STRONGEST CASE YET.** Session 9 found that `bridge-power` and `backup-generation` teach **five of six** buying criteria of their segment vendor-blind, and that `bridge-power`'s three-ways-out-of-a-queue taxonomy maps one-for-one onto that segment's challenger bench. **Your lesson is the third `READ_NEXT` entry of that same segment and of `grid-equipment`**, so two landscape modules will point at it the day it lands — and both already name vendors this lesson must not. **`landscape-grid-equipment-2026-09` ranks the grid-stability machine explicitly** (four of its five incumbents sell {{synchronous condenser}}s or {{STATCOM}}s, and it states that *no source in this corpus sizes the grid-stability machine market*), and **`landscape-bridge-and-on-site-generation-2026-09` names sixty-two flywheel-backed synchronous condensers on one operator's campuses.** So write this lesson **vendor-blind by construction**, the way `bridge-power` and `backup-generation` were, and consider pre-declaring the handoff in a tile or a callout the way `the-campus-as-a-power-project` and `the-ups-room` did — **a declined judgment upstream is the cleanest possible statement of where the boundary runs**, and it has now paid four times.

**TWO OPEN ITEMS THIS ROW IS THE NATURAL OWNER OF, AND ONE IT IS NOT.** (i) **`the-800-vdc-shift`'s stale `three-chains` intro is now twelve inserts stale** and this row is another near miss — grid-forming inverters are the physics under the 800 VDC papers' stability claims, but the lesson lives in track 2 and that one in track 3, so opening it would still be a G4 wording change. **It belongs to whichever session opens that lesson to write in it.** (ii) **§8 item 8's `bridge-power` → `where-batteries-stop` reverse pointer: session 9 read `bridge-power` in full and reports the debt REAL IN THE LETTER AND PAID IN SUBSTANCE.** `where-batteries-stop` states the boundary **twice** — *"Same machines, different question: that one asks how soon, this one asks how long"* and *"That is the boundary this lesson hands on — Bridge Power picks the machines up where the question changes from how long to how soon"* — naming `bridge-power` by title both times and declaring the non-duplication in both directions. `bridge-power` carries no back-pointer and its only cross-reference is forward, to the transformer lesson. **The recommendation session 9 leaves: close §8 item 8's open half as paid by the newer lesson, and add the back-pointer only if a future session opens `bridge-power` for a substantive revision — never as a standalone edit, which is exactly the G4 change P8 exists to fail.** (iii) The `.cl-tbl` `min-width` question now has **sixteen** clean data points (session 9's thirteen-row, three-column bets table and its sixty-row ledger both rendered clean at 1400px); it still wants a session that edits `Classroom.html`.

**The baselines to capture BEFORE any edit, all measured at the close of v06.03r.** `check-classroom-content.py` is **0 errors / 0 warnings at 43 lessons, 8 tracks, 142 gate cases** — held unbroken through fourteen consecutive sessions — and your commit takes it to **44 lessons** with the gate-case count moving as your lesson's stamp adds fixtures. **The guidance-module assertion is now 18 and it does NOT move on a Phase 4 row** — that number rises only on an S2 session. `check-classroom-curriculum.py --strict`: no structural findings, **28 stale pins** (pre-existing, on the hand-authored lessons) and **2 items due for review** — `landscape-utilities-2026-09` and `landscape-in-hall-power-2026-09`, both at 2026-10-01 and both by design. **Do not "fix" either.** `check-classroom-pipeline.py --selftest`: **13 fixtures / 0 failures**.

**`reviewBy` — the roster has no obvious dated gate, so expect the six-month default and say so.** None of the eight inputs is a policy document and the segment fence is not yours to sort. Session 9's discipline still applies: **look in the study guides' own forward-looking material before defaulting**, write your rejections down, and if nothing dated survives take six months from `updated`. **One arithmetic constraint**: the curriculum checker reports **2** items due for review today and a `reviewBy` inside your own 30-day horizon makes it 3. Sessions 8 and 9 both avoided that deliberately. Choose knowingly.

**Versions and counters to bump from.** Classroom GAS **v01.48g** (both `Classroom.gs` `VERSION` and `live-site-pages/gs-versions/Classroomgs.version.txt`); page **v01.14w**, and no page bump is expected. Scraper **v02.10g** and **not yours** — a Phase 4 row adds no Scraper seed. Profiler v01.39g and never yours. **Repo version: read `repository-information/repository.version.txt` on the day and bump it by 0.01 — do not take the number from this brief.** This is the one counter a brief structurally cannot state, because the handover commit that finishes writing the brief consumes the next version itself; the recursion was closed at v06.01r by deleting the claim rather than correcting it, and this brief keeps it deleted.

**THE DEPLOY — the §7.19 order, on its seventeenth use.** Read **Pages** first (`https://lightaisolutions.github.io/Sales/gs-versions/Classroomgs.version.txt` is a static file, free to read as often as you like) and the workflow's own `Classroom deploy confirmed (GET)` line from the job log. **Probe `?op=deploy` only if those two disagree** — the probe *deploys* whenever the running `/exec` bundle is behind GitHub. Use `curl -sL` if you do probe; without `-L` the body is empty and reads as a failure. **Deployment counters: Classroom 59/200 and Scraper 155/200, both MEASURED at v06.03r against the job log.** Session 9 put **two** `.gs` files in the merge diff, so exactly two steps fired and exactly two lines logged — `Scraper deploy confirmed (GET): Updated to v02.10g (deployment 155) | 155/200` and `Classroom deploy confirmed (GET): Updated to v01.48g (deployment 59) | 59/200` — both on the **first GET leg** with no POLL and no probe, the **sixteenth consecutive clean use** of the order. **Your row puts only `Classroom.gs` in the diff, so expect exactly one line and Classroom at 60/200.** Scraper's 155/200 does not move until a session touches `Scraper.gs`, which a Phase 4 row does not. **The counter rule, unchanged**: `Updated to` consumes one; a step that never fires consumes none and logs nothing; and **`Already up to date` describes the state at READ time and is silent on whether the call consumed a deployment** — never forecast it as zero.

**CHANGELOGS — YOU ARE THE ROTATOR AND YOU CANNOT ESCAPE IT.** Keep using `TZ=America/New_York date '+%Y-%m-%d'` and **trust it over the session harness's own date banner, which follows UTC.** The trap has now decided the rotation on **four consecutive sessions**: at v06.03r EST read **2026-09-15 23:31** against UTC's **2026-09-16 03:31** and the harness banner said 2026-09-16, so §7.33's escape clause — *"unless your push also lands on 2026-09-15 EST"* — **fired rather than its conclusion**, and session 9 did **not** rotate. It closed at **105 raw / 80 non-exempt**, counter `Sections: 105/100`, with **twenty-five sections dated 2026-09-15 EST**. **The moment EST rolled past that date all twenty-five stopped being exempt, so the file now stands at 105 non-exempt against a 100 trigger and your push — which cannot land on 2026-09-15 — rotates.** Rotate **the oldest whole date group**, which is **2026-09-07 (13 sections)**, and re-check: 105 − 13 = 92, plus yours = 93, under the cap in one pass. **Re-count every changelog from the files rather than from this brief**, deepen the clone first so every rotated header enriches, and run the **mandatory post-rotation grep** (`grep '^## \[v' CHANGELOG-archive.md | grep -v '— \[' | head -5`) — v05.83r's enrichment failed silently on the first attempt and only that grep caught it. `Classroomgs.changelog.md`, `Scrapergs.changelog.md` and `Profilerhtml.changelog.md` stand where the v06.03r CHANGELOG's Notes record them; **`Profilerhtml.changelog.md` is the closest of the three to its own trigger and nothing a Phase 4 row does touches Profiler.**

**Five open items recorded and not taken.** (i) The `.cl-tbl` `min-width` question — sixteen clean data points; **close it** the next time a session has reason to edit `Classroom.html`. (ii) The generated segment lesson's `read-next` **names** the landscape module without linking to it: `clFmt` has no link token at all, so it belongs with (i) plus a developer `--all` run. (iii) **`the-800-vdc-shift`'s `three-chains` intro is twelve inserts stale** — see above. (iv) **§8 item 8's `bridge-power` pointer — session 9 reports it real in the letter and paid in substance**, with a recommendation to close the open half; see above. (v) **`section 232` was DECIDED at v06.03r rather than deferred again** and the recommendation is recorded: it wants a policy-level Scraper seed of its own (`topic-trade-tariffs`, or an addition to `topic-federal-action`), which is a developer or policy-session decision and **not** a landscape session's. **§10.6's session-4 bullet block still sits out of chronological order** — cosmetic, and a future session can fix it deliberately.

> Author Phase 4 row 14 — the `grid-stability-and-the-generator` lesson — on Opus 5 xhigh as a fresh session. This is ONE lesson. It authors no guidance module, no dossier and no report, and it creates no track.
>
> READ FIRST: repository-information/SESSION-CONTEXT.md (Latest Session); INTEGRATED-REMEDIATION-PLAN.md §7.34 (your brief), §7.32 and §7.33 (for the both-halves due-count rule, the P7 same-EST-day rule and the §10.6 (t) pre-declaration case), §7.3 order 6 and §6; CLASSROOM-CURRICULUM-PLAN.md §3.1's spec for this lesson IN FULL — **its seven section ids are the contract and they carry a conflict you must resolve** — plus §4's track table, §7 row 14, §8 items 2, 6 and 8, and §10.4/§10.9. Then .claude/rules/classroom-app.md IN FULL, repository-information/CLASSROOM-SCHEMA.md, and .claude/rules/changelog-security.md before you write any public changelog line.
>
> **UNSHALLOW THE CLONE FIRST** — `git fetch --unshallow origin main`, before any measurement and before any pin. A shallow clone's boundary commit looks like it introduced every file, so `git log -1 --format=%cs -- <path>` returns the boundary's date rather than the file's. If `--check` reports a number wildly above the 10 below, suspect the clone before the corpus.
>
> THE WORK — one push commit. (1) Author `clLessonGridStabilityAndTheGenerator_()` **inside** the `// CONTENT START` … `// CONTENT END` fence, register it at the **end** of `clLessons_()` and **append** it to `electrical-foundations`'s `lessons[]`, bumping that track's `updated`. Id `grid-stability-and-the-generator`, group `Technology Foundations`, gate `tracks` (all-public inputs fold to `tracks`, so the lesson is analyst-visible — verify rather than assume). (2) **RESOLVE §3.1'S TWO-BACK-TO-BACK-PROSE CONFLICT WITH §8 ITEM 6 DELIBERATELY** and write the resolution into the §8 CHANGELOG record; the brief's own preference is to convert `frequency-is-a-meter` to a `bars` or `timeline` **only if a source states the numbers**, else insert a section and say why the count moved from seven to eight. (3) Pin all **eight** inputs — seven study guides plus the concepts file — each date read off the fetched document and each ref resolved by opening the file and matching its own `slug` (G2, G7). (4) **Regenerate BOTH `--segment grid-equipment` AND `--segment bridge-and-on-site-generation`** — your lesson is the first in Phase 4 to sit in two segments' `READ_NEXT` — and nothing else. (5) Flip the §7.3 Phase 4 row, §6's Phase 4 row, curriculum §7 row 14 and §4's track table, dated. (6) Write §7.35 — the next run's brief, which by the interleave is **S2 session 10, `clean-firm-and-nuclear`** — in the same commit, and **edit BOTH halves of any §7.x brief you refresh**: the prose paragraphs and the quoted prompt block.
>
> PREDICT BOTH HALVES OF THE DUE COUNT. §10.6 (w) requires both halves. **`grid-stability-and-the-generator` sits in TWO segments' `READ_NEXT`** — `grid-equipment` and `bridge-and-on-site-generation` — so registering it makes both due and the membership half is **10 → 12 → regenerate both → 10**. **You are the first Phase 4 row to regenerate two segments.** The section half is **`read-next` alone for both**, derived rather than guessed: this lesson id is **not** in `CRITERION_LEXICON`, checked entry by entry at v06.03r, so row 13's deepening cannot repeat — that fired because `breakers-relays-and-faults` sat in both a segment's `READ_NEXT` **and** in `CRITERION_LEXICON`, with criterion 4 naming it. The standing ten are `cells-and-chemistry`, `clean-firm-and-nuclear`, `compute-and-the-rack`, `epc-and-construction`, `storage-developers-and-ipps`, `hyperscalers-and-ai-labs`, `neoclouds`, `capital`, `software-and-optimization`, `insurance-and-risk-transfer`; none is yours under §10.9. Record which halves actually fired.
>
> WRITE IT VENDOR-BLIND, BECAUSE TWO LANDSCAPE MODULES WILL POINT AT IT THE DAY IT LANDS. This lesson is the third `READ_NEXT` entry of both `grid-equipment` and `bridge-and-on-site-generation`, and **both of those landscapes already name vendors this lesson must not** — one ranks the grid-stability machine explicitly and records that no source sizes that market, the other names sixty-two flywheel-backed synchronous condensers on one operator's campuses. §10.6 (t) has now paid four times: **consider pre-declaring the handoff in a tile or a callout**, the way `the-campus-as-a-power-project` and `the-ups-room` did. A declined judgment upstream is the cleanest possible statement of where the boundary runs.
>
> EXPECT P1 AND — FORESEEABLY — P7. **both** segments you must regenerate were last regenerated on **2026-09-15 EST**, so a push on that same EST day could not advance either `updated` and `--base origin/main` would report **P7**. Any later day clears both, which is the likely case. §7.32's rule stands and **there is no legitimate way to clear it**: the UTC date is the one this repo refuses and hand-editing a generated literal is forbidden. It is pipeline-rule noise on a file the pipeline never writes. **Read both literals' `updated` and compare against your EST date before you worry about it, and record the outcome either way. No P3** — a lesson inside the fence moves no `GATE_SYMBOLS` member, so do not refresh `gateDigest`; refreshing would hide a real signal.
>
> DO NOT: author or revise a guidance module — the module assertion in `check-classroom-content.py` stays at **18** and is **not** a Phase 4 row's to move. Do not hand-edit a generated `segment-*` literal, and run the generator only with the two `--segment` flags above (§10.4, §10.9). Do not touch `Scraper.gs`, `Profiler.gs` or `Profiler.html`. Do not "fix" the two items the curriculum checker reports as due for review: `landscape-utilities-2026-09` and `landscape-in-hall-power-2026-09` are **both at 2026-10-01 and both by design**.
>
> VERIFY: `node --check` on a `.js` copy of `Classroom.gs`; `scripts/check-gas-inner-scripts.js`; `scripts/check-classroom-content.py` — **capture a pristine HEAD baseline BEFORE any edit. IT IS 0 ERRORS / 0 WARNINGS at 43 lessons / 8 tracks / 142 gate cases** and must be 0/0 at **44 lessons** after, with the module assertion **still 18**. Watch the micro-markup check: it has caught a nested-emphasis defect in three separate sessions and nothing else in the toolchain sees it. Then `scripts/check-classroom-curriculum.py --strict` (no structural findings; 28 stale pins and 2 items due for review are pre-existing and not yours); `scripts/check-classroom-pipeline.py --selftest` (13 fixtures, 0 failures) and `--base origin/main`. `build-classroom-segments.py --check` — expect **10 → 12 → 10**, both joining and leaving on `read-next` alone. `scripts/check-readme-tree.py` after the version bumps. Playwright render of the new lesson at **analyst** (the negative half is that a `tracks`-gated lesson IS visible to an analyst — confirm it, because this is a public lesson and the gate should let it through) with ZERO page errors, ops answered by the REAL serving functions — the lesson entrypoint is `clShowLesson(id)` and the **guidance** entrypoint is `clShowGuidanceDoc(id)`; **route `action=guidance` payloads to `handleGuidanceOp_` and everything else to `handleClassroomOp_`** (session 9 lost a cycle to sending a guidance op to the classroom handler and getting `unknown_cop`); `_gasPost` returns a PROMISE, `sess.permissions` must be an ARRAY in the shim, the session token must be at least 32 characters or the real guard returns SESSION_EXPIRED, the session keys are `Classroom_gas_session_token` / `_gas_user_email` / `_gas_user_role` / `_gas_user_permissions`, overlays to hide are `#auth-wall` / `#signing-out-wall` / `#auth-timers` / `#gas-layer-toggle`, and the scratch site root needs `favicon.ico` copied in beside `profiler-data/`, `sounds/` and `images/`. **SERVE THE SCRATCH COPY AT `/Classroom.html`, NOT AT THE SITE ROOT** — the page derives its sessionStorage key prefix from the URL path. **Call the handlers through the vm context object (`ctx.handleClassroomOp_`), not from Node's own scope.** Then READ EVERY SECTION'S SCREENSHOT. Bump Classroom's GAS version in BOTH `Classroom.gs` `VERSION` (from `v01.48g`) and `Classroomgs.version.txt`, with a GENERIC entry in its changelog.
>
> AFTER THIS SESSION Phase 4 is 14 of 26 and S2 is 9 of ~19, so the interleave returns to S2 and the recommended next run is **S2 session 10 — `clean-firm-and-nuclear`** — for which §7.35 should be written.

### 7.35 · Paste-in brief for S2 session 10 — `clean-firm-and-nuclear` (Opus 5 xhigh) — written 2026-09-16 at v06.05r, at the close of Phase 4 row 14

**Why this and not another Phase 4 row.** Phase 4 is now **14 of 26** and S2 is **9 of ~19**, so the interleave stands at 14:9 and §7.4 item 6 hands the turn back to S2. Its next segment in the §7.3 sales-value order is **`clean-firm-and-nuclear`** — the tenth landscape and the **nineteenth** guidance module. Row 15 (`string-versus-central`) is the lowest unbuilt mechanism row and is not going anywhere; it closes `electrical-foundations`, which row 14 otherwise completed.

**UNSHALLOW FIRST — a Session Start Checklist item, not a brief's favour.** `git fetch --unshallow origin main` before any pin read, any `--check` run, any `git log -1 --format=%cs` date and any CHANGELOG rotation. Row 14 ran it as the checklist's "Deepen the clone" bullet and took the clone to **1,269 commits**; `--check` then opened at the true 10 with `inputs moved: none` on every line, and all thirteen rotation SHAs resolved.

**THE SEGMENT — 16 members (3 · 2 · 11) at v06.05r, and its SHAPE is the thing to read before writing a line.** Incumbents `constellation-energy` · `vistra` · `talen-energy`; challengers `oklo` · `x-energy`; adjacents `form-energy` · `fermi-america` · `nextera-energy-resources` · `southern-company` · `dominion-energy` · `entergy` · `xcel-energy` · `bechtel` · `kiewit` · `black-veatch` · `sargent-lundy`. **All sixteen carry a dossier** — verified at v06.05r by opening each `<slug>.profile.json`. **Re-measure it yourself on the day**; nine consecutive sessions have confirmed a brief's roster member for member and the tenth should be earned, not assumed. Two structural facts worth carrying into the draft: at **69 % adjacent (11 of 16)** this is the **second most adjacent-heavy segment in the registry**, behind `software-and-optimization`'s 75 %; and its **two-name challenger bench is the joint second-smallest**, after `grid-equipment`'s and `cooling`'s one. Session 8's finding (xxiii) — that a thin challenger bench is writable when the registry's own `notes` explains the shape — is the nearest precedent, **but this segment's `notes` is EMPTY**, so the explanation has to come from the members' own dossiers rather than from the registry. That is a real difference from session 8 and the brief flags it rather than solving it.

**THE `READ_NEXT` IS TWO ENTRIES LONG AND ONE OF THEM DOES NOT EXIST — the thinnest mechanism coverage any landscape has faced.** `READ_NEXT["clean-firm-and-nuclear"]` is `clean-firm-power` and `the-campus-as-a-power-project`. The second is **built** (Phase 4 row 9, analyst-visible); the first is **§7 row 16 and unbuilt**. Session 8's (xxii) established the precedent — a landscape written against a half-unbuilt curriculum **names the gap rather than appointing itself to teach it** — and it applies harder here: four of the six buying criteria (licensing path, the price ladder, capacity factor and firmness, HALEU supply) have **no** built owner at all, and the module must not become the nuclear lesson by default. Say so in the header comment and in the module's own opening, the way session 8 did.

**§10.6 (t) HAS A BUILT NEIGHBOUR THAT ALREADY PRE-DECLARED ITS HANDOFF, AND IT IS THE SAME TILE SESSION 6 USED.** `the-campus-as-a-power-project` carries a tile reading *0 league tables — no ranking of these landlords exists in this corpus and this lesson does not invent one*, and it reads nine named projects without ranking their sponsors. Row 14 added a second instance of the same idiom one lane over (*0 vendors named*, plus a note declining the ranking in terms), so the pattern now has **six** payments behind it. Read that lesson before drafting and enumerate what it owns as deliberate omissions.

**THE DUE-COUNT ARITHMETIC IS THE NETTING CASE, NOT THE RAISING CASE — and your own segment is already due.** `build-classroom-segments.py --check` reports **10 due** at v06.05r and **`clean-firm-and-nuclear` is one of the ten** (`sections differing: read-next`). So session 5's rule (v) governs: **a landscape whose own segment is already due cannot raise the count**, and the arithmetic is **10 → 10 → 9**. Predict **both halves** anyway (§10.6 (w)): the section half should be `read-next` alone, because the only thing registering the module changes is the `self.landscapes` lookup inside `sec_read_next`. **Row 14 corrected the standing derivation of the section half and the correction is load-bearing** — see the next paragraph. The other nine are `cells-and-chemistry`, `compute-and-the-rack`, `epc-and-construction`, `storage-developers-and-ipps`, `hyperscalers-and-ai-labs`, `neoclouds`, `capital`, `software-and-optimization`, `insurance-and-risk-transfer`; **none is yours** under §10.4 and §10.9. Run `--segment clean-firm-and-nuclear` and nothing else.

**A CORRECTION ROW 14 MADE TO §7.34'S OWN CLAIM, AND EVERY FUTURE BRIEF INHERITS IT.** §7.34 asserted that `grid-stability-and-the-generator` is **not** in `CRITERION_LEXICON`, "checked entry by entry at v06.03r". **It is** — keyed on `grid-forming`, `inertia`, `reactive`, `stability`, `black start`. The prediction (no deepening) still held, but for a reason the brief never stated: **none of those five keywords appears in either segment's six `buyingCriteria` strings**, which row 14 checked programmatically rather than by eye. So row 12's gate needs one more turn. The section half is bounded by `READ_NEXT` ∩ `CRITERION_LEXICON` **and then by an actual case-insensitive keyword hit against that segment's own `buyingCriteria`** — three tests, not two, and only the third can be run without guessing. **Do not take a lexicon membership claim from a brief; grep the file.**

**EXPECT P1 AND P2, AND NEITHER IS A DEFECT.** Session 9 settled this: a guidance module is authored **below** the `// CONTENT END` fence *by design*, so every line of it sits outside the pipeline's write set and `check-classroom-pipeline.py --base origin/main` reports **P2** on it as structurally as it reports **P1** on the plan documents. **P2 is not a Phase 4 finding and it is not an S2 defect — it is the contract working.** No P3: a landscape session moves no `GATE_SYMBOLS` member, so **do not refresh `gateDigest`** — refreshing would hide a real signal. On P7: your segment was last regenerated **2026-09-16 EST** (row 14's push), so **a push on that same EST day cannot advance `segment-clean-firm-and-nuclear`'s `updated` and will report P7**, which §7.32 established has no legitimate fix — the UTC date is the one this repo refuses and hand-editing a generated literal is forbidden by §10.4, §10.9 and `classroom-app.md`. **Read the literal's `updated` and compare it against your EST date before you worry about it**, exactly as row 14 did; any later day clears it.

**THE MODULE ASSERTION IS YOURS TO MOVE — 18 → 19.** `scripts/check-classroom-content.py` asserts the registered guidance-module count and it is the **one edit outside the fence a landscape session makes**, mandatory per §10.6 (c). Row 14 deliberately left it at 18; you take it to 19 in the same commit. **Baselines to capture BEFORE any edit, all measured at the close of v06.05r:** `check-classroom-content.py` **0 errors / 0 warnings at 44 lessons, 8 tracks, 142 gate cases** — held unbroken through fifteen consecutive sessions; `check-classroom-curriculum.py --strict` no structural findings, **28 stale pins** (pre-existing, on the hand-authored lessons) and **2 items due for review** (`landscape-utilities-2026-09` and `landscape-in-hall-power-2026-09`, both 2026-10-01, both by design — **do not "fix" either**); `check-classroom-pipeline.py --selftest` **13 fixtures / 0 failures**.

**`reviewBy` — read, do not sort, for the tenth consecutive session, and this segment's fence is unusually rich.** Nine sessions running have found the date in a member's prose rather than in a sorted `effectiveDate` column, and five distinct failure modes are on record (a sort returning nothing at all; a sort returning a truck engine's model year; a sort returning ten months late; the nearest *date* rather than the nearest *gate*; the nearest gate already being two other modules' clock). This segment's criteria are made of licensing milestones and fuel-supply schedules, so expect **several** defensible candidates and write every rejection down. **One arithmetic constraint**: the curriculum checker reports **2** items due today and a `reviewBy` inside your own 30-day horizon makes it 3; sessions 8, 9 and row 14 all avoided that deliberately. Choose knowingly. **And the (r)/(u) bar still stands** — do not take a calendar day that is already another module's own `reviewBy`; session 9's exception was for a *different bell on the same day*, not for the same gate.

**The Scraper seed — state your prior as a prior.** §10.6 step 9 wants `topic-landscape-clean-firm-and-nuclear` scored from scratch against **every** term in both seed arrays before a single term is kept. The rule is now 3-for-5 on brief priors (session 5 wrong, session 6 half right, session 8 right in both halves, session 9 right) so the honest instruction is to run the check rather than to trust a forecast. **Two live recommendations sit in this area and are NOT yours to take**: `section 232` wants its own `topic-trade-tariffs` seed or an addition to `topic-federal-action` (decided at v06.03r as a developer or policy-session call), and the HALEU/fuel-cycle vocabulary may be genuinely unscored — check it and seed it here **only if it belongs to this segment rather than to policy**.

**Versions and counters to bump from.** Classroom GAS **v01.49g** (both `Classroom.gs` `VERSION` and `live-site-pages/gs-versions/Classroomgs.version.txt`); page **v01.14w**, and no page bump is expected. Scraper **v02.10g** — yours only if you add a seed. Profiler v01.39g and never yours. **Repo version: read `repository-information/repository.version.txt` on the day and bump it by 0.01 — do not take the number from this brief.** This is the one counter a brief structurally cannot state, because the handover commit that finishes writing the brief consumes the next version itself; the recursion was closed at v06.01r by deleting the claim rather than correcting it, and this brief keeps it deleted.

**THE DEPLOY — the §7.19 order, on its eighteenth use.** Read **Pages** first (`https://lightaisolutions.github.io/Sales/gs-versions/Classroomgs.version.txt` is a static file, free to read as often as you like) and the workflow's own `Classroom deploy confirmed (GET)` line from the job log. **Probe `?op=deploy` only if those two disagree** — the probe *deploys* whenever the running `/exec` bundle is behind GitHub. Use `curl -sL` if you do probe; without `-L` the body is empty and reads as a failure. **Deployment counters: Classroom 60/200 and Scraper 155/200, both MEASURED at v06.06r against the job log.** Row 14 put one `.gs` in the merge diff, so exactly one step fired and exactly one line logged — `Classroom deploy confirmed (GET): Updated to v01.49g (deployment 60) | 60/200` — on the first GET leg with no POLL and no probe, the **seventeenth consecutive clean use** of the order, and Pages agreed before the log was closed. **Your session adds one `.gs` too if you seed Scraper and one if you do not**, so forecast the line count from the diff, not from the work. Scraper's 155/200 does not move unless you touch `Scraper.gs`. **The counter rule, unchanged**: `Updated to` consumes one; a step that never fires consumes none and logs nothing; and **`Already up to date` describes the state at READ time and is silent on whether the call consumed a deployment** — never forecast it as zero.

**CHANGELOGS — you are probably NOT the repo rotator, and you are probably NOT the GAS rotator either. Re-count both from the files.** Row 14 rotated: EST read **2026-09-16 00:07** against UTC's **04:07**, so the twenty-six sections dated 2026-09-15 all stopped being exempt and the file stood at **106 non-exempt against a 100 trigger**. The **2026-09-07 group (13 sections)** moved, all thirteen SHA-resolved on a deepened clone, and the mandatory post-rotation grep passed on the first attempt. `CHANGELOG.md` closed at **94 raw**, one section of which (row 14's) is dated 2026-09-16 — so on any later EST day the file reads **94 non-exempt** and the next group in line is **2026-09-08 (10 sections)**, which does not arm until the file is back at 100. `Classroomgs.changelog.md` closed at **49/50**: your section takes it to **50 raw**, but your own section is exempt on your push day, so the non-exempt count is 49 and **the trigger does not fire on your push** — it arms for the session after yours, whose oldest group is **2026-09-02 (11 sections)**. `Scrapergs.changelog.md` **31/50**; `Profilerhtml.changelog.md` **49/50**, the closest of the three to its own trigger and nothing an S2 session does touches Profiler. **Keep using `TZ=America/New_York date` and trust it over the harness banner, which follows UTC** — that check has now decided the rotation on five consecutive sessions, four times against the banner.

**Four open items recorded and not taken.** (i) The `.cl-tbl` `min-width` question now has **seventeen** clean data points — row 14's four-column `two-knobs` table rendered clean at 1400px — and still wants a session that edits `Classroom.html`. (ii) The generated `read-next` **names** the landscape module without linking to it: `clFmt` has no link token at all, so it belongs with (i) plus a developer `--all` run. (iii) **`the-800-vdc-shift`'s `three-chains` intro is now thirteen inserts stale** — it belongs to whichever session opens that lesson to write in it, never as a standalone G4 edit. (iv) **§7.3's S2 row (order 5) is one session behind**: it records sessions 1–8 and closes with *"Session 9 by this table's own order is `bridge-and-on-site-generation`"* — session 9's own record went into §6's S2 row but not into §7.3's. **Close it as part of your own flip**, because it is your lane's record and you will be appending session 10 to the same cell. **§10.6's session-4 bullet block still sits out of chronological order** — cosmetic, and a future session can fix it deliberately.

> Author S2 session 10 — the `clean-firm-and-nuclear` landscape module — on Opus 5 xhigh as a fresh session. This is ONE guidance module. It authors no lesson, no dossier and no report, creates no track, and regenerates exactly ONE segment: `clean-firm-and-nuclear`.
>
> READ FIRST: repository-information/SESSION-CONTEXT.md (Latest Session); INTEGRATED-REMEDIATION-PLAN.md §7.35 (your brief), §7.33 and §7.34 (for the both-halves due-count rule, the P2-is-structural rule, the P7 same-EST-day rule and the §10.6 (t) pre-declaration case), §7.3 orders 5 and 6, and §6; CLASSROOM-CURRICULUM-PLAN.md §10.6 IN FULL — its nine section ids and their order are the contract — plus §10.2's floor table, §10.4, §10.9, §10.10's two role paths and §8 item 2. Then .claude/rules/industry-guidance.md and .claude/rules/classroom-app.md IN FULL, repository-information/CLASSROOM-SCHEMA.md, and .claude/rules/changelog-security.md before you write any public changelog line.
>
> **UNSHALLOW THE CLONE FIRST** — `git fetch --unshallow origin main`, before any measurement and before any pin. A shallow clone's boundary commit looks like it introduced every file, so `git log -1 --format=%cs -- <path>` returns the boundary's date rather than the file's. If `--check` reports a number wildly above the 10 below, suspect the clone before the corpus.
>
> THE WORK — one push commit. (1) Author `guidanceDocLandscapeCleanFirmAndNuclear_()` **below** the `// CONTENT END` fence at §10.6's nine section ids in its order — `who-dominates-and-on-what-basis` · `who-threatens` · `each-players-bet` · `the-indicators` · `the-sellers-play` · `claims-ledger` · `what-the-record-does-not-say` · `drill` · `check-yourself` — id `landscape-clean-firm-and-nuclear-2026-09`, lane `The Value Chain`, tier contributor, and register it at the **end** of `guidanceDocs_()`. (2) Write the analysis markdown at `repository-information/industry-guidance/landscape-clean-firm-and-nuclear-analysis.md` as its source of truth, and **write §2 — the split — before any other section**. (3) **Bump the guidance-module assertion in `scripts/check-classroom-content.py` from 18 to 19** — the one edit outside the fence a landscape session makes, mandatory per §10.6 (c), and the number Phase 4 row 14 deliberately left alone. (4) Re-measure the segment on the day (the registry says **16 members: 3 · 2 · 11**, and all sixteen carried a dossier at v06.05r) and pin every claim in the ledger to `profile:<slug>` at its `profileVersion` plus the dossier field it rests on. (5) Add the `topic-landscape-clean-firm-and-nuclear` seed to `Scraper.gs` per step 9 **only if** the terms score zero against every existing seed array — score first, decide after. (6) Regenerate **only** `--segment clean-firm-and-nuclear`. (7) Flip the §7.3 S2 row and §10.6's progress note, dated — **and close the one-session gap in §7.3's S2 row while you are in it**: it records sessions 1–8 and never got session 9. (8) Write §7.36 — the next run's brief, which by the interleave is **Phase 4 row 15, `string-versus-central`** — in the same commit, and **edit BOTH halves of any §7.x brief you refresh**: the prose paragraphs and the quoted prompt block.
>
> PREDICT BOTH HALVES OF THE DUE COUNT. §10.6 (w) requires both halves. **Your own segment is already due**, so session 5's netting rule (v) governs and the arithmetic is **10 → 10 → 9**, not 10 → 11 → 10. The section half should be **`read-next` alone** — `self.landscapes` is read at exactly one place in the generator, inside `sec_read_next`. **Derive it, do not inherit it**: §7.34 asserted a `CRITERION_LEXICON` membership that was false, and row 14 found the real test is three-part — `READ_NEXT` ∩ `CRITERION_LEXICON` ∩ **an actual keyword hit against that segment's own `buyingCriteria` strings**. Grep the file. The other nine due are `cells-and-chemistry`, `compute-and-the-rack`, `epc-and-construction`, `storage-developers-and-ipps`, `hyperscalers-and-ai-labs`, `neoclouds`, `capital`, `software-and-optimization`, `insurance-and-risk-transfer`; none is yours under §10.4 and §10.9. Record which halves actually fired.
>
> READ THE SEGMENT'S SHAPE BEFORE YOU WRITE ITS THREAT SECTION. At **69 % adjacent (11 of 16)** this is the second most adjacent-heavy segment in the registry, and its **two-name challenger bench** is the joint second-smallest. Session 8's (xxiii) showed a thin bench is writable when the registry's `notes` explains the shape — **but this segment's `notes` is EMPTY**, so the explanation has to be built from the members' own dossiers. Its `READ_NEXT` is only **two** entries and **one of them is unbuilt** (`clean-firm-power`, §7 row 16), so four of the six buying criteria have no built owner at all: name the gap, per session 8's (xxii), and do not let the module become the nuclear lesson by default. The one built neighbour, `the-campus-as-a-power-project`, already pre-declares the handoff in a tile (*0 league tables … this lesson does not invent one*) — read it in full and enumerate what it owns as deliberate omissions.
>
> EXPECT P1 AND P2 — NEITHER IS A DEFECT. A guidance module lives **below** the fence by design, so P2 on the module literal is as structural as P1 on the plan documents (session 9's finding). **NO P3** — a landscape session moves no `GATE_SYMBOLS` member, so do not refresh `gateDigest`; refreshing would hide a real signal. On P7: `segment-clean-firm-and-nuclear` was last regenerated **2026-09-16 EST**, so a push on that same EST day cannot advance its `updated` and will report P7, which has no legitimate fix. **Read the literal's `updated` and compare it against your EST date before you worry about it, and record the outcome either way.**
>
> DO NOT: author or revise a lesson — a landscape session's only lesson write is the one `--segment` regeneration. Do not hand-edit a generated `segment-*` literal, and run the generator only with the one `--segment` flag above (§10.4, §10.9). Do not touch `Profiler.gs` or `Profiler.html`. Do not "fix" the two items the curriculum checker reports as due for review: `landscape-utilities-2026-09` and `landscape-in-hall-power-2026-09` are **both at 2026-10-01 and both by design**. And **NEVER sweep a date across `Classroom.gs` with a blanket find-and-replace** — it holds eighteen independently dated guidance modules and session 9 corrupted a comment-deadline date inside `guidanceDocEo14420_()` that way; scope every replacement to the function you are authoring.
>
> VERIFY: `node --check` on a `.js` copy of `Classroom.gs`; `scripts/check-gas-inner-scripts.js`; `scripts/check-classroom-content.py` — **capture a pristine HEAD baseline BEFORE any edit. IT IS 0 ERRORS / 0 WARNINGS at 44 lessons / 8 tracks / 142 gate cases** and must be 0/0 after, with the module assertion moved **18 → 19** in the same commit. Watch the micro-markup check: it has caught a nested-emphasis defect in three separate sessions and nothing else in the toolchain sees it, and row 14 hit its other half — **a `proscons` card's `t` and `meta`, and a `glossary[].d`, are `textContent` fields, so a `{{term}}` in any of them prints its braces to the reader** and the checker errors on it. Then `scripts/check-classroom-curriculum.py --strict` (no structural findings; 28 stale pins and 2 items due for review are pre-existing and not yours); `scripts/check-classroom-pipeline.py --selftest` (13 fixtures, 0 failures) and `--base origin/main`. `build-classroom-segments.py --check` — expect **10 → 10 → 9**. `scripts/check-readme-tree.py` after the version bumps. Playwright render of the new module at **contributor** with ZERO page errors, plus an **analyst `ROLE_DENIED`** from the real serving path (a contributor-tier module must NOT reach an analyst — that is the negative half and it is the normal direction, unlike row 14's inverted public-lesson case). Ops answered by the REAL serving functions — the guidance entrypoint is `clShowGuidanceDoc(id)` and the lesson entrypoint is `clShowLesson(id)`; **route `action=guidance` payloads to `handleGuidanceOp_` and everything else to `handleClassroomOp_`** (session 9 lost a cycle to sending a guidance op to the classroom handler and getting `unknown_cop`); `_gasPost` returns a PROMISE, `sess.permissions` must be an ARRAY in the shim, the session token must be at least 32 characters or the real guard returns SESSION_EXPIRED, the session keys are `Classroom_gas_session_token` / `_gas_user_email` / `_gas_user_role` / `_gas_user_permissions`, overlays to hide are `#auth-wall` / `#signing-out-wall` / `#auth-timers` / `#gas-layer-toggle`, and the scratch site root needs `favicon.ico` copied in beside `profiler-data/`, `sounds/` and `images/`. **SERVE THE SCRATCH COPY AT `/Classroom.html`, NOT AT THE SITE ROOT** — the page derives its sessionStorage key prefix from the URL path. **The page's CSP `connect-src` is `'self'` plus Google hosts, so a shim server on another port is REFUSED**: row 14 solved it by pointing `_gasPost` at a same-origin path (`/__gas`) and fulfilling it with Playwright's `page.route()`. **Call the handlers through the vm context object (`ctx.handleGuidanceOp_`), not from Node's own scope.** Then READ EVERY SECTION'S SCREENSHOT. Bump Classroom's GAS version in BOTH `Classroom.gs` `VERSION` (from `v01.49g`) and `Classroomgs.version.txt`, with a GENERIC entry in its changelog.
>
> AFTER THIS SESSION Phase 4 is 14 of 26 and S2 is 10 of ~19, so the interleave returns to Phase 4 and the recommended next run is **Phase 4 row 15 — `string-versus-central`**, which closes `electrical-foundations` — for which §7.36 should be written.

### 7.36 · Paste-in brief for Phase 4 row 15 — `string-versus-central` (Opus 5 xhigh) — written 2026-09-16 at v06.07r, at the close of S2 session 10

**Why this and not another S2 segment.** S2 is now **10 of ~19** and Phase 4 is **14 of 26**, so the interleave stands at 14:10 and §7.4 item 6 hands the turn back to Phase 4. Its lowest unbuilt row is **15 — `string-versus-central`, *One Big Box or Many Small Ones*** — the **forty-fifth** lesson, at §4 position **2** of `electrical-foundations`, and **the last lesson that track needs**. Row 14 built position 5 and left the track complete but for this one.

**UNSHALLOW FIRST — a Session Start Checklist item, not a brief's favour.** `git fetch --unshallow origin main` before any pin read, any `--check` run, any `git log -1 --format=%cs` date and any CHANGELOG rotation. Session 10 ran it and took the clone to **1,273 commits**; `--check` then opened at the true 10 with `inputs moved: none` on every line.

**THE ONE PREDICTION THAT IS NEW, AND IT IS A SIGNATURE ELEMENT NO PHASE 4 ROW HAS SEEN: THIS LESSON IS AN INSERT, NOT AN APPEND, AND IT WILL FIRE P5.** `electrical-foundations`'s `lessons[]` currently reads `["four-machines", "the-transformer-and-the-substation", "breakers-relays-and-faults", "grid-stability-and-the-generator"]` — §4 positions **1, 3, 4, 5**. Row 15 is **position 2**, so the correct write puts it *between* the first and second entries, and `check-classroom-pipeline.py`'s P5 asserts `head[:len(base)] == base` on every track's `lessons[]`. **A mid-array insert fails that test by construction.** It is **not a defect**: P5 binds the *unattended committer*, which may only append; a developer session placing a lesson at its planned §4 position is doing the thing the pipeline is forbidden to do. Row 6's session recorded the mirror case (*"the first Phase 4 row that appends rather than inserts"*) and got **P1 and P8 only**; yours is the first to insert into a track that has since filled up behind it. **Expect P1, P5 and P8, predict them in the CHANGELOG before you run the checker, and do not "fix" P5 by appending** — appending would put the lesson at position 5 of the track and silently contradict §4 and the track's own stated order. Registry order in `clLessons_()` is a different matter: **append there**, because `clStudyNext_` walks registry order and P5 tests that separately.

**THE INPUTS ARE NINE AND EVERY ONE IS PUBLIC, so the gate is `tracks` and the lesson is analyst-visible.** §7 row 15 fixes them: `study:huawei-digital-power` · `study:fluence` · `study:sungrow` · `study:liteon` · `study:power-electronics` · `study:vertiv` · `study:schneider-electric` · `study:rehlko` · `concepts:profiler-concepts`. **Measured 2026-09-16, all nine resolve and all nine carry a date** — `huawei-digital-power` and `liteon` at **2026-08-21**, `fluence` and `sungrow` at **2026-08-08**, `power-electronics` at **2026-09-06**, `vertiv`, `schneider-electric` and `rehlko` at **2026-09-04**, and `profiler-concepts` at **2026-09-13** by `git log -1 --format=%cs` on a deepened clone. **Re-read every one off the fetched document (G2) and match each `slug` field (G7) — do not take these dates from this brief**; they are here so you can tell at a glance whether anything moved, not so you can copy them.

**THE SECTION CONTRACT IS §3.2'S SEVEN, AND §8 ITEM 6 IS SATISFIED AS WRITTEN — check it anyway.** `the-fork` (prose) · `solar-taught-it` (prose) · `same-argument-three-rooms` (table) · `the-hidden-engineering` (callout) · `where-it-fails` (callout) · `drill` · `check-yourself`. **Two prose sections open back to back**, which §8 item 6 forbids — *"no lesson runs two `prose` sections back to back without a `table`, `callout`, `proscons`, `timeline` or `bars` between them."* **Row 14 hit this exact contradiction and resolved it rather than obeying either half**, and its resolution is the precedent: §7.34 preferred converting the second prose to `bars`, *only if a source states the numbers*; none did, so route (a) was taken — **a `table` inserted between them, the count moved seven → eight, and the insert made load-bearing rather than a spacer**. Here you have a genuinely better option than row 14 had: `solar-taught-it` is about **mismatch, MPPT and what a failed string costs against a failed central unit**, and the corpus may well state those numbers. **Check whether `study:huawei-digital-power` gives a stated harvest delta or a stated availability figure.** If it does, `bars` or a small `table` is honest and the outline's own second prose becomes the visual section. If it does not, insert a load-bearing section as row 14 did and say why in the header comment. **Either way the section count and ids are permanent once written**, so decide before the first write.

**C7'S FINDING IS BAKED INTO THIS ROW AND IT IS THE LESSON'S DISCIPLINE, NOT A CAVEAT.** §3.2's v05.07r input note says it in as many words: `study:power-electronics` carries *"C7's finding that no independent US utility-scale PCS ranking exists — teach the fork without a league table."* This is a **mechanism lesson**, so the content contract already forbids company trivia — but the sharper instruction is that the *fork itself* has no winner in this corpus, and a lesson that implies one would be inventing the thing C7 established does not exist. `landscape-power-conversion-and-rack-power-silicon-2026-09` is the module that ranks this layer's parties, and it is **contributor-only** while this lesson is **public**: session 6's (s) line applies in its usual direction — the lesson teaches the test, the module names the parties.

**§10.6 (t) — CHECK YOUR NEIGHBOURS' TILES AND CALLOUTS BEFORE DRAFTING, because three of them are yours.** `four-machines` (position 1, built, public) is the lesson immediately before yours and teaches the four conversion machines; `the-800-vdc-shift` and `the-ups-room` both carry modular-versus-monolithic material; and `nvidia-800vdc-2026-08` is deliberately vendor-blind about the architecture. **Read `four-machines` in full** — it is the only lesson a reader is guaranteed to have finished before this one — and enumerate what it owns as deliberate omissions in your header comment. Session 10 found a **new** shape worth watching for: a built public lesson that pre-declares a handoff **to an unbuilt lesson**, leaving the material in nobody's hands. If you find one here, name the gap rather than filling it.

**`reviewBy` — read, do not sort, for the eleventh consecutive session, and this row's fence is probably thin.** Six distinct failure modes are now on record: a sort returning nothing at all (sessions 3, 8); a sort returning the wrong product line (sessions 7, 9 — a truck engine, an on-highway platform); a sort returning ten months late (session 2); the nearest *date* rather than the nearest *gate* (session 7); a gate that is already another module's own clock (sessions 5, 6); and — **new at session 10** — *both* rejection classes intersecting until no candidate survives, so the only honest choice is a day another module already carries. **This lesson's material is product architecture rather than policy**, so expect few dated gates and be ready for the six-month default from `updated`, stated explicitly. **One arithmetic constraint**: the curriculum checker reports **2** items due today (`landscape-utilities-2026-09` and `landscape-in-hall-power-2026-09`, both 2026-10-01, both by design — **do not "fix" either**) and a `reviewBy` inside your own 30-day horizon makes it 3. Sessions 8, 9, 10 and row 14 all avoided that deliberately. Choose knowingly.

**NO MODULE ASSERTION MOVE, AND NO SCRAPER SEED.** `scripts/check-classroom-content.py` now asserts **19** registered guidance modules; session 10 moved it 18 → 19 and **a Phase 4 row authors no module, so you leave it at 19**. Likewise the `industry-guidance.md` step 9 Scraper seed is an S2 obligation and not yours — `Scraper.gs` holds at **v02.11g** and stays there, which means **one `.gs` in your merge diff, one deploy step and one logged line**.

**Baselines to capture BEFORE any edit, all measured at the close of v06.07r.** `check-classroom-content.py` **0 errors / 0 warnings at 44 lessons, 8 tracks, 142 gate cases** — held unbroken through sixteen consecutive sessions, and your commit must return it to 0/0 at **45 lessons** with the module assertion **unchanged at 19**. `check-classroom-curriculum.py --strict`: no structural findings, **28 stale pins** (pre-existing, on the hand-authored lessons, unmoved by session 10's nine fresh module pins because a guidance module carries no `provenance`) and **2 items due for review**. `check-classroom-pipeline.py --selftest`: **13 fixtures / 0 failures**. `build-classroom-segments.py --check`: **9 due** — `cells-and-chemistry`, `compute-and-the-rack`, `epc-and-construction`, `storage-developers-and-ipps`, `hyperscalers-and-ai-labs`, `neoclouds`, `capital`, `software-and-optimization`, `insurance-and-risk-transfer`. **None is yours** and a Phase 4 row regenerates nothing — but **the count WILL move, and for once it can be forecast exactly.** Session 10 measured the mechanism rather than guessing it, and the answer has two parts. **The deepening half is NO**: the three-part test is `READ_NEXT` ∩ `CRITERION_LEXICON` ∩ a real keyword hit on that segment's own `buyingCriteria`, and `string-versus-central` passes the first two — it is in `CRITERION_LEXICON` keyed on `string` and `central`, and in exactly one segment's `READ_NEXT` (`power-conversion-and-rack-power-silicon`) — but **neither keyword appears in any of that segment's six criteria**, checked programmatically at v06.07r. So `what-is-bought-and-on-what` cannot move. **The membership half is YES, by the mechanism session 10 discovered while tracing its own netting case**: registering a lesson flips `lesson_ref` for every segment naming it in `READ_NEXT` from `*Title* (planned)` to the real title, which changes that segment's `read-next` section. `power-conversion-and-rack-power-silicon` is **not** in the standing nine, so **expect 9 → 10, differing in `read-next` alone, and expect it to STAY at 10** because a Phase 4 row regenerates nothing. **Re-measure it rather than trusting this paragraph** — §7.34 asserted a lexicon membership that was false and §7.35 asserted two more that were, which is why every one of these numbers is now stated with the command that produced it.

**Versions and counters to bump from.** Classroom GAS **v01.50g** (both `Classroom.gs` `VERSION` and `live-site-pages/gs-versions/Classroomgs.version.txt`); page **v01.14w**, and no page bump is expected. Scraper **v02.11g** and **not yours**. Profiler v01.39g and never yours. **Repo version: read `repository-information/repository.version.txt` on the day and bump it by 0.01 — do not take the number from this brief.** This is the one counter a brief structurally cannot state, because the handover commit that finishes writing the brief consumes the next version itself; the recursion was closed at v06.01r by deleting the claim rather than correcting it, and this brief keeps it deleted.

**THE DEPLOY — the §7.19 order, on its nineteenth use.** Read **Pages** first (`https://lightaisolutions.github.io/Sales/gs-versions/Classroomgs.version.txt` is a static file, free to read as often as you like) and the workflow's own `Classroom deploy confirmed (GET)` line from the job log. **Probe `?op=deploy` only if those two disagree** — the probe *deploys* whenever the running `/exec` bundle is behind GitHub. Use `curl -sL` if you do probe; without `-L` the body is empty and reads as a failure. **Deployment counters: Classroom 61/200 and Scraper 156/200, both MEASURED at v06.08r against the job log.** Session 10 put **two** `.gs` files in the merge diff, so exactly two steps fired and exactly two lines logged, both on the first GET leg with no POLL and no probe — `Scraper … Updated to v02.11g (deployment 156) | 156/200` and `Classroom … Updated to v01.50g (deployment 61) | 61/200`, the **eighteenth consecutive clean use** of the order and the two-line forecast holding exactly. **Your session adds one `.gs` and no more**, so forecast one step and one line; Scraper's 156/200 does not move unless a session touches `Scraper.gs`. **The counter rule, unchanged**: `Updated to` consumes one; a step that never fires consumes none and logs nothing; and **`Already up to date` describes the state at READ time and is silent on whether the call consumed a deployment** — never forecast it as zero.

**CHANGELOGS — you are probably NOT the repo rotator, but `Classroomgs.changelog.md` IS AT ITS CAP, so re-count both from the files.** Session 10 rotated nothing. `CHANGELOG.md` closed at **96 raw**, three of them dated 2026-09-16 EST, so on any later EST day it reads **96 non-exempt** against a 100 trigger and the next group in line (**2026-09-08, 10 sections**) does not arm until the file is back at 100. **`Classroomgs.changelog.md` closed at 50 raw / 48 non-exempt against a cap of 50** — two sections are dated 2026-09-16 EST, and **the moment EST rolls past that date the file stands at 50 non-exempt, which is the trigger**. So unless your push also lands on 2026-09-16 EST, **you rotate it**, and the group you move is its oldest whole date group — **2026-09-02 (11 sections)**. Deepen the clone first so every rotated header enriches, and run the **mandatory post-rotation grep** (`grep '^## \[v' Classroomgs.changelog-archive.md | grep -v '— \[' | head -5`) — v05.83r's enrichment failed silently on the first attempt and only that grep caught it. `Scrapergs.changelog.md` is **32/50** and untouched by a Phase 4 row; `Profilerhtml.changelog.md` is **49/50**, the closest of the three to its own trigger, and nothing a Phase 4 row does touches Profiler. **Keep using `TZ=America/New_York date` and trust it over the harness banner, which follows UTC** — that check has now decided the rotation on six consecutive sessions.

**Five open items recorded and not taken.** (i) The `.cl-tbl` `min-width` question now has **twenty** clean data points — session 10's three-column five-row bets table and its three-column nine-row indicators table both rendered clean at 1400px — and still wants a session that edits `Classroom.html`. (ii) The generated `read-next` **names** the landscape module without linking to it: `clFmt` has no link token at all, so it belongs with (i) plus a developer `--all` run. (iii) **`the-800-vdc-shift`'s `three-chains` intro is now fourteen inserts stale** — it belongs to whichever session opens that lesson to write in it, never as a standalone G4 edit, **and your row is the nearest miss yet**: `string-versus-central` is the modularity argument that lesson's rack-power half rests on. (iv) **§10.6's session-4 bullet block still sits out of chronological order** — cosmetic. (v) **A stale `lesson_ref` can make a segment due on its own**, which session 10 discovered while tracing its netting case: `the-campus-as-a-power-project` was built after `segment-clean-firm-and-nuclear`'s last regeneration, so the generator had been rendering a **built** lesson as `(planned)` for two days. **Every Phase 4 row that lands a lesson named in some segment's `READ_NEXT` leaves exactly this stale reference behind**, and nobody currently owns clearing it. Worth a developer decision rather than another session's incidental discovery.

> Author Phase 4 row 15 — the `string-versus-central` lesson — on Opus 5 xhigh as a fresh session. This is ONE mechanism lesson. It authors no guidance module, no dossier and no report, creates no track, regenerates no segment, and adds no Scraper seed.
>
> READ FIRST: repository-information/SESSION-CONTEXT.md (Latest Session); INTEGRATED-REMEDIATION-PLAN.md §7.36 (your brief), §7.34 and §7.35 (for the §8-item-6 contradiction and how row 14 resolved it, the both-halves due-count rule, and the three-part deepening test), §7.3 orders 5 and 6, and §6; CLASSROOM-CURRICULUM-PLAN.md §3.2's `string-versus-central` spec IN FULL — its seven section ids and their order are the contract — plus §4's teaching order, §7 row 15, §8 items 2, 4, 6 and 7, and §10.9. Then .claude/rules/classroom-app.md IN FULL, repository-information/CLASSROOM-SCHEMA.md, and .claude/rules/changelog-security.md before you write any public changelog line.
>
> **UNSHALLOW THE CLONE FIRST** — `git fetch --unshallow origin main`, before any measurement and before any pin. A shallow clone's boundary commit looks like it introduced every file, so `git log -1 --format=%cs -- <path>` returns the boundary's date rather than the file's — which is exactly how `concepts:profiler-concepts` gets a wrong pin.
>
> THE WORK — one push commit. (1) Author `clLessonStringVersusCentral_()` **inside** the `// CONTENT START` … `// CONTENT END` fence at §3.2's section ids in its order, id `string-versus-central`, group `Technology Foundations`, gate `tracks` on nine public stamps, and register it at the **end** of `clLessons_()`. (2) **INSERT it at §4 position 2 of `electrical-foundations`'s `lessons[]`** — between `four-machines` and `the-transformer-and-the-substation` — and bump that track's `updated`. **This completes the track.** (3) Resolve §8 item 6: the outline opens two `prose` sections back to back, which item 6 forbids. Check whether the corpus states a harvest delta or an availability figure for the mismatch/MPPT argument; if it does, make the second section `bars` or `table`; if it does not, insert a load-bearing section as row 14 did and say why. **Never invent proportions.** (4) Pin every input off the fetched document and resolve every ref by opening the file and matching its own `slug`. (5) Leave the module assertion at **19** and `Scraper.gs` at **v02.11g**. (6) Flip §7.3's Phase 4 row, §6's Phase 4 row, curriculum §7 row 15 and §4's track table, dated. (7) Write §7.37 — the next run's brief, which by the interleave is **S2 session 11, `hyperscalers-and-ai-labs`** — in the same commit, and **edit BOTH halves of any §7.x brief you refresh**: the prose paragraphs and the quoted prompt block.
>
> PREDICT THE PIPELINE SIGNATURE, AND IT IS NEW: **EXPECT P5.** `electrical-foundations`'s `lessons[]` holds §4 positions 1, 3, 4, 5, and yours is position **2**, so the correct write is a **mid-array insert** and P5 asserts `head[:len(base)] == base` on every track's `lessons[]`. **P5 firing here is the contract working, not a defect** — it binds the unattended committer, which may only append, while a developer session places a lesson at its planned position. **Do NOT "fix" it by appending**: that would put the lesson at position 5 and contradict §4 and the track's own stated order. Append in `clLessons_()` as normal — the registry is tested separately. Expect **P1, P5 and P8**; no P3 (a lesson inside the fence moves no `GATE_SYMBOLS` member, so **do not refresh `gateDigest`** — refreshing would hide a real signal); no P7 unless you regenerate a segment, which you do not.
>
> PREDICT BOTH HALVES OF THE DUE COUNT ANYWAY. §10.6 (w) requires both halves even though a Phase 4 row regenerates nothing. `--check` should read **9 due** as you start — `cells-and-chemistry`, `compute-and-the-rack`, `epc-and-construction`, `storage-developers-and-ipps`, `hyperscalers-and-ai-labs`, `neoclouds`, `capital`, `software-and-optimization`, `insurance-and-risk-transfer` — and none is yours under §10.4 and §10.9. **Both halves are forecast exactly in §7.36 and both were measured rather than guessed — re-measure anyway.** The deepening half is **no**: `string-versus-central` is in `CRITERION_LEXICON` (keys `string`, `central`) and in exactly one segment's `READ_NEXT` (`power-conversion-and-rack-power-silicon`), but neither keyword hits any of that segment's six `buyingCriteria`, so the three-part test fails at the third. The membership half is **yes**: registering a lesson flips `lesson_ref` for every segment naming it in `READ_NEXT`, which moves that segment's `read-next`, and that segment is not in the standing nine — so **expect `--check` 9 → 10, `read-next` alone, and STAYING at 10** because you regenerate nothing. **Do not take a lexicon claim from a brief**; §7.34 asserted one that was false and §7.35 asserted two more.
>
> TEACH THE FORK WITHOUT A LEAGUE TABLE. §3.2's own input note records C7's finding that **no independent US utility-scale PCS ranking exists**, so a lesson implying a winner would invent the thing C7 established does not exist. This is a public mechanism lesson and the parties belong to `landscape-power-conversion-and-rack-power-silicon-2026-09`, which is contributor-only: the lesson teaches the test, the module names the parties. **Read `four-machines` in full** — position 1, the only lesson a reader is guaranteed to have finished before yours — and enumerate what it owns as deliberate omissions in the header comment. Check the neighbours' tiles and callouts for a §10.6 (t) pre-declaration, including the new shape session 10 found: **a built public lesson handing off to an UNBUILT one**, leaving material in nobody's hands. Name such a gap; never fill it.
>
> DO NOT: author or revise a guidance module — `check-classroom-content.py` must stay at **19** registered modules. Do not add a Scraper seed or touch `Scraper.gs`. Do not touch `Profiler.gs` or `Profiler.html`. Do not run the segment generator at all. Do not "fix" the two items the curriculum checker reports as due for review: `landscape-utilities-2026-09` and `landscape-in-hall-power-2026-09` are **both at 2026-10-01 and both by design**. And **NEVER sweep a date across `Classroom.gs` with a blanket find-and-replace** — it holds nineteen independently dated guidance modules and session 9 corrupted a comment-deadline date inside `guidanceDocEo14420_()` that way; scope every replacement to the function you are authoring.
>
> VERIFY: `node --check` on a `.js` copy of `Classroom.gs`; `scripts/check-gas-inner-scripts.js`; `scripts/check-classroom-content.py` — **capture a pristine HEAD baseline BEFORE any edit. IT IS 0 ERRORS / 0 WARNINGS at 44 lessons / 8 tracks / 142 gate cases** and must be 0/0 at **45 lessons** after, with the module assertion **unchanged at 19**. Watch the micro-markup check: it has caught a defect in four separate sessions and nothing else in the toolchain sees it — nested emphasis in any clFmt-fed field, and `{{term}}` in any **textContent** field (a `proscons` card's `t` and `meta`, a `glossary[].d`, a tile's `k`/`v`/`sub`, the lesson's own `title`/`short`, a `bars` item's `label`/`sub`). Then `scripts/check-classroom-curriculum.py --strict` (no structural findings; 28 stale pins and 2 items due for review are pre-existing and not yours); `scripts/check-classroom-pipeline.py --selftest` (13 fixtures, 0 failures) and `--base origin/main` — **expect P1, P5 and P8, and read the P5 paragraph above before you see it.** `build-classroom-segments.py --check` — expect **9 → 10** (your registration makes `power-conversion-and-rack-power-silicon` due on `read-next` alone) and **expect it to stay at 10**, because a Phase 4 row regenerates nothing. Say so in the CHANGELOG before you run it. `scripts/check-readme-tree.py` after the version bumps. Playwright render of the new lesson at **analyst** (its gate is `tracks`) with ZERO page errors, plus a **viewer** denial from the real serving path — the lesson entrypoint is `clShowLesson(id)`; route `action=guidance` payloads to `handleGuidanceOp_` and everything else to `handleClassroomOp_`; `_gasPost` returns a PROMISE, `sess.permissions` must be an ARRAY in the shim, the session token must be at least 32 characters or the real guard returns SESSION_EXPIRED, the session keys are `Classroom_gas_session_token` / `_gas_user_email` / `_gas_user_role` / `_gas_user_permissions`, overlays to hide are `#auth-wall` / `#signing-out-wall` / `#auth-timers` / `#gas-layer-toggle`, and the scratch site root needs `favicon.ico` copied in beside `profiler-data/`, `sounds/` and `images/`. **SERVE THE SCRATCH COPY AT `/Classroom.html`, NOT AT THE SITE ROOT** — the page derives its sessionStorage key prefix from the URL path. **The page's CSP `connect-src` is `'self'` plus Google hosts, so a shim server on another port is REFUSED**: point `_gasPost` at a same-origin path (`/__gas`) and fulfil it with Playwright's `page.route()`, proxying to the Node shim. **Call the handlers through the vm context object (`ctx.handleClassroomOp_`), not from Node's own scope.** Then READ EVERY SECTION'S SCREENSHOT — session 10's only real defect was a number that contradicted itself two sections apart, and **only the screenshots caught it.** Bump Classroom's GAS version in BOTH `Classroom.gs` `VERSION` (from `v01.50g`) and `Classroomgs.version.txt`, with a GENERIC entry in its changelog.
>
> THE CHANGELOG ARITHMETIC: `CHANGELOG.md` is at **96 raw**, three sections dated 2026-09-16 EST, so it reads 96 non-exempt on any later day — **under the 100 trigger, you are probably not the repo rotator.** But **`Classroomgs.changelog.md` is at 50 raw / 48 non-exempt against a cap of 50**, with two sections dated 2026-09-16 EST: **the moment EST rolls past that date it stands at 50 non-exempt, which IS the trigger, so unless your push also lands on 2026-09-16 EST you rotate it** — the oldest whole date group, **2026-09-02 (11 sections)**. Deepen the clone first so every rotated header enriches, and run the **mandatory post-rotation grep**. Re-count both from the files rather than from this brief.
>
> Normal Pre-Commit and Pre-Push checklists; `claude/*` branch restarted from `origin/main` first, `git ls-remote` before each push — the auto-merge workflow adds a `[skip ci]` SHA-tracker commit after every merge, and after it merges use `git fetch --prune origin` rather than `git fetch origin main`, or the stale remote-tracking ref makes the stop hook report that SHA-tracker commit as unpushed. **Read `repository-information/repository.version.txt` for the version to bump from — do not take it from this brief**, whose own handover commit consumes the next number.
>
> AFTER THIS SESSION Phase 4 is 15 of 26 and S2 is 10 of ~19, so the interleave returns to S2 and the recommended next run is **S2 session 11 — `hyperscalers-and-ai-labs`** — for which §7.37 should be written.


### 7.37 · Paste-in brief for S2 session 11 — `hyperscalers-and-ai-labs` (Opus 5 xhigh) — written 2026-09-16 at v06.09r, at the close of Phase 4 row 15

**Why this and not another Phase 4 row.** Phase 4 is now **15 of 26** and S2 is **10 of ~19**, so the interleave stands at 15:10 and §7.4 item 6 hands the turn back to S2. Its next segment in the §7.3 order is **`hyperscalers-and-ai-labs`** — the eleventh landscape, the **twentieth** guidance module, and the first in the third Value Chain track's buyer half.

**UNSHALLOW FIRST — a Session Start Checklist item, not a brief's favour.** `git fetch --unshallow origin main` before any pin read, any `--check` run, any `git log -1 --format=%cs` date and any CHANGELOG rotation. Row 15 ran it and opened `--check` at the true 9 with `inputs moved: none` on every line.

**THE SEGMENT IS THE SMALLEST S2 HAS EVER AUTHORED, AND ITS SHAPE IS UNLIKE ANY OF THE TEN BEFORE IT — all of this measured at v06.09r, not recalled.** Eight members: **5 incumbents · 3 challengers · 0 adjacents** — `amazon`, `google`, `meta`, `microsoft`, `oracle` against `anthropic`, `openai`, `xai`. Above the floor comfortably, every one of the eight carrying a dossier and a study guide. Compare the ten built: integrators 32, developers and IPPs 34, AIDC landlords 30, in-hall 27, cells 22, power conversion 20, bridge 17, clean firm 16, grid equipment 15, utilities 14. **Eight is barely half the previous smallest**, so the `each-players-bet` table will be **eight rows** against clean firm's five and power conversion's twelve — and §10.6's one-row-per-incumbent-and-challenger rule holds at any size, so do not pad it.

**AND IT IS ONE OF ONLY TWO *CLOSED* SEGMENTS IN THE WHOLE TAXONOMY — a genuinely new shape for the (l)/(y4) instrument, and the measurement is the finding.** Three segments carry **zero adjacent members** (`hyperscalers-and-ai-labs`, `neoclouds`, `insurance-and-risk-transfer`); of those, only two are **closed** — zero adjacents *and* not one member belonging to any other segment. `hyperscalers-and-ai-labs` is one, at **8 of 8 pure plays**, and `insurance-and-risk-transfer` (3 members) is the other. **Session 10's instrument cannot be run here at all**: its rule — *adjacents 100 % ranked elsewhere means the segment is a PRODUCT other industries make, not a LAYER they sell into* — is computed over adjacents, and there are none. What the closure means instead is worth stating rather than borrowing: this segment is the chain's **terminus**. It buys from every segment above it and sells into none of them, which is why nobody arrives here as an adjacent and nobody leaves. **Re-measure it** (`profiler-segments.json`, count roles and cross-memberships) rather than trusting this paragraph; §7.34 asserted a lexicon membership that was false and §7.35 asserted two more.

**THE §10.6 (j) SPLIT TEST FIRES, BUT WEAKLY — check it, and expect to write a narrow split rather than a broad one.** Scanning the nineteen registered modules for the eight members' names at v06.09r: `utility-aidc-procurement-2026-08` names Google twice and Meta seven times and owns the **procurement process from the utility's side** — the four moves, the tariff terms, the buyer map; `nvidia-800vdc-2026-08` names Microsoft and Google four times each and owns the **reference architecture**; `landscape-aidc-developers-and-landlords-2026-09` names Microsoft and xAI once each and owns **the landlords these companies lease from**, which is the segment directly beneath this one; `power-infra-aidc-2026-08` names Meta once; `large-load-interconnection-2026-09` names none of them. **No module covers this segment's parties as parties**, so (j)'s full form does not fire — but the landlord landscape is close enough that the boundary must be stated in the opening paragraph anyway, in the direction session 4 established: **that module owns who builds and leases the hall; this one owns who signs the lease and sets the capex.**

**THE MEMBER VERSIONS AND DATES TO PIN THE CLAIMS LEDGER AGAINST — measured 2026-09-16, and to be re-read off the fetched documents anyway (G2, G7).** `amazon`@v10 / 2026-09-06 · `google`@v9 / 2026-09-07 · `meta`@v9 / 2026-09-06 · `microsoft`@v4 / 2026-08-30 · `oracle`@v4 / 2026-08-30 · `anthropic`@v3 / 2026-09-06 · `openai`@v5 / 2026-09-05 · `xai`@v4 / 2026-08-30. **The `recentDevelopments` distribution is lopsided and it will shape the module**: `anthropic` carries **45** and **7 `policyExposure[]` entries**, `google` 22, `xai` 12, `meta` 11, `microsoft`/`oracle`/`openai` 10 each, `amazon` 9 — while `openai` and `xai` carry **zero** `policyExposure` at all. Eleven policy entries across eight members is the thinnest fence any landscape has had, so plan the `the-fence` section around scarcity and say so rather than padding it.

**`reviewBy` — read, do not sort, for the twelfth consecutive session, and this fence is the thinnest yet.** Six distinct failure modes are on record: a sort returning nothing (sessions 3, 8); the wrong product line (7, 9); ten months late (2); the nearest *date* rather than the nearest *gate* (7); a gate that is already another module's clock (5, 6); and both rejection classes intersecting until nothing survives (10). **With eleven policy entries in total and two members carrying none, expect the six-month default from `updated` and expect to write the rejections out.** One arithmetic constraint unchanged: the curriculum checker reports **2** items due today (`landscape-utilities-2026-09` and `landscape-in-hall-power-2026-09`, both 2026-10-01, both by design — **do not "fix" either**) and a `reviewBy` inside your own 30-day horizon makes it 3. Sessions 8, 9, 10 and rows 14 and 15 all avoided that deliberately. Choose knowingly.

**THE MODULE ASSERTION MOVES, AND IT IS THE ONE EDIT OUTSIDE THE FENCE A LANDSCAPE SESSION MAKES.** `scripts/check-classroom-content.py` asserts **19** registered guidance modules at v06.09r; this session takes it to **20**. Row 15 left it at 19 because a Phase 4 row authors none. **The Scraper seed of `industry-guidance.md` step 9 is yours to consider** — score every candidate term against the existing roster before adding one, as session 10 did against all 431. `Scraper.gs` stands at **v02.11g**; if you seed, it bumps and your merge diff carries **two** `.gs` files, two deploy steps and two logged lines. If you do not, it carries one.

**THE SEGMENT REGENERATION IS OBLIGATORY AND ITS ARITHMETIC IS FORECASTABLE.** §10.4 makes S2 the owner of its own segment's regeneration because §10.6 requires the segment lesson's `read-next` to deep-link the landscape module, which cannot happen before the module exists. Run `--segment hyperscalers-and-ai-labs` **and nothing else** (§10.4, §10.9) with `--today` set to your EST date. **`--check` should open at 10 due**, and the ten are `cells-and-chemistry`, **`power-conversion-and-rack-power-silicon`**, `compute-and-the-rack`, `epc-and-construction`, `storage-developers-and-ipps`, **`hyperscalers-and-ai-labs`**, `neoclouds`, `capital`, `software-and-optimization`, `insurance-and-risk-transfer` — **your segment is already in the set**, so expect **10 → 10 → 9** exactly as session 10 measured, `read-next` alone at every step. **Predict both halves and record which fired** (§10.6 (w)) even though the deepening test is structurally irrelevant to a landscape — row 14's session established that it governs a *lesson* registration, and row 15's confirmed it a second time.

**P7 IS FORESEEABLE AND YOU ARE ALMOST CERTAINLY CLEAR OF IT.** `segment-hyperscalers-and-ai-labs`'s literal reads `updated: 2026-09-14`, so any EST push date later than that advances it and P7 cannot fire. **Read the literal before you worry about it**, as row 14 did, and record the outcome either way. Expect **P1** on the developer files and **P7 not firing**; **no P3** (a landscape below the fence moves no `GATE_SYMBOLS` member, so leave `gateDigest` alone — refreshing it hides a real signal); **no P5** unless you insert rather than append, which a landscape never does.

**`power-conversion-and-rack-power-silicon` IS IN YOUR OPENING TEN AND IT IS NOT YOURS.** Row 15 registered `string-versus-central`, which flipped that segment's `lesson_ref` from `(planned)` to the real title and made it due on `read-next` alone. It is the **third consecutive Phase 4 row to leave one behind**, which is the evidence open item (v) was waiting for: this is a systematic consequence of the design rather than an incident, and nobody owns clearing it. **Do not clear it** — §10.4 and §10.9 keep one `--segment` run per session — but name it in the §8 record so the count stops looking like drift.

**Baselines to capture BEFORE any edit, all measured at the close of v06.09r.** `check-classroom-content.py` **0 errors / 0 warnings at 45 lessons, 8 tracks, 142 gate cases** — held unbroken through seventeen consecutive sessions — and your commit must return it to 0/0 at **45 lessons** (a landscape adds no lesson) with the module assertion moved **19 → 20**. `check-classroom-curriculum.py --strict`: no structural findings, **28 stale pins** (pre-existing, on the hand-authored lessons; a guidance module carries no `provenance`, so you will add none) and **2 items due for review**. `check-classroom-pipeline.py --selftest`: **13 fixtures / 0 failures**. `build-classroom-segments.py --check`: **10 due**, listed above.

**Versions and counters to bump from.** Classroom GAS **v01.51g** (both `Classroom.gs` `VERSION` and `live-site-pages/gs-versions/Classroomgs.version.txt`); page **v01.14w**, and no page bump is expected. Scraper **v02.11g** — yours only if you seed. Profiler v01.39g and never yours. **Repo version: read `repository-information/repository.version.txt` on the day and bump it by 0.01 — do not take the number from this brief.** This is the one counter a brief structurally cannot state, because the handover commit that finishes writing the brief consumes the next version itself.

**THE DEPLOY — the §7.19 order, on its twentieth use.** Read **Pages** first (`https://lightaisolutions.github.io/Sales/gs-versions/Classroomgs.version.txt` is a static file, free to read as often as you like) and the workflow's own `Classroom deploy confirmed (GET)` line from the job log. **Probe `?op=deploy` only if those two disagree** — the probe *deploys* whenever the running `/exec` bundle is behind GitHub. Use `curl -sL` if you do probe; without `-L` the body is empty and reads as a failure. **Deployment counters, MEASURED at v06.10r against the job log, and the two figures have DIVERGED for the first time.** Row 15's line reads `Classroom deploy confirmed (POLL 1): Updated to v01.51g (deployment 63) | 62/200` — **`deployment 63` against a tally of `62/200`**, where every previous record had them equal. The log says why: the GET leg fired a deploy that did not confirm, the POST fallback fired another, and only the poll read a confirmation, so **two deployments were consumed and one was tallied**. It is also the **first non-first-GET confirmation in nineteen uses** of this order, ending an eighteen-session streak. **Treat the deployment number as authoritative and the counter as under-reporting by one**: Classroom's real headroom is 200 − 63 = **137**. Scraper is unchanged at **156/200** and does not move unless you seed. **One `.gs` in your diff means one step and one line; two means two** — that half of the forecast held exactly. The counter rule, now with an observed instance: `Updated to` consumes one; a step that never fires consumes none and logs nothing; **a leg that fires and does not confirm still consumes one and the tally does not see it**; and `Already up to date` describes the state at READ time and is silent on consumption — never forecast it as zero.

**CHANGELOGS — YOU ARE THE GAS ROTATOR AND YOU ALMOST CERTAINLY CANNOT ESCAPE IT.** Row 15's push landed **on** 2026-09-16 EST, so §7.36's escape clause fired and nothing rotated. It closed `Classroomgs.changelog.md` at **51 raw / 48 non-exempt against a cap of 50**, with **three** sections dated 2026-09-16 EST — and the moment EST rolls past that date all three stop being exempt and the file stands at **51 non-exempt**, which is the trigger. **Unless your push also lands on 2026-09-16 EST, you rotate it**, and the group you move is its oldest whole date group — **2026-09-02 (11 sections)**; 51 − 11 = 40, plus yours = 41, under the cap in one pass. `CHANGELOG.md` closed at **99 raw** after the v06.10r handover, five dated 2026-09-16 EST, so on a later day it reads **99 non-exempt** against a 100 trigger — **one clear, so your own section takes it to 100, which IS the trigger by the "reaches" rule. Expect to rotate BOTH files.** Re-count from the file rather than from this paragraph. The next repo group in line is **2026-09-08 (10 sections)**. `Scrapergs.changelog.md` **32/50**, and it moves only if you seed; `Profilerhtml.changelog.md` **49/50** and untouched by S2. Deepen the clone first so every rotated header enriches, and run the **mandatory post-rotation grep** (`grep '^## \[v' Classroomgs.changelog-archive.md | grep -v '— \[' | head -5`) — v05.83r's enrichment failed silently on the first attempt and only that grep caught it. **Keep using `TZ=America/New_York date` and trust it over the harness banner, which follows UTC** — that check has now decided the rotation on seven consecutive sessions, and at row 15 it decided *against* rotating.

**Five open items recorded and not taken.** (i) The `.cl-tbl` `min-width` question now has **twenty-one** clean data points — row 15's six-row four-column table rendered clean at 1400px — and still wants a session that edits `Classroom.html`. (ii) The generated `read-next` **names** the landscape module without linking to it: `clFmt` has no link token at all, so it belongs with (i) plus a developer `--all` run. (iii) **`the-800-vdc-shift`'s `three-chains` intro is now fifteen inserts stale**; it belongs to whichever session opens that lesson to write in it, never as a standalone G4 edit. (iv) **§10.6's session-4 bullet block still sits out of chronological order** — cosmetic. (v) **A stale `lesson_ref` can make a segment due on its own, and row 15 was the third consecutive Phase 4 row to leave one** — `power-conversion-and-rack-power-silicon` is due for exactly that reason and nobody owns clearing it. Three in a row makes it systematic rather than incidental, which is the developer decision this item has been waiting for. **A sixth, new at row 15:** `the-800-vdc-shift` hands the DC arc taxonomy to *"the engineering lesson at the stricter gate"* — `dc-fault-engineering`, §7 row 20, **unbuilt** — so a built public lesson has declined material into nobody's hands for the second time in the corpus. Row 20's session inherits it; nothing else should touch it.

> Author S2 session 11 — the `hyperscalers-and-ai-labs` landscape module — on Opus 5 xhigh as a fresh session. This is ONE guidance module. It authors no lesson, no dossier and no report, creates no track, and regenerates exactly ONE segment: `hyperscalers-and-ai-labs`.
>
> READ FIRST: repository-information/SESSION-CONTEXT.md (Latest Session); INTEGRATED-REMEDIATION-PLAN.md §7.37 (your brief), §7.35 and §7.36 (for the closed-segment measurement, the both-halves due-count rule and the three-part deepening test), §7.3 orders 5 and 6, and §6; CLASSROOM-CURRICULUM-PLAN.md §10.2 (the floor rule), §10.4 (the generator and who owns a run), §10.6 IN FULL — its nine section ids and their order are the contract, and every lettered finding (a)–(aa5) is a rule a previous session paid for — §10.5 and §10.9. Then .claude/rules/industry-guidance.md IN FULL, .claude/rules/classroom-app.md, repository-information/CLASSROOM-SCHEMA.md, and .claude/rules/changelog-security.md before you write any public changelog line.
>
> **UNSHALLOW THE CLONE FIRST** — `git fetch --unshallow origin main`, before any measurement and before any pin. A shallow clone's boundary commit looks like it introduced every file, so `git log -1 --format=%cs -- <path>` returns the boundary's date rather than the file's, and every rotation SHA lookup beyond the horizon fails.
>
> THE WORK — one push commit. (1) Write `repository-information/industry-guidance/landscape-hyperscalers-and-ai-labs-analysis.md` FIRST as the source of truth, and write its split section before any other section. (2) Author `guidanceDocLandscapeHyperscalersAndAiLabs_()` **below** the `// CONTENT END` fence in `Classroom.gs` at §10.6's nine section ids in its order, id `landscape-hyperscalers-and-ai-labs-2026-09`, lane `The Value Chain`, tier **contributor**, and register it at the **END** of `guidanceDocs_()`'s The Value Chain lane. (3) Move the guidance-module assertion in `scripts/check-classroom-content.py` from **19 to 20** — the one edit outside the fence a landscape session makes. (4) Regenerate **exactly one** segment: `--segment hyperscalers-and-ai-labs --today <your EST date>`, and nothing else. (5) Consider the `industry-guidance.md` step 9 Scraper seed, scoring every candidate against the existing roster first; `Scraper.gs` is at v02.11g and only moves if you seed. (6) Flip §7.3's S2 row, §6's S2 row, §10.6's progress note and its findings block, dated. (7) Write §7.38 — the next run's brief, which by the interleave is **Phase 4 row 16, `clean-firm-power`** — in the same commit, and **edit BOTH halves of any §7.x brief you refresh**: the prose paragraphs and the quoted prompt block.
>
> THE SEGMENT IS THE SMALLEST S2 HAS AUTHORED AND ONE OF ONLY TWO *CLOSED* SEGMENTS IN THE TAXONOMY — MEASURED AT v06.09r, RE-MEASURE IT ANYWAY. Eight members: **5 incumbents · 3 challengers · 0 adjacents** — `amazon`, `google`, `meta`, `microsoft`, `oracle` against `anthropic`, `openai`, `xai`, every one carrying a dossier and a study guide. The previous smallest landscape was utilities at 14; eight is barely half of it, so `each-players-bet` is an **eight-row** table and §10.6's one-row-per-incumbent-and-challenger rule holds at any size — **do not pad it**. Three segments carry zero adjacents; only two are **closed**, meaning zero adjacents *and* not one member belonging to any other segment, and this is one of them at **8 of 8 pure plays**. **Session 10's instrument cannot be run here**: its rule is computed over adjacents and there are none. What the closure means is the chain's **terminus** — this segment buys from every segment above it and sells into none, which is why nobody arrives as an adjacent and nobody leaves. State that as a measurement, not as a borrowed rule, and re-derive it from `profiler-segments.json` before you write it: §7.34 asserted a membership claim that was false and §7.35 asserted two more.
>
> THE §10.6 (j) SPLIT TEST FIRES WEAKLY — RUN IT AND WRITE A NARROW SPLIT. Scanning all nineteen registered modules for the eight members' names at v06.09r: `utility-aidc-procurement-2026-08` owns the procurement process from the **utility's** side; `nvidia-800vdc-2026-08` owns the **reference architecture**; `landscape-aidc-developers-and-landlords-2026-09` owns **the landlords these companies lease from**, the segment directly beneath this one; `power-infra-aidc-2026-08` names Meta once; `large-load-interconnection-2026-09` names none of them. **No module covers this segment's parties as parties**, so (j)'s full form does not fire — but state the boundary against the landlord landscape in your opening paragraph anyway, in session 4's direction: **that module owns who builds and leases the hall; this one owns who signs the lease and sets the capex.**
>
> PREDICT BOTH HALVES OF THE DUE COUNT. `--check` should read **10 due** as you start — `cells-and-chemistry`, `power-conversion-and-rack-power-silicon`, `compute-and-the-rack`, `epc-and-construction`, `storage-developers-and-ipps`, `hyperscalers-and-ai-labs`, `neoclouds`, `capital`, `software-and-optimization`, `insurance-and-risk-transfer`. **Your segment is already in the set**, so expect **10 → 10 → 9**, `read-next` alone at every step, exactly as session 10 measured. **`power-conversion-and-rack-power-silicon` is in that ten and is NOT yours** — Phase 4 row 15 registered `string-versus-central`, which flipped its `lesson_ref` from `(planned)` to the real title; that is the **third consecutive Phase 4 row** to leave one behind, so name it in the §8 record and **do not clear it** (§10.4 and §10.9 keep one `--segment` run per session). Record which half fired even though the deepening test is structurally irrelevant to a landscape.
>
> P7 IS FORESEEABLE AND YOU ARE ALMOST CERTAINLY CLEAR. `segment-hyperscalers-and-ai-labs`'s literal reads `updated: 2026-09-14`, so any later EST push date advances it and P7 cannot fire. **Read the literal before you worry about it** and record the outcome either way. Expect **P1** on the developer files; **no P3** — a landscape below the fence moves no `GATE_SYMBOLS` member, so leave `gateDigest` alone, since refreshing it hides a real signal; **no P5**, because a landscape appends and never inserts.
>
> `reviewBy` — READ, DO NOT SORT, and this fence is the thinnest any landscape has had. Eleven `policyExposure[]` entries across eight members, seven of them `anthropic`'s, with `openai` and `xai` carrying **none at all** — so expect the six-month default from `updated` and write your rejections out. Six distinct failure modes are on record and session 10 added the sixth: both standing rejection classes intersecting until nothing survives. **One arithmetic constraint**: the curriculum checker reports **2** items due today (`landscape-utilities-2026-09` and `landscape-in-hall-power-2026-09`, both 2026-10-01, both by design — **do not "fix" either**) and a `reviewBy` inside your own 30-day horizon makes it 3. Choose knowingly.
>
> DO NOT: author or revise a lesson — `check-classroom-content.py` must stay at **45 lessons / 8 tracks / 142 gate cases**. Do not touch `Profiler.gs` or `Profiler.html`. Do not run the generator with `--all` or on any segment but your own. Do not "fix" the two items the curriculum checker reports as due for review. And **NEVER sweep a date across `Classroom.gs` with a blanket find-and-replace** — it holds nineteen independently dated guidance modules and session 9 corrupted a comment-deadline date inside `guidanceDocEo14420_()` that way; scope every replacement to the function you are authoring. One more mechanical trap: when appending a function to `Classroom.gs`, the anchor is the previous function's closing `}` on its own line, NOT the literal's `};` — splicing after the `};` nests your function inside the previous one, which is valid JavaScript so `node --check` passes while the registry can never resolve the name. Row 15 verified its own splice by counting unbalanced braces before the definition and getting 0; do the same.
>
> VERIFY: `node --check` on a `.js` copy of `Classroom.gs`; `scripts/check-gas-inner-scripts.js`; `scripts/check-classroom-content.py` — **capture a pristine HEAD baseline BEFORE any edit. IT IS 0 ERRORS / 0 WARNINGS at 45 lessons / 8 tracks / 142 gate cases** and must be 0/0 after, with the module assertion moved **19 → 20**. Watch the micro-markup check: it has now caught a defect in **five** separate sessions and nothing else in the toolchain sees it — nested emphasis in any clFmt-fed field, and `{{term}}` in any **textContent** field (a `proscons` card's `t` and `meta`, a `glossary[].d`, a tile's `k`/`v`/`sub`, a `bars` item's `label`/`sub`, and a section's own `title`). Then `scripts/check-classroom-curriculum.py --strict` (no structural findings; 28 stale pins and 2 items due for review are pre-existing and not yours); `scripts/check-classroom-pipeline.py --selftest` (13 fixtures, 0 failures) and `--base origin/main`. `build-classroom-segments.py --check` before and after each step. `scripts/check-readme-tree.py` after the version bumps. Playwright render of the new module at **contributor** with ZERO page errors, plus an **analyst** denial from the real serving path — the module entrypoint is `clShowGuidanceDoc(id)` and `action=guidance` payloads route to `handleGuidanceOp_` while everything else routes to `handleClassroomOp_`; `_gasPost` returns a PROMISE, `sess.permissions` must be an ARRAY in the shim, the session token must be at least 32 characters or the real guard returns SESSION_EXPIRED, the session keys are `Classroom_gas_session_token` / `_gas_user_email` / `_gas_user_role` / `_gas_user_permissions`, overlays to hide are `#auth-wall` / `#signing-out-wall` / `#auth-timers` / `#gas-layer-toggle` plus `#cl-app{display:block !important}`, and the scratch site root needs `favicon.ico` copied in beside `profiler-data/`, `sounds/` and `images/`. **SERVE THE SCRATCH COPY AT `/Classroom.html`, NOT AT THE SITE ROOT** — the page derives its sessionStorage key prefix from the URL path. **The page's CSP `connect-src` is `'self'` plus Google hosts, so a shim server on another port is REFUSED**: point `_gasPost` at a same-origin path (`/__gas`) and fulfil it with Playwright's `page.route()`, proxying to the Node shim. **Call the handlers through the vm context object (`ctx.handleGuidanceOp_`), not from Node's own scope.** Then READ EVERY SECTION'S SCREENSHOT — session 10's only real defect was a number that contradicted itself two sections apart, and only the screenshots caught it. Bump Classroom's GAS version in BOTH `Classroom.gs` `VERSION` (from `v01.51g`) and `Classroomgs.version.txt`, with a GENERIC entry in its changelog.
>
> THE DEPLOY: the §7.19 order, on its twentieth use. Read Pages first (`https://lightaisolutions.github.io/Sales/gs-versions/Classroomgs.version.txt` is a static file) and the workflow's own `Classroom deploy confirmed (GET)` line from the job log. Probe `?op=deploy` only if those two disagree, and use `curl -sL` if you do. Deployment counters, MEASURED at v06.10r against the job log: Classroom logged `Updated to v01.51g (deployment 63) | 62/200` and Scraper is unchanged at 156/200. **The deployment number and the tally have diverged for the first time** — 63 against 62 — because the GET leg fired a deploy that did not confirm, the POST fallback fired another, and only POLL 1 confirmed, so two deployments were consumed and one was tallied. That is also the **first non-first-GET confirmation in nineteen uses**. Treat the deployment number as authoritative: Classroom's real headroom is 200 − 63 = **137**. One `.gs` in the merge diff still means one step and one logged line; two means two. The counter rule, with the new instance folded in: `Updated to` consumes one; a step that never fires consumes none and logs nothing; **a leg that fires and does not confirm still consumes one and the tally does not see it**; `Already up to date` is silent on consumption and must never be forecast as zero.
>
> THE CHANGELOG ARITHMETIC: **you are the GAS rotator and you almost certainly cannot escape it.** `Classroomgs.changelog.md` closed at **51 raw / 48 non-exempt against a cap of 50**, with three sections dated 2026-09-16 EST — the moment EST rolls past that date it stands at **51 non-exempt**, which IS the trigger, so unless your push also lands on 2026-09-16 EST you rotate it. The group you move is the oldest whole date group, **2026-09-02 (11 sections)**: 51 − 11 = 40, plus yours = 41, under the cap in one pass. `CHANGELOG.md` closed at **99 raw** after the v06.10r handover, five dated 2026-09-16 EST, so on a later day it reads 99 non-exempt against a 100 trigger — one clear, so **your own section takes it to 100, which IS the trigger, and you should expect to rotate BOTH files**; the repo group in line is 2026-09-08 (10 sections). Re-count from the file rather than from this prompt. Deepen the clone first so every rotated header enriches, and run the mandatory post-rotation grep. Keep using `TZ=America/New_York date` and trust it over the harness banner, which follows UTC — at row 15 that check decided *against* rotating.
>
> Normal Pre-Commit and Pre-Push checklists; `claude/*` branch restarted from `origin/main` first, `git ls-remote` before each push — the auto-merge workflow adds a `[skip ci]` SHA-tracker commit after every merge, and after it merges use `git fetch --prune origin` rather than `git fetch origin main`, or the stale remote-tracking ref makes the stop hook report that SHA-tracker commit as unpushed. Read `repository-information/repository.version.txt` for the version to bump from — do not take it from this prompt, whose own handover commit consumes the next number.
>
> AFTER THIS SESSION Phase 4 is 15 of 26 and S2 is 11 of ~19, so the interleave returns to Phase 4 and the recommended next run is **Phase 4 row 16 — `clean-firm-power`** — for which §7.38 should be written.

### 7.38 · Paste-in brief for Phase 4 row 16 — `clean-firm-power` (Opus 5 xhigh) — written 2026-09-16 at v06.11r, at the close of S2 session 11

**Why this and not another S2 segment.** Phase 4 is **15 of 26** and S2 is now **11 of ~19**, so the interleave stands at 15:11 and §7.4 item 6 hands the turn back to Phase 4. Its lowest unbuilt row is **16, `clean-firm-power`** — and it is not an ordinary row: it is **the lesson session 10 found a built public lesson handing material to, by name, when the lesson did not exist**. That debt has been outstanding for one session. `aidc-campus` position 4, gate `tracks`, group The AI Data-Center Wave; spec in `CLASSROOM-CURRICULUM-PLAN.md` §3.4 and §7 row 16; the §2.2 nuclear reversal is argued at §2.2's "Nuclear and SMRs" bullet and should be read before drafting.

**UNSHALLOW FIRST — a Session Start Checklist item, not a brief's favour.** `git fetch --unshallow origin main` before any pin read, any `--check` run, any `git log -1 --format=%cs` date and any CHANGELOG rotation. Session 11 ran it and opened `--check` at the true 10 with `inputs moved: none` on every line.

**YOU ARE PAYING OFF A NAMED DEBT, AND THE CREDITOR'S EXACT WORDS ARE THE SPEC.** `contracts-and-revenue` (row 11, public, built) carries a section `clean-firm-and-the-restart` whose opening sentence reads: *"The physics — why a reactor follows load badly, what a small modular reactor changes, what clean firm admits — belongs to Clean Firm Power in the campus track, and is deliberately not repeated here."* **Open that section and read it before drafting.** It names three things it declined; your outline owes all three — `why-a-reactor-follows-badly`, the SMR rung of `the-ladder-of-first-power`, and `four-kinds-of-clean-firm`. Session 10's (aa1) recorded this as a **third posture** for §10.6 (s): a lesson that says the test is unowned. Row 16 is the row that makes it owned, and **that is the single most important thing this row does**. Say so in the lesson, and check whether any *other* built lesson pre-declares to you as well (session 11 searched all twenty-six for its own id and found none; run the same search across every registered hand-authored lesson for `clean-firm-power`, its title *When the Campus Signs a Reactor*, and the phrase *clean firm*).

**THE SEGMENT LANDSCAPE ON THIS SUBJECT IS BUILT, AND THE SPLIT IS THE INVERSE OF THE USUAL ONE.** `landscape-clean-firm-and-nuclear-2026-09` (v06.07r, contributor) ranks the parties: three incumbents as three answers to one objection, two pre-commercial challengers, eleven adjacents, a price ladder nobody disclosed. **You are the public lesson and you own the physics** — which is the reverse of every split S2 has written, where the module owns the parties and the *built* lesson owned the mechanism. Read that module's `what-the-record-does-not-say` and its `each-players-bet` intro before you write: they state what it declined, and anything it declined on the grounds that the physics belongs elsewhere is yours. **Do not name a company in a ranking** — the module is the ranking and it sits behind a gate; this lesson is `tracks` and public.

**FOUR NEIGHBOURS, AND TWO OF THEM ARE THIS ROW'S OWN `READ_NEXT` PARTNERS.** `the-campus-as-a-power-project` (row 9, built, public, position 3 of the same track — this lesson sits immediately after it) owns the campus lifecycle, the three clocks and the five-rung ladder; `contracts-and-revenue` (row 11, built, public) owns the instruments — hourly versus annual matching, the corporate and virtual PPA, additionality as a quality test, the uprate, the restart as a contract, the green tariff; `landscape-clean-firm-and-nuclear-2026-09` owns the parties; and `landscape-utilities-2026-09` owns the franchise. **Enumerate the omissions in the lesson's header comment** as every S2 session does — the practice has paid on both sides of the fence.

**THE STAMPS ARE NINE AND THE SPEC NAMES THE SECTIONS EACH ONE ANSWERS.** `study:oklo` (19 sections — what a reactor is doing, the coolant sets the building, the fuel is the constraint, two regulators and two clocks, why a reactor follows badly, nobody sells you a reactor, the megawatt ladder, the design-to-first-power timeline) · `study:x-energy` (14 sections — the fuel is the reactor, why 750 degrees is the product, the fuel factory, what a construction permit is) · `study:sargent-lundy` · `study:constellation-energy` (restarting a reactor; capacity markets) · `study:talen-energy` (behind the meter versus in front of it; what FERC actually decided; the additionality objection) · `study:vistra` · `study:southern-company` (Vogtle and the risk appetite) · `study:fermi-america` (what a COL is and what 2033 means) · `study:google` (hourly matching; clean firm power) · `concepts:profiler-concepts`. **Read every date off the fetched file and resolve every ref by matching its own `slug` (G2, G7)** — do not take a version or a date from this brief.

**THE OUTLINE IS SPECIFIED AND IT IS NINE SECTIONS.** `what-firm-means` (prose) · `the-ladder-of-first-power` (timeline — restart → uprate → large new build → SMR) · `why-a-reactor-follows-badly` (prose) · `the-campus-still-buys-gas-and-batteries` (callout — **the storage argument stated as physics rather than preference**, which is the lesson's commercial payload) · `four-kinds-of-clean-firm` (table — nuclear · geothermal · gas with capture · storage-firmed renewables) · `the-additionality-fight` (callout) · `where-it-fails` (callout) · `drill` · `check-yourself`. §3.2's seven-section default does not bind here because §3.4 writes nine down; **if the evidence pushes you off the spec, say which section moved and why**, as row 15 did when it inserted an eighth.

**THE INSERT IS MID-ARRAY AND P5 WILL FIRE.** `aidc-campus` reads `heat-is-the-constraint` → `the-cooling-plant-and-water` → `the-campus-as-a-power-project` → **`clean-firm-power`** → `where-bess-plugs-in`, so the id goes at **position 4 of five**, between row 9's lesson and the guidance-gated closer. Row 15's insert at §4 position 2 failed `head[:len(base)] == base` by construction and reported **P5**; expect the same, and expect **P1** on the developer files. **P8 will probably not fire** — it iterates only lessons present at both base and head *and touched* — but **run the three-part deepening test anyway and record both halves** (§10.6 (w)): `clean-firm-power` **is** in `CRITERION_LEXICON` (keys `nuclear`, `smr`, `clean firm`, `reactor`) and **is** in exactly one `READ_NEXT` (`clean-firm-and-nuclear`), and session 10 measured that its criterion 2 hits on the keyword `reactor` — **so this registration is a live candidate to deepen `segment-clean-firm-and-nuclear`'s `what-is-bought-and-on-what`, which is the one case the netting rule does not cover.** Predict it, then measure it.

**THE DUE COUNT, AND YOUR ROW MAY MOVE IT.** `--check` closed at **9 due** after session 11's regeneration: `cells-and-chemistry`, `power-conversion-and-rack-power-silicon`, `compute-and-the-rack`, `epc-and-construction`, `storage-developers-and-ipps`, `neoclouds`, `capital`, `software-and-optimization`, `insurance-and-risk-transfer`. **`clean-firm-and-nuclear` is NOT in that set** — session 10 regenerated it — so this row is the sessions-1-to-4 shape: expect **9 → 10 → 10**, the tenth entrant being `clean-firm-and-nuclear` itself, differing in `read-next` (the `(planned)` flip) **and possibly `what-is-bought-and-on-what`** if the deepening fires. **Do not regenerate it** — §10.4 and §10.9 keep the `--segment` run inside the S2 session that owns the segment, and a Phase 4 row runs none. Leave it and name it, as session 11 named `power-conversion-and-rack-power-silicon`. **This will be the fourth consecutive Phase 4 row to leave a stale `lesson_ref` behind**; four in a row is past the point where open item (v) is a hypothesis.

**`reviewBy`** — six months from `updated` unless the lesson's own dated gates say otherwise, and here they might: the §3.4 spec builds a whole section on the licence being the schedule, and the clean-firm landscape took **2026-12-31** on a licensing convergence. A *lesson* is not bound by the module's clock and should not copy it reflexively; read the stamped guides for a gate the lesson itself leans on, and **check the curriculum checker's horizon before choosing** — it reports **2** items due today (`landscape-utilities-2026-09` and `landscape-in-hall-power-2026-09`, both 2026-10-01, both by design; **do not "fix" either**), and a date inside your own 30 days makes it 3.

**Baselines to capture BEFORE any edit, all measured at the close of v06.11r.** `check-classroom-content.py` **0 errors / 0 warnings at 45 lessons, 8 tracks, 142 gate cases** — held unbroken through eighteen consecutive sessions — and your commit must return it to 0/0 at **46 lessons** with the guidance-module assertion left at **20** (a Phase 4 row authors none). `check-classroom-curriculum.py --strict`: no structural findings, **28 stale pins**, **2** items due for review. `check-classroom-pipeline.py --selftest`: **13 fixtures / 0 failures**. `build-classroom-segments.py --check`: **9 due**, listed above.

**Versions and counters to bump from.** Classroom GAS **v01.52g** (both `Classroom.gs` `VERSION` and `live-site-pages/gs-versions/Classroomgs.version.txt`); page **v01.14w** and no page bump expected. Scraper **v02.12g** and never a Phase 4 row's. Profiler v01.39g and never yours. **Repo version: read `repository-information/repository.version.txt` on the day and bump by 0.01** — do not take it from this brief, whose own handover commit consumes the next number.

**THE DEPLOY — the §7.19 order, on its twenty-first use.** Read **Pages** first (`https://lightaisolutions.github.io/Sales/gs-versions/Classroomgs.version.txt` is a static file) and the workflow's own `Classroom deploy confirmed` line from the job log. **Probe `?op=deploy` only if those two disagree**, with `curl -sL`. **Deployment counters, MEASURED at v06.12r against the v06.11r job log, and the v06.10r divergence has RECONCILED.** Two `.gs` files in the merge diff fired exactly two steps and logged exactly two lines, both confirming on the **first GET** — `Scraper deploy confirmed (GET): Updated to v02.12g (deployment 157) | 157/200` and `Classroom deploy confirmed (GET): Updated to v01.52g (deployment 64) | 64/200`. **Classroom's deployment number and its tally are equal again**: the number went 63 → 64 (one step) while the tally went 62 → **64** (two steps), so the tally caught up rather than staying one behind. **That corrects v06.10r's reading**: the divergence was a *read-time artifact* of the unconfirmed leg, not a permanent offset — both figures derive from the same state and reconcile on the next read. **Classroom's real headroom is 200 − 64 = 136; Scraper's is 200 − 157 = 43.** Scraper crossed three-quarters of its allowance this session and is the counter to watch. Pages agreed with the log on both files and **no `?op=deploy` probe was made** — the §7.19 order resolved on its two reads for the **twentieth** time, with the first-GET streak restored after v06.10r's single break. The counter rule: `Updated to` consumes one; a step that never fires consumes none and logs nothing; **a leg that fires and does not confirm still consumes one and the tally does not see it**; `Already up to date` is silent on consumption and must never be forecast as zero.

**CHANGELOGS — THE ROTATION HAS BEEN DEFERRED TWICE BY THE SAME CLAUSE AND YOU WILL ALMOST CERTAINLY NOT ESCAPE IT A THIRD TIME.** §7.36 and §7.37 both forecast a rotation and both were beaten by the same fact: the push landed **on** 2026-09-16 EST, so that day's sections stayed exempt. v06.11r closed `Classroomgs.changelog.md` at **52 raw / 48 non-exempt against a cap of 50** with **four** sections dated 2026-09-16 EST, and `CHANGELOG.md` at **100 raw / 93 non-exempt** against a 100 trigger with **seven** dated that day. **The moment EST rolls past 2026-09-16 both files jump to their raw counts** — 52 against 50 and 100 against 100 — **and both are then armed**. Oldest whole date groups: `Classroomgs.changelog.md` **2026-09-02 (11 sections)** → 52 − 11 = 41 plus yours = 42; `CHANGELOG.md` **2026-09-08 (10 sections)** → 100 − 10 = 90 plus yours = 91. Both clear in one pass. **Re-count from the files rather than from this paragraph**, deepen the clone first so every rotated header enriches, and run the **mandatory post-rotation grep** (`grep '^## \[v' <archive> | grep -v '— \[' | head -5`). `Scrapergs.changelog.md` 33/50; `Profilerhtml.changelog.md` **49/50**, the closest of the three to its own trigger and untouched by anything Phase 4 does. **Keep using `TZ=America/New_York date` and trust it over the harness banner, which follows UTC** — that check has now decided the rotation on eight consecutive sessions and decided *against* it on the last two.

**Six open items recorded and not taken.** (i) The `.cl-tbl` `min-width` question has **twenty-three** clean data points after session 11's two tables at 1400px, and still wants a session that edits `Classroom.html`. (ii) The generated `read-next` **names** the landscape module without linking to it — `clFmt` has no link token — so it belongs with (i) plus a developer `--all` run. (iii) **`the-800-vdc-shift`'s `three-chains` intro is now sixteen inserts stale**; it belongs to whichever session opens that lesson to write in it. (iv) **§10.6's session-4 bullet block still sits out of chronological order** — cosmetic. (v) **A stale `lesson_ref` can make a segment due on its own**, and row 16 will be the **fourth consecutive** Phase 4 row to leave one; that is now systematic beyond argument and wants a developer decision. (vi) **The DC-arc-taxonomy gap**: `the-800-vdc-shift` hands the taxonomy to `dc-fault-engineering`, §7 row 20, unbuilt — row 20's session inherits it. **A seventh, new at session 11:** `restart` scores zero across all 444 Scraper terms and belongs in `topic-landscape-clean-firm-and-nuclear`'s seed, recorded for that module's next revision rather than taken by a landscape that does not own it — and a Phase 4 row adds no seed at all, so it is not row 16's either.

> Author Phase 4 row 16 — the `clean-firm-power` lesson — on Opus 5 xhigh as a fresh session. This is ONE mechanism lesson. It authors no guidance module, no dossier and no report, creates no track, regenerates no segment, and adds no Scraper seed.
>
> READ FIRST: repository-information/SESSION-CONTEXT.md (Latest Session); INTEGRATED-REMEDIATION-PLAN.md §7.38 (your brief), §7.36 and §7.37 (for the P5 signature on a mid-array insert, the both-halves due-count rule and the three-part deepening test), §7.3 orders 5 and 6, and §6; CLASSROOM-CURRICULUM-PLAN.md §3.4's `clean-firm-power` spec IN FULL, §2.2's "Nuclear and SMRs" reversal bullet, §4 (the `aidc-campus` order), §7 row 16, §8 IN FULL and §10.6 (for what the clean-firm landscape already owns). Then .claude/rules/classroom-app.md IN FULL, repository-information/CLASSROOM-SCHEMA.md, and .claude/rules/changelog-security.md before you write any public changelog line.
>
> **UNSHALLOW THE CLONE FIRST** — `git fetch --unshallow origin main`, before any measurement and before any pin. A shallow clone's boundary commit looks like it introduced every file, so `git log -1 --format=%cs -- <path>` returns the boundary's date rather than the file's, and every rotation SHA lookup beyond the horizon fails.
>
> **THE WORK — one push commit.** (1) Author `clLessonCleanFirmPower_()` **inside** the `// CONTENT START` … `// CONTENT END` fence in `Classroom.gs`, register it at the **end** of `clLessons_()`, and **insert its id at position 4 of five** in `clTrackAidcCampus_()`'s `lessons[]` — between `the-campus-as-a-power-project` and `where-bess-plugs-in`. (2) Nine sections at §3.4's ids and order. (3) Flip §7.3's Phase 4 row and §6's Phase 4 row to 16 of 26, curriculum §7 row 16 to built with the full record, and the §4 `aidc-campus` table's status cell. (4) Write §7.39 — the next run's brief, which by the interleave is **S2 session 12, `neoclouds`** — in the same commit, and edit BOTH halves of any §7.x brief you refresh: the prose paragraphs and the quoted prompt block.
>
> **YOU ARE PAYING OFF A DEBT A BUILT PUBLIC LESSON NAMED.** `contracts-and-revenue`'s `clean-firm-and-the-restart` section says in its opening sentence that the physics — *why a reactor follows load badly, what a small modular reactor changes, what clean firm admits* — **belongs to Clean Firm Power and is deliberately not repeated here**. Open that section and read it before drafting; those three declined things are your outline's spine. Session 10 recorded this as the first time a built public lesson pre-declared a handoff to a lesson that did not exist. **Row 16 is the row that makes it exist.** Search every hand-authored lesson registered in `clLessons_()` for `clean-firm-power`, for its title *When the Campus Signs a Reactor*, and for the phrase *clean firm*, and say in the §8 record what you found.
>
> **THE SPLIT RUNS THE OTHER WAY FROM EVERY S2 SPLIT.** `landscape-clean-firm-and-nuclear-2026-09` is built, contributor-gated, and owns the **parties** — three incumbents as three answers to one objection, two pre-commercial challengers, a price ladder nobody disclosed. **You are public and you own the physics.** Read that module's `what-the-record-does-not-say` and its `each-players-bet` intro before writing: whatever it declined because the physics belongs elsewhere is yours, and **nothing it ranks is.** Do not name a company in a ranking anywhere in this lesson. The other three neighbours are `the-campus-as-a-power-project` (immediately before you in the same track; owns the lifecycle, the three clocks, the five-rung ladder), `contracts-and-revenue` (owns the instruments — hourly versus annual matching, the corporate and virtual PPA, additionality, the uprate, the restart as a contract, the green tariff) and `landscape-utilities-2026-09` (owns the franchise). **Enumerate the omissions in the function's header comment.**
>
> **NINE STAMPS, EVERY DATE READ OFF THE FETCHED FILE (G2, G7):** `study:oklo` · `study:x-energy` · `study:sargent-lundy` · `study:constellation-energy` · `study:talen-energy` · `study:vistra` · `study:southern-company` · `study:fermi-america` · `study:google` · `concepts:profiler-concepts`. Resolve every ref by opening the file and matching its own `slug`. Do not take a version or a date from this brief — §7.34, §7.35 and §7.37 each carried a measurement that did not survive inspection.
>
> **NINE SECTIONS, SPECIFIED AT §3.4:** `what-firm-means` (prose) · `the-ladder-of-first-power` (timeline) · `why-a-reactor-follows-badly` (prose) · `the-campus-still-buys-gas-and-batteries` (callout — **the storage argument stated as physics rather than preference**, which is the lesson's commercial payload) · `four-kinds-of-clean-firm` (table) · `the-additionality-fight` (callout) · `where-it-fails` (callout) · `drill` · `check-yourself`. If the evidence pushes you off the spec, **say which section moved and why**. Reactor engineering beyond what the firmness argument needs stays excluded — §2.2 says so and the Oklo and X-energy guides remain the deep link.
>
> **PREDICT BOTH HALVES OF THE DUE COUNT, AND THIS TIME THE DEEPENING IS A LIVE CANDIDATE.** `--check` should read **9 due** as you start — `cells-and-chemistry`, `power-conversion-and-rack-power-silicon`, `compute-and-the-rack`, `epc-and-construction`, `storage-developers-and-ipps`, `neoclouds`, `capital`, `software-and-optimization`, `insurance-and-risk-transfer`. **`clean-firm-and-nuclear` is NOT in that set**, so expect **9 → 10 → 10**, the entrant being that segment. Run the three-part test programmatically and record both halves: `clean-firm-power` **is** in `CRITERION_LEXICON` (`nuclear`, `smr`, `clean firm`, `reactor`) and **is** in exactly one `READ_NEXT`, and session 10 measured a criterion-2 keyword hit — **so `what-is-bought-and-on-what` may deepen alongside `read-next`, which is the one case the netting rule does not cover.** **Do not regenerate the segment** (§10.4, §10.9) — name it in the §8 record as the fourth consecutive Phase 4 row to leave a stale `lesson_ref`.
>
> **P5 WILL FIRE AND IT IS THE CONTRACT WORKING.** The insert is mid-array, so `head[:len(base)] == base` fails by construction — exactly row 15's signature. Expect **P1** on the developer files and **P5**; **no P3** unless you move a `GATE_SYMBOLS` member (and if you do, refresh `gateDigest` in the same commit); **P8 probably not**, because it iterates only lessons present at both base and head *and touched*. Record what actually fired.
>
> **`reviewBy`** — six months from `updated` unless the stamped guides give the lesson its own dated gate. **Do not copy the clean-firm landscape's 2026-12-31 reflexively**: a lesson is not bound by a module's clock. The curriculum checker reports **2** items due today (`landscape-utilities-2026-09` and `landscape-in-hall-power-2026-09`, both 2026-10-01, both by design — **do not "fix" either**) and a date inside your own 30-day horizon makes it 3. Choose knowingly.
>
> **DO NOT:** author or revise a guidance module — `check-classroom-content.py` must stay at **20** registered modules. Do not touch `Profiler.gs`, `Profiler.html` or `Scraper.gs`. Do not run the segment generator with `--all` or on any segment. Do not "fix" the two items the curriculum checker reports as due for review. And **NEVER sweep a date across `Classroom.gs` with a blanket find-and-replace** — it holds twenty independently dated guidance modules and session 9 corrupted a comment-deadline date inside `guidanceDocEo14420_()` that way; scope every replacement to the function you are authoring. One more mechanical trap: when appending a function to `Classroom.gs`, the anchor is the previous function's closing `}` on its own line, **NOT** the literal's `};` — splicing after the `};` nests your function inside the previous one, which is valid JavaScript so `node --check` passes while the registry can never resolve the name. Verify by counting unbalanced braces before your definition and getting **0**, as rows 15 and session 11 both did.
>
> **VERIFY:** `node --check` on a `.js` copy of `Classroom.gs`; `scripts/check-gas-inner-scripts.js`; `scripts/check-classroom-content.py` — capture a pristine HEAD baseline BEFORE any edit. It is **0 errors / 0 warnings at 45 lessons / 8 tracks / 142 gate cases** and must be 0/0 after at **46 lessons**, with the module assertion left at 20. **Watch the micro-markup check: it has now caught a defect in SIX separate sessions** and nothing else in the toolchain sees it — nested emphasis in any clFmt-fed field, and `{{term}}` in any textContent field (a `proscons` card's `t` and `meta`, a `glossary[].d`, a tile's `k`/`v`/`sub`, a `bars` item's `label`/`sub`, and a section's own `title`). Then `scripts/check-classroom-curriculum.py --strict` (no structural findings; 28 stale pins and 2 items due for review are pre-existing and not yours); `scripts/check-classroom-pipeline.py --selftest` (13 fixtures, 0 failures) and `--base origin/main`. `build-classroom-segments.py --check` before and after each step. `scripts/check-readme-tree.py` after the version bump. Playwright render of the new lesson at analyst with ZERO page errors, plus a viewer denial from the real serving path — the lesson entrypoint is `clShowLesson(id)` and `action=guidance` payloads route to `handleGuidanceOp_` while everything else routes to `handleClassroomOp_`; `_gasPost` returns a PROMISE, `sess.permissions` must be an ARRAY in the shim, the guidance op reads `p.session` (**not** `p.token`) and it must be at least 32 characters or the real guard returns SESSION_EXPIRED, the session keys are `Classroom_gas_session_token` / `_gas_user_email` / `_gas_user_role` / `_gas_user_permissions`, overlays to hide are `#auth-wall` / `#signing-out-wall` / `#auth-timers` / `#gas-layer-toggle` plus `#cl-app{display:block !important}`, `AUTO_REFRESH` is a `var AUTO_REFRESH = true;` declaration (patch the declaration, not a `key: value` pair), and the scratch site root needs `favicon.ico` copied in beside `profiler-data/`, `sounds/` and `images/`. **SERVE THE SCRATCH COPY AT `/Classroom.html`, NOT AT THE SITE ROOT** — the page derives its sessionStorage key prefix from the URL path. The page's CSP `connect-src` is `'self'` plus Google hosts, so a shim server on another port is REFUSED: point `_gasPost` at a same-origin path (`/__gas`) and fulfil it with Playwright's `page.route()`, proxying to the Node shim. Call the handlers through the vm context object (`ctx.handleClassroomOp_`), not from Node's own scope. **Then READ EVERY SECTION'S SCREENSHOT** — session 11's only two real defects were a fabricated percentage in a sales note and a figure that read as contradicting another section two sections away, and only the screenshots caught either. Bump Classroom's GAS version in BOTH `Classroom.gs` `VERSION` (from `v01.52g`) and `Classroomgs.version.txt`, with a GENERIC entry in its changelog.
>
> **THE DEPLOY:** the §7.19 order, on its twenty-first use. Read Pages first (`https://lightaisolutions.github.io/Sales/gs-versions/Classroomgs.version.txt` is a static file) and the workflow's own `Classroom deploy confirmed` line from the job log. Probe `?op=deploy` only if those two disagree, and use `curl -sL` if you do. **Deployment counters, MEASURED at v06.12r against the v06.11r job log: Classroom `deployment 64 | 64/200`, Scraper `deployment 157 | 157/200`**, both confirming on the first GET. Two `.gs` files fired two steps and logged two lines, exactly as forecast — **and the v06.10r divergence RECONCILED**: Classroom's number went 63 → 64 while its tally went 62 → 64, so the tally caught up and the two are equal again. The divergence was a read-time artifact of an unconfirmed leg, not a standing offset. **Classroom's headroom is 200 − 64 = 136; Scraper's is 200 − 157 = 43** — Scraper is past three quarters and is now the counter to watch, though a Phase 4 row never moves it. One `.gs` in your merge diff means one step and one logged line. The counter rule: `Updated to` consumes one; a step that never fires consumes none and logs nothing; a leg that fires and does not confirm still consumes one and the tally does not see it; `Already up to date` is silent on consumption and must never be forecast as zero.
>
> **THE CHANGELOG ARITHMETIC: THE ROTATION HAS BEEN DEFERRED TWICE BY THE SAME CLAUSE AND YOU ALMOST CERTAINLY CANNOT ESCAPE IT A THIRD TIME.** `Classroomgs.changelog.md` closed at **52 raw / 48 non-exempt against a cap of 50**, with **four** sections dated 2026-09-16 EST; `CHANGELOG.md` closed at **100 raw / 93 non-exempt** against a 100 trigger, with **seven** dated that day. The moment EST rolls past 2026-09-16 both jump to their raw counts and both are armed. The groups in line are **2026-09-02 (11 sections)** for the GAS changelog (52 − 11 = 41, plus yours = 42) and **2026-09-08 (10 sections)** for the repo changelog (100 − 10 = 90, plus yours = 91) — both clear in one pass. Re-count from the files rather than from this prompt. Deepen the clone first so every rotated header enriches, and run the mandatory post-rotation grep. Keep using `TZ=America/New_York date` and trust it over the harness banner, which follows UTC — at sessions 10 and 11 that check decided *against* rotating.
>
> Normal Pre-Commit and Pre-Push checklists; `claude/*` branch restarted from `origin/main` first, `git ls-remote` before each push — the auto-merge workflow adds a `[skip ci]` SHA-tracker commit after every merge, and after it merges use `git fetch --prune origin` rather than `git fetch origin main`, or the stale remote-tracking ref makes the stop hook report that SHA-tracker commit as unpushed. Read `repository-information/repository.version.txt` for the version to bump from — do not take it from this prompt, whose own handover commit consumes the next number.
>
> AFTER THIS SESSION Phase 4 is 16 of 26 and S2 is 11 of ~19, so the interleave returns to S2 and the recommended next run is **S2 session 12 — `neoclouds`** — for which §7.39 should be written.
>
> Then, give me a prompt to paste into a new Opus 5 xhigh session to continue the action plan, then remember session.

### 7.39 · Paste-in brief for S2 session 12 — `neoclouds` (Opus 5 xhigh) — written 2026-09-16 at v06.13r, at the close of Phase 4 row 16

**Why this and not another Phase 4 row.** Phase 4 is now **16 of 26** and S2 is **11 of ~19**, so the interleave stands at 16:11 and §7.4 item 6 hands the turn back to S2. Its next segment in the §7.3 sales-value order is **`neoclouds`** — segment position 14, **seven members: one incumbent and six challengers**, the most lopsided roster S2 has met. Spec: `CLASSROOM-CURRICULUM-PLAN.md` §10.6, the section contract and the order; the module is the **twelfth** landscape and the **twenty-first** guidance module.

**UNSHALLOW FIRST — a Session Start Checklist item, not a brief's favour.** `git fetch --unshallow origin main` before any pin read, any `--check` run, any `git log -1 --format=%cs` date and any CHANGELOG rotation. Row 16 ran it and took the clone to **1,285 commits**; `--check` then opened at the true 9 with `inputs moved: none` on every line.

**ONE INCUMBENT AND SIX CHALLENGERS — AND THE SHAPE IS THE MODULE'S SUBJECT, NOT AN AWKWARDNESS TO WRITE AROUND.** Every landscape so far has had a plural incumbency to sort: nine incumbents holding nine stages of a chain, six franchises that do not compete, three incumbents as three answers to one objection. **This one has a single incumbent**, which means `who-dominates-and-on-what-basis` is a section about *one* company's basis and `each-players-bet` is a seven-row table in which six rows are attacks on the same position. Read that as the finding rather than the constraint: **a segment with one incumbent and six challengers is either a market that has just formed or one that is about to consolidate**, and the dossiers say which. Measure it before writing — re-derive the roster from `profiler-segments.json` on the day rather than copying this paragraph, because §7.34, §7.35, §7.37 and §7.38 each carried a measurement that did not survive inspection (§7.38's was the GAS changelog's oldest date group: it said 2026-09-02 with 11 sections, and the file's oldest whole group is **2026-09-01 with 2**).

**THE SEGMENT'S OWN DEFINITION NAMES THE THING THAT MAKES IT INTERESTING, AND IT IS A CREDIT STRUCTURE RATHER THAN A TECHNOLOGY.** The registry defines neoclouds as *"the GPU-cloud operators that sell contracted AI compute to labs and hyperscalers on leased or self-built capacity and thin balance sheets — faster than hyperscalers, weaker credit, and the tenants whose leases carry most of the credit-substitution structures in the corpus."* **Credit substitution is already taught**, and by the Value Chain lane opener: `reading-the-graph` uses this segment's own credit-substitution chain as its worked example, names it out loud, and is **public**. Session 11 recorded that as §10.6 (t)'s newest shape — a landscape pre-empted by the lesson that teaches people to read it. **Read `reading-the-graph` in full before drafting** and enumerate what it owns as deliberate omissions; the module's job is who sits where, not how to read an edge.

**FIVE BUYING CRITERIA, AND THE READ_NEXT SET IS THREE BUILT PUBLIC LESSONS.** The criteria are time to power and the landlord's ability to deliver critical IT megawatts on schedule; GPU-backed and capacity-backed financing terms; leased shell against own-build and the power ownership that comes with it; backlog quality and customer concentration behind the lease; and rack density with the liquid-cooling specification the halls must meet. `READ_NEXT` is `the-campus-as-a-power-project` · `bridge-power` · `redundancy-by-the-numbers`, **all three built and all three public** — the first S2 session in several whose whole mapped set exists. Run §10.6 (y3)'s check anyway and print the intersection rather than assuming it: the generator maps a criterion to a lesson only when that lesson is in this segment's `READ_NEXT` **and** a `CRITERION_LEXICON` key appears in the criterion's text.

**THE DUE COUNT, AND YOUR SEGMENT IS ALREADY IN IT.** `--check` closes at **10 due** after row 16: `cells-and-chemistry`, `power-conversion-and-rack-power-silicon`, **`clean-firm-and-nuclear`**, `compute-and-the-rack`, `epc-and-construction`, `storage-developers-and-ipps`, **`neoclouds`**, `capital`, `software-and-optimization`, `insurance-and-risk-transfer`. Yours is among them, differing in `read-next` alone, and **you regenerate exactly one segment — your own** (`--segment neoclouds --today <your EST date>`), never `--all` and never another. Expect **10 → 10 → 9**: your regeneration removes yours from the set and a landscape adds nobody. `clean-firm-and-nuclear` entered the set at row 16 with **two** differing sections and is **not yours to clear** — it belongs to whichever session next regenerates it, and it is the standing evidence for open item (v).

**ROW 16 SETTLED A QUESTION §10.6 (w) LEFT OPEN, AND THE ANSWER CHANGES WHAT A PHASE 4 ROW COSTS.** The three-part deepening test — a lesson in `CRITERION_LEXICON`, in exactly one `READ_NEXT`, with a real keyword hit on that segment's own `buyingCriteria` — had never passed all three parts until row 16. It passed, and the consequence was measured: registering `clean-firm-power` moved **two** sections of `segment-clean-firm-and-nuclear`, `read-next` **and** `what-is-bought-and-on-what`, which is the one case the netting rule does not cover. **A Phase 4 row can now be expected to deepen a segment as well as flip its `lesson_ref`**, and a session that forecasts only the membership half will under-call the count.

**`reviewBy` — read, do not sort, for the twelfth consecutive session.** Seven failure modes are on record and row 16 met the first one again (a sort returning nothing at all: 43 `policyExposure` entries across nine dossiers, 24 dated, **zero** future day-level dates). This segment's members are **compute operators rather than regulated entities**, so expect the same thinness and be ready for the six-month default stated explicitly with the rejections in writing. **The arithmetic constraint stands**: the curriculum checker reports **2** items due today (`landscape-utilities-2026-09` and `landscape-in-hall-power-2026-09`, both 2026-10-01, both by design — **do not "fix" either**) and a `reviewBy` inside your own 30-day horizon makes it 3.

**THE MODULE ASSERTION MOVES 20 → 21, AND THE SCRAPER SEED IS YOURS TO CONSIDER.** `scripts/check-classroom-content.py` asserts **20** registered guidance modules today; a landscape session moves it by one and adds its session line to the running comment. The `industry-guidance.md` step 9 Scraper seed **is** an S2 obligation: score every candidate term against **both** seed arrays first (`SCRAPER_INTEREST_TOPIC_SEEDS` and `SCRAPER_SEGMENT_SEEDS` — 469 raw, **444 distinct**; session 11 nearly published a false correction by counting one array), and `Scraper.gs` moves off **v02.12g** only if you seed. **One open seed is already recorded and is not yours**: `restart` scores zero across all 444 terms and belongs to `topic-landscape-clean-firm-and-nuclear`'s next revision.

**Baselines to capture BEFORE any edit, all measured at the close of v06.13r.** `check-classroom-content.py` **0 errors / 0 warnings at 46 lessons, 8 tracks, 142 gate cases** — nineteen consecutive sessions unbroken — and your commit must return it to 0/0 at **46 lessons** (a landscape authors no lesson) with the module assertion at **21**. `check-classroom-curriculum.py --strict`: no structural findings, **28 stale pins**, **2** items due for review. `check-classroom-pipeline.py --selftest`: **13 fixtures / 0 failures**. `build-classroom-segments.py --check`: **10 due**, listed above.

**Versions and counters to bump from.** Classroom GAS **v01.53g** (both `Classroom.gs` `VERSION` and `live-site-pages/gs-versions/Classroomgs.version.txt`); page **v01.14w** and no page bump expected. Scraper **v02.12g**, which moves only if you seed. Profiler v01.39g and never yours. **Repo version: read `repository-information/repository.version.txt` on the day and bump by 0.01** — do not take it from this brief, whose own handover commit consumes the next number.

**THE DEPLOY — the §7.19 order, on its twenty-second use.** Read **Pages** first (`https://lightaisolutions.github.io/Sales/gs-versions/Classroomgs.version.txt` is a static file) and the workflow's own `Classroom deploy confirmed` line from the job log. **Probe `?op=deploy` only if those two disagree**, with `curl -sL`. **Counters, MEASURED at v06.14r against the v06.13r job log: Classroom `deployment 65 | 65/200`, confirming on the first GET; Scraper unchanged at `157/200` because its step never fired.** One `.gs` in the merge diff fired one step and logged one line, exactly as forecast. Classroom's number and tally are equal for a second consecutive cycle (64/64 → 65/65), holding v06.12r's reconciliation. Headroom: Classroom **135**, Scraper **43**. **Scraper is the counter to watch** — it passed three quarters of its 200-deployment allowance at v06.11r and a landscape session that seeds will move it, where a Phase 4 row never does. **Scraper is the counter to watch** — it crossed three quarters of its 200-deployment allowance at v06.11r and a landscape session that seeds will move it, where a Phase 4 row never does. The counter rule is unchanged: `Updated to` consumes one; a step that never fires consumes none and logs nothing; a leg that fires and does not confirm still consumes one and the tally does not see it; `Already up to date` is silent on consumption and must never be forecast as zero.

**CHANGELOGS — THE ROTATION HAS NOW BEEN DEFERRED THREE TIMES BY THE SAME CLAUSE, AND THE FOURTH DEFERRAL IS ARITHMETICALLY IMPOSSIBLE.** §7.36, §7.37 and §7.38 each forecast a rotation and each was beaten by the same fact: the push landed **on** 2026-09-16 EST, so that day's sections stayed exempt. v06.13r closed `Classroomgs.changelog.md` at **53 raw / 48 non-exempt against a cap of 50** with **five** sections dated 2026-09-16 EST, and `CHANGELOG.md` at **102 raw / 93 non-exempt** against a 100 trigger with **nine** dated that day. **No later day can repeat the escape**: the moment EST reads 2026-09-17 or later, both files jump to their raw counts — 53 against 50 and 102 against 100 — and both rotate. Oldest whole date groups, **re-counted from the files at v06.13r and correcting §7.38**: `Classroomgs.changelog.md` **2026-09-01 (2 sections)** then **2026-09-02 (11)**; `CHANGELOG.md` **2026-09-08 (10 sections)**. The GAS file needs both of its first two groups to clear (53 − 2 = 51, still above 50; 51 − 11 = 40, plus yours = 41), so **do not stop after one group** — the rule is repeat until fewer than the trigger remain. The repo file clears in one (102 − 10 = 92, plus yours = 93). Deepen the clone first so every rotated header enriches, and run the **mandatory post-rotation grep** (`grep '^## \[v' <archive> | grep -v '— \[' | head -5`). `Scrapergs.changelog.md` 33/50; `Profilerhtml.changelog.md` **49/50**, the closest of the three to its own trigger and untouched by anything S2 does unless it seeds. **Keep using `TZ=America/New_York date` and trust it over the harness banner, which follows UTC.**

**Seven open items recorded and not taken.** (i) The `.cl-tbl` `min-width` question has **twenty-four** clean data points after row 16's five-column table at 1400px, and still wants a session that edits `Classroom.html`. (ii) The generated `read-next` **names** the landscape module without linking to it — `clFmt` has no link token — so it belongs with (i) plus a developer `--all` run. (iii) **`the-800-vdc-shift`'s `three-chains` intro is now seventeen inserts stale**. (iv) **§10.6's session-4 bullet block still sits out of chronological order** — cosmetic. (v) **A stale `lesson_ref` can make a segment due on its own**, and row 16 was the **fourth consecutive** Phase 4 row to leave one — now with **two** differing sections rather than one, which strengthens rather than softens the case for a developer decision. (vi) **The DC-arc-taxonomy gap**: `the-800-vdc-shift` hands the taxonomy to `dc-fault-engineering`, §7 row 20, unbuilt. (vii) **`restart` scores zero across all 444 Scraper terms** and belongs in `topic-landscape-clean-firm-and-nuclear`'s seed at that module's next revision. **And one closed at row 16**: the unbuilt-handoff shape session 10 named is no longer open for `clean-firm-power` — `contracts-and-revenue`'s declined physics now has an owner.

> Author S2 session 12 — the `neoclouds` landscape module — on Opus 5 xhigh as a fresh session. This is ONE guidance module. It authors no lesson, no dossier and no report, creates no track, and regenerates exactly ONE segment — its own.
>
> READ FIRST: repository-information/SESSION-CONTEXT.md (Latest Session); INTEGRATED-REMEDIATION-PLAN.md §7.39 (your brief), §7.37 and §7.38 (for the landscape signature, the both-halves due-count rule and what row 16 settled about the deepening test), §7.3 orders 5 and 6, and §6; CLASSROOM-CURRICULUM-PLAN.md §10.6 IN FULL (the section contract, the order, and every session's findings block), §10.2 (the floor), §10.3–10.4 (the generator and what it owns), §10.9, and §7 for the mechanism lessons this segment's `READ_NEXT` maps to. Then .claude/rules/industry-guidance.md IN FULL, .claude/rules/classroom-app.md, repository-information/CLASSROOM-SCHEMA.md, and .claude/rules/changelog-security.md before you write any public changelog line.
>
> **UNSHALLOW THE CLONE FIRST** — `git fetch --unshallow origin main`, before any measurement and before any pin. A shallow clone's boundary commit looks like it introduced every file, so `git log -1 --format=%cs -- <path>` returns the boundary's date rather than the file's, and every rotation SHA lookup beyond the horizon fails. **You are almost certainly the rotator** (see the changelog paragraph below), so this is load-bearing rather than hygienic.
>
> **THE WORK — one push commit.** (1) Write `repository-information/industry-guidance/landscape-neoclouds-analysis.md` FIRST as the source of truth, and write its split section before any other section. (2) Author `guidanceDocLandscapeNeoclouds_()` **below** the `// CONTENT END` fence in `Classroom.gs` at §10.6's nine section ids in its order, id `landscape-neoclouds-2026-09`, lane `The Value Chain`, tier **contributor**, and register it at the **END** of `guidanceDocs_()`'s The Value Chain lane. (3) Move the guidance-module assertion in `scripts/check-classroom-content.py` from **20 to 21** — the one edit outside the fence a landscape session makes — and add your session line to its running comment. (4) Regenerate **exactly one** segment: `--segment neoclouds --today <your EST date>`, and nothing else. (5) Consider the `industry-guidance.md` step 9 Scraper seed, scoring every candidate against **both** existing seed arrays first; `Scraper.gs` is at v02.12g and only moves if you seed. (6) Flip §7.3's S2 row, §6's S2 row, §10.6's progress note and its findings block, dated. (7) Write §7.40 — the next run's brief, which by the interleave is **Phase 4 row 17, `where-the-chain-breaks`** — in the same commit, and **edit BOTH halves of any §7.x brief you refresh**: the prose paragraphs and the quoted prompt block.
>
> **ONE INCUMBENT AND SIX CHALLENGERS — THE MOST LOPSIDED ROSTER S2 HAS MET, AND THE SHAPE IS THE SUBJECT.** Seven members: `coreweave` (incumbent) and `nebius`, `lambda`, `crusoe`, `iren`, `fluidstack`, `nscale` (challengers). Every landscape so far has had a plural incumbency to sort; this one has **one**, so `who-dominates-and-on-what-basis` is a section about a single company's basis and `each-players-bet` is a seven-row table in which six rows attack the same position. **Write that as the finding, not around it**: a segment with one incumbent and six challengers is either a market that has just formed or one about to consolidate, and the dossiers say which. **Re-derive the roster from `profiler-segments.json` on the day** — §7.34, §7.35, §7.37 and §7.38 each carried a measurement that did not survive inspection.
>
> **THE LANE OPENER ALREADY TEACHES THIS SEGMENT'S OWN STRUCTURE, IN PUBLIC.** The registry defines neoclouds as *"GPU-cloud operators that sell contracted AI compute to labs and hyperscalers on leased or self-built capacity and thin balance sheets — faster than hyperscalers, weaker credit, and the tenants whose leases carry most of the credit-substitution structures in the corpus."* **`reading-the-graph` — the Value Chain lane opener, built, public — uses this segment's own credit-substitution chain as its worked example and names the segment out loud.** Session 11 recorded that as §10.6 (t)'s newest shape: a landscape pre-empted by the lesson that teaches people to read it. Read it in full before drafting and **enumerate what it owns as deliberate omissions** in the function's header comment. Your job is who sits where, not how to read an edge.
>
> **FIVE BUYING CRITERIA AND A READ_NEXT OF THREE BUILT PUBLIC LESSONS.** Criteria: time to power and the landlord's ability to deliver critical IT megawatts on schedule · GPU-backed and capacity-backed financing terms · leased shell against own-build and the power ownership that comes with it · backlog quality and customer concentration behind the lease · rack density and the liquid-cooling specification the halls must meet. `READ_NEXT` is `the-campus-as-a-power-project` · `bridge-power` · `redundancy-by-the-numbers`, **all three built and all three public** — the first S2 session in several whose whole mapped set exists. **Print the §10.6 (y3) intersection rather than assuming it**: the generator maps a criterion to a lesson only when that lesson is in this segment's `READ_NEXT` **and** a `CRITERION_LEXICON` key appears in the criterion's own text.
>
> **PREDICT BOTH HALVES OF THE DUE COUNT.** `--check` should read **10 due** as you start — `cells-and-chemistry`, `power-conversion-and-rack-power-silicon`, `clean-firm-and-nuclear`, `compute-and-the-rack`, `epc-and-construction`, `storage-developers-and-ipps`, **`neoclouds`**, `capital`, `software-and-optimization`, `insurance-and-risk-transfer`. **Yours is already in the set**, differing in `read-next` alone, so expect **10 → 10 → 9**: your one regeneration clears yours and a landscape adds nobody. **`clean-firm-and-nuclear` is NOT yours to clear** — row 16 put it in the set with **two** differing sections and it belongs to whichever session next regenerates it.
>
> **ROW 16 SETTLED §10.6 (w)'s OPEN QUESTION AND THE ANSWER MATTERS TO YOUR FORECASTS.** The three-part deepening test had never passed all three parts until row 16. It passed, and registering `clean-firm-power` moved **two** sections of `segment-clean-firm-and-nuclear` — `read-next` **and** `what-is-bought-and-on-what` — which is the one case the netting rule does not cover. **A Phase 4 row can now deepen a segment as well as flip its `lesson_ref`**, and a session forecasting only the membership half will under-call the count.
>
> **`reviewBy`** — read, do not sort, for the twelfth consecutive session. Seven failure modes are on record and row 16 met the first again: a sort returning **nothing at all** (43 `policyExposure` entries across nine dossiers, 24 dated, zero future day-level dates). Your members are **compute operators rather than regulated entities**, so expect the same thinness and be ready for the six-month default stated explicitly with the rejections in writing. The curriculum checker reports **2** items due today (`landscape-utilities-2026-09` and `landscape-in-hall-power-2026-09`, both 2026-10-01, both by design — **do not "fix" either**) and a date inside your own 30-day horizon makes it 3. Choose knowingly.
>
> **DO NOT:** author or revise a lesson — `check-classroom-content.py` must stay at **46 lessons / 8 tracks / 142 gate cases**. Do not touch `Profiler.gs` or `Profiler.html`. Do not run the segment generator with `--all` or on any segment but your own. Do not "fix" the two items the curriculum checker reports as due for review, and do not clear `clean-firm-and-nuclear` from the due set. And **NEVER sweep a date across `Classroom.gs` with a blanket find-and-replace** — it holds twenty independently dated guidance modules and session 9 corrupted a comment-deadline date inside `guidanceDocEo14420_()` that way; scope every replacement to the function you are authoring. One mechanical trap: when appending a function to `Classroom.gs`, the anchor is the previous function's closing `}` on its own line, **NOT** the literal's `};` — splicing after the `};` nests your function inside the previous one, which is valid JavaScript so `node --check` passes while the registry can never resolve the name. Verify by counting unbalanced braces before your definition and getting **0**, as rows 15 and 16 both did.
>
> **VERIFY:** `node --check` on a `.js` copy of `Classroom.gs`; `scripts/check-gas-inner-scripts.js`; `scripts/check-classroom-content.py` — capture a pristine HEAD baseline BEFORE any edit. It is **0 errors / 0 warnings at 46 lessons / 8 tracks / 142 gate cases** and must be 0/0 after, with the module assertion moved to **21**. **Watch the micro-markup check**: it caught a defect in six consecutive sessions, then found nothing at row 16 — nested emphasis in any clFmt-fed field, and `{{term}}` in any textContent field (a `proscons` card's `t` and `meta`, a `glossary[].d`, a tile's `k`/`v`/`sub`, a `bars` item's `label`/`sub`, and a section's own `title`). Then `scripts/check-classroom-curriculum.py --strict` (no structural findings; 28 stale pins and 2 items due for review are pre-existing and not yours); `scripts/check-classroom-pipeline.py --selftest` (13 fixtures, 0 failures) and `--base origin/main`. `build-classroom-segments.py --check` before and after each step. `scripts/check-readme-tree.py` after the version bump. Playwright render of the new module at **contributor** with ZERO page errors, plus an **analyst denial** from the real serving path — the module entrypoint is `clShowGuidanceDoc(id)` and `action=guidance` payloads route to `handleGuidanceOp_` while everything else routes to `handleClassroomOp_`; `_gasPost` returns a PROMISE, `sess.permissions` must be an ARRAY in the shim, the guidance op reads `p.session` (**not** `p.token`) and it must be at least 32 characters or the real guard returns SESSION_EXPIRED, the session keys are `Classroom_gas_session_token` / `_gas_user_email` / `_gas_user_role` / `_gas_user_permissions`, overlays to hide are `#auth-wall` / `#signing-out-wall` / `#auth-timers` / `#gas-layer-toggle` plus `#cl-app{display:block !important}`, `AUTO_REFRESH` is a `var AUTO_REFRESH = true;` declaration (patch the declaration, not a `key: value` pair), and the scratch site root needs `favicon.ico` copied in beside `profiler-data/`, `sounds/` and `images/`. **SERVE THE SCRATCH COPY AT `/Classroom.html`, NOT AT THE SITE ROOT** — the page derives its sessionStorage key prefix from the URL path. The page's CSP `connect-src` is `'self'` plus Google hosts, so a shim on another port is REFUSED: serve the static files and the `/__gas` route **from the same Node process on one port**, which is the simplest same-origin answer and removes the need for Playwright routing entirely. Call the handlers through the vm context object (`ctx.handleGuidanceOp_`), not from Node's own scope. **Then READ EVERY SECTION'S SCREENSHOT** — at row 16 the micro-markup check found nothing and the screenshots still caught three defects nothing else in the toolchain sees, including a timeline rung that rendered with a **blank gutter year** because `clTimeline` takes `Math.floor(x)` and suppresses repeats. Bump Classroom's GAS version in BOTH `Classroom.gs` `VERSION` (from `v01.53g`) and `Classroomgs.version.txt`, with a GENERIC entry in its changelog.
>
> **THE DEPLOY:** the §7.19 order, on its twenty-second use. Read Pages first (`https://lightaisolutions.github.io/Sales/gs-versions/Classroomgs.version.txt` is a static file) and the workflow's own `Classroom deploy confirmed` line from the job log. Probe `?op=deploy` only if those two disagree, and use `curl -sL` if you do. **Deployment counters, MEASURED at v06.14r against the v06.13r job log: Classroom `deployment 65 | 65/200` confirming on the FIRST GET, Scraper unchanged at `157/200` because its step never fired.** Exactly one `.gs` in the merge diff fired exactly one step and logged exactly one line, as forecast; every other `Deploy <Project>` step exited 0 silently on its own `git diff` guard and logged nothing. Classroom's deployment number and its tally are **equal for a second consecutive cycle** (64/64 → 65/65), which holds v06.12r's reconciliation: the v06.10r divergence was a read-time artefact of an unconfirmed leg, not a standing offset. Headroom: Classroom **200 − 65 = 135**, Scraper **200 − 157 = 43**. Pages agreed with the log and **no `?op=deploy` probe was made** — the §7.19 order resolved on its two reads for the **twenty-first** time. One `.gs` in the merge diff means one step and one logged line; **two if you seed the Scraper**, and Scraper is the counter to watch — it passed three quarters of its allowance at v06.11r. The counter rule: `Updated to` consumes one; a step that never fires consumes none and logs nothing; a leg that fires and does not confirm still consumes one and the tally does not see it; `Already up to date` is silent on consumption and must never be forecast as zero.
>
> **THE CHANGELOG ARITHMETIC: THE ROTATION HAS BEEN DEFERRED THREE TIMES BY THE SAME CLAUSE AND THE FOURTH DEFERRAL IS ARITHMETICALLY IMPOSSIBLE.** §7.36, §7.37 and §7.38 each forecast a rotation and each was beaten by the same fact: the push landed **on** 2026-09-16 EST. v06.13r closed `Classroomgs.changelog.md` at **53 raw / 48 non-exempt against a cap of 50** with five sections dated 2026-09-16 EST, and `CHANGELOG.md` at **102 raw / 93 non-exempt** against a 100 trigger with nine dated that day. **No later day can repeat the escape** — the moment EST reads 2026-09-17 or later both jump to their raw counts and both rotate. Oldest whole date groups, **re-counted from the files at v06.13r and correcting §7.38's figure**: `Classroomgs.changelog.md` **2026-09-01 (2 sections)** then **2026-09-02 (11)**; `CHANGELOG.md` **2026-09-08 (10 sections)**. **The GAS file needs BOTH of its first two groups** — 53 − 2 = 51 is still above 50, and 51 − 11 = 40 plus yours = 41 — so do not stop after one group; the rule is repeat until fewer than the trigger remain. The repo file clears in one: 102 − 10 = 92, plus yours = 93. Re-count from the files rather than from this prompt. Deepen the clone first so every rotated header enriches, and run the mandatory post-rotation grep (`grep '^## \[v' <archive> | grep -v '— \[' | head -5`). Keep using `TZ=America/New_York date` and trust it over the harness banner, which follows UTC — at sessions 10, 11 and row 16 that check decided *against* rotating.
>
> Normal Pre-Commit and Pre-Push checklists; `claude/*` branch restarted from `origin/main` first, `git ls-remote` before each push — the auto-merge workflow adds a `[skip ci]` SHA-tracker commit after every merge, and after it merges use `git fetch --prune origin` rather than `git fetch origin main`, or the stale remote-tracking ref makes the stop hook report that SHA-tracker commit as unpushed. Read `repository-information/repository.version.txt` for the version to bump from — do not take it from this prompt, whose own handover commit consumes the next number.
>
> AFTER THIS SESSION Phase 4 is 16 of 26 and S2 is 12 of ~19, so the interleave returns to Phase 4 and the recommended next run is **Phase 4 row 17 — `where-the-chain-breaks`** — for which §7.40 should be written.
>
> Then, give me a prompt to paste into a new Opus 5 xhigh session to continue the action plan, then remember session.

### 7.40 · Paste-in brief for Phase 4 row 17 — `where-the-chain-breaks` (Opus 5 xhigh) — written 2026-09-16 at v06.15r, at the close of S2 session 12

**REFRESHED AT v06.20r** — a revision session ran between this brief and its execution: it revised `landscape-clean-firm-and-nuclear-2026-09` against the newly built `clean-firm-power` lesson and regenerated that segment, so the due set is **8** rather than 9 and Classroom GAS is **v01.55g**. Both halves of this brief were updated. It also established a standing rule you may hit: **a Phase 4 row that builds a lesson an existing landscape declared unbuilt must revise that landscape in the same commit** — check your row's `READ_NEXT` neighbours for a landscape asserting your lesson does not exist.

**Why this and not another S2 segment.** Phase 4 is **16 of 26** and S2 is now **12 of ~19**, so the interleave stands at 16:12 and §7.4 item 6 hands the turn back to Phase 4. Its next row is **17, `where-the-chain-breaks`**, and it is the one row in the twenty-six the developer asked for by name: **the capstone of `aidc-grid-to-chip`**, spec at `CLASSROOM-CURRICULUM-PLAN.md` §3.3 and §5, cut-line entry §7 row 17. It is the **forty-seventh** lesson.

**THE PREREQUISITE CONDITION IS MET, MEASURED RATHER THAN ASSUMED.** §7 row 17 says this row is authored "after rows 3, 4, 7, 8 and 13, so every stage it maps has been taught". All five are built — `backup-generation`, `the-ups-room`, `inside-the-rack`, `redundancy-by-the-numbers`, `breakers-relays-and-faults` — as is `the-800-vdc-shift`. **Two lessons in the track's §4 order are not**: `interconnection-for-large-loads` (the G6 guidance lesson) and `dc-fault-engineering` (§7 row 20, guidance-gated). Neither blocks this row; both change where it lands.

**THIS ROW IS AN APPEND, NOT A MID-ARRAY INSERT — WHICH BREAKS A TWO-ROW RUN AND CHANGES THE EXPECTED SIGNATURE.** `clTrackAidcGridToChip_()`'s `lessons[]` holds **eight** ids today, in exactly §4's order with the two unbuilt guidance lessons absent: `the-fence-line` · `bridge-power` · `the-aidc-power-chain` · `backup-generation` · `the-ups-room` · `redundancy-by-the-numbers` · `inside-the-rack` · `the-800-vdc-shift`. §4 puts this lesson **last of eleven**, so with the two absent it appends at **position 9 of 9**. Rows 15 and 16 were both mid-array inserts and both reported **P1 and P5**, P5 firing because `head[:len(base)] == base` fails by construction on an insert. **An append keeps the prefix intact, so the forecast here is P1 alone** — P1 on the two developer plan files. Verify it rather than assuming it; if P5 fires on an append, that is a finding about the checker, not about the row.

**THE DEEPENING TEST FAILS AT PART ONE, AND THE MEMBERSHIP HALF NETS.** Run §10.6's three-part test programmatically before the write, as rows 14 and 16 did. Measured at v06.15r: `where-the-chain-breaks` appears in **no `CRITERION_LEXICON` entry at all**, so part one fails and **no deepening can fire** — row 16's case does not repeat. It does sit in **exactly one `READ_NEXT`**, `epc-and-construction`, so registering it flips that segment's `lesson_ref` from `(planned)` to built and changes its `read-next`. **But `epc-and-construction` is already in the standing due set, and already differing in `read-next` AND `what-is-bought-and-on-what`** — so the change nets and the count does not move. **Forecast both halves: `--check` 9 → 9 → 9**, with no regeneration (a Phase 4 row regenerates nothing).

**THE §5 MAP IS THE SPEC AND IT IS FOURTEEN ROWS, NOT TWELVE.** §3.3's outline names six sections — `one-chain-twelve-weak-points` (table) · `the-physics-behind-them` (callout) · `who-owns-the-failure` (prose) · `a-fault-walks-the-chain` (timeline) · `drill` · `check-yourself` — and the first section's **id says twelve while §5's table has fourteen rows**. §5 resolves it: rows 1–12 are the power chain the developer asked for, and **rows 13–14 (the thermal chain, and commissioning) are the two failures that most often *present* as power failures and are not**. Decide that deliberately and say which you did: the section **id is permanent** once written, so if the table carries fourteen rows the id still reads `one-chain-twelve-weak-points`. Recommend keeping the id, carrying all fourteen rows, and making the 12-versus-14 split an explicit line in the table's `note` — it is the lesson's sharpest single teaching point.

**FOUR CAUSES RECUR AND §5 NAMES THEM — DO NOT INVENT A FIFTH.** Current squared becomes heat · direct current has no zero crossing · heat flows through a series path · human hands are in the loop. `the-physics-behind-them` is the callout that carries them, and every row of the map should be traceable to one of the four or honestly marked as belonging to none.

**THE DC-ARC TAXONOMY GAP LANDS INSIDE THIS ROW, AND IT IS NOT THIS ROW'S TO FILL.** §5's row 11 is DC distribution at 800 VDC, whose stated failure is *a sustained series arc*, and open item (vi) records that `the-800-vdc-shift` hands the arc taxonomy to `dc-fault-engineering` — **§7 row 20, guidance-gated, unbuilt**. So this row must **state the failure and decline the taxonomy**, exactly as session 10's **(aa1)** posture required when a built public lesson declined physics to a lesson that did not exist. Name the handoff in the row and in the header comment rather than quietly teaching a DC-arc classification the curriculum has reserved.

**STAMPS: PIN ONLY WHAT YOU USED.** §3.3 and §7 row 17 list up to twenty-five study guides across the fourteen rows; §3.3 says in as many words that *an authoring session pins only the guides whose failure content it actually used, and the §5 map names the source per row*. **All of the named guides exist in the corpus** — verified at v06.15r, none missing. Read every date off the fetched dossier (G2) and resolve every ref by matching its own `slug`. All inputs are public, so the stamp folds to **`tracks`** — analyst-visible — and there is no `guidance:`, `study:`-gated, `report:` or module input to raise it.

**`reviewBy` — read, do not sort, for the thirteenth consecutive session.** This is a **lesson**, so the six-month default from `updated` is the ordinary answer and the sort is over the guides' `policyExposure[]`. Session 12 met session 3's **(g)** case for the fourth time. **The arithmetic constraint has tightened**: the curriculum checker now reports **3** items due for review, not 2 — `landscape-utilities-2026-09` and `landscape-in-hall-power-2026-09` at 2026-10-01 and **`landscape-neoclouds-2026-09` at 2026-09-30**, all three by design. **Do not "fix" any of the three**, and note that the neoclouds one **passes on 30 September**, after which the checker reports it as overdue until a session revises that module — which is correct behaviour, not a defect.

**Baselines to capture BEFORE any edit, all measured at the close of v06.15r.** `check-classroom-content.py` **0 errors / 0 warnings at 46 lessons, 8 tracks, 142 gate cases**, module assertion at **21** — twenty consecutive sessions unbroken. Your commit must return it to 0/0 at **47 lessons**, 8 tracks, **142** gate cases (a new lesson adds no fixture shape) and leave the module assertion at **21** (a Phase 4 row registers no module). `check-classroom-curriculum.py --strict`: no structural findings, **28** stale pins, **3** items due for review. `check-classroom-pipeline.py --selftest`: **13 fixtures / 0 failures**. `build-classroom-segments.py --check`: **9 due** — `cells-and-chemistry`, `power-conversion-and-rack-power-silicon`, `compute-and-the-rack`, `epc-and-construction`, `storage-developers-and-ipps`, `capital`, `software-and-optimization`, `insurance-and-risk-transfer`. **`neoclouds` is no longer in the set** and **`clean-firm-and-nuclear` still is, with two differing sections** — not yours to clear.

**Versions and counters to bump from.** Classroom GAS **v01.55g** (`Classroom.gs` `VERSION` **and** `live-site-pages/gs-versions/Classroomgs.version.txt`, together in one commit); page **v01.14w**, no page bump expected. Scraper **v02.13g** and **not yours** — a Phase 4 row adds no Scraper seed. Profiler v01.39g and never yours. **Repo version: read `repository-information/repository.version.txt` on the day and bump by 0.01** — do not take it from this brief, whose own handover commit consumes the next number.

**THE DEPLOY — the §7.19 order, on its twenty-third use, and the first two-project cycle in several.** Read **Pages** first (`https://lightaisolutions.github.io/Sales/gs-versions/Classroomgs.version.txt` is a static file) and the workflow's own `Classroom deploy confirmed` line from the job log; probe `?op=deploy` only if the two disagree, with `curl -sL`. **Counters, MEASURED at v06.16r against the v06.15r job log: Classroom `deployment 66 | 66/200` and Scraper `deployment 158 | 158/200`, both confirming on the FIRST GET.** The v06.15r merge carried **two** `.gs` files — `Classroom.gs` for the module and `Scraper.gs` for the seed — and fired **exactly two** deploy steps logging **exactly two** lines, as forecast; every other `Deploy <Project>` step exited 0 silently on its own `git diff` guard, in zero seconds, and logged nothing. **This is the first two-project cycle since the counters began being recorded**, and it confirms the rule in its positive branch as cleanly as the last three cycles confirmed the never-fires branch. **Both projects' deployment numbers equal their tallies** — 66/66 and 158/158 — which is Classroom's **third** consecutive equal cycle (64/64 → 65/65 → 66/66) and holds v06.12r's reconciliation for good. Your own row touches **one** `.gs`, so forecast one step and one logged line. **Real headroom: Classroom 200 − 66 = 134, Scraper 200 − 158 = 42.** **Scraper remains the counter to watch** — it is past four fifths of its allowance, and a Phase 4 row never moves it, so it will sit at 42 until the next S2 session that seeds. The counter rule is unchanged: `Updated to` consumes one; a step that never fires consumes none and logs nothing; a leg that fires and does not confirm still consumes one and the tally does not see it; `Already up to date` is silent on consumption and must never be forecast as zero.

**CHANGELOGS — THE ROTATION WAS DEFERRED A FOURTH TIME, AND §7.39's "ARITHMETICALLY IMPOSSIBLE" WAS WRONG FOR A REASON WORTH KEEPING.** §7.36, §7.37, §7.38 and now §7.39 each forecast a rotation and each was beaten by the same clause — the push landed **on** 2026-09-16 EST. §7.39 argued the fourth deferral was impossible; it was not, because **four sessions ran on the same calendar day** and the deferral is a function of **the day of the push, not of the section count**. State it that way: **the rotation fires on the first push dated 2026-09-17 EST or later, whenever that is.** v06.15r closed `CHANGELOG.md` at **104 raw / 93 non-exempt** against a 100 trigger (eleven sections dated 2026-09-16 EST, counter `Sections: 104/100`) and `Classroomgs.changelog.md` at **54 raw / 48 non-exempt** against a cap of 50 (six dated that day, counter `Sections: 54/50`). Re-counted from the files at v06.15r: **`CHANGELOG.md` clears in ONE group** — 2026-09-08, **10 sections**, 104 + yours − 10 = 95 — and **`Classroomgs.changelog.md` needs TWO** — 2026-09-01 (**2**) then 2026-09-02 (**11**), because 54 + yours − 2 = 53 is still above 50 while − 11 more is 42. **Do not stop after one group**; repeat until fewer than the trigger remain. `Scrapergs.changelog.md` **34/50**; `Profilerhtml.changelog.md` **49/50**, closest to its own trigger and untouched by a Phase 4 row. **Re-count from the files rather than from this brief.** Deepen the clone first so every rotated header enriches, and run the mandatory post-rotation grep (`grep '^## \[v' <archive> | grep -v '— \[' | head -5`). Keep using `TZ=America/New_York date` and trust it over the harness banner, which follows UTC.

**Eight open items recorded and not taken.** (i) The `.cl-tbl` `min-width` question now has **twenty-five** clean data points — session 12 added two seven-row, three-column tables clean at 1400 px — and still wants a session that edits `Classroom.html`. (ii) The generated `read-next` **names** the landscape module without linking to it (`clFmt` has no link token), so it belongs with (i) plus a developer `--all` run. (iii) **`the-800-vdc-shift`'s `three-chains` intro is now eighteen registrations stale** — and it is **in this row's own track, immediately before this lesson**, so row 17 is the natural place to raise it with the developer even though fixing it is out of scope. (iv) **§10.6's session-4 bullet block still sits out of chronological order** — cosmetic. (v) **A stale `lesson_ref` can make a segment due on its own**, now traced to its cause at three separate segments, and session 12 found a segment carrying **two** stale refs at once — the case for a developer decision keeps strengthening. (vi) **The DC-arc-taxonomy gap** stays with §7 row 20 and, as above, **lands inside this row's map**. (vii) **`restart` scores zero across all 454 Scraper terms** and belongs in `topic-landscape-clean-firm-and-nuclear`'s seed at that module's next revision — unseeded for two consecutive sessions now. (viii) **NEW — a Scraper gap that is blocked in both directions.** Session 12 found that the neocloud segment's largest bear-case variable, GPU residual value and the useful-life assumption, **cannot be seeded at all**: every precise form is a superstring of the existing term `gpu` and every short form mis-bands into battery degradation. It is a fifth drop class and it wants a developer decision about whether the digest scorer should admit a narrowing term over an existing broad one.

> Author Phase 4 row 17 — the `where-the-chain-breaks` lesson — on Opus 5 xhigh as a fresh session. This is ONE mechanism lesson. It authors no guidance module, no dossier and no report, creates no track, regenerates no segment, and adds no Scraper seed.
>
> READ FIRST: repository-information/SESSION-CONTEXT.md (Latest Session); INTEGRATED-REMEDIATION-PLAN.md §7.40 (your brief), §7.38 and §7.39 (for the mechanism-lesson signature, the both-halves due-count rule and what session 12 settled about the deepening test and the empty criterion intersection), §7.3 orders 5 and 6, and §6; CLASSROOM-CURRICULUM-PLAN.md §3.3 IN FULL (the track, its order and this lesson's spec), §5 IN FULL (the failure-point map — this lesson's actual specification, fourteen rows), §4 (teaching order), §7 row 17, §8, and §10.6 for the landscape layer this lesson does NOT touch. Then .claude/rules/classroom-app.md, repository-information/CLASSROOM-SCHEMA.md, and .claude/rules/changelog-security.md before you write any public changelog line.
>
> **UNSHALLOW THE CLONE FIRST** — `git fetch --unshallow origin main`, before any measurement and before any pin. A shallow clone's boundary commit looks like it introduced every file, so `git log -1 --format=%cs -- <path>` returns the boundary's date rather than the file's, and every rotation SHA lookup beyond the horizon fails. **You are very likely the rotator** — see the changelog paragraph below — so this is load-bearing rather than hygienic.
>
> **THE WORK — one push commit.** (1) Author `clLessonWhereTheChainBreaks_()` **inside** the `// CONTENT START` … `// CONTENT END` fence in `Classroom.gs`, id `where-the-chain-breaks`, group The AI Data-Center Wave, gate `tracks`, at §3.3's six section ids in §3.3's order, and register it at the **END** of `clLessons_()`. (2) **APPEND** it to `clTrackAidcGridToChip_()`'s `lessons[]` — this is an append at position 9 of 9, not a mid-array insert; see the signature paragraph. (3) Flip §7.3's Phase 4 row, §6's Phase 4 row, curriculum §7 row 17 to built with the full record, and §4's `aidc-grid-to-chip` table. (4) Write §7.41 — the next run's brief, which by the interleave is **S2 session 13, `capital`** — in the same commit, and **edit BOTH halves of any §7.x brief you refresh**: the prose paragraphs and the quoted prompt block.
>
> **THE §5 MAP IS THE SPEC, AND IT HAS FOURTEEN ROWS WHILE THE SECTION ID SAYS TWELVE.** §5 resolves it in its own closing line: rows 1–12 are the power chain the developer asked for, and **rows 13–14 (the thermal chain, and commissioning) are the two failures that most often PRESENT as power failures and are not.** Section ids are permanent, so decide deliberately and say which you did. The recommendation in §7.40 is: keep the id `one-chain-twelve-weak-points`, carry all fourteen rows, and make the 12-against-14 split an explicit line in the table's `note` — it is the lesson's sharpest single teaching point. **Four causes recur and §5 names them** — current squared becomes heat, direct current has no zero crossing, heat flows through a series path, human hands are in the loop — so every row should trace to one of the four or be honestly marked as belonging to none. Do not invent a fifth.
>
> **THE DC-ARC TAXONOMY LANDS INSIDE YOUR MAP AND IS NOT YOURS TO FILL.** §5 row 11 is 800 VDC distribution and its stated failure is *a sustained series arc*. `the-800-vdc-shift` hands the arc taxonomy to `dc-fault-engineering` — §7 row 20, guidance-gated, **unbuilt**. **State the failure and decline the taxonomy**, and name the handoff in the row and in the function's header comment. This is session 10's (aa1) posture: say the test is unowned rather than quietly owning it.
>
> **THIS IS AN APPEND, WHICH BREAKS A TWO-ROW RUN — FORECAST P1 ALONE.** Rows 15 and 16 were both mid-array inserts and both reported **P1 and P5**, P5 firing because `head[:len(base)] == base` fails by construction on an insert. `clTrackAidcGridToChip_()` holds **eight** ids today — §4's order with the two unbuilt guidance lessons absent — and §4 puts this lesson last, so it **appends**. Verify the signature rather than assuming it; if P5 fires on an append that is a finding about the checker, not about the row.
>
> **PREDICT BOTH HALVES OF THE DUE COUNT.** `--check` should read **8 due** as you start *(9 at v06.15r; v06.20r regenerated `clean-firm-and-nuclear` and cleared it)*: `cells-and-chemistry`, `power-conversion-and-rack-power-silicon`, `compute-and-the-rack`, `epc-and-construction`, `storage-developers-and-ipps`, `capital`, `software-and-optimization`, `insurance-and-risk-transfer`. Run §10.6's three-part deepening test programmatically before the write: measured at v06.15r it **fails at part one** — `where-the-chain-breaks` is in **no `CRITERION_LEXICON` entry at all** — so no deepening can fire. It does sit in **exactly one `READ_NEXT`** (`epc-and-construction`), which is **already due and already differing in `read-next`**, so the membership half nets. **Expect 8 → 8 → 8**, and regenerate nothing. *(v06.20r cleared `clean-firm-and-nuclear` when it regenerated that segment alongside the module revision, so it is no longer in the set and no longer yours to leave alone.)*
>
> **SESSION 12 FOUND A THIRD MECHANISM FOR AN EMPTY CRITERION INTERSECTION AND IT MATTERS TO ANY CLAIM YOU MAKE ABOUT DASH ROWS.** Sessions 8, 10 and 11 met a dash row as a real curriculum gap or as mapped lessons absent from `CRITERION_LEXICON`. At `neoclouds` **every criterion hit a lexicon key and none of the matching lessons was in that segment's `READ_NEXT`** — the two sets were disjoint, so the dash was largely a keyword artefact. If you state anything about what the curriculum teaches, name which of the three mechanisms you measured.
>
> **`reviewBy`** — read, do not sort, for the thirteenth consecutive session. This is a lesson, so the six-month default from `updated` is the ordinary answer and the sort is over the study guides' `policyExposure[]`. **The curriculum checker now reports 3 items due for review, not 2** — `landscape-utilities-2026-09` and `landscape-in-hall-power-2026-09` at 2026-10-01 and `landscape-neoclouds-2026-09` at **2026-09-30**, all three by design. **Do not "fix" any of the three.** The neoclouds one passes on 30 September and the checker will then report it overdue until a session revises that module — correct behaviour, not a defect. A `reviewBy` of your own inside the 30-day horizon would make it 4. Choose knowingly.
>
> **ONE RULE ADDED AT v06.20r THAT MAY BIND YOU:** a Phase 4 row that builds a lesson an existing landscape declared unbuilt **must revise that landscape in the same commit**. A generated `segment-*` lesson catches this mechanically (`lesson_ref` stops rendering `(planned)` and `--check` reports the segment due); a hand-authored guidance module has **no detector at all** and its `reviewBy` is months out. Before you write, grep the registered modules for your lesson's id and title and for a claim that it is planned or unbuilt — row 16 did not, and the module it invalidated went three and a half months from being surfaced. Revising it is **not** authoring a module, so the assertion stays at 21.
>
> **DO NOT:** author or revise a guidance module — `check-classroom-content.py`'s module assertion must stay at **21**. Do not touch `Profiler.gs`, `Profiler.html` or `Scraper.gs`. Do not run the segment generator at all. Do not clear `clean-firm-and-nuclear` from the due set. And **NEVER sweep a date across `Classroom.gs` with a blanket find-and-replace** — it holds twenty-one independently dated guidance modules and session 9 corrupted a comment-deadline date inside `guidanceDocEo14420_()` that way; scope every replacement to the function you are authoring. One mechanical trap: when appending a function to `Classroom.gs`, the anchor is the previous function's closing `}` on its own line, **NOT** the literal's `};` — splicing after the `};` nests your function inside the previous one, which is valid JavaScript so `node --check` passes while the registry can never resolve the name. Verify by counting unbalanced braces before your definition and getting **0**, as rows 15, 16 and session 12 all did.
>
> **VERIFY:** `node --check` on a `.js` copy of `Classroom.gs`; `scripts/check-gas-inner-scripts.js`; `scripts/check-classroom-content.py` — capture a pristine HEAD baseline BEFORE any edit. It is **0 errors / 0 warnings at 46 lessons / 8 tracks / 142 gate cases** with the module assertion at **21**, and must be 0/0 at **47 lessons / 8 tracks / 142 gate cases** after, assertion still 21. **Watch the micro-markup check**: nested emphasis in any clFmt-fed field, and `{{term}}` in any textContent field (a `proscons` card's `t` and `meta`, a `glossary[].d`, a tile's `k`/`v`/`sub`, a `bars` item's `label`/`sub`, and a section's own `title`). It found nothing at row 16 or at session 12. Then `scripts/check-classroom-curriculum.py --strict` (no structural findings; 28 stale pins and 3 items due for review are pre-existing and not yours); `scripts/check-classroom-pipeline.py --selftest` (13 fixtures, 0 failures) and `--base origin/main`. `build-classroom-segments.py --check` before and after each step. `scripts/check-readme-tree.py` after the version bump. Playwright render of the new lesson at **analyst** with ZERO page errors, plus a **viewer denial** from the real serving path — the lesson entrypoint is `clShowLesson(id)` and non-guidance payloads route to `handleClassroomOp_`; `_gasPost` returns a PROMISE, `sess.permissions` must be an ARRAY in the shim, the session token must be at least 32 characters or the real guard returns SESSION_EXPIRED, the session keys are `Classroom_gas_session_token` / `_gas_user_email` / `_gas_user_role` / `_gas_user_permissions`, overlays to hide are `#auth-wall` / `#signing-out-wall` / `#auth-timers` / `#gas-layer-toggle` plus `#cl-app{display:block !important}`, `AUTO_REFRESH` is a `var AUTO_REFRESH = true;` declaration (patch the declaration, not a `key: value` pair), and the scratch site root needs `favicon.ico` copied in beside `profiler-data/`, `sounds/` and `images/`. **SERVE THE SCRATCH COPY AT `/Classroom.html`, NOT AT THE SITE ROOT** — the page derives its sessionStorage key prefix from the URL path. The page's CSP `connect-src` is `'self'` plus Google hosts, so a shim on another port is REFUSED: serve the static files and the `/__gas` route **from the same Node process on one port**. Call the handlers through the vm context object (`ctx.handleClassroomOp_`), not from Node's own scope. **Then READ EVERY SECTION'S SCREENSHOT** — at row 16 the micro-markup check found nothing and the screenshots caught three defects, and at session 12 they caught **four**, including **literal backticks rendered visibly in two quiz questions** (`clFmt` resolves `**bold**`, `*italic*` and `{{term}}` and nothing else) and two arithmetic slips in the module's own prose that every checker passed. Watch `clTimeline` in particular: it takes `Math.floor(x)` and suppresses repeats, so two rungs inside the same integer year render one blank gutter — and **this lesson has a millisecond-scale timeline**, where that failure mode is most likely. Bump Classroom's GAS version in BOTH `Classroom.gs` `VERSION` (from `v01.55g`) and `Classroomgs.version.txt`, with a GENERIC entry in its changelog.
>
> **THE DEPLOY:** the §7.19 order, on its twenty-third use. Read Pages first (`https://lightaisolutions.github.io/Sales/gs-versions/Classroomgs.version.txt` is a static file) and the workflow's own `Classroom deploy confirmed` line from the job log. Probe `?op=deploy` only if those two disagree, and use `curl -sL` if you do. **Deployment counters, MEASURED at v06.16r against the v06.15r job log: Classroom `deployment 66 | 66/200` and Scraper `deployment 158 | 158/200`, both confirming on the FIRST GET.** That merge carried **two** `.gs` files and fired **exactly two** steps logging **exactly two** lines — the first two-project cycle on record, and the rule's positive branch confirmed as cleanly as the last three cycles confirmed its never-fires branch; every other `Deploy <Project>` step exited 0 silently in zero seconds and logged nothing. Both numbers equal their tallies (66/66 and 158/158), Classroom's third consecutive equal cycle. Headroom: Classroom **134**, Scraper **42**. **Your own row touches one `.gs`, so forecast one step and one logged line**, taking Classroom to 67/200 and leaving Scraper untouched at 158/200. Scraper is the counter to watch — past four fifths of its allowance, and only an S2 seed moves it. The counter rule: `Updated to` consumes one; a step that never fires consumes none and logs nothing; a leg that fires and does not confirm still consumes one and the tally does not see it; `Already up to date` is silent on consumption and must never be forecast as zero.
>
> **THE CHANGELOG ARITHMETIC: DEFERRED FOUR TIMES BY THE SAME CLAUSE, AND §7.39's "IMPOSSIBLE" WAS WRONG FOR A REASON WORTH KEEPING.** §7.36 through §7.39 each forecast a rotation and each was beaten by the same fact: the push landed **on** 2026-09-16 EST. §7.39 argued a fourth deferral was arithmetically impossible; it was not, because **four sessions ran on the same calendar day**. The deferral is a function of **the day of the push, not of the section count** — it lapses on the first push dated **2026-09-17 EST or later**, whenever that is. v06.15r closed `CHANGELOG.md` at **104 raw / 93 non-exempt** against a 100 trigger (counter `Sections: 104/100`) and `Classroomgs.changelog.md` at **54 raw / 48 non-exempt** against a cap of 50 (counter `Sections: 54/50`). Re-counted from the files: **`CHANGELOG.md` clears in ONE group** (2026-09-08, 10 sections) and **`Classroomgs.changelog.md` needs TWO** (2026-09-01's 2, then 2026-09-02's 11) — do not stop after one group; repeat until fewer than the trigger remain. `Scrapergs.changelog.md` 34/50; `Profilerhtml.changelog.md` **49/50**, untouched by a Phase 4 row. **RE-COUNT FROM THE FILES rather than from this prompt.** Deepen the clone first so every rotated header enriches, and run the mandatory post-rotation grep (`grep '^## \[v' <archive> | grep -v '— \[' | head -5`). Keep using `TZ=America/New_York date` and trust it over the harness banner, which follows UTC.
>
> Normal Pre-Commit and Pre-Push checklists; `claude/*` branch restarted from `origin/main` first, `git ls-remote` before each push — the auto-merge workflow adds a `[skip ci]` SHA-tracker commit after every merge, and after it merges use `git fetch --prune origin` rather than `git fetch origin main`, or the stale remote-tracking ref makes the stop hook report that SHA-tracker commit as unpushed. End every commit message with the two attribution lines this repo's recent commits carry (`Co-Authored-By:` and `Claude-Session:`). Read `repository-information/repository.version.txt` for the version to bump from — do not take it from this prompt, whose own handover commit consumes the next number.
>
> AFTER THIS SESSION Phase 4 is 17 of 26 and S2 is 12 of ~19, so the interleave returns to S2 and the recommended next run is **S2 session 13 — `capital`** — for which §7.41 should be written.
>
> Then, give me a prompt to paste into a new Opus 5 xhigh session to continue the action plan, then remember session.

### 7.41 · Paste-in brief for S2 session 13 — `capital` (Opus 5 xhigh) — written 2026-09-16 at v06.22r, at the close of Phase 4 row 17

**Why this and not another Phase 4 row.** Phase 4 is now **17 of 26** and S2 is **12 of ~19**, so the interleave stands at 17:12 and §7.4 item 6 hands the turn back to S2. Its next segment in the §7.3 sales-value order is **`capital`** — segment **position 16 of 19**, **eight members: four incumbents, one challenger and three adjacents**. Spec: `CLASSROOM-CURRICULUM-PLAN.md` §10.6, the section contract and the order; the module is the **thirteenth** landscape and the **twenty-second** guidance module, so `check-classroom-content.py`'s module assertion moves **21 → 22** with its session line.

**UNSHALLOW FIRST — a Session Start Checklist item, not a brief's favour.** `git fetch --unshallow origin main` before any pin read, any `--check` run, any `git log -1 --format=%cs` date and any CHANGELOG rotation. Row 17 ran it and took the clone to **1,303 commits**; `--check` then opened at the true 8 with `inputs moved: none` on every line. Also: a container restart can leave the clone behind `origin/main` — `git fetch --prune origin` and `git checkout -B <branch> origin/main` **before any measurement**, because a session that measures first reads every counter wrong.

**THE ROSTER SHAPE IS NEW AND IT INVERTS SESSION 12's.** `neoclouds` was **1 · 6 · 0** — one incumbent, six challengers, no adjacents — the most lopsided roster S2 had met, and session 12's finding was that a one-incumbent segment can be a *legibility* effect rather than a market structure. `capital` is **4 · 1 · 3**: an incumbent plurality, a **single** challenger, and three adjacents that are operating companies with capital-markets franchises rather than investors. That is the first roster where `who-dominates-and-on-what-basis` has to sort **four** bases against each other and `each-players-bet` has exactly one attacking row. **Re-derive it from `profiler-segments.json` on the day rather than copying this paragraph** — §7.34, §7.35, §7.37 and §7.38 each carried a measurement that did not survive inspection, and §7.40's own "you are very likely the rotator" was wrong for a reason it had itself written down.

**THE FOUR INCUMBENTS DO NOT SHARE A BASIS, AND THE REGISTRY SAYS SO IN ITS OWN WORDS.** Their `basis` lines describe four different instruments: the largest **landlord-by-proxy and one of the largest lenders**; the most **operationally integrated** investor, holding generation and data-centre platforms together; the exemplar of the **finite-life infrastructure fund**, taking a platform from 85 MW to a roughly USD 40 bn exit and more often a lender than an owner; and **sovereign capital** behind the largest build-outs. **A segment whose incumbents compete on four different instruments is not one market**, and the module's job is to say which of the four a reader is actually in front of. Test that against the graph before asserting it: count typed edges among the eight and see whether any pair is typed `competitor` at all.

**THE THREE ADJACENTS ARE THE SEGMENT'S MOST INTERESTING ROW AND THE EASIEST TO MIS-READ.** All three are primarily something else — a landlord with a USD 3.25 bn hyperscale fund and fee income, a digital-asset financial firm with an infrastructure-solutions arm, and a landlord whose capital-markets franchise ran **the industry's first data-centre securitization**. So the adjacency runs the opposite way from most segments: these are **operators who learned to sell capital**, not investors who bought operations. **Role inversion is measurable here** — at least two of the three sit as `incumbent` or `challenger` in other segments, so §7.37's role-inversion rate is computable and will not be the disjoint-set case session 12 met. Compute it; do not assume the direction.

**THE CRITERION INTERSECTION IS A FOUR-WAY MIX AND MAY BE A FOURTH MECHANISM.** Sessions 8, 10 and 11 met a dash row as a real curriculum gap or as mapped lessons absent from `CRITERION_LEXICON`; session 12 met a third mechanism — every criterion hitting a lexicon key while **none** of the matching lessons sat in that segment's `READ_NEXT`, so the two sets were disjoint and the dash was a keyword artefact. Measured at v06.22r, `capital`'s five criteria produce **all of it at once**: criterion 1 (contracted cash flow) hits `contracts-and-revenue`, which **is** in `READ_NEXT` **and is built** — a genuine intersection; criterion 2 (the independent engineer's report) hits `what-bankable-means`, which **is** in `READ_NEXT` and is **UNBUILT** (§7 row 24), plus a false positive where the key `engine` matches `engineer` and maps to `bridge-power`; criteria 3 and 4 (securitizability; platform scale and the exit route) hit **no lexicon key at all**; criterion 5 (regulatory exposure of owning both supply and demand) hits `how-a-utility-buys`, which is built but is **not** in this segment's `READ_NEXT`. **A criterion whose mapped lesson is in `READ_NEXT` and unbuilt is arguably a fourth mechanism** and the module must name which one it measured for each row rather than making one claim about the dash.

**`READ_NEXT` IS TWO-THIRDS UNBUILT, AND (bb1) POINTS THE OTHER WAY HERE.** The segment's three `READ_NEXT` lessons are `what-bankable-means` (§7 row 24, guidance-gated, **unbuilt**, and explicitly **not to be authored before the bankability module's quarterly review lands** — §8 item 3), `contracts-and-revenue` (**built**, v05.88r) and `reading-the-numbers` (§7 row 25, **unbuilt**). So this module *may* honestly say two of its three follow-on lessons do not exist yet — but **verify it on the day**, because (bb1) exists precisely because a module asserted a gap that a row had closed hours earlier. State the check in the module's own honesty section, and remember that the generated `segment-capital` lesson already renders both unbuilt titles as `(planned)`, which is where the claim has to stay consistent.

**`reviewBy` — read, do not sort, for the fourteenth consecutive session.** This is a **module**, so the sort is over the members' `policyExposure[]` and the result is the module's own nearest dated gate. Session 12 met session 3's **(g)** case for the fourth time — 14 policy entries across seven members, three carrying none, eleven dated and not one future at any granularity. Expect the same shape here for a segment of financial sponsors, and record the rejections in writing. **The arithmetic constraint is tighter than it was**: the curriculum checker reports **3 items due for review** — `landscape-utilities-2026-09` and `landscape-in-hall-power-2026-09` at 2026-10-01 and `landscape-neoclouds-2026-09` at **2026-09-30**, all three by design. **Do not "fix" any of the three.** The neoclouds one **passes on 30 September** and the checker will then report it overdue until a session revises that module — correct behaviour, not a defect. A `reviewBy` of your own inside the 30-day horizon would make it 4. Choose knowingly.

**Baselines to capture BEFORE any edit, all measured at the close of v06.22r.** `check-classroom-content.py` **0 errors / 0 warnings at 47 lessons, 8 tracks, 142 gate cases**, module assertion **21** — twenty-one consecutive sessions unbroken. Your commit takes the lesson count to **48** (the generated segment lesson is not re-authored, so the count moves only if you regenerate — see below) and the **module assertion to 22**, and must return 0/0. `check-classroom-curriculum.py --strict`: no structural findings, **28** stale pins, **3** items due for review. `check-classroom-pipeline.py --selftest`: **13 fixtures / 0 failures**. `build-classroom-segments.py --check`: **8 due** — `cells-and-chemistry`, `power-conversion-and-rack-power-silicon`, `compute-and-the-rack`, `epc-and-construction`, `storage-developers-and-ipps`, **`capital`**, `software-and-optimization`, `insurance-and-risk-transfer`. **`capital` is already in the due set** with `read-next, what-is-bought-and-on-what` differing, so **your own segment is one you clear rather than one you enter** — the opposite of sessions 10 and 12, and the due count should fall rather than hold.

**PREDICT BOTH HALVES OF THE DUE COUNT AND SAY SO BEFORE YOU RUN THE GENERATOR.** An S2 session regenerates **exactly one** segment (`--segment capital --today <date>`). Registering the module changes that segment's `module_ref`, and the segment is already differing in `read-next` and `what-is-bought-and-on-what` for reasons that predate you. So forecast three numbers, not two: **8 at the start, 8 after the write and before the regeneration, 7 after it** — and if the third number is not 7, that is a finding about the generator or about a second segment your module touched, and it goes in the record. Run §10.6's three-part deepening test **programmatically** against the parsed `CRITERION_LEXICON` and `READ_NEXT` blocks, as row 17 did; do not take a lexicon claim from a brief.

**Versions and counters to bump from.** Classroom GAS **v01.56g** (`Classroom.gs` `VERSION` **and** `live-site-pages/gs-versions/Classroomgs.version.txt`, together in one commit); page **v01.14w**, no page bump expected. Scraper **v02.13g** — **and this one IS yours**: an S2 session seeds `topic-landscape-capital`, which is the only thing that moves Scraper's deployment counter. Profiler v01.39g and never yours. **Repo version: read `repository-information/repository.version.txt` on the day and bump by 0.01** — do not take it from this brief, whose own handover commit consumes the next number.

**THE SCRAPER SEED IS YOURS AND IT IS THE HARDEST ONE SO FAR.** The seed convention is `topic-landscape-<segment id>`; six exist today (`bridge-and-on-site-generation`, `clean-firm-and-nuclear`, `grid-equipment`, `hyperscalers-and-ai-labs`, `neoclouds`, and the base entry). Session 12 recorded **five drop classes**, two of them new: near-duplicates an exact-match score cannot see (`time to power` against `speed to power`), and **blocked in both directions** — a concept whose every precise form is a superstring of an existing broad term and whose every short form mis-bands elsewhere. A capital segment's vocabulary is mostly finance words that will collide with the broadest terms already in the roster, so **run the zero-score check on every candidate and expect a low yield**. Two debts are recorded and unpaid: **`restart` scores zero across all 454 terms** and belongs in `topic-landscape-clean-firm-and-nuclear` at that module's next revision — **three sessions unseeded now** — and the GPU-residual-value gap still wants a developer decision about whether the digest scorer should admit a narrowing term over an existing broad one. Read `.claude/rules/scraper-sources.md` before adding any outlet.

**THE DEPLOY — the §7.19 order, on its twenty-fourth use.** Read **Pages** first (`https://lightaisolutions.github.io/Sales/gs-versions/Classroomgs.version.txt` is a static file) and the workflow's own `Classroom deploy confirmed` line from the job log; probe `?op=deploy` only if the two disagree, with `curl -sL`. **Counters MEASURED at v06.23r against the v06.22r job log: Classroom `Updated to v01.56g (deployment 68) | 68/200`, and Scraper unchanged at 158/200** because its step exited silently on its own `git diff` guard and logged nothing. **Note the correction this measurement forced**: §7.40 forecast 67, because its counters were read at v06.16r (66/66) and it did not account for **v06.20r's own Classroom `.gs` change** — the module revision — which consumed deployment 67. **A counter forecast has to add every intervening `.gs` push, not just the forecasting session's own.** Classroom is now at its **fourth consecutive equal cycle** (66/66 → 67 → 68/68) and has **132** of its allowance left. Your own session touches **two** `.gs` files (Classroom for the module, Scraper for the seed), so forecast **two** steps and **two** logged lines, taking Classroom to 68 and Scraper to 159. **Scraper is the counter to watch** — past four fifths of its allowance, with about 41 left, and an S2 seed is the only thing that spends it. The counter rule is unchanged: `Updated to` consumes one; a step that never fires consumes none and logs nothing; a leg that fires and does not confirm still consumes one and the tally does not see it; `Already up to date` is silent on consumption and must never be forecast as zero.

**CHANGELOGS — THE ROTATION WAS DEFERRED A NINTH TIME, AND THE RULE IS THE ONE §7.40 WROTE DOWN AND THEN CONTRADICTED.** §7.36 through §7.40 each forecast a rotation and each was beaten by the same clause: the push landed **on** 2026-09-16 EST. §7.40 went further and called row 17 "very likely the rotator" — and row 17 was not, for exactly the reason §7.40's own changelog paragraph stated: **the deferral is a function of the day of the push, not of the section count.** Nine sessions have now run on one calendar day. It lapses on the first push dated **2026-09-17 EST or later**, whenever that is. v06.22r closed `CHANGELOG.md` at **111 raw / 93 non-exempt** against a 100 trigger (eighteen sections dated 2026-09-16 EST, counter `Sections: 111/100`) and `Classroomgs.changelog.md` at **56 raw / 48 non-exempt** against a cap of 50 (eight dated that day, counter `Sections: 56/50`). Re-counted from the files at v06.22r: **`CHANGELOG.md` clears in ONE group** — 2026-09-08, **10 sections** — and **`Classroomgs.changelog.md` needs TWO** — 2026-09-01 (**2**) then 2026-09-02 (**11**), because one group leaves it above the cap. **Do not stop after one group**; repeat until fewer than the trigger remain. `Scrapergs.changelog.md` **34/50** and **is yours this session**; `Profilergs.changelog.md` 39/50; `Profilerhtml.changelog.md` **49/50**, closest to its own trigger and untouched by an S2 session. **RE-COUNT FROM THE FILES rather than from this brief.** Deepen the clone first so every rotated header enriches, and run the mandatory post-rotation grep (`grep '^## \[v' <archive> | grep -v '— \[' | head -5`). Keep using `TZ=America/New_York date` and trust it over the harness banner, which follows UTC.

**Eight open items recorded and not taken.** (i) The `.cl-tbl` `min-width` question now has **twenty-six** data points — row 17 added a **five-column, fourteen-row** table clean at 1400 px, the widest the corpus has rendered — and still wants a session that edits `Classroom.html`. (ii) The generated `read-next` **names** the landscape module without linking to it (`clFmt` has no link token), so it belongs with (i) plus a developer `--all` run. (iii) **`the-800-vdc-shift`'s `three-chains` intro is now nineteen registrations stale** — and it sits in row 17's track, immediately before the lesson row 17 just built, so the two are now adjacent on the analyst walk. (iv) §10.6's session-4 bullet block is still out of chronological order — cosmetic. (v) **A stale `lesson_ref` can make a segment due on its own**; row 17 is the **fifth consecutive Phase 4 row** to leave one behind, and the case for a developer decision keeps strengthening. (vi) **The DC-arc taxonomy gap stays with §7 row 20** — row 17 stated the failure, declined the classification and named the handoff in three places, so the debt is now explicit in shipped content rather than only in a plan. (vii) **`restart` scores zero across all 454 Scraper terms** and is unseeded for a third session. (viii) The **GPU-residual-value gap blocked in both directions** still wants a developer decision on the scorer.

> Author S2 session 13 — the `capital` landscape module — on Opus 5 xhigh as a fresh session. This is ONE guidance module plus ONE segment regeneration plus ONE Scraper seed. It authors no lesson, no dossier and no report, and creates no track.
>
> READ FIRST: repository-information/SESSION-CONTEXT.md (Latest Session); INTEGRATED-REMEDIATION-PLAN.md §7.41 (your brief), §7.39 and §7.40 (for the S2 module signature, the both-halves due-count rule, what session 12 settled about the empty criterion intersection, and what row 17 settled about declining a reserved taxonomy), §7.3 orders 5 and 6, and §6; CLASSROOM-CURRICULUM-PLAN.md §10.6 IN FULL (the landscape spec, its nine section ids and their order, the claims ledger, and the (bb1)–(cc6) findings blocks), §10.2 and §10.3 (the floor rule and the segment-lesson template), §10.4 (the generator), and §7 rows 24 and 25 for the two UNBUILT lessons this segment's `READ_NEXT` points at. Then .claude/rules/industry-guidance.md, .claude/rules/classroom-app.md, repository-information/CLASSROOM-SCHEMA.md, .claude/rules/scraper-sources.md, and .claude/rules/changelog-security.md before you write any public changelog line.
>
> **UNSHALLOW THE CLONE FIRST** — `git fetch --unshallow origin main`, before any measurement and before any pin. A shallow clone's boundary commit looks like it introduced every file, so `git log -1 --format=%cs -- <path>` returns the boundary's date rather than the file's, and every rotation SHA lookup beyond the horizon fails. **If a container restart has left your clone behind `origin/main`**, run `git fetch --prune origin` and `git checkout -B <branch> origin/main` BEFORE any measurement; a session that measures first reads every counter wrong.
>
> **THE WORK — one push commit.** (1) Write `repository-information/industry-guidance/landscape-capital-analysis.md` FIRST, with its split section before any other section per §10.6 (j). (2) Author `guidanceDocLandscapeCapital_()` **below** the `// CONTENT END` fence in `Classroom.gs`, id `landscape-capital-2026-09`, lane The Value Chain, tier **contributor**, at §10.6's nine section ids in §10.6's order, and register it at the **END** of `guidanceDocs_()`'s Value Chain lane. (3) Regenerate **exactly one** segment: `python3 scripts/build-classroom-segments.py --segment capital --today <date>`. (4) Seed `topic-landscape-capital` in `Scraper.gs` and bump its GAS version. (5) Flip §7.3's S2 row, §6's S2 row and §10.6's progress note to **13 of ~19**, and write the **(dd1)–(ddN)** findings block. (6) Write §7.42 — the next run's brief, which by the interleave is **Phase 4 row 18, `the-cooling-plant-and-water`** — in the same commit, and **edit BOTH halves of any §7.x brief you refresh**: the prose paragraphs and the quoted prompt block.
>
> **THE ROSTER IS 4 · 1 · 3 AND IT INVERTS SESSION 12's 1 · 6 · 0.** Four incumbents, one challenger, three adjacents — the first roster where `who-dominates-and-on-what-basis` must sort **four** bases against each other and `each-players-bet` has exactly one attacking row. **The four incumbents do not share a basis**: the registry's own `basis` lines describe a landlord-by-proxy that is also a major lender, an operationally integrated owner of generation and data-centre platforms, the exemplar finite-life infrastructure fund that is more often a lender than an owner, and sovereign capital. **A segment whose incumbents compete on four different instruments may not be one market**, and saying which one a reader is in front of is the module's job. Test it against the graph: count typed edges among the eight and see whether any pair is typed `competitor` at all before asserting rivalry.
>
> **THE THREE ADJACENTS RUN THE OPPOSITE WAY FROM MOST SEGMENTS.** All three are primarily something else — a landlord with a multi-billion hyperscale fund and fee income, a digital-asset financial firm with an infrastructure arm, and a landlord whose capital-markets franchise ran the industry's first data-centre securitization. These are **operators who learned to sell capital**, not investors who bought operations. **Compute §7.37's role-inversion rate rather than assuming its direction** — at least two of the three hold non-adjacent roles in other segments, so unlike session 12 this will not be the disjoint-set case.
>
> **THE CRITERION INTERSECTION IS A FOUR-WAY MIX AND MAY BE A FOURTH MECHANISM.** Measured at v06.22r, the five criteria produce all three known mechanisms at once plus a new one: criterion 1 hits a lesson that **is in `READ_NEXT` and is built**; criterion 2 hits a lesson that **is in `READ_NEXT` and is UNBUILT**, plus a false positive where the lexicon key `engine` matches the word *engineer*; criteria 3 and 4 hit **no lexicon key at all**; criterion 5 hits a **built lesson that is not in this segment's `READ_NEXT`**. **Name which mechanism produced each row** rather than making one claim about the dash, and say explicitly whether "mapped but unbuilt" is a fourth mechanism or a variant of the first. Grep the file; do not take the lexicon from this prompt.
>
> **TWO OF THE THREE `READ_NEXT` LESSONS ARE UNBUILT, AND (bb1) POINTS THE OTHER WAY HERE.** `what-bankable-means` (§7 row 24, guidance-gated, and **not to be authored before the bankability module's quarterly review lands** — §8 item 3) and `reading-the-numbers` (§7 row 25) are both unbuilt; `contracts-and-revenue` is built. So this module **may** honestly say two of its three follow-on lessons do not exist — but **verify it on the day**, because (bb1) exists precisely because a module asserted a gap a Phase 4 row had closed hours earlier. Keep the claim consistent with the generated `segment-capital` lesson, which renders both unbuilt titles as `(planned)`.
>
> **PREDICT THREE NUMBERS, NOT TWO.** `--check` should read **8 due** as you start, with **`capital` already among them** (`read-next, what-is-bought-and-on-what`). Forecast **8 at the start, 8 after the write and before the regeneration, and 7 after it** — your own segment is one you **clear**, not one you enter, which is the opposite of sessions 10 and 12. If the third number is not 7, that is a finding about the generator or about a second segment your module touched, and it belongs in the record. Run §10.6's three-part deepening test **programmatically** against the parsed `CRITERION_LEXICON` and `READ_NEXT` blocks.
>
> **`reviewBy`** — read, do not sort, for the fourteenth consecutive session. This is a module, so the sort is over the members' `policyExposure[]`. Expect session 3's **(g)** case again for a segment of financial sponsors, and record every rejection in writing. **The curriculum checker reports 3 items due for review** — `landscape-utilities-2026-09` and `landscape-in-hall-power-2026-09` at 2026-10-01 and `landscape-neoclouds-2026-09` at **2026-09-30**, all three by design. **Do not "fix" any of the three.** The neoclouds one passes on 30 September and the checker will then report it overdue until a session revises that module — correct behaviour, not a defect. A `reviewBy` of your own inside the 30-day horizon would make it 4. Choose knowingly.
>
> **THE SCRAPER SEED IS YOURS AND IT IS THE HARDEST SO FAR.** A capital segment's vocabulary is finance words that will collide with the broadest terms already in the roster, so **run the zero-score check on every candidate and expect a low yield**. Session 12 recorded five drop classes, two of them new — near-duplicates an exact-match score cannot see, and **blocked in both directions**. Two debts are unpaid: **`restart` scores zero across all 454 terms** and belongs in `topic-landscape-clean-firm-and-nuclear` at that module's next revision (**three sessions unseeded**), and the GPU-residual-value gap wants a developer decision on the scorer. Read `.claude/rules/scraper-sources.md` before adding any outlet, and **never add one without a live feed probe**.
>
> **DO NOT:** author or revise a lesson by hand — the lesson count stays where the generator leaves it, and the module assertion moves **21 → 22** with its session line. Do not touch `Profiler.gs` or `Profiler.html`. Do not regenerate more than the one segment. And **NEVER sweep a date across `Classroom.gs` with a blanket find-and-replace** — it holds twenty-one independently dated guidance modules and session 9 corrupted a comment-deadline date inside `guidanceDocEo14420_()` that way; scope every replacement to the function you are authoring. One mechanical trap: when appending a function to `Classroom.gs`, the anchor is the previous function's closing `}` on its own line, **NOT** the literal's `};` — splicing after the `};` nests your function inside the previous one, which is valid JavaScript so `node --check` passes while the registry can never resolve the name. **Verify by counting unbalanced braces before your definition and getting 0**, with a tokenizer that skips strings and comments, as row 17 did.
>
> **VERIFY:** `node --check` on a `.js` copy of `Classroom.gs`; `scripts/check-gas-inner-scripts.js`; `scripts/check-classroom-content.py` — capture a pristine HEAD baseline BEFORE any edit. It is **0 errors / 0 warnings at 47 lessons / 8 tracks / 142 gate cases** with the module assertion at **21**, and must be 0/0 after with the assertion at **22**. **Watch the micro-markup check**: nested emphasis in any clFmt-fed field, and `{{term}}` in any textContent field (a `proscons` card's `t` and `meta`, a `glossary[].d`, a tile's `k`/`v`/`sub`, a `bars` item's `label`/`sub`, a `timeline`'s `lanes` values, and a section's own `title`/`read`). Then `scripts/check-classroom-curriculum.py --strict` (no structural findings; 28 stale pins and 3 items due for review are pre-existing and not yours); `scripts/check-classroom-pipeline.py --selftest` (13 fixtures, 0 failures) and `--base origin/main`; `build-classroom-segments.py --check` before and after each step; `scripts/check-readme-tree.py` after the version bump. Playwright render of the new module at **contributor** with ZERO page errors, plus an **analyst denial** from the real serving path — the module entrypoint is `clShowGuidanceDoc(id)` and `payload.action === 'guidance'` routes to `handleGuidanceOp_`; `_gasPost` returns a PROMISE, `sess.permissions` must be an ARRAY in the shim, the session token must be at least 32 characters or the real guard returns SESSION_EXPIRED, the session keys are `Classroom_gas_session_token` / `_gas_user_email` / `_gas_user_role` / `_gas_user_permissions`, overlays to hide are `#auth-wall` / `#signing-out-wall` / `#auth-timers` / `#gas-layer-toggle` plus `#cl-app{display:block !important}`, `AUTO_REFRESH` is a `var AUTO_REFRESH = true;` declaration, and the scratch site root needs `favicon.ico` copied in beside `profiler-data/`, `sounds/` and `images/`. **SERVE THE SCRATCH COPY AT `/Classroom.html`, NOT AT THE SITE ROOT.** The page's CSP `connect-src` is `'self'` plus Google hosts, so a shim on another port is REFUSED: serve the static files and the `/__gas` route **from the same Node process on one port**. Call the handlers through the vm context object (`ctx.handleGuidanceOp_`), not from Node's own scope. **Two shell traps worth keeping**: `pgrep -f "[s]him[.]js"` still matches the invoking shell's own command line because the pattern's literal text is in it — match on `node .*/shim` with `ps -eo pid,args` instead; and a heredoc with an **unquoted** delimiter lets bash interpolate backticks inside Python, so write scripts to a file with a **quoted** delimiter. **Then READ EVERY SECTION'S SCREENSHOT** — at row 17 every checker passed and a screenshot caught a false cross-reference in a timeline rung, and at session 12 they caught four defects including literal backticks rendered in two quiz questions. Bump Classroom's GAS version in BOTH `Classroom.gs` `VERSION` (from `v01.56g`) and `Classroomgs.version.txt`, and Scraper's in both of its files (from `v02.13g`), with GENERIC entries in both changelogs.
>
> **THE DEPLOY:** the §7.19 order, on its twenty-fourth use. Read Pages first (`https://lightaisolutions.github.io/Sales/gs-versions/Classroomgs.version.txt` is a static file) and the workflow's own `Classroom deploy confirmed` and `Scraper deploy confirmed` lines from the job log. Probe `?op=deploy` only if those disagree, and use `curl -sL` if you do. **Your session touches TWO `.gs` files, so forecast two steps and two logged lines**, taking Classroom from a measured **68/200** to 69 and Scraper from **158/200** to 159. The counter rule: `Updated to` consumes one; a step that never fires consumes none and logs nothing; a leg that fires and does not confirm still consumes one and the tally does not see it; `Already up to date` is silent on consumption and must never be forecast as zero. **Scraper is the counter to watch** — past four fifths of its allowance, and an S2 seed is the only thing that spends it.
>
> **THE CHANGELOG ARITHMETIC — DEFERRED NINE TIMES BY ONE CLAUSE, AND §7.40's "VERY LIKELY THE ROTATOR" WAS WRONG FOR THE REASON §7.40 ITSELF WROTE DOWN.** Every brief from §7.36 forecast a rotation and every one was beaten by the same fact: the push landed **on** 2026-09-16 EST. **The deferral is a function of the day of the push, not of the section count**, and it lapses on the first push dated **2026-09-17 EST or later**, whenever that is. v06.22r closed `CHANGELOG.md` at **111 raw / 93 non-exempt** against a 100 trigger and `Classroomgs.changelog.md` at **56 raw / 48 non-exempt** against a cap of 50. Re-counted from the files: **`CHANGELOG.md` clears in ONE group** (2026-09-08, 10 sections) and **`Classroomgs.changelog.md` needs TWO** (2026-09-01's 2, then 2026-09-02's 11) — do not stop after one group; repeat until fewer than the trigger remain. `Scrapergs.changelog.md` **34/50** and yours this session; `Profilerhtml.changelog.md` **49/50**, untouched by an S2 session. **RE-COUNT FROM THE FILES rather than from this prompt.** Deepen the clone first so every rotated header enriches, and run the mandatory post-rotation grep (`grep '^## \[v' <archive> | grep -v '— \[' | head -5`). Keep using `TZ=America/New_York date` and trust it over the harness banner, which follows UTC.
>
> Normal Pre-Commit and Pre-Push checklists; `claude/*` branch restarted from `origin/main` first, `git ls-remote` before each push — the auto-merge workflow adds a `[skip ci]` SHA-tracker commit after every merge, and after it merges use `git fetch --prune origin` rather than `git fetch origin main`, or the stale remote-tracking ref makes the stop hook report that SHA-tracker commit as unpushed. End every commit message with the two attribution lines this repo's recent commits carry (`Co-Authored-By:` and `Claude-Session:`). Read `repository-information/repository.version.txt` for the version to bump from — do not take it from this prompt, whose own handover commit consumes the next number.
>
> AFTER THIS SESSION Phase 4 is 17 of 26 and S2 is 13 of ~19, so the interleave returns to Phase 4 and the recommended next run is **Phase 4 row 18 — `the-cooling-plant-and-water`** — for which §7.42 should be written.
>
> Then, give me a prompt to paste into a new Opus 5 xhigh session to continue the action plan, then remember session.

### 7.42 · Paste-in brief for Phase 4 row 18 — `the-cooling-plant-and-water` (Opus 5 xhigh) — written 2026-09-16 at v06.24r, at the close of S2 session 13

**Why this and not another S2 segment.** Phase 4 is **17 of 26** and S2 is now **13 of ~19**, so the interleave stands at 17:13 and §7.4 item 6 hands the turn back to Phase 4. Its next row is **18, `the-cooling-plant-and-water`** — spec in `CLASSROOM-CURRICULUM-PLAN.md` §3.4, gate `tracks` (**public**), group The AI Data-Center Wave, id permanent, title *PUE, WUE, and the Plant Outside the Hall*. It is the **forty-eighth** lesson and goes at **position 2 of `aidc-campus`**, between `heat-is-the-constraint` and `the-campus-as-a-power-project` per §4 — a **mid-array insert**, not an append, so expect **P5** where session 13 had none and where row 17's append did not.

**UNSHALLOW FIRST — a Session Start Checklist item, not a brief's favour.** `git fetch --unshallow origin main` before any pin read, any `--check` run, any `git log -1 --format=%cs` date and any CHANGELOG rotation. Session 13 ran it and took the clone to **1,307 commits**. And if a container restart has left the clone behind `origin/main`, run `git fetch --prune origin` and `git checkout -B <branch> origin/main` **before any measurement** — a session that measures first reads every counter wrong.

**THE THREE-PART DEEPENING TEST PASSES ON ALL THREE PARTS, AND IT IS THE STRONGEST PASS ANY PHASE 4 ROW HAS HAD. MEASURED at v06.24r against the parsed literals, not asserted.** Part one: `the-cooling-plant-and-water` sits in **`CRITERION_LEXICON`** on **nine** keys — `cooling`, `liquid`, `pue`, `water`, `wue`, `chiller`, `cdu`, `refrigerant`, `approach temperature`. Part two: it sits in exactly one segment's `READ_NEXT` — **`cooling`**. Part three: those keys hit **all six** of that segment's own `buyingCriteria` (criterion 1 on `water` and `approach temperature`, 2 on `pue`/`water`/`wue`, 3 on `chiller`, 4 on `cdu`, 5 on `chiller`/`cdu`, 6 on `refrigerant`). Row 14's test passed on a single criterion; **this passes on six of six**. So **both halves of the deepening fire**: registering the lesson flips `lesson_ref` in the `read-next` sentence *and* rewrites **every one of the six rows** of `what-is-bought-and-on-what`, all of which currently render *PUE, WUE, and the Plant Outside the Hall* **(planned)**.

**PREDICT BOTH HALVES AND SAY SO BEFORE YOU WRITE. `cooling` IS NOT IN THE DUE SET, so this is the ENTER case, not session 13's clear case.** At v06.24r `--check` reads **7 due** — `cells-and-chemistry`, `power-conversion-and-rack-power-silicon`, `compute-and-the-rack`, `epc-and-construction`, `storage-developers-and-ipps`, `software-and-optimization`, `insurance-and-risk-transfer`, with `capital` cleared. `cooling` is **not** among them and its lesson was last generated **2026-09-14**. So forecast **7 at the start, 8 after the registration, and 8 at the end** — a Phase 4 row regenerates nothing — with `cooling` entering and **differing in two sections**, `read-next` and `what-is-bought-and-on-what`. **Re-measure it rather than trusting this paragraph**, and record which halves actually fired: §10.6 **(w)** says deepening is a real possibility rather than a regular one, and the rule now stands at 3-for-5 across the rows that predicted it.

**THE INPUTS ARE THE WIDEST OF ANY PHASE 4 ROW AND ALL TWELVE EXIST — CHECKED BY FILE ON 2026-09-16.** §3.4 pins eight at the re-plan (`study:vertiv`, `study:aligned`, `study:qts`, `study:holder-construction`, `study:vantage`, `study:switch`, `study:google`, `study:schneider-electric`) plus `concepts:profiler-concepts`, and the v05.07r note adds four more (`study:coolit`, `study:trane-technologies`, `study:edgecore`, `study:mccarthy`) — **twelve study guides, every one present in `live-site-pages/profiler-data/`**. All `study:` and `concepts:`, so the fold is **`tracks`** and the lesson is analyst-visible. **Pin only what you use** (§3.3's rule, exercised in both directions at row 17): open each guide, match its own `slug`, and pin the date off the fetched file per G2 — never from this brief and never from §1.

**§2.3's LINE IS THE ONE CONSTRAINT THAT MATTERS AND IT HAS ALREADY BEEN RE-ARGUED ONCE.** §7 row "Cooling — KEEP the one-lesson cap" records the developer's verdict: G7 closed both halves of the cooling corpus (CoolIT 18 sections, Trane 17; 41 guides and 41 registry concepts touch cooling) and the answer was still **one** lesson. The reversal was to this lesson's **inputs**, not to the line — the `liquid-cooling-hardware` lesson G7 imagined **stays unscheduled**. So this row carries the equipment side *inside* the eight sections §3.4 specifies, and a session that finds itself writing a ninth section about CDU internals has crossed the line the developer drew. **CDU internals decide no storage sale** is the test, in the plan's own words.

**THE SPLIT IS AGAINST A LESSON THAT SITS IMMEDIATELY BEFORE IT IN THE SAME TRACK.** `heat-is-the-constraint` is built, public, position 1 of `aidc-campus`, and it is the other lesson in `cooling`'s `READ_NEXT` — it already owns two of that segment's six criterion rows jointly. Read it before drafting and write the omissions down: §10.6 **(t)** has fired on a tile, a callout, four whole sections and a lane opener across sessions 6 to 12, and a lesson one position upstream in the same track is the most likely place yet for a pre-declared handoff. **Apply §7.23's test** — the one number both lessons cite must do a different job in each.

**`reviewBy` — read, do not sort.** This is a lesson, so the gate is the nearest dated one **in its own material**, not a module's. §8 item 3's default is ~6 months from `updated` for a public lesson, but `the-fence-line` and `bridge-power` both took a real date out of their own subject and `bridge-power` carries 2026-12-31. Cooling has candidates a sort will not find — refrigerant phase-down steps, ASHRAE class revisions, a named water-rights or moratorium date in a member's `policyExposure[]` prose. **Record every rejection in writing**, and note that the curriculum checker currently reports **3** items due for review (`landscape-utilities-2026-09` and `landscape-in-hall-power-2026-09` at 2026-10-01, `landscape-neoclouds-2026-09` at 2026-09-30) — all three by design, **none of them to be "fixed"**. The neoclouds one passes on 30 September and will read overdue until a session revises that module; that is correct behaviour. A `reviewBy` of your own inside the 30-day horizon would make it 4, and `landscape-capital-2026-09`'s **2026-11-06** is the nearest date now held outside it.

**Baselines to capture BEFORE any edit, all measured at the close of v06.24r.** `check-classroom-content.py` **0 errors / 0 warnings at 47 lessons, 8 tracks, 142 gate cases**, module assertion **22** — twenty-two consecutive sessions unbroken. Your commit takes the lesson count to **48** and leaves the module assertion at 22 (a Phase 4 row registers no module), and must return 0/0. `check-classroom-curriculum.py --strict`: no structural findings, **28** stale pins, **3** items due for review. `check-classroom-pipeline.py --selftest`: **13 fixtures / 0 failures**. `build-classroom-segments.py --check`: **7 due**, as listed above. **Watch the micro-markup check**: nested emphasis in any clFmt-fed field, and `{{term}}` in any textContent field — a `proscons` card's `t` and `meta`, a `glossary[].d`, a tile's `k`/`v`/`sub`, a `bars` item's `label`/`sub`, a `timeline`'s `lanes` values, and a section's own `title`/`read`. §3.4's outline specifies a **`proscons`** section (`waterless`), so the `t`/`meta` trap is live on this row in a way it was not on the last two.

**THE SCREENSHOTS ARE NOT OPTIONAL AND THEY HAVE NOW CAUGHT SOMETHING IN FOUR CONSECUTIVE SESSIONS.** Row 17: a false cross-reference in a timeline rung. Session 12: four defects including literal backticks in two quiz questions. **Session 13: three defects every checker passed** — an **unverified corpus-wide superlative** ("the widest any landscape has measured", about a ratio no prior landscape had measured), and **two arithmetic slips**, one of them a sentence shape carried over from the previous module and never recounted against its own table. **Read every section's screenshot individually and recount every number the prose asserts about its own tables.**

**Versions and counters to bump from.** Classroom GAS **v01.57g** (`Classroom.gs` `VERSION` **and** `live-site-pages/gs-versions/Classroomgs.version.txt`, together in one commit); page **v01.14w**, no page bump expected. Scraper **v02.14g** and **not yours** — a Phase 4 row seeds nothing. Profiler v01.39g and never yours. **Repo version: read `repository-information/repository.version.txt` on the day and bump by 0.01** — do not take it from this brief, whose own handover commit consumes the next number.

**THE DEPLOY — the §7.19 order, on its twenty-fifth use.** Read **Pages** first (`https://lightaisolutions.github.io/Sales/gs-versions/Classroomgs.version.txt` is a static file) and the workflow's own `Classroom deploy confirmed` line from the job log; probe `?op=deploy` only if the two disagree, with `curl -sL`. **Counters MEASURED at v06.24r against that push's own job log: `Classroom deploy confirmed (GET): Updated to v01.57g (deployment 69) | 69/200` and `Scraper deploy confirmed (GET): Updated to v02.14g (deployment 159) | 159/200`.** Classroom is now at a **fifth consecutive equal cycle** (69 = 69) with **131** of its allowance left; **Scraper sits at 159/200 with 41 left**, and only an S2 seed moves it. Your session touches **one** `.gs` file — Classroom — so forecast **one** step, **one** logged line, and Classroom at **70**. Counter rule unchanged: `Updated to` consumes one; a step that never fires consumes none and logs nothing; a leg that fires and does not confirm still consumes one and the tally does not see it; `Already up to date` is silent on consumption and must never be forecast as zero. **A counter forecast has to add every intervening `.gs` push, not just the forecasting session's own** — the correction §7.41 recorded after §7.40 was wrong by one.

**CHANGELOGS — AND THIS IS THE FIRST BRIEF THAT CAN STATE THE ROTATION CONDITION AS A DATE TEST RATHER THAN A FORECAST.** §7.36 through §7.41 each predicted a rotation and each was beaten by the same clause: the push landed **on** 2026-09-16 EST. **The deferral is a function of the day of the push, not of the section count**, and it lapses on the first push dated **2026-09-17 EST or later**. So: run `TZ=America/New_York date '+%Y-%m-%d'` and **trust it over the harness banner, which follows UTC and flips five hours early**. If it reads 2026-09-17 or later, **you are the rotator** and the arithmetic is already worked out: `CHANGELOG.md` clears in **ONE** group (2026-09-08, 10 sections) and `Classroomgs.changelog.md` needs **TWO** (2026-09-01's 2, then 2026-09-02's 11) — **do not stop after one group**; repeat until fewer than the trigger remain. **RE-COUNT FROM THE FILES rather than from this brief.** Deepen the clone first so every rotated header enriches, and run the mandatory post-rotation grep (`grep '^## \[v' <archive> | grep -v '— \[' | head -5`).

**Eight open items recorded and not taken.** (i) The `.cl-tbl` `min-width` question still wants a session that edits `Classroom.html`. (ii) The generated `read-next` **names** the landscape module without linking to it (`clFmt` has no link token) — with (i), plus a developer `--all` run. (iii) **`the-800-vdc-shift`'s `three-chains` intro is now twenty registrations stale.** (iv) §10.6's session-4 bullet block is still out of chronological order — cosmetic. (v) **A stale `lesson_ref` can make a segment due on its own**, now traced at four separate segments; the case for a developer decision keeps strengthening. (vi) The DC-arc taxonomy gap stays with §7 row 20. (vii) **`restart` scores zero across all 472 Scraper terms** and is unseeded for a **fourth** session. (viii) **A SECOND standing reservation debt**, identified at session 13: six terms session 12 assigned "on split grounds to `topic-aidc-landlords`" are **in no seed and still score zero**, because an S2 session may only add `topic-landscape-<segment>` and that seed has not been edited since session 6. **A term assigned to another seed's territory is not a term seeded there** — the pattern is general, and it now wants a developer decision alongside the GPU-residual-value gap, which is unchanged.

> Author Phase 4 row 18 — the `the-cooling-plant-and-water` lesson — on Opus 5 xhigh as a fresh session. This is ONE lesson. It authors no guidance module, no dossier and no report, creates no track, seeds no Scraper topic, and regenerates no segment.
>
> READ FIRST: repository-information/SESSION-CONTEXT.md (Latest Session); INTEGRATED-REMEDIATION-PLAN.md §7.42 (your brief), §7.40 and §7.41 (for the Phase 4 lesson signature, the both-halves due-count rule, what row 17 settled about declining a reserved taxonomy, and what session 13 settled about a criterion row that is populated rather than dashed), §7.3 orders 5 and 6, and §6; CLASSROOM-CURRICULUM-PLAN.md §3.4 IN FULL (the lesson spec, its eight section ids and their order, the twelve inputs and the v05.07r inputs note), §4 (the teaching order — this lesson goes at **position 2** of `aidc-campus`), §2.3 and the §7 "Cooling — KEEP the one-lesson cap" row (the line you may not cross), §7 row 18, §8 items 2 and 3, and §10.2–§10.4 and §10.6 (bb1) for what a lesson registration does to the segment layer. Then .claude/rules/classroom-app.md, repository-information/CLASSROOM-SCHEMA.md, and .claude/rules/changelog-security.md before you write any public changelog line.
>
> **UNSHALLOW THE CLONE FIRST** — `git fetch --unshallow origin main`, before any measurement and before any pin. A shallow clone's boundary commit looks like it introduced every file, so `git log -1 --format=%cs -- <path>` returns the boundary's date rather than the file's, and every rotation SHA lookup beyond the horizon fails. **If a container restart has left your clone behind `origin/main`**, run `git fetch --prune origin` and `git checkout -B <branch> origin/main` BEFORE any measurement.
>
> **THE WORK — one push commit.** (1) Author `clLessonTheCoolingPlantAndWater_()` **INSIDE** the `// CONTENT START` … `// CONTENT END` fence in `Classroom.gs`, id `the-cooling-plant-and-water`, type `module`, group The AI Data-Center Wave, gate `tracks`, at §3.4's **eight** section ids in §3.4's order. (2) Register it at the **END** of `clLessons_()` and **INSERT it at position 2** of `clTrackAidcCampus_()`'s `lessons[]` — between `heat-is-the-constraint` and `the-campus-as-a-power-project` — per §4, advancing that track's `updated` and extending its running `short` if it names the sequence. (3) Flip §7.3's Phase 4 row and §6's Phase 4 row to **18 of 26**, flip curriculum §7 row 18 to built with a record, and update §4's `aidc-campus` table at position 2. (4) Write §7.43 — the next run's brief, which by the interleave is **S2 session 14, `epc-and-construction`** — in the same commit, and **edit BOTH halves of any §7.x brief you refresh**: the prose paragraphs and the quoted prompt block.
>
> **THE DEEPENING TEST PASSES ON ALL THREE PARTS AND THIS IS ITS STRONGEST PASS YET — MEASURED at v06.24r, not asserted.** `the-cooling-plant-and-water` sits in `CRITERION_LEXICON` on **nine** keys (`cooling`, `liquid`, `pue`, `water`, `wue`, `chiller`, `cdu`, `refrigerant`, `approach temperature`); it sits in exactly one segment's `READ_NEXT` (**`cooling`**); and those keys hit **all six** of that segment's own `buyingCriteria`. So both halves fire: registering the lesson flips the `read-next` sentence **and** rewrites **every one of the six** `what-is-bought-and-on-what` rows, which today all render *PUE, WUE, and the Plant Outside the Hall* **(planned)**. **Grep the file; do not take the lexicon from this prompt.**
>
> **PREDICT BOTH NUMBERS BEFORE YOU WRITE, AND THIS IS THE ENTER CASE.** `--check` should read **7 due** as you start — `cells-and-chemistry`, `power-conversion-and-rack-power-silicon`, `compute-and-the-rack`, `epc-and-construction`, `storage-developers-and-ipps`, `software-and-optimization`, `insurance-and-risk-transfer` — with **`cooling` NOT among them** and its lesson last generated 2026-09-14. Forecast **7 at the start and 8 after the registration**, with `cooling` entering and differing in **two** sections, and **8 at the end** because a Phase 4 row regenerates nothing. If the second number is not 8, or if `cooling` differs in only one section, that is a finding about the generator and it belongs in the record.
>
> **§2.3's LINE IS THE CONSTRAINT AND IT HAS ALREADY BEEN RE-ARGUED ONCE.** The developer's verdict is on the record: G7 closed both halves of the cooling corpus and the answer was still **one** lesson; the reversal was to this lesson's **inputs**, not to the line, and the `liquid-cooling-hardware` lesson G7 imagined **stays unscheduled**. The equipment side goes **inside** §3.4's eight sections. The test, in the plan's own words: **CDU internals decide no storage sale.** A ninth section is the tell that the line has been crossed.
>
> **TWELVE INPUTS, ALL PRESENT, AND PIN ONLY WHAT YOU USE.** `study:vertiv`, `study:aligned`, `study:qts`, `study:holder-construction`, `study:vantage`, `study:switch`, `study:google`, `study:schneider-electric`, `study:coolit`, `study:trane-technologies`, `study:edgecore`, `study:mccarthy`, plus `concepts:profiler-concepts` — all verified present on 2026-09-16. All `study:` and `concepts:`, so the fold is **`tracks`** and the lesson is analyst-visible. **Open each guide and pin the date off the fetched file** (G2); §3.3's rule cuts both ways — drop an input you did not use, and pin one §3.4 does not name if you used it and said why.
>
> **READ THE LESSON ONE POSITION UPSTREAM BEFORE DRAFTING.** `heat-is-the-constraint` is built, public, position 1 of the same track, and the other lesson in `cooling`'s `READ_NEXT`; it already owns two of that segment's six criterion rows jointly with this one. §10.6 **(t)** has now fired on a tile, a callout, four whole sections and a lane opener — **a lesson immediately upstream in the same track is the likeliest pre-declared handoff yet**. Write the omissions into the function's header comment, and apply §7.23's test: the one number both lessons cite must do a **different job** in each.
>
> **`reviewBy`** — read, do not sort. This is a lesson, so the gate is the nearest dated one **in its own material**. §8 item 3's default is ~6 months from `updated`, but `the-fence-line` and `bridge-power` both took a real date out of their own subject. Cooling has candidates a sort will not find — a refrigerant phase-down step, an ASHRAE class revision, a named water-rights or moratorium date in a member's `policyExposure[]` prose. **Record every rejection in writing.** The curriculum checker reports **3** items due for review — `landscape-utilities-2026-09` and `landscape-in-hall-power-2026-09` at 2026-10-01 and `landscape-neoclouds-2026-09` at **2026-09-30**, all three by design. **Do not "fix" any of the three.** The neoclouds one passes on 30 September and will then read overdue until a session revises that module — correct behaviour, not a defect. A `reviewBy` of your own inside the 30-day horizon would make it 4. Choose knowingly.
>
> **DO NOT:** author or revise a guidance module; touch `Scraper.gs` (a Phase 4 row seeds nothing); touch `Profiler.gs` or `Profiler.html`; regenerate any segment. And **NEVER sweep a date across `Classroom.gs` with a blanket find-and-replace** — it holds twenty-two independently dated guidance modules and session 9 corrupted a comment-deadline date inside `guidanceDocEo14420_()` that way; scope every replacement to the function you are authoring. One mechanical trap for the registry insert: `clTrackAidcCampus_()`'s `lessons[]` is a **mid-array insert at position 2**, not an append, so read the array before editing and confirm the four existing ids are `heat-is-the-constraint`, `the-campus-as-a-power-project`, `clean-firm-power`, `where-bess-plugs-in` in that order. A second trap for the lesson itself: the anchor for a new function inside the fence is the previous function's closing `}` **on its own line**, NOT the literal's `};` — splicing after the `};` nests your function inside the previous one, which is valid JavaScript so `node --check` passes while the registry can never resolve the name. **Verify by counting unbalanced braces before your definition and getting 0**, with a tokenizer that skips strings and comments.
>
> **VERIFY:** `node --check` on a `.js` copy of `Classroom.gs`; `scripts/check-gas-inner-scripts.js`; `scripts/check-classroom-content.py` — capture a pristine HEAD baseline BEFORE any edit. It is **0 errors / 0 warnings at 47 lessons / 8 tracks / 142 gate cases** with the module assertion at **22**, and must be 0/0 after at **48 lessons** with the assertion **still 22**. **Watch the micro-markup check**: nested emphasis in any clFmt-fed field, and `{{term}}` in any textContent field — and note that §3.4 specifies a **`proscons`** section (`waterless`), so the card `t`/`meta` trap is live on this row in a way it was not on the last two. Then `scripts/check-classroom-curriculum.py --strict` (no structural findings; 28 stale pins and 3 items due for review are pre-existing and not yours); `scripts/check-classroom-pipeline.py --selftest` (13 fixtures, 0 failures) and `--base origin/main` (expect **P1 and P5** — P5 for the mid-array insert; **never P3**, a lesson touches no gate symbol); `build-classroom-segments.py --check` before and after; `scripts/check-readme-tree.py` after the version bump. Playwright render at **analyst** with ZERO page errors — this lesson is public, so analyst is the tier that proves the gate — plus a **viewer denial** from the real serving path. The lesson entrypoint is `clShowLesson(id)` and section nodes are `#cl-<sectionId>`; `payload.action === 'guidance'` routes to `handleGuidanceOp_` and everything else to `handleClassroomOp_`; `_gasPost` returns a PROMISE, `sess.permissions` must be an ARRAY in the shim, the session token must be at least 32 characters or the real guard returns SESSION_EXPIRED, the session keys are `Classroom_gas_session_token` / `_gas_user_email` / `_gas_user_role` / `_gas_user_permissions`, overlays to hide are `#auth-wall` / `#signing-out-wall` / `#auth-timers` / `#gas-layer-toggle` plus `#cl-app{display:block !important}`, `AUTO_REFRESH` is a `var AUTO_REFRESH = true;` declaration, and the scratch site root needs `favicon.ico` copied in beside `profiler-data/`, `sounds/` and `images/`. **SERVE THE SCRATCH COPY AT `/Classroom.html`, NOT AT THE SITE ROOT.** The page's CSP `connect-src` is `'self'` plus Google hosts, so a shim on another port is REFUSED: serve the static files and the `/__gas` route **from the same Node process on one port**, and call the handlers through the vm context object (`ctx.handleClassroomOp_`), not from Node's own scope. **THREE shell traps now, one of them new at session 13.** `pgrep -f "[s]him[.]js"` matches the invoking shell's own command line — but so does the replacement §7.41 recommends, in reverse: `ps -eo pid,args | grep -E "node .*/shim"` **misses the process entirely when the shim is launched from its own directory**, because the arguments are then literally `node shim.js` with no slash. **Match on `node .*shim`.** And **treat an unchanged rendered character count after an edit as evidence of a stale process, not of an unchanged page** — session 13 re-rendered after three fixes, got a byte-identical count, and the cause was a `vm` that had loaded `Classroom.gs` at start-up. Third: a heredoc with an **unquoted** delimiter lets bash interpolate backticks inside Python; write scripts to a file with a **quoted** delimiter. **Then READ EVERY SECTION'S SCREENSHOT** — four consecutive sessions have had a defect caught there and nowhere else, and session 13's three were an unverified corpus-wide superlative plus two arithmetic slips against the module's own tables. **Recount every number the prose asserts about a table it sits beside.** Bump Classroom's GAS version in BOTH `Classroom.gs` `VERSION` (from `v01.57g`) and `Classroomgs.version.txt`, with a GENERIC entry in the GAS changelog.
>
> **THE DEPLOY:** the §7.19 order, on its twenty-fifth use. Read Pages first (`https://lightaisolutions.github.io/Sales/gs-versions/Classroomgs.version.txt` is a static file) and the workflow's own `Classroom deploy confirmed` line from the job log. Probe `?op=deploy` only if those disagree, and use `curl -sL` if you do. **Your session touches ONE `.gs` file, so forecast one step and one logged line** — Classroom from a **measured 69/200 to 70**, with Scraper unchanged at **159/200** (41 left, and only an S2 seed moves it). The counter rule: `Updated to` consumes one; a step that never fires consumes none and logs nothing; a leg that fires and does not confirm still consumes one and the tally does not see it; `Already up to date` is silent on consumption and must never be forecast as zero. **A counter forecast has to add every intervening `.gs` push, not just your own.**
>
> **THE CHANGELOG ARITHMETIC — AND YOU MAY BE THE ROTATOR, SO TEST THE DATE RATHER THAN GUESSING.** Six consecutive briefs forecast a rotation and every one was beaten by the same fact: the push landed **on** 2026-09-16 EST. **The deferral is a function of the day of the push, not of the section count**, and it lapses on the first push dated **2026-09-17 EST or later**. Run `TZ=America/New_York date '+%Y-%m-%d'` and **trust it over the harness banner, which follows UTC and flips five hours early**. If it reads 2026-09-17 or later you are the rotator: `CHANGELOG.md` clears in **ONE** group (2026-09-08, 10 sections) and `Classroomgs.changelog.md` needs **TWO** (2026-09-01's 2, then 2026-09-02's 11) — **do not stop after one group**; repeat until fewer than the trigger remain. `Scrapergs.changelog.md` is not yours this session; `Profilerhtml.changelog.md` sits closest to its own trigger and is untouched by a Phase 4 row. **RE-COUNT FROM THE FILES rather than from this prompt.** Deepen the clone first so every rotated header enriches, and run the mandatory post-rotation grep (`grep '^## \[v' <archive> | grep -v '— \[' | head -5`).
>
> Normal Pre-Commit and Pre-Push checklists; `claude/*` branch restarted from `origin/main` first, `git ls-remote` before each push — the auto-merge workflow adds a `[skip ci]` SHA-tracker commit after every merge, and after it merges use `git fetch --prune origin` rather than `git fetch origin main`, or the stale remote-tracking ref makes the stop hook report that SHA-tracker commit as unpushed. End every commit message with the two attribution lines this repo's recent commits carry (`Co-Authored-By:` and `Claude-Session:`). Read `repository-information/repository.version.txt` for the version to bump from — do not take it from this prompt, whose own handover commit consumes the next number.
>
> AFTER THIS SESSION Phase 4 is 18 of 26 and S2 is 13 of ~19, so the interleave returns to S2 and the recommended next run is **S2 session 14 — `epc-and-construction`** — for which §7.43 should be written.
>
> Then, give me a prompt to paste into a new Opus 5 xhigh session to continue the action plan, then remember session.


### 7.43 · Paste-in brief for S2 session 14 — `epc-and-construction` (Opus 5 xhigh) — written 2026-09-16 at v06.26r, at the close of Phase 4 row 18

**Why this and not another Phase 4 row.** Phase 4 is **18 of 26** and S2 is **13 of ~19**, so the interleave stands at 18:13 and §7.4 item 6 hands the turn back to S2. Its next segment in the sales-value order is **`epc-and-construction`** — the fourteenth landscape and the **twenty-third** guidance module, lane The Value Chain, tier **contributor**, id `landscape-epc-and-construction-2026-09`, authored **below** the `// CONTENT END` fence and registered at the **END** of `guidanceDocs_()`'s Value Chain lane. Nine sections at §10.6's ids and order; the Scraper seed is `topic-landscape-epc-and-construction`; the segment is regenerated with `--today` set to the EST day of the push.

**UNSHALLOW FIRST — a Session Start Checklist item, not a brief's favour.** `git fetch --unshallow origin main` before any pin read, any `--check` run, any `git log -1 --format=%cs` date and any CHANGELOG rotation. **And if a container restart has left the clone behind `origin/main`**, run `git fetch --prune origin` and `git checkout -B <branch> origin/main` BEFORE any measurement — a session that measures first reads every counter wrong.

**THIS IS THE MOST INCUMBENT-HEAVY ROSTER IN THE TAXONOMY AND THE RANKING PROBLEM IS THEREFORE THE OPPOSITE OF SESSION 13'S. MEASURED at v06.26r against the parsed registry.** Nineteen members, **16 · 2 · 1** — sixteen incumbents, two challengers, one adjacent. Session 12's `neoclouds` was one incumbent against six challengers and session 13's `capital` was four incumbents that shared no basis; here **sixteen** members are typed incumbent and the segment carries **no `notes` field**, so `who-dominates-and-on-what-basis` has sixteen bases to sort and no registry ordering to lean on. **The bases are not one kind of claim**: several are third-party rankings on named scales (ENR #1, #2, #6, #7; BD+C #1; a #1 US electrical contractor at 3.7× the runner-up; #2 US BESS builder 2024 at 4.6 GWh), several are gigawatt or dollar volumes, and several are role claims with no number at all. **Do not rank sixteen firms on four incompatible scales.** Session 13's verdict — decline the ranking and test the rivalry against the graph — is the precedent, and the graph is likely to be far denser here than in `capital`: measure the edges before deciding, and if a ranking *is* publishable say on which single basis and for how many of the sixteen it is available.

**EIGHT OF NINETEEN ARE SHARED AND THE INVERSION RUNS ONE WAY.** Measured: `bechtel`, `black-veatch`, `burns-mcdonnell`, `kiewit`, `quanta-services` and `rosendin` are **incumbent here and `adjacent` everywhere else** they appear (`clean-firm-and-nuclear`, `assurance`, `grid-equipment`, `in-hall-power`); `samsung-ct` is a challenger here and adjacent twice elsewhere; `strata-clean-energy` is the lone adjacent here and a **`challenger`** in `storage-developers-and-ipps`. So **seven of the eight invert downward and one inverts upward** — the mirror of session 13's finding, where all three adjacents were ranked higher elsewhere. The reading to test: this is the segment everyone *else's* segment borrows a builder from, which is what a services layer looks like from the registry's side. **Re-measure it; do not take these eight from this brief.**

**THE SEGMENT IS ALREADY DUE, AND YOU CLEAR IT — THIS IS THE CLEAR CASE, NOT ROW 18'S ENTER CASE.** At v06.26r `--check` reads **8 due** and `epc-and-construction` is among them, differing in **`read-next` and `what-is-bought-and-on-what`**, with `inputs moved: none` — the stale `lesson_ref` Phase 4 row 17 left behind when it built `where-the-chain-breaks` without regenerating (open item (v), now traced at six segments). **Three of its six criteria already resolve** to *From Site Control to Commercial Operation* and **three print a dash**; read the generator before you write and say which of the three mechanisms produced each dash — (cc3)'s keyword-artefact case is live here, since criteria 4, 5 and 6 are written in commercial vocabulary (`bonding`, `percentage-of-completion`, `TRIR`) that the lexicon does not carry at all. Forecast **8 at the start, 8 after the module** (a module registration moves no `lesson_ref`) **and 7 at the end**, this segment clearing on its own regeneration. **Record all three numbers before you write them.**

**`reviewBy` — read, do not sort, and this segment's fence is unusually thin on dates.** Sixteen incumbents means sixteen `policyExposure[]` arrays to read, and construction regimes date differently from equipment ones: prevailing-wage rules, apprenticeship-ratio requirements and safety regimes carry *enjoined* and *in-effect* statuses more often than future effective dates. **Row 18's failure mode is the one to watch for**: its sort returned four genuine future day-level dates and every one was twenty-seven months out or further, so the default was taken *despite* a non-empty result rather than because of an empty one. Record every rejection in writing. The curriculum checker currently reports **3** items due for review — `landscape-utilities-2026-09` and `landscape-in-hall-power-2026-09` at 2026-10-01, `landscape-neoclouds-2026-09` at **2026-09-30** — all three by design; **do not "fix" any of the three**. `landscape-capital-2026-09`'s **2026-11-06** is the nearest date held outside the 30-day horizon.

**Baselines to capture BEFORE any edit, all measured at the close of v06.26r.** `check-classroom-content.py` **0 errors / 0 warnings at 48 lessons, 8 tracks, 142 gate cases**, module assertion **22** — your commit takes the assertion to **23** and leaves the lesson count at 48 until the regeneration rewrites `segment-epc-and-construction` in place. `check-classroom-curriculum.py --strict`: no structural findings, **28** stale pins, **3** items due for review. `check-classroom-pipeline.py --selftest`: **13 fixtures / 0 failures**. `build-classroom-segments.py --check`: **8 due**. **Watch the micro-markup check** on both tables: nested emphasis in any clFmt-fed field, and `{{term}}` in any `textContent` field — a `proscons` card's `t`/`meta`, a `glossary[].d`, a tile's `k`/`v`/`sub`, a `bars` item's `label`/`sub`, a `timeline`'s `lanes` values.

**AND A NEW CLASS OF SCREENSHOT DEFECT, FOUND AT ROW 18: A GLYPH THAT LIES.** The row-18 tile `PUE ↕ WUE` rendered in the tile's serif headline as a thin mark that reads as a **colon** — *PUE : WUE*, saying *ratio* where the tile meant *trade*. The string was valid JSON, the field sits in `PLAIN_BY_KIND` so no micro-markup rule applied, and `cat -A` on the rendered text confirmed U+2195 was present in the DOM. **It is a font-coverage defect and nothing in the toolchain can see one.** Prefer ASCII in any `tiles[].k`, and read every arrow, dash and symbol in a screenshot rather than in the source. That was one of **three** defects the screenshots caught on row 18, for a **fifth** consecutive session.

**Versions and counters to bump from.** Classroom GAS **v01.58g**, Scraper **v02.14g**, Profiler v01.39g and never yours; page versions unchanged (Classroom v01.14w). **Repo version: read `repository-information/repository.version.txt` on the day and bump by 0.01** — do not take it from this brief.

**THE DEPLOY — the §7.19 order, on its twenty-sixth use.** Read **Pages** first (`https://lightaisolutions.github.io/Sales/gs-versions/Classroomgs.version.txt` and `.../Scrapergs.version.txt` are static files) and the workflow's own `deploy confirmed` lines from the job log; probe `?op=deploy` only if the two disagree, with `curl -sL`. **Counters MEASURED at v06.26r against that push's own job log: `Classroom deploy confirmed (GET): Updated to v01.58g (deployment 70) | 70/200`, with Scraper's step exiting 0 silently in zero seconds on its own `git diff` guard and logging nothing — so Scraper stands at `159/200`.** Exactly **one** step fired and logged exactly one line, and **both legs of v06.26r's forecast were right**. Classroom's headroom is **130**; Scraper's is **41**, and only an S2 seed moves it. So an S2 session touching **both** `.gs` files forecasts **two** steps, **two** logged lines, Classroom **70 → 71** and Scraper **159 → 160** — adding every intervening `.gs` push, not just its own. Counter rule unchanged: `Updated to` consumes one; a step that never fires consumes none and logs nothing; a leg that fires and does not confirm still consumes one and the tally does not see it; `Already up to date` is silent on consumption and must never be forecast as zero. Scraper headroom is **41** and only an S2 seed moves it.

**CHANGELOGS — THE DEFERRAL IS STILL A DATE TEST, AND IT HAS NOW SURVIVED ELEVEN SESSIONS.** Re-counted from the files at v06.26r: `CHANGELOG.md` **114 versioned / 93 non-exempt** against a 100 trigger with **21** sections dated 2026-09-16 EST, and this push makes it **115 / 93** with 22 same-day — the non-exempt count does not move, because every section added since 2026-09-08 has been same-day. `Classroomgs.changelog.md` **57 / 48** against a cap of 50 with 9 same-day, going to **58 / 48**. `Scrapergs.changelog.md` **35**; `Profilerhtml.changelog.md` **49**, closest to its own trigger and untouched by an S2 row. **Run `TZ=America/New_York date '+%Y-%m-%d'` and trust it over the harness banner, which follows UTC and flips five hours early.** If it reads **2026-09-17 or later you are the rotator**: `CHANGELOG.md` clears in **ONE** group (2026-09-08, 10 sections) and `Classroomgs.changelog.md` needs **TWO** (2026-09-01's 2, then 2026-09-02's 11) — **do not stop after one group**; repeat until fewer than the trigger remain. **RE-COUNT FROM THE FILES rather than from this brief.** Deepen the clone first so every rotated header enriches, and run the mandatory post-rotation grep (`grep '^## \[v' <archive> | grep -v '— \[' | head -5`).

**Nine open items recorded and not taken.** (i) The `.cl-tbl` `min-width` question still wants a session that edits `Classroom.html` — twenty-seven data points now. (ii) The generated `read-next` **names** the landscape module without linking to it (`clFmt` has no link token). (iii) **`the-800-vdc-shift`'s `three-chains` intro is now twenty-one registrations stale.** (iv) §10.6's session-4 bullet block is still out of chronological order — cosmetic. (v) **A stale `lesson_ref` can make a segment due on its own**, now traced at **six** segments; the case for a developer decision keeps strengthening. (vi) The DC-arc taxonomy gap stays with §7 row 20. (vii) **`restart` scores zero across all 472 Scraper terms** and is unseeded for a **fifth** session. (viii) **The second standing reservation debt**: six terms session 12 assigned to `topic-aidc-landlords` are in no seed and still score zero. (ix) **NEW at row 18 — a tile glyph can render as a different character and no checker can see it** (above); if it recurs, the fix is a rule rather than a habit.

> Author S2 session 14 — the `epc-and-construction` landscape module — on Opus 5 xhigh as a fresh session. This is ONE guidance module plus its analysis file, one Scraper seed and ONE segment regeneration. It authors no lesson, no dossier and no report, and creates no track.
>
> READ FIRST: repository-information/SESSION-CONTEXT.md (Latest Session); INTEGRATED-REMEDIATION-PLAN.md §7.43 (your brief), §7.41 and §7.42 (for the S2 session signature, the both-halves due-count rule, what session 13 settled about declining a ranking whose bases do not share a basis, and what row 18 settled about a `reviewBy` sort that returns real dates and still fails); §7.3 orders 5 and 6, and §6; CLASSROOM-CURRICULUM-PLAN.md §10.2–§10.6 IN FULL (the segment layer, the generator, the module's nine section ids and their order, and the (t)/(bb1)/(cc3) findings), §10.9, and §8 items 2 and 3. Then .claude/rules/industry-guidance.md, .claude/rules/classroom-app.md, repository-information/CLASSROOM-SCHEMA.md, .claude/rules/scraper-sources.md and .claude/rules/changelog-security.md before you write any public changelog line.
>
> **UNSHALLOW THE CLONE FIRST** — `git fetch --unshallow origin main`, before any measurement and before any pin. A shallow clone's boundary commit looks like it introduced every file, so `git log -1 --format=%cs -- <path>` returns the boundary's date rather than the file's, and every rotation SHA lookup beyond the horizon fails. **If a container restart has left your clone behind `origin/main`**, run `git fetch --prune origin` and `git checkout -B <branch> origin/main` BEFORE any measurement.
>
> **THE WORK — one push commit.** (1) Write `repository-information/industry-guidance/landscape-epc-and-construction-analysis.md`, the source of truth, with §2 (the split) drafted before any other section. (2) Author `guidanceDocLandscapeEpcAndConstruction_()` **BELOW** the `// CONTENT END` fence in `Classroom.gs`, id `landscape-epc-and-construction-2026-09`, lane The Value Chain, tier **contributor**, nine sections at §10.6's ids and order, and register it at the **END** of `guidanceDocs_()`'s Value Chain lane. (3) Seed `topic-landscape-epc-and-construction` in `Scraper.gs`. (4) Regenerate **exactly one** segment — `build-classroom-segments.py --only epc-and-construction --today <EST day of the push>`. (5) Flip §7.3's S2 row, §6's S2 row and §10.6's progress note to **14 of ~19**, strike `epc-and-construction`, and write the findings block. (6) Write §7.44 — the next run's brief, which by the interleave is **Phase 4 row 19, `how-a-cell-is-made`** — in the same commit, and **edit BOTH halves of any §7.x brief you refresh**: the prose paragraphs and the quoted prompt block.
>
> **SIXTEEN INCUMBENTS OF NINETEEN — THE MOST INCUMBENT-HEAVY ROSTER IN THE TAXONOMY, AND THE RANKING PROBLEM IS THE INVERSE OF SESSION 13'S. MEASURED at v06.26r, not asserted.** 19 members, **16 · 2 · 1**, and the segment carries **no `notes` field**, so `who-dominates-and-on-what-basis` has sixteen bases to sort and no registry ordering. **The bases are four incompatible kinds of claim** — third-party rankings on named scales (ENR #1/#2/#6/#7, BD+C #1, #1 US electrical contractor at 3.7× the runner-up, #2 US BESS builder 2024 at 4.6 GWh), gigawatt volumes, dollar volumes, and role claims with no number. **Do not rank sixteen firms on four scales.** Session 13's verdict — decline the ranking and test the rivalry against the graph — is the precedent; measure the edges first, and if a ranking *is* publishable, say on which single basis and for how many of the sixteen it is available. **Parse the registry; do not take the roster from this prompt.**
>
> **EIGHT OF NINETEEN ARE SHARED AND SEVEN OF THE EIGHT INVERT DOWNWARD.** `bechtel`, `black-veatch`, `burns-mcdonnell`, `kiewit`, `quanta-services` and `rosendin` are incumbent here and **`adjacent` everywhere else**; `samsung-ct` is a challenger here and adjacent twice elsewhere; `strata-clean-energy` is the lone adjacent here and a **`challenger`** in `storage-developers-and-ipps`. That is the mirror of session 13, where all three adjacents ranked *higher* elsewhere. The reading to test: this is the segment every other segment borrows a builder from. **Re-measure it.**
>
> **PREDICT ALL THREE NUMBERS BEFORE YOU WRITE, AND THIS IS THE CLEAR CASE.** `--check` should read **8 due** as you start, with **`epc-and-construction` among them** — differing in `read-next` and `what-is-bought-and-on-what`, `inputs moved: none`, the stale `lesson_ref` Phase 4 row 17 left behind. Forecast **8 at the start, 8 after the module** (a module registration moves no `lesson_ref`) **and 7 at the end**, this segment clearing on its own regeneration. **Three of its six criteria already resolve and three print a dash** — read the generator before you write and name which of the three mechanisms produced each dash; (cc3)'s keyword-artefact case is live, since criteria 4, 5 and 6 are written in commercial vocabulary (`bonding`, `percentage-of-completion`, `TRIR`) the lexicon does not carry.
>
> **`reviewBy`** — read, do not sort. Sixteen incumbents means sixteen `policyExposure[]` arrays, and construction regimes carry *enjoined* and *in-effect* statuses more often than future effective dates. **Row 18's failure mode is the one to watch**: its sort returned four genuine future day-level dates and every one was twenty-seven months out or further, so the default was taken *despite* a non-empty result. **Record every rejection in writing.** The curriculum checker reports **3** items due for review — `landscape-utilities-2026-09` and `landscape-in-hall-power-2026-09` at 2026-10-01 and `landscape-neoclouds-2026-09` at **2026-09-30**, all three by design. **Do not "fix" any of the three.**
>
> **DO NOT:** author or revise a lesson; regenerate more than the one segment; edit a generated `segment-*` literal by hand; touch `Profiler.gs` or `Profiler.html`. And **NEVER sweep a date across `Classroom.gs` with a blanket find-and-replace** — it holds twenty-two independently dated guidance modules and session 9 corrupted a comment-deadline date inside `guidanceDocEo14420_()` that way; scope every replacement to the function you are authoring.
>
> **VERIFY:** `node --check` on a `.js` copy of `Classroom.gs` and of `Scraper.gs`; `scripts/check-gas-inner-scripts.js`; `scripts/check-classroom-content.py` — capture a pristine HEAD baseline BEFORE any edit. It is **0 errors / 0 warnings at 48 lessons / 8 tracks / 142 gate cases** with the module assertion at **22**, and must be 0/0 after with the assertion at **23** and the lesson count still 48. Then `scripts/check-classroom-curriculum.py --strict` (no structural findings; 28 stale pins and 3 items due for review are pre-existing and not yours); `scripts/check-classroom-pipeline.py --selftest` (13 fixtures, 0 failures) and `--base origin/main`; `build-classroom-segments.py --check` before, after the module, and after the regeneration; `scripts/check-readme-tree.py` after the version bumps. Playwright render at **contributor** with ZERO page errors, plus an **analyst denial** from the real serving path. The module entrypoint is `clShowGuidanceDoc(id)` and section nodes are `#cl-<sectionId>`; `payload.action === 'guidance'` routes to `handleGuidanceOp_` and everything else to `handleClassroomOp_`; `_gasPost` returns a PROMISE, `sess.permissions` must be an ARRAY in the shim, the session token must be at least 32 characters or the real guard returns SESSION_EXPIRED, the session keys are `Classroom_gas_session_token` / `_gas_user_email` / `_gas_user_role` / `_gas_user_permissions`, overlays to hide are `#auth-wall` / `#signing-out-wall` / `#auth-timers` / `#gas-layer-toggle` plus `#cl-app{display:block !important}`, `AUTO_REFRESH` is a `var AUTO_REFRESH = true;` declaration, and the scratch site root needs `favicon.ico` copied in beside `profiler-data/`, `sounds/` and `images/`. **SERVE THE SCRATCH COPY AT `/Classroom.html`, NOT AT THE SITE ROOT.** The page's CSP `connect-src` is `'self'` plus Google hosts, so a shim on another port is REFUSED: serve the static files and the `/__gas` route **from the same Node process on one port**, and call the handlers through the vm context object (`ctx.handleClassroomOp_`), not from Node's own scope. **THE SHELL TRAPS, NOW SETTLED.** `pgrep -f "[s]him[.]js"` matches the invoking shell's own command line; `ps -eo pid,args | grep -E "node .*/shim"` misses the process when the shim is launched from its own directory. **`ps -eo pid,args | grep -E "node .*shim"` is the pattern that works** — verified at row 18, where it found and killed a stale `vm` and the re-render's character count then moved (29,217 → 29,232), which is the evidence to demand: **an unchanged rendered character count after an edit is evidence of a stale process, not of an unchanged page.** And a heredoc with an **unquoted** delimiter lets bash interpolate backticks inside Python; write scripts to a file with a **quoted** delimiter. **Then READ EVERY SECTION'S SCREENSHOT** — five consecutive sessions have had a defect caught there and nowhere else, and row 18's three included **a glyph that lies**: `↕` rendered as a colon in a tile's serif headline, valid JSON, exempt from every micro-markup rule, present in the DOM. **Prefer ASCII in `tiles[].k`, and read every arrow, dash and symbol in the screenshot rather than in the source.** Recount every number the prose asserts about a table it sits beside. Bump Classroom's GAS version in BOTH `Classroom.gs` `VERSION` (from `v01.58g`) and `Classroomgs.version.txt`, and Scraper's in BOTH `Scraper.gs` `VERSION` (from `v02.14g`) and `Scrapergs.version.txt`, with GENERIC entries in both GAS changelogs.
>
> **THE DEPLOY:** the §7.19 order, on its twenty-sixth use. Read Pages first (`https://lightaisolutions.github.io/Sales/gs-versions/Classroomgs.version.txt` and `.../Scrapergs.version.txt` are static files) and the workflow's own `deploy confirmed` lines from the job log. Probe `?op=deploy` only if those disagree, and use `curl -sL` if you do. **Your session touches TWO `.gs` files, so forecast two steps and two logged lines** — Classroom from a **measured 70/200 to 71** and Scraper from a **measured 159/200 to 160**. v06.26r's log read `Classroom deploy confirmed (GET): Updated to v01.58g (deployment 70) | 70/200` and logged nothing for Scraper, whose step exited 0 silently on its `git diff` guard. A counter forecast has to add every intervening `.gs` push, not just your own. Scraper headroom is **41** and only an S2 seed moves it.
>
> **THE CHANGELOG ARITHMETIC — AND YOU MAY BE THE ROTATOR, SO TEST THE DATE RATHER THAN GUESSING.** Eleven consecutive sessions have been deferred by the same fact: the push landed **on** 2026-09-16 EST. **The deferral is a function of the day of the push, not of the section count**, and it lapses on the first push dated **2026-09-17 EST or later**. Run `TZ=America/New_York date '+%Y-%m-%d'` and **trust it over the harness banner, which follows UTC and flips five hours early**. If it reads 2026-09-17 or later you are the rotator: `CHANGELOG.md` clears in **ONE** group (2026-09-08, 10 sections) and `Classroomgs.changelog.md` needs **TWO** (2026-09-01's 2, then 2026-09-02's 11) — **do not stop after one group**; repeat until fewer than the trigger remain. `Scrapergs.changelog.md` sits at 35 and `Profilerhtml.changelog.md` at 49, closest to its own trigger and untouched by an S2 row. **RE-COUNT FROM THE FILES rather than from this prompt.** Deepen the clone first so every rotated header enriches, and run the mandatory post-rotation grep (`grep '^## \[v' <archive> | grep -v '— \[' | head -5`).
>
> Normal Pre-Commit and Pre-Push checklists; `claude/*` branch restarted from `origin/main` first, `git ls-remote` before each push — the auto-merge workflow adds a `[skip ci]` SHA-tracker commit after every merge, and after it merges use `git fetch --prune origin` rather than `git fetch origin main`, or the stale remote-tracking ref makes the stop hook report that SHA-tracker commit as unpushed. End every commit message with the two attribution lines this repo's recent commits carry (`Co-Authored-By:` and `Claude-Session:`). Read `repository-information/repository.version.txt` for the version to bump from — do not take it from this prompt, whose own handover commit consumes the next number.
>
> AFTER THIS SESSION Phase 4 is 18 of 26 and S2 is 14 of ~19, so the interleave returns to Phase 4 and the recommended next run is **Phase 4 row 19 — `how-a-cell-is-made`** — for which §7.44 should be written.
>
> Then, give me a prompt to paste into a new Opus 5 xhigh session to continue the action plan, then remember session.

Developed by: LightAISolutions

### 7.44 · Paste-in brief for Phase 4 row 19 — `how-a-cell-is-made` (Opus 5 xhigh) — written 2026-09-16 at v06.28r, at the close of S2 session 14

**Why this and not another S2 segment.** Phase 4 is **18 of 26** and S2 is now **14 of ~19**, so the interleave stands at 18:14 and §7.4 item 6 hands the turn back to Phase 4. Its next row is **19, `how-a-cell-is-made`** — spec in `CLASSROOM-CURRICULUM-PLAN.md` §3.1, gate `tracks` (**public**), group Technology Foundations, id permanent, title *From Powder to Cell: Manufacturing, Formats, and Yield*. It is the **forty-ninth** lesson and goes at **position 5 of `bess-foundations`**, between `the-control-stack` and `where-batteries-stop` per §4 — a **mid-array insert**, not an append, so expect **P5**, as rows 15, 16, 17 and 18 did.

**UNSHALLOW FIRST — a Session Start Checklist item, not a brief's favour.** `git fetch --unshallow origin main` before any pin read, any `--check` run, any `git log -1 --format=%cs` date and any CHANGELOG rotation. Session 14 ran it and took the clone to **1,315 commits**. **And if a container restart has left the clone behind `origin/main`**, run `git fetch --prune origin` and `git checkout -B <branch> origin/main` BEFORE any measurement — a session that measures first reads every counter wrong.

**THE G2 TRAP ON THIS ROW IS THE WORST ANY ROW HAS CARRIED: ALL TEN STUDY GUIDES' `lastUpdated` DIFFER FROM THEIR `git log` DATE. MEASURED at v06.28r, guide by guide.** Row 18 found six of twelve; this row is **ten of ten**. `eve-energy`, `lg-energy-solution`, `panasonic` and `samsung-sdi` each read `lastUpdated` **2026-08-21** against a commit date of **2026-08-31**; `hithium` reads **2026-08-08** against **2026-08-31**; and `gotion`, `rept`, `sunwoda`, `trina-storage` and `envision-energy` each read **2026-09-06** against **2026-09-07**. **A session that took the commit date — which IS the correct source for `project:` and `concepts:` — would write ten wrong pins and no checker would see one.** §8 item 2 is the rule: a `study:` ref pins the guide's own `lastUpdated`, read off the fetched file. The `concepts:profiler-concepts` pin is the opposite case and its commit date is **2026-09-13**. **Re-read all eleven on the day; do not take these from this brief.**

**THE STAMP IS ELEVEN INPUTS AND ALL ELEVEN ARE PUBLIC, SO THE GATE FOLDS TO `tracks`.** Ten `study:` refs — `eve-energy`, `lg-energy-solution`, `panasonic`, `samsung-sdi`, `hithium` (the planning five) plus `gotion`, `rept`, `sunwoda`, `trina-storage`, `envision-energy` (the five §7 added at v05.07r) — and `concepts:profiler-concepts` for vocabulary only. **All ten guides exist and were verified present at v06.28r.** §3.1 assigns each a job: the line and yield economics to `eve-energy`; gigafactory capital, JVs and content rules *taught generically* to `lg-energy-solution`; cylindrical engineering and statistical process control at billions of units to `panasonic`; breaking the thermal-runaway chain at the cell to `samsung-sdi`; the big-cell ladder 280 → 587 → 1,175 Ah to `hithium`; and the C12 pair `gotion`/`rept` for **the factory-as-status example — an Illinois plant that cannot claim §45X because status follows ownership**. **Policy mechanics stay in track 5**: this lesson takes 45X and thermal runaway as *vocabulary*, not as a regime to teach, and `the-china-policy-stack` owns the levers.

**THE DUE COUNT IS THE HOLD CASE AND BOTH HALVES ARE ALREADY MEASURED — SO PREDICT THEM BEFORE YOU RUN ANYTHING.** At v06.28r `--check` reads **7 due**: `cells-and-chemistry`, `power-conversion-and-rack-power-silicon`, `cooling`, `compute-and-the-rack`, `storage-developers-and-ipps`, `software-and-optimization`, `insurance-and-risk-transfer`. **`how-a-cell-is-made` sits in exactly one segment's `READ_NEXT` — `cells-and-chemistry` — which is ALREADY due and differing in `read-next` alone**, so registering this lesson changes that same section a second time and **the count HOLDS at 7 → 7**, with `cells-and-chemistry` still differing in `read-next` only. **There is NO deepening on this row**: measured against the parsed `CRITERION_LEXICON`, **no lexicon entry maps to `how-a-cell-is-made` at all**, so §10.6 (w)'s three-part test cannot fire and the section half is derived rather than guessed. A Phase 4 row regenerates nothing, so the stale `lesson_ref` this leaves behind in `cells-and-chemistry` is open item **(v)** at a **seventh** segment — record it; do not fix it by regenerating.

**WHAT §10.6 (t) WILL FIRE ON, AND IT IS A LESSON IN THE SAME TRACK TWO POSITIONS AWAY.** `cell-to-container` is position 1 of `bess-foundations` and `spec-sheet-decoded` is position 3, and between them they already teach a good deal of this row's outline: the format vocabulary, the Ah ladder as a spec-sheet number, and — per §10.6's own note on §3.1 — the spec sheet "gestures at" the scaling counterweight this row is meant to teach **from the cell side**. **Grep those two lessons before writing a section, and grep `where-batteries-stop` (position 6, built at row 12) too** — it owns the engine boundary and the sodium route, and a manufacturing lesson that re-taught either would breach §2.3's one-lesson cap. §8 item 8's standing rule applies in its own words: *the engine table appears once*. Row 18's inverted (t) is the case to watch for — a lesson that took ground it never named — so check for **undeclared** pre-emption as well as declared handoffs, and write the omissions into the header comment either way.

**`reviewBy` — read, do not sort, and this row's material is unusually likely to produce a real date.** Unlike a landscape's `policyExposure[]` sort, a lesson's gate comes from its own material, and cell manufacturing has two dated families in the corpus: **§45X production-credit step-downs** and **foreign-entity-of-concern status dates**, both of which the C12 pair carries. §8 item 3 is the constraint — a **public** lesson defaults to ~6 months from `updated` and the four lessons added at v05.07r *should* take the nearest dated gate in their own material where one exists. **Do not take a date from `the-china-policy-stack`'s own `reviewBy` (2026-12-31)** — (r) and (u) reject a candidate that is already another module's gate, and session 9's (z5) permits a shared day only when the alternative is fabrication. Record every rejection in writing. The curriculum checker reports **3** items due for review — `landscape-utilities-2026-09` and `landscape-in-hall-power-2026-09` at 2026-10-01, `landscape-neoclouds-2026-09` at **2026-09-30** — all three by design; **do not "fix" any of the three.** `landscape-neoclouds-2026-09` **passes on 30 September** and the checker will then report it overdue until a session revises that module: correct behaviour, not a defect.

**Baselines to capture BEFORE any edit, all measured at the close of v06.28r.** `check-classroom-content.py` **0 errors / 0 warnings at 48 lessons, 8 tracks, 142 gate cases**, module assertion **23** — your commit takes the lesson count to **49** and leaves the module assertion at 23. `check-classroom-curriculum.py --strict`: no structural findings, **28** stale pins, **3** items due for review. `check-classroom-pipeline.py --selftest`: **13 fixtures / 0 failures**. `build-classroom-segments.py --check`: **7 due**. **Watch the micro-markup check**: `PLAIN_BY_KIND` makes a `proscons` card's `t`/`meta`, a `timeline`'s `lanes` values and a `bars` item's `label`/`sub` plain text, and `PLAIN_DOC_FIELDS`/`PLAIN_DOC_LISTS` do the same for `title`, `short`, every `tiles[].k`/`v`/`sub`, every `glossary[].t`/`d` and a `revisions[].note` — **no `{{term}}`, no `**bold**`, no `*italic*` in any of them.** §3.1's outline puts `three-formats` at kind `proscons`, so that is exactly where the rule will bite.

**AND THE SCREENSHOT DISCIPLINE HAS TWO CLASSES NOW, NOT ONE.** Row 18 found **a glyph that lies** — `PUE ↕ WUE` rendering as a colon in a tile's serif headline, valid JSON, exempt from every micro-markup rule, invisible to the whole toolchain. Session 14's countermeasure held (**all four `tiles[].k` in pure ASCII**; the rendered DOM carried only `U+2014`, `U+00B7` and the page's own back-arrow) — **but session 14 was caught twice in a second class: a number the prose asserts about a table it sits beside.** One section said *two rows carry a day* where **three** did; another said *eleven of the nineteen are private* where the `ownership` field says **twelve**. Both passed every checker at 0/0. **So: prefer ASCII in tiles, read every arrow, dash and symbol in the screenshot rather than in the source, AND recount every figure the prose asserts about an adjacent table.** Six consecutive sessions have had a defect caught there and nowhere else.

**Versions and counters to bump from.** Classroom GAS **v01.59g**, Scraper **v02.15g** (and Scraper is **not** yours — a Phase 4 row touches one `.gs` file), Profiler v01.39g and never yours; page versions unchanged (Classroom v01.14w). **Repo version: read `repository-information/repository.version.txt` on the day and bump by 0.01** — do not take it from this brief, whose own handover commit consumes the next number.

**THE DEPLOY — the §7.19 order, on its twenty-seventh use.** Read **Pages** first (`https://lightaisolutions.github.io/Sales/gs-versions/Classroomgs.version.txt` and `.../Scrapergs.version.txt` are static files) and the workflow's own `deploy confirmed` lines from the job log; probe `?op=deploy` only if the two disagree, with `curl -sL`. **Counters MEASURED at v06.28r against that push's own job log, and BOTH legs of its forecast were right: `Scraper deploy confirmed (GET): Updated to v02.15g (deployment 160) | 160/200` and `Classroom deploy confirmed (GET): Updated to v01.59g (deployment 71) | 71/200`.** Exactly **two** steps fired — Scraper in 9 s, Classroom in 14 s — and every other `Deploy <Project>` step exited 0 silently in zero seconds on its own `git diff` guard. **So Classroom stands at 71/200 with 129 of its allowance left, and Scraper at 160/200 with 40.** Pages read `|v01.58g|`/`|v02.14g|` at 10:25 PM EST before the push and `|v01.59g|`/`|v02.15g|` at 10:34 PM after, so the §7.19 order resolved on its **two reads** for the **twenty-sixth** time and no `?op=deploy` probe was made. **Add every intervening `.gs` push to your forecast, not just your own** — that is the correction v06.23r's measurement forced on §7.40. A Phase 4 row touches **Classroom only**, so forecast **one** step and **one** logged line; Scraper's step will exit 0 silently on its own `git diff` guard and log nothing, which is not a failure. Counter rule unchanged: `Updated to` consumes one; a step that never fires consumes none and logs nothing; a leg that fires and does not confirm still consumes one and the tally does not see it; `Already up to date` is silent on consumption and must never be forecast as zero. **Scraper's headroom is 40 and only an S2 seed moves it.**

**CHANGELOGS — THE DEFERRAL SURVIVED A TWELFTH SESSION AND IT IS STILL A DATE TEST.** Re-counted from the files at v06.28r: `CHANGELOG.md` **116 versioned / 93 non-exempt** against a 100 trigger with **23** sections dated 2026-09-16 EST, going to **117 / 93** with 24 same-day — the non-exempt count does not move, because every section added since 2026-09-08 has been same-day. `Classroomgs.changelog.md` **58 / 48** against a cap of 50 with 10 same-day, going to **59 / 48**. `Scrapergs.changelog.md` **35**; `Profilerhtml.changelog.md` **49**, closest to its own trigger and untouched by a Phase 4 row. **Run `TZ=America/New_York date '+%Y-%m-%d'` and trust it over the harness banner, which follows UTC and flips five hours early.** If it reads **2026-09-17 or later you are the rotator**: `CHANGELOG.md` clears in **ONE** group (2026-09-08, **10** sections) and `Classroomgs.changelog.md` needs **TWO** (2026-09-01's **2**, then 2026-09-02's **11**) — **do not stop after one group**; repeat until fewer than the trigger remain. **RE-COUNT FROM THE FILES rather than from this brief.** Deepen the clone first so every rotated header enriches, and run the mandatory post-rotation grep (`grep '^## \[v' <archive> | grep -v '— \[' | head -5`).

**Nine open items recorded and not taken.** (i) The `.cl-tbl` `min-width` question still wants a session that edits `Classroom.html` — **twenty-eight** data points now, session 14's four-column eighteen-row bet table the latest and clean at 1400 px. (ii) The generated `read-next` **names** the landscape module without linking to it (`clFmt` has no link token) — session 14 saw it render correctly as plain text in `segment-epc-and-construction`. (iii) **`the-800-vdc-shift`'s `three-chains` intro is now twenty-two registrations stale.** (iv) §10.6's session-4 bullet block is still out of chronological order — cosmetic. (v) **A stale `lesson_ref` can make a segment due on its own** — now traced at **six** segments, and your row makes `cells-and-chemistry` the **seventh**. (vi) The DC-arc taxonomy gap stays with §7 row 20. (vii) **`restart` scores zero across all 472 Scraper terms** — a **fifth** session unseeded, and it is `topic-landscape-clean-firm-and-nuclear`'s word for that module's next revision. (viii) The **second standing reservation debt**: session 12's six terms assigned to `topic-aidc-landlords` are in no seed and still score zero, re-confirmed at v06.28r. (ix) **NEW at v06.28r — `CRITERION_LEXICON` has no entry that matches a criterion whose material an eligible built lesson already teaches** (§10.6 **(ee4)**), so a dash row can be a *generator* gap rather than a curriculum gap or an artefact. It is the only one of the four dash mechanisms a generator edit could fix, and it wants a developer decision.

> Author Phase 4 row 19 — the `how-a-cell-is-made` lesson — on Opus 5 xhigh as a fresh session. This is ONE lesson. It authors no guidance module, no dossier and no report, regenerates no segment and creates no track.
>
> READ FIRST: repository-information/SESSION-CONTEXT.md (Latest Session); INTEGRATED-REMEDIATION-PLAN.md §7.44 (your brief), §7.42 and §7.43 (for the Phase 4 lesson signature, the mid-array-insert rule, what row 18 settled about an inverted (t) firing and a `reviewBy` sort that returns real dates and still fails, and what session 14 settled about recounting a number the prose asserts about an adjacent table); §7.3 orders 5 and 6, and §6. Then CLASSROOM-CURRICULUM-PLAN.md §3.1 IN FULL (your spec — the six section ids, their kinds and their order), §4 (the `bess-foundations` order, where position 5 is yours), §7 row 19, §8 items 1, 2, 3, 6, 7 and 8, and §10.6's (t)/(w)/(z2)/(aa5)/(ee4)/(ee7) findings. Then .claude/rules/classroom-app.md, repository-information/CLASSROOM-SCHEMA.md, and .claude/rules/changelog-security.md before you write any public changelog line.
>
> UNSHALLOW THE CLONE FIRST — git fetch --unshallow origin main, before any measurement and before any pin. A shallow clone's boundary commit looks like it introduced every file, so git log -1 --format=%cs -- <path> returns the boundary's date rather than the file's. If a container restart has left your clone behind origin/main, run git fetch --prune origin and git checkout -B <branch> origin/main BEFORE any measurement.
>
> **THE WORK — one push commit.** (1) Author `clLessonHowACellIsMade_()` **INSIDE** the `// CONTENT START` … `// CONTENT END` fence in `Classroom.gs`, id `how-a-cell-is-made`, title *From Powder to Cell: Manufacturing, Formats, and Yield*, type `module`, group Technology Foundations, gate folding to `tracks`, at §3.1's six section ids in §3.1's order — `the-line` (prose) · `three-formats` (**proscons**) · `yield-is-the-game` (table) · `scaling-physics` (prose) · `factories-are-the-product` (callout) · `drill` (6 cards) · `check-yourself` (5 items). (2) Register at the END of `clLessons_()` and **MID-ARRAY INSERT at position 5 of `clTrackBessFoundations_()`**, between `the-control-stack` and `where-batteries-stop`. (3) Flip §7.3's Phase 4 row and §6's Phase 4 row to **19 of 26**, flip curriculum §7 row 19 to built with the full record, update §4's `bess-foundations` table at position 5, and write the §10.6 findings. (4) Write **§7.45** — the next run's brief, which by the interleave is **S2 session 15** — in the same commit, and edit **BOTH halves** of any §7.x brief you refresh: the prose paragraphs and the quoted prompt block.
>
> THE G2 TRAP ON THIS ROW IS THE WORST ANY ROW HAS CARRIED — TEN OF TEN. MEASURED at v06.28r, guide by guide: every one of the ten `study:` refs has a `lastUpdated` that DIFFERS from its `git log -1 --format=%cs` date. `eve-energy`, `lg-energy-solution`, `panasonic` and `samsung-sdi` read 2026-08-21 against a commit date of 2026-08-31; `hithium` reads 2026-08-08 against 2026-08-31; `gotion`, `rept`, `sunwoda`, `trina-storage` and `envision-energy` read 2026-09-06 against 2026-09-07. A session taking the commit date — which IS correct for `project:` and `concepts:` — writes TEN wrong pins and NO CHECKER SEES ONE. §8 item 2: a `study:` ref pins the guide's own `lastUpdated`, read off the fetched file. The `concepts:profiler-concepts` pin is the opposite case and its commit date is 2026-09-13. Re-read all eleven on the day; do not take them from this prompt.
>
> PREDICT BOTH NUMBERS BEFORE YOU WRITE, AND THIS IS THE HOLD CASE. `--check` reads 7 due as you start — `cells-and-chemistry`, `power-conversion-and-rack-power-silicon`, `cooling`, `compute-and-the-rack`, `storage-developers-and-ipps`, `software-and-optimization`, `insurance-and-risk-transfer`. `how-a-cell-is-made` sits in exactly ONE segment's `READ_NEXT` — `cells-and-chemistry` — which is ALREADY due and differing in `read-next` alone, so registering the lesson changes that same section a second time and the count HOLDS at 7 → 7. There is NO DEEPENING on this row: measured against the parsed `CRITERION_LEXICON`, no entry maps to `how-a-cell-is-made` at all, so §10.6 (w)'s three-part test cannot fire. Run the test programmatically anyway and say so. A Phase 4 row regenerates nothing, so record the stale `lesson_ref` as open item (v) at a SEVENTH segment rather than fixing it.
>
> §10.6 (t) WILL FIRE, AND ON TWO LESSONS IN YOUR OWN TRACK. `cell-to-container` (position 1) and `spec-sheet-decoded` (position 3) already carry the format vocabulary and the Ah ladder as a spec-sheet number, and §3.1's own outline says the spec sheet "gestures at" the scaling counterweight this row teaches from the cell side. `where-batteries-stop` (position 6, built at row 12) owns the engine boundary and the sodium route. GREP ALL THREE before writing a section, honour §8 item 8's "the engine table appears once", and check for row 18's INVERTED (t) — undeclared pre-emption, not just a declared handoff. Write every omission into the header comment and keep §2.3's one-lesson cap.
>
> POLICY MECHANICS STAY IN TRACK 5. 45X and thermal runaway enter as VOCABULARY only; `the-china-policy-stack` owns the levers and `spec-sheet-decoded` owns the spec sheet. The C12 pair `gotion`/`rept` gives you the factory-as-status example — an Illinois plant that cannot claim §45X because status follows ownership — and that is a FACT about a factory, not a policy lesson.
>
> reviewBy — read, do not sort, and this row is likelier than most to find a real date: cell manufacturing has two dated families in the corpus, §45X step-downs and foreign-entity-of-concern status dates, both carried by the C12 pair. Do NOT take `the-china-policy-stack`'s own 2026-12-31 — (r) and (u) reject a candidate that is already another module's gate. Record every rejection in writing. The curriculum checker reports 3 items due for review — `landscape-utilities-2026-09` and `landscape-in-hall-power-2026-09` at 2026-10-01, `landscape-neoclouds-2026-09` at 2026-09-30, all three by design. Do not "fix" any of the three.
>
> DO NOT: author or revise a guidance module; regenerate any segment; edit a generated `segment-*` literal by hand; touch Profiler.gs or Profiler.html; touch Scraper.gs (a Phase 4 row seeds nothing). And NEVER sweep a date across Classroom.gs with a blanket find-and-replace — it holds twenty-three independently dated guidance modules and session 9 corrupted a comment-deadline date inside `guidanceDocEo14420_()` that way; scope every replacement to the function you are authoring.
>
> VERIFY: `node --check` on a `.js` copy of `Classroom.gs`; `scripts/check-gas-inner-scripts.js`; `scripts/check-classroom-content.py` — capture a pristine HEAD baseline BEFORE any edit. It is 0 errors / 0 warnings at 48 lessons / 8 tracks / 142 gate cases with the module assertion at 23, and must be 0/0 after at **49 lessons** with the assertion still 23. Then `scripts/check-classroom-curriculum.py --strict` (no structural findings; 28 stale pins and 3 items due for review are pre-existing and not yours); `scripts/check-classroom-pipeline.py --selftest` (13 fixtures, 0 failures) and `--base origin/main` — expect **P1** on the plan files, **P5** on the mid-array insert, **no P3** (no gate symbol moves, so `gateDigest` is untouched), **no P8**, and **no P12** provided `VERSION` moves exactly one step. `build-classroom-segments.py --check` before and after (7 → 7). `scripts/check-readme-tree.py` after the version bump. Playwright render at **analyst** with ZERO page errors — the tier that proves a public lesson's gate — plus a `viewer` denial from the real serving path. The lesson entrypoint is `clShowLesson(id)` and section nodes are `#cl-<sectionId>`; `payload.action === 'guidance'` routes to `handleGuidanceOp_` and everything else to `handleClassroomOp_`; `_gasPost` returns a PROMISE, `sess.permissions` must be an ARRAY in the shim, the session parameter is `session` and must be at least 32 characters or the real guard returns SESSION_EXPIRED, the session keys are `Classroom_gas_session_token` / `_gas_user_email` / `_gas_user_role` / `_gas_user_permissions`, overlays to hide are `#auth-wall` / `#signing-out-wall` / `#auth-timers` / `#gas-layer-toggle` plus `#cl-app{display:block !important}`, `AUTO_REFRESH` is a `var AUTO_REFRESH = true;` declaration, and the scratch site root needs `favicon.ico` copied in beside `profiler-data/`, `sounds/` and `images/`. SERVE THE SCRATCH COPY AT `/Classroom.html`, NOT AT THE SITE ROOT. The page's CSP `connect-src` is `'self'` plus Google hosts, so a shim on another port is REFUSED: serve the static files and the `/__gas` route from the same Node process on one port, and call the handlers through the vm context object (`ctx.handleClassroomOp_`), not from Node's own scope. The Chromium binary is at `/opt/pw-browsers/chromium-1194/chrome-linux/chrome` — the unversioned path does NOT exist. THE SHELL TRAPS, SETTLED: `pgrep -f "[s]him[.]js"` matches the invoking shell's own command line and `ps -eo pid,args | grep -E "node .*/shim"` misses a shim launched from its own directory; `ps -eo pid,args | grep -E "node .*shim"` is the pattern that works. Re-extract the PROJECT region after every edit and DEMAND that the rendered character count MOVE — session 14's re-render went 62,894 → 63,051, and an unchanged count after an edit is evidence of a stale process, not of an unchanged page. And a heredoc with an unquoted delimiter lets bash interpolate backticks inside Python; write scripts to a file with a quoted delimiter. Then READ EVERY SECTION'S SCREENSHOT — six consecutive sessions have had a defect caught there and nowhere else, and session 14's two were BOTH the recount class: a "two rows carry a day" claim beside a table where three did, and an "eleven of the nineteen are private" claim where the field says twelve. Prefer ASCII in `tiles[].k` (row 18's glyph that lied), read every arrow, dash and symbol in the screenshot rather than in the source, AND RECOUNT EVERY NUMBER THE PROSE ASSERTS ABOUT A TABLE IT SITS BESIDE. Bump Classroom's GAS version in BOTH `Classroom.gs` VERSION (from v01.59g) and `Classroomgs.version.txt`, with a GENERIC entry in the GAS changelog.
>
> THE DEPLOY: the §7.19 order, on its twenty-seventh use. Read Pages first (`https://lightaisolutions.github.io/Sales/gs-versions/Classroomgs.version.txt` is a static file) and the workflow's own deploy confirmed lines from the job log. Probe `?op=deploy` only if those disagree, and use `curl -sL` if you do. Your session touches ONE `.gs` file, so forecast ONE step and ONE logged line; Scraper's step will exit 0 silently on its own `git diff` guard and log nothing, which is not a failure. Counters measured at v06.28r against that push's own job log, both legs right: Scraper confirmed v02.15g at deployment 160 (160/200) and Classroom confirmed v01.59g at deployment 71 (71/200), with exactly two steps firing and every other Deploy step exiting 0 silently in zero seconds. So Classroom is at 71/200 with 129 left and Scraper at 160/200 with 40 — and add every intervening .gs push to your forecast, not just your own. Scraper headroom is 40 and only an S2 seed moves it.
>
> THE CHANGELOG ARITHMETIC — AND YOU MAY BE THE ROTATOR, SO TEST THE DATE RATHER THAN GUESSING. Twelve consecutive sessions have been deferred by the same fact: the push landed ON 2026-09-16 EST. The deferral is a function of the day of the push, not of the section count, and it lapses on the first push dated 2026-09-17 EST or later. Run `TZ=America/New_York date '+%Y-%m-%d'` and trust it over the harness banner, which follows UTC and flips five hours early. If it reads 2026-09-17 or later you are the rotator: `CHANGELOG.md` clears in ONE group (2026-09-08, 10 sections) and `Classroomgs.changelog.md` needs TWO (2026-09-01's 2, then 2026-09-02's 11) — do not stop after one group; repeat until fewer than the trigger remain. `Scrapergs.changelog.md` sits at 35 and `Profilerhtml.changelog.md` at 49, closest to its own trigger and untouched by a Phase 4 row. RE-COUNT FROM THE FILES rather than from this prompt. Deepen the clone first so every rotated header enriches, and run the mandatory post-rotation grep (`grep '^## \[v' <archive> | grep -v '— \[' | head -5`).
>
> Normal Pre-Commit and Pre-Push checklists; claude/* branch restarted from origin/main first, git ls-remote before each push — the auto-merge workflow adds a [skip ci] SHA-tracker commit after every merge, and after it merges use `git fetch --prune origin` rather than `git fetch origin main`, or the stale remote-tracking ref makes the stop hook report that SHA-tracker commit as unpushed. End every commit message with the two attribution lines this repo's recent commits carry (Co-Authored-By: and Claude-Session:). Read `repository-information/repository.version.txt` for the version to bump from — do not take it from this prompt, whose own handover commit consumes the next number.
>
> AFTER THIS SESSION Phase 4 is 19 of 26 and S2 is 14 of ~19, so the interleave returns to S2 and the recommended next run is S2 session 15 — the next of the five remaining segments (`cooling`, `compute-and-the-rack`, `assurance`, `software-and-optimization`, `insurance-and-risk-transfer`, all five already in the standing due set) — for which §7.45 should be written.
>
> Then, give me a prompt to paste into a new Opus 5 xhigh session to continue the action plan, then remember session.

### 7.45 · Paste-in brief for S2 session 15 — `cooling` (Opus 5 xhigh) — written 2026-09-17 at v06.30r, at the close of Phase 4 row 19

**Why this and not another Phase 4 row.** Phase 4 is **19 of 26** and S2 is **14 of ~19**, so the interleave stands at 19:14 and §7.4 item 6 hands the turn back to S2. **Which segment is next needs one sentence of honesty, because the §7.3 sales-value order is not written out anywhere as a list.** Five remain — `cooling`, `compute-and-the-rack`, `software-and-optimization`, `assurance`, `insurance-and-risk-transfer` — and only the last two are fixed: the v05.62r CHANGELOG record states that `assurance` and `insurance-and-risk-transfer` are **18th and 19th of 19**. That leaves positions 15–17 unranked among the other three, and this brief takes **`cooling`** on three grounds a session can re-test in a minute: it is the **deepest of the three at eleven members** against `software-and-optimization`'s twenty-of-which-fifteen-are-adjacent and `compute-and-the-rack`'s four; it is the only one of the three whose **mechanism lesson landed yesterday** (`the-cooling-plant-and-water`, Phase 4 row 18, v06.26r), which makes the §10.6 split the cleanest it will ever be; and it sits at **chain position 8**, immediately after the seven built supply segments. **If the developer's order says otherwise, take theirs and say so** — everything below except the roster numbers survives a swap.

**UNSHALLOW FIRST — a Session Start Checklist item, not a brief's favour.** `git fetch --unshallow origin main` before any pin read, any `--check` run, any `git log -1 --format=%cs` date and any CHANGELOG rotation. **And if a container restart has left the clone behind `origin/main`**, run `git fetch --prune origin` and `git checkout -B <branch> origin/main` BEFORE any measurement — a session that measures first reads every counter wrong.

**THE ROSTER IS THE SMALLEST SUPPLY SEGMENT S2 HAS MET AND ITS TWO PUREST MEMBERS ARE ALSO ITS MOST ISOLATED. MEASURED at v06.30r against the parsed registry.** Eleven members, **4 · 1 · 6** — four incumbents (`vertiv`, `schneider-electric`, `trane-technologies`, `coolit`), one challenger (`eaton`), six adjacent (`aligned`, `delta-electronics`, `flex`, `liteon`, `mitsubishi-electric`, `supermicro`). **Nine of the eleven are shared, across 22 memberships elsewhere, and 17 of those memberships invert this segment's role** — the highest raw inversion count any landscape has met, and it decomposes on a line (dd3) and (ee3) did not offer: **the two members that are pure cooling plays — `trane-technologies` and `coolit` — appear in NO other segment at all**, while every multi-segment member is an electrical or IT company that cools as one line of business. Test that reading before publishing it: it says the segment is **two markets wearing one name**, a thermal-equipment market and a power-and-IT market that has annexed the cold plate, and the `notes` field on `supermicro` argues the same boundary from the registry's own side (*"adjacent, not challenger"*, on a manufactured product line rather than a market position, with Dell'Oro naming Vertiv the liquid-cooling leader and not naming Supermicro at all).

**THIS IS THE CLEAR CASE, AND FOR ONCE BOTH OF THE SEGMENT'S DIFFERING SECTIONS ARE ALREADY EXPLAINED.** At v06.30r `--check` reads **7 due** and `cooling` is among them, differing in **`read-next` and `what-is-bought-and-on-what`** with `inputs moved: none` — and row 18's record says exactly why: it built `the-cooling-plant-and-water` and **all six** of this segment's `buyingCriteria` now resolve to a built title where they previously printed `(planned)`, the strongest deepening the programme has measured. So forecast **7 at the start, 7 after the module** (a module registration moves no `lesson_ref`) **and 6 at the end**, this segment clearing on its own regeneration. **Record all three numbers before you write them.**

**THE SPLIT IS AGAINST TWO PUBLIC LESSONS, BOTH IN THIS SEGMENT'S `READ_NEXT`, AND ONE OF THEM IS A DAY OLD.** `heat-is-the-constraint` (position 1 of `aidc-campus`) and `the-cooling-plant-and-water` (position 2, row 18) are the whole of `READ_NEXT`, and the lexicon maps the second onto **nine** keys — `cooling`, `liquid`, `pue`, `water`, `wue`, `chiller`, `cdu`, `refrigerant`, `approach temperature`. Read row 18's own record before drafting: it found §10.6 **(t) firing inverted** — `heat-is-the-constraint` had taken six things the spec read as row 18's, with no decline sentence anywhere — and row 19 then found a **third (t) class**, a *declared* handoff naming a sibling that did not carry the material. **Check for all three shapes**: a pre-declared handoff, an undeclared pre-emption, and a declaration that is simply wrong. The line to draw is the ordinary S2 one — **the two public lessons own the tests, this contributor module owns the named parties** — and §2.3's cooling cap means the module must not become the ninth section row 18 was forbidden.

**`reviewBy` — read, do not sort.** Row 18 took the six-month default **despite** four genuine future day-level dates, all twenty-seven months out or further, and enumerated five rejections; row 19 met the **empty set** inside a lesson's own material. A landscape sorts the members' `policyExposure[]`, so start there and expect refrigerant phase-down steps and a water- or moratorium-dated entry to be the live candidates — row 18 already rejected the EPA Technology Transitions chiller date (1 January 2030) and the Texas audit pause (in effect since 2026-08-03, no end date) **as a lesson's gate**, which does not bind a module's. **Record every rejection in writing.** The curriculum checker reports **3** items due for review — `landscape-utilities-2026-09` and `landscape-in-hall-power-2026-09` at 2026-10-01, `landscape-neoclouds-2026-09` at **2026-09-30** — all three by design; **do not "fix" any of the three.** `landscape-neoclouds-2026-09` passes on 30 September and will read overdue until a session revises that module: correct behaviour, not a defect.

**THE SCRAPER SEED IS A REAL GAP AND IT IS BIGGER THAN ONE SEED.** Grepped at v06.30r, counting **quoted** literals rather than loose matches (a loose grep returns eight because one seed is split across a line break and matches twice): `Scraper.gs` holds **seven** `topic-landscape-*` seeds against **fourteen** built landscapes, and **`topic-landscape-cooling` is not one of them**. Audit your candidate terms against **all** distinct terms in both arrays per **(bb5)** before adding any, record the denominator, and treat the **seven-seed** shortfall as an observation for the developer rather than a backlog to clear in one session — (ee6) found the roster has never held a construction seed and called every drop editorial, which is the same shape.

**Baselines to capture BEFORE any edit, all measured at the close of v06.30r.** `check-classroom-content.py` **0 errors / 0 warnings at 49 lessons, 8 tracks, 142 gate cases**, module assertion **23** — your commit registers the twenty-fourth module, so **the assertion must be edited to 24 in the same commit** or the checker stops you. `check-classroom-curriculum.py --strict`: no structural findings, **28** stale pins, **3** items due for review. `check-classroom-pipeline.py --selftest`: **13 fixtures / 0 failures**. `build-classroom-segments.py --check`: **7 due**, as listed above. **Watch the micro-markup check**: `PLAIN_BY_KIND` makes a `proscons` card's `t`/`meta`, a `timeline`'s `lanes` values and a `bars` item's `label`/`sub` plain text, and `PLAIN_DOC_FIELDS`/`PLAIN_DOC_LISTS` do the same for `title`, `short`, every `tiles[].k`/`v`/`sub`, every `glossary[].t`/`d` and a `revisions[].note` — **no `{{term}}`, no `**bold**`, no `*italic*` in any of them.** Row 19 ran that simulation locally before splicing and it caught three defects in one pass — a nested italic inside a bold, an unresolved `{{term}}`, and two glossary entries never used — **so run it before the splice, not after.**

**Versions and counters to bump from.** Classroom GAS **v01.60g**, Scraper **v02.15g** (an S2 session seeds, so Scraper **is** yours), Profiler v01.39g and never yours; page versions unchanged (Classroom v01.14w). **Repo version: read `repository-information/repository.version.txt` on the day and bump by 0.01** — do not take it from this brief, whose own handover commit consumes the next number.

**THE DEPLOY — the §7.19 order, on its twenty-eighth use.** Read **Pages** first (`https://lightaisolutions.github.io/Sales/gs-versions/Classroomgs.version.txt` and `.../Scrapergs.version.txt` are static files) and the workflow's own `deploy confirmed` lines from the job log; probe `?op=deploy` only if the two disagree, with `curl -sL`. **Counters MEASURED at v06.30r against that push's own job log, and the single-leg forecast was right: `Classroom deploy confirmed (GET): Updated to v01.60g (deployment 72) | 72/200`.** Exactly **one** step fired — Classroom in 26 s — and every other `Deploy <Project>` step, Scraper's included, exited 0 silently in zero seconds on its own `git diff` guard. **So Classroom stands at 72/200 with 128 of its allowance left, and Scraper at 160/200 with 40.** Pages read `|v01.59g|` at 12:03 AM EST before the push and `|v01.60g|` at 12:04 AM after, so the §7.19 order resolved on its **two reads** for the **twenty-seventh** time and no `?op=deploy` probe was made. An S2 session touches **two** `.gs` files, so forecast **two** steps and **two** logged lines. Counter rule unchanged: `Updated to` consumes one; a step that never fires consumes none and logs nothing; a leg that fires and does not confirm still consumes one and the tally does not see it; `Already up to date` is silent on consumption and must never be forecast as zero. **Add every intervening `.gs` push to your forecast, not just your own.**

**CHANGELOGS — THE TWELVE-SESSION DEFERRAL ENDED AT v06.30r AND THE FILES ARE NOW WELL INSIDE THEIR TRIGGERS.** Row 19's push landed on **2026-09-17 EST** and was therefore the rotator: `CHANGELOG.md` cleared **two** date groups (2026-09-08's 10 sections and 2026-09-09's 11) into `CHANGELOG-archive.md`, and `Classroomgs.changelog.md` cleared **two** (2026-09-01's 2 and 2026-09-02's 11) into its archive — the first rotation either archive has ever held, every header SHA-enriched, both post-rotation greps clean. **RE-COUNT FROM THE FILES rather than from this brief**, and note the standing arithmetic: the trigger is tested against the **non-exempt** count (total minus today's EST sections), so a same-day push can sit above the raw limit without rotating, and a push on the following day can owe **two** groups where the previous session's brief forecast one. Deepen the clone first so every rotated header enriches, and run the mandatory post-rotation grep (`grep '^## \[v' <archive> | grep -v '— \[' | head -5`).

**Ten open items recorded and not taken.** (i) The `.cl-tbl` `min-width` question still wants a session that edits `Classroom.html` — **twenty-nine** data points now, row 19's three-column six-row defect table the latest and clean at 1400 px. (ii) The generated `read-next` **names** the landscape module without linking to it. (iii) **`the-800-vdc-shift`'s `three-chains` intro is now twenty-three registrations stale.** (iv) §10.6's session-4 bullet block is still out of chronological order — cosmetic. (v) **A stale `lesson_ref` can make a segment due on its own** — now traced at **seven** segments, `cells-and-chemistry` added by row 19. (vi) The DC-arc taxonomy gap stays with §7 row 20. (vii) **`restart` scores zero across all Scraper terms** — a **sixth** session unseeded. (viii) The **second standing reservation debt**: session 12's six terms assigned to `topic-aidc-landlords` are in no seed and still score zero. (ix) `CRITERION_LEXICON` has no entry matching a criterion whose material an eligible built lesson already teaches (§10.6 **(ee4)**) — a *generator* gap wanting a developer decision. (x) **NEW at v06.30r — two findings from row 19 that are the developer's, not a session's**: §10.6 **(ff4)**, that **160 of the 240 quiz items authored before row 19 put the correct answer at index 0** and `Classroom.html` does not shuffle, which is a retention defect fixable only at the cost of resetting every affected item's content hash; and §10.6 **(ff5)**, that a gated lesson's **title** is withheld by the track view and published by the generator's `read-next` in two shipped public lessons. Neither was changed.

> Author S2 session 15 — the `cooling` landscape module — on Opus 5 xhigh as a fresh session. This is ONE guidance module. It authors no mechanism lesson, no dossier and no report, and it creates no track.
>
> READ FIRST: repository-information/SESSION-CONTEXT.md (Latest Session); INTEGRATED-REMEDIATION-PLAN.md §7.45 (your brief), §7.43 and §7.44 (for the S2 session signature, the below-the-fence rule, and what rows 18 and 19 settled about (t) firing inverted and then firing on a DECLARED handoff that named the wrong sibling); §7.3 orders 5 and 6, and §6. Then CLASSROOM-CURRICULUM-PLAN.md §10.6 IN FULL (your spec — the nine section ids, their order, the claims ledger, the admin extras) plus §10.1–§10.5 and §10.9, §2.3's cooling one-lesson cap, and §10.6's (t)/(w)/(bb1)/(dd3)/(ee3)/(ee5)/(ee6)/(ff1)–(ff6) findings. Then .claude/rules/industry-guidance.md, .claude/rules/classroom-app.md, repository-information/CLASSROOM-SCHEMA.md, and .claude/rules/changelog-security.md before you write any public changelog line.
>
> UNSHALLOW THE CLONE FIRST — git fetch --unshallow origin main, before any measurement and before any pin. If a container restart has left your clone behind origin/main, run git fetch --prune origin and git checkout -B <branch> origin/main BEFORE any measurement.
>
> **THE WORK — one push commit.** (1) Write `repository-information/industry-guidance/landscape-cooling-analysis.md`, the source of truth, with §2 (the split) drafted before any other section. (2) Author `guidanceDocLandscapeCooling_()` **BELOW** the `// CONTENT END` fence in `Classroom.gs`, id `landscape-cooling-2026-09`, lane The Value Chain, tier **contributor**, nine sections at §10.6's ids and order, and register it at the **END** of `guidanceDocs_()`'s Value Chain lane. (3) Seed `topic-landscape-cooling` in `Scraper.gs`. (4) Regenerate **exactly one** segment — `build-classroom-segments.py --segment cooling --today <EST day of the push>` (the flag is `--segment`, repeatable; §7.43's quoted block says `--only`, which does not exist — a correction, not a variant). (5) Flip §7.3's S2 row, §6's S2 row and §10.6's progress note to **15 of ~19**, strike `cooling` from the remaining list, and write the findings block — **the (ff…) label is spent; continue at (gg1)**. (6) Write §7.46 — the next run's brief, which by the interleave is **Phase 4 row 20, `dc-fault-engineering`** — in the same commit, and edit **BOTH halves** of any §7.x brief you refresh: the prose paragraphs and the quoted prompt block.
>
> WHICH SEGMENT, AND WHY IT NEEDS SAYING. The §7.3 sales-value order is not written out as a list anywhere. Five segments remain and only the last two are fixed — the v05.62r CHANGELOG record puts `assurance` and `insurance-and-risk-transfer` **18th and 19th of 19**. Positions 15–17 are unranked among `cooling`, `compute-and-the-rack` and `software-and-optimization`, and this brief takes `cooling` because it is the deepest of the three at **eleven members**, because its mechanism lesson landed one day earlier, and because it sits at chain position 8 right after the seven built supply segments. If the developer's order says otherwise, take theirs and say so.
>
> THE ROSTER, MEASURED AT v06.30r — PARSE IT AGAIN, DO NOT TAKE IT FROM THIS PROMPT. Eleven members, **4 · 1 · 6**: incumbents `vertiv`, `schneider-electric`, `trane-technologies`, `coolit`; challenger `eaton`; adjacent `aligned`, `delta-electronics`, `flex`, `liteon`, `mitsubishi-electric`, `supermicro`. **Nine of eleven are shared across 22 memberships elsewhere and 17 of those invert this segment's role** — the highest raw inversion count S2 has met. It decomposes on a new line: **the only two pure cooling plays, `trane-technologies` and `coolit`, appear in NO other segment**, while every multi-segment member is an electrical or IT company that cools as one line of business. The reading to test is that the segment is two markets under one name. The registry's own `notes` on `supermicro` argues the same boundary — adjacent on a manufactured product line rather than a market position, with Dell'Oro naming Vertiv the liquid-cooling leader and not naming Supermicro at all.
>
> PREDICT ALL THREE NUMBERS BEFORE YOU WRITE, AND THIS IS THE CLEAR CASE. `--check` should read **7 due** as you start with `cooling` among them, differing in `read-next` AND `what-is-bought-and-on-what`, `inputs moved: none`. Forecast **7 at the start, 7 after the module** (a module registration moves no `lesson_ref`) **and 6 at the end**, this segment clearing on its own regeneration. Record all three before you write them. Note that row 18 already cleared every `(planned)` from this segment's six criterion rows, so the `what-is-bought-and-on-what` difference is row 18's deepening waiting to be written down rather than a gap.
>
> §10.6 (t) HAS THREE SHAPES NOW AND THIS SEGMENT'S `READ_NEXT` HOLDS BOTH ITS NEIGHBOURS. `heat-is-the-constraint` and `the-cooling-plant-and-water` are the whole of `READ_NEXT` and both are **public**. Row 18 found (t) firing **inverted** — an undeclared pre-emption, `heat-is-the-constraint` having already taken six things the spec read as row 18's. Row 19 found a **third** shape — a *declared* handoff naming a sibling that carried none of the material. Check for all three before drafting, honour §2.3's cooling one-lesson cap (the module must not become the ninth section row 18 was forbidden), and draw the ordinary S2 line: **the two public lessons own the tests, this contributor module owns the named parties.**
>
> reviewBy — read, do not sort. Start from the members' `policyExposure[]`; refrigerant phase-down steps and a water or moratorium date are the live candidates. Row 18 rejected the EPA Technology Transitions chiller date (1 January 2030) and the Texas audit pause (in effect 2026-08-03, no end date) **as a lesson's gate**, which does not bind a module's — re-test both on their merits. Record every rejection in writing. The curriculum checker reports 3 items due for review — `landscape-utilities-2026-09` and `landscape-in-hall-power-2026-09` at 2026-10-01, `landscape-neoclouds-2026-09` at 2026-09-30 — all three by design. Do not "fix" any of the three.
>
> THE SCRAPER SEED IS A GAP LARGER THAN ONE SEED. `Scraper.gs` holds **seven** `topic-landscape-*` seeds against **fourteen** built landscapes and `topic-landscape-cooling` is not one of them — count the QUOTED literals, because a loose grep returns eight (one seed is split across a line break and matches twice). Audit your candidates against ALL distinct terms in both arrays per (bb5), record the denominator, and report the seven-seed shortfall to the developer rather than clearing it in one session.
>
> DO NOT: author or revise a mechanism lesson; edit a generated `segment-*` literal by hand (regenerate it); touch Profiler.gs or Profiler.html. And NEVER sweep a date across Classroom.gs with a blanket find-and-replace — it holds twenty-three independently dated guidance modules and session 9 corrupted a comment-deadline date inside `guidanceDocEo14420_()` that way; scope every replacement to the function you are authoring.
>
> VERIFY: `node --check` on a `.js` copy of `Classroom.gs` and of `Scraper.gs`; `scripts/check-gas-inner-scripts.js`; `scripts/check-classroom-content.py` — capture a pristine HEAD baseline BEFORE any edit. It is 0 errors / 0 warnings at **49 lessons / 8 tracks / 142 gate cases** with the module assertion at **23**, and your commit registers the twenty-fourth module, so **edit that assertion to 24 in the same commit** or the checker stops you. Then `scripts/check-classroom-curriculum.py --strict` (no structural findings; 28 stale pins and 3 items due for review are pre-existing and not yours); `scripts/check-classroom-pipeline.py --selftest` (13 fixtures, 0 failures) and `--base origin/main` — expect **P1** on the plan files, **P2** on the below-the-fence module, **no P3**, no P5, no P8, and **no P12** provided each VERSION moves exactly one step. `build-classroom-segments.py --check` before and after (7 → 7 → 6). `scripts/check-readme-tree.py` after the version bumps. Playwright render at **contributor** with ZERO page errors, plus an **analyst** denial from the real serving path. The module entrypoint is `clShowGuidanceDoc(id)` and section nodes are `#cl-<sectionId>`; `payload.action === 'guidance'` routes to `handleGuidanceOp_` and everything else to `handleClassroomOp_`; `_gasPost` returns a PROMISE, `sess.permissions` must be an ARRAY in the shim, the session parameter is `session` and must be at least 32 characters or the real guard returns SESSION_EXPIRED, the session keys are `Classroom_gas_session_token` / `_gas_user_email` / `_gas_user_role` / `_gas_user_permissions`, overlays to hide are `#auth-wall` / `#signing-out-wall` / `#auth-timers` / `#gas-layer-toggle` plus `#cl-app{display:block !important}`, `AUTO_REFRESH` is a `var AUTO_REFRESH = true;` declaration, and the scratch site root needs `favicon.ico` copied in beside `profiler-data/`, `sounds/` and `images/`. SERVE THE SCRATCH COPY AT `/Classroom.html`, NOT AT THE SITE ROOT. The page's CSP `connect-src` is `'self'` plus Google hosts, so a shim on another port is REFUSED: serve the static files and the `/__gas` route from the same Node process on one port, and call the handlers through the vm context object (`ctx.handleClassroomOp_`), not from Node's own scope. The Chromium binary is at `/opt/pw-browsers/chromium-1194/chrome-linux/chrome`. `ps -eo pid,args | grep -E "node .*shim"` is the pattern that works. Re-extract the PROJECT region after every edit and DEMAND that the rendered character count MOVE — row 19's two re-renders went 25,968 → 26,279 → 26,328. Write scripts to a file with a QUOTED heredoc delimiter, or bash interpolates backticks inside Python. **RUN THE MICRO-MARKUP SIMULATION BEFORE THE SPLICE, NOT AFTER** — row 19 did and it caught a nested italic inside a bold, an unresolved `{{term}}` and two unused glossary entries in one pass. Then READ EVERY SECTION'S SCREENSHOT — seven consecutive sessions have had a defect caught there and nowhere else. Prefer ASCII in `tiles[].k`; RECOUNT every number the prose asserts about a table it sits beside; and, new at row 19, **CHECK EVERY ENUMERATION THE PROSE PROMISES THE ADJACENT CONTENT WILL ANSWER** — row 19's intro told the reader to read three cards against four named questions and one card answered three. Bump Classroom's GAS version in BOTH `Classroom.gs` VERSION (from v01.60g) and `Classroomgs.version.txt`, and Scraper's in BOTH `Scraper.gs` and `Scrapergs.version.txt` (from v02.15g), with GENERIC entries in both GAS changelogs.
>
> THE DEPLOY: the §7.19 order, on its twenty-eighth use. Read Pages first (the `gs-versions/*.txt` files are static) and the workflow's own deploy confirmed lines from the job log. Probe `?op=deploy` only if those disagree, and use `curl -sL` if you do. Your session touches TWO `.gs` files, so forecast TWO steps and TWO logged lines. Counters MEASURED against v06.30r's own job log: `Classroom deploy confirmed (GET): Updated to v01.60g (deployment 72) | 72/200`, exactly one step firing in 26 s with every other Deploy step exiting 0 silently in zero seconds. **Classroom is at 72/200 with 128 left and Scraper at 160/200 with 40** — and add every intervening `.gs` push to your forecast, not just your own.
>
> THE CHANGELOG ARITHMETIC — THE DEFERRAL IS OVER AND BOTH FILES ARE INSIDE THEIR TRIGGERS. v06.30r rotated two date groups out of `CHANGELOG.md` (2026-09-08's 10, 2026-09-09's 11) and two out of `Classroomgs.changelog.md` (2026-09-01's 2, 2026-09-02's 11) — the first rotation either archive has held. RE-COUNT FROM THE FILES rather than from this prompt, run `TZ=America/New_York date '+%Y-%m-%d'` and trust it over the harness banner, and remember the trigger is tested against the NON-EXEMPT count (total minus today's EST sections). `Scrapergs.changelog.md` and `Profilerhtml.changelog.md` both sit near their own triggers — re-count both. Deepen the clone first so every rotated header enriches, and run the mandatory post-rotation grep (`grep '^## \[v' <archive> | grep -v '— \[' | head -5`).
>
> Normal Pre-Commit and Pre-Push checklists; claude/* branch restarted from origin/main first, git ls-remote before each push — the auto-merge workflow adds a [skip ci] SHA-tracker commit after every merge, and after it merges use `git fetch --prune origin` rather than `git fetch origin main`, or the stale remote-tracking ref makes the stop hook report that SHA-tracker commit as unpushed. End every commit message with the two attribution lines this repo's recent commits carry (Co-Authored-By: and Claude-Session:). Read `repository-information/repository.version.txt` for the version to bump from — do not take it from this prompt, whose own handover commit consumes the next number.
>
> AFTER THIS SESSION Phase 4 is 19 of 26 and S2 is 15 of ~19, so the interleave returns to Phase 4 and the recommended next run is **Phase 4 row 20 — `dc-fault-engineering`** — for which §7.46 should be written. Note that row 20 is a **guidance**-gated lesson stamped on `guidance:nvidia-800vdc-2026-08`, so it inherits that module's `reviewBy` of 2026-11-30 and is the first gated lesson Phase 4 has authored.
>
> Then, give me a prompt to paste into a new Opus 5 xhigh session to continue the action plan, then remember session.


### 7.46 · Paste-in brief for Phase 4 row 20 — `dc-fault-engineering` (Opus 5 xhigh) — written 2026-09-17 at v06.32r, at the close of S2 session 15

**Why this and not another S2 segment.** Phase 4 is **19 of 26** and S2 is now **15 of ~19**, so the interleave stands at 19:15 and §7.4 item 6 hands the turn back to Phase 4. Its next row is **20, `dc-fault-engineering`** — spec in `CLASSROOM-CURRICULUM-PLAN.md` §3, group The AI Data-Center Wave, title *Faults, Grounding, and the Interlock at 800 Volts*. It is the **fiftieth** lesson and goes at **position 10 of 11 in `aidc-grid-to-chip`**, between `the-800-vdc-shift` and `where-the-chain-breaks` per §4 — a **mid-array insert**, so expect **P5**.

**THIS IS THE FIRST GATED LESSON PHASE 4 HAS AUTHORED, AND THE GATE IS THE ROW'S DEFINING FACT.** Forty-nine lessons exist and every one folds to `tracks` — public. This one folds to **`guidance`** (contributor and admin only) because `guidance:nvidia-800vdc-2026-08` is in its stamp, and **the gate is the stamp's fold, never a field you set** (`CLASSROOM-SCHEMA.md`). Three consequences a public row never has to think about. **(1)** The **analyst render is now the denial test, not the content test**: an analyst asking for this track gets a lesson card count one short and `withheld` incremented, and the section payload must be empty. Verify both from the real serving path, as every S2 session verifies its module. **(2)** §10.6 **(ff5)** becomes live on a second lesson: the track view withholds a gated lesson's **title** while the generator's `read-next` publishes it, and `segment-power-conversion-and-rack-power-silicon` is **public** and will print this lesson's title the moment it is registered. That asymmetry is recorded and unchanged — **do not "fix" either surface** — but say in the record that this row put a second title on the permissive side of it. **(3)** The public sibling `the-800-vdc-shift` at position 9 is where the analyst stops, so the module-level thesis stays theirs and the engineering is yours.

**UNSHALLOW FIRST — a Session Start Checklist item, not a brief's favour.** `git fetch --unshallow origin main` before any pin read, any `--check` run, any `git log -1 --format=%cs` date and any CHANGELOG rotation. **And if a container restart has left the clone behind `origin/main`**, run `git fetch --prune origin` and `git checkout -B <branch> origin/main` BEFORE any measurement.

**TWO DECLARED HANDOFFS ARE ALREADY WAITING FOR THIS LESSON, AND (ff1) MAKES CHECKING THEM MANDATORY RATHER THAN POLITE.** `the-800-vdc-shift` (public, built) hands off in terms: *"The taxonomy of arc types and how each is detected and cleared belongs to the engineering lesson at the stricter gate."* And §5 row 11 plus `breakers-relays-and-faults` both **reserve the arc taxonomy for this row by name**, stating that the classification is currently held by nobody. So unlike every recent row, this one **inherits a debt rather than creating one** — and row 19's **(ff1)** is the reason that is not a formality: *a declared handoff is not self-maintaining*, and row 19 found one naming a sibling that carried none of the material. **Grep both declarations' own words against your draft before you ship it**: if the lesson you write does not actually carry the arc taxonomy and the detection-and-clearing story, the two declarations become (ff1) defects the same day. Conversely, S2 session 15 met a **fourth** (t) shape — a neighbour that is silent — so also check whether the two public siblings have quietly *taken* any of this row's material without saying so.

**THE DUE COUNT IS FULLY DERIVABLE AND IT WAS DERIVED, NOT GUESSED — RUN THE TEST YOURSELF ANYWAY.** Measured at v06.32r against the parsed generator: **`dc-fault-engineering` appears in NO `CRITERION_LEXICON` entry**, so §10.6 **(ff2)**'s part one fails and the `what-is-bought-and-on-what` half **cannot** fire whatever the segment looks like. And it sits in **exactly one** segment's `READ_NEXT` — `power-conversion-and-rack-power-silicon` — which is **already due and differing in `read-next` alone**. So registering this lesson changes that same section a second time and **the count HOLDS at 6 → 6**, with that segment still differing in `read-next` only. **Record both numbers before you run anything.** A Phase 4 row regenerates nothing, so the stale `lesson_ref` this leaves behind is open item **(v)** at an **eighth** segment — record it, do not fix it by regenerating.

**THE STAMP IS SIX INPUTS AND ONE OF THEM IS A GUIDANCE MODULE, WHICH IS WHAT SETS THE GATE AND THE CLOCK.** `guidance:nvidia-800vdc-2026-08` (the four deployment architectures, AC against DC fault behaviour, the grounding schemes, the protection zones and interlock, SSCBs, and what the paper does not say) · `study:zhonhen` (DC safety literacy) · `study:abb` (solid-state breakers; why DC has no natural moment to die) · `study:powell-industries` (`arc-flash`, `how-a-protection-study-is-built`) · `study:infineon` (`four-switches-one-table`, for the SSCB row) · `concepts:profiler-concepts` (vocabulary). **All four study guides were verified present at v06.32r.** §8 item 2 is the rule that bites hardest here: a `study:` ref pins the guide's own **`lastUpdated` read off the fetched file**, and rows 18 and 19 found six-of-twelve and **ten-of-ten** guides whose `lastUpdated` differs from their `git log` date. **Re-read all six on the day; a session that takes the commit date writes wrong pins no checker can see.** The `guidance:` ref pins that module's own `updated`.

**`reviewBy` — THIS ROW INHERITS RATHER THAN READS, AND THAT IS THE SPEC, NOT A SHORTCUT.** §3 fixes it: the lesson is contributor-only and **inherits `nvidia-800vdc-2026-08`'s `reviewBy` of 2026-11-30**. That is the first Phase 4 row whose clock is decided before the session starts, so the work is to **confirm the module still carries that date on the day** and to record the confirmation — not to sort a fence. If its own material turns up a **nearer** dated gate, §8 item 3 still applies and the nearer gate wins; say so in writing either way. Note what this does to the checker: 2026-11-30 sits **outside** the 30-day horizon, so the count stays at whatever the modules make it. **S2 session 15 deliberately took it to 4** (`landscape-cooling-2026-09` at **2026-09-28**, the earliest `reviewBy` in the corpus, alongside `landscape-neoclouds-2026-09` at 2026-09-30 and `landscape-utilities-2026-09` / `landscape-in-hall-power-2026-09` at 2026-10-01). **All four are by design — do not "fix" any of them**, and expect `landscape-cooling-2026-09` to read overdue from 29 September until a session revises it.

**Baselines to capture BEFORE any edit, all measured at the close of v06.32r.** `check-classroom-content.py` **0 errors / 0 warnings at 49 lessons, 8 tracks, 142 gate cases**, module assertion **24** — your commit takes the lesson count to **50** and leaves the module assertion at 24. `check-classroom-curriculum.py --strict`: no structural findings, **28** stale pins, **4** items due for review. `check-classroom-pipeline.py --selftest`: **13 fixtures / 0 failures**. `build-classroom-segments.py --check`: **6 due** — `cells-and-chemistry`, `power-conversion-and-rack-power-silicon`, `compute-and-the-rack`, `storage-developers-and-ipps`, `software-and-optimization`, `insurance-and-risk-transfer`. **Watch the micro-markup check**: `PLAIN_BY_KIND` makes a `proscons` card's `t`/`meta`, a `timeline`'s `lanes` values and a **`bars` item's `label`/`sub`** plain text, and `PLAIN_DOC_FIELDS`/`PLAIN_DOC_LISTS` do the same for `title`, `short`, every `tiles[].k`/`v`/`sub`, every `glossary[].t`/`d` and a `revisions[].note` — **no `{{term}}`, no `**bold**`, no `*italic*` in any of them.** §3's outline puts `four-deployment-options` at kind **`proscons`** and `sscb-versus-mccb` at kind **`bars`**, so **both** plain-field families bite on this row, which no recent lesson has had. **Run the simulation before the splice, not after** — S2 session 15 did and it caught an unused glossary entry in one pass.

**Versions and counters to bump from.** Classroom GAS **v01.61g** (a lesson lives in `Classroom.gs`); Scraper **v02.16g** and **not yours** — a Phase 4 row adds no seed; Profiler v01.39g and never yours; page versions unchanged (Classroom v01.14w). **Repo version: read `repository-information/repository.version.txt` on the day and bump by 0.01** — do not take it from this brief, whose own handover commit consumes the next number.

**THE DEPLOY — the §7.19 order, on its twenty-ninth use.** Read **Pages** first (`https://lightaisolutions.github.io/Sales/gs-versions/Classroomgs.version.txt` and `.../Scrapergs.version.txt` are static files) and the workflow's own `deploy confirmed` lines from the job log; probe `?op=deploy` only if the two disagree, with `curl -sL`. **A Phase 4 row touches ONE `.gs` file, so forecast ONE step and ONE logged line.** **Counters MEASURED at v06.32r against that push's own job log, and the two-leg forecast was right on both legs**: `Scraper deploy confirmed (GET): Updated to v02.16g (deployment 161) | 161/200` in 10 s and `Classroom deploy confirmed (GET): Updated to v01.61g (deployment 73) | 73/200` in 17 s — exactly **two** `Deploy <Project>` steps fired, every other one exiting 0 silently in zero seconds on its own `git diff` guard, and the run closed on *All GAS deploys confirmed the merged version*. **So Classroom stands at 73/200 with 127 of its allowance left, and Scraper at 161/200 with 39.** Pages read `|v01.60g|` / `|v02.15g|` at 01:08:09 AM EST before the push and `|v01.61g|` / `|v02.16g|` at 01:09:31 AM after, so the §7.19 order resolved on its **two reads** for the **twenty-eighth** time and no `?op=deploy` probe was made. Counter rule unchanged: `Updated to` consumes one; a step that never fires consumes none and logs nothing; a leg that fires and does not confirm still consumes one and the tally does not see it; `Already up to date` is silent on consumption and must never be forecast as zero. **Add every intervening `.gs` push to your forecast, not just your own.**

**CHANGELOGS — NO ROTATION IS DUE AND THE ARITHMETIC IS WHY, BUT RE-COUNT FROM THE FILES.** v06.30r ended the twelve-session deferral and cleared two date groups from each of `CHANGELOG.md` and `Classroomgs.changelog.md`, which put every file well inside its trigger. Re-counted at v06.32r: `CHANGELOG.md` **100 raw / 97 non-exempt** against a 100 trigger with 3 sections dated 2026-09-17 EST; `Classroomgs.changelog.md` **48 / 46** against a cap of 50; `Scrapergs.changelog.md` **37 / 36**; `Profilerhtml.changelog.md` **49**, closest to its own trigger and untouched by a Phase 4 row. **The trigger is tested against the NON-EXEMPT count**, so a same-day push can sit at the raw limit without rotating. Run `TZ=America/New_York date '+%Y-%m-%d'` and trust it over the harness banner, which follows UTC and flips five hours early. Deepen the clone first so any rotated header enriches, and if you do rotate, **dry-run into a copy and read the seams** — v06.30r's dry run caught three bugs (date groups landing oldest-first in a reverse-chronological archive, a lost blank line at the seam, and the source file's trailing `Developed by:` footer dragged into the archive).

**Twelve open items recorded and not taken.** (i) The `.cl-tbl` `min-width` question still wants a session that edits `Classroom.html` — **thirty** data points now, S2 session 15's eleven-row three-column bet table the latest and clean at 1400 px. (ii) The generated `read-next` **names** the landscape module without linking to it. (iii) **`the-800-vdc-shift`'s `three-chains` intro is now twenty-four registrations stale** — and it is the sibling directly above this row, so the session that writes row 20 is the natural one to fix it if the developer wants it fixed. (iv) §10.6's session-4 bullet block is still out of chronological order — cosmetic. (v) **A stale `lesson_ref` can make a segment due on its own** — seven segments so far, and this row makes an eighth. (vi) The DC-arc taxonomy gap **is this row's to close** — it stops being an open item the day row 20 ships, provided the draft actually carries it. (vii) **`restart` scores zero across all Scraper terms** for a **seventh** session. (viii) Session 12's six `topic-aidc-landlords` terms are still in no seed for a **third** session. (ix) `CRITERION_LEXICON` has no entry matching a criterion an eligible built lesson already teaches (§10.6 **(ee4)**) — a generator gap wanting a developer decision. (x) §10.6 **(ff4)**: **160 of the 240 quiz items authored before row 19 put the correct answer at index 0** and `Classroom.html` does not shuffle; fixing it resets every affected item's content hash. (xi) §10.6 **(ff5)**: a gated lesson's **title** is withheld by the track view and published by the generator's `read-next` — **and this row adds a second lesson to that asymmetry**. (xii) **`Scraper.gs` holds EIGHT `topic-landscape-*` seeds against FIFTEEN built landscapes** (counted as quoted `key:` literals; a loose grep returns more), so seven S2 sessions seeded nothing and an S2 session may only add its own — the backlog needs a developer pass.

> Author Phase 4 row 20 — the `dc-fault-engineering` lesson — on Opus 5 xhigh as a fresh session. This is ONE mechanism lesson. It authors no guidance module, no dossier and no report, it creates no track, and it regenerates no segment.
>
> READ FIRST: repository-information/SESSION-CONTEXT.md (Latest Session); INTEGRATED-REMEDIATION-PLAN.md §7.46 (your brief), §7.44 and §7.45 (for the Phase 4 row signature, the inside-the-fence rule, and what S2 session 15 settled about a brief's hypothesis being REJECTED by the graph and about §10.6 (t) firing in a FOURTH shape); §7.3 orders 5 and 6, and §6. Then CLASSROOM-CURRICULUM-PLAN.md §3's `dc-fault-engineering` entry IN FULL (your spec — the eight section ids, their order, the six inputs and the gate), §4's `aidc-grid-to-chip` table (your position), §5, §7 row 20, §8 items 2, 3 and 9, §2.2 and §2.3. Then .claude/rules/classroom-app.md, repository-information/CLASSROOM-SCHEMA.md, repository-information/CLASSROOM-COMMITTER-CONTRACT.md, and .claude/rules/changelog-security.md before you write any public changelog line.
>
> UNSHALLOW THE CLONE FIRST — git fetch --unshallow origin main, before any measurement and before any pin. If a container restart has left your clone behind origin/main, run git fetch --prune origin and git checkout -B <branch> origin/main BEFORE any measurement.
>
> **THE WORK — one push commit.** (1) Author `clLessonDcFaultEngineering_()` **INSIDE** the `// CONTENT START` … `// CONTENT END` fence in `Classroom.gs`, id `dc-fault-engineering`, title *Faults, Grounding, and the Interlock at 800 Volts*, group The AI Data-Center Wave, gate **`guidance`**, eight sections at §3's ids and order: `four-deployment-options` (proscons) · `ac-versus-dc-faults` (table) · `grounding` (table) · `zones-and-the-interlock` (prose) · `sscb-versus-mccb` (bars) · `what-the-paper-does-not-say` (callout) · `drill` · `check-yourself`. (2) Register at the END of `clLessons_()` and **MID-ARRAY INSERT at position 10 of `clTrackAidcGridToChip_()`**, between `the-800-vdc-shift` and `where-the-chain-breaks` per §4 — expect **P5**. (3) Flip §7.3's Phase 4 row and §6's Phase 4 row to **20 of 26**, mark curriculum §7 row 20 built with the full record, update §4's table at position 10, and write the findings block — **the (gg…) label is spent; continue at (hh1)**. (4) Write §7.47 — the next run's brief, which by the interleave is **S2 session 16** — in the same commit, and edit **BOTH halves** of any §7.x brief you refresh.
>
> THIS IS THE FIRST GATED LESSON PHASE 4 HAS AUTHORED AND THE GATE IS THE ROW'S DEFINING FACT. All forty-nine existing lessons fold to `tracks`. This one folds to `guidance` because `guidance:nvidia-800vdc-2026-08` is in its stamp — **the gate is the stamp's fold, never a field you set**. So the analyst render becomes the DENIAL test: an analyst asking for `aidc-grid-to-chip` must get one fewer lesson card and `withheld` incremented, with an empty section payload. Verify from the real serving path. And §10.6 **(ff5)** goes live on a second lesson: the track view withholds a gated lesson's title while the generator's `read-next` publishes it, and `segment-power-conversion-and-rack-power-silicon` is PUBLIC and will print this lesson's title. That asymmetry is recorded and unchanged — do not "fix" either surface — but say that this row put a second title on the permissive side.
>
> TWO DECLARED HANDOFFS ARE ALREADY WAITING FOR THIS LESSON, AND (ff1) MAKES CHECKING THEM MANDATORY. `the-800-vdc-shift` states that *"the taxonomy of arc types and how each is detected and cleared belongs to the engineering lesson at the stricter gate"*, and §5 row 11 plus `breakers-relays-and-faults` both reserve the arc taxonomy for this row by name, saying the classification is held by nobody. **This row inherits a debt rather than creating one.** Row 19's (ff1) found a declared handoff naming a sibling that carried none of the material, so **grep both declarations' own words against your draft before you ship**: if the lesson does not actually carry the arc taxonomy and the detection-and-clearing story, those two declarations become (ff1) defects the same day. Also check the reverse, because S2 session 15 met a FOURTH (t) shape — a neighbour that is SILENT, taking nothing and declaring nothing while standing on the material.
>
> PREDICT BOTH NUMBERS BEFORE YOU WRITE, AND THIS ONE IS FULLY DERIVABLE. Measured at v06.32r: **`dc-fault-engineering` appears in NO `CRITERION_LEXICON` entry**, so (ff2)'s part one fails and the `what-is-bought-and-on-what` half CANNOT fire; and it sits in **exactly one** segment's `READ_NEXT` — `power-conversion-and-rack-power-silicon` — which is ALREADY due and differing in `read-next` alone. So the count **HOLDS at 6 → 6**. Run the lexicon test yourself rather than taking it from this prompt, and record both numbers before the run. The stale `lesson_ref` this leaves behind is open item (v) at an EIGHTH segment — record it, do not regenerate.
>
> THE STAMP IS SIX INPUTS: `guidance:nvidia-800vdc-2026-08` · `study:zhonhen` · `study:abb` · `study:powell-industries` · `study:infineon` · `concepts:profiler-concepts`. All four study guides were verified present at v06.32r. **§8 item 2 bites hardest here**: a `study:` ref pins the guide's own `lastUpdated` READ OFF THE FETCHED FILE, and rows 18 and 19 found six-of-twelve and TEN-OF-TEN guides whose `lastUpdated` differs from their `git log` date. Re-read all six on the day. The `guidance:` ref pins that module's own `updated`.
>
> reviewBy — THIS ROW INHERITS RATHER THAN READS. §3 fixes it: contributor-only, inheriting `nvidia-800vdc-2026-08`'s **2026-11-30**. Confirm the module still carries that date on the day and record the confirmation; if the lesson's own material turns up a NEARER dated gate, §8 item 3 still applies and the nearer gate wins — say so in writing either way. The curriculum checker reports **4** items due for review: `landscape-cooling-2026-09` at **2026-09-28** (the earliest reviewBy in the corpus, taken deliberately inside its own horizon), `landscape-neoclouds-2026-09` at 2026-09-30, and `landscape-utilities-2026-09` and `landscape-in-hall-power-2026-09` at 2026-10-01. **All four are by design — do not "fix" any**, and expect the first to read overdue from 29 September.
>
> DO NOT: author or revise a guidance module; add a Scraper seed (a Phase 4 row adds none); regenerate any segment; edit a generated `segment-*` literal by hand; touch Profiler.gs or Profiler.html. And NEVER sweep a date across Classroom.gs with a blanket find-and-replace — it holds twenty-four independently dated guidance modules and session 9 corrupted a comment-deadline date inside `guidanceDocEo14420_()` that way; scope every replacement to the function you are authoring.
>
> VERIFY: `node --check` on a `.js` copy of `Classroom.gs`; `scripts/check-gas-inner-scripts.js`; `scripts/check-classroom-content.py` — capture a pristine HEAD baseline BEFORE any edit. It is 0 errors / 0 warnings at **49 lessons / 8 tracks / 142 gate cases** with the module assertion at **24**, and must be 0/0 after at **50 lessons** with the assertion still 24. Then `scripts/check-classroom-curriculum.py --strict` (no structural findings; 28 stale pins and 4 items due for review are pre-existing and not yours); `scripts/check-classroom-pipeline.py --selftest` (13 fixtures, 0 failures) and `--base origin/main` — expect **P1** on the plan files, **P5** on the mid-array insert, and watch for **P3**, because a gated lesson is exactly the kind of write that moves `gateDigest`: if P3 reports, refresh the digest in the same commit. `build-classroom-segments.py --check` before and after (6 → 6). `scripts/check-readme-tree.py` after the version bump. **Playwright render at contributor with ZERO page errors, AND an analyst denial that is this row's real test** — the track view must return one fewer card with `withheld` incremented. The lesson entrypoint is `clShowLesson(id)` and section nodes are `#cl-<sectionId>`; `payload.action === 'guidance'` routes to `handleGuidanceOp_` and everything else to `handleClassroomOp_`; `_gasPost` returns a PROMISE, `sess.permissions` must be an ARRAY in the shim, the session parameter is named `session` and must be at least 32 characters or the real guard returns SESSION_EXPIRED, the session keys are `Classroom_gas_session_token` / `_gas_user_email` / `_gas_user_role` / `_gas_user_permissions`, overlays to hide are `#auth-wall` / `#signing-out-wall` / `#auth-timers` / `#gas-layer-toggle` plus `#cl-app{display:block !important}`, `AUTO_REFRESH` is a `var AUTO_REFRESH = true;` declaration, and the scratch site root needs `favicon.ico` copied in beside `profiler-data/`, `sounds/` and `images/`. SERVE THE SCRATCH COPY AT `/Classroom.html`, NOT AT THE SITE ROOT. The page's CSP `connect-src` is `'self'` plus Google hosts, so a shim on another port is REFUSED: serve the static files and the `/__gas` route from the same Node process on one port, and call the handlers through the vm context object (`ctx.handleClassroomOp_`), not from Node's own scope. The Chromium binary is at `/opt/pw-browsers/chromium-1194/chrome-linux/chrome`; `pip install playwright` first, and do NOT run `playwright install`. `ps -eo pid,args | grep -E "node .*shim"` is the pattern that works. Re-extract the PROJECT region after every edit and DEMAND that both the rendered character count AND the PROJECT byte count MOVE — S2 session 15's re-render went 49,973 → 50,330 characters on a region that went 3,459,343 → 3,459,716 bytes. Write scripts to a file with a QUOTED heredoc delimiter, or bash interpolates backticks inside Python. **RUN THE MICRO-MARKUP SIMULATION BEFORE THE SPLICE** — this row has BOTH plain-field families in play (`proscons` cards' `t`/`meta` AND a `bars` item's `label`/`sub`), which no recent lesson has had. Then READ EVERY SECTION'S SCREENSHOT — eight consecutive sessions have had a defect caught there and nowhere else, and S2 session 15's four were all the recount-and-promise classes: a count contradicted by the adjacent table, a table intro promising an ordering the rows did not use, a claim contradicted by the same module two sections earlier, and a ROLE named wrongly against the module's own roster. Prefer ASCII in `tiles[].k`; RECOUNT every number the prose asserts about a table it sits beside; CHECK EVERY ENUMERATION the prose promises the adjacent content will answer; and vary the quiz answer index per (ff4). Bump Classroom's GAS version in BOTH `Classroom.gs` VERSION (from v01.61g) and `Classroomgs.version.txt`, with a GENERIC entry in the Classroom GAS changelog. **Scraper is NOT yours on this row.**
>
> THE DEPLOY: the §7.19 order, on its twenty-ninth use. Read Pages first (the `gs-versions/*.txt` files are static) and the workflow's own deploy confirmed lines from the job log. Probe `?op=deploy` only if those disagree, and use `curl -sL` if you do. Your session touches ONE `.gs` file, so forecast ONE step and ONE logged line. Counters MEASURED against v06.32r's own job log, where a two-leg forecast was right on both legs: `Scraper deploy confirmed (GET): Updated to v02.16g (deployment 161) | 161/200` and `Classroom deploy confirmed (GET): Updated to v01.61g (deployment 73) | 73/200`. **Classroom is at 73/200 with 127 left and Scraper at 161/200 with 39** — and add every intervening `.gs` push to your forecast, not just your own.
>
> THE CHANGELOG ARITHMETIC — NO ROTATION IS DUE, AND RE-COUNT FROM THE FILES ANYWAY. v06.30r ended the twelve-session deferral and both files are now well inside their triggers. Re-counted at v06.32r: `CHANGELOG.md` 100 raw / 97 non-exempt against a 100 trigger with 3 sections dated 2026-09-17 EST; `Classroomgs.changelog.md` 48 / 46 against a cap of 50; `Scrapergs.changelog.md` 37 / 36; `Profilerhtml.changelog.md` 49 and untouched by a Phase 4 row. Run `TZ=America/New_York date '+%Y-%m-%d'` and trust it over the harness banner, and remember the trigger is tested against the NON-EXEMPT count. If you do rotate, deepen the clone first so every header enriches, dry-run into a copy and READ THE SEAMS, and run the mandatory post-rotation grep (`grep '^## \[v' <archive> | grep -v '— \[' | head -5`).
>
> Normal Pre-Commit and Pre-Push checklists; claude/* branch restarted from origin/main first, git ls-remote before each push — the auto-merge workflow adds a [skip ci] SHA-tracker commit after every merge, and after it merges use `git fetch --prune origin` rather than `git fetch origin main`, or the stale remote-tracking ref makes the stop hook report that SHA-tracker commit as unpushed. End every commit message with the two attribution lines this repo's recent commits carry (Co-Authored-By: and Claude-Session:). Read `repository-information/repository.version.txt` for the version to bump from — do not take it from this prompt, whose own handover commit consumes the next number.
>
> AFTER THIS SESSION Phase 4 is 20 of 26 and S2 is 15 of ~19, so the interleave returns to S2 and the recommended next run is **S2 session 16** — for which §7.47 should be written. Note that only positions 18 and 19 of the sales-value order are on the record (`assurance` and `insurance-and-risk-transfer`), so §7.47 must argue its choice among `compute-and-the-rack` and `software-and-optimization` and say to take the developer's order if one exists.
>
> Then, give me a prompt to paste into a new Opus 5 xhigh session to continue the action plan, then remember session.

### 7.47 · Paste-in brief for S2 session 16 — `compute-and-the-rack` (Opus 5 xhigh) — written 2026-09-17 at v06.34r, at the close of Phase 4 row 20

**Which segment, and why this one rather than the other candidate — because the order is NOT settled and the choice had to be argued.** Phase 4 is now **20 of 26** and S2 is **15 of ~19**, so the interleave stands at 20:15 and §7.4 item 6 hands the turn back to S2. Four segments remain — `compute-and-the-rack`, `software-and-optimization`, `insurance-and-risk-transfer`, `assurance` — and **only the last two of the four are settled in the sales-value order**; §7.3's strike list happens to print `compute, software` in that order, but nothing measured stands behind the pair. So this brief argues it. **Take `compute-and-the-rack`, and if the developer has stated an order, take the developer's.**

**The argument, measured from `profiler-segments.json` on 2026-09-17.** `compute-and-the-rack` holds **four members — NVIDIA incumbent, AMD and Supermicro challengers, Flex adjacent** — which is *exactly* the floor and **the smallest roster S2 will have covered** (session 15's `cooling` at eleven was the smallest so far). `software-and-optimization` holds **twenty — 2 incumbents, 3 challengers and FIFTEEN adjacents**, the most adjacent-heavy roster in the taxonomy at 75 %. Three reasons take `compute-and-the-rack` first. **(1)** It is the last **supply-side** segment S2 has not written; the three behind it — software, insurance, assurance — are all service and financial layers, so covering it **closes the physical chain** before S2 leaves it. **(2)** Its registry note already carries the module's sharpest finding rather than a blocker: *no dossier exists for Dell or HPE*, the two largest OEM competitors, and **no ODM is covered at all** (Foxconn, Quanta, Wiwynn) although ODM Direct is now above half the server market. A landscape that states that coverage boundary in its own opening is better guidance than one that defers — session 15's **(gg1)** established exactly that posture. **(3)** Both challenger typings were decided **on evidence and recorded**: AMD because independent estimates put Instinct at 5–10 % of accelerator revenue against NVIDIA's 75–92 %, Supermicro because IDC's series shows its share peaking at 8.0 % in 4Q23 and falling behind Dell's 16.5 % by 1Q26 with gross margin down 719 basis points in three years. That is a roster whose roles a module can *test* rather than inherit. **The counter-case, stated honestly:** `software-and-optimization` is the richer (ee1)/(dd2) subject — its note records that **no reachable third party ranks any player in storage bidding except Modo Energy**, whose 2025 US directory is vendor-written and does not quantify one of the two incumbents, while the one source that would settle it (Wood Mackenzie's *Global BESS software landscape 2026*) is paywalled. That is a ranking vacuum worth a module, and it will still be there next session. **Four members is the risk on this row** — a four-member roster gives a thin players table and few edges, and the brief for it should say so up front rather than discover it in section three.

**UNSHALLOW FIRST — a Session Start Checklist item, not a brief's favour.** `git fetch --unshallow origin main` before any pin read, any `--check` run, any `git log -1 --format=%cs` date and any CHANGELOG rotation. Row 20's container opened at **50 commits** and unshallowing took it to **1,327**. **And if a container restart has left the clone behind `origin/main`**, run `git fetch --prune origin` and `git checkout -B <branch> origin/main` BEFORE any measurement.

**THE MODULE ASSERTION MOVES ON THIS ROW AND IT IS THE ONE EDIT A SESSION FORGETS.** `check-classroom-content.py` hard-asserts the number of modules `guidanceDocs_()` registers — the line reads `expected 24` today. Your commit registers the **twenty-fifth** guidance module and the **sixteenth** landscape, so **edit that assertion to 25 in the same commit** or the checker fails on a write that is otherwise correct. Row 20 left it at 24 because a mechanism lesson registers no module.

**Baselines to capture BEFORE any edit, all measured at the close of v06.34r.** `check-classroom-content.py` **0 errors / 0 warnings at 50 lessons, 8 tracks, 142 gate cases**, module assertion **24**. `check-classroom-curriculum.py --strict`: **no structural findings**, **28** stale pins, **4** items due for review. `check-classroom-pipeline.py --selftest`: **13 fixtures / 0 failures**. `build-classroom-segments.py --check`: **6 due** — `cells-and-chemistry`, `power-conversion-and-rack-power-silicon`, `compute-and-the-rack`, `storage-developers-and-ipps`, `software-and-optimization`, `insurance-and-risk-transfer`. **Your segment is already in that set, differing in `read-next` AND `what-is-bought-and-on-what`**, so its own regeneration should clear both halves and take the count **6 → 5** — call it before you run it, per **(aa5)**.

**`reviewBy` — the sort, and the four that are due by design.** A landscape's gate comes from its members' `policyExposure[]`, sorted for the nearest future day-level date, with every rejection written down — **(bb4)**, **(dd5)** and session 15's **(gg6)** are the accumulated traps. The curriculum checker currently reports **4** items due for review: `landscape-cooling-2026-09` at **2026-09-28**, `landscape-neoclouds-2026-09` at 2026-09-30, and `landscape-utilities-2026-09` and `landscape-in-hall-power-2026-09` at 2026-10-01. **All four are by design — do not "fix" any**, and `landscape-cooling-2026-09` reads **overdue from 29 September** until a session revises it. If your own sort returns a date inside the horizon, that is a fifth by design and is fine; say so in writing.

**WHAT ROW 20 LEARNED THAT THIS ROW INHERITS — three findings, not decoration.** **(hh2)** is the one most likely to bite: a declared handoff can be **honoured in substance and false in its status clause on the same day**. Row 20 satisfied both declarations word for word and still had to correct two sentences, because they reported the destination's *state* (*"not yet built"*, *"held by nobody"*) rather than merely naming it. **Write every handoff as a pointer, never as a status report** — and when your module closes a reservation, grep the corpus for the reservation's own words. **(hh3)**: the reverse (t) sweep returned **empty**, and the same grep surfaced a vocabulary collision that became teaching material — `interlock` already meant four other things in this curriculum. **Run the reverse sweep for its collisions even when you expect the null result.** **(hh7)**: the literal was written **100 % ASCII**, which retires row 18's glyph-that-lies class by construction rather than by vigilance — the rendered DOM's only non-ASCII characters were the page's own `·`, `—` and back-arrow. **Do the same; it costs a module nothing.**

**Watch the micro-markup check.** `PLAIN_BY_KIND` makes a `proscons` card's `t`/`meta`, a `timeline`'s `lanes` values and a **`bars` item's `label`/`sub`** plain text, and `PLAIN_DOC_FIELDS`/`PLAIN_DOC_LISTS` do the same for `title`, `short`, every `tiles[].k`/`v`/`sub`, every `glossary[].t`/`d` and a `revisions[].note` — **no `{{term}}`, no `**bold**`, no `*italic*` in any of them.** Run the simulation **before** the splice, not after: row 20 did and its plain fields came back clean, and session 15's caught a glossary entry defined and never used. **Then read every section's screenshot** — **nine consecutive sessions** have had a defect caught there and nowhere else, and row 20's two were both claims the adjacent content refutes: a table `note` asserting a provenance the table contradicted, and a callout saying *"the section above"* about a section three earlier. **RECOUNT every number the prose asserts about a table beside it, and CHECK EVERY ENUMERATION the prose promises the adjacent content will answer.**

**Versions and counters to bump from.** Classroom GAS **v01.62g** (a landscape module lives in `Classroom.gs`, below the `// CONTENT END` fence); Scraper **v02.16g** — **yours on this row**, because an S2 session seeds its own topic. Profiler v01.39g and never yours; page versions unchanged (Classroom v01.14w). **Repo version: read `repository-information/repository.version.txt` on the day and bump by 0.01** — do not take it from this brief, whose own handover commit consumes the next number.

**THE DEPLOY — the §7.19 order, on its thirtieth use.** Read **Pages** first (`https://lightaisolutions.github.io/Sales/gs-versions/Classroomgs.version.txt` and `.../Scrapergs.version.txt` are static files) and the workflow's own `deploy confirmed` lines from the job log; probe `?op=deploy` only if the two disagree, with `curl -sL`. **An S2 session touches TWO `.gs` files — Classroom and Scraper — so forecast TWO steps and TWO logged lines.** **Counters MEASURED against v06.34r's own job log, where the one-leg forecast was right on its one leg:** `Classroom deploy confirmed (GET): Updated to v01.62g (deployment 74) | 74/200` in **18 seconds**, and **exactly one `Deploy <Project>` step fired** — every other one, Scraper included, exited 0 silently in zero seconds on its own `git diff` guard and logged nothing, the run closing on *All GAS deploys confirmed the merged version*. **So Classroom stands at 74/200 with 126 of its allowance left, and Scraper at 161/200 with 39.** Pages read `|v01.61g|` / `|v02.16g|` at 02:00:46 AM EST before the push and `|v01.62g|` / `|v02.16g|` at 02:04:29 AM after, so the §7.19 order resolved on its **two reads** for the **twenty-ninth** time and no `?op=deploy` probe was made. **Add every intervening `.gs` push to your forecast, not just your own.** Counter rule unchanged: `Updated to` consumes one; a step that never fires consumes none and logs nothing; a leg that fires and does not confirm still consumes one and the tally does not see it; `Already up to date` is silent on consumption and must never be forecast as zero. **Scraper's headroom is the tight one at roughly 39.**

**CHANGELOGS — NO ROTATION IS DUE, AND RE-COUNT FROM THE FILES ANYWAY.** Re-counted from the files at v06.35r, this brief's own handover push included: `CHANGELOG.md` **103 raw / 97 non-exempt** against a 100 trigger, with **6** sections dated 2026-09-17 EST; `Classroomgs.changelog.md` **49 / 46** against a cap of 50 with 3 same-day; `Scrapergs.changelog.md` **37 / 36**; `Profilerhtml.changelog.md` **49**, closest to its own trigger and untouched by an S2 row. **The trigger is tested against the NON-EXEMPT count**, so a same-day push can sit at or above the raw limit without rotating. Run `TZ=America/New_York date '+%Y-%m-%d'` and trust it over the harness banner, which follows UTC and flips five hours early — **a push on 18 September or later makes you the rotator on `CHANGELOG.md`**, because the six same-day sections stop being exempt and the non-exempt count jumps to 103. Deepen the clone first so any rotated header enriches, dry-run into a copy and **read the seams**, and run the mandatory post-rotation grep.

**Twelve open items recorded and not taken.** (i) The `.cl-tbl` `min-width` question — **thirty-one** data points now, row 20's six-row **five-column** fault table the latest and clean at 1400 px, equalling row 17's widest-by-column-count. (ii) The generated `read-next` **names** a landscape module without linking to it. (iii) **`the-800-vdc-shift`'s `three-chains` intro is now twenty-five registrations stale** — row 20 inserted *below* it, so the item did not get worse, and it remains the developer's call. (iv) §10.6's session-4 bullet block is out of chronological order — cosmetic. (v) **A stale `lesson_ref` can make a segment due on its own** — row 20 makes `power-conversion-and-rack-power-silicon` the **eighth**. (vi) ~~The DC-arc taxonomy gap~~ — **CLOSED by row 20 on 2026-09-17**; the taxonomy, its detection and its clearing are rows 2–5 and two columns of `ac-versus-dc-faults`. (vii) `restart` scores zero across all Scraper terms for an **eighth** session. (viii) Session 12's six `topic-aidc-landlords` terms are in no seed for a **fourth** session. (ix) `CRITERION_LEXICON` has no entry matching a criterion an eligible built lesson already teaches (**(ee4)**) — and row 20 measured a second face of it: the new lesson matches **none** of the 27 entries at all. (x) **(ff4)**: the quiz answer-index skew; row 20 used **2, 0, 3, 1, 2** and the corpus-wide fix still resets every affected item's content hash. (xi) **(ff5)/(hh6)**: a gated lesson's **title** is withheld by the track view and published by the generator's `read-next`, and row 20 put a **second** lesson on the permissive side — it is now a pattern, not an instance. (xii) **`Scraper.gs` holds EIGHT `topic-landscape-*` seeds against FIFTEEN built landscapes**, so seven S2 sessions seeded nothing and an S2 session may only add its own — the backlog needs a developer pass.

> Author S2 session 16 — the `compute-and-the-rack` landscape module — on Opus 5 xhigh as a fresh session. This is ONE guidance module. It authors no mechanism lesson, no dossier and no report, and it creates no track.
>
> READ FIRST: repository-information/SESSION-CONTEXT.md (Latest Session); INTEGRATED-REMEDIATION-PLAN.md §7.47 (your brief), §7.45 and §7.46 (for the S2 session signature, the below-the-fence rule, and what Phase 4 row 20 settled about a declared handoff being honoured in substance and false in its status clause, about a reverse (t) sweep returning EMPTY, and about writing a literal 100 % ASCII); §7.3 orders 5 and 6, and §6. Then CLASSROOM-CURRICULUM-PLAN.md §10 IN FULL — especially §10.3 (the generator spec), §10.5 (the lane), §10.6 (the landscape-module shape, its nine section ids and order, and the whole (a)–(hh7) findings list), §10.11 (where the taxonomy strained — `compute-and-the-rack` is named FIRST there and that note is your opening problem), §4, §7, §8 items 2, 3 and 9, and §2.2. Then .claude/rules/industry-guidance.md, .claude/rules/classroom-app.md, repository-information/CLASSROOM-SCHEMA.md, .claude/rules/scraper-sources.md, and .claude/rules/changelog-security.md before you write any public changelog line.
>
> UNSHALLOW THE CLONE FIRST — git fetch --unshallow origin main, before any measurement and before any pin. If a container restart has left your clone behind origin/main, run git fetch --prune origin and git checkout -B <branch> origin/main BEFORE any measurement.
>
> **THE WORK — one push commit.** (1) Write `repository-information/industry-guidance/landscape-compute-and-the-rack-analysis.md`, the source of truth, with the coverage-boundary section drafted BEFORE any other section — see the next paragraph for why. (2) Author `guidanceDocLandscapeComputeAndTheRack_()` **BELOW** the `// CONTENT END` fence in `Classroom.gs`, id `landscape-compute-and-the-rack-2026-09`, lane The Value Chain, tier contributor, nine sections at §10.6's ids and order, and register it at the **END** of `guidanceDocs_()`'s Value Chain lane. (3) **EDIT THE MODULE ASSERTION IN `scripts/check-classroom-content.py` FROM 24 TO 25 IN THE SAME COMMIT** — it is a hard assertion and your module is the twenty-fifth. (4) Seed `topic-landscape-compute-and-the-rack` in `Scraper.gs`, every term scored against both arrays first. (5) Regenerate **exactly one** segment — yours. (6) Flip §7.3's S2 row, §6's S2 row and §10.6's progress note to **16 of ~19**, strike `compute` from the remaining list, and write the findings block — **the (hh…) label is spent; continue at (ii1)**. (7) Write §7.48 — the next run's brief, which by the interleave is Phase 4 row 21 — in the same commit, and edit **BOTH halves** of any §7.x brief you refresh.
>
> FOUR MEMBERS IS THE SMALLEST ROSTER S2 HAS COVERED AND THE COVERAGE BOUNDARY IS THE MODULE'S OPENING PROBLEM, NOT A FOOTNOTE. The registry gives you **NVIDIA (incumbent), AMD and Supermicro (challengers), Flex (adjacent)** — exactly the floor and no more, against `cooling`'s eleven. The same registry note records what is missing and you must not discover it in section three: **no dossier exists for Dell or HPE**, the two largest OEM competitors, and **no ODM is covered at all** (Foxconn, Quanta, Wiwynn) although ODM Direct is now above half the server market. Session 15's **(gg1)** is the posture to copy — publish the measurement, including the one that is uncomfortable. Both challenger typings were decided on recorded evidence (AMD's Instinct at 5–10 % of accelerator revenue against NVIDIA's 75–92 %; Supermicro's share peaking at 8.0 % in 4Q23 and behind Dell's 16.5 % by 1Q26, gross margin down 719 bp in three years), so the roles are testable rather than inherited — **test them against the dossiers as they read on the day, and say so either way**.
>
> THE MODULE ASSERTION IS THE EDIT SESSIONS FORGET. `scripts/check-classroom-content.py` hard-asserts `guidanceDocs_() registers %d module(s), expected 24`. Your module is the twenty-fifth. Change it to 25 in the same commit or a correct write fails its own checker.
>
> PREDICT THE `--check` NUMBER BEFORE YOU WRITE. At v06.34r `--check` reads **6 due**: `cells-and-chemistry`, `power-conversion-and-rack-power-silicon`, `compute-and-the-rack`, `storage-developers-and-ipps`, `software-and-optimization`, `insurance-and-risk-transfer`. **Your own segment is in that set and differs in `read-next` AND `what-is-bought-and-on-what`**, so regenerating it should clear both halves and take the count to **5** — derive it from the parsed generator rather than assuming, and record the number before the run. **(aa5)** has held for nine sessions.
>
> reviewBy — SORT, DO NOT INVENT, AND WRITE EVERY REJECTION DOWN. The gate is the nearest future day-level date in the members' `policyExposure[]`, and **(bb4)**, **(dd5)** and **(gg6)** are the traps: a candidate that is already the generator's own date, a date that is on-subject and still unusable, a date that is future and off-subject. The curriculum checker reports **4** items due for review — `landscape-cooling-2026-09` at 2026-09-28 (**overdue from 29 September**), `landscape-neoclouds-2026-09` at 2026-09-30, `landscape-utilities-2026-09` and `landscape-in-hall-power-2026-09` at 2026-10-01. **All four are by design — do not "fix" any.**
>
> DO NOT: author or revise a mechanism lesson; edit a generated `segment-*` literal by hand; regenerate a segment that is not yours; touch Profiler.gs or Profiler.html. And NEVER sweep a date across Classroom.gs with a blanket find-and-replace — it holds twenty-four independently dated guidance modules and session 9 corrupted a comment-deadline date inside `guidanceDocEo14420_()` that way; scope every replacement to the function you are authoring.
>
> VERIFY: `node --check` on a `.js` copy of `Classroom.gs` and of `Scraper.gs`; `scripts/check-gas-inner-scripts.js`; `scripts/check-classroom-content.py` — capture a pristine HEAD baseline BEFORE any edit. It is 0 errors / 0 warnings at **50 lessons / 8 tracks / 142 gate cases** with the module assertion at **24**, and must be 0/0 after at 50 lessons with the assertion at **25**. Then `scripts/check-classroom-curriculum.py --strict` (no structural findings; 28 stale pins and 4 items due for review are pre-existing and not yours); `scripts/check-classroom-pipeline.py --selftest` (13 fixtures, 0 failures) and `--base origin/main` — expect **P1** on the plan and script files and **P2** on the below-the-fence module, and watch for **P3**, which must NOT appear. `build-classroom-segments.py --check` before and after (6 → 5). `scripts/check-readme-tree.py` after the version bumps. **Playwright render at contributor with ZERO page errors, and analyst denied at the server.** Module entrypoint is `clShowGuidanceDoc(id)` and section nodes are `#cl-<sectionId>`; `payload.action === 'guidance'` routes to `handleGuidanceOp_` and everything else to `handleClassroomOp_`; `_gasPost` returns a PROMISE, `sess.permissions` must be an ARRAY in the shim, the session parameter is named `session` and must be at least 32 characters or the real guard returns SESSION_EXPIRED, the session keys are `Classroom_gas_session_token` / `_gas_user_email` / `_gas_user_role` / `_gas_user_permissions` and **must be seeded AFTER the page loads, not through an init script — the page clears them on load and row 20 lost twenty minutes to that**. Overlays to hide are `#auth-wall` / `#signing-out-wall` / `#auth-timers` / `#gas-layer-toggle` plus `#cl-app{display:block !important}`; `AUTO_REFRESH` is a `var AUTO_REFRESH = true;` declaration; the scratch site root needs `favicon.ico` beside `profiler-data/`, `sounds/` and `images/`. **When you blank `var _e`, match the WHOLE literal with a regex (`var _e = '[^']*';`)** — row 20's hand-counted slice ate one quote too many and broke the page's only script block with a bare *"Unexpected string"*. SERVE THE SCRATCH COPY AT `/Classroom.html`, NOT AT THE SITE ROOT. The page's CSP `connect-src` is `'self'` plus Google hosts, so a shim on another port is REFUSED: serve the static files and the `/__gas` route from the same Node process on one port, and call the handlers through the vm context object (`ctx.handleGuidanceOp_`), not from Node's own scope. The Chromium binary is at `/opt/pw-browsers/chromium-1194/chrome-linux/chrome`; `pip install playwright` first, and do NOT run `playwright install`. `ps -eo pid,args | grep -E "node .*shim"` is the pattern that works. Re-extract the PROJECT region after every edit and DEMAND that both the rendered character count AND the PROJECT byte count MOVE — row 20's PROJECT region went **3,459,716 → 3,523,030 bytes** and its render moved 25,257 → 25,556 characters across the fix round. Write scripts to a file with a QUOTED heredoc delimiter, or bash interpolates backticks inside Python. **RUN THE MICRO-MARKUP SIMULATION BEFORE THE SPLICE**, and confirm every glossary entry you define is actually used. Then READ EVERY SECTION'S SCREENSHOT — **nine consecutive sessions** have had a defect caught there and nowhere else, and row 20's two were a table `note` claiming a provenance the table refuted and a callout saying *"the section above"* about a section three earlier. **Prefer pure ASCII for the whole literal** — row 20 wrote one and the glyph class could not fire. RECOUNT every number the prose asserts about a table beside it; CHECK EVERY ENUMERATION the prose promises; and vary the quiz answer index per **(ff4)**. Bump Classroom's GAS version in BOTH `Classroom.gs` VERSION (from v01.62g) and `Classroomgs.version.txt`, and Scraper's in BOTH (from v02.16g), each with a GENERIC entry in its GAS changelog.
>
> THE DEPLOY: the §7.19 order, on its thirtieth use. Read Pages first (the `gs-versions/*.txt` files are static) and the workflow's own deploy confirmed lines from the job log. Probe `?op=deploy` only if those disagree, and use curl -sL if you do. Your session touches TWO `.gs` files, so forecast TWO steps and TWO logged lines. Counters MEASURED against v06.34r's own job log: `Classroom deploy confirmed (GET): Updated to v01.62g (deployment 74) | 74/200` in 18 seconds, with exactly ONE Deploy step firing and every other one — Scraper included — exiting 0 silently in zero seconds on its own git diff guard. **Classroom stands at 74/200 with 126 left and Scraper at 161/200 with 39.** Add every intervening .gs push to your forecast, not just your own. Scraper's headroom is the tight one at roughly 39.
>
> THE CHANGELOG ARITHMETIC — NO ROTATION IS DUE ON 17 SEPTEMBER AND YOU MAY BE THE ROTATOR ON 18. Re-counted from the files at v06.35r, this brief's own handover push included: `CHANGELOG.md` 103 raw / **97 non-exempt** against a 100 trigger with **6** sections dated 2026-09-17 EST; `Classroomgs.changelog.md` 49 / 46 against a cap of 50; `Scrapergs.changelog.md` 37 / 36; `Profilerhtml.changelog.md` 49 and untouched by an S2 row. Run `TZ=America/New_York date '+%Y-%m-%d'` and trust it over the harness banner. **If it reads 2026-09-18 or later, those six sections stop being exempt, the non-exempt count becomes 103 and you ARE the rotator on `CHANGELOG.md`** — deepen the clone first so every header enriches, dry-run into a copy and READ THE SEAMS, and run the mandatory post-rotation grep (`grep '^## \[v' <archive> | grep -v '— \[' | head -5`).
>
> Normal Pre-Commit and Pre-Push checklists; claude/* branch restarted from origin/main first, git ls-remote before each push — the auto-merge workflow adds a [skip ci] SHA-tracker commit after every merge, and after it merges use `git fetch --prune origin` rather than `git fetch origin main`, or the stale remote-tracking ref makes the stop hook report that SHA-tracker commit as unpushed. End every commit message with the two attribution lines this repo's recent commits carry (Co-Authored-By: and Claude-Session:). Read `repository-information/repository.version.txt` for the version to bump from — do not take it from this prompt, whose own handover commit consumes the next number.
>
> AFTER THIS SESSION Phase 4 is 20 of 26 and S2 is 16 of ~19, so the interleave returns to Phase 4 and the recommended next run is **row 21, `utility-procurement-meets-ai-load`** — `market-access` position 9, guidance-gated, inheriting `guidance:utility-aidc-procurement-2026-08`'s reviewBy 2026-12-10, and the SECOND gated lesson Phase 4 will have authored. §7.48 should be written for it, and should carry forward what row 20 learned about the denial being the test.
>
> Then, give me a prompt to paste into a new Opus 5 xhigh session to continue the action plan, then remember session.

### 7.48 · Paste-in brief for Phase 4 row 21 — `utility-procurement-meets-ai-load` (Opus 5 xhigh) — written 2026-09-17 at v06.36r, at the close of S2 session 16

**Which row, and why there is nothing to argue this time.** Phase 4 is **20 of 26** and S2 is **16 of ~19**, so the interleave stands at 20:16 and §7.4 item 6 hands the turn back to Phase 4. Unlike §7.47, this one needs no argument: Phase 4 takes the next unbuilt row in the curriculum plan's §7 order and that is **row 21, `utility-procurement-meets-ai-load`** — `market-access` position **9**, the last row of that track, and the lesson §2.2's own 2026-09-07 reversal decided to keep guidance-gated when it promoted the machinery half into the public `how-a-utility-buys` (row 2, built v05.54r).

**IT IS THE SECOND GATED LESSON PHASE 4 WILL HAVE AUTHORED, AND ROW 20 ALREADY WROTE THE ACCEPTANCE TEST — reuse it rather than reinventing it.** Row 20's **(hh1)** is the finding that matters: every one of the forty-nine lessons before it folded to `tracks`, so every prior row's render was a *content* test; a `guidance:` stamp makes `clGateForProvenance_()` return `guidance` and **the denial becomes the test**. Row 20 measured base against head through the real `handleClassroomOp_` and got **9 cards / `withheld` 0 → 9 cards / `withheld` 1** on `aidc-grid-to-chip`, `cop=lesson` → `ROLE_DENIED`, **0 of 8 section nodes** in the analyst DOM, the index holding at 47 of 50, and the title never entering the analyst DOM. **Run the same measurement on `market-access`** — it already carries three gated lessons (`the-certification-stack`, `what-bankable-means`, `the-china-policy-stack`), so its analyst `withheld` count moves **3 → 4** rather than 0 → 1, which is a *different* arithmetic and worth calling before you measure it. And §8 item 10's standing check names `where-bess-plugs-in` and "the four guidance market lessons" — **this lesson is the fifth of them**, so update that sentence in the same commit if it still reads four.

**`reviewBy` IS INHERITED AND ROW 20 SHOWED HOW THAT GOES WRONG.** §7 row 21 fixes it at the module's **2026-12-10** (`guidance:utility-aidc-procurement-2026-08`'s own clock, shared with `power-infra-aidc-2026-08` and the built `where-bess-plugs-in`). Row 20's **(hh5)** is the precedent: it inherited, then **ran the sort anyway** and recorded that it returned an empty set, with two rejections in writing. Do the same — **re-read the module on the day to confirm it still carries 2026-12-10**, then sort the six utility guides' and the module's own dated material and write down what you rejected. Sharing a module's date is not an (r)/(u)/(bb4) violation when §7 prescribes it; sharing it *silently* is the defect.

**PREDICT THE `--check` NUMBER, AND THIS ROW'S IS DERIVABLE EXACTLY.** At v06.36r `--check` reads **5 due** — `cells-and-chemistry`, `power-conversion-and-rack-power-silicon`, `storage-developers-and-ipps`, `software-and-optimization`, `insurance-and-risk-transfer`. Parsed from the generator: `utility-procurement-meets-ai-load` sits in the `READ_NEXT` of **exactly two** segments, `hyperscalers-and-ai-labs` and `utilities`, and **neither is currently due**; its `PLANNED_TITLES` entry is *The Gatekeeper*, so both will stop rendering `(planned)` the moment it registers. It matches **none** of the 27 `CRITERION_LEXICON` entries, so **(ff2)**'s part-one test fails and no `what-is-bought-and-on-what` moves. **Forecast `5 → 7`, both new ones differing in `read-next` alone.** Record it before the run. **(aa5)** has now held for ten sessions.

**Baselines to capture BEFORE any edit, all measured at the close of v06.36r.** `check-classroom-content.py` **0 errors / 0 warnings at 50 lessons, 8 tracks, 142 gate cases**, module assertion **25** — **a mechanism lesson registers no module, so LEAVE IT AT 25**; row 20 correctly left it alone and S2 session 16 moved it 24 → 25. `check-classroom-curriculum.py --strict`: **no structural findings**, **28** stale pins, **4** items due for review — `landscape-cooling-2026-09` at 2026-09-28 (overdue from 29 September), `landscape-neoclouds-2026-09` at 2026-09-30, `landscape-utilities-2026-09` and `landscape-in-hall-power-2026-09` at 2026-10-01. **All four by design — do not "fix" any.** `check-classroom-pipeline.py --selftest`: **13 fixtures / 0 failures**. `node --check` on `.js` copies of both `.gs` files, and `check-gas-inner-scripts.js`: clean.

**THE SHAPE: eight sections, ids fixed by curriculum plan §3.5, inside the `// CONTENT START` fence.** `the-gatekeeper` (prose) · `the-four-moves` (table) · `five-channels` (**proscons** — storage RFPs, all-source RFPs, self-build supply agreements, ERCOT merchant, ride-through buffering) · `who-signs-the-po` (callout) · `ercot-versus-regulated` (table) · `the-dated-gates` (**timeline**) · `drill` · `check-yourself`. Registered at the **END** of `clLessons_()` and **appended** to `clTrackMarketAccess_()` — position 9 is the last of that track, so this is the rare Phase 4 row that appends rather than inserting mid-array, and **P5 should NOT fire**. If it does, you inserted somewhere you did not mean to. **Two section kinds this row uses that row 20 did not** — `proscons` and `timeline` — and both are in `PLAIN_BY_KIND`: a `proscons` card's `t`/`meta` and a `timeline`'s `lanes` values are printed as plain text, so **no `{{term}}`, no `**bold**`, no `*italic*` in any of them**. Timeline lane colours are the CVD-validated trio (gold `#b18f35`, blue `#4f83e6`, rose `#cc5f75`). **Run the micro-markup simulation BEFORE the splice.**

**THE SPLIT IS ALREADY DECIDED AND ITS OTHER HALF IS BUILT — so check it, do not re-derive it.** §2.2's reversal note enumerates, section by section, what `how-a-utility-buys` and the six utility guides now carry publicly: the gatekeeper and the monopoly bargain, IRP → CPCN → RFP, the rate case, the large-load tariffs compared, who pays, ERCOT against the regulated states, and the channels themselves. What it reserves for **this** lesson is the **seller's playbook**: the four moves, the buyer map across the five channels, the concrete AIDC-driven BESS awards, and the dated gates as one timeline. **Row 20's (hh2) is the trap to avoid here and this row is unusually exposed to it**: a declared handoff can be honoured in substance and false in its *status clause*, and §2.2's note was written in 2026-09-07 language ("today the whole utility-side story is guidance-only") that row 2 falsified at v05.54r. **Grep `how-a-utility-buys` and the six utility guides for the four reserved items before drafting**, and if any has been taken, say so and draw the line where the corpus actually is rather than where the note says it is. Run the **reverse (t) sweep** too, per **(hh3)** and **(ii2)** — expect a null result and read whatever collisions it returns.

**WHAT S2 SESSION 16 LEARNED THAT THIS ROW INHERITS — three findings, not decoration.** **(ii10)**: the screenshots caught **seven** defects for a tenth consecutive session and **five of the seven were the prose contradicting the content beside it** — a `sales` line promising three items and enumerating four, a table `intro` claiming a row count the spec does not give, a `sales` line asserting two dated rows when the table beside it had none, an unsupported fraction, and a date span off by eight months. **RECOUNT every number the prose asserts about a table beside it and RE-ENUMERATE every list it promises.** **(ii5)**: the cross-attestation instrument **(dd2)**/**(gg2)** returned its first *negative* result — two dossiers disagreeing rather than corroborating — and publishing the disagreement was the useful half. **Run an instrument for its negative result.** **(ii7)**: an ISO-date scan and a word-form scan **disagreed on the same segment**, so a `reviewBy` sort implemented as a regex over `YYYY-MM-DD` was right about the fence and wrong about the file. **Run both scans.**

**Versions and counters to bump from.** Classroom GAS **v01.63g** (a mechanism lesson lives in `Classroom.gs`, **inside** the fence). **Scraper is NOT yours** — a Phase 4 row seeds nothing; it stands at **v02.17g** after S2 session 16's `topic-landscape-compute-and-the-rack`. Profiler v01.39g and never yours; page versions unchanged (Classroom v01.14w). **Repo version: read `repository-information/repository.version.txt` on the day and bump by 0.01** — do not take it from this brief, whose own handover commit consumes the next number.

**THE DEPLOY — the §7.19 order, on its thirty-first use.** Read **Pages** first (`https://lightaisolutions.github.io/Sales/gs-versions/Classroomgs.version.txt` and `.../Scrapergs.version.txt` are static files) and the workflow's own `deploy confirmed` lines from the job log; probe `?op=deploy` only if the two disagree, with `curl -sL`. **A Phase 4 row touches ONE `.gs` file, so forecast ONE step and ONE logged line** — S2 session 16 touched two and its own forecast is recorded in its record. **Counters MEASURED against v06.36r's own job log, where the TWO-leg forecast was right on both legs:** `Scraper deploy confirmed (GET): Updated to v02.17g (deployment 162) | 162/200` in **16 seconds** and `Classroom deploy confirmed (GET): Updated to v01.63g (deployment 75) | 75/200` in **17 seconds** — exactly **two** `Deploy <Project>` steps fired and every other one exited 0 silently in zero seconds on its own `git diff` guard, the run closing on *All GAS deploys confirmed the merged version*. **So Classroom stands at 75/200 with 125 of its allowance left, and Scraper at 162/200 with 38.** Pages read `|v01.62g|` / `|v02.16g|` at 03:12:17 AM EST before the deploy landed and `|v01.63g|` / `|v02.17g|` at 03:13 AM after, so the §7.19 order resolved on its **two reads** for the **thirtieth** time and no `?op=deploy` probe was made. The post-merge `check-readme-tree.py --fix` step also reported *already in sync — nothing to commit*, so no correction commit landed on `main` behind the push. Counter rule unchanged: `Updated to` consumes one; a step that never fires consumes none and logs nothing; a leg that fires and does not confirm still consumes one and the tally does not see it; `Already up to date` is silent on consumption and must never be forecast as zero. **Scraper's headroom is the tight one at roughly 38. Add every intervening `.gs` push to your forecast, not just your own.**

**CHANGELOGS — YOU ARE PROBABLY THE ROTATOR, AND ON TWO FILES.** Re-counted from the files at v06.36r, this brief's own push included: **`CHANGELOG.md` 104 raw with SEVEN sections dated 2026-09-17 EST — 97 non-exempt** against a 100 trigger; **`Classroomgs.changelog.md` 50 raw with FOUR same-day — 46 non-exempt** against a cap of 50; `Scrapergs.changelog.md` 38 raw with two same-day — 36; `Profilerhtml.changelog.md` **49** and untouched by a Phase 4 row. **Run `TZ=America/New_York date '+%Y-%m-%d'` and trust it over the harness banner, which follows UTC and flips five hours early.** If it reads **2026-09-18 or later**, those same-day sections stop being exempt and the arithmetic turns: `CHANGELOG.md` goes to **104 non-exempt against 100** and `Classroomgs.changelog.md` to **50 against 50** — so you are the rotator on the first for certain and on the second at the boundary, which is the one to re-count by hand rather than infer. **Deepen the clone first so every rotated header SHA-enriches, dry-run into a copy and READ THE SEAMS, and run the mandatory post-rotation grep** (`grep '^## \[v' <archive> | grep -v '— \[' | head -5`).

**Thirteen open items recorded and not taken.** (i) The `.cl-tbl` `min-width` question — **thirty-two** data points now, S2 session 16's seven-row four-column indicator table the latest and clean at 1400 px. (ii) The generated `read-next` **names** a landscape module without linking to it — re-observed this session on `segment-compute-and-the-rack`, which now prints the module **id** rather than its title. (iii) `the-800-vdc-shift`'s stale `three-chains` intro is now **twenty-six** registrations stale. (iv) §10.6's session-4 bullet block is out of chronological order — cosmetic. (v) **A stale `lesson_ref` can make a segment due on its own** — `compute-and-the-rack` was the **ninth**, due on `inside-the-rack` having been built after its last regeneration. (vi) ~~The DC-arc taxonomy gap~~ — closed by row 20. (vii) `restart` scores zero for a **ninth** session. (viii) Session 12's six `topic-aidc-landlords` terms are in no seed for a **fifth**. (ix) **(ee4)** now has both dash rows of one segment on it (**(ii6)**), plus the measured correction that **two** segments resolve no criterion, not the three **(gg7)** recorded. (x) **(ff4)**'s quiz answer-index skew; S2 session 16 used **3, 1, 2, 0, 3, 1, 2**. (xi) **(ff5)**/**(hh6)**'s gated-title asymmetry — **this row adds a third lesson to the permissive side**, so check whether `utility-procurement-meets-ai-load` leaks its title through any segment's generated `read-next` and record which. (xii) **`Scraper.gs` holds NINE `topic-landscape-*` seeds against SIXTEEN built landscapes** — the shortfall is unchanged at seven and needs one developer pass. (xiii) **NEW — the Scraper roster is biased in the direction of the market it observes** (**(ii9)**): the incumbent's product roadmap is seeded and the challenger's was not, which a digest would read as confirmation. S2 session 16 corrected it for one segment; whether the same tilt exists elsewhere is unmeasured and is the developer's.

> Author Phase 4 row 21 — the `utility-procurement-meets-ai-load` lesson — on Opus 5 xhigh as a fresh session. This is ONE mechanism lesson. It authors no guidance module, no landscape, no dossier and no report, and it creates no track.
>
> READ FIRST: repository-information/SESSION-CONTEXT.md (Latest Session); INTEGRATED-REMEDIATION-PLAN.md §7.48 (your brief), §7.46 and §7.47 (for the Phase 4 session signature, the inside-the-fence rule, and what row 20 settled about a GATED lesson's denial being the acceptance test and about a declared handoff being honoured in substance and false in its status clause); §7.3 orders 5 and 6, and §6. Then CLASSROOM-CURRICULUM-PLAN.md §3.5's `utility-procurement-meets-ai-load` spec IN FULL (your eight section ids and their order), §2.2's utility reversal note (the split, decided 2026-09-07, whose other half `how-a-utility-buys` has been BUILT since v05.54r), §4's teaching order, §7 row 21, §8 items 2, 3, 9 and 10, and §10.6's findings — especially (t), (ff2), (ff4), (ff5), (hh1)–(hh7) and this session's (ii1)–(ii10). Then .claude/rules/classroom-app.md, repository-information/CLASSROOM-SCHEMA.md, and .claude/rules/changelog-security.md before you write any public changelog line.
>
> UNSHALLOW THE CLONE FIRST — git fetch --unshallow origin main, before any measurement and before any pin. If a container restart has left your clone behind origin/main, run git fetch --prune origin and git checkout -B <branch> origin/main BEFORE any measurement.
>
> **THE WORK — one push commit.** (1) Author `clLessonUtilityProcurementMeetsAiLoad_()` **INSIDE** the `// CONTENT START` … `// CONTENT END` fence in `Classroom.gs`, id `utility-procurement-meets-ai-load`, group Market Access & Bankability, eight sections at §3.5's ids and order, registered at the **END** of `clLessons_()` and **APPENDED** to `clTrackMarketAccess_()` — position 9 is the last of that track, so this row appends rather than inserting and **P5 should NOT fire**. (2) **LEAVE the module assertion in `scripts/check-classroom-content.py` AT 25** — a mechanism lesson registers no module; S2 session 16 moved it 24 → 25. (3) Regenerate NOTHING — a lesson registration does not entitle you to run the segment generator; the two segments your registration makes due are the next S2 session's or a developer's. (4) Flip §7.3's Phase 4 row and §6's Phase 4 row to **21 of 26**, mark curriculum §7 row 21 built with the full record, update §4's table at `market-access` position 9, and write the findings block — **the (ii…) label is spent; continue at (jj1)**. (5) Write §7.49 — the next run's brief, which by the interleave is S2 session 17 — in the same commit, and edit **BOTH halves** of any §7.x brief you refresh.
>
> IT IS THE SECOND GATED LESSON PHASE 4 HAS AUTHORED AND THE DENIAL IS THE TEST, NOT THE CONTENT. Row 20's (hh1): every lesson before it folded to `tracks`, so every prior render was a content test; a `guidance:` stamp makes `clGateForProvenance_()` return `guidance` and the analyst denial becomes the acceptance check. Measure base against head through the real `handleClassroomOp_`, exactly as row 20 did. **The arithmetic differs from row 20's and is worth calling first:** `market-access` already carries THREE gated lessons, so its analyst `withheld` count moves **3 → 4**, not 0 → 1. Confirm `cop=lesson` returns ROLE_DENIED, that ZERO of eight section nodes render in the analyst DOM, that the analyst index stays at 50 of 51, and that the title never enters the analyst DOM. §8 item 10's standing check names "the four guidance market lessons" — this is the FIFTH; update that sentence in the same commit.
>
> PREDICT THE `--check` NUMBER BEFORE YOU WRITE, AND THIS ONE IS DERIVABLE EXACTLY. At v06.36r `--check` reads **5 due**: cells-and-chemistry, power-conversion-and-rack-power-silicon, storage-developers-and-ipps, software-and-optimization, insurance-and-risk-transfer. Parsed from the generator: your lesson sits in the `READ_NEXT` of **exactly two** segments — `hyperscalers-and-ai-labs` and `utilities` — and **neither is currently due**; its `PLANNED_TITLES` entry is *The Gatekeeper*, so both stop printing `(planned)` the moment it registers. It matches **NONE** of the 27 `CRITERION_LEXICON` entries, so (ff2)'s part-one test fails and no `what-is-bought-and-on-what` moves. **Forecast 5 → 7, both new ones differing in `read-next` alone.** Derive it yourself from the parsed generator and record it before the run. (aa5) has held for ten sessions.
>
> reviewBy IS INHERITED — READ IT, RUN THE SORT ANYWAY, AND WRITE EVERY REJECTION DOWN. §7 row 21 fixes it at `guidance:utility-aidc-procurement-2026-08`'s own **2026-12-10**, shared with `power-infra-aidc-2026-08` and the built `where-bess-plugs-in`. Re-read the module on the day to confirm it still carries that date. Then sort the six utility guides' and the module's own dated material and record what you rejected — row 20's (hh5) inherited, ran the sort, met an EMPTY SET and wrote two rejections, and that is the standard. Sharing a module's date is not an (r)/(u)/(bb4) violation when §7 prescribes it; sharing it silently is. The curriculum checker reports **4** items due for review — landscape-cooling-2026-09 at 2026-09-28 (overdue from 29 September), landscape-neoclouds-2026-09 at 2026-09-30, landscape-utilities-2026-09 and landscape-in-hall-power-2026-09 at 2026-10-01. **All four are by design — do not "fix" any.**
>
> THE SPLIT IS DECIDED AND ITS OTHER HALF IS BUILT — CHECK IT RATHER THAN RE-DERIVING IT, AND WATCH THE STATUS CLAUSE. §2.2's reversal note enumerates what `how-a-utility-buys` (row 2, v05.54r) and the six utility guides carry publicly, and reserves for THIS lesson the seller's playbook: the four moves, the buyer map across the five channels, the concrete AIDC-driven BESS awards, and the dated gates as one timeline. **Row 20's (hh2) is the exposure**: that note was written on 2026-09-07 in language row 2 falsified ("today the whole utility-side story is guidance-only"). Grep `how-a-utility-buys` and the six utility guides for all four reserved items BEFORE drafting; if any has been taken, say so and draw the line where the corpus is. Run the reverse (t) sweep too per (hh3) and (ii2) — expect the null result and read whatever collisions come back.
>
> DO NOT: author or revise a guidance module or a landscape; run `build-classroom-segments.py` with `--segment` or `--all`; edit a generated `segment-*` literal by hand; seed `Scraper.gs` (a Phase 4 row seeds nothing); touch Profiler.gs or Profiler.html; move the module assertion off 25. And NEVER sweep a date across Classroom.gs with a blanket find-and-replace — it holds twenty-five independently dated guidance modules and session 9 corrupted a comment-deadline date inside `guidanceDocEo14420_()` that way; scope every replacement to the function you are authoring.
>
> VERIFY: `node --check` on a `.js` copy of `Classroom.gs`; `scripts/check-gas-inner-scripts.js`; `scripts/check-classroom-content.py` — capture a pristine HEAD baseline BEFORE any edit. It is 0 errors / 0 warnings at **50 lessons / 8 tracks / 142 gate cases** with the module assertion at **25**, and must be 0/0 after at **51 lessons** with the assertion still at **25**. Then `scripts/check-classroom-curriculum.py --strict` (no structural findings; 28 stale pins and 4 items due for review are pre-existing and not yours); `scripts/check-classroom-pipeline.py --selftest` (13 fixtures, 0 failures) and `--base origin/main` — expect **P1** on the plan files and **P2** on nothing you wrote inside the fence, and watch for **P3**, which must NOT appear, and for **P5**, which should NOT appear either because this row APPENDS. `build-classroom-segments.py --check` before and after (5 → 7). `scripts/check-readme-tree.py` after the version bump. **Playwright render at contributor with ZERO page errors, and analyst denied at the server.** Lesson entrypoint is `clShowLesson(id)` and section nodes are `#cl-<sectionId>`; `payload.action === 'guidance'` routes to `handleGuidanceOp_` and everything else to `handleClassroomOp_`; `_gasPost` returns a PROMISE, the shim's `validateSessionForData` must return `role`, `permissions` (an **ARRAY**) and `email` at the **TOP LEVEL** — `clRoleOf_` reads `sess.role` directly, and a `{ ok: true, session: {...} }` shape silently resolves to `viewer` and denies everything; the session parameter is named `session` and must be at least 32 characters; the session keys are `Classroom_gas_session_token` / `_gas_user_email` / `_gas_user_role` / `_gas_user_permissions` and **must be seeded AFTER the page loads, not through an init script** — the page clears them on load. Overlays to hide are `#auth-wall` / `#signing-out-wall` / `#auth-timers` / `#gas-layer-toggle` plus `#cl-app{display:block !important}`; `AUTO_REFRESH` is a `var AUTO_REFRESH = true;` declaration; the scratch site root needs `favicon.ico` beside `profiler-data/`, `sounds/` and `images/`. **When you blank `var _e`, match the WHOLE literal with a regex (`var _e = '[^']*';`).** SERVE THE SCRATCH COPY AT `/Classroom.html`, NOT AT THE SITE ROOT — the page's CSP `connect-src` is `'self'` plus Google hosts, so a shim on another port is REFUSED: serve the static files and the `/__gas` route from the same Node process on one port, and call the handlers through the vm context object (`ctx.handleClassroomOp_`), not from Node's own scope. Chromium is at `/opt/pw-browsers/chromium-1194/chrome-linux/chrome`; `pip install playwright` first, and do NOT run `playwright install`. `ps -eo pid,args | grep -E "node .*shim"` is the pattern that works; start it with `setsid … &` **inside a subshell** — `setsid … &` on its own line gets killed by a later `pkill` in the same shell. Re-extract the PROJECT region after every edit and DEMAND that both the rendered character count AND the PROJECT byte count MOVE — S2 session 16's went 3,559,602 → 3,560,190 bytes and 43,179 → 43,782 characters across its fix round. Write scripts to a file with a QUOTED heredoc delimiter, or bash interpolates backticks inside Python. **RUN THE MICRO-MARKUP SIMULATION BEFORE THE SPLICE** — this row uses `proscons` and `timeline`, and `PLAIN_BY_KIND` makes a `proscons` card's `t`/`meta` and a `timeline`'s `lanes` values plain text, so no `{{term}}`, no `**bold**` and no `*italic*` in any of them — and confirm every glossary entry you define is actually used. Then READ EVERY SECTION'S SCREENSHOT — **ten consecutive sessions** have had a defect caught there and nowhere else, and S2 session 16's SEVEN were five of the prose-contradicts-the-adjacent-content class: a sales line promising three items and listing four, a table intro claiming a row count the spec does not give, a sales line asserting two dated rows when the table beside it had none, an unsupported fraction, and a date span off by eight months. **RECOUNT every number the prose asserts about a table beside it; CHECK EVERY ENUMERATION the prose promises; and vary the quiz answer index per (ff4)** — S2 session 16 used 3, 1, 2, 0, 3, 1, 2. **Prefer pure ASCII for the whole literal.** Bump Classroom's GAS version in BOTH `Classroom.gs` VERSION (from v01.63g) and `Classroomgs.version.txt`, with a GENERIC entry in its GAS changelog. **Scraper is NOT yours on this row.**
>
> THE DEPLOY: the §7.19 order, on its thirty-first use. Read Pages first (the `gs-versions/*.txt` files are static) and the workflow's own deploy confirmed lines from the job log. Probe `?op=deploy` only if those disagree, and use curl -sL if you do. **A Phase 4 row touches ONE `.gs` file, so forecast ONE step and ONE logged line.** Counters MEASURED against v06.36r's own job log, where the two-leg forecast was right on both legs: `Scraper deploy confirmed (GET): Updated to v02.17g (deployment 162) | 162/200` in 16 seconds and `Classroom deploy confirmed (GET): Updated to v01.63g (deployment 75) | 75/200` in 17 seconds, with exactly TWO Deploy steps firing and every other one exiting 0 silently in zero seconds. **Classroom stands at 75/200 with 125 left and Scraper at 162/200 with 38.** Add every intervening `.gs` push to your forecast, not just your own. Scraper's headroom is the tight one at roughly 38.
>
> THE CHANGELOG ARITHMETIC — YOU ARE PROBABLY THE ROTATOR, AND ON TWO FILES. Re-counted from the files at v06.36r: `CHANGELOG.md` **104 raw with SEVEN sections dated 2026-09-17 EST — 97 non-exempt** against a 100 trigger; `Classroomgs.changelog.md` **50 raw with FOUR same-day — 46 non-exempt** against a cap of 50; `Scrapergs.changelog.md` 38 / 36; `Profilerhtml.changelog.md` 49 and untouched by a Phase 4 row. Run `TZ=America/New_York date '+%Y-%m-%d'` and trust it over the harness banner. **If it reads 2026-09-18 or later, those same-day sections stop being exempt: `CHANGELOG.md` goes to 104 non-exempt against 100 and `Classroomgs.changelog.md` to 50 against 50** — the first is a certainty and the second is exactly at the boundary, so re-count that one by hand rather than inferring it. Deepen the clone first so every rotated header enriches, dry-run into a copy and READ THE SEAMS, and run the mandatory post-rotation grep (`grep '^## \[v' <archive> | grep -v '— \[' | head -5`).
>
> Normal Pre-Commit and Pre-Push checklists; claude/* branch restarted from origin/main first, git ls-remote before each push — the auto-merge workflow adds a [skip ci] SHA-tracker commit after every merge, and after it merges use `git fetch --prune origin` rather than `git fetch origin main`. End every commit message with the two attribution lines this repo's recent commits carry (Co-Authored-By: and Claude-Session:). Read `repository-information/repository.version.txt` for the version to bump from — do not take it from this prompt, whose own handover commit consumes the next number.
>
> AFTER THIS SESSION Phase 4 is 21 of 26 and S2 is 16 of ~19, so the interleave returns to S2 and the recommended next run is **S2 session 17**. Three segments remain — `software-and-optimization`, `insurance-and-risk-transfer` and `assurance` — and the v05.62r CHANGELOG record fixes the last two as **18th and 19th of 19**, so the order is settled for the first time since §7.45: take **`software-and-optimization`**, whose twenty members (2 · 3 · 15) are the most adjacent-heavy roster in the taxonomy at 75 %, and whose registry note records a genuine ranking vacuum — no reachable third party ranks any player in storage bidding except Modo Energy, whose 2025 US directory is vendor-written and does not quantify one of the two incumbents, while the one source that would settle it is paywalled. §7.49 should be written for it.
>
> Then, give me a prompt to paste into a new Opus 5 xhigh session to continue the action plan, then remember session.

### 7.49 · Paste-in brief for S2 session 17 — `software-and-optimization` (Opus 5 xhigh) — written 2026-09-17 at v06.38r, at the close of Phase 4 row 21

**Which segment, and why the order is finally settled.** Phase 4 is **21 of 26** and S2 is **16 of ~19**, so the interleave stands at 21:16 and §7.4 item 6 hands the turn back to S2. Three segments remain — `software-and-optimization`, `insurance-and-risk-transfer`, `assurance` — and the **v05.62r CHANGELOG record fixes the last two as 18th and 19th of 19**, so for the first time since §7.45 there is nothing to argue: take **`software-and-optimization`**. It is `profiler-segments.json` position **18**, tier **services**, and its own registry `notes` record that the landscape hold was **released** on 2026-09-12 when S3 V2 (E1b) landed Habitat Energy and Gridmatic.

**THE ROSTER IS THE MOST ADJACENT-HEAVY IN THE TAXONOMY AND THAT IS THE OPENING PROBLEM, NOT A FOOTNOTE.** Twenty members: **2 incumbents · 3 challengers · 15 adjacent** — **75 % adjacent**, against `compute-and-the-rack`'s four-member floor and `cooling`'s mix. Both incumbents reach the segment **from an adjacent business** (FlexGen from integration, Stem from solar asset-performance management) and all three pure-play challengers are **private**. So the segment's own coverage note is that it is defined by a capability its covered members mostly acquired sideways. **Measure the ratio yourself off `profiler-segments.json` before drafting rather than taking these numbers from this brief — (jj1) is exactly the failure of trusting a brief's arithmetic about the corpus.**

**AND THE RANKING VACUUM IS REAL, DOCUMENTED, AND THE SEGMENT'S HONEST HEADLINE.** The registry `notes` already say it: **no third party reachable in the P3 research ranks any player in storage bidding except Modo Energy**, whose April 2025 US directory is **vendor-written**, quantifies Habitat, Gridmatic, Equilibrium, Tesla, Tierra and Caerus, and **does not quantify Stem** — one of the segment's two incumbents. Wood Mackenzie's *Global BESS software landscape 2026* is the one source that would settle it and is **paywalled**. That is **(ii1)**'s shape again — the coverage boundary is the segment's only honest ranking, so it is the opening section — but with a sharper edge: here a named third-party ranking *exists* and **omits an incumbent**, which is a different and more publishable finding than having no ranking at all. Do not launder a vendor-written directory into a market share.

**Baselines to capture BEFORE any edit, all measured at the close of v06.38r.** `check-classroom-content.py` **0 errors / 0 warnings at 51 lessons, 8 tracks, 142 gate cases**, module assertion **25** — **your module is the twenty-sixth, so EDIT THE ASSERTION 25 → 26 in the same commit**; it is a hard assertion and the edit sessions forget. `check-classroom-curriculum.py --strict`: **no structural findings**, **28** stale pins, **4** items due for review — `landscape-cooling-2026-09` at 2026-09-28 (overdue), `landscape-neoclouds-2026-09` at 2026-09-30, `landscape-utilities-2026-09` and `landscape-in-hall-power-2026-09` at 2026-10-01. **All four by design — do not "fix" any.** `check-classroom-pipeline.py --selftest`: **13 fixtures / 0 failures**. `build-classroom-segments.py --check`: **7 due** — `cells-and-chemistry`, `power-conversion-and-rack-power-silicon`, `storage-developers-and-ipps`, **`hyperscalers-and-ai-labs`**, **`utilities`**, `software-and-optimization`, `insurance-and-risk-transfer`. **The two bolded ones are row 21's residue** — its registration put `utility-procurement-meets-ai-load` into their `READ_NEXT` and neither was regenerated, by instruction. **Your segment is already in that list**, so regenerating it clears one and your forecast is a *decrement plus whatever your module's registration adds*, not an increment — derive both halves from the parsed generator before the run. **(aa5) has held for eleven sessions; do not be the one who stops calling it.**

**THE WORK — one push commit.** (1) Write `repository-information/industry-guidance/landscape-software-and-optimization-analysis.md`, the source of truth, with the **coverage-boundary section drafted BEFORE any other section**. (2) Author `guidanceDocLandscapeSoftwareAndOptimization_()` **BELOW** the `// CONTENT END` fence in `Classroom.gs`, id `landscape-software-and-optimization-2026-09`, lane **The Value Chain**, tier **contributor**, nine sections at §10.6's ids and order, registered at the **END** of `guidanceDocs_()`'s Value Chain lane. (3) **EDIT THE MODULE ASSERTION IN `scripts/check-classroom-content.py` FROM 25 TO 26.** (4) Seed `topic-landscape-software-and-optimization` in `Scraper.gs`, **every term scored against both arrays first** — and read **(ii9)** before you choose them: S2 session 16 found the roster tilted toward the incumbent it exists to measure, and this segment has two incumbents whose vocabulary comes from two *different* adjacent businesses. (5) Regenerate **exactly one** segment — yours — with `--segment software-and-optimization --today <EST day of the push>`. (6) Flip §7.3's S2 row, §6's S2 row and §10.6's progress note to **17 of ~19**, strike `software-and-optimization` from the remaining list, and write the findings block — **the (jj…) label is spent; continue at (kk1)**. (7) Write §7.50 — the next run's brief, which by the interleave is **Phase 4 row 22, `the-china-policy-stack`** — in the same commit, and edit **BOTH halves** of any §7.x brief you refresh.

**WHAT ROW 21 LEARNED THAT THIS ROW INHERITS — four findings, and the first is aimed at this brief.** **(jj1)**: a brief's arithmetic about the corpus is a claim about the corpus. §7.48 asserted three built lessons that register **zero** times and every downstream number inherited the error. **Grep for every entity this brief names before you plan against it — this one included.** **(jj4)**: the timeline renderer has two undocumented contracts — `CL_LANE_COLORS` is keyed on the literal names `gen`/`deploy`/`eco` (any other key renders faint grey), and `clTimeline` prints `Math.floor(x)` in the left gutter as a year label. If your module carries a `timeline`, use those lane keys and put meaningful values in `x`. **(jj5)**: run **both** `reviewBy` scans — an ISO regex found one date where a word-form scan found five. **(jj6)**: the recount pass and the screenshot pass catch **different** defects and neither substitutes for the other — six before the splice, four after, eleven consecutive sessions.

**Versions and counters to bump from.** Classroom GAS **v01.64g**; Scraper **v02.17g** — **and Scraper IS yours on this row**, because an S2 session seeds a topic. Profiler v01.39g and never yours; page versions unchanged (Classroom v01.14w). **Repo version: read `repository-information/repository.version.txt` on the day and bump by 0.01** — do not take it from this brief.

**THE DEPLOY — the §7.19 order, on its thirty-second use.** Read **Pages** first (`https://lightaisolutions.github.io/Sales/gs-versions/Classroomgs.version.txt` and `.../Scrapergs.version.txt` are static files) and the workflow's own `deploy confirmed` lines from the job log; probe `?op=deploy` only if the two disagree, with `curl -sL`. **An S2 session touches TWO `.gs` files, so forecast TWO steps and TWO logged lines.** Counters **MEASURED against v06.38r's own job log**, where the one-leg forecast was right: `Classroom deploy confirmed (GET): Updated to v01.64g (deployment 76) | 76/200` in **16 seconds**, with exactly **one** `Deploy <Project>` step firing and the other seven exiting 0 silently in zero seconds on their own `git diff` guard. **Classroom 76/200 with 124 left; Scraper 162/200 with 38** — Scraper is the tight one and an S2 session spends one of its 38. Counter rule unchanged: `Updated to` consumes one; a step that never fires consumes none and logs nothing; a leg that fires and does not confirm still consumes one and the tally does not see it; `Already up to date` is silent on consumption and must never be forecast as zero. **Add every intervening `.gs` push to your forecast, not just your own.**

**CHANGELOGS — RE-COUNT ON THE DAY, BECAUSE THE EXEMPTION IS WHAT IS HOLDING THE ROTATION BACK.** After v06.38r: `CHANGELOG.md` **106 raw with NINE sections dated 2026-09-17 EST — 97 non-exempt** against a 100 trigger; `Classroomgs.changelog.md` **51 raw with FIVE same-day — 46 non-exempt** against a cap of 50; `Scrapergs.changelog.md` 38 / 36; `Profilerhtml.changelog.md` 49. **Run `TZ=America/New_York date '+%Y-%m-%d'` and trust it over the harness banner, which follows UTC and flips five hours early.** If it reads **2026-09-18 or later**, those same-day sections stop being exempt and the arithmetic turns hard: `CHANGELOG.md` goes to **106 against 100** and `Classroomgs.changelog.md` to **51 against 50** — **rotator on both, and the second only just**, so re-count that one by hand rather than inferring it. Deepen the clone first so every rotated header SHA-enriches, dry-run into a copy and **READ THE SEAMS**, and run the mandatory post-rotation grep (`grep '^## \[v' <archive> | grep -v '— \[' | head -5`).

**Thirteen open items recorded and not taken.** (i) The `.cl-tbl` `min-width` question — **thirty-four** data points now, row 21's 4×4 and 3×5 tables the latest and clean at 1400 px. (ii) The generated `read-next` names a landscape module without linking to it. (iii) `the-800-vdc-shift`'s stale `three-chains` intro is **twenty-seven** registrations stale. (iv) §10.6's session-4 bullet block is out of chronological order. (v) A stale `lesson_ref` can make a segment due on its own — row 21 left **two** such segments behind, by instruction. (vi) ~~The DC-arc taxonomy gap~~ — closed by row 20. (vii) `restart` scores zero for a **tenth** session. (viii) Session 12's six `topic-aidc-landlords` terms are unseeded for a **sixth**. (ix) **(ee4)**/**(gg7)**'s dash rows and the measured two-not-three correction. (x) **(ff4)**'s quiz answer-index skew; row 21 used **1, 3, 0, 2, 3**. (xi) **(ff5)**/**(hh6)** — **now answered and widened by (jj3)**: the generator publishes gated lessons' titles through `PLANNED_TITLES` **before they are written**, and `segment-utilities` leaks **two** unbuilt guidance lessons in one callout. Registration does not create the leak; it only removes the word that made it honest. **Still the developer's call.** (xii) `Scraper.gs` holds **nine** `topic-landscape-*` seeds against **sixteen** built landscapes — shortfall seven, one developer pass. (xiii) **(jj4) — NEW**: two built timelines violate undocumented renderer contracts (`where-bess-plugs-in` loses the CVD trio to faint grey and prints `1 2 3 4 5 6` as year labels). Cosmetic-looking, and both make the instrument say something false.

> Author S2 session 17 — the `software-and-optimization` landscape module — on Opus 5 xhigh as a fresh session. This is ONE guidance module. It authors no mechanism lesson, no dossier and no report, and it creates no track.
>
> READ FIRST: repository-information/SESSION-CONTEXT.md (Latest Session); INTEGRATED-REMEDIATION-PLAN.md §7.49 (your brief), §7.47 and §7.48 (for the S2 session signature, the below-the-fence rule, and what Phase 4 row 21 settled about a brief's arithmetic being a claim that must be measured, about a handoff written as a POINTER surviving the row that closes it, and about the recount pass and the screenshot pass catching different defects); §7.3 orders 5 and 6, and §6. Then CLASSROOM-CURRICULUM-PLAN.md §10 IN FULL — especially §10.3 (the generator spec), §10.5 (the lane), §10.6 (the landscape-module shape, its nine section ids and order, and the whole (a)–(jj6) findings list), §10.11 (where the taxonomy strained), §4, §7, §8 items 2, 3 and 9, and §2.2. Then .claude/rules/industry-guidance.md, .claude/rules/classroom-app.md, repository-information/CLASSROOM-SCHEMA.md, .claude/rules/scraper-sources.md, and .claude/rules/changelog-security.md before you write any public changelog line.
>
> UNSHALLOW THE CLONE FIRST — git fetch --unshallow origin main, before any measurement and before any pin. If a container restart has left your clone behind origin/main, run git fetch --prune origin and git checkout -B <branch> origin/main BEFORE any measurement.
>
> **THE WORK — one push commit.** (1) Write `repository-information/industry-guidance/landscape-software-and-optimization-analysis.md`, the source of truth, with the coverage-boundary section drafted BEFORE any other section. (2) Author `guidanceDocLandscapeSoftwareAndOptimization_()` **BELOW** the `// CONTENT END` fence in `Classroom.gs`, id `landscape-software-and-optimization-2026-09`, lane The Value Chain, tier contributor, nine sections at §10.6's ids and order, registered at the **END** of `guidanceDocs_()`'s Value Chain lane. (3) **EDIT THE MODULE ASSERTION IN `scripts/check-classroom-content.py` FROM 25 TO 26 IN THE SAME COMMIT** — it is a hard assertion and your module is the twenty-sixth. (4) Seed `topic-landscape-software-and-optimization` in `Scraper.gs`, every term scored against both arrays first. (5) Regenerate **exactly one** segment — yours. (6) Flip §7.3's S2 row, §6's S2 row and §10.6's progress note to **17 of ~19**, strike `software-and-optimization` from the remaining list, and write the findings block — **the (jj…) label is spent; continue at (kk1)**. (7) Write §7.50 — the next run's brief, which by the interleave is Phase 4 row 22, `the-china-policy-stack` — in the same commit, and edit **BOTH halves** of any §7.x brief you refresh.
>
> THE ROSTER IS THE MOST ADJACENT-HEAVY IN THE TAXONOMY AND THAT IS THE OPENING PROBLEM. Twenty members: 2 incumbents, 3 challengers, 15 adjacent — **75 % adjacent**. Both incumbents reach the segment from an ADJACENT business (FlexGen from integration, Stem from solar asset-performance management) and all three pure-play challengers are PRIVATE. **Measure the ratio yourself off `profiler-segments.json` rather than taking it from this brief** — (jj1) is exactly the failure of trusting a brief's arithmetic about the corpus, and this brief is not exempt from its own finding.
>
> AND THE RANKING VACUUM IS REAL AND DOCUMENTED IN THE REGISTRY'S OWN NOTES. No third party reachable in the P3 research ranks any player in storage bidding except **Modo Energy**, whose April 2025 US directory is **vendor-written**, quantifies Habitat, Gridmatic, Equilibrium, Tesla, Tierra and Caerus, and **does not quantify Stem** — one of this segment's two incumbents. Wood Mackenzie's *Global BESS software landscape 2026* is the one source that would settle it and is paywalled. This is (ii1)'s shape with a sharper edge: a named ranking EXISTS and OMITS an incumbent, which is more publishable than having none. **Do not launder a vendor-written directory into a market share.**
>
> PREDICT THE `--check` NUMBER BEFORE YOU WRITE, AND NOTE THAT YOURS IS A DECREMENT. At v06.38r `--check` reads **7 due**: cells-and-chemistry, power-conversion-and-rack-power-silicon, storage-developers-and-ipps, **hyperscalers-and-ai-labs**, **utilities**, software-and-optimization, insurance-and-risk-transfer. The two bolded are Phase 4 row 21's residue — its lesson entered their `READ_NEXT` and neither was regenerated, by instruction. **Your segment is already in the list**, so regenerating it clears one; your forecast is that decrement plus whatever your module's registration adds. Derive both halves from the parsed generator and record them before the run. (aa5) has held for eleven sessions.
>
> DO NOT: author or revise a mechanism lesson; run `build-classroom-segments.py` with `--all`; edit a generated `segment-*` literal by hand; touch Profiler.gs or Profiler.html; leave the module assertion at 25. And NEVER sweep a date across Classroom.gs with a blanket find-and-replace — it holds twenty-six independently dated guidance modules once yours lands, and session 9 corrupted a comment-deadline date inside `guidanceDocEo14420_()` that way; scope every replacement to the function you are authoring.
>
> VERIFY: `node --check` on `.js` copies of both `.gs` files; `scripts/check-gas-inner-scripts.js`; `scripts/check-classroom-content.py` — capture a pristine HEAD baseline BEFORE any edit. It is 0 errors / 0 warnings at **51 lessons / 8 tracks / 142 gate cases** with the module assertion at **25**, and must be 0/0 after at 51 lessons with the assertion at **26**. Then `scripts/check-classroom-curriculum.py --strict` (no structural findings; 28 stale pins and 4 items due for review are pre-existing and not yours — landscape-cooling-2026-09 at 2026-09-28 is overdue BY DESIGN); `scripts/check-classroom-pipeline.py --selftest` (13 fixtures, 0 failures) and `--base origin/main`. `build-classroom-segments.py --check` before and after. `scripts/check-readme-tree.py` after the version bump. **Playwright render of the module at contributor with ZERO page errors, and analyst denied at the server.** Guidance entrypoint is the `#guidance` route and `payload.action === 'guidance'` routes to `handleGuidanceOp_`; everything else goes to `handleClassroomOp_`. **The page needs a real `_e`, not a blank one** — blanking it means no `#gas-app` element and therefore no `dataset.baseUrl`, and the page renders "Classroom content is not connected yet"; instead set `_e` to `btoa('/__gas'.reverse())` = `c2FnX18v` so the transport points same-origin, and hide `#gas-app` along with `#auth-wall` / `#signing-out-wall` / `#auth-timers` / `#gas-layer-toggle` / `#tab-takeover-wall`, plus `#cl-app{display:block !important}`. `_gasPost` puts every parameter in the QUERY STRING, not the body, so the shim must read `u.searchParams`; the simplest role plumbing is to carry the role in the session-token prefix (`contributor_` + 32 chars) and have the shim's `validateSessionForData` return `role`, `permissions` (an ARRAY) and `email` at the **TOP LEVEL** — `clRoleOf_` reads `sess.role` directly and a `{ ok: true, session: {...} }` shape silently resolves to viewer. Seed the session keys AFTER the page loads. `AUTO_REFRESH` is a `var AUTO_REFRESH = true;` declaration. Serve the static files and `/__gas` from ONE Node process on ONE port at `/Classroom.html` — the CSP `connect-src` is `'self'` plus Google hosts — and call the handlers through the vm context object (`ctx.handleGuidanceOp_`). Chromium is at `/opt/pw-browsers/chromium-1194/chrome-linux/chrome`; `pip install playwright` first, and do NOT run `playwright install`. **Do not put `pkill -f "node .*shim"` in the same Bash call that starts the shim** — the pattern matches the enclosing shell's own command line and kills it; use `fuser -k -n tcp <port>` instead, and start with `(setsid … &)` inside a subshell. Re-extract the PROJECT region after every edit and DEMAND that both the rendered character count AND the PROJECT byte count MOVE — row 21's went 3,599,117 → 3,599,178 bytes and 24,973 → 25,030 characters across its fix round. Write scripts to a file with a QUOTED heredoc delimiter, or bash interpolates backticks inside Python. RUN THE MICRO-MARKUP SIMULATION BEFORE THE SPLICE, and confirm every glossary entry you define is actually used. **If your module carries a `timeline`, read (jj4) first**: `CL_LANE_COLORS` is keyed on the literal lane names `gen`/`deploy`/`eco` and anything else renders faint grey, and `clTimeline` prints `Math.floor(x)` in the left gutter as a year label. Then READ EVERY SECTION'S SCREENSHOT — **eleven consecutive sessions** have had a defect caught there and nowhere else, and (jj6) is that the recount pass and the screenshot pass catch DIFFERENT defects: row 21's recount caught six contradictions inside single fields, and the screenshots then caught four more BETWEEN adjacent fields, which are only visible once laid out together. **Run both, in that order.** RECOUNT every number the prose asserts about a table beside it; CHECK EVERY ENUMERATION the prose promises; and vary the quiz answer index per (ff4) — row 21 used 1, 3, 0, 2, 3. **Prefer pure ASCII for the whole literal.** Bump Classroom's GAS version in BOTH `Classroom.gs` VERSION (from v01.64g) and `Classroomgs.version.txt`, and Scraper's (from v02.17g) in both of its files, with a GENERIC entry in each GAS changelog.
>
> THE DEPLOY: the §7.19 order, on its thirty-second use. Read Pages first (the `gs-versions/*.txt` files are static) and the workflow's own deploy confirmed lines from the job log. Probe `?op=deploy` only if those disagree, and use curl -sL if you do. **An S2 session touches TWO `.gs` files, so forecast TWO steps and TWO logged lines.** Counters MEASURED against v06.38r's own job log: `Classroom deploy confirmed (GET): Updated to v01.64g (deployment 76) | 76/200` in 16 seconds, one Deploy step firing and seven exiting 0 silently. **Classroom 76/200 with 124 left; Scraper 162/200 with 38.** Scraper is the tight one and this session spends one of its 38. Add every intervening `.gs` push to your forecast, not just your own.
>
> THE CHANGELOG ARITHMETIC — RE-COUNT ON THE DAY. After v06.38r: `CHANGELOG.md` **106 raw with NINE sections dated 2026-09-17 EST — 97 non-exempt** against a 100 trigger; `Classroomgs.changelog.md` **51 raw with FIVE same-day — 46 non-exempt** against a cap of 50; `Scrapergs.changelog.md` 38 / 36; `Profilerhtml.changelog.md` 49. Run `TZ=America/New_York date '+%Y-%m-%d'` and trust it over the harness banner. **If it reads 2026-09-18 or later, those same-day sections stop being exempt: `CHANGELOG.md` goes to 106 against 100 and `Classroomgs.changelog.md` to 51 against 50** — rotator on both, and the second only just, so re-count that one by hand rather than inferring it. Deepen the clone first so every rotated header enriches, dry-run into a copy and READ THE SEAMS, and run the mandatory post-rotation grep (`grep '^## \[v' <archive> | grep -v '— \[' | head -5`).
>
> Normal Pre-Commit and Pre-Push checklists; claude/* branch restarted from origin/main first, git ls-remote before each push — the auto-merge workflow adds a [skip ci] SHA-tracker commit after every merge, and after it merges use `git fetch --prune origin` rather than `git fetch origin main`. End every commit message with the two attribution lines this repo's recent commits carry (Co-Authored-By: and Claude-Session:). Read `repository-information/repository.version.txt` for the version to bump from — do not take it from this prompt, whose own handover commit consumes the next number.
>
> AFTER THIS SESSION Phase 4 is 21 of 26 and S2 is 17 of ~19, so the interleave returns to Phase 4 and the recommended next run is **row 22, `the-china-policy-stack`** — `market-access` position 8, guidance-gated on TWO modules (`china-policy-stack-2026-08` at reviewBy 2026-12-31 and `eo14420-bulk-power-2026-08` at 2026-12-24), so it is the first lesson in the programme to inherit from two clocks and must take the EARLIER. It will be the THIRD gated lesson Phase 4 has authored and the SECOND in `market-access`, moving that track's analyst withheld count 1 → 2. §7.50 should be written for it.
>
> Then, give me a prompt to paste into a new Opus 5 xhigh session to continue the action plan, then remember session.

### 7.50 · Paste-in brief for Phase 4 row 22 — `the-china-policy-stack` (Opus 5 xhigh) — written 2026-09-17 at v06.40r, at the close of S2 session 17

**Which row, and the two things about it that are new.** Phase 4 is **21 of 26** and S2 is **17 of ~19**, so the interleave stands at 21:17 and §7.4 item 6 hands the turn back to Phase 4. The lowest unbuilt row is **22, `the-china-policy-stack`** — `market-access` **§4 position 8 of 9**, §7 row 22. Two firsts: it is the **first lesson in the programme guidance-gated on TWO modules**, and it will be the **third gated lesson Phase 4 has authored** and the **second in `market-access`**, moving that track's analyst `withheld` count **1 → 2**.

**THE TWO CLOCKS, MEASURED OFF `Classroom.gs` ON 2026-09-17 RATHER THAN TAKEN FROM A BRIEF.** `china-policy-stack-2026-08` carries `reviewBy` **2026-12-31**; `eo14420-bulk-power-2026-08` carries `reviewBy` **2026-12-24**. **Take the EARLIER — 2026-12-24.** Re-read both on the day before you write: §10.6 **(kk1)** is that a brief's arithmetic is usable only once you have run it yourself, and this brief is not exempt from its own finding. Run the nearest-gate sort anyway and record the rejections, per **(kk6)** — and run **both** scans, because their in-horizon results were **disjoint** on the last row that measured them.

**AND THE POSITION IS AN INSERT, NOT AN APPEND — WHICH FLIPS THE PIPELINE SIGNATURE FROM ROW 21's.** §4 puts this lesson at **position 8 of nine**, and **position 9 (`utility-procurement-meets-ai-load`) is BUILT** — it was appended by row 21. Positions 4, 6 and 7 are unbuilt. So among the five ids `clTrackMarketAccess_()` currently carries — `who-buys-storage`, `how-a-storage-project-happens`, `contracts-and-revenue`, `how-a-utility-buys`, `utility-procurement-meets-ai-load` — the correct write is a **MID-ARRAY INSERT at index 4**, immediately before `utility-procurement-meets-ai-load`. **That fires P5 by construction and correctly.** Forecast **P1 and P5**; row 21 reported P1 alone because it appended, and a session that copies row 21's forecast will be wrong.

**Baselines to capture BEFORE any edit, all measured at the close of v06.40r.** `check-classroom-content.py` **0 errors / 0 warnings at 51 lessons, 8 tracks, 142 gate cases**, module assertion **26** — **LEAVE IT AT 26**: a mechanism lesson registers no module, and S2 session 17 moved it 25 → 26. `check-classroom-curriculum.py --strict`: **no structural findings**, **28** stale pins, **4** items due for review — `landscape-cooling-2026-09` at 2026-09-28 (overdue), `landscape-neoclouds-2026-09` at 2026-09-30, `landscape-utilities-2026-09` and `landscape-in-hall-power-2026-09` at 2026-10-01. **All four by design — do not "fix" any.** `check-classroom-pipeline.py --selftest`: **13 fixtures / 0 failures**. `build-classroom-segments.py --check`: **6 due** — `cells-and-chemistry`, `power-conversion-and-rack-power-silicon`, `storage-developers-and-ipps`, `hyperscalers-and-ai-labs`, `utilities`, `insurance-and-risk-transfer`. **Those six are residue from earlier rows, not yours.** Derive your own two halves from the parsed generator before the run: `READ_NEXT` membership (which segments name this lesson) and the `CRITERION_LEXICON` deepening test. **(aa5) has now held for twelve sessions; do not be the one who stops calling it.**

**THE WORK — one push commit.** (1) Author `clLessonTheChinaPolicyStack_()` **INSIDE** the `// CONTENT START` … `// CONTENT END` fence in `Classroom.gs`, id `the-china-policy-stack`, group Market Access & Bankability, the section ids and order §3.5 fixes, registered at the **END** of `clLessons_()` and **INSERTED at index 4** of `clTrackMarketAccess_()`. (2) **LEAVE the module assertion at 26.** (3) Regenerate **NOTHING** — a lesson registration does not entitle you to run the segment generator; whatever segments your registration makes due belong to the next S2 session or to a developer. (4) Flip §7.3's Phase 4 row and §6's Phase 4 row to **22 of 26**, mark curriculum §7 row 22 built with the full record, update §4's table at `market-access` position 8, and write the findings block — **the (kk…) label is spent; continue at (ll1)**. (5) Write §7.51 — the next run's brief, which by the interleave is **S2 session 18, `insurance-and-risk-transfer`** — in the same commit, and edit **BOTH halves** of any §7.x brief you refresh.

**WHAT S2 SESSION 17 LEARNED THAT THIS ROW INHERITS — five findings.** **(kk1)**: a brief's arithmetic is a claim; §7.49's was *right* and measuring it still changed the module, because only the measurement produced the superlative that could open it — and the same pass caught the module's *own* first-draft miscount. **Run every number in this brief before planning against it.** **(kk3)**: an instrument that fires identically from two positions is measuring the position, not the subject — **check the denominator before publishing a result.** **(kk6)**: run both `reviewBy` scans and widen the scan past `policyExposure[]`; the nearest candidate on the last row was visible to only one scan and only outside the specified scope. **(kk7)**: the recount pass and the screenshot pass caught **nine** defects between them and **not one overlapped** — **superlatives are the recount pass's richest vein and enumerations are the screenshot pass's.** **(kk8)**: a new corpus-wide open item **(xiv)** — the Scraper matcher's word-boundary guard makes 447 of 451 singular held terms blind to their own plural. Not this row's to fix.

**Versions and counters to bump from.** Classroom GAS **v01.65g**; Scraper **v02.18g** and **not yours on this row** — a mechanism lesson seeds no topic. Profiler v01.39g and never yours; page versions unchanged (Classroom v01.14w). **Repo version: read `repository-information/repository.version.txt` on the day and bump by 0.01** — do not take it from this brief, whose own handover commit consumes the next number.

**THE DEPLOY — the §7.19 order, on its thirty-third use.** Read **Pages** first (`https://lightaisolutions.github.io/Sales/gs-versions/Classroomgs.version.txt` is a static file) and the workflow's own `deploy confirmed` lines from the job log; probe `?op=deploy` only if the two disagree, with `curl -sL`. **A Phase 4 row touches ONE `.gs` file, so forecast ONE step and ONE logged line** — S2 session 17 touched two and forecast two. Counters **MEASURED against v06.40r's own job log, where the TWO-leg forecast was right on both legs**: `Scraper deploy confirmed (GET): Updated to v02.18g (deployment 163) | 163/200` in **11 seconds** and `Classroom deploy confirmed (GET): Updated to v01.65g (deployment 77) | 77/200` in **15 seconds**, with exactly **two** `Deploy <Project>` steps firing and the other **six** exiting 0 silently in zero seconds on their own `git diff` guard. The run closed on *All GAS deploys confirmed the merged version*. **Classroom now stands at 77/200 with 123 of its allowance left; Scraper at 163/200 with 37** — Scraper is the tight one and this row does not spend any of it. Counter rule unchanged: `Updated to` consumes one; a step that never fires consumes none and logs nothing; a leg that fires and does not confirm still consumes one and the tally does not see it; `Already up to date` is silent on consumption and must never be forecast as zero. **Add every intervening `.gs` push to your forecast, not just your own. Scraper's headroom is the tight one and this row does not spend any of it.**

**CHANGELOGS — RE-COUNT ON THE DAY.** After v06.40r: `CHANGELOG.md` **108 raw with ELEVEN sections dated 2026-09-17 EST — 97 non-exempt** against a 100 trigger; `Classroomgs.changelog.md` **52 raw with SIX same-day — 46 non-exempt** against a cap of 50; `Scrapergs.changelog.md` **39 / 36**; `Profilerhtml.changelog.md` **49**. **Run `TZ=America/New_York date '+%Y-%m-%d'` and trust it over the harness banner, which follows UTC and flips five hours early.** If it reads **2026-09-18 or later**, those same-day sections stop being exempt and the arithmetic turns hard: `CHANGELOG.md` goes to **108 against 100** and `Classroomgs.changelog.md` to **52 against 50** — **rotator on both**, so re-count the second by hand rather than inferring it. Deepen the clone first so every rotated header SHA-enriches, dry-run into a copy and **READ THE SEAMS**, and run the mandatory post-rotation grep (`grep '^## \[v' <archive> | grep -v '— \[' | head -5`).

**Fourteen open items recorded and not taken.** (i) The `.cl-tbl` `min-width` question — **thirty-six** data points now, S2 session 17's 3×5 and 4×8 tables the latest and clean at 1400 px. (ii) The generated `read-next` names a landscape module without linking to it. (iii) `the-800-vdc-shift`'s stale `three-chains` intro is **twenty-eight** registrations stale. (iv) §10.6's session-4 bullet block is out of chronological order. (v) A stale `lesson_ref` can make a segment due on its own — six such segments stand today, none of them this row's. (vi) ~~The DC-arc taxonomy gap~~ — closed by row 20. (vii) `restart` scores zero for an **eleventh** session. (viii) Session 12's six `topic-aidc-landlords` terms are unseeded for a **seventh**. (ix) **(ee4)**/**(gg7)**'s dash rows — extended by S2 session 17, which found **two dashes with different mechanisms on one segment**: one suppressed by the eligibility gate onto a real absence, one where **no key in the entire 27-entry lexicon fires at all**. Two different fixes, neither an S2 session's. (x) **(ff4)**'s quiz answer-index skew; S2 session 17 used **2, 0, 3, 1, 0**. (xi) **(ff5)**/**(hh6)**/**(jj3)** — the generator publishes gated lessons' titles through `PLANNED_TITLES` **before they are written**, and `segment-utilities` leaks two unbuilt guidance lessons in one callout. **Still the developer's call — and note that THIS row is one of the leaked titles.** (xii) `Scraper.gs` holds **ten** `topic-landscape-*` seeds against **seventeen** built landscapes — shortfall seven, one developer pass. (xiii) **(jj4)**: two built timelines violate undocumented renderer contracts (`CL_LANE_COLORS` is keyed on the literal names `gen`/`deploy`/`eco`, and `clTimeline` prints `Math.floor(x)` as a year label). If this row carries a `timeline`, read it first. (xiv) **NEW — (kk8)**: `scTermsHit_()`'s word-boundary guard makes **447 of 451** singular held terms blind to their own plural, corpus-wide. A seed written for "executive order" cannot fire on "executive orders". **One developer pass, and not an S2 session's to make.**

> Author Phase 4 row 22 — the `the-china-policy-stack` mechanism lesson — on Opus 5 xhigh as a fresh session. This is ONE lesson. It authors no guidance module, no dossier and no report, it seeds no Scraper topic, and it creates no track.
>
> READ FIRST: repository-information/SESSION-CONTEXT.md (Latest Session); INTEGRATED-REMEDIATION-PLAN.md §7.50 (your brief), §7.48 and §7.49 (for the Phase 4 session signature, the inside-the-fence rule, and what S2 session 17 settled about a brief's arithmetic being usable only once you have run it, about an instrument that measures the position rather than the subject, and about the recount pass and the screenshot pass catching DIFFERENT defects with zero overlap); §7.3 orders 5 and 6, and §6. Then CLASSROOM-CURRICULUM-PLAN.md §3.5 IN FULL (your lesson's spec), §4 (the teaching order, and the market-access table in particular), §7 row 22, §8 items 2, 3, 6 and 9, §2.2, §2.3, and §10.6's whole (a)–(kk8) findings list. Then .claude/rules/classroom-app.md, repository-information/CLASSROOM-SCHEMA.md, and .claude/rules/changelog-security.md before you write any public changelog line.
>
> UNSHALLOW THE CLONE FIRST — git fetch --unshallow origin main, before any measurement and before any pin. If a container restart has left your clone behind origin/main, run git fetch --prune origin and git checkout -B <branch> origin/main BEFORE any measurement.
>
> **THE WORK — one push commit.** (1) Author `clLessonTheChinaPolicyStack_()` **INSIDE** the `// CONTENT START` … `// CONTENT END` fence in `Classroom.gs`, id `the-china-policy-stack`, group Market Access & Bankability, the section ids and order §3.5 fixes, registered at the **END** of `clLessons_()` and **INSERTED AT INDEX 4** of `clTrackMarketAccess_()`. (2) **LEAVE the module assertion in `scripts/check-classroom-content.py` AT 26** — a mechanism lesson registers no module; S2 session 17 moved it 25 → 26. (3) Regenerate **NOTHING**. (4) Flip §7.3's Phase 4 row and §6's Phase 4 row to **22 of 26**, mark curriculum §7 row 22 built with the full record, update §4's table at `market-access` position 8, and write the findings block — **the (kk…) label is spent; continue at (ll1)**. (5) Write §7.51 — the next run's brief, which by the interleave is S2 session 18, `insurance-and-risk-transfer` — in the same commit, and edit **BOTH halves** of any §7.x brief you refresh.
>
> THIS IS THE FIRST LESSON IN THE PROGRAMME GATED ON TWO MODULES, AND IT MUST TAKE THE EARLIER CLOCK. `china-policy-stack-2026-08` carries reviewBy **2026-12-31** and `eo14420-bulk-power-2026-08` carries **2026-12-24**, both read off `Classroom.gs` on 2026-09-17. **Take 2026-12-24.** Re-read both on the day before you write — (kk1) is that a brief's arithmetic is usable only once you have run it yourself, and this prompt is not exempt from its own finding. Run the nearest-gate sort anyway and record the rejections in writing, and per (kk6) run **BOTH** scans — an ISO regex and a word-form scan — and widen the scan past `policyExposure[]`, because on the last row that measured it the two scans' in-horizon results were **disjoint** and the nearest candidate was visible to only one of them and only outside the specified scope.
>
> AND THE POSITION IS AN INSERT, NOT AN APPEND — DO NOT COPY ROW 21's FORECAST. §4 puts this lesson at **position 8 of nine** in `market-access`, and **position 9 is BUILT** (row 21 appended `utility-procurement-meets-ai-load` there). Positions 4, 6 and 7 are unbuilt. So the correct write is a **MID-ARRAY INSERT at index 4** of the five ids the track carries, immediately before `utility-procurement-meets-ai-load`. **Forecast P1 AND P5.** Row 21 reported P1 alone because it appended; a session that inherits that forecast will be wrong.
>
> IT IS THE THIRD GATED LESSON PHASE 4 HAS AUTHORED AND THE SECOND IN `market-access`. Measure the denial base against head through the real `handleClassroomOp_` rather than asserting it: the track's analyst `withheld` count should move **1 → 2**, `cop=lesson` should answer `ROLE_DENIED` with **zero** section nodes rendered and the title never entering the analyst DOM, and contributor and admin should get every section. Row 21's brief got this arithmetic wrong in both halves and only the measurement caught it.
>
> PREDICT THE `--check` NUMBER BEFORE YOU WRITE. At v06.40r `--check` reads **6 due**: cells-and-chemistry, power-conversion-and-rack-power-silicon, storage-developers-and-ipps, hyperscalers-and-ai-labs, utilities, insurance-and-risk-transfer. **All six are residue from earlier rows, not yours.** Derive both halves from the parsed generator before the run — the `READ_NEXT` membership half (which segments name this lesson) and the `CRITERION_LEXICON` deepening half — and record them. (aa5) has held for twelve sessions.
>
> DO NOT: author or revise a guidance module; run `build-classroom-segments.py` with `--all` or at all; edit a generated `segment-*` literal by hand; touch Profiler.gs, Profiler.html or Scraper.gs; move the module assertion off 26. And NEVER sweep a date across Classroom.gs with a blanket find-and-replace — it holds twenty-six independently dated guidance modules and fifty-one lessons, and session 9 corrupted a comment-deadline date inside `guidanceDocEo14420_()` that way; scope every replacement to the function you are authoring.
>
> VERIFY: `node --check` on a `.js` copy of `Classroom.gs`; `scripts/check-gas-inner-scripts.js`; `scripts/check-classroom-content.py` — capture a pristine HEAD baseline BEFORE any edit. It is 0 errors / 0 warnings at **51 lessons / 8 tracks / 142 gate cases** with the module assertion at **26**, and must be 0/0 after at **52 lessons** with the assertion still at 26. Then `scripts/check-classroom-curriculum.py --strict` (no structural findings; 28 stale pins and 4 items due for review are pre-existing and not yours — landscape-cooling-2026-09 at 2026-09-28 is overdue BY DESIGN); `scripts/check-classroom-pipeline.py --selftest` (13 fixtures, 0 failures) and `--base origin/main` (expect **P1 on the plan files and P5 on the insert** — a lesson authored inside the fence should produce **no P2**, and **no P3**: if P3 fires, the gate digest needs refreshing per `.claude/rules/classroom-app.md`). `build-classroom-segments.py --check` before and after. `scripts/check-readme-tree.py` after the version bump — run it yourself, because the one-bump GAS display drift is the item [PC-GS-VERSION] #1 warns is most often missed and it fired again at v06.40r. **Playwright render of the lesson at contributor with ZERO page errors, and analyst denied at the server.**
>
> THE RENDER HARNESS, WHICH WORKS — five facts that cost S2 session 17 an hour between them. **Set `var _e` to `c2FnX18v`** (base64 of '/__gas' reversed) so the transport points same-origin; **do NOT blank it**, or the page never creates `#gas-app`, `_gasPost` has no `dataset.baseUrl`, and you get "Classroom content is not connected yet". Hide `#auth-wall` / `#signing-out-wall` / `#auth-timers` / `#gas-layer-toggle` / `#gas-app` / `#tab-takeover-wall` and add `#cl-app{display:block !important}`. **`_gasPost` puts every parameter in the QUERY STRING, not the body** — the shim must read `u.searchParams`. Carry the role in the **session-token prefix** (`contributor_` + 32 chars) and have the shim's `validateSessionForData` return `role`, `permissions` (an ARRAY) and `email` at the **TOP LEVEL** — a `{ ok: true, session: {...} }` shape silently resolves to viewer. Seed the four sessionStorage keys **after** the page loads: `Classroom_gas_session_token`, `Classroom_gas_user_email`, `Classroom_gas_user_role`, `Classroom_gas_user_permissions` (STORAGE_TYPE is `sessionStorage`). **And the page does not boot itself in a shim: after seeding, call `clAppMount()` and then `clRoute()` through `page.evaluate` — the router is gated on `clAdmitted()` and is mounted from the sign-in wrapper, so without those two calls the app makes ZERO `__gas` requests and `#cl-app` renders 0 characters.** `payload.action === 'guidance'` routes to `handleGuidanceOp_` (it reads `p.gop`); everything else goes to `handleClassroomOp_`. Serve the static files and `/__gas` from ONE Node process on ONE port at `/Classroom.html`, and call the handlers through the vm context object (`ctx.handleClassroomOp_`). Chromium is at `/opt/pw-browsers/chromium-1194/chrome-linux/chrome`; `pip install playwright` first (allow ~3 minutes, it times out on the default), and do NOT run `playwright install`. **Do not put `pkill -f "node .*shim"` in the same Bash call that starts the shim** — the pattern matches the enclosing shell's own command line and kills it; use `fuser -k -n tcp <port>` instead, and start with `(setsid … &)` inside a subshell. **Restart the shim after every `.gs` edit** — it loads the file into the vm once at startup. Re-extract the PROJECT region after every edit and DEMAND that both the rendered character count AND the PROJECT byte count MOVE — S2 session 17's went 3,678,970 → 3,679,196 bytes and 44,518 → 44,737 characters across its fix round.
>
> Write scripts to a file with a QUOTED heredoc delimiter, or bash interpolates backticks inside Python. RUN THE MICRO-MARKUP SIMULATION BEFORE THE SPLICE, and confirm every glossary entry you define is actually used. IF YOUR LESSON CARRIES A `timeline`, READ (jj4) FIRST: `CL_LANE_COLORS` is keyed on the literal lane names `gen`/`deploy`/`eco` and anything else renders faint grey, and `clTimeline` prints `Math.floor(x)` in the left gutter as a year label. **Then RECOUNT, then READ EVERY SECTION'S SCREENSHOT — twelve consecutive sessions have had a defect caught there and nowhere else, and (kk7) is that the two passes catch DIFFERENT defects with ZERO overlap: S2 session 17's recount caught five (two miscounts and THREE SUPERLATIVES ITS OWN ADJACENT CONTENT CONTRADICTED) and the screenshots then caught four more (a fence claim about "adjacent" members when one was an incumbent, a "regulatory date" line beside a statutory filing deadline, and TWICE a paragraph promising six items and enumerating five). Superlatives are the recount pass's richest vein; enumerations are the screenshot pass's. Run both, in that order.** RECOUNT every number the prose asserts about a table beside it; CHECK EVERY ENUMERATION the prose promises; and vary the quiz answer index per (ff4) — S2 session 17 used 2, 0, 3, 1, 0. **Prefer pure ASCII for the whole literal.** Bump Classroom's GAS version in BOTH `Classroom.gs` VERSION (from v01.65g) and `Classroomgs.version.txt`, with a GENERIC entry in the GAS changelog. **Scraper is NOT yours on this row** (it stands at v02.18g).
>
> THE DEPLOY: the §7.19 order, on its thirty-third use. Read Pages first (the `gs-versions/*.txt` files are static) and the workflow's own deploy confirmed lines from the job log. Probe `?op=deploy` only if those disagree, and use curl -sL if you do. **A Phase 4 row touches ONE `.gs` file, so forecast ONE step and ONE logged line** — S2 session 17 touched two. Counters measured against v06.40r's job log, recorded in §7.50's prose half. Add every intervening `.gs` push to your forecast, not just your own.
>
> THE CHANGELOG ARITHMETIC — RE-COUNT ON THE DAY. After v06.40r: `CHANGELOG.md` **108 raw with ELEVEN sections dated 2026-09-17 EST — 97 non-exempt** against a 100 trigger; `Classroomgs.changelog.md` **52 raw with SIX same-day — 46 non-exempt** against a cap of 50; `Scrapergs.changelog.md` 39 / 36; `Profilerhtml.changelog.md` 49. Run `TZ=America/New_York date '+%Y-%m-%d'` and trust it over the harness banner. **If it reads 2026-09-18 or later, those same-day sections stop being exempt: `CHANGELOG.md` goes to 108 against 100 and `Classroomgs.changelog.md` to 52 against 50** — rotator on both, so re-count the second by hand rather than inferring it. Deepen the clone first so every rotated header enriches, dry-run into a copy and READ THE SEAMS, and run the mandatory post-rotation grep (`grep '^## \[v' <archive> | grep -v '— \[' | head -5`).
>
> Normal Pre-Commit and Pre-Push checklists; claude/* branch restarted from origin/main first, git ls-remote before each push — the auto-merge workflow adds a [skip ci] SHA-tracker commit after every merge, and after it merges use `git fetch --prune origin` rather than `git fetch origin main`. End every commit message with the two attribution lines this repo's recent commits carry (Co-Authored-By: and Claude-Session:). Read `repository-information/repository.version.txt` for the version to bump from — do not take it from this prompt, whose own handover commit consumes the next number.
>
> AFTER THIS SESSION Phase 4 is 22 of 26 and S2 is 17 of ~19, so the interleave returns to S2 and the recommended next run is **S2 session 18, `insurance-and-risk-transfer`** — the v05.62r CHANGELOG record fixes it as the **eighteenth of nineteen**, with `assurance` last. It is the **smallest roster in the taxonomy at three members** (2 incumbents, 1 challenger, **zero adjacent**), which is the exact inverse of the segment S2 session 17 just wrote, and it is already among the six segments `--check` reports due. §7.51 should be written for it.
>
> Then, give me a prompt to paste into a new Opus 5 xhigh session to continue the action plan, then remember session.
