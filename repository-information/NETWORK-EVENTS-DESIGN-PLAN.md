# Network + Events — Design Proposal and Long-Term Action Plan

**Provenance.** Written 2026-09-19 (v06.70r → v06.71r) in a strategic design session on **Fable 5.1 High**, from the developer's founding brief for two new federated apps — **Network** (business-card capture + a personal, account-centric contact layer) and **Events** (an industry-wide event tracker and planner). Inputs: a full read of the Receipts app (`Receipts.html` / `Receipts.gs`, the scan → Drive → Sheets pattern Network inherits), the Profiler data model and app surfaces, the Scraper roster and corpus route, the Classroom gate surface, the ecosystem plumbing (setup script, Master ACL, peer-token routes, Routines), plus two web research passes — the business-card-scanner / personal-CRM market and the event-platform / event-data market, with a live probe of the organiser sites that matter to a BESS/AIDC seller. Every count in §1 was measured from the working tree, not recalled. **Nothing in the repo was changed by this session except this file and the housekeeping train.** This is a **proposal for a design gate**, the role `PHASE6-CLASSROOM-DESIGN.md`'s conversation played for Classroom — it becomes an executable spec only after the developer decides §3.

**Big picture.** Profiler is the first-party skeleton, Scraper is the fresh third-party blood, Classroom is the institution — and the two apps designed here are **the people layer and the calendar**: Network holds *who the developer actually knows* at the companies the corpus describes, and Events holds *where those people will be next*. The sales motion they serve is the one the corpus is built around: carrying a challenger power product into US AIDC and storage buyers with no installed base, where the whole job is building a reference book and a named-decision-maker network from zero. Ecosystem stance unchanged (developer directive 2026-08-31): federated apps, each with its own page + GAS project, synergising through public Pages data and token-gated server-to-server routes, never a monolith. Quality over build economy.

**How to use this file.** §1 is the ground truth the design stands on. §2 is what the market already builds, so the base app ships with parity before it exceeds anything. §3 is the decision list for the gate — every one carries a recommendation and the reason. §4–§5 are the two designs; §6 is how they join the other three apps; §7 applies the repo's model rule; §8–§9 are the phase plan; §10 the sequencing and preconditions; §11 the (empty) status ledger; §12 the open questions; §13 the paste-in brief for the gate session.

---

## 1 · Ground truth at v06.70r (measured 2026-09-19)

| Layer | Fact |
|---|---|
| Apps | 8 GAS projects on one consumer account (Testauthgas1, Testauthhtml1, Globalacl, MasterACL, Scraper, Receipts, Profiler, Classroom). Network + Events make **10** — the threshold at which `FUTURE-CONSIDERATIONS.md` says quota tracking must start. All ten share **20,000 script executions/day** |
| Receipts (the pattern Network inherits) | UI lives entirely in `Receipts.html` over a JSON-API `.gs`; `<input capture="environment">` → `compressImage()` (≤2000 px JPEG 0.82) → **own-Drive upload with the user's `drive.file` token** → link-only registration; extraction = **Gemini `responseSchema`** (`gemini-3.6-flash`, fallback `3.5-flash`, 3-step retry, 600 s digest cache); review card before save; record-level dedupe with "save anyway"; **hard delete, no soft delete**; `Shares` tab with owner scoping that answers *not-found* rather than *forbidden*; `.xlsx` export via a temp spreadsheet; PWA manifest behind a `manifest-src 'self'` PROJECT OVERRIDE; **no service worker by rule** (`worker-src 'none'`); effectively **no role gating** — ownership + Shares is the access model |
| Profiler | **177** dossiers, 19 segments / 319 memberships, 1,481 graph edges, 9 projects, 1,477 concepts, 8 reports. `decisionMakers[]` = **1,479 people** (name, title, background; 59 verified LinkedIn URLs; **no email, phone, or "met at" field**). `relationships[].type` ∈ competitor · customer · supplier · partner · other · investor · portfolio — **between covered companies only; there is no first-person axis anywhere** (no "my account", no target, no stage, no owner). `#network` is a **reserved Profiler route** and `network` a key in `OV_ROLE_CAPS` |
| Profiler private layer | Field notes live in the script owner's Drive (`Profiler/profiler-notes.json`, M3 2026-08-10), `sourceType` ∈ **contact · event** · call · news · other, developer-rated `confidence` 0–100. "Notes are not sources" — never cited, never lesson content, no `note:` prefix in Classroom |
| Scraper | 35 RSS outlets (12 tier-1 trade, 18 tier-2, 5 federal primary). **Zero events/calendar sources.** 45 topic seeds (`source:` ∈ `guidance:` 27 · `market` 9 · `project:` 9) + 29 segment seeds; a registered project adds a seed in the same commit. Corpus route `?action=corpus&t=<CORPUS_TOKEN>&cop=timeline|candidates`; item identity is the base36 `key`, never a URL. Scheduled AI runs still gated off (`SCRAPER_SCHED_RUNS_ENABLED = false`) |
| Classroom | `CL_ROLE_CAPS` admin/contributor/analyst (viewer not admitted). `CL_PROVENANCE_REF_KINDS` is a closed set of nine prefixes, **byte-frozen for the pipeline (P4)** and one of 32 `GATE_SYMBOLS` behind `gateDigest` (P3). C5 scenarios carry the only pipeline vocabulary in the repo: `stage` ∈ prospecting · discovery · rfp · shortlist · negotiation · post-award |
| Events already in the corpus | **31 distinct events** named in free text (DCD 49 files, GTC 33, Bisnow 14, OCP 12, RE+ 12, SNEC 11, Intersolar 11, ees 10, Computex 9, Data Center World 8, CERAWeek 8, iMasons 7 …) — every one a bare string in a headline, roadmap, spec note or source label. **No event field, slug, date, venue or registry exists.** `recentDevelopments[].category` has no event value |
| Peer routes | Two token namespaces exist and must not be reused: `CORPUS_TOKEN` (Profiler ↔ Scraper) and `GUIDANCE_PEER_TOKEN` (Profiler ↔ Classroom). `guidanceMentionsProxy_()` is the canonical template (`.trim()` the property, `upstream_not_json` vs transport failure, `not_configured` while unset) |
| Masthead convention | Since Profiler v01.90w apps do **not** carry permanent buttons to each other; what survives is content-level deep links (`Classroom.html#guidance/<id>`, `Profiler.html#<slug>`) rendered once the server has answered |
| Routines | A Routine can commit **only if created in the claude.ai UI with the repository attached** (settled 2026-09-18; `create_trigger` stores `sources: []`, and `add_repo` does not exist in a fired session). The earnings desk is rebuilt that way and awaits Monday's proof; four more rebuilds are queued. Any scheduled Events/Network job inherits this |
| Email | `MailApp` only across the repo; the Scraper digest is the mailer precedent (`getRemainingDailyQuota()` check before every send). Consumer cap **100 recipients/day**, Workspace 1,500. No `GmailApp`, no Gmail API, no `CalendarApp`, no Maps/Places anywhere |
| PII rules | The auth template runs the `hipaa` preset. Business cards carry Safe Harbor identifier categories A, B, D, E, F, N, Q. `HIPAA-CODING-REQUIREMENTS.md` §164.514(c): an identifier must **not be derived from the individual** — Receipts' `Store_Name-YYYYMMDD` id pattern is *not* transferable to people |
| Mentions of these apps | **None.** "Network app", "Events app", "business card", "CRM", "trade show" return only unrelated corpus prose. Both apps are greenfield |

---

## 2 · What the market builds — parity first, then the gap

The developer asked for the base app to have everything comparable apps have, then to build past them. Two research passes (2026-09-20, sources in the session chat) produced the two inventories below. ✅ = confirmed from a vendor page or review; ◐ = vendor claim; "inference" = this session's reasoning.

### 2.1 Business-card scanners and personal CRMs (15 apps surveyed: CamCard, ABBYY BCR, HiHello, Blinq, Popl, Covve, Sansan/Eight, Zoho Card Scanner, HubSpot, Pipedrive, Haystack, Mobilo, Linq, Contacts+, Habsy)

**Table stakes — in ten or more of the fifteen; Network v1 ships all thirteen:**

1. Photo → parsed name / title / company / phones / emails / website
2. Review-before-save with the low-confidence fields flagged
3. The card image attached to the record (front and back)
4. Save to the phone's contacts (vCard)
5. CSV / vCard export
6. At least one CRM push (we substitute: Sheets is the CRM; export covers the rest)
7. Tags / groups + free-text notes
8. Search by name and company
9. Some enrichment (company website, LinkedIn URL when known)
10. A shareable digital card of your own via QR
11. Capture that works offline and syncs later
12. Multi-language OCR (Chinese-script cards matter for this corpus)
13. A free tier with a monthly scan cap — irrelevant here, we own the stack

**Differentiators (four apps or fewer) — Network picks these up in N4:** cadence-based "who should I reconnect with" (Covve, Dex, Cloze, Mesh); auto-logged touches from Gmail/Calendar (Cloze, Affinity, folk); AI voice note → next steps (CamCard, Blinq); AI follow-up email in a chosen tone (Popl, Contacts+); relationship-strength score (Affinity — *no scanner app has it*); pre-meeting brief (Dex, Mesh); network map (Dex); "met at event" as a first-class object (only the $199+/month event-lead-capture add-ons).

**The gap nobody fills** — and the reason to build rather than buy: an **account relationship × contact role** taxonomy tied to a corpus of dossiers, Drive auto-sort by company, and an Events linkage that says *which of my target contacts will be in the hall*. None of the fifteen does any of the three (inference from the matrix; Vendelux in §2.2 is the only product near the third, at enterprise pricing).

**Extraction stack, costed per 1,000 cards:** Gemini Flash-Lite with `responseSchema` ≈ $0.7–1.1 (computed from published token rules; the free tier covers solo volume) · Claude Haiku 4.5 ≈ $2.9 · Cloud Vision `TEXT_DETECTION` $1.50 but *no field parsing* · Drive OCR $0 but slow and it creates a Doc per card · browser `TextDetector` not on Safari · Tesseract.js needs a Web Worker, which `worker-src 'none'` forbids. **Recommendation: Gemini primary (the house pattern), Claude Haiku as the fallback on 429/5xx or low confidence.** Both are third-party processors of name + email + phone — see D9.

### 2.2 Event discovery and attendee planning (Bizzabo, Cvent, Swapcard, Grip, Brella, Whova, 10times, TSNN, Eventbrite, Luma, Meetup, LinkedIn Events, ExhibitDay, Map Your Show / a2z, Expo Logic, Vendelux, ZoomInfo, Apollo)

| Feature | Organiser attendee apps | Directories | Exhibitor planners | Sales-intel (Vendelux) |
|---|---|---|---|---|
| Cross-event discovery / recommendation | ✗ (one event each) | ✓ (10times 200k, TSNN 19.5k) | ✗ | ✓ priority-scored by your account list |
| Personal agenda builder | ✓ | ✗ | per show (MYS "My Show Planner") | ✗ |
| Floor plan + booth targeting | ✓ Swapcard, Grip | ✗ | ✓ starred exhibitors pinned + printable | ✗ |
| Matchmaking / meeting booking | ✓ Grip, Brella | ✗ | ✗ | ✓ pre-booked meetings, CRM-synced |
| Attendee lists | inside the app, contact hidden | RSVP counts only | ✗ | ✓ confirmed / historic / **predicted** |
| Calendar sync / ICS | ✓ | Luma ICS | ✓ | ✗ |
| Budgets / ROI per event | ✗ | ✗ | ✓ ExhibitDay | ✓ CRM attribution |
| Post-event follow-up workflow | partial | ✗ | ✓ tasks | ✓ |

**Table stakes for Events v1:** starred-event calendar with ICS and Add-to-Google-Calendar links, a detail page (venue, dates, times, organiser, website), session / speaker lists where published, reminders, the floor-plan link and exhibitor list, notes. **Differentiators nobody bundles** (inference): discovery + attendance-signal fusion below the mega-convention tier; an auto-generated day plan whose booth list is *reasoned from the dossiers and the developer's own account tags*; open meeting slots with nearby venues; and **source transparency** — every event shows which feed produced it and when it was last confirmed.

**Vendelux is the closest analogue** to what the developer described (its Event Graph fuses registration pages, agendas, speaker bios and public "attending" posts into confirmed / historic / predicted attendees, then matches an uploaded account list to upcoming shows). It is enterprise-priced and organiser-agnostic; Events reproduces the legally clean two-thirds of it (§5.5) for this one industry.

**Event data, probed live on 2026-09-20 from organiser sites:** JSON-LD `Event` markup is present on The Battery Show NA, DISTRIBUTECH, Data Center World, POWERGEN, Yotta and Intersolar/IESNA; absent on RE+, CLEANPOWER, Energy Storage Summit USA, PTC, DCF Trends, GCPA, NARUC (HTML only). ICS feeds: 7x24 Exchange (`?ical=1`), the International Battery Seminar, Luma calendars. **Bot walls that `UrlFetchApp` cannot pass:** 10times (Cloudflare, and its ToS reserves data for licensed partners), DCD, OCP, Enlit — these become hand-maintained registry rows, never fetches. Eventbrite's public search API is gone (2020); Meetup's needs Pro; LinkedIn's Events API returns only your own organisation's events. **A 64-row verified calendar for 2026-Q4 → 2027 came out of the pass** and seeds E0 (§5.2).

**Attendance signals, ranked by reliability and legality:** exhibitor directories (high for *companies*; Map Your Show pages render server-side) · speaker rosters (high for named people) · "visit us at booth #" press releases via PR Newswire / Business Wire / GlobeNewswire RSS (high, biased to vendors) · organiser attendee lists (gated to paying sponsors; Bisnow's robots.txt blocks `/events/attendee-list/*`; Uptime bars vendors outright) · **LinkedIn posts — the highest-value signal for target *contacts* and the one that cannot be automated**: the User Agreement (§8.2.2, §8.2.4, §8.2.13) forbids it, *hiQ v. LinkedIn* ended in a $500k consent judgment and permanent injunction against the scraper, and no LinkedIn API is open to an individual. **Design consequence (D11): LinkedIn intel enters Network by hand — a "paste the post URL" field — exactly as field notes enter Profiler.**

---

## 3 · Decisions — approved 2026-09-20 (gate NE0, Fable 5.1 xhigh)

Every row now carries a decision. The gate walked the eleven open rows one at a time on 2026-09-19 → 20 in §13.1's order (D4 · D5 · D7 · D8 · D9 · D6 · D2 · D14 · D17 · D12 · D16); the six earlier rows were not re-opened. Where the developer overrode or the gate departed from the proposal, the row says so and why — no re-argument. The "Recommendation" column is kept as written so the record shows what was proposed against what was decided.

| # | Decision | Recommendation | Why |
|---|---|---|---|
| **D1** | **Names.** `Network` collides with Profiler's reserved `#network` route, its `network` capability key and the `ovNx*` prefix | **Keep "Network" and "Events."** The collision is confined to `Profiler.html`'s own namespace — `Network.html` owns its own hash routes, its capabilities are `NW_ROLE_CAPS`, its functions `nw*`. In N0, relabel Profiler's explorer button from "Ecosystem Network" to **"Ecosystem Graph"** (a label, not a key — `verify-profiler-roles.py` is untouched). Alternative if the developer prefers zero overlap: "Rolodex" | The developer chose the names; a label change costs one line and removes the only user-facing confusion **DECIDED 2026-09-19 (developer):** resolve the collision by renaming Profiler's explorer to **"Ecosystem"** — the `#network` heading (`Profiler.html` line ~3519), the denied-view sentence, and the matching label assertion in `scripts/verify-profiler-roles.py` (line ~364) change together in **N0**; the `network` capability key and the `#network` hash stay (keys, not labels). App names stay Network and Events |
| **D2** | **Form factor** | Two own apps via `setup-gas-project.sh` from the **auth** template (`hipaa` preset, Master ACL, deploy webhook), each with a **PWA manifest** (`network.webmanifest`, `events.webmanifest` + icons, added by hand — the script does not generate them) and the `manifest-src 'self'` PROJECT OVERRIDE. **No service worker**, per the repo's `worker-src 'none'` rule; installability without offline caching, exactly as Receipts | Both are used on a phone in an exhibition hall; both hold data the sign-in wall must protect **DECIDED 2026-09-20 (developer): approved as recommended.** Two auth-template apps (`hipaa` preset), hand-made manifests + icons with the `manifest-src 'self'` override, no service worker. Recorded limit: without a service worker the installed app has no cached shell, so the IndexedDB offline queue covers signal lost *mid-session*, not a cold launch in a dead hall |
| **D3** | **Where the data lives — the decisive fork** | **Everything about people is private; the event registry is public.** Contacts, card images, interactions, plans, attendance signals about *named people*, and account tags live in **Drive + the app's own spreadsheet behind the Master ACL**, served only by `Network.gs` / `Events.gs` — the field-notes architecture (M3). The **event registry** (`live-site-pages/events-data/events.json` + `events-sources.json`) is public-safe and lives on Pages like `profiler-projects.json` | `live-site-pages/` is public even on a private repo; M3 moved exactly this class of data out for that reason. Two registries on Pages cost nothing and let Profiler, Classroom and Scraper read events with one `fetch` **DECIDED 2026-09-19 (developer): approved as recommended.** |
| **D4** | **Account-centric, not card-centric** | The primary object is the **Account** (a company), which owns **Contacts** (people), which own **Cards** (images) and **Interactions** (touches). An Account links to a Profiler `slug` when the company is covered (resolved through the registry's `aka[]` + `domains[]`); uncovered companies are local Accounts carrying a one-tap **"propose a dossier"** hook that emits the `profiler <Company>` command text | Every feature the developer asked for — filter by company type, chase targets, event recommendations — hangs off the account, and the dossier corpus attaches at that level, not the person **DECIDED 2026-09-20 (developer): approved as recommended.** A Contact holds exactly one current `Account ID`; a change of employer is an `account-change` Interaction carrying the previous id, so history is kept without a many-to-many table. Shapes: `NETWORK-SCHEMA.md` §3 |
| **D5** | **Taxonomy** | Account `relationship` ∈ **customer · partner · supplier · target · competitor · channel · other**; Account `stage` ∈ **none · prospecting · discovery · rfp · shortlist · negotiation · post-award · won · lost** (the C5 enum plus the three terminal states); Contact `role` ∈ **decision-maker · economic-buyer · technical-evaluator · point-of-contact · champion · influencer · gatekeeper · procurement · peer · other**; Contact `warmth` is **computed** from interactions, never stored. Both enums live in `NETWORK-SCHEMA.md` and are mirrored server-side (flat enum for Gemini) and client-side (grouped map), the Receipts `RECEIPT_CATEGORIES` / `SUBCATS` idiom | Aligning `stage` with C5 means a scenario lesson and a live account speak the same word for the same moment in a sale **DECIDED 2026-09-20 (developer): approved as recommended, with two refinements.** `relationship` stays single-valued and a secondary facet goes in a new Account `Tags (JSON)` column; `stage` may be anything other than `none` only when `relationship` ∈ `target` · `customer` (validator rule). Enums, labels and the two mirrors: `NETWORK-SCHEMA.md` §4 |
| **D6** | **Extraction** | **Gemini `responseSchema` primary** (the Receipts pattern, model pinned by constant, key in Script Properties), **Claude Haiku 4.5 fallback** on 429/5xx or when any required field's `confidence < 0.7` (a new per-field confidence array the card schema carries, which Receipts lacks). Front and back captured as a **pair** in one review. No Tesseract (needs a Worker) | One `UrlFetch` per card, ≈ $1 per 1,000, and the review card already exists in Receipts **DECIDED 2026-09-20 (developer): Gemini only — departs from the proposal.** No Claude Haiku fallback leg and no `ANTHROPIC_API_KEY`: Receipts' three-leg Gemini retry plan already covers 429/5xx, and a second processor of every card's name + email + phone for a ≈ $0.003-per-card benefit runs against D9's minimum-necessary posture. A low-confidence field is outlined on the review card with "Retry extraction", never sent to a second vendor automatically. **§12.1 answered: Gemini may process card PII.** The Claude leg stays available as an N1 add-on if outages ever bite. Schema: `NETWORK-SCHEMA.md` §7 |
| **D7** | **Access tiers** | **Network:** admin + contributor admitted; analyst and viewer turned away at the door (a personal address book has no read-only audience). Ownership + a `Shares` tab decide what a contributor sees (the Receipts model, verbatim). **Mass email and any AI-backed action are admin-only** — they are budget surfaces. **Events:** admin + contributor + analyst; viewer excluded; starring and plans are per-account; the source roster and poller controls admin-only | Mirrors Classroom's "viewer not admitted" decision and Profiler's "budget triggers stay admin-only" rule **DECIDED 2026-09-20 (developer) — OVERRIDE: Network and Events start admin-only.** The developer will widen the tiers if they ever want to share. `NW_ROLE_CAPS` / `EV_ROLE_CAPS` keep all four tier keys so widening is a map edit; `nwAdmitted_` / `evAdmitted_` admit `admin` only and the verifiers assert the other three tiers are turned away; the `Owner` column and the Receipts ownership machinery stay as the widening path, `Shares` is created without UI; the contributor scan cap discussed at the gate is not built. **§12.4 settled by this: no team layer.** Matrices: `NETWORK-SCHEMA.md` §2, `EVENTS-SCHEMA.md` §2 |
| **D8** | **Identity and deletion** | Contact and Account ids are **opaque random** (`c-<base36>`, `a-<base36>`), never derived from a name — §164.514(c). **Soft delete** (`Deleted At` column, purge admin-only after 30 days), because Receipts' hard delete is wrong for a relationship record | A person's record is the one thing here that gets un-deleted **DECIDED 2026-09-20 (developer): approved as recommended.** Ids are a prefix (`a-` `c-` `i-` `s-` `d-` `m-`) + 13 base36 characters from 8 random bytes, collision-checked, never name-derived; soft delete with one-tap restore; admin-only purge after 30 days trashes the card images **browser-side** with the user's own `drive.file` token (they live in the user's Drive, so GAS cannot) and then deletes the rows; an Account with live Contacts cannot be deleted — nothing cascades silently. `NETWORK-SCHEMA.md` §1, §13 |
| **D9** | **Privacy posture** | Columns `Source Event`, `Consent Marketing` (yes / no / unknown, default unknown), `Do Not Contact`; every mass email carries an unsubscribe line and a postal address; list ops return the **minimum-necessary** subset and detail ops the full row; audit rows log event + user, **never card contents**; a share or export writes a disclosure row (the template's §164.528 machinery). Retention: a **review list** of contacts with no touch in 36 months — surfaced, never auto-purged (user-owned data) | GDPR legitimate-interest and CAN-SPAM cost nothing when built in; CCPA's thresholds almost certainly do not apply to a sole seller but deletion requests are honoured anyway. **Gemini/Claude as processors of third-party PII is a judgment the developer should make consciously**: Receipts already sends merchant addresses to Gemini, and neither vendor trains on API data by default, but a card is a *person* **DECIDED 2026-09-20 (developer): approved as recommended, made precise.** `Do Not Contact` excludes from every draft and export; `Consent Marketing = no` excludes from bulk drafts; `unknown` is allowed for the two D15 mail occasions (post-event follow-up, reconnect nudges) because a handed-over card is an invitation to follow up; every draft template carries an unsubscribe line and a postal address by default; audit rows carry ids and counts, never a card field; exports write disclosure rows; a deletion request is the D8 path and an access request is the per-contact vCard export. `NETWORK-SCHEMA.md` §10, §12 |
| **D10** | **Events source strategy** | **Hybrid, in three layers.** (1) A **corpus extraction pass** structures the 31 events already named in dossiers (cheap, deterministic). (2) **One Opus 5 xhigh research session** builds the verified registry (~60 events with organiser URL, JSON-LD/ICS availability, tier, audience, exhibitor/speaker-list URLs) from the 64-row calendar this session produced. (3) A **weekly no-AI GAS poller** refreshes dates/venues from JSON-LD and ICS and queues diffs for admin approval; a **quarterly discovery Routine** (UI-created, repo attached) proposes new events. The developer asked Medium / High / xhigh: research is Opus's job under the model rule; the extraction and poller code are Fable High; the roster bookkeeping is Fable Medium | Independent research finds the sub-mega tier (Infocast, GCPA, Bisnow regionals, iMasons socials) the corpus never names; the corpus supplies the relevance judgment research cannot **DECIDED 2026-09-19 (developer): approved as recommended** — Opus 5 xhigh for the registry research, Fable High for the extraction and poller code, Fable Medium for the roster |
| **D11** | **Attendance-signal stack** | Exhibitor-directory diff (weekly) + speaker-roster diff + press-release RSS keyword watch (`<company> AND booth`) + Luma ICS for socials + **manual LinkedIn capture** (paste URL + one line + confidence). **No scraping** of LinkedIn, 10times, or organiser attendee lists. Every signal stores `evidenceUrl`, `firstSeen`, `kind`, `confidence` so the app can always say *why it thinks they are going* | The only stack that survives a ToS read; it is also two-thirds of Vendelux **DECIDED 2026-09-19 (developer): approved, and widened to the full source catalogue in §5.5.1** — every source listed there is in scope, the quoted-people extraction over Scraper's articles explicitly included (its route is D17) |
| **D12** | **Plans** | **v1 is deterministic**: booths ranked from account relationship × stage × segment membership × live signals, each with a "why" line lifted verbatim from the dossier (`strategyRead[]`, `recentDevelopments[]`, `relationships[].note`); sessions ranked by segment keywords; a day plan with the show's hours, the ranked visits, **open slots**, and nearby venues from Places Nearby Search (Pro tier, free at this volume). **The narrative plan is session-authored**: an `events plan <event>` command (Opus 5 xhigh) writes a private plan file to the developer's Drive. **In-app runtime AI stays deferred**, the ecosystem's standing decision since 2026-08-22 | A deterministic plan is explainable and free; the narrative one is where model quality shows and is worth a session **DECIDED 2026-09-20 (developer): approved as recommended, with one departure — venues come from OpenStreetMap Overpass** (free, keyless, no billing account) rather than Places. **§12.5 answered: no Google Cloud project with billing exists**; Places stays a `PLACES_API_KEY` option in E5 with the same normalised shape. The `events plan` command writes to Drive through the session's Drive connector when it is attached and otherwise hands the plan back as text to paste into the event's Plans row. `EVENTS-SCHEMA.md` §6, §10 |
| **D13** | **Classroom hook** | **An `event:` prefix over the public registry is defensible; a `contact:` prefix is not and is never proposed.** Defer the prefix until E0's registry has survived one poller cycle. It is a gate-surface change: `CL_PROVENANCE_REF_KINDS` + `CLASSROOM-SCHEMA.md` mirror + `gateDigest` refresh in the same commit. The teaching value is real but narrow — a **pre-event briefing lesson** ("who from your segments exhibits at RE+ and what the record says") — and the developer said not to force it | The absence of `note:` is the precedent: private first-person intel never becomes lesson content **DECIDED 2026-09-19 (developer): approved as recommended** — `event:` deferred behind E0, `contact:` never |
| **D14** | **Quota discipline** | Network `HEARTBEAT_INTERVAL` 600 s, `DATA_POLL_INTERVAL` 60 s; Events **no data poll** (the registry is static and starred state is fetched on demand), heartbeat 600 s; start the audit-row quota counter `FUTURE-CONSIDERATIONS.md` describes, on all ten projects, in N0 | Ten projects on one 20,000-execution/day account; a 15 s poll on two more apps is 11,520 executions/day for one open tab each **DECIDED 2026-09-20 (developer): approved as recommended, tightened.** **No `DATA_POLL_INTERVAL` in either app** — data is fetched on load, on `visibilitychange` and after every write, because under D7 there is one user and refresh-on-focus already gives cross-device sync (Receipts and Classroom poll every 15 s = 5,760 executions/day per open tab); heartbeat 600 s in both. The execution counter is an `op=quota` API defined once in N0 (shared template region of Network and Events) and rolled to the eight existing projects by **one Fable 5.1 Medium session (Q0)** in a single commit, so Q reads a full month across all ten. **§12.2 answered: stay on the consumer account** — a Workspace move is a re-deploy of every project under a new script owner, decided at Q on measured data |
| **D15** | **Mass email — who sends** | **The browser sends, as the developer, through the Gmail API with the user's own `gmail.send` token** — the own-Drive upload pattern applied to mail. Per-recipient raw-MIME sends with `List-Unsubscribe`, merge fields from the contact row, a review-every-recipient step, throttled to the account's own Gmail limit (500/day consumer). `Network.gs` never sends mail and never sees the token | `MailApp` would send *from the script account*; the Apps Script cap is 100 recipients/day; and mail must leave the developer's own mailbox to land in the reply thread **AMENDED 2026-09-19 after the developer asked when mail is actually sent:** neither app sends digests. The only occasions are *post-event follow-up* (one templated note to each card scanned at a show, 20–60 at a time) and *reconnect nudges* (a handful a week). **v1 therefore creates Gmail drafts, not sends** — the browser calls `gmail.compose` `drafts.create` per recipient with the merge fields filled, and the developer reviews and sends from Gmail itself. The send path above is kept as an N3 option, off by default; `MailApp` stays ruled out for the reasons given **DECIDED 2026-09-19 (developer) — supersedes both versions above: the apps never send and never touch Gmail.** The developer mails from a work account that changes with the employer. v1 creates **in-app drafts** — one per recipient, merge fields filled from the contact and account rows, editable in the app — and hands them off by **export** (`.eml` bundle, `.txt`/CSV with one column per recipient, and per-draft **copy to clipboard** with subject and body) or by a `mailto:` link that opens whatever client is the default. No `gmail.*` scope, no `MailApp`, no Gmail API anywhere in either app. Sent-ness is recorded by the developer marking a draft sent, which writes the `email-out` interaction |
| **D16** | **Order of build** | Gate → Network N0–N2 → Events E0–E1 → **the bridge B** → Network N3 → Events E2–E3 → Events E4 → Network N4 → Events E5 → X. Network first because the bridge and the recommendation engine both read account tags that only Network holds; the bridge comes before the recommendation engine because the score reads those tags over it; Events' registry (E0) is research and can run in parallel with N1–N2 on a different model | The critical path is Network's account model, not either app's UI **DECIDED 2026-09-20 (developer): approved as recommended.** Gate → N0 → **Q0** → N1 → N2 → E0–E1 → B → N3 → E2–E3 → E4 → N4 → E5 → X, with E0 (Opus research) beside N1–N2 and E1 beside N3, the lanes joining at B. Dated target: **N1 (card capture) and E1 (the calendar) both live before RE+ 2026 opens on 2026-11-16**, the first show cards will be scanned at |
| **D17** | **The people route — where the quoted-people extraction runs and how Network reads it** | **Scraper extracts, Network reads over a new peer token.** Scraper already calls the model once per ingested article; its summarisation output schema gains `people[] { name, title, company, role ∈ quoted\|author\|named, context }`, stored in the item's signals blob at near-zero extra spend. The corpus route gains `cop=people&slug=<registry slug>&since=` returning `{ key, publishedAt, source, people[] }` per item. Network reads it through its own proxy behind a **new `NETWORK_CORPUS_TOKEN`** set in both projects ("different peers, different secret"), gated by `nwCan('signals')`, and writes matches to the `Signals` tab as `kind: press-quote` with the article `key` as evidence. Alternative: route through Profiler's existing corpus proxy — rejected because it makes Profiler a hop for another app's private need and widens `CORPUS_TOKEN`'s consumer set by the back door | The press Scraper already pays to read is the cheapest legal source of named people at target accounts; the peer-token pattern is the ecosystem's only sanctioned private channel **DECIDED 2026-09-20 (developer): approved as recommended.** `people[]` accrues per Scraper summarisation pass, so back-fill of older items is a one-time admin job in E4 s3, not automatic. Route shapes and the `press-quote` signal row: `NETWORK-SCHEMA.md` §9 |

---

## 4 · Network — the design

**One sentence.** Network is Receipts' scan → Drive → Sheets → PWA pattern with a person instead of a purchase in the record, an account layer over the people, the dossier corpus attached at the account, and a privacy posture that treats every row as a third party's data.

### 4.1 Objects and tabs (`Network.gs`, one spreadsheet; `ensureNetworkTabs_()` with the Receipts in-place header-upgrade idiom)

| Tab | Columns (first cut — `NETWORK-SCHEMA.md` is the single source of truth once N0 writes it) |
|---|---|
| `Accounts` | Account ID · Owner · Name · Normalised Name · Domain · Profiler Slug · Relationship · Stage · Segment IDs · HQ · Notes · Created At · Updated At · Deleted At |
| `Contacts` | Contact ID · Owner · Account ID · Full Name · First · Last · Title · Role · Emails (JSON) · Phones (JSON) · Address · LinkedIn · Website · Source Event · Met Date · Consent Marketing · Do Not Contact · Tags (JSON) · Notes · Card Front Link · Card Back Link · Raw Extraction · Created At · Updated At · Deleted At |
| `Interactions` | Interaction ID · Owner · Contact ID · Account ID · Kind (scan · meeting · call · email-out · email-in · calendar · note · linkedin) · Date · Summary · Evidence Link · Event Slug · Created At |
| `Signals` | Signal ID · Owner · Account ID · Contact ID (optional) · Event Slug · Kind (exhibitor · speaker · press-release · linkedin-manual · luma · other) · Evidence URL · First Seen · Last Seen · Confidence · Note — **written by Events over the bridge (§6), read by both** |
| `Shares` · `Profiles` | Verbatim from Receipts (grant by email, `view`/`edit`, 20 per owner; Drive folder id + display name) |

Owner scoping, `resolveOwnerScope_` / `resolveOwnerSet_`, and *not-found-not-forbidden* are copied verbatim from `Receipts.gs`. Emails and phones are JSON arrays in one cell because a card carries several and the review card already has repeatable rows.

### 4.2 Capture → extraction → review → save (N1)

- **Capture**: the Receipts inputs (`capture="environment"` single, `multiple` batch of 15) plus a **front/back toggle** so one review card holds a pair; `compressImage()` unchanged at 2,000 px (cards are small but Chinese-script cards are dense).
- **Offline**: when `navigator.onLine` is false, the compressed base64 pair is queued in **IndexedDB** (no Worker needed) and drained on reconnect with the same pipeline — the one table-stakes feature Receipts lacks.
- **Upload**: own-Drive with the user's `drive.file` token into **`Network App/<Company>/`** — the auto-sort the developer asked for; the folder is created on first save (the company name is known only after extraction, so the file uploads to `Network App/_inbox/` and **moves** on save via a Drive `files.update` with `addParents`/`removeParents`). Filenames `<ContactID>-front.jpg` / `-back.jpg` — opaque, per D8.
- **Extraction** (`nwExtractFromBase64_`): the Receipts function with a new schema — `fullName`, `firstName`, `lastName`, `title`, `company`, `department`, `emails[]`, `phones[]{kind,number}`, `address`, `website`, `linkedin`, `socials[]`, `languages[]`, `rawText`, and **`confidence{field: 0–1}`**; prompt rules for Chinese/Japanese/Korean cards (romanise, keep native script in parentheses — the `decisionMakers[].name` convention), two-sided cards, QR codes present on the card (decode client-side with the browser `BarcodeDetector` where available; it needs no Worker).
- **Company resolution** at review time: normalise the company string, match against `profiler-companies.json` `name` / `aka[]` / `domains[]` (one public fetch, cached page-lifetime) → propose the Profiler slug and pre-fill `segments` from the registry; otherwise propose a new local Account.
- **Dedupe**: on normalised email, else phone, else name + account → **offer merge** (field-by-field, newest wins by default, both card images kept), never a silent reject.
- **Review card**: the Receipts card with the role/relationship selects, the low-confidence fields outlined, "Retry extraction" and "Swap front/back".
- **Save**: writes Contact + (new or existing) Account + one `scan` Interaction stamped with **`Source Event`** — which defaults to whatever Events says the developer is attending today (§6, the bridge's first use).

### 4.3 The list, the filters, the exports, the mail (N3)

- **List** = the Receipts History card generalised: search (name, company, title, email), filters on relationship, stage, role, segment, source event, tag, met-date range, "has consent"; sort by name / company / last touch / warmth; per-row expand → the contact detail with both card images, the account's relationship badge, and a **`Profiler.html#<slug>` deep link when the account is covered** (rendered after the registry answers, per the masthead convention).
- **Bulk actions** (Receipts has none): multi-select → tag, set relationship/stage, export selection, start a mailing, soft-delete.
- **Exports**: `.xlsx` via the Receipts temp-spreadsheet path (Contacts / Accounts / Interactions sheets); **CSV**; **vCard 3.0** per contact and as a bundle (hand-rolled text, no library — the house style, cf. `_buildJpegPdf`); the vCard carries the card image as `PHOTO` when the user opts in.
- **Follow-up drafts** per D15 (decided): pick recipients → choose or write a template with merge fields (`{{first}}`, `{{company}}`, `{{metAt}}`, `{{lastTopic}}`) → the app renders **one editable draft per recipient** in a review list → hand off by `.eml` bundle export, one-column CSV/`.txt` export, per-draft copy-to-clipboard (subject + body), or a `mailto:` link for the default client → marking a draft "sent" writes an `email-out` Interaction. **The app never sends and holds no mail scope.** Admin-only, because it is the one bulk surface.
- **Your own card**: a QR code (client-rendered, no library or the small inline generator already used elsewhere in the repo if one exists — check before adding) that encodes a vCard of the developer, on a "My card" panel; tap to show full-screen in the hall.

### 4.4 Relationship intelligence (N4 — the differentiators)

- **Warmth** (computed): a decayed sum of Interactions weighted by kind, shown as a chip; **"reconnect" list** = contacts whose `cadence` (per role × relationship, e.g. champion at a target = 30 days, supplier = 180) has lapsed.
- **Imported touches** (amended at the gate, 2026-09-20 — a D15 consequence): the proposal's opt-in `GmailApp` / `CalendarApp` sweep is **dropped**. D15 rules out any `gmail.*` scope, and the sweep was wrong on its own terms anyway — `GmailApp` and `CalendarApp` run as the *script* account, while the developer mails and meets from a work account that changes with the employer, so it would have read the wrong mailbox. v1 instead has an **import panel**: an `.ics` export or a sent-mail CSV pasted in, matched to contacts by email, each row confirmed by the developer before it becomes an `email-in` / `email-out` / `calendar` Interaction with the developer's own reference as evidence. No new scopes, no trigger, no consent prompt.
- **Pre-meeting brief**: one tap on a contact → a compact Word export (the `ovRelWordExport` precedent) that stitches the contact timeline, the account's tags and notes, the dossier's `strategyRead[]` and last five `recentDevelopments[]`, open Signals, and the pipeline stage.
- **"On the record" check**: if a contact's name + company match a `decisionMakers[]` entry in the dossier, show it (title as the record has it, background bullets) — the corpus verifying the card, not the other way round; the card never edits the dossier.
- **Promote to field note**: an Interaction can be copied into the Profiler intake as a `sourceType: contact` note with the developer's confidence score — one-way, developer-triggered, the existing private layer unchanged.
- **Network map**: a company → contacts → shared-events graph in the page (vanilla SVG; the ecosystem graph explorer is the precedent for the interaction model).

### 4.5 What Network deliberately does not do

No two-way CRM sync (Sheets *is* the CRM; exports cover HubSpot/Salesforce imports). No in-app AI email writing in v1 (a budget surface; revisit after D12's narrative-plan command proves the session-authored route). No LinkedIn automation of any kind. No editing of dossiers. No public data — nothing under `live-site-pages/` except the page, its manifest and icons.

---

## 5 · Events — the design

**One sentence.** Events is a public event registry with its own probed source roster, a private per-account layer of stars, plans and signals, a calendar-first phone UI, and a recommendation engine that reasons from Profiler segments and Network accounts rather than from popularity.

### 5.1 The public registry (`live-site-pages/events-data/`, E0)

`events.json` — `{ schemaVersion: 1, built, events[] }`, each event modelled on `profiler-projects.json`:

```
{ slug, name, organiser, series, kind ∈ mega|conference|regional|social|corporate-summit|webinar,
  start, end, tz, city, region, country, venue, website, registrationUrl,
  exhibitorListUrl?, agendaUrl?, speakersUrl?, floorPlanUrl?,
  audience[] (segment ids from profiler-segments.json), relevance 1–5, tierNote,
  editions[] { year, start, end, city, venue },
  sources[] { sourceKey, kind ∈ jsonld|ics|html|manual, url, lastConfirmed },
  mentions[] { slug, where } (dossiers that name it — derived by the build script, never hand-kept),
  status ∈ confirmed|tentative|cancelled|past, lastUpdated }
```

`events-sources.json` — the **source roster**, one row per organiser/feed: `{ key, name, url, feedKind, robots: allowed|disallowed|unknown, lastProbe { at, status, itemCount, newestItem }, cadence, notes, blocked?: reason }`. The `scraper-sources.md` discipline applies verbatim: **probe live before adding** (HTTP status, body shape, item recency), record every unavailable outlet with a reason so it is never re-proposed (10times: Cloudflare + partner-only ToS; DCD, OCP, Enlit: 403 to non-browser clients — hand-maintained rows), and never substitute a Google-News feed for a dead source.

**Bootstrap:** `scripts/extract-corpus-events.py` walks every dossier's `recentDevelopments[]`, `productsAndServices[]`, `technicalSpecs[]` and `sources[]` for the 31 known event strings, emitting `mentions[]` and a seed row per event; the Opus research session (E0) then verifies each against the organiser site and adds the sub-mega tier the corpus never names. `scripts/check-events-registry.py` asserts slugs, dates, segment ids, that every `mentions[].slug` resolves, and that a `sources[].url` host appears in the roster. The registry's counts are never taught (the corpus-wide-count corollary).

### 5.2 The seed calendar this session verified

Of the 64 rows the research pass produced for 2026-Q4 → 2027, the ones a BESS/AIDC seller cannot miss (relevance 5, dates read from the organiser's own site unless marked U): Data Center World Power (Dallas, Sep 21–23 2026) · ACP RECHARGE (Aurora CO, Sep 22–24) · Yotta (Las Vegas, Sep 28–30) · The Battery Show NA (Detroit, Oct 12–15) · OCP Global Summit (San Jose, Oct 12–15, U) · 7x24 Exchange Fall (San Antonio, Oct 25–28) · DCD>Connect Virginia (Leesburg, Nov 3–4, U) · **RE+ 2026 (Las Vegas, Nov 16–19)** · NVIDIA GTC DC (Nov 30–Dec 3) · POWERGEN (Salt Lake City, Jan 18–21 2027) · Intersolar & ESNA (San Diego, Feb 9–11) · **DISTRIBUTECH (Atlanta, Mar 1–4)** · GTC 2027 (San Jose, Mar 15–18, U) · International Battery Seminar (Orlando, Mar 15–18) · Energy Storage Summit USA (Dallas, Apr 20–21) · DCF Trends + Microgrid Knowledge (Glendale AZ, May 5–7) · **Data Center World 2027 (Nashville, May 24–27)** · **CLEANPOWER 2027 (Anaheim, May 24–27)** · 7x24 Spring (Orlando, Jun 6–9). The regional and social tier the corpus never mentions and the developer's brief explicitly wants: Infocast PowerUp Data Centers / ERCOT Power NEXT / Southeast Power NEXT, GCPA Fall, Bisnow's DICE and regional data-center days, iMasons chapter socials (Luma-hosted). The full 64-row table with tiers, exhibitor/speaker-list availability and JSON-LD/ICS flags is **Appendix A** — E0's input.

### 5.3 The app (`Events.html` / `Events.gs`, E1–E2)

- **Calendar UI**: a **vanilla agenda scroller** — sticky month header, an infinite day-grouped list driven by `IntersectionObserver`, tap → detail sheet; a second "day plan" tab renders a timeline. Not FullCalendar (v6 injects `<style>` at runtime and needs a per-response nonce a static page cannot mint). Filters: relevance, kind, region, segment, starred, "signals only". **Add to Google Calendar** links (`calendar.google.com/calendar/render?action=TEMPLATE…&ctz=`) and **ICS download** per event (RFC 5545 text, hand-rolled); a published `events.ics` next to the page for `webcal://` subscription.
- **Stars, notes, plans, attendance** are per-account rows in Events' own spreadsheet (`Stars`, `Plans`, `Meetings`), owner-scoped like Receipts.
- **The poller** (E2): a weekly time-driven trigger walks `events-sources.json`, fetches JSON-LD / ICS with `UrlFetchApp`, normalises, and writes **diffs** (new edition, moved dates, changed venue) to a `Proposed` tab for admin approval; approved diffs are applied to the registry **by a session, not by GAS** — the registry is a repo file and a fired session cannot push (the Routines lesson), so the queue lives in Sheets (the Scraper pattern) and the write happens on the developer's next `events sync` command. No AI in the poller.
- **Discovery Routine** (quarterly, UI-created with the repo attached, later): reads the roster and the Proposed tab, researches new candidates, opens them as proposals. It follows STEP 0 of every committing Routine.

### 5.4 Recommendation (E3)

A score per event, explainable line by line in the UI: **segment fit** (audience ∩ the developer's seats' segments) + **account presence** (Network accounts with `target`/`customer`/`partner` relationship that have a live Signal for the event, weighted by stage) + **corpus salience** (`mentions[]` count, decayed) + **proximity/cost** (region preference, optional) − **conflict** (overlaps a starred event). The weights live in a `Tuning` tab the admin can edit; defaults are written down in `EVENTS-SCHEMA.md`. The "why" panel lists the contributing rows — including the Network contacts by name, which is why this surface is contributor-gated and the score is computed **in Events.gs from data fetched over the bridge**, never in the page from a public file.

### 5.5 Attendance signals (E4 — the bridge's payload)

Weekly, per **starred or top-scored** event: diff the exhibitor directory (Map Your Show / a2z pages parse server-side; store company names only), diff the speaker roster (names + companies), and search the three newswire RSS feeds for `<account name> AND (booth OR exhibit OR "will present")` for every Network account tagged target/customer/partner. Each hit becomes a `Signals` row **in Network's spreadsheet** via the bridge route — with `evidenceUrl`, `firstSeen`, `kind`, `confidence` (exhibitor 0.9, speaker 0.9, press release 0.8, Luma 0.6, manual as rated). The developer's own LinkedIn reads enter through Network's manual capture. Signals expire with the event.

#### 5.5.1 The signal-source catalogue (approved 2026-09-19 — every row is in scope)

| # | Source | What it yields | How it is read | Phase |
|---|---|---|---|---|
| 1 | Exhibitor directories (Map Your Show, a2z, organiser HTML) | companies, sometimes booth staff and booth numbers | weekly server-side diff per starred/top-scored event | E4 s1 |
| 2 | Speaker and session rosters | named people with titles and companies, session times | weekly diff; JSON-LD `performer` where present, HTML otherwise | E4 s1 |
| 3 | Newswire RSS (PR Newswire, Business Wire, GlobeNewswire) | "visit us at booth", "will present", named presenters | keyword watch per target/customer/partner account | E4 s1 |
| 4 | Company newsroom and "events / meet us at" pages | shows the company will attend, sometimes who | monthly fetch per target account (URL kept on the Account row) | E4 s2 |
| 5 | **Quoted people in the trade press Scraper already ingests** | executives quoted or bylined, with title and topic, dated | Scraper extracts at summarisation time; Network reads `cop=people` over the D17 route | E4 s3 (Scraper side) + N4 |
| 6 | Conference agendas (PDF/HTML) and session recordings (YouTube channels of OCP, DCD, 7x24, DISTRIBUTECH …) | speaker names months ahead; past talks by person | agenda fetch per starred event; recordings by manual link | E4 s2 |
| 7 | Regulatory dockets (FERC eLibrary, state PUC dockets) | witnesses and signatories for utilities, IPPs and large-load applicants | keyword watch per utility/IPP account on the FERC RSS the Scraper roster already carries; PUC dockets manual | E4 s2 (FERC) · manual |
| 8 | SEC filings and investor materials | officers, named executives, earnings-call presenters | per public account on refresh; the dossier's `decisionMakers[]` is the primary copy | N2 (on-the-record) |
| 9 | Association directories and committees (7x24 chapters, iMasons, ESIG, NAATBatt, IEEE PES working groups) | active members by company | manual capture with the directory URL as evidence | N4 |
| 10 | Google News RSS per named person | next public appearance of a person already known | opt-in per contact; one feed per contact, polled weekly | N4 |
| 11 | Registrant mail from organisers ("who's attending", sponsor lists) | attendee previews you receive legitimately | pasted in as manual signals | E4 s1 (manual path) |
| 12 | The corpus itself — 1,479 `decisionMakers[]`, 59 verified LinkedIn URLs | the seed people list for every covered target account | on-the-record check and "people at this account" panel | N2 |
| 13 | LinkedIn and X posts, organiser attendee lists | the highest-value person-level signal | **manual only** — paste URL + one line + confidence; never fetched | E4 s1 (manual path) |

Every signal row stores `kind`, `evidenceUrl` (or the corpus item `key`), `firstSeen`, `lastSeen`, `confidence`, and the account or contact it attaches to, so the app can always say why it thinks someone is going.

### 5.6 Plans (E5)

For a starred event: (1) **booth list** — every exhibitor that matches a Network account or a covered dossier, ranked per §5.4's account and segment terms, each with a *why* line lifted from the dossier and the account's stage; (2) **sessions** — the agenda filtered by segment keywords and by speakers who are Network contacts or dossier decision makers; (3) **day plan** — show hours from the registry, ranked visits placed by hall/booth proximity where a floor plan gives it, **open slots** for meetings, and **nearby venues** from OpenStreetMap Overpass (cafés, restaurants, bars, hotels within 600 m of `venueLatLng`; Places Nearby Search only if a billed GCP project ever exists — D12 as decided) around the venue's coordinates, cached per event; (4) **meetings** — the developer books a slot against a contact; the row writes a `meeting` Interaction to Network and an ICS invite; (5) **post-event** — the day after `end`, a checklist: cards scanned at the event, meetings held, follow-up mailing (opens Network's mass-email flow pre-filtered on `Source Event`), and an ROI line (cards, meetings, stage moves) that feeds the next year's recommendation. The narrative plan is the `events plan <event>` command (D12).

---

## 6 · The bridge — how the five apps connect

The rule is the ecosystem's: **public Pages data is read directly by any page; anything private crosses only server-to-server, behind a peer token, after the user's own app has already decided the user may ask.** Every route below is a copy of `guidanceMentionsProxy_()` with its own token.

| From → To | Channel | Payload | Token / gate |
|---|---|---|---|
| Network → Profiler | public `fetch` of `profiler-companies.json`, `profiler-segments.json`, `<slug>.profile.json` | company resolution, segments, `decisionMakers[]` for the on-the-record check, `strategyRead[]` / `recentDevelopments[]` for the brief | none (public); rendered behind `nwCan('profiler')` as an app-experience gate |
| Network → Profiler | deep links `Profiler.html#<slug>` from an account; `profiler <Company>` command text for uncovered accounts | — | link rendered after the registry answers |
| Network → Profiler (private) | developer-triggered "promote to field note" → the Profiler intake (`sourceType: contact`, confidence rated) | one note | the existing intake path, unchanged |
| Events → Profiler / Scraper | public `fetch` of the registries; `mentions[]` built from dossiers by script at build time | salience, segment audience | none |
| Profiler → Events | deep link from a dossier's developments to `Events.html#event/<slug>` when an event slug is named (rendered from the public registry's `mentions[]`, no new Profiler code path until Events v1 is live) | — | public |
| **Events → Network** (private) | `Network.gs ?action=peer&t=<NETWORK_PEER_TOKEN>&nop=accounts` → `{ accounts[] { id, name, slug, relationship, stage, segments[] } }` (no contacts, no emails — minimum necessary) and `nop=signals` (POST) to write `Signals` rows | account tags for the recommendation score; signal rows | `NETWORK_PEER_TOKEN`; `Events.gs` asks only for a session that already passed `evCan('recommend')` |
| **Network → Events** (private) | `Events.gs ?action=peer&t=<EVENTS_PEER_TOKEN>&eop=today\|starred\|signals&owner=<email>` → the developer's starred events, today's event for the scan default, and open signals per account | Source Event default, "will be at" chips on a contact | `EVENTS_PEER_TOKEN`; `Network.gs` asks only after `validateSessionForData` |
| Scraper ↔ Events | Events registers each event as a Scraper seed **`key: 'event-<slug>', source: 'event:<slug>'`** (the project pattern; `tv` bump on every terms edit; multi-word terms only); Scraper's corpus route (`cop=timeline`) is read by Events **through Profiler's existing proxy pattern** — a third `CORPUS_TOKEN` consumer is a policy change, so v1 Events does *not* hold the corpus token and reads corpus salience only from `profiler-graph.json` | event-tagged news in the digest; timeline items on an event page (later) | existing `CORPUS_TOKEN` policy — do not widen it without a decision |
| **Scraper → Network** (private, D17) | `Scraper.gs ?action=corpus&t=<NETWORK_CORPUS_TOKEN>&cop=people&slug=&since=` proxied by `Network.gs` behind `nwCan('signals')` | quoted and bylined people per account from the trade press | `NETWORK_CORPUS_TOKEN` — a third token namespace, never `CORPUS_TOKEN` |
| Classroom ← Events | deferred (D13): `event:<slug>` provenance prefix over the public registry; a `briefing`-typed **pre-event lesson** authored by a session; gate-surface change with `gateDigest` refresh | — | `guidance` fold at most |
| Classroom ← Network | **never** — no `contact:` prefix, no private first-person data in lessons | — | — |

Three token namespaces are added (`NETWORK_PEER_TOKEN`, `EVENTS_PEER_TOKEN`, `NETWORK_CORPUS_TOKEN`), each set to the same 16+ character random value in both peers' Script Properties, `.trim()`-ed on read, never committed, never quoted back. While unset, every dependent surface reports **not configured** rather than erroring — the scan card simply has no default event, the recommendation panel says "connect Network to score by account".

---

## 7 · The model rule, applied

`PROFILER-COVERAGE-PLAN.md` §2, unchanged: **Medium buys comparison of records the repo already holds; High buys reading depth; Opus 5 xhigh buys research and authoring; Fable 5.1 xhigh buys design and evaluation against a corpus.** Two additions this program makes explicit:

- **App code on the templates is reading-depth work, so it is Fable 5.1 High.** Every build session starts by reading a 7,900-line page and a 9,600-line `.gs` (Receipts) or their Network/Events descendants, and the failure mode is missing a constraint that is already written down (the postMessage rules, the partial-grant trap, the `tv` rule, the ContentService-HTML-at-200 trap). That is depth, not research. The S1 code half and K1 ran this way and shipped clean.
- **Research and narrative authoring stay on Opus 5 xhigh** — the event registry (E0) and the `events plan` command are the two places the plan needs new sourcing and prose against sources, and Opus is half the rate against the weekly limit with no cap. **Fable 5.1 High is the substitute** for E0 if the developer wants a single model family, at the cost of the weekly cap; record the substitution in §11 either way.
- **Fable 5.1 xhigh is spent exactly twice**: the gate (NE0) and the Classroom decision (X). The developer asked whether this session should have been xhigh: **no — a proposal is reading and synthesis, and High produced it**; the *gate* is where evaluation quality compounds across the twenty sessions that follow, and that is the session to run at xhigh.
- **Fable 5.1 Medium** takes the bookkeeping: source-roster probes and rows, checker runs, ledger flips, the Scraper seed registrations, the quota review.

---

## 8 · The phase plan

| Phase | Model · effort | Sessions | What | Done when |
|---|---|---|---|---|
| **NE0** | **Fable 5.1 xhigh** | 1 | **The design gate.** Decide D1–D16; amend this file in place (decisions → "approved", reasons recorded); write the `NETWORK-SCHEMA.md` and `EVENTS-SCHEMA.md` skeletons (objects, enums, id formats, the public/private line) as the single sources of truth the builds edit against | Every D row carries a decision; both schema files exist with the enums of D5 and the registry shape of §5.1; §11 rows flipped from *proposed* to *open* |
| **N0** | Fable 5.1 High | 1 | **Network scaffold** — brief in §13.3, shapes in **`NETWORK-SCHEMA.md`**. `setup-gas-project.sh` (auth, hipaa) → `Network.html`/`Network.gs`; manifest + icons + the `manifest-src` override; `NW_ROLE_CAPS` on both sides admitting admin only (D7) and `scripts/verify-network-roles.py`; `ensureNetworkTabs_()` for the schema's tabs; Drive root folder ops; Master ACL column; `HEARTBEAT_INTERVAL` 600 s, no data poll (D14); the `op=quota` execution counter defined in the shared template region; Profiler's explorer relabel to "Ecosystem" (D1) | Sign-in works on the live page, `DEPLOYMENT_ID` recorded, deploy webhook confirmed by a GET probe, admin sees the empty list, contributor / analyst / viewer are turned away; Playwright screenshots of all four |
| **Q0** | Fable 5.1 Medium | 1 | **Quota counter rollout** (D14): the `op=quota` API from N0 copied into the eight existing projects in one commit (eight GAS bumps, eight changelog entries), so Q reads a full month across all ten | Every deployed project answers `?action=api&op=quota` with today's execution count by event type; `scripts/check-acl-health.sh`-style probe script reads all ten |
| **N1** | Fable 5.1 High | 2 | **Capture → extraction → review → save** per §4.2: the card schema + prompt with per-field confidence, front/back pair, IndexedDB offline queue, own-Drive `_inbox` → `<Company>/` move on save, company resolution against the registry, dedupe-and-merge, the review card | 20 real cards including three Chinese-script and two two-sided round-trip with at most one manual correction each; an airplane-mode capture drains on reconnect; the Drive tree shows `Network App/<Company>/<id>-front.jpg`; no name-derived id anywhere (checker) |
| **N2** | Fable 5.1 High | 1 | **Accounts and the corpus attachment**: Accounts tab, relationship/stage/role selects, `Profiler.html#<slug>` deep links, the propose-a-dossier hook, the on-the-record check against `decisionMakers[]` | Every contact belongs to an account; a covered account shows its slug, segments and the record's title for a matching name; an uncovered one emits a valid `profiler <Company>` line |
| **E0** | **Opus 5 xhigh** (research) · Fable 5.1 Medium (roster) | 1–2 | **The registry and the roster** — brief in §13.4, shapes in **`EVENTS-SCHEMA.md`** §3–4. `scripts/extract-corpus-events.py`, `events.json` ≥ 60 events verified against organiser sites, `events-sources.json` with a live probe per row and every blocked source recorded with its reason, `scripts/check-events-registry.py` | Checker exits 0; every event carries `lastConfirmed` and a `sources[]` row whose host is in the roster; the 31 corpus events all have `mentions[]`; the roster has no un-probed row |
| **E1** | Fable 5.1 High | 2 | **Events scaffold + calendar** per §5.3: setup script (auth, admitted tiers per D7), `EV_ROLE_CAPS` + verifier, the agenda scroller and detail sheet, stars/notes tabs, Add-to-Google-Calendar + ICS, `events.ics` published beside the page | Live; a starred event opens correctly in Google Calendar from the link and imports from the `.ics`; Playwright screenshots of month, agenda, detail at phone width; zero page errors; **before 2026-11-16** (D16) |
| **B** | Fable 5.1 High | 1 | **The bridge** per §6: `NETWORK_PEER_TOKEN` and `EVENTS_PEER_TOKEN`, the four peer ops, proxies copied from `guidanceMentionsProxy_` with the six token-boundary cases and the `upstream_not_json` distinction | Node harness proves every unauthorised case returns flat `denied` with zero upstream calls; with tokens set, the scan card's Source Event defaults from a starred event dated today; with tokens unset both surfaces say *not configured* |
| **N3** | Fable 5.1 High | 2 | **List, filters, bulk actions, exports, mail, my card** per §4.3 | A filtered selection exports to `.xlsx`, CSV and a vCard bundle that imports on iOS and Android; a three-recipient mailing renders three editable drafts, exports as an `.eml` bundle that opens in a mail client, and marking them sent writes three `email-out` interactions (D15 — nothing is sent by the app); the QR resolves to a vCard on a second phone |
| **E2** | Fable 5.1 High | 1 | **The poller + `events sync`**: weekly trigger over the roster (JSON-LD, ICS), `Proposed` tab, the session-side apply command in `.claude/rules/events-app.md` | One cycle proposes at least the JSON-LD sites' current editions; an approved diff lands in `events.json` through the command and the checker stays at 0 |
| **E3** | Fable 5.1 High | 1 | **Recommendation** per §5.4: the score, the *why* panel, the `Tuning` tab, computed server-side from bridge data | For the developer's real account tags the top five recommended events are defensible line by line; changing a weight in `Tuning` reorders them without a deploy |
| **E4** | Fable 5.1 High | 3 | **Attendance signals** per §5.5 and the §5.5.1 catalogue: session 1 — exhibitor and speaker diffs, newswire RSS, the manual path; session 2 — newsroom/events pages, agendas, FERC docket watch; session 3 — the Scraper-side `people[]` extraction, the `cop=people` route, `NETWORK_CORPUS_TOKEN` and Network's proxy (D17) | For RE+ 2026 the exhibitor diff yields at least one signal for a tagged account with an evidence URL and `firstSeen`; a manual signal shows on both the contact and the event; nothing fetches LinkedIn, 10times or an attendee list (grep-checked) |
| **N4** | Fable 5.1 High | 2 | **Relationship intelligence** per §4.4: warmth, reconnect list, the `.ics` / sent-mail CSV import panel (no scopes — D15), pre-meeting brief export, promote-to-note, network map | The reconnect list is correct against a hand-computed sample; an imported `.ics` writes only `calendar` interactions the developer confirmed, with the developer's own reference as evidence and never a message body; the brief exports for a covered and an uncovered contact |
| **E5** | Fable 5.1 High (code) · **Opus 5 xhigh** (the `events plan` command rule + first plan) | 2 | **Plans** per §5.6: booth list with dossier *why* lines, sessions, day plan with open slots and Overpass venues (D12), meetings → Network interactions + ICS, the post-event checklist and ROI line; the `events plan <event>` command | For one starred mega-show and one regional: a day plan renders with ≥ 5 ranked booths each carrying a verbatim dossier line, open slots, and three venues within 600 m; a booked meeting appears on the contact's timeline; the narrative plan lands in Drive (or is handed back as text when no Drive connector is attached) |
| **X** | **Fable 5.1 xhigh** (decide) · Fable 5.1 High (build if yes) | 1 | **Classroom hook** per D13: decide; if yes, `event:` prefix + schema mirror + `gateDigest` refresh in one commit and the first pre-event briefing lesson | A written decision either way; if built, `check-classroom-pipeline.py` and `check-classroom-content.py` clean and the lesson's fold is `tracks` or `guidance`, never `briefing` from a private input |
| **R** | Developer (UI) · Fable 5.1 Medium (prompt) | in parallel, after Monday's proof | **The discovery Routine** (quarterly, repo attached, STEP 0, calendar-file-as-queue) | Created in the UI with the **Runs with** card showing the repo; first run proposes ≥ 1 event and commits or stands down with a report |
| **Q** | Fable 5.1 Medium | 1, after E5 | **Quota and ops review**: read the audit-row counter across all ten projects, retune intervals, confirm the ten-project account is inside 20,000/day with headroom | A dated table of executions/day per project in `FUTURE-CONSIDERATIONS.md` and any interval change committed |

**≈ 24 sessions**: NE0 1 · N0–N4 8 · Q0 1 · E0–E5 10–11 · B 1 · X 1 · Q 1; R is outside the count. Twenty-one of them are Fable 5.1 High; two are Fable 5.1 xhigh; two are Fable 5.1 Medium (Q0, Q); two carry an Opus 5 xhigh half.

---

## 9 · Per-phase detail and the reason for each model

**NE0 — Fable 5.1 xhigh.** Sixteen decisions against a five-app ecosystem with three years of recorded rules; the cost of a wrong call here is paid twenty times. Design and evaluation against a corpus is what xhigh is for.

**N0 — Fable 5.1 High.** The setup script does most of the work; the session's job is reading `gas-scripts-reference.md`'s fourteen setup steps, the partial-grant trap, and the Receipts config header carefully enough to produce a live sign-in on the first deploy. Reading depth. It also starts the quota counter because ten projects on one account is the program's standing risk.

**N1 — Fable 5.1 High, two sessions.** The Receipts capture pipeline is 1,200 lines the session must understand before changing the schema, the prompt and the Drive flow; the offline queue and the folder move are new. Two sessions because twenty real cards is a real test and the second session absorbs what the first learned about Chinese-script and two-sided cards.

**N2 — Fable 5.1 High.** Account modelling is mostly schema and UI on the pattern N1 built, plus one careful read of the registry's `aka[]`/`domains[]` resolution rules so company matching neither over- nor under-links.

**E0 — Opus 5 xhigh + Fable 5.1 Medium.** Building sixty verified event rows with organiser URLs, exhibitor-list URLs and audience segments is research; probing sixty sources and writing the roster rows is bookkeeping with a rule already written (`scraper-sources.md`). Two models, possibly two sessions. It can run in parallel with N1–N2 because nothing in it touches Network.

**E1 — Fable 5.1 High, two sessions.** The calendar scroller is the one genuinely new UI component in the program and phone-width layout work is where the estimate heuristics say to budget iterations; the second session is the Playwright pass and the ICS/Calendar-link verification on real devices.

**B — Fable 5.1 High.** One session, one pattern, copied faithfully: the `guidanceMentionsProxy_` invariants are written down, and the failure modes (a trailing newline in a property, an exception page at HTTP 200) are exactly the kind a careful read prevents.

**N3 — Fable 5.1 High, two sessions.** The exports are house-style hand-rolled formats (vCard joins JPEG-in-PDF and xlsx-via-temp-sheet); the drafts flow (D15) renders one editable draft per recipient and hands off by `.eml` bundle, CSV/`.txt`, clipboard or `mailto:` — no scope, no send path — and the CAN-SPAM hygiene (unsubscribe line, postal address) is in the default template so it is right the first time real contacts are mailed from the developer's own client.

**E2 — Fable 5.1 High.** The poller is a Scraper-shaped state machine without the AI; the design point that matters is that it *proposes* and a session *applies*, because a fired session cannot push and a GAS trigger cannot commit.

**E3 — Fable 5.1 High.** A scoring function with a tuning table and an explanation panel. Its correctness is judged by the developer against their own account list, so the session's deliverable is the *why* panel as much as the score.

**E4 — Fable 5.1 High, two sessions.** Server-side HTML parsing of exhibitor pages is brittle and each organiser differs; the first session lands the newswire RSS and the manual path (certain), the second the directory diffs (per-site).

**N4 — Fable 5.1 High, two sessions.** The import panel (an `.ics` or a sent-mail CSV, matched by email, confirmed row by row) replaces the proposal's Gmail/Calendar sweep per D15 and must never store a message body — a privacy-sensitive read of the template's audit rules. The brief export and the map are UI on existing data.

**E5 — Fable 5.1 High + Opus 5 xhigh, two sessions.** The deterministic plan is code; the narrative plan is authoring against dossiers and a registry, with the same "paraphrase, never fabricate about a person" discipline C5 wrote down (D4 there: the counterparty is a role, never a person — a plan may say *visit Sungrow's booth because the record says X*, never *ask Jane for Y*).

**X — Fable 5.1 xhigh to decide, High to build.** Whether a pre-event briefing lesson is worth a gate-surface change is an evaluation question; if yes, the change itself is a careful three-file commit with a digest refresh.

**R — the developer.** A committing Routine can only be made in the UI; the session writes the prompt (STEP 0 included) and the developer pastes it. Not before Monday's earnings-desk proof.

**Q0 — Fable 5.1 Medium.** Copying one API op into eight projects with eight version bumps — bookkeeping with the op already written in N0.

**Q — Fable 5.1 Medium.** Reading a counter and adjusting four constants.

---

## 10 · Sequencing, the critical path, and what must be true first

**Critical path:** NE0 → N0 → N1 → N2 → B → E3 → E4 → E5. Everything on it reads Network's account tags; nothing on it can start before the gate. **Parallel lane:** E0 (Opus) beside N1–N2 (Fable), then E1 beside N3. The two lanes join at B.

**Preconditions, in the order they bite:**

1. **The gate.** No build session starts until §3's rows carry decisions — the Classroom precedent (`PHASE6-CLASSROOM-DESIGN.md`) held its gate before C0 and every later session cited it.
2. **Monday 2026-09-21's earnings-desk A/B** decides whether *any* scheduled job in this program can commit. Until a UI-created Routine has landed a commit, R does not exist and E2's poller must propose into Sheets, which it does by design. Nothing in N0–E5 depends on a Routine.
3. **Keys and scopes.** `GEMINI_API_KEY` exists for Receipts; Network needs its own project's copy (D6 — Gemini only, no `ANTHROPIC_API_KEY`). No mail or calendar scope is ever added (D15; N4 imports instead of sweeping), and no Places key is needed (D12 — Overpass; §12.5: no billed GCP project). The only scope work left is the standard auth-template manifest, so the partial-grant trap applies at N0's first authorization and nowhere later.
4. **Two new peer tokens** set in four Script Properties before B is verified.
5. **The ten-project quota** — the counter from N0 runs from the first day so Q has a month of data.
6. **The weekly Fable cap.** Twenty Fable High sessions at roughly one to two a day is the same load the Classroom program carried; if the cap binds, E0 and E5's Opus halves are already off-Fable, and any code session can drop to Opus 5 xhigh with the substitution recorded in §11.

**What the program does not depend on:** Classroom (X is optional and last), Scraper changes (seed registration is one commit per event batch, Fable Medium), any change to Profiler beyond a one-word label.

---

## 11 · Status ledger

A row is flipped by the developer or by a later session against the "done when" in §8, never by the session that wrote the criterion.

| Phase | Model · effort | Status |
|---|---|---|
| NE0 · design gate | Fable 5.1 xhigh | **Done — v06.74r, 2026-09-20.** All seventeen D rows carry a decision (D7 overridden to admin-only; D6 Gemini-only and D12 Overpass depart from the proposal; D14 tightened to no data poll; D5 and D9 refined); `NETWORK-SCHEMA.md` and `EVENTS-SCHEMA.md` written and cited by §8's N0 and E0 rows; §13.3 (N0) and §13.4 (E0) briefs written; all eight §12 questions settled; the D15 consequence for N4 recorded in §4.4 |
| N0 · Network scaffold | Fable 5.1 High | **Done — v06.76r, 2026-09-20.** `Network.html` / `Network.gs` scaffolded from the auth template (`hipaa`, Master ACL `1kG2K…`, own spreadsheet); PWA manifest + icons with the `manifest-src 'self'` override on both CSP tags, no service worker; the admin-only door on both sides (`NW_ROLE_CAPS`, `nwAdmitted_` / `nwAdmitted()`) with `scripts/verify-network-roles.py` passing — admin sees the empty list, contributor / analyst / viewer are turned away with zero requests, `?as=` only subtracts; `ensureNetworkTabs_()` with the eight `NETWORK-SCHEMA.md` §3 tabs, the §4 enum mirrors, `NW_ID_RE` / `nwNewId_`, the `nwfolders` registry, ownership helpers verbatim from Receipts; heartbeat 600 s and no data poll on both sides; `op=quota` (+ `op=aclhealth`) defined; Profiler's explorer relabelled "Ecosystem" (v01.91w) with the 13 × 4 matrix unchanged. **Live checks closed 2026-09-20 (v06.77r):** deployed as `AKfycbxu…c0_U8`, `DEPLOYMENT_ID` recorded and synced (v01.02w / v01.02g); admin signed in on the live page and saw the empty list; `op=deploy` → `Already up to date (v01.02g)`, `op=aclhealth` → `acl_ok`, `op=quota` answering, `check-acl-health.sh` lists Network. One bootstrap lesson for E1: code pasted before the deployment exists cannot repoint its own deployment on the first webhook run — Manage deployments → Edit → New version once, by hand |
| Q0 · quota counter rollout | Fable 5.1 Medium | **Done — v06.79r, 2026-09-20.** `quotaProbe_()` + the `op=quota` dispatch copied verbatim from `Network.gs` into the eight existing projects (Network's `nwQuotaProbe_` renamed to match — nine GAS bumps, nine changelog entries); `scripts/check-quota.sh` discovers every project that dispatches the op, skips the three placeholder-id projects, prints one row per deployed project and the fleet total against 20,000/day. Events inherits the op from the shared template region at E1; intervals untouched (Q) |
| N1 · capture → save | Fable 5.1 High | **Done — v06.90r, 2026-09-21.** Session 1 (v06.82r–v06.88r, `Network.html` v01.03w→v01.09w, `Network.gs` v01.04g): capture card with the front/back pair and the one-/two-sided batch toggle, IndexedDB offline queue drained on `online`, own-Drive upload into `Network App/_inbox/` with the `c-` id minted before upload (D8), Gemini-only extraction with the §7 `responseSchema` and per-field confidence (D6), QR merge, the held-card strip with unclear-scan notes, an inline editor, delete and the name-case rule; 20 real cards scanned by the developer (three Chinese-script, two two-sided). Session 2 (v06.90r → v06.91r, v01.11w / v01.06g): the review block on the editor (role, the account block under the D5 stage rule, source event, met date, consent, do-not-contact), Retry / Swap, company resolution against the public registry (slug + segments proposed, an existing Account reused), `nwFindDuplicate_` answered before the write with the field-by-field merge sheet (never a silent reject), `nop=save` writing Contact + Account + `scan` Interaction (a `merge` Interaction carrying the absorbed id on a merge), the browser-side Drive move `_inbox/` → `<Company>/` with the folder parked through `setfolders`, list rows name · title · company with `nop=get` detail, edit-in-place (`nop=update`) and soft delete / restore, the title / department / company standardisation rule (`nwStdField`), `scripts/check-network-schema.py` (exit 0) and the verifier's save round-trip. **Done-when standing on the phone:** the 20 held cards round-trip after this push — the Contacts list and the Drive tree are the evidence; an airplane-mode capture drained on reconnect in session 1 |
| N2 · accounts + corpus attachment | Fable 5.1 High | **Done — v06.94r, 2026-09-21.** `Network.gs` v01.07g: `nop=account` (body-POST; the save-path validator — enums, the D5 stage rule, the slug shape — plus Tags / Newsroom URL / Notes; a rename rewrites `Normalised Name` and is refused when another live account owns the key; audit rows `accountId`, a rename flag and the tag count), the list op's one widening (`contactCount` per account), `nop=get` on an `a-` id answering the live contacts beneath. `Network.html` v01.14w: the **Accounts** card under the Contacts list (name · relationship · stage · contact count; tap → the full row with its contacts; **Edit** through the shared `nwAccountBlock` — the same block the review card uses, in full; Delete refused with the count, Restore), the relative `Profiler.html#<slug>` deep link on the account row, the account detail and the contact detail's account line, segments shown as their registry labels (`profiler-segments.json`, fetched once), **Propose a dossier** on an uncovered account (the exact `profiler <Company>` line copied and always shown; the `dossier-proposed` tag through `nop=account`; nothing generated in-app), the **on-the-record** check (the dossier fetched only when a detail opens, cached per slug; `nwNameKey` mirrors `nwNameKey_`; a match shows the record's title and the dossier's source line, a differing card title is a note and never written), and the §6 folder rename on the next save (`nwFolderRenameIfDrifted`). `scripts/check-network-schema.py` asserts the D5 validator on both `nop=save` and `nop=account`; `scripts/verify-network-roles.py` runs the accounts round-trip against the stub and the served `abb` dossier. Done-when met on the stub; the phone check is the 20 saved cards' accounts |
| E0 · registry + roster | Opus 5 xhigh | **Done — v06.95r, 2026-09-21.** `live-site-pages/events-data/events.json`: **100 events — 72 `confirmed`, 27 `tentative`, 1 `past`**. Confirmed means an organiser page was read this session; the `past` row (NAATBatt 2026) was organiser-read too and the checker flipped its status. Every one of the 27 `tentative` rows carries a `manual` source and a `tierNote` saying why it is not confirmed — eleven because the organiser blocks non-browser clients, the rest because the organiser has published no dates for the edition. `events-sources.json`: **58 roster rows, every one probed live before it was written** (HTTP status, `Event`/`VEVENT` count, newest item, robots for the fetched path); **24 blocked**, each with its reason and date — `cloudflare-challenge` on OCP, DCD, Enlit, 10times, SEMI/SEMICON West and Gartner; `403-akamai-non-browser` on CERAWeek; `403-datadome` on Reuters Events; `403-azure-waf` on GCPA and NAATBatt; `no-feed` on Uptime Institute (302s off-site to google.com), Hannover Messe, Microsoft Ignite, EEI, NARUC and Hot Chips; 404/503/DNS on Solar & Storage Live, SNEC, CIBF, ESIE, IDEE, Battery Japan and AMD. **11 live JSON-LD feeds** the E2 poller can read — including two the seed calendar did not know about, iMasons (11 `Event` objects) and ESIG (12), which are the whole sub-mega/social tier. COMPUTEX is on the roster with `robots: disallowed` and must never be polled; the 10times row is kept **only** so it is never re-proposed and no event may cite it (the checker asserts that). `scripts/extract-corpus-events.py`: **33 corpus events, 256 `mentions[]` rows across 90 dossiers on 32 registry rows** (`ees Europe` and `The smarter E` resolve to the same edition), every corpus event with ≥ 1 mention; idempotent, `--check` and `--report`. `scripts/check-events-registry.py` exits 0 with `--fix-past` applied once. **Appendix A caveats resolved:** the DCD New York 2027 conflict stands unresolved by design — Clocate's JSON-LD (read this session) says 17–18 Mar 2027 at the New York Marriott Marquis, a second mirrored listing says 17–18 May, and DCD's own page is Cloudflare-blocked, so the row is `tentative` with both recorded; NAATBatt's weakly-sourced Aug 1–5 2027 row is **dropped** — the organiser publishes only the Feb 9–12 2026 edition and no 2027 dates; of the "not yet dated for 2027" list, **Wood Mackenzie NA Power & Renewables (Apr 28–29, Omni Interlocken Denver) and Datacloud USA (Aug 31–Sep 2, Fairmont Austin) are now dated and confirmed**, and Solar & Storage Live USA, DCD Silicon Valley/Dallas, Bisnow DICE South/West and Uptime 2027 are still undated. Three seed-calendar errors corrected against the organiser: ACP Siting + Permitting and ACP PEAK are two events (Apr 13–15 and Apr 15–17), not one Apr 13–17; Energy Storage Summit USA 2027 moves to the Renaissance Dallas at Plano Legacy West; GTC 2027, InterBattery 2027, CIGRE Grid of the Future, IEEE PES GM 2027, The Battery Show Europe 2027 and AWS re:Invent 2026 were all third-party sourced and are now read from the organiser. Nothing under `Events.html` / `Events.gs` — that is E1 (§13.7) |
| E1 · Events scaffold + calendar | Fable 5.1 High | **Done — v07.08r, 2026-09-22 (two sessions: v07.07r + v07.08r).** `Events.html` v01.02w / `Events.gs` v01.01g. **Session 1 (v07.07r):** scaffolded from the auth template (`hipaa`, the fleet Master ACL `1kG2K…UvE`); PWA manifest + icons with the `manifest-src 'self'` override, no service worker; the admin-only door on both sides (`EV_ROLE_CAPS`, `evAdmitted_` / `evAdmitted()`) with `scripts/verify-events-roles.py`; `ensureEventsTabs_()` creating all five §5 tabs (+ `Shares` · `Profiles`), `EV_ID_RE` / `evNewId_`, the ownership helpers from Receipts; `eop=list` / `star` / `unstar` / `note` (body-POST, enum-validated Attending, audit rows ids and counts only); heartbeat 600 s, no data poll, `op=quota` + `op=aclhealth`; the vanilla agenda scroller over `events-data/events.json` (sticky month header, day groups, IntersectionObserver month-in-view label, past fold, tentative rows labelled), the kind / region / segment / starred pills with "Signals only" disabled until E4, the detail sheet with `mentions[]` chips → `Profiler.html#<slug>`, Add to Google Calendar per §9 and the hand-rolled RFC 5545 `.ics` per event. **Session 2 (v07.08r):** `scripts/build-events-ics.py` → the published `live-site-pages/events-data/events.ics` (72 confirmed of 100 events, `X-WR-CALNAME: BESS/AIDC events`, stable `<slug>@events.lightaisolutions.github.io` UIDs, 75-octet folding, CRLF — `*.ics -text` in `.gitattributes` so the CRLF survives the repo's LF normalisation; `--check` / `--stamp`) with the `.ics` walk wired into `check-events-registry.py` (the file is required, must parse, and its UID set must equal the confirmed slugs with DTSTART / STATUS matching each row — exit 0); the **Subscribe** pill on the masthead offering the `webcal://` URL of the published file with a copy-by-hand fallback and a one-time download; the **Day plan** tab — a read-only timeline of the starred events on a chosen day (date picker, a strip of starred days, the venue's hours where the registry has them, attending state, note; E5 fills it); the page's semicolon escape fixed (`'\;'` was `';'`) so the per-event text is byte-identical to the published file; the Playwright phone pass at 390 × 844 in `verify-events-roles.py` — the month header sticks below the user pill while its month scrolls, the month-in-view label changes across a month boundary, the sheet opens and closes, a star round-trips through a stateful stub, one event's `.ics` parses under a minimal VEVENT walker and matches its published VEVENT byte for byte (DTSTAMP aside), the Calendar href carries `dates=` / `ctz=`, the day plan lists the starred event, the Subscribe pill serves the file; screenshots of month / agenda / detail / day plan; zero page errors. **Still the developer's (§13.7 step 8, reported in the hand-off, not asserted):** `SPREADSHEET_ID` / `DEPLOYMENT_ID` are placeholders until the spreadsheet is created and the project deployed (recorded the N0 way; the Deploy Events workflow step no-ops until then), and the real-phone check — the Calendar link opens Google Calendar prefilled, the `.ics` imports, the `webcal://` subscription lands. The B brief is §13.8 |
| B · the bridge | Fable 5.1 High | **Done — v07.10r, 2026-09-22.** `Network.gs` v01.08g / `Events.gs` v01.03g / `Network.html` v01.15w / `Events.html` v01.04w. The far sides `nwHandlePeer_` (`?action=peer&t=<NETWORK_PEER_TOKEN>&nop=accounts\|signals`) and `evHandlePeer_` (`?action=peer&t=<EVENTS_PEER_TOKEN>&eop=today\|starred\|signals`) in both `doGet` / `doPost` before session validation — the property `.trim()`-ed, the six token-boundary cases one flat `denied` with zero reads (`not_configured` is the calling side's word, as Classroom's `clHandleGuidancePeer_` reasons); `nop=accounts` (scored relationships only, no contacts / emails / notes / owner), `nop=signals` POST upsert on (`accountId`, `eventSlug`, `kind`, `evidenceUrl`) with `linkedin_not_fetched`, `Source = events` and a GET read leg, `eop=today` / `starred` over the Stars tab joined to the public registry fetched server-side from the Pages URL with today decided in each event's `tz`, `eop=signals` read-through; the near sides `nwEventsProxy_` / `evNetworkProxy_` — `guidanceMentionsProxy_` verbatim with the peer `/exec` constants and the `upstream_not_json` snippet — behind `nop=eventstoday` (after `validateSessionForData` + `nwRequire_`) and `eop=netaccounts` (after `evRequire_(sess, 'recommend')`); the scan card's Source Event defaults from today's starred event (one, prefilled; several, pills; none or not configured, untouched — N1's field kept), the E3 placeholder line on the Events sheet. `scripts/check-peer-bridge.js`: 61 checks, exit 0 — every unauthorised case flat `denied` with zero `openById` / `fetch`, the ops over in-memory tabs and the committed registry, both proxies' four failure words. **Still the developer's:** set both tokens to one shared value in both projects, redeploy, star an event dated today and open the scan card (§8's done-when with tokens set) |
| N3 · list, exports, mail | Fable 5.1 High | **Done — v07.14r, 2026-09-22 (two sessions: v07.13r + v07.14r).** `Network.gs` v01.10g / `Network.html` v01.19w. **Session 1 (v07.13r):** the Contacts list generalised from the Receipts History card — search over name / company / title / email and the eight filters answered server-side by `nwListOp_`, `lastTouch` computed once per list and carried on the row (the one §12 widening), the four sorts with a flip (Warmth disabled until N4), per-row expand to the existing detail; a checkbox per row and a bar fixed to the bottom — tag and relationship / stage through `nop=bulk` (each id judged on its own, `rejected[]`, the D5 rule per account), CSV through `nop=export`, delete one request per row. **Session 2 (v07.14r):** `nop=export` gathers once and answers `csv` · `xlsx` (the Receipts temp-spreadsheet path — Contacts / Accounts / Interactions sheets, base64) · `vcard` (hand-rolled 3.0 per contact and as a bundle; `PHOTO;ENCODING=b;TYPE=JPEG` spliced by the page from the user's own Drive only when "include card image" is ticked, folded at 75 octets; the per-contact set a client-side store-only zip), every format with the D9 exclusion and the disclosure row; the Export menu replaces the CSV button. The follow-up drafts per D15 — `nop=mailings` / `nop=drafts` / `nop=draftstatus`: recipients from the selection (`do_not_contact` and `no_consent` skipped with the reason), a saved template from the Mailings tab or one written now with the merge fields, one editable draft per recipient in the Drafts tab, the review list edited in place, hand-off by `.eml` bundle (RFC 5322, `From` typed once and kept in `localStorage`, the default template's unsubscribe line and `{{myAddress}}`), CSV / `.txt`, per-draft Copy or `mailto:` (Copy fallback over ~1,800 characters); marking sent writes the `email-out` Interaction with the d- id as evidence — the app never sends, no `MailApp` / `GmailApp` / Gmail scope in the PROJECT region or the page, asserted by the verifier. The "My card" panel (Profiles row: Display Name · Company Name + the new Title · Phone) rendered as a QR full-screen by a hand-rolled byte-mode encoder (versions 1–10, level M, no library — the repo had only decoders) that the verifier proves equal to python-qrcode module for module. `verify-network-roles.py` ALL CHECKS PASSED, zero page errors at 390 × 844. **Reported, not asserted** (§8's done-when): the vCard bundle importing on iOS / Android Contacts and the QR resolving on a second phone — the developer's live check |
| E2 · poller + `events sync` | Fable 5.1 High | **Done — v07.15r, 2026-09-22.** `Events.html` v01.05w / `Events.gs` v01.04g. **The poller** `evPollRun_` (trigger handler `evPollTick` — a time-driven trigger cannot target a `_` function; `eop=installpoller` deletes every trigger on either name and creates one weekly, Monday 06:00 America/New_York; `eop=pollnow` runs the same walk as the admin who pressed it): the roster and the registry read once per run from their Pages URLs; only `jsonld` / `ics` rows with a cadence other than `manual`, no `blocked` and no `robots: disallowed` are fetched (`evPollSkipReason_` — COMPUTEX and the 24 blocked rows never are); `UrlFetchApp` with `muteHttpExceptions`, a per-source try/catch, a 15-second allowance against a 270-second run budget that stops cleanly; JSON-LD `Event` objects from every `ld+json` block (arrays, `@graph`, `subEvent`, `…Event` subtypes, `EventCancelled`) and a minimal RFC 5545 walker (unfold, `VALUE=DATE` exclusive `DTEND`, escapes — it reads `build-events-ics.py`'s own output) normalised to §3; the match (derived §1 slug → same name / URL among the rows citing the key → same series in another year → new) and the **six diff kinds** written as `Proposed` rows with canonical Before · After, deduplicated on (Source Key, Event Slug, Change, After) over every existing row whatever its status; a failed source one `Polls` row (Source Key · Ran At · Status · Items · Newest Start — the tab added to `ensureEventsTabs_()` and §5) and one audit row, no proposal. `eop=proposed` / `decide` / `applied` behind `roster`. **The Proposed tab** on the page (admin only — the other tiers never reach the strip): the poller card (trigger state, Install poller, Poll now, Refresh, the last outcome per source), the approved set as the sync JSON (Copy as JSON, the field for copying by hand, Mark applied with the version), the pending rows grouped by source with Before → After, Approve / Reject (reversible until applied). **`events sync`** in `.claude/rules/events-app.md` (registered in CLAUDE.md): apply per change kind, `lastUpdated` and `lastConfirmed`, the roster's `lastProbe` from `polls[]`, `build-events-ics.py`, `check-events-registry.py` exit 0 as the gate (reverted on a finding), the `pr-` ids and the version for Mark applied; the session never calls the app. **`scripts/check-events-poller.js`**: 63 checks, zero live calls — the six kinds one row each, the zero-row re-run, the never-fetched rows, the 403, the refusals, the idempotent install. `verify-events-roles.py` extended (the tab admin-only, one `eop=proposed` on open, Approve → `decide`, the JSON parses back, Mark applied / Install / Poll now reach the stub; zero page errors at 390 × 844). **Reported, not asserted**: the first live cycle — redeploy, Install poller (first-run authorisation), Poll now, approve one row, `events sync` in a fresh session |
| E3 · recommendation | Fable 5.1 High | Proposed |
| E4 · attendance signals (3 sessions, §5.5.1 catalogue) | Fable 5.1 High | Proposed |
| N4 · relationship intelligence | Fable 5.1 High | Proposed |
| E5 · plans + `events plan` | Fable 5.1 High · Opus 5 xhigh | Proposed |
| X · Classroom hook | Fable 5.1 xhigh · Fable 5.1 High | Proposed — deferred behind E0 stability by design |
| R · discovery Routine | Developer · Fable 5.1 Medium | Blocked on Monday's earnings-desk proof |
| Q · quota review | Fable 5.1 Medium | Proposed — after E5 |

---

## 12 · Open questions for the developer — all eight settled at the gate (2026-09-20)

1. **Third-party processors of card PII (D6, D9).** Gemini and Claude both receive name + email + phone per card. Neither trains on API traffic by default and Receipts already sends merchant addresses to Gemini, but a card is a person. Accept as-is, or restrict extraction to Drive OCR (free, slower, weaker) for cards whose owner asks? **Settled 2026-09-20: Gemini may process card PII; no second vendor (D6 — Gemini only).**
2. **Consumer account vs Workspace.** Every cap in this plan — 20,000 executions/day across ten projects, 100 recipients/day for `MailApp` (avoided by D15), 500/day Gmail — is the consumer figure. A Workspace seat raises them five- to ten-fold and is the single cheapest de-risk if the program grows past one user. **Settled 2026-09-20: stay on the consumer account; decide at Q on the counter's data (D14).**
3. **The Profiler relabel (D1).** "Ecosystem Network" → "Ecosystem Graph" is cosmetic; confirm or keep. **Settled 2026-09-19: "Ecosystem" (D1); executes in N0.**
4. **Team scope.** D7 admits contributors with owner-scoped sharing, the Receipts model. If a team will use Network, the C6 "team layer" question (instructor dashboard's analogue: a shared account list with per-owner contacts) should be decided at the gate, not discovered later. **Settled 2026-09-20: no team layer — both apps start admin-only (D7 override); ownership scoping stays as the widening path.**
5. **Places API billing.** E5's venue recommendation needs a Google Cloud project with billing enabled (Pro tier is free at this volume, but the key requires it). Does one exist, or is the Gemini key an AI Studio key with no billing project behind it? If none, E5 ships with the free OpenStreetMap Overpass fallback and adds Places later. **Settled 2026-09-20: no billed GCP project exists; Overpass is the default (D12), Places a later option.**
6. **Seats.** Recommendation defaults (E3) assume both seats — `storage-seller` and `aidc-power-seller` — so the default segment set is the union. If one seat dominates today, say which and E3 weights it. **Settled 2026-09-20: both seats weigh equally — the union (`EVENTS-SCHEMA.md` §6).**
7. **Event registry public exposure.** `events.json` on Pages reveals which shows the corpus tracks but nothing about attendance or the developer's stars (private). Confirm the public/private line at D3 is acceptable — it is the same line `profiler-projects.json` already sits on. **Settled 2026-09-19 with D3: approved.**
8. **This session's effort.** High produced this proposal; the recommendation stands to run the gate at xhigh (§7). If the developer prefers to stay at High for the gate, the cost is judgment quality on D3, D7 and D13 — the three decisions with the longest tail. **Settled: the gate ran on Fable 5.1 xhigh, 2026-09-19 → 20.**

---

## 13 · Paste-in brief for NE0 (Fable 5.1 xhigh)

> Read `repository-information/NETWORK-EVENTS-DESIGN-PLAN.md` in full — it is the proposal for the Network + Events design gate, written 2026-09-19 at v06.71r on Fable 5.1 High. Then read, in this order: `PHASE6-CLASSROOM-DESIGN.md` (the shape of an approved gate output), `PROFILER-COVERAGE-PLAN.md` §2 (the model rule), `PROFILER-SCHEMA.md` §"Field notes" and §"Named-projects registry" (the private layer and the registry pattern), `.claude/rules/profiler-app.md` §"Scheduled Refreshes" (why a Routine cannot push), `KNOWN-CONSTRAINTS-AND-FIXES.md`, and the `Receipts.gs` config header and `saveReceipt`/`uploadReceipt`/`geminiExtractFromBase64_` (the pattern Network inherits).
>
> Hold the gate as a conversation with the developer over §3's sixteen decisions, one at a time, recommendation first. Stress-test D3 (the public/private line), D7 (admitted tiers), D13 (the Classroom prefix) and D15 (browser-side Gmail send) hardest — they are the ones with the longest consequences. Where the developer overrides a recommendation, record the override and its reason in the row; do not re-argue.
>
> Then, in one push: amend §3 so every row reads as a decision (header line "approved YYYY-MM-DD"); flip §11's NE0 row; write `repository-information/NETWORK-SCHEMA.md` (objects, tabs, enums of D5, id formats, the public/private line, the peer ops' request and response shapes) and `repository-information/EVENTS-SCHEMA.md` (the §5.1 registry shape, the roster row shape, the score terms of §5.4, the `Signals` row shape) as skeletons the build sessions edit against; write §7.x-style paste-in briefs for N0 and E0 only — later briefs are written at the close of the session before them, as the Classroom program did. Do not scaffold either app in the gate session. Standard Pre-Commit train; CHANGELOG entry; README tree entries for the two schema files.
>
> **Done when:** every D row carries a decision; both schema files exist and are cited by name in §8's N0 and E0 rows; the N0 and E0 briefs exist; the developer has said which of §12's eight questions are settled and which stay open.

### 13.1 · The NE0 session, step by step (written 2026-09-19 at the developer's request, before the session runs)

> **Ran 2026-09-19 09:55 PM → 2026-09-20 EST on Fable 5.1 xhigh (v06.74r).** Every step below was executed in order; the decisions are in §3, the schemas in `NETWORK-SCHEMA.md` / `EVENTS-SCHEMA.md`, the briefs in §13.3–13.4. The one thing the reading surfaced that the brief did not anticipate: D15 collided with §4.4's Gmail/Calendar sweep, resolved as import-only (recorded in §4.4, §8 N4, §9, §10).

Budget ≈ 60–90 minutes of a Fable 5.1 xhigh session; one push at the end.

| Step | What the session does | Output | ≈ time |
|---|---|---|---|
| 0 | Session Start Checklist; read this file in full and the six pointers in the §13 brief | — | 10 min |
| 1 | **Restate the six settled rows** (D1 "Ecosystem", D3, D10, D11 widened, D13, D15 in-app drafts) and confirm nothing in the reading changed them | one paragraph in chat | 3 min |
| 2 | **Walk the eleven open rows one at a time**, recommendation first, in this order — D4 account model · D5 taxonomy · D7 admitted tiers · D8 ids and soft delete · D9 privacy posture · D6 extraction (with §12.1, the processor question) · D2 form factor · D14 quota · D17 the people route · D12 plans · D16 build order. For each: the session states the recommendation in two sentences, names the one alternative if a real trade-off exists, asks, records the answer and the reason in the row. Where the developer overrides, the override is recorded without re-argument | §3 rows rewritten as decisions; header "approved YYYY-MM-DD" | 25–35 min |
| 3 | **Settle the §12 questions the decisions touch** — 1 (processors) with D6, 2 (consumer vs Workspace) with D14, 4 (team scope) with D7, 5 (Places billing) with D12, 6 (seats) with the E3 defaults; 3, 7 and 8 are already answered by today's decisions | §12 rows marked settled / open | 10 min |
| 4 | **Write `NETWORK-SCHEMA.md`** — the five tabs of §4.1 with every column typed; the D5 enums with display labels (flat server list + grouped client map, the Receipts idiom); id formats (`a-`, `c-`, `i-`, `s-` + base36); the public/private line stated once; the two peer ops' request and response shapes (`nop=accounts`, `nop=signals`); the `Signals` row shape shared with Events; the vCard field mapping; the audit-row rule (event + user, never card contents) | new file, ~120 lines, skeleton + Edits | 10 min |
| 5 | **Write `EVENTS-SCHEMA.md`** — the §5.1 registry shape with every field typed and the `kind`/`status` enums; the roster row shape with the probe record; the `Stars` / `Plans` / `Meetings` tabs; the §5.4 score terms and default weights; the `Proposed` diff row; the two peer ops (`eop=today|starred|signals`); the ICS field mapping; the corpus-count corollary | new file, ~120 lines | 10 min |
| 6 | **Write the N0 and E0 paste-in briefs** as §7.x-style blocks in this file — N0: setup-script invocation JSON, manifest and icons to hand-make, the tier matrix and verifier, the tab bootstrap, the Profiler "Ecosystem" relabel with the verifier line, the quota counter, the done-when; E0: the extraction script, the Appendix A rows as input, the probe procedure from `scraper-sources.md`, the checker, the done-when. Later briefs are written at the close of the session before them | §13.2, §13.3 | 10 min |
| 7 | **Push train**: flip §11's NE0 row to Done with the version; README tree entries for the two schema files; CHANGELOG with the gate's prompt; repo version bump; one push. No app scaffolding in this session | v06.7xr | 5 min |

**What the developer brings:** answers to the eleven rows (the recommendations are the defaults, so silence on a row means "as recommended"), and the §12 answers listed in step 3 — in particular whether a Google Cloud project with billing exists (§12.5) and whether Gemini/Claude may process card PII (§12.1).

**Done when** (unchanged from the brief): every D row carries a decision; both schema files exist and are cited by name in §8's N0 and E0 rows; the N0 and E0 briefs exist; the developer has said which of §12's questions are settled.

### 13.2 · The paste-in prompt for the gate session (Fable 5.1 xhigh)

> Start the NE0 design gate for Network + Events. Read `repository-information/NETWORK-EVENTS-DESIGN-PLAN.md` in full, then follow §13 (the brief) and §13.1 (the step-by-step) exactly. Six rows are settled and are not re-opened: D1 (Profiler's explorer becomes "Ecosystem" in N0), D3 (people private in Drive, the event registry public on Pages), D10 (hybrid corpus + Opus 5 xhigh research sources), D11 (the full §5.5.1 signal-source catalogue, quoted-people extraction included), D13 (`event:` prefix deferred, `contact:` never), D15 (in-app editable drafts exported or copied to any mail client — the apps never send). Walk the remaining eleven with me one row at a time in §13.1's order — D4, D5, D7, D8, D9, D6, D2, D14, D17, D12, D16 — recommendation first in two sentences, one alternative only where a real trade-off exists, then stop and wait for my answer before the next row; my silence on a row means "as recommended". Then settle the §12 questions each decision touches (I will tell you whether a Google Cloud project with billing exists and whether Gemini or Claude may process card PII), write `NETWORK-SCHEMA.md` and `EVENTS-SCHEMA.md` with exactly the contents §13.1 steps 4–5 list (skeleton + Edits, never a single large Write), write the N0 and E0 paste-in briefs as §13.3 and §13.4 of the plan, flip §11's NE0 row to Done with the version, and push once. Do not scaffold either app in this session. Normal Session Start, Pre-Commit and Pre-Push checklists on a `claude/*` branch restarted from `origin/main`. The repo CHANGELOG stands at 98 non-exempt sections against a 100 trigger: if your push lands on 2026-09-20 EST or later (`TZ=America/New_York date`), it rotates the 2026-09-14 date group (20 sections) into the archive with SHA enrichment on every header — run `git fetch --unshallow origin main` first.

### 13.3 · Paste-in brief for N0 — Network scaffold (Fable 5.1 High; written at the gate, 2026-09-20)

> Scaffold the Network app — phase N0 of `repository-information/NETWORK-EVENTS-DESIGN-PLAN.md`. Read first, in this order: the plan's §3 (every D row is decided — D7 is **admin-only**, D14 is **no data poll**, D1 is the Profiler "Ecosystem" relabel), `repository-information/NETWORK-SCHEMA.md` in full (the single source of truth you build against — §1 ids, §2 access model, §3 tabs, §4 enums), `.claude/rules/gas-scripts-reference.md` §"Setup Steps" and §"Partial OAuth Grants", the `Receipts.gs` config header and `ensureReceiptTabs_()`, `clAdmitted_` / `CL_ROLE_CAPS` in `Classroom.gs` and `clAdmitted()` in `Classroom.html`, `scripts/verify-profiler-roles.py`, and the header comment of `scripts/setup-gas-project.sh`. Do not build capture, extraction or the list — that is N1. Do not scaffold Events.
>
> **1 · Run the setup script.** Pipe the config JSON from the developer's gas-project-creator page (or write it) into `bash scripts/setup-gas-project.sh` with `PROJECT_ENVIRONMENT_NAME: "Network"`, `TITLE: "Network"`, `INCLUDE_AUTH: "true"`, `AUTH_PRESET: "hipaa"`, `ACL_PAGE_NAME: "Network"`, `PORTAL_ICON: "📇"`, `PORTAL_DESCRIPTION: "Business-card capture and the account-centric contact layer."`, the developer's `SPREADSHEET_ID` and `CLIENT_ID`, `SHEET_NAME: "Live_Sheet"`, and `DEPLOYMENT_ID` left as the placeholder until the developer deploys; let `MASTER_ACL_SPREADSHEET_ID` default from the Global ACL config and confirm the generated `.gs` and `.config.json` carry the real id (Setup GAS Project Command step 3). The script creates the ten files, registers the GAS Projects table row, the README tree entries, the REPO-ARCHITECTURE nodes and the workflow deploy step. Fix any placeholder warning it prints.
>
> **2 · PWA.** Hand-make `live-site-pages/network.webmanifest` on the `receipts.webmanifest` shape (`id`/`start_url`/`scope` = `./Network.html`, `display: standalone`) and `images/network-icon-192.png` + `-512.png` (generate with Pillow — a flat glyph on the app's accent, `purpose: any maskable` on the 512); add the `manifest-src 'self'` PROJECT OVERRIDE comment and directive to **both** CSP `<meta>` tags, `<link rel="manifest">`, `<meta name="theme-color">` and `apple-touch-icon`, exactly as `Receipts.html` lines 8–21. `worker-src 'none'` stays — no service worker (D2).
>
> **3 · The door (D7).** `NW_ROLE_CAPS` in `Network.gs` with all four tier keys (`admin` holding `contacts` · `accounts` · `profiler` · `signals` · `drafts` · `export` · `purge`, the other three empty), `nwAdmitted_(sess)` = `role === 'admin'`, `nwCan_(sess, cap)` and `nwRequire_` on the Classroom pattern with an audit entry for every turned-away tier; the client mirror `NW_ROLE_CAPS` / `nwAdmitted()` / `nwCan()` in `Network.html` rendering the turned-away card and issuing **zero** data requests for non-admin tiers. Write `scripts/verify-network-roles.py` on the `verify-profiler-roles.py` shape: admin sees the empty list, contributor / analyst / viewer see the card, `?as=<tier>` preview keeps only-subtracting semantics.
>
> **4 · Tabs and folders.** `ensureNetworkTabs_()` creating `Accounts` · `Contacts` · `Interactions` · `Signals` · `Mailings` · `Drafts` · `Shares` · `Profiles` with exactly the columns and order in `NETWORK-SCHEMA.md` §3 (in-place header upgrade idiom, frozen row 1); the flat enum lists of §4 (`NW_RELATIONSHIPS`, `NW_STAGES`, `NW_ROLES`, `NW_INTERACTION_KINDS`, `NW_SIGNAL_KINDS`) in the `.gs` and the grouped `NW_ENUMS` map with labels in the `.html`; `NW_ID_RE` and `nwNewId_(prefix)` per §1 (8 random bytes → 13 base36 characters, collision-checked against the tab — never a name, never a date); `nwfolders` / `setnwfolders` admin ops parking the `Network App` and `_inbox` folder ids in Script Properties (the Profiler `recfolders` precedent). Copy `resolveOwnerScope_` / `resolveOwnerSet_` and the not-found-not-forbidden convention from `Receipts.gs` verbatim.
>
> **5 · Quota discipline (D14).** `HEARTBEAT_INTERVAL` 600 s in the `.gs` AUTH config **and** the `.html` config block (Pre-Commit [PC-SESSION-SYNC] #20 — both sides, same commit); disable the data poll per `DATA-POLL-ARCHITECTURE.md` (if the template has no off switch, set `DATA_POLL_INTERVAL: 0` and guard the timer so it never arms), fetch on load, on `visibilitychange` and after writes. Add `?action=api&op=quota` in the shared template region beside `op=aclhealth`: counts today's `SessionAuditLog` rows (EST) grouped by event and returns `{ success, date, executions, byEvent }` — this is the op Q0 copies into the other eight projects.
>
> **6 · Profiler relabel (D1).** In `Profiler.html`: the `#network` heading (`ovEl('h2', null, 'Ecosystem Network')`, ~line 3519) → `'Ecosystem'`, and the denied-view sentence for that route to match; in `scripts/verify-profiler-roles.py` the `'ecosystem network'` label in the deep-link loop (~line 364) → `'ecosystem'`. The `network` capability key and the `#network` hash stay. Profiler page version bump + page changelog + README tree display, per the normal train. Run `python3 scripts/verify-profiler-roles.py` and confirm the 13 × 4 matrix is unchanged.
>
> **7 · Deploy hand-off and verification.** Write the developer's steps in chat: create the Apps Script project, paste the code, set `appsscript.json` from `gas-scripts-reference.md` (including `script.scriptapp`), link the GCP project, deploy as Web app, paste `DEPLOYMENT_ID` into `Network.config.json` (sync per [PC-GAS-CONFIG] #14), set `GITHUB_TOKEN`, run any function to authorise **and tick every consent checkbox**, load the page once so `registerSelfProject()` creates the ACL column, tick TRUE, run `clearAllAccessCache`. Then verify the §8 done-when: sign-in works on the live page, the deploy webhook answers a GET probe, `verify-network-roles.py` passes for all four tiers, and `scripts/playwright-harness.py` screenshots of admin's empty list and the three turned-away cards at phone width read clean with zero page errors. `node --check` on the `.gs` copy and `scripts/check-gas-inner-scripts.js`.
>
> **8 · Train.** Normal Pre-Commit and Pre-Push checklists; CHANGELOG entry; flip §11's N0 row to Done with the version and write the **N1 brief as §13.5** of the plan before closing (the Classroom rule: the next brief is written at the close of the session before it). One push.

#### The paste-in prompt for the N0 session (Fable 5.1 High) — written 2026-09-20

> Run N0 — the Network scaffold — from `repository-information/NETWORK-EVENTS-DESIGN-PLAN.md`. Read the plan's §3 (every decision row is decided — D7 admin-only, D14 no data poll, D1 the Profiler "Ecosystem" relabel), §8 (the N0 and Q0 rows) and §13.3 (the N0 brief — follow its eight steps exactly, in order), then `repository-information/NETWORK-SCHEMA.md` in full, and the reading list §13.3 opens with. Do not build capture, extraction or the list (that is N1); do not scaffold Events (E1); do not touch Scraper. Before running `scripts/setup-gas-project.sh`, ask me for the values only I hold — `SPREADSHEET_ID`, `CLIENT_ID`, and whether the Global ACL default for `MASTER_ACL_SPREADSHEET_ID` is right — then run it once; every other key in the JSON is fixed by §13.3 step 1. Build the PWA files with the `manifest-src 'self'` override on both CSP tags, the admin-only door on both sides and `scripts/verify-network-roles.py`, `ensureNetworkTabs_()` with exactly the `NETWORK-SCHEMA.md` §3 columns and the §4 enum mirrors, `NW_ID_RE` / `nwNewId_`, the folder ops, the D14 intervals (600 s heartbeat, no data poll — both `.gs` and `.html` per [PC-SESSION-SYNC] #20) and the `op=quota` API, and the Profiler "Ecosystem" relabel with its Profiler page bump, page changelog and a clean `scripts/verify-profiler-roles.py` run. Verify with `node --check` on the `.gs` copy, `scripts/check-gas-inner-scripts.js`, `python3 scripts/check-readme-tree.py`, and served Playwright screenshots (`scripts/playwright-harness.py`) of admin's empty list and the three turned-away cards at phone width with zero page errors. End by writing my deploy hand-off steps in chat (§13.3 step 7), flipping §11's N0 row to Done with the version, and writing the N1 brief as §13.5 of the plan — then one push. Normal Session Start, Pre-Commit and Pre-Push checklists on a `claude/*` branch restarted from `origin/main`; run `git fetch --unshallow origin main` first. The repo CHANGELOG rotated at v06.75r — read its `Sections:` counter before assuming anything about rotation.

#### The paste-in prompt for the Q0 session (Fable 5.1 Medium) — written 2026-09-20 at the close of N0

> Run Q0 — the quota-counter rollout — from `repository-information/NETWORK-EVENTS-DESIGN-PLAN.md`: §3 row D14 and the §8 Q0 row are the whole spec. Read first `nwQuotaProbe_()` and the two `op=quota` dispatch lines beside `op=aclhealth` in `googleAppsScripts/Network/Network.gs` `doGet` (the source of truth — copy it, do not redesign it), the `op=aclhealth` dispatch in `Receipts.gs` `doGet` as the precedent for where a PROJECT-marked unauthenticated probe sits inside the AUTH `doGet`, `scripts/check-acl-health.sh` (the probe-script shape to follow, including how it discovers projects from the `.gs` files and skips `YOUR_DEPLOYMENT_ID` projects), and `.claude/rules/gas-scripts.md` §"Template vs Project Code Separation". Then, in **one commit**: (1) copy the function into the eight existing projects — Classroom, Globalacl, MasterACL, Profiler, Receipts, Scraper, Testauthgas1, Testauthhtml1 (`Claspdeploytest` has no config and is not a project) — as `quotaProbe_()` with the same body and comment, and rename Network's `nwQuotaProbe_` to `quotaProbe_` so all nine read identically for Q's grep; it depends only on template globals (`SPREADSHEET_ID`, `AUTH_CONFIG`, `ACL_PAGE_NAME`, `VERSION`, `getEpochCache`) that every auth project has, and every one of the eight already runs with `ENABLE_AUDIT_LOG` on (the two `standard`-preset projects, Classroom and Profiler, override it on), so no preset changes; (2) add the `if (action === 'api' && op === 'quota')` dispatch beside `op=deploy` in each `doGet`, marked `// PROJECT:` exactly as Network's is; (3) write `scripts/check-quota.sh` on the `check-acl-health.sh` shape — one row per deployed project (`page`, `gasVersion`, `date`, `executions`, the top three `byEvent` keys), a total across the fleet against the 20,000/day account quota, `audit_log_disabled` / `spreadsheet_not_configured` surfaced as warnings not failures, exit 0 healthy / 1 any probe unreachable / 2 nothing probed; (4) nine GAS bumps with nine GAS changelog entries (user-facing: "a daily execution counter the operator can read"), no page bumps (no HTML changes), README tree entry for the script, CHANGELOG entry, flip §11's Q0 row to Done with the version. Verify with `node --check` on every `.gs` copy and `scripts/check-gas-inner-scripts.js`; after the merge, run `bash scripts/check-quota.sh` and paste its table into the CHANGELOG entry's Notes (the three placeholder-id projects — Globalacl, Testauthgas1, Testauthhtml1 — never deploy, so their copies are repo-only bookkeeping and the script skips them; the deployed six plus Network answer). Do not touch `Events` (E1 inherits the op from the shared template region when it is scaffolded), do not change any interval (that is Q), do not add auth to the probe (counts only, never a user or a details cell — the trust model is `aclhealth`'s). Normal Session Start, Pre-Commit and Pre-Push checklists on a `claude/*` branch restarted from `origin/main`; run `git fetch --unshallow origin main` first. The repo CHANGELOG stands at `Sections: 100/100` with four sections dated 2026-09-20 EST exempt: **if your push lands on 2026-09-21 EST or later, 100 non-exempt reaches the 100 trigger and the oldest date group rotates** into the archive with SHA enrichment on every header — read the counter and `CHANGELOG-archive.md` §"Rotation Logic" before assuming otherwise.

### 13.4 · Paste-in brief for E0 — the registry and the roster (Opus 5 xhigh for the research, Fable 5.1 Medium for the roster rows; written at the gate, 2026-09-20)

> Build the Events registry and source roster — phase E0 of `repository-information/NETWORK-EVENTS-DESIGN-PLAN.md`. This is research and data, not app code: nothing in it touches Network, so it runs beside N1–N2. Read first: the plan's §5.1–5.2 and **Appendix A** (the 64-row seed calendar — your input, every row re-verified here), `repository-information/EVENTS-SCHEMA.md` §1, §3, §4, §11 and §12 in full (the registry row, the roster row, the corollary, the checker's assertions), `.claude/rules/scraper-sources.md` §"Source roster: mandatory check before suggesting a source" (the probe procedure and the blocked-vs-offline rule — it applies verbatim), `PROFILER-SCHEMA.md` §"Named-projects registry" (the registry pattern), `live-site-pages/profiler-data/profiler-segments.json` (the `audience[]` vocabulary), and `scripts/check-profiler-reports.py` (the checker style to follow). Do not scaffold `Events.html` / `Events.gs` — that is E1.
>
> **1 · The corpus pass (deterministic, first).** Grep `live-site-pages/profiler-data/*.profile.json` for the 31 event strings §1 of the plan counts (DCD, GTC, Bisnow, OCP, RE+, SNEC, Intersolar, ees, Computex, Data Center World, CERAWeek, iMasons …) and build the string → slug table inside `scripts/extract-corpus-events.py`; the script walks `recentDevelopments[]`, `productsAndServices[]`, `technicalSpecs[]`, `strategyRead[]` and `sources[]`, emits `mentions[] { slug, where }` per event and a seed registry row (slug, name, series, `status: tentative`) for every event not yet present; idempotent, `--check` mode. Run it; every one of the 31 must end with ≥ 1 mention.
>
> **2 · The research (Opus 5 xhigh).** For every Appendix A row and every corpus seed, read the **organiser's own page** (WebFetch; a (U) row is third-party until you have): confirm `name`, `series`, `organiser`, `kind`, `start`/`end`, `tz`, `city`/`region`/`country`, `venue`, `website`, `registrationUrl`, `exhibitorListUrl`/`agendaUrl`/`speakersUrl`/`floorPlanUrl` where published, `hours[]` where published, `venueLatLng` where the page or a verified geocode gives it; set `audience[]` from the segments registry (≥ 1 id, justified by who exhibits and who attends), `relevance` 1–5 (Appendix A's Rel column is the prior), `tierNote`, `editions[]` for the prior year where known, and `sources[] { sourceKey, kind, url, lastConfirmed: today }`. A row you could not read stays `tentative` with the third-party listing as its `manual` source and a note — never `confirmed` on a listing. Resolve or record the Appendix A caveats: the DCD New York 2027 date conflict (DCD is blocked to non-browser clients — record what the two listings say and leave it `tentative`), NAATBatt 2027 (weakly sourced), the "not yet dated for 2027" list (re-check each; add if dated). Add the sub-mega tier the corpus never names where the organiser page confirms it (Infocast, GCPA, Bisnow regionals, iMasons Luma socials). Target ≥ 60 events.
>
> **3 · The roster (Fable 5.1 Medium, or the same session after step 2).** One `events-sources.json` row per organiser feed or page, keyed per `EVENTS-SCHEMA.md` §1. **Probe every row live before writing it**, the `scraper-sources.md` way: `curl -sL -o /tmp/f -w '%{http_code}\n' --max-time 20 "$URL"`, then for `jsonld` count `application/ld+json` blocks containing `"@type": "Event"` (or `BusinessEvent`) and read the newest `startDate`; for `ics` count `BEGIN:VEVENT` and read the newest `DTSTART`; read `/robots.txt` for the fetched path; write `lastProbe { at, status, itemCount, newestItem }` and `robots`. A `403` with `server: cloudflare` / `cf-mitigated: challenge` or a `Just a moment…` title is **blocked** — record `blocked: "<reason> <date>"`, keep the row, do not try another URL or a browser User-Agent. Known from the gate: 10times (Cloudflare + partner-only ToS), DCD, OCP, Enlit — hand-maintained rows. Never add a Google-News feed as a substitute; never add LinkedIn, 10times or an organiser attendee list as a source.
>
> **4 · The checker.** Write `scripts/check-events-registry.py` implementing every assertion in `EVENTS-SCHEMA.md` §12 (slug rule and uniqueness, dates, IANA tz, `audience[]` ids exist, `sources[].sourceKey` in the roster and the URL host on that row, `mentions[].slug` resolves, `lastUpdated` and ≥ 1 confirmed source per row, no un-probed roster row, `past` iff `end` < today; `--fix-past` flips status only). It must exit 0 on the files you commit.
>
> **5 · Files and train.** `live-site-pages/events-data/events.json` and `events-sources.json` per the §3/§4 shapes (`schemaVersion: 1`, `built`), sorted by `start` then `slug`; the two scripts; README tree entries for the new `events-data/` folder, both files and both scripts (NEW FOLDERS in the end-of-response block); mirror `profiler-data/`'s treatment in `REPO-ARCHITECTURE.md` if it appears there. **No Scraper seed registration yet** (that is E2/E4), no `.ics` yet (E1 publishes `events.ics`). The registry's counts are never taught (§11). Normal Pre-Commit and Pre-Push checklists; CHANGELOG entry naming how many rows were verified on the organiser's page, how many stay `tentative`, and every blocked source with its reason; flip §11's E0 row to Done with the version and write the **E1 brief as §13.6** before closing. One push — or two if the roster runs as its own Fable Medium session.
>
> **Done when** (§8): checker exits 0; every event carries `lastUpdated` and a `sources[]` row whose host is in the roster; the 31 corpus events all have `mentions[]`; the roster has no un-probed row; ≥ 60 events, each `confirmed` only on an organiser page read this session.

### 13.5 · Paste-in brief for N1 — capture → extraction → review → save (Fable 5.1 High, two sessions; written at the close of N0, 2026-09-20)

> Build N1 of `repository-information/NETWORK-EVENTS-DESIGN-PLAN.md` — the card pipeline of §4.2 on the N0 scaffold. Read first, in this order: the plan's §3 (D6 **Gemini only**, D8 opaque ids, D9 the privacy posture), §4.2 in full, `repository-information/NETWORK-SCHEMA.md` §1, §3, §6, §7, §12 and §13 (ids, the `Contacts` / `Accounts` / `Interactions` columns, the Drive layout, the extraction schema with per-field `confidence{}`, the audit-row rule, soft delete), the N0 code you inherit — `Network.gs` PROJECT region (`NW_ROLE_CAPS`, `nwRequire_`, `ensureNetworkTabs_`, `nwNewId_`, `nwFoldersGet_` / `nwFoldersSet_`, `handleNetworkOp_`, `nwListRows_`) and `Network.html` PROJECT region (`nwApi`, `nwLoadList`, `nwAfterWrite`, `NW_ENUMS`) — then in `Receipts.gs` the config header, `geminiExtractFromBase64_` (the three-leg retry plan and the `responseSchema` idiom) and `saveReceipt`, and in `Receipts.html` `compressImage`, the own-Drive upload (`drive.file` token, `Receipts App/` folder creation, `_gasPostBody`) and the review card. Do not build the list's filters, exports or drafts (N3), accounts UI beyond what save needs (N2), or anything in Events.
>
> **Session 1 — capture and extraction.** (1) The capture inputs on the Receipts shape (`capture="environment"` single, `multiple` batch of 15) with a **front / back toggle** so one review holds a pair; `compressImage` unchanged at 2,000 px. (2) The **IndexedDB offline queue**: when `navigator.onLine` is false the compressed base64 pair is queued (no Worker) and drained on `online` with the same pipeline; a queued count shows on the capture card. (3) Own-Drive upload with the user's `drive.file` token into `Network App/_inbox/` — the folder tree is created browser-side on first use and its ids parked through `nop=setfolders` (N0's registry; read them back with `nop=folders` on load); filenames `<c-id>-front.jpg` / `-back.jpg`, the id minted by a new `nop=newid` op **before** upload so the filename is opaque from the first byte. (4) `nwExtractFromBase64_(frontB64, backB64, mime)` in the `.gs`: `geminiExtractFromBase64_` with the §7 `responseSchema` (`fullName … languages[], rawText, confidence{}`), the same pinned `GEMINI_MODEL` / `GEMINI_FALLBACK_MODEL` and three-leg retry, the key from **this project's** `GEMINI_API_KEY` Script Property, both images as `inline_data` parts in one call, the prompt rules of §7 (romanise CJK and keep the native script in parentheses; a second image is the back of the same card; never invent a field; honest per-field confidence). No second vendor, no `ANTHROPIC_API_KEY` (D6). (5) Client-side QR decode with `BarcodeDetector` where present, merged before the model call. Audit rows for the extraction carry the `c-` id and the field count only — never a card field (§12).
>
> **Session 2 — review, dedupe, save.** (6) The review card: the extracted fields editable, `role` from `NW_ENUMS.role`, the account block (company name, proposed `relationship` — `target` by default for a card scanned at a show — and `stage: none`), low-confidence fields (`confidence < 0.7`) outlined with **Retry extraction** and **Swap front / back**; `Source Event` free text in N1 (the bridge fills it in B), `Met Date` defaulting to today, `Consent Marketing` defaulting to `unknown`, `Do Not Contact` off. (7) Company resolution against `profiler-companies.json` (`name` / `aka[]` / `domains[]`, one public fetch cached page-lifetime) proposing the slug and pre-filling `Segment IDs`; otherwise a new local Account with `nwNormaliseCompany_` as the dedupe key. (8) `nwFindDuplicate_` on normalised email → E.164 phone → normalised name + Account; a hit offers **merge** field-by-field (newest wins by default, both card pairs kept, the absorbed `c-` id recorded in a `merge` Interaction), never a silent reject and never a bare "save anyway". (9) `nop=save`: validates every enum against the flat lists and the D5 `stage` rule, writes the Contact + the new-or-existing Account + one `scan` Interaction, moves the two Drive files from `_inbox/` to `<Company>/` **browser-side** (`files.update` with `addParents` / `removeParents`, the per-Account folder id parked through `setfolders.accounts`), then calls `nwAfterWrite()` so the list refreshes without a poll (D14). Soft delete + one-tap restore on the row (`nop=delete` / `nop=restore` set and clear `Deleted At`). (10) `scripts/check-network-schema.py` per `NETWORK-SCHEMA.md` §14: the three enum mirrors identical to the schema, every test id matching `NW_ID_RE`, no id-generating function taking a name or a date, every `auditLog(` details argument built from ids and counts only. Extend `scripts/verify-network-roles.py` with the capture card present for admin and absent for the other three tiers.
>
> **Train (both sessions).** `node --check` on the `.gs` copy, `scripts/check-gas-inner-scripts.js`, `python3 scripts/check-readme-tree.py`, `verify-network-roles.py`, `check-network-schema.py`; page + GAS bumps and changelogs; CHANGELOG entry; README tree entry for the checker; flip §11's N1 row to Done with the version at the close of session 2 and write the **N2 brief as §13.6** before closing. One push per session.
>
> **Done when** (§8): 20 real cards including three Chinese-script and two two-sided round-trip with at most one manual correction each; an airplane-mode capture drains on reconnect; the Drive tree shows `Network App/<Company>/<c-id>-front.jpg`; no name-derived id anywhere (the checker exits 0).

#### The paste-in prompt for N1 session 1 (Fable 5.1 High) — written 2026-09-20 at the close of Q0

*Model / effort: **Fable 5.1 High**, per §8's N1 row and §10 (build sessions on High; xhigh is spent only on the gate and X). E0 (§13.4, Opus 5 xhigh for the research) can run beside it on a different branch per D16 — it touches only `live-site-pages/events-data/` and the plan, so the two do not collide. Session 2's prompt is written by session 1 at its close, the way this one was written at the close of Q0.*

> Run N1 session 1 — capture and extraction — from `repository-information/NETWORK-EVENTS-DESIGN-PLAN.md`: §13.5 is the brief (follow its reading list in order, then steps 1–5 of **Session 1** exactly; do not start session 2's steps 6–10 — the review card, dedupe, save and the schema checker are the next session's), §4.2 is the pipeline it implements, and `repository-information/NETWORK-SCHEMA.md` §1, §3, §6, §7, §12, §13 are the shapes you build against. D6 is **Gemini only** (the Receipts `geminiExtractFromBase64_` idiom with the §7 `responseSchema`, `GEMINI_API_KEY` from this project's Script Properties, no second vendor), D8 opaque ids (`nop=newid` mints the `c-` id **before** upload so the filename is opaque from the first byte), D9 the privacy posture (audit rows carry the `c-` id and a field count only — never a card field), D14 no data poll (fetch on load, on `visibilitychange`, after writes; `nwAfterWrite()` is the refresh). Build in the PROJECT regions of `Network.gs` and `Network.html` only — never edit a TEMPLATE region. Verify with `node --check` on a `.js` copy of `Network.gs` (Node refuses the `.gs` extension), `scripts/check-gas-inner-scripts.js`, `python3 scripts/check-readme-tree.py`, `scripts/verify-network-roles.py` (the capture card present for admin, absent for the other three tiers, at phone width, zero page errors) and served Playwright screenshots of the capture card with a queued count. Page + GAS bumps with changelogs, CHANGELOG entry, no §11 flip (N1 closes at session 2) — then write my hand-off in chat: the `GEMINI_API_KEY` Script Property to set, the `drive.file` consent the first upload will ask for, and what to photograph for the session-2 done-when (20 real cards, three Chinese-script, two two-sided). Do not touch Events, the list's filters or exports (N3), accounts beyond what capture needs (N2), or any interval (Q). Normal Session Start, Pre-Commit and Pre-Push checklists on a `claude/*` branch restarted from `origin/main`; run `git fetch --unshallow origin main` first. The repo CHANGELOG stands at `Sections: 103/100` with seven sections dated 2026-09-20 EST exempt (96 non-exempt): **if your push lands on 2026-09-21 EST or later, the exemption lifts and 103 non-exempt is over the 100 trigger, so the oldest date group (2026-09-15) rotates** into the archive with SHA enrichment on every header — read the counter and `CHANGELOG-archive.md` §"Rotation Logic" before assuming otherwise. One push.

#### The paste-in prompt for N1 session 2 (Fable 5.1 High) — written 2026-09-21 at the close of session 1

*Model / effort: **Fable 5.1 High**, per §8's N1 row and §10 (build sessions on High). Session 1 shipped more than its five steps — at the developer's request while scanning, the held card already carries an inline editor, removable "unclear in the scan" notes with Rescan, Delete, a one-/two-sided batch toggle and the name-case rule — so session 2 builds the review card on that editor rather than from Receipts, and its done-when runs against the **20 real cards already held in the phone's IndexedDB** (v01.09w). Keep every one of them.*

> Run N1 session 2 — review, dedupe, save — from `repository-information/NETWORK-EVENTS-DESIGN-PLAN.md`: §13.5's **Session 2** paragraph (steps 6–10) is the brief, §4.2 the pipeline, and `repository-information/NETWORK-SCHEMA.md` §1, §3, §4, §6, §7 (dedupe paragraph), §12, §13 and §14 the shapes and the checker spec. Read first, in this order: the plan's §3 (D4, D5, D8, D9, D14), the `Network.gs` PROJECT region as it stands after session 1 (`nwExtractOp_`, `nop=newid`, `handleNetworkOp_`, `nwNewId_`, `nwNormaliseCompany_`, `nwFoldersGet_` / `nwFoldersSet_` with its `accounts` map, `nwListRows_`, the enum lists), the `Network.html` PROJECT region (`nwProcessPair`, the IndexedDB `pending` store and its record shape — `id`, `extraction` in the §7 shape with `confidence{}`, `viaQr`, `sides`, `frontLink`, `backLink`, `driveError`, `createdAt`, `dismissed{}`, `edited` — `nwRenderStrip`, `nwEditCard` / `NW_EDIT_FIELDS`, `nwDeleteCard`, `nwApiBody`, `nwUploadPair`, `nwEnsureFolders`, `_nwFolders`, `nwCapName`, `nwAfterWrite`), then in `Receipts.gs` `saveReceipt` and in `Receipts.html` the review card only for the select idiom. **Session 1's UI decisions stand**: no `c-` id and no confidence numbers shown on a card, no "missing field" cues, names ALL CAPS or all-lower become First-letter caps and mixed case is never touched, the Front / Back / Edit / Delete pill row, and the two-half control rows. The developer scanned **20 real cards (three Chinese-script, two two-sided)** into v01.09w; they are held in that phone's IndexedDB `pending` store with their photos in Drive `Network App/_inbox/`. **Do not change the IndexedDB name or version and do not drop or rewrite `pending`** — session 2's save reads exactly those records.
>
> Build, in the PROJECT regions of `Network.gs` and `Network.html` only: (6) the review card **on the existing editor** — add `role` from `NW_ENUMS.role`, the account block (company name pre-filled from `extraction.company`, `relationship` defaulting to `target`, `stage: none`, the D5 rule that `stage` may leave `none` only for `target` · `customer`), `Source Event` free text, `Met Date` defaulting to the record's `createdAt` date, `Consent Marketing` defaulting to `unknown`, `Do Not Contact` off, the low-confidence outline reusing the existing note/`nwUnclearFields`, **Retry extraction** (re-runs `nop=extract` with the same `c-` id from the held base64 if still present, else from the Drive files via the user's token) and **Swap front / back**; a **Save** pill on every held card and a **Save all** for the stack. (7) Company resolution against `live-site-pages/profiler-companies.json` (`name` / `aka[]` / `domains[]`, one public fetch cached page-lifetime) proposing the slug and pre-filling `Segment IDs` from the registry's `segments[]`; otherwise a new local Account keyed by `nwNormaliseCompany_`; the developer confirms in the account block. (8) `nwFindDuplicate_` server-side on normalised email → E.164 phone → normalised name + Account, answered before the write so the client can offer **merge** field-by-field (newest wins by default, both card pairs kept, the absorbed `c-` id recorded in a `merge` Interaction) — never a silent reject, never a bare "save anyway". (9) `nop=save` (body-POST — `Raw Extraction` carries the model response): validate every enum against the flat lists and the D5 stage rule, write the Contact + the new-or-existing Account + one `scan` Interaction; then **browser-side** move the two Drive files from `_inbox/` to `<Company>/` (`files.update` with `addParents` / `removeParents`; the per-Account folder created on first save and parked through `setfolders` `accounts`), write the new links back, delete the `pending` record, and call `nwAfterWrite()` (D14). The list rows then need a row surface: name · title · company, tap for the full row (`nop=get`), soft **delete** with one-tap **restore** (`nop=delete` / `nop=restore` set and clear `Deleted At`); an Account with live Contacts cannot be deleted (`account_has_contacts`). Audit rows carry ids and counts only (§12). (10) `scripts/check-network-schema.py` per §14 — the three enum mirrors identical to the schema's lists, every test id matching `NW_ID_RE`, no id-generating function taking a name or a date, every `auditLog(` details argument built from ids and counts only — plus a README tree entry for it; extend `scripts/verify-network-roles.py` with a save round-trip against the stub (the review card, a duplicate offering merge, the Drive move requests in order, the pending record gone, the list row present, delete → restore).
>
> Verify with `node --check` on a `.js` copy of `Network.gs`, `scripts/check-gas-inner-scripts.js`, `python3 scripts/check-readme-tree.py`, `scripts/verify-network-roles.py` (zero page errors at phone width) and `scripts/check-network-schema.py` (exit 0). Page + GAS bumps with changelogs, CHANGELOG entry, flip §11's N1 row to **Done** with the versions, and write the **N2 brief as §13.6** (or §13.7 if E0 has taken 13.6 by then) before closing — then hand off in chat: which of the 20 held cards to save first (a two-sided one, a Chinese-script one, then the pair of Avantus cards that share a title and must NOT merge), and what "done when" looks like in the Contacts list and the Drive tree. Do not touch Events, the list's filters or exports (N3), accounts beyond what save needs (N2), or any interval (Q). Session-1 lessons that still apply: `GEMINI_API_KEY` is set on the Network project and the `drive.file` consent has been given once; Android kills the browser tab when memory is low while the camera is open, so test on the gallery path first. Normal Session Start, Pre-Commit and Pre-Push checklists on a `claude/*` branch restarted from `origin/main`; run `git fetch --unshallow origin main` first. The repo CHANGELOG stands at `Sections: 85/100` with seven sections dated 2026-09-21 EST — **no rotation is due on any later date either** (85 < 100), so expect none. One push per session.


### 13.6 · Paste-in brief for N2 — accounts and the corpus attachment (Fable 5.1 High, one session; written at the close of N1, 2026-09-21)

> Build N2 of `repository-information/NETWORK-EVENTS-DESIGN-PLAN.md` — the account layer of §4.1 and D4 on what N1 left: every contact already belongs to an Account (`nop=save` resolves or mints one), the review card already proposes the Profiler slug and segments from the registry, and the `Accounts` tab already carries `Profiler Slug`, `Relationship`, `Stage`, `Segment IDs`, `Tags`, `HQ`, `Newsroom URL`, `Notes`. Read first, in this order: the plan's §3 (D4 account-centric, D5 taxonomy with the stage rule, D8 ids and the no-cascade delete, D9 minimum-necessary list ops), §4.1, §8's N2 row and §9's N2 paragraph, `NETWORK-SCHEMA.md` §3 (`Accounts`), §4, §12, §13 (an Account with live Contacts cannot be deleted — `account_has_contacts` is already served by `nop=delete`), then the N1 code you inherit — `Network.gs` `nwAccountResolve_`, `nwAccountFromPayload_`, `nwAccountPublic_`, `nwGetOp_` (already answers an `a-` id), `nwDeleteRestoreOp_`, `nwListRows_` and the list op's `accounts` subset; `Network.html` `_nwAccountsById`, `nwResolveCompany` / `nwRegistry` (the registry index, name / aka / domains → slug + `segmentIds` + `hq`), `nwReviewSection` (the account block on the review card), `nwEnsureAccountFolder` / `nwSafeFolderName`, `nwContactRow` / `nwRowDetail`; then `repository-information/PROFILER-SCHEMA.md` for `decisionMakers[]` (name, title, source) and one `live-site-pages/profiler-data/<slug>.profile.json` to see the shape, and `Profiler.html`'s hash routing for the `#<slug>` deep link. Do not build the list's filters, exports or drafts (N3), Events, the bridge (B), or any interval (Q).
>
> **Build, in the PROJECT regions of `Network.gs` and `Network.html` only.** (1) **The Accounts surface**: an Accounts card under the Contacts list — one row per live Account (name · relationship · stage · contact count), tap for the full row (`nop=get` with the `a-` id) with its live contacts listed beneath; an **Edit** on the row opening the same account block the review card uses (name, relationship, stage under the D5 rule, tags, HQ, newsroom URL, notes, the Profiler link checkbox + segments) and a new `nop=account` op (body-POST) that validates the enums and the stage rule and writes the row (`Updated At`; a rename also rewrites `Normalised Name` and renames the Drive folder browser-side on the next save — §6); Delete → `nop=delete` (already refuses `account_has_contacts` — surface that message with the count, never cascade) and Restore. (2) **The Profiler deep link**: a covered Account's slug renders as `Profiler.html#<slug>` (relative, same origin) on the account row, the account detail and the contact detail's account line; the segments as their registry labels (from `profiler-segments.json`, fetched once like the companies file). (3) **The propose-a-dossier hook** (D4): an uncovered Account gets a one-tap **Propose a dossier** that copies the exact command text `profiler <Company Name>` to the clipboard (with a visible fallback showing the line to copy) and marks the account with an `dossier-proposed` tag through `nop=account`, so the developer can paste it into a Profiler session; nothing is generated in-app (the standing no-runtime-AI decision). (4) **The on-the-record check**: for a contact at a covered Account, fetch `profiler-data/<slug>.profile.json` (public, cached page-lifetime per slug, fetched only when a detail opens — never on the list paint) and compare the contact's romanised name against `decisionMakers[].name` with the same key as `nwNameKey_` (a client mirror, `nwNameKey`); a match shows the record's title and the dossier's source line under the contact ("On the record as … — Profiler, <source>"), and a mismatch of title (card vs record) is surfaced as a note, never written anywhere. The list op stays minimum-necessary (§12): add `contactCount` per account server-side; nothing else widens. (5) **Checker + verifier**: extend `scripts/check-network-schema.py` so the `nop=account` validator is covered by the same enum / stage assertions (the op name appears with `STAGE_NEEDS_TARGET_OR_CUSTOMER` in both the save and the account paths) and `scripts/verify-network-roles.py` with an accounts round-trip against the stub — the Accounts card present for admin only, the row tap, an edit that flips relationship to `partner` and confirms stage resets to `none`, the deep link href for a covered account (stub a saved account with slug `abb`), the on-the-record match against a served profile JSON (use a real covered slug's file so the shape is the shipped one), the propose-a-dossier line for an uncovered one, and the `account_has_contacts` refusal shown with its count.
>
> **Train.** `node --check` on a `.js` copy of `Network.gs`, `scripts/check-gas-inner-scripts.js`, `python3 scripts/check-readme-tree.py`, `scripts/verify-network-roles.py` (zero page errors at phone width), `scripts/check-network-schema.py` (exit 0); page + GAS bumps with changelogs, CHANGELOG entry, README tree descriptions; flip §11's N2 row to **Done** with the versions and write the **E1 brief as §13.7** (or the next free number — E0's brief is §13.4 and may add its own section) before closing. One push.
>
> **Done when** (§8): every contact belongs to an account; a covered account shows its slug, segments and the record's title for a matching name; an uncovered one emits a valid `profiler <Company>` line.

#### The paste-in prompt for N2 (Fable 5.1 High) — written 2026-09-21 at the close of N1

*Model / effort: **Fable 5.1 High**, per §8's N2 row and §9 ("account modelling is mostly schema and UI on the pattern N1 built, plus one careful read of the registry's `aka[]`/`domains[]` resolution rules" — those rules are now code, `nwResolveCompany`, and the session inherits them). E0 (§13.4, Opus 5 xhigh) can still run beside it on a different branch; it touches only `live-site-pages/events-data/` and the plan.*

> Run N2 — accounts and the corpus attachment — from `repository-information/NETWORK-EVENTS-DESIGN-PLAN.md`: §13.6 is the brief (follow its reading list in order, then its five build steps exactly), §4.1 and D4 the design, and `repository-information/NETWORK-SCHEMA.md` §3 (`Accounts`), §4, §12, §13 the shapes. N1 is done (v06.91r; `Network.html` v01.11w, `Network.gs` v01.06g): every saved contact already has an Account, the review card already resolves the company against the public registry and the D5 stage rule is enforced on both sides — build the Accounts surface, `nop=account`, the `Profiler.html#<slug>` deep links, the propose-a-dossier hook and the on-the-record check against `decisionMakers[]` on top of that, in the PROJECT regions of `Network.gs` and `Network.html` only. Keep every UI rule N1 set (no ids or confidence numbers on a card, the pill rows, the two-half control rows, the paper-and-ink family) and never touch the IndexedDB name, version or `pending` store. List ops stay minimum-necessary (§12); audit rows carry ids and counts only; the dossier file is fetched only when a detail opens. Verify with `node --check` on a `.js` copy of `Network.gs`, `scripts/check-gas-inner-scripts.js`, `python3 scripts/check-readme-tree.py`, `scripts/verify-network-roles.py` (zero page errors at phone width) and `scripts/check-network-schema.py` (exit 0). Page + GAS bumps with changelogs, CHANGELOG entry, flip §11's N2 row to **Done** with the versions and write the E1 brief as §13.7 (or the next free number) before closing — then hand off in chat what to check on the phone: an account row for every company saved from the 20 cards, the Profiler link on a covered one, the `profiler <Company>` line copied from an uncovered one, and the on-the-record title for any contact who is in a dossier's decision-makers. Do not touch Events, the bridge (B), the list's filters or exports (N3), or any interval (Q). Normal Session Start, Pre-Commit and Pre-Push checklists on a `claude/*` branch restarted from `origin/main`; run `git fetch --unshallow origin main` first. The repo CHANGELOG stands at `Sections: 87/100` with nine sections dated 2026-09-21 EST — no rotation is due on any later date either (87 < 100), so expect none. One push.

---

### 13.7 · Paste-in brief for E1 — Events scaffold + calendar (Fable 5.1 High, two sessions; written at the close of N2, 2026-09-21)

> Build E1 of `repository-information/NETWORK-EVENTS-DESIGN-PLAN.md` — the Events app of §5.3 on the registry E0 wrote. **E0 is a prerequisite**: `live-site-pages/events-data/events.json`, `events-sources.json` and `scripts/check-events-registry.py` must be on `main` with the checker at 0 (§11's E0 row **Done**); if they are not, stop and run §13.4 first — this brief does not research events. Read first, in this order: the plan's §3 (D1 names, D2 no worker, D7 **admin-only** for Events too, D12 Overpass, D14 no data poll, D16 the 2026-11-16 date), §5.1 (the registry row you render), §5.3 in full (the vanilla agenda scroller — not FullCalendar — the detail sheet, the filters, the two calendar hand-offs, stars / notes / plans / attendance as owner-scoped rows), §8's E1 row and §9's E1 paragraph, `repository-information/EVENTS-SCHEMA.md` §1 (ids `st-` `pl-` `mt-`), §2, §3 (the registry row — every field the agenda and the sheet show), §5 (`Stars` · `Plans` · `Meetings` · `Proposed` · `Tuning` — create all five in `ensureEventsTabs_()` now so E2–E5 edit against them; write only `Stars` in E1), §9 (the ICS and the Add-to-Google-Calendar mappings, byte for byte), §12; then the code you copy from: `googleAppsScripts/Network/Network.gs` (`NW_ROLE_CAPS` / `nwAdmitted_` / `nwRequire_` / `ensureNetworkTabs_` / `nwNewId_` / `nwListRows_` / the audit-row shape) and `live-site-pages/Network.html`'s PROJECT region (the door, `nwApi` / `nwApiBody`, the paper-and-ink family, the pill rows and the two-half control rows — Events shares the family, `ev*`-prefixed), `scripts/verify-network-roles.py` (the door verifier to mirror as `scripts/verify-events-roles.py`), and `scripts/setup-gas-project.sh` with the Network project's creator config as the example. Do not build the poller or `events sync` (E2), the score (E3), signals (E4), plans (E5) or the bridge (B); no `EVENTS_PEER_TOKEN`, no peer ops — the Source Event default on Network's card waits for B.
>
> **Session 1 — scaffold and the agenda.** (1) `setup-gas-project.sh` (auth, hipaa; own spreadsheet; `DEPLOYMENT_ID` recorded the N0 way) → `Events.html` / `Events.gs`, the manifest + icons with the `manifest-src 'self'` override on both CSP tags, no service worker; `EV_ROLE_CAPS` on both sides admitting **admin only** (D7 — all four tier keys kept), `evAdmitted_` / `evAdmitted()`, `scripts/verify-events-roles.py` turning the other three tiers away with zero requests; `HEARTBEAT_INTERVAL` 600 s, no data poll, `quotaProbe_` + `op=quota` inherited from the shared template region; Profiler's masthead gets no new slot. (2) `ensureEventsTabs_()` for the five §5 tabs; `?action=events&eop=list` answering the owner's `Stars` rows (minimum-necessary: id, slug, attending, updatedAt) and `eop=star` / `eop=unstar` / `eop=note` (body-POST, enum-validated `attending`, audit rows ids only). (3) The **agenda scroller**: the page fetches `events-data/events.json` (relative — never a GitHub endpoint) and renders a sticky month header over a day-grouped list driven by `IntersectionObserver`, `status ≠ past` first and a "past" fold at the bottom; every row name · dates · city · kind with a star toggle; filters as pills across a two-half control row (kind, region, audience segment from `profiler-segments.json`, starred, and a "signals only" pill **present but disabled with a "from E4" note**); tap → the **detail sheet** (organiser, venue, `tierNote`, registration URL, `mentions[]` as chips deep-linking `Profiler.html#<slug>`, the `sources[]` line with `lastConfirmed`). (4) On the sheet: **Add to Google Calendar** (the §9 template URL) and **Download .ics** (one hand-rolled RFC 5545 `VEVENT` per §9 — folding at 75 octets, CRLF, escaping); a **Note** field and an **Attending** select (`planning` · `registered` · `attended` · `skipped`) written through `eop=note`.
>
> **Session 2 — the published calendar and the phone pass.** (5) `scripts/build-events-ics.py` writes `live-site-pages/events-data/events.ics` (every `confirmed` event, `X-WR-CALNAME`, stable UIDs) — run by the session and wired into `check-events-registry.py`'s `.ics` walk; a **Subscribe** pill on the masthead offering the `webcal://` URL of the published file with a copy fallback. (6) The **day-plan tab** as a timeline of the starred events on a chosen day — read-only in E1 (E5 fills it). (7) The Playwright pass at 390 × 844: the month header sticks, the agenda scrolls past a month boundary and the header changes, the sheet opens and closes, a star round-trips through the stub, one event's ICS text parses (a minimal `VEVENT` walker in the verifier) and the Google Calendar href carries `dates=` / `ctz=`; screenshots of month, agenda, detail; zero page errors. (8) On a real phone: the Calendar link opens Google Calendar prefilled and the `.ics` imports — reported in the hand-off, not asserted by the verifier.
>
> **Train.** `node --check` on a `.js` copy of `Events.gs`, `scripts/check-gas-inner-scripts.js`, `python3 scripts/check-readme-tree.py`, `scripts/verify-events-roles.py` (zero page errors at phone width), `scripts/check-events-registry.py` (exit 0 — the `.ics` is part of it from session 2); page + GAS at their initial versions from the setup script (bump only files you edit after creation), CHANGELOG entry, README tree entries, REPO-ARCHITECTURE.md + the per-environment diagram the setup script adds; flip §11's E1 row to **Done** with the versions and write the **B brief as §13.8** (or the next free number) before closing. One push per session.
>
> **Done when** (§8): live; a starred event opens correctly in Google Calendar from the link and imports from the `.ics`; Playwright screenshots of month, agenda, detail at phone width; zero page errors; **before 2026-11-16** (D16).

#### The paste-in prompt for E1 (Fable 5.1 High) — written 2026-09-21 at the close of N2

*Model / effort: **Fable 5.1 High**, two sessions, per §8's E1 row and §9 ("the calendar scroller is the one genuinely new UI component in the program and phone-width layout work is where the estimate heuristics say to budget iterations"). Session 1 is the scaffold + agenda; session 2 the published `.ics`, the day-plan tab and the phone pass. E1 runs after E0 has landed and can run beside N3 (D16).*

> Run E1 session 1 — Events scaffold + calendar — from `repository-information/NETWORK-EVENTS-DESIGN-PLAN.md`: §13.7 is the brief (follow its reading list in order, then session 1's four build steps exactly), §5.3 the design, and `repository-information/EVENTS-SCHEMA.md` §1, §2, §3, §5, §9, §12 the shapes. E0 must be Done in §11 (`events-data/events.json` + the roster + `check-events-registry.py` at 0 on `main`) — stop and say so if it is not. Scaffold `Events.html` / `Events.gs` with `scripts/setup-gas-project.sh` the way N0 did for Network (auth, hipaa, own spreadsheet, PWA manifest with the `manifest-src 'self'` override, the admin-only door on both sides with `scripts/verify-events-roles.py`, `HEARTBEAT_INTERVAL` 600 s, no data poll), `ensureEventsTabs_()` for the five §5 tabs, `eop=list` / `eop=star` / `eop=unstar` / `eop=note` in the PROJECT regions only, then the vanilla agenda scroller over the public registry, the filter pills, the detail sheet with the `Profiler.html#<slug>` mention chips, Add-to-Google-Calendar and the per-event `.ics` per §9. Keep the Network UI family (paper-and-ink, the pill rows, the two-half control rows, no ids on a card); no poller, no score, no signals, no bridge. Verify with `node --check` on a `.js` copy of `Events.gs`, `scripts/check-gas-inner-scripts.js`, `python3 scripts/check-readme-tree.py`, `scripts/verify-events-roles.py` (zero page errors at phone width) and `scripts/check-events-registry.py` (exit 0). CHANGELOG entry, README tree entries, REPO-ARCHITECTURE.md; set §11's E1 row to *In progress — session 1* with the versions and write nothing new in §13 yet (session 2 flips the row to Done and writes the B brief as §13.8) — then hand off in chat what to check on the phone: the agenda at today's month, a starred event, the Calendar link prefilled. Normal Session Start, Pre-Commit and Pre-Push checklists on a `claude/*` branch restarted from `origin/main`; run `git fetch --unshallow origin main` first. Read the CHANGELOG's `Sections:` counter at session start and rotate only if it exceeds 100 on the push date. One push.

---

### 13.8 · Paste-in brief for B — the bridge (Fable 5.1 High, one session; written at the close of E1, 2026-09-22)

> Build B of `repository-information/NETWORK-EVENTS-DESIGN-PLAN.md` — the bridge of §6 between `Network.gs` and `Events.gs`, the first private server-to-server route in the program. **N2 and E1 are prerequisites** (§11: N2 Done v06.94r, E1 Done v07.08r) and **Events must be deployed** — `googleAppsScripts/Events/Events.config.json` must carry a real `DEPLOYMENT_ID` and `SPREADSHEET_ID` (E1 left both as placeholders for the developer; if they are still placeholders, stop and ask for them before writing a proxy URL, then record them the N0 way — [PC-GAS-CONFIG] #14 syncs the `.gs` and the page's `_e`). Read first, in this order: the plan's §3 (D7 admin-only on both apps, D14 no data poll, D16 the order of build — B sits between E1 and N3), §6 in full (the rule: public Pages data is read directly by any page, anything private crosses only server-to-server behind a peer token after the user's own app already decided the user may ask; the two private rows **Events → Network** and **Network → Events**; the three token namespaces and that `CORPUS_TOKEN` is never widened), §8's B row and §9's B paragraph, `repository-information/NETWORK-SCHEMA.md` §8 (`nop=accounts` GET · `nop=signals` POST JSON, the upsert key, the `linkedin_not_fetched` rejection, `Source = events`) and `repository-information/EVENTS-SCHEMA.md` §8 (`eop=today` · `eop=starred` · `eop=signals`, the same far side); then the code you copy from: `googleAppsScripts/Profiler/Profiler.gs` `guidanceMentionsProxy_()` — read it whole; its invariants are the brief: the token `.trim()`-ed on read, `not_configured` under 16 characters, `muteHttpExceptions` with `upstream_http_<code>`, `upstream_unreachable` on a throw, and **`upstream_not_json` with a 160-character snippet** because Apps Script serves its own exception pages as HTML at HTTP 200 — and `Events.gs` `handleEventsOp_` / `evStarOp_` (the Stars read you expose) and `Network.gs` `nwListRows_` / the Accounts and Signals tab shapes (`NW_SIGNAL_KINDS`). Do not build the recommendation score (E3), the weekly signal diff (E4) or the day-plan writes (E5); B only lays the road.
>
> **Build, in the PROJECT regions of `Network.gs`, `Events.gs`, `Network.html` and `Events.html` only.** (1) **The far side, both apps**: `?action=peer&t=<token>&nop=…` in `Network.gs` and `?action=peer&t=<token>&eop=…` in `Events.gs`, handled in `doGet` / `doPost` **before** session validation (the caller is a server, not a browser), each a copy of `guidanceMentionsProxy_()`'s far side: read `NETWORK_PEER_TOKEN` / `EVENTS_PEER_TOKEN` from Script Properties with `.trim()`; a property shorter than 16 characters answers `{ success:false, error:'not_configured' }`; each of the **six token-boundary cases** (property unset, wrong token, absent `t`, empty `t`, sub-16-character property, unknown op) answers a flat `{ success:false, error:'denied' }` with **zero spreadsheet reads** — the tab handle is not even opened; never echo the token or the property back. (2) **The four ops on the far sides**: Network's `nop=accounts` (GET — live Accounts with `relationship` ∈ target · customer · partner · channel, scoped by `owner` exactly as `resolveOwnerScope_` would; **no contacts, no emails, no notes**) and `nop=signals` (POST JSON — upsert on (`accountId`, `eventSlug`, `kind`, `evidenceUrl`), `kind` validated against `NW_SIGNAL_KINDS`, a LinkedIn `evidenceUrl` rejected with `linkedin_not_fetched`, rows written `Source = events`, the answer `{ written, updated, rejected[] }`); Events' `eop=today` (the owner's starred events whose dates contain today **in the event's own `tz`**, from the Stars tab joined to the public registry the server fetches once per execution by its Pages URL — a relative fetch is not available server-side, so `Events.gs` reads `https://<org>.github.io/<repo>/events-data/events.json` with `UrlFetchApp`, never a GitHub API host) and `eop=starred` (the same rows, every date, with `attending`); `eop=signals` is **read-through only** — Events proxies Network's Signals for one account so Network's later "will be at" chips need one proxy, not two. (3) **The near sides**: `nwEventsProxy_(eop, params)` in `Network.gs` and `evNetworkProxy_(nop, params)` in `Events.gs` — `guidanceMentionsProxy_` verbatim with the peer's `/exec` URL (from the peer's `*.config.json` `DEPLOYMENT_ID`, pasted as a constant the way `CLASSROOM_GUIDANCE_EXEC` is) and the token, called **only** after the user's own app has decided: Network after `validateSessionForData` + `nwAdmitted_`, Events after `evRequire_(sess, 'recommend')`. While either token is unset both surfaces report **not configured**, never an error. (4) **The first use**: on Network's scan card, **Source Event defaults** from `eop=today` — the field is prefilled with the slug when exactly one starred event is on today, offered as a pill row when several are, left empty when none or when not configured; the card keeps the free-text field N1 built (a fallback and an override, never removed). On Events' sheet nothing new is painted in B — the recommendation panel that reads `nop=accounts` is E3's — but `Events.html` gets the one-line **"connect Network to score by account"** placeholder E3 replaces, so the `not_configured` answer has somewhere to land. (5) **The Node harness** `scripts/check-peer-bridge.js`: loads both `.gs` files into a sandbox with stubbed `PropertiesService`, `SpreadsheetApp` and `UrlFetchApp` (the `check-guidance-migration.js` idiom) and proves, for **each** far side, that every one of the six token-boundary cases returns flat `denied` with **zero** `SpreadsheetApp.openById` calls and zero `UrlFetchApp` calls, that a sub-16 property returns `not_configured`, that a correct token reaches the op, and that the near-side proxies map an HTML-at-200 body to `upstream_not_json` with a snippet and a throw to `upstream_unreachable`. Zero live calls in the harness.
>
> **Train.** `node --check` on `.js` copies of both `.gs` files, `scripts/check-gas-inner-scripts.js`, `node scripts/check-peer-bridge.js` (exit 0), `python3 scripts/check-readme-tree.py`, `scripts/verify-network-roles.py` and `scripts/verify-events-roles.py` (zero page errors at phone width — a turned-away tier still issues zero requests; the bridge adds no client request of its own), `python3 scripts/check-events-registry.py` (exit 0). Page + GAS bumps on all four files with changelogs (the GAS changelogs must not name the token, the property or the peer URL — [PC-PAGE-CHANGELOG] #16 and `changelog-security.md`), CHANGELOG entry, README tree descriptions and the new script's entry, REPO-ARCHITECTURE.md's flowchart if it gains an edge between the two GAS nodes; the developer's hand-off in chat: set `NETWORK_PEER_TOKEN` and `EVENTS_PEER_TOKEN` to the **same** random 16+ character value in **both** projects' Script Properties (never committed, never quoted back), redeploy both through their `Deploy` workflow steps, then star an event dated today and open the scan card. Flip §11's B row to **Done** with the versions and write the **N3 brief as §13.9** (or the next free number) before closing. One push.
>
> **Done when** (§8): the harness proves every unauthorised case returns flat `denied` with zero upstream calls; with tokens set, the scan card's Source Event defaults from a starred event dated today; with tokens unset both surfaces say *not configured*.

#### The paste-in prompt for B (Fable 5.1 High) — written 2026-09-22 at the close of E1

*Model / effort: **Fable 5.1 High**, one session, per §8's B row and §9 ("one pattern, copied faithfully: the `guidanceMentionsProxy_` invariants are written down, and the failure modes — a trailing newline in a property, an exception page at HTTP 200 — are exactly the kind a careful read prevents"). B runs after E1 and before N3 (D16). It needs Events deployed: if `Events.config.json` still carries `YOUR_DEPLOYMENT_ID`, the session must stop and ask before it can write a peer URL.*

> Run B — the bridge — from `repository-information/NETWORK-EVENTS-DESIGN-PLAN.md`: §13.8 is the brief (follow its reading list in order, then its five build steps exactly), §6 the design, and `repository-information/NETWORK-SCHEMA.md` §8 and `repository-information/EVENTS-SCHEMA.md` §8 the shapes. N2 (v06.94r) and E1 (v07.08r; `Events.html` v01.02w, `Events.gs` v01.01g) are Done in §11. Events must be deployed for this: read `googleAppsScripts/Events/Events.config.json` first — if `DEPLOYMENT_ID` or `SPREADSHEET_ID` is still a placeholder, ask me for both before anything else and record them the N0 way ([PC-GAS-CONFIG] #14 syncs the `.gs` and the page's `_e`); if they are real, leave them. Build the far sides in `Network.gs` (`?action=peer&nop=accounts|signals`) and `Events.gs` (`?action=peer&eop=today|starred|signals`) as copies of `Profiler.gs` `guidanceMentionsProxy_()`'s far side — `.trim()`-ed token, `not_configured` under 16 characters, every one of the six token-boundary cases a flat `denied` with zero spreadsheet reads, handled before session validation — and the near-side proxies `nwEventsProxy_` / `evNetworkProxy_` as `guidanceMentionsProxy_` verbatim with the `upstream_not_json` distinction, called only after the user's own app has admitted the user; the first use is the scan card's Source Event defaulting from `eop=today`, with the free-text field kept; Events gets only the "connect Network to score by account" placeholder for E3. Write `scripts/check-peer-bridge.js`, a Node sandbox harness over both `.gs` files that proves every unauthorised case is flat `denied` with zero upstream calls and that an HTML-at-200 body maps to `upstream_not_json`. No score (E3), no signal diff (E4), no plan writes (E5); never widen `CORPUS_TOKEN`; never commit, log or echo a token. Verify with `node --check` on `.js` copies of both `.gs` files, `scripts/check-gas-inner-scripts.js`, `node scripts/check-peer-bridge.js`, `python3 scripts/check-readme-tree.py`, both role verifiers (zero page errors at phone width) and `python3 scripts/check-events-registry.py`. Bump all four files per [PC-HTML-VERSION] #2 / [PC-GS-VERSION] #1 with page and GAS changelog entries that name no token, property or URL; CHANGELOG entry; README tree entries; flip §11's B row to Done with the versions and write the N3 brief as §13.9. Then hand off in chat: the two Script Properties to set to one shared value in both projects, the redeploys, and what to check — star an event dated today and open the scan card. Normal Session Start, Pre-Commit and Pre-Push checklists on a `claude/*` branch restarted from `origin/main`; run `git fetch --unshallow origin main` first; parallel sessions push, so check `git ls-remote` before pushing. Read the CHANGELOG's `Sections:` counter at session start and rotate only if it exceeds 100 on the push date. One push.

### 13.9 · Paste-in brief for N3 — the list, the exports, the drafts and the QR card (Fable 5.1 High, two sessions; written at the close of B, 2026-09-22)

> Build N3 of `repository-information/NETWORK-EVENTS-DESIGN-PLAN.md` — §4.3 in full on what N1, N2 and B left: every contact belongs to an account (D4), the Interactions tab carries a `scan` row per card and the ownership machinery is Receipts' verbatim. **N2 and B are prerequisites** (§11: N2 Done v06.94r, B Done v07.10r). Read first, in this order: §3's D5 (the stage rule), D7 (admin-only on both apps — the drafts flow is the one bulk surface and stays admin-only even if the tiers are ever widened), D8 (opaque ids, own-Drive files), D9 (audit rows are ids and counts), D15 (**the app never sends mail and holds no mail scope** — drafts are handed off), D14 (no data poll: refresh on load, on `visibilitychange`, after every write); §4.3 (the list, the filters, the bulk actions, the exports, the follow-up drafts, the QR card); §8's N3 row and §9's N3 paragraph; `repository-information/NETWORK-SCHEMA.md` §3 (the Mailings and Drafts tabs already exist — `ensureNetworkTabs_()` created them in N0 — with `NW_DRAFT_STATUS` = draft · sent · discarded), §4, §12 (minimum necessary in every list payload) and §13; then the code you extend: `Network.gs` `handleNetworkOp_` (the op shape, `nwRequire_` first, `resolveOwnerSet_` for list ops), `nwListRows_` (the list subset — widen it only with what the filters need), `nwGetOp_` (the detail), `nwInteractionAdd_` (the `email-out` row a sent draft writes); `Network.html` `nwLoadList` and the Contacts / Accounts cards (the list you generalise), `nwEditCard` / `nwReviewSection` (the editor you reuse for a row), `nwSourceEventDefault` (B's pattern for a quiet, optional default); and the Receipts precedents named in §4.3 — the History card (search, filters, sort, per-row expand), the temp-spreadsheet `.xlsx` export path and `_buildJpegPdf` (the hand-rolled house style). Do not build warmth, the reconnect list, the import panel, the pre-meeting brief or the network map (N4); do not add a Gmail, Calendar or any new OAuth scope; do not widen any peer token.
>
> **Session 1 — the list, the filters, the bulk actions** (`Network.html` + `Network.gs`): (1) the list generalised from Receipts' History card — search over name, company, title and email; filters on relationship, stage, role, segment, source event, tag, met-date range and "has consent"; sort by name / company / last touch (the newest Interaction date per contact, computed server-side once per list and carried on the row as `lastTouch` — the one widening of the list payload, §12); per-row expand → the existing detail (both card images, the account's relationship badge, the `Profiler.html#<slug>` deep link after the registry answers); (2) multi-select with a sticky action bar — tag, set relationship / stage (through `nop=account`, the D5 rule enforced there already), export selection (session 2 wires the formats; session 1 ships the selection and CSV), start a mailing (session 2), soft-delete (the existing `nop=delete`, one request per row, a count in the confirm); (3) `nop=bulk` on the server for tag and relationship / stage over a list of ids — validated per row, audited with ids and counts, a per-row `rejected[]` like B's signals upsert; (4) `scripts/verify-network-roles.py` extended: the filters narrow the stub's five contacts, a two-row selection tags both, the sort flips; zero page errors at 390 × 844.
>
> **Session 2 — the exports, the drafts, the QR card**: (5) exports — `.xlsx` through the Receipts temp-spreadsheet path with Contacts / Accounts / Interactions sheets for the selection (or all), **CSV** (RFC 4180, UTF-8 BOM so Excel reads the accents), **vCard 3.0** per contact and as one `.vcf` bundle, hand-rolled (N, FN, ORG, TITLE, EMAIL;TYPE=WORK, TEL, ADR, URL, NOTE with the met date and source event, `PHOTO;ENCODING=b;TYPE=JPEG` from the card front **only when the user ticks "include card image"**, base64 folded at 75 octets) — the bundle must import on iOS Contacts and Android Contacts, reported in the hand-off; (6) **follow-up drafts** per D15: pick recipients from the selection → choose a saved template (the Mailings tab: name, subject template, body template) or write one with the merge fields `{{first}}`, `{{company}}`, `{{metAt}}`, `{{lastTopic}}` (the newest Interaction summary) → `nop=drafts` renders **one editable draft per recipient** into the Drafts tab (`status = draft`) → a review list where each draft is edited in place → hand off by **`.eml` bundle** (one RFC 5322 file per draft, `From` the developer's address typed once and remembered in localStorage, an unsubscribe line and a postal address in the default template so CAN-SPAM hygiene is right the first time), one-column CSV / `.txt`, per-draft copy-to-clipboard (subject + body), or a `mailto:` link → **marking a draft sent** (`nop=draftstatus`) writes an `email-out` Interaction with the draft id as evidence and sets `Sent At`; discarding sets `discarded`. The app never sends; `Network.gs` never sees a mail token; `MailApp` / `GmailApp` are not called anywhere — assert that in the verifier by grepping the served page and the `.gs`; (7) **your own card** — a "My card" panel: the developer's own vCard (name, title, company, email, phone, entered once and kept in the Profiles tab row), rendered as a QR code full-screen for the hall. **Check the repo for an inline QR generator before adding one** (`grep -rn "qrcode\|QRCode\|qr-" live-site-pages/*.html`); if none, a minimal byte-mode QR encoder (version ≤ 10, error level M) hand-rolled in the page, no library; (8) `verify-network-roles.py`: the CSV parses back to the five rows, the vCard bundle parses under a minimal `BEGIN:VCARD` walker with `N` / `FN` / `EMAIL` per contact, a three-recipient mailing renders three drafts, one edited draft round-trips, marking sent writes the `email-out` Interaction the stub records, the `.eml` has `From` / `To` / `Subject` and a body; the QR panel renders a canvas or SVG with a non-trivial module count; zero page errors.
>
> **Train (both sessions).** `node --check` on a `.js` copy of `Network.gs`, `scripts/check-gas-inner-scripts.js`, `python3 scripts/check-network-schema.py` (extend `ALLOWED_KEYS` only with count / id keys, as N2 and B did), `python3 scripts/check-readme-tree.py`, `scripts/verify-network-roles.py`. Page + GAS bumps with page and GAS changelog entries that name no token, template body or address ([PC-PAGE-CHANGELOG] #16, `changelog-security.md`); CHANGELOG entry; README tree descriptions; flip §11's N3 row to Done with the versions after session 2 and write the **E2 brief as §13.10** before closing. One push per session.
>
> **Done when** (§8): a filtered selection exports to `.xlsx`, CSV and a vCard bundle that imports on iOS and Android; a three-recipient mailing renders three editable drafts, exports as an `.eml` bundle that opens in a mail client, and marking them sent writes three `email-out` Interactions (nothing is sent by the app); the QR resolves to a vCard on a second phone.

#### The paste-in prompt for N3 (Fable 5.1 High) — written 2026-09-22 at the close of B

*Model / effort: **Fable 5.1 High**, two sessions, per §8's N3 row and §9 ("the exports are house-style hand-rolled formats … the drafts flow renders one editable draft per recipient and hands off by `.eml` bundle, CSV / `.txt`, clipboard or `mailto:` — no scope, no send path"). N3 runs after B and before E2–E3 (D16).*

> Run N3 session 1 — the list, the filters and the bulk actions — from `repository-information/NETWORK-EVENTS-DESIGN-PLAN.md`: §13.9 is the brief (follow its reading list in order, then session 1's steps 1–4 exactly; session 2's steps 5–8 are a second session), §4.3 the design, `repository-information/NETWORK-SCHEMA.md` §3 / §4 / §12 / §13 the shapes. N2 (v06.94r) and B (v07.10r; `Network.html` v01.15w, `Network.gs` v01.08g) are Done in §11. Generalise the Receipts History card into the Contacts list with search, the eight filters, the four sorts (`lastTouch` computed server-side once per list, the only widening of the list payload) and per-row expand to the existing detail; add multi-select with a sticky action bar — tag, set relationship / stage, export selection (CSV now; the other formats are session 2), start a mailing (session 2), soft-delete — with `nop=bulk` validating per row and answering `rejected[]` like B's signals upsert; extend `scripts/verify-network-roles.py` for the filters, a two-row tag and the sort flip. No warmth, no reconnect list, no import panel (N4); no new OAuth scope; never widen a peer token. Verify with `node --check` on a `.js` copy of `Network.gs`, `scripts/check-gas-inner-scripts.js`, `python3 scripts/check-network-schema.py`, `python3 scripts/check-readme-tree.py` and the verifier (zero page errors at phone width). Bump `Network.html` / `Network.gs` per [PC-HTML-VERSION] #2 / [PC-GS-VERSION] #1 with page and GAS changelog entries; CHANGELOG entry; README tree descriptions; leave §11's N3 row **In progress — session 1** with the versions. Normal Session Start, Pre-Commit and Pre-Push checklists on a `claude/*` branch restarted from `origin/main`; run `git fetch --unshallow origin main` first; parallel sessions push, so check `git ls-remote` before pushing. Read the CHANGELOG's `Sections:` counter at session start and rotate only if it exceeds 100 on the push date. One push. Then give me a prompt to paste into a new session for N3 session 2, and remember session.

---

### 13.10 · Paste-in brief for E2 — the poller and `events sync` (Fable 5.1 High, one session; written at the close of N3, 2026-09-22)

> Build E2 of `repository-information/NETWORK-EVENTS-DESIGN-PLAN.md` — the weekly no-AI poller of §5.3 and the session-side `events sync` command, the pair that keeps the public registry current without a GAS trigger ever committing or a fired session ever pushing. **E0, E1 and B are prerequisites** (§11: E0 Done v06.95r, E1 Done v07.08r, B Done v07.10r) and **Events must be deployed** (`googleAppsScripts/Events/Events.config.json` with a real `DEPLOYMENT_ID` and `SPREADSHEET_ID` — if either is a placeholder, stop and ask before installing a trigger). Read first, in this order: §3's D3 (the public/private line — the registry and the roster are public files, the `Proposed` tab is private), D7 (admin-only), D12 (no billed GCP project — `UrlFetchApp` only), D14 (no data poll on the page; the poller is a time-driven trigger, not a page poll); §5.3's poller paragraph and §8's E2 row and §9's E2 paragraph (**it proposes; a session applies**); `repository-information/EVENTS-SCHEMA.md` §1 (the slug rule the poller must reproduce), §3 (the event shape it normalises to — `sources[].lastConfirmed` is what a confirmed probe advances), §4 (the roster — `feedKind` ∈ jsonld · ics · html · manual, `cadence`, `blocked`; **a blocked or manual row is never fetched**), §7 (the `Proposed` row — Change ∈ `new-edition` · `moved-dates` · `changed-venue` · `changed-url` · `cancelled` · `new-event`, Before · After JSON, Status ∈ `pending` · `approved` · `rejected` · `applied`, Decided At · Applied In) and §12; `.claude/rules/scraper-sources.md` (probe live before trusting a source; never substitute a news feed for a dead one); then the code you extend or copy: `Events.gs` `handleEventsOp_` / `evRequire_` (the op shape and the door), `evIcs()` / `evVevent()` (the RFC 5545 text — the ICS **reader** you write must accept what they emit), `ensureEventsTabs_()` (the `Proposed` tab already exists), the B near side `evNetworkProxy_` / the `eop=today` registry fetch (how `Events.gs` reads a Pages file with `UrlFetchApp` — the poller reads `events-sources.json` and `events.json` the same way, never a GitHub API host), `Scraper.gs`'s fetch loop (the `muteHttpExceptions`, per-source status, rate and time-budget idioms — copy the discipline, not the AI), `scripts/build-events-ics.py` (the `events.ics` rebuild `events sync` re-runs) and `scripts/check-events-registry.py` (the gate every apply must pass). Do not build the recommendation score (E3), the attendance signals (E4), the day plans (E5) or the quarterly discovery Routine (R); do not add a scope; never widen `EVENTS_PEER_TOKEN` or `NETWORK_PEER_TOKEN` — the session never calls the app through the bridge.
>
> **Build.** (1) **The poller** in `Events.gs`'s PROJECT region — `evPollRun_()` reached by a time-driven trigger and by an admin op `eop=pollnow` (behind `evRequire_`, for the developer's first run): fetch the roster from its Pages URL; for every row with `feedKind` ∈ `jsonld` · `ics`, `cadence` ≠ `manual` and no `blocked`, fetch the `url` with `UrlFetchApp` (`muteHttpExceptions`, a per-source try/catch, a 15-second budget per source and a total budget under the 6-minute execution cap — stop cleanly and resume next week rather than time out); parse JSON-LD `Event` objects (every `<script type="application/ld+json">` block, `@graph` and arrays included, `@type` `Event` or a subtype) and `VEVENT`s (a minimal RFC 5545 walker: unfold, `DTSTART` / `DTEND` with `VALUE=DATE` and `TZID`, `SUMMARY`, `LOCATION`, `URL`, `UID`); normalise each to the §3 shape (name, `start` / `end` dates in the event's own `tz`, city / venue, url) and derive the §1 slug; **diff against the registry** — the same slug with different dates → `moved-dates`, a different venue → `changed-venue`, a different url → `changed-url`, a next-year edition of a known series (the slug's year suffix) → `new-edition`, an event the registry has never seen → `new-event`, a registry event the feed now marks cancelled → `cancelled`; write one `Proposed` row per diff (`pr-` id, Source Key, Event Slug, Change, Before · After, Evidence URL = the fetched page, Seen At, Status `pending`), **deduplicated on (Source Key, Event Slug, Change, After)** so a weekly re-run never repeats a pending or decided row; a source whose fetch fails or answers a non-2xx writes **no** proposal and one audit row with the status code — no hand-typed events, no fallback feed. (2) **The `Proposed` panel** on `Events.html` (admin only, the Network UI family): the pending rows grouped by source with Before → After, Approve / Reject per row (`eop=decide` — `id`, `status` ∈ approved · rejected, sets Decided At), an "Approved → copy as JSON" button that puts the approved rows on the clipboard in the exact shape the sync command reads, and a "Mark applied" that stamps `applied` with the version the developer types after the sync; the roster's `lastProbe` is **not** written by the poller (the roster is a repo file) — the panel shows each source's last outcome from a `Polls` tab the poller appends to (Source Key, Ran At, Status, Items, Newest Start), added to `ensureEventsTabs_()` and to `EVENTS-SCHEMA.md` §5. (3) **`events sync`**, a session command written into a new `.claude/rules/events-app.md` (path-scoped to `Events.*` and `events-data/**`, registered in CLAUDE.md's Reference Files table and pointed to from a new `## Events Sync Command` section there, the Profiler / Industry Guidance pattern): the developer pastes the approved JSON (or points the session at the Events spreadsheet's `Proposed` tab through the Google Drive connector when that read is available — try it, report it, do not depend on it); the session applies each row to `events.json` (the field the Change names, `lastUpdated`, the row's `sources[].lastConfirmed` = Seen At, a `new-event` / `new-edition` row appended in the §3 shape with `status` `tentative` until an organiser page has been read), updates the roster row's `lastProbe` from the `Polls` data the JSON carries, re-runs `scripts/build-events-ics.py` and `scripts/check-events-registry.py` (exit 0 is the gate — a failing apply is reverted, never committed half-way), and lists the `pr-` ids and the version for the developer to stamp with "Mark applied". (4) **The trigger**: `eop=installpoller` (admin, idempotent — deletes any existing trigger on `evPollRun_` before creating one weekly, Monday 06:00 in `America/New_York`) so the developer installs it from the panel once; document in the hand-off that `ScriptApp.newTrigger` needs the script's own authorisation on first run. (5) **`scripts/check-events-poller.js`**: a Node sandbox harness over `Events.gs` (the `check-peer-bridge.js` idiom — stubbed `UrlFetchApp`, `SpreadsheetApp`, `PropertiesService`) that feeds one JSON-LD page, one ICS feed (built by `build-events-ics.py`'s own output so the reader and the writer agree), one blocked row and one 403 and proves: the six diff kinds each produce exactly one `Proposed` row with the right Before · After, a second run writes zero new rows, the blocked and manual rows are never fetched, the 403 writes no proposal, and `pollnow` / `decide` / `installpoller` are refused to a non-admin session. Zero live calls in the harness. (6) `scripts/verify-events-roles.py`: the `Proposed` panel is admin-only (the other tiers still issue zero requests), Approve on a stub row sends `eop=decide`, the copy-as-JSON text parses back to the rows, zero page errors at 390 × 844.
>
> **Train.** `node --check` on a `.js` copy of `Events.gs`, `scripts/check-gas-inner-scripts.js`, `node scripts/check-events-poller.js` (exit 0), `python3 scripts/check-events-registry.py` (exit 0 — the registry is untouched by the build itself), `python3 scripts/check-readme-tree.py`, `scripts/verify-events-roles.py` (Playwright is `pip install playwright` with the pre-installed Chromium, no `playwright install`). Page + GAS bumps on `Events.html` / `Events.gs` with changelog entries that name no token, spreadsheet id or trigger id; CHANGELOG entry; README tree entries for the new rules file and harness; `EVENTS-SCHEMA.md` §5 (the `Polls` tab) and §7 (the ops); the developer's hand-off in chat: redeploy Events, open the panel, tap Install poller, then Poll now, then approve one row and run `events sync` in a fresh session. Flip §11's E2 row to **Done** with the versions and write the **E3 brief as §13.11** before closing. One push.
>
> **Done when** (§8): one cycle proposes at least the JSON-LD sites' current editions; an approved diff lands in `events.json` through the command and the checker stays at 0.

#### The paste-in prompt for E2 (Fable 5.1 High) — written 2026-09-22 at the close of N3

*Model / effort: **Fable 5.1 High**, one session, per §8's E2 row and §9 ("a Scraper-shaped state machine without the AI; the design point that matters is that it *proposes* and a session *applies*, because a fired session cannot push and a GAS trigger cannot commit"). E2 runs after N3 and before E3 (D16). It needs Events deployed: if `Events.config.json` still carries a placeholder, the session must stop and ask before it can install a trigger.*

> Run E2 — the poller and `events sync` — from `repository-information/NETWORK-EVENTS-DESIGN-PLAN.md`: §13.10 is the brief (follow its reading list in order, then its six build steps exactly), §5.3 the design, `repository-information/EVENTS-SCHEMA.md` §1 / §3 / §4 / §7 the shapes. E0 (v06.95r), E1 (v07.08r), B (v07.10r) and N3 (v07.14r; `Events.html` v01.04w, `Events.gs` v01.03g) are Done in §11. Build the weekly no-AI poller in `Events.gs` (`evPollRun_` — the roster and the registry read from their Pages URLs with `UrlFetchApp`, JSON-LD and ICS sources only, blocked and manual rows never fetched, the six diff kinds written as `Proposed` rows deduplicated on source · slug · change · after, a failed source an audit row and nothing else), the admin `Proposed` panel on `Events.html` (Approve / Reject through `eop=decide`, Approved → copy as JSON, Mark applied, the `Polls` tab's last outcome per source, `eop=installpoller` idempotent), the `events sync` session command in a new `.claude/rules/events-app.md` (apply the approved JSON to `events.json`, advance `lastConfirmed` and the roster's `lastProbe`, rebuild `events.ics`, gate on `check-events-registry.py` exit 0, list the `pr-` ids to stamp), and `scripts/check-events-poller.js`, a Node sandbox harness proving the six diff kinds, the zero-row re-run, the never-fetched rows and the admin refusals with zero live calls. No score (E3), no signals (E4), no plans (E5), no discovery Routine (R); no new scope; never widen a peer token — the session never calls the app. Verify with `node --check` on a `.js` copy of `Events.gs`, `scripts/check-gas-inner-scripts.js`, `node scripts/check-events-poller.js`, `python3 scripts/check-events-registry.py`, `python3 scripts/check-readme-tree.py` and `scripts/verify-events-roles.py` (zero page errors at phone width; Playwright is `pip install playwright` with the pre-installed Chromium, no `playwright install`). Bump `Events.html` / `Events.gs` per [PC-HTML-VERSION] #2 / [PC-GS-VERSION] #1 with page and GAS changelog entries that name no token, spreadsheet id or trigger id; CHANGELOG entry; README tree entries; `EVENTS-SCHEMA.md` §5 and §7; register the rules file in CLAUDE.md; flip §11's E2 row to Done with the versions and write the E3 brief as §13.11. Then hand off in chat: redeploy, Install poller, Poll now, approve a row, run `events sync` in a fresh session. Normal Session Start, Pre-Commit and Pre-Push checklists on a `claude/*` branch restarted from `origin/main`; run `git fetch --unshallow origin main` first; parallel sessions push, so check `git ls-remote` before pushing. The CHANGELOG stands at `Sections: 85/100` — read the counter, no rotation is due. One push. Then give me a prompt to paste into a new session for E3, and remember session.

---

### 13.11 · Paste-in brief for E3 — the recommendation score (Fable 5.1 High, one session; written at the close of E2, 2026-09-22)

> Build E3 of `repository-information/NETWORK-EVENTS-DESIGN-PLAN.md` — the recommendation of §5.4: a score per upcoming event, explainable line by line, computed **in `Events.gs` from data fetched over the bridge**, never in the page from a public file, because the *why* panel names Network accounts. **B and E2 are prerequisites** (§11: B Done v07.10r — `eop=netaccounts` already proxies Network's scored accounts through `evNetworkProxy_`; E2 Done v07.15r) and both peer tokens must be set on the live deployments (the panel must degrade to "connect Network to score by account" when they are not — never fail). Read first, in this order: §3's D7 (admin-only; the score and its *why* are behind the `recommend` capability), D12 (nothing here needs a billed project), D14 (no data poll — the score is fetched on demand: on the Recommended pill, on a sheet whose score is not yet loaded, on `visibilitychange`); §5.4 in full; §8's E3 row and §9's E3 paragraph (**the deliverable is the *why* panel as much as the score**); §12.6 (both seats weigh equally — `seatSegments` is the union of the two seats' segment sets, read from `profiler-segments.json` at run time, never a copy); `repository-information/EVENTS-SCHEMA.md` §6 (the six terms, their formulas and default weights — `segmentFit` 0.35 · `accountPresence` 0.35 · `corpusSalience` 0.15 · `proximity` 0.10 · `conflict` 0.25 subtracted · `relevancePrior` 0.05 — and the `stageWeight` table), §5 (`Tuning`: Term · Weight · Note, created empty by E1 — seed it), §8 (`eop=signals` — the read leg the term needs); `repository-information/NETWORK-SCHEMA.md` §3 (the Signals row — `confidence`, `kind`, `evidenceUrl`) and §8 (`nop=accounts` — id, name, relationship, stage, segments, tags; `nop=signals` GET); then the code you extend: `Events.gs` `handleEventsOp_` (`eop=netaccounts` — the accounts input, already gated by `recommend`), `evNetworkProxy_` / `evPeerSignals_` (the signals join), `evRegistry_` (`mentions[]` is on the row), `evListRows_` over Stars (the conflict term reads `Attending` ∈ registered · attended), `ensureEventsTabs_()`; `Events.html` `evOpenSheet` (the `ev-netnote` placeholder line is what the *why* panel replaces), `evFiltersCard` (the Mine row's pills), `evRow` / `evRender` (a score chip per row, a sort by score); `Network.gs` `nwPeerAccounts_` / `nwPeerSignalsRead_` (what the far side actually answers); `scripts/check-peer-bridge.js` (the sandbox idiom — copy it as `scripts/check-events-score.js`). Do not build the attendance sweeps (E4 — until it lands the `accountPresence` term is fed only by Network's manual signals and is mostly 0; the harness proves the term with stub rows), the day plans (E5), the discovery Routine (R); no new scope; never widen `EVENTS_PEER_TOKEN` or `NETWORK_PEER_TOKEN`.
>
> **Build.** (1) **`Tuning`** — `evTuning_()` reads the tab on every score and, when it is empty, seeds it once with §6's six weight rows and a `regions` row (a comma list of preferred region codes, default empty) with a Note per row; the admin edits weights in the sheet (a "Tuning" line on the panel names the tab; no editor UI in v1); a malformed weight falls back to the default and is named in the answer. (2) **`eop=recommend`** (behind `recommend`): the upcoming registry rows (`status` ∉ cancelled · past), Network's scored accounts over the bridge **once**, the signals for each scored account over the read leg (cap the calls — 40 accounts — and stop cleanly), the seat segments from `profiler-segments.json` fetched server-side from its Pages URL, `mentions[]` from the row, the owner's Stars for the conflict term; compute the six terms exactly as §6 writes them, `score = Σ weight × term` to two decimals; answer `{ success, weights, notConfigured: bool, events: [ { slug, score, terms: { segmentFit, accountPresence, corpusSalience, proximity, conflict, relevancePrior }, why: { segments: [ids matched], accounts: [ { id, name, stage, relationship, signal: { kind, confidence, evidenceUrl } } ], mentions: [dossier slugs], conflicts: [starred slugs] } } ] }` sorted by score then slug; when the bridge answers `not_configured` the terms that need it are 0, `notConfigured` is true and the rest is still computed. Audit counts only — never an account name. (3) **The page**: a **Recommended** pill on the Mine row that fetches the score once and sorts the agenda by it with a score chip on every row (the date grouping gives way to a ranked list while the pill is pressed; the month header shows "Recommended"); the sheet's `ev-netnote` replaced by the ***why* panel** — the score, the six terms as one bar each with its weight, and the contributing rows by name: the accounts (stage, the signal kind and its evidence link), the segments matched, the dossiers that mention the show as Profiler chips, the starred conflicts; a `notConfigured` answer paints the "connect Network" line in the panel's place; fetched on demand only (D14). (4) **`scripts/check-events-score.js`** — the sandbox harness: fixture accounts, signals, stars, a registry and a `Tuning` tab; every term against a hand-computed value; a weight change reorders the answer; `not_configured` degrades with `notConfigured: true`; `recommend` refused to a non-admin with zero fetches; no account name in any audit row. Zero live calls. (5) `scripts/verify-events-roles.py`: the Recommended pill issues exactly one `eop=recommend`, the rows re-order by the stub's scores with a chip each, the sheet's *why* panel names the stub account and its evidence link, the other tiers still issue zero requests; zero page errors at 390 × 844.
>
> **Train.** `node --check` on a `.js` copy of `Events.gs`, `scripts/check-gas-inner-scripts.js`, `node scripts/check-events-score.js` (exit 0), `node scripts/check-peer-bridge.js` and `node scripts/check-events-poller.js` (unchanged, still 0), `python3 scripts/check-events-registry.py`, `python3 scripts/check-readme-tree.py`, `scripts/verify-events-roles.py` (Playwright is `pip install playwright` with the pre-installed Chromium, no `playwright install`). Page + GAS bumps on `Events.html` / `Events.gs` with changelog entries that name no token or account; CHANGELOG entry; README tree entries for the harness; `EVENTS-SCHEMA.md` §5 (the `Tuning` defaults and the `regions` row) and §6 (the answer shape); the developer's hand-off in chat: redeploy, press Recommended, open the top event, judge the *why* line by line against their own account list, change one weight in `Tuning` and press again. Flip §11's E3 row to **Done** with the versions and write the **E4 brief as §13.12** (three sessions per §5.5.1) before closing. One push.
>
> **Done when** (§8): for the developer's real account tags the top five recommended events are defensible line by line; changing a weight in `Tuning` reorders them without a deploy.

#### The paste-in prompt for E3 (Fable 5.1 High) — written 2026-09-22 at the close of E2

*Model / effort: **Fable 5.1 High**, one session, per §8's E3 row and §9 ("a scoring function with a tuning table and an explanation panel. Its correctness is judged by the developer against their own account list, so the session's deliverable is the *why* panel as much as the score"). E3 runs after E2 and before E4 (D16). It needs both peer tokens set on the live deployments for the account terms to be non-zero; the panel must degrade, never fail, when they are not.*

> Run E3 — the recommendation score — from `repository-information/NETWORK-EVENTS-DESIGN-PLAN.md`: §13.11 is the brief (follow its reading list in order, then its five build steps exactly), §5.4 the design, `repository-information/EVENTS-SCHEMA.md` §5 / §6 / §8 the shapes. B (v07.10r) and E2 (v07.15r; `Events.html` v01.05w, `Events.gs` v01.04g) are Done in §11. Build `eop=recommend` in `Events.gs` (the six §6 terms computed server-side from Network's scored accounts and their signals over the bridge, the seat segments from `profiler-segments.json`, `mentions[]` and the owner's stars; `Tuning` seeded once with the default weights and a `regions` row and read on every score; `notConfigured` degrades, never fails; audit counts only), the Recommended pill and score chips on the agenda and the *why* panel on the sheet in `Events.html` (fetched on demand — no poll), `scripts/check-events-score.js` (a Node sandbox harness proving every term against hand-computed values, a weight change reordering, the not_configured degrade and the non-admin refusal with zero live calls) and the verifier's Recommended pass. No attendance sweeps (E4), no plans (E5), no discovery Routine (R); no new scope; never widen a peer token. Verify with `node --check` on a `.js` copy of `Events.gs`, `scripts/check-gas-inner-scripts.js`, `node scripts/check-events-score.js`, `node scripts/check-peer-bridge.js`, `node scripts/check-events-poller.js`, `python3 scripts/check-events-registry.py`, `python3 scripts/check-readme-tree.py` and `scripts/verify-events-roles.py` (zero page errors at phone width; Playwright is `pip install playwright` with the pre-installed Chromium, no `playwright install`). Bump `Events.html` / `Events.gs` per [PC-HTML-VERSION] #2 / [PC-GS-VERSION] #1 with page and GAS changelog entries that name no token or account; CHANGELOG entry; README tree entries; `EVENTS-SCHEMA.md` §5 and §6; flip §11's E3 row to Done with the versions and write the E4 brief as §13.12. Then hand off in chat: redeploy, press Recommended, judge the top five line by line, change a weight and press again. Normal Session Start, Pre-Commit and Pre-Push checklists on a `claude/*` branch restarted from `origin/main`; run `git fetch --unshallow origin main` first; parallel sessions push, so check `git ls-remote` before pushing. The CHANGELOG stands at `Sections: 86/100` — read the counter, no rotation is due. One push. Then give me a prompt to paste into a new session for E4 session 1, and remember session.

---

## Appendix A · The 64-row seed calendar (researched 2026-09-20; E0 verifies every row against the organiser)

Tier: **M** mega-convention · **C** industry conference · **R** regional summit · **S** social. **Rel** = relevance to a BESS/AIDC seller, 1–5. **Ex/Sp** = public exhibitor list / speaker roster. **Feed** = JSON-LD `Event` (LD), ICS, HTML only (✗), bot wall (403), not probed (n/p). **(V)** read from the organiser's site; **(U)** third-party listing only.

| # | Event | Organiser | City | Dates | Tier | Rel | Ex/Sp | Feed |
|---|---|---|---|---|---|---|---|---|
| 1 | Data Center World Power | AFCOM/Informa | Dallas | Sep 21–23 2026 (V) | C | 5 | Ex Sp | LD |
| 2 | Uptime Network Americas Fall | Uptime Institute | Chicago | Sep 22–23 2026 (V) | C | 2 — vendors excluded | Sp | ✗ |
| 3 | ACP RECHARGE | ACP | Aurora, CO | Sep 22–24 2026 (V) | C | 5 | Sp | ✗ |
| 4 | Yotta 2026 | Yotta | Las Vegas | Sep 28–30 2026 (V) | M | 5 | Ex Sp | LD |
| 5 | GCPA Fall Conference | Gulf Coast Power Assn | Austin | Sep 28–30 2026 (V) | R | 4 | Sp | ✗ |
| 6 | Bisnow Central DC Construction/Design | Bisnow | DFW | Oct 6 2026 (V) | R | 3 | Sp | ✗ |
| 7 | The Battery Show North America | Informa | Detroit | Oct 12–15 2026 (V) | M | 5 | Ex (1,300) Sp | LD |
| 8 | OCP Global Summit | OCP Foundation | San Jose | Oct 12–15 2026 (U) | M | 5 | Ex Sp | 403 |
| 9 | SEMICON West | SEMI | San Francisco | Oct 13–15 2026 (V) | M | 3 | Ex | n/p |
| 10 | Bisnow National DC Connectivity & AI Infra | Bisnow | national | Oct 20 2026 (V) | R | 3 | Sp | ✗ |
| 11 | Bisnow West Coast DC Ops & Cooling | Bisnow | West | Oct 21 2026 (V) | R | 3 | Sp | ✗ |
| 12 | 7x24 Exchange Fall | 7x24 Exchange | San Antonio | Oct 25–28 2026 (V) | C | 5 | Sp | ICS |
| 13 | ESIG Fall Technical Workshop | ESIG | Reston, VA | Oct 26–29 2026 (V) | C | 3 | Sp | ✗ |
| 14 | CIGRE Grid of the Future | CIGRE USNC | Richmond, VA | Oct 26–29 2026 (U) | C | 3 | Sp | n/p |
| 15 | Infocast PowerUp Data Centers – Investment | Infocast/Clarion | McLean, VA | Oct 27–29 2026 (V) | C | 4 | Sp | ✗ |
| 16 | iMasons Data Center Energy Industry Update | iMasons | Texas | Nov 2 2026 (V) | S | 4 | — | ✗ |
| 17 | DCD>Connect Virginia | DCD/Informa | Leesburg, VA | Nov 3–4 2026 (U) | C | 5 | Ex Sp | 403 |
| 18 | Southeast Power NEXT | Infocast | Charlotte | Nov 4–6 2026 (V) | R | 3 | Sp | ✗ |
| 19 | NARUC Annual Meeting | NARUC | San Juan, PR | Nov 8–11 2026 (V) | C | 2 | Sp | ✗ |
| 20 | Enlit Europe | Clarion | Vienna | Nov 10–12 2026 (U) | M (intl) | 3 | Ex (750) Sp | 403 |
| 21 | Bisnow Carolinas DC Conference | Bisnow | Charlotte | Nov 12 2026 (V) | R | 3 | Sp | ✗ |
| 22 | SC26 | ACM/IEEE | Chicago | Nov 15–20 2026 (V) | M | 3 | Ex | n/p |
| 23 | **RE+ 2026** | SEIA/SEPA | Las Vegas LVCC | Nov 16–19 2026 (V) | M | 5 | Ex (MYS, 1,300+) Sp | ld+json, no Event |
| 24 | Microsoft Ignite | Microsoft | San Francisco | Nov 17–20 2026 (U) | M | 2 | Sp | n/p |
| 25 | NVIDIA GTC Washington DC | NVIDIA | Washington DC | Nov 30–Dec 3 2026 (V) | M | 4 | Sp, Ex partial | n/p |
| 26 | AWS re:Invent | AWS | Las Vegas | Nov 30–Dec 4 2026 (U) | M | 2 | Ex | n/p |
| 27 | Gartner IT Infra, Ops & Cloud | Gartner | Las Vegas | Dec 8–10 2026 (V) | C | 2 | Ex | n/p |
| 28 | Reuters Events Energy LIVE | Reuters | Houston | Dec 8–9 2026 (V) | C | 3 | Sp | n/p |
| 29 | CES 2027 | CTA | Las Vegas | Jan 6–9 2027 (V) | M | 2 | Ex (MYS) | n/p |
| 30 | PTC'27 | Pacific Telecom Council | Honolulu | Jan 17–20 2027 (V) | C | 3 | Sp | ✗ |
| 31 | POWERGEN International | Clarion | Salt Lake City | Jan 18–21 2027 (V) | M | 4 | Ex Sp | LD |
| 32 | Infocast ERCOT Power NEXT | Infocast | Austin | Jan 25–27 2027 (V) | R | 4 | Sp | ✗ |
| 33 | RE+ Northeast | RE+ Events | Boston | Feb 2–4 2027 (U) | R | 3 | Ex | n/p |
| 34 | Intersolar & Energy Storage NA | Solar Promotion/Diversified | San Diego | Feb 9–11 2027 (V) | M | 4 | Ex Sp | LD (BusinessEvent) |
| 35 | ACP OMS Conference | ACP | Long Beach | Feb 23–25 2027 (V) | C | 3 | Ex | ✗ |
| 36 | **DTECH (DISTRIBUTECH)** | Clarion | Atlanta | Mar 1–4 2027 (V) | M | 5 | Ex Sp | LD |
| 37 | InterBattery | KBIA/COEX | Seoul | Mar 10–12 2027 (U) | M (intl) | 3 | Ex | n/p |
| 38 | Solar + Wind Finance & Investment | Infocast | Phoenix | Mar 14–17 2027 (V) | C | 3 | Sp | ✗ |
| 39 | **NVIDIA GTC 2027** | NVIDIA | San Jose | Mar 15–18 2027 (U) | M | 5 | Sp Ex | n/p |
| 40 | International Battery Seminar | Cambridge EnerTech | Orlando | Mar 15–18 2027 (V) | C | 4 | Ex Sp | ICS |
| 41 | DCD>Connect New York | DCD | New York | Mar 17–18 **or** May 17–18 2027 (U — conflicting listings) | C | 5 | Ex Sp | 403 |
| 42 | RE+ Texas | RE+ Events | Houston | Mar 24–25 2027 (U) | R | 4 | Ex | n/p |
| 43 | Battery Japan | RX Japan | Tokyo | Mar 24–26 2027 (U) | M (intl) | 2 | Ex | n/p |
| 44 | SEMICON West (new spring slot) | SEMI | Phoenix | Mar 30–Apr 1 2027 (V) | M | 3 | Ex | n/p |
| 45 | Hannover Messe | Deutsche Messe | Hannover | Apr 5–8 2027 (U) | M (intl) | 2 | Ex | n/p |
| 46 | ACP Siting+Permitting / PEAK | ACP | New Orleans | Apr 13–17 2027 (V) | C | 2 | Sp | ✗ |
| 47 | Energy Storage Summit USA | Solar Media | Dallas | Apr 20–21 2027 (V) | C | 5 | Sp, Ex (sponsors) | ✗ |
| 48 | Bisnow DICE National | Bisnow | national | May 4–6 2027 (V) | C | 4 | Sp | ✗ |
| 49 | Data Center Frontier Trends + Microgrid Knowledge | Endeavor | Glendale, AZ | May 5–7 2027 (V) | C | 5 | Sp Ex | ✗ |
| 50 | CIBF | CIAPS | Shenzhen | May 12–14 2027 (U) | M (intl) | 3 | Ex | n/p |
| 51 | **Data Center World 2027** | AFCOM/Informa | Nashville | May 24–27 2027 (V) | M | 5 | Ex Sp | LD |
| 52 | **CLEANPOWER 2027** | ACP | Anaheim | May 24–27 2027 (V) | M | 5 | Ex Sp | ✗ |
| 53 | Datacloud Global Congress | BroadGroup | Cannes | Jun 1–4 2027 (U) | C (intl) | 3 | Sp | n/p |
| 54 | SNEC PV+ES | SNEC | Shanghai | Jun 2–4 2027 (U) | M (intl) | 3 | Ex | n/p |
| 55 | 7x24 Exchange Spring | 7x24 | Orlando | Jun 6–9 2027 (V) | C | 5 | Sp | ICS |
| 56 | The smarter E / ees Europe | Solar Promotion | Munich | Jun 8–10 2027 (V) | M (intl) | 3 | Ex | n/p |
| 57 | EEI 2027 | EEI | Orlando | Jun 14–16 2027 (U) | C | 3 | Sp | n/p |
| 58 | The Battery Show Europe | Informa | Stuttgart | Jun 22–24 2027 (U) | M (intl) | 3 | Ex | n/p |
| 59 | Infocast PowerUp Data Centers | Infocast | TBA | Jul 20–22 2027 (V) | C | 4 | Sp | ✗ |
| 60 | IEEE PES General Meeting | IEEE PES | San Francisco | Jul 25–29 2027 (U) | C | 3 | Sp | n/p |
| 61 | NAATBatt Annual | NAATBatt | TBA | Aug 1–5 2027 (U, weak) | C | 3 | Sp | ✗ |
| 62 | Energy Storage Finance & Investment | Infocast | San Diego | Sep 8–9 2027 (V) | C | 4 | Sp | ✗ |
| 63 | 7x24 Exchange Fall | 7x24 | Phoenix | Oct 3–6 2027 (V) | C | 5 | Sp | ICS |
| 64 | RE+ 2027 | RE+ Events | Las Vegas | Nov 15–18 2027 (V) | M | 5 | Ex | — |

**Not yet dated for 2027** (E0 re-checks): Wood Mackenzie North American Power & Renewables Forum (2026: Apr 29–30, Denver), Datacloud USA (2026: Sep 2–3, Austin), Solar & Storage Live USA (2026: Aug 12–13, Philadelphia), DCD Silicon Valley / Dallas, Bisnow DICE South / West (2026: Aug 12–13 Arlington TX; Jun 25 Bay Area), Uptime 2027. **Socials** follow the iMasons chapter pattern — Luma- or LinkedIn-hosted, city-specific — and enter through the Luma ICS layer and manual capture.

**Caveats carried from the research pass:** the DCD New York 2027 date conflict is unresolved because DCD blocks non-browser fetches; NAATBatt 2027 is weakly sourced; every (U) row is a third-party listing until E0 reads the organiser's page.

Developed by: LightAISolutions
