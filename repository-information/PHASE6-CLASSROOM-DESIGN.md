# Phase 6 — Classroom Design (approved gate output)

**Provenance:** Phase 6 of the 7-phase BESS/AIDC action plan. The design gate the plan required was held 2026-08-30 → 31 (v03.98r session); every decision below was made or approved by the developer in that conversation. This document is the **executable spec** the Classroom build sessions work from — the successor to `PHASE5-LEARNING-LAYER-PLAN.md`, playing the same role Phase 5's spec played for its build.

**Big picture:** Profiler is the first-party skeleton, Scraper is the fresh third-party blood, and **Classroom is the institution** — it turns the accumulated corpus into taught, retained, current knowledge for the developer and the team they may lead, in service of mastering the US BESS/AIDC market for a sales/BD career. Ecosystem stance (developer directive, 2026-08-31): a growing family of **federated apps** — each with its own page + GAS project — synergizing through public Pages data and token-gated server-to-server routes, never a monolith. Quality over build economy.

## Decisions (developer-approved 2026-08-31)

1. **Form factor — own app**: `Classroom.html` + `Classroom.gs`, spawned via `setup-gas-project.sh` from the auth template (own sign-in wall, Master ACL, deploy webhook, version/changelog files)
2. **Audience**: admin, contributor, and analyst tiers get Classroom; viewer is excluded
3. **Content**: "Everything" — the whole corpus teaches, under the provenance-gating rule below; field notes never become content; the deep analysis/authoring half runs as scheduled Claude sessions, not in GAS
4. **V1 scope = C0 – C2** (scaffold + learning core + curriculum pipeline); drills and simulations ride the next train
5. **Profiler access retune** — approved as proposed (analyst loses Relationships/Network, Coverage, Export; viewer goes strict dossier-only); executes as C0's first slice
6. **Industry Guidance migrates to Classroom** (developer-proposed, agreed): target state is guidance living in `Classroom.gs`; executed as dedicated phase **C3 — Guidance homecoming** after v1 is verified. No interim Profiler→Classroom guidance route is built — until C3, Classroom tracks deep-link out to Profiler's guidance hub for tiers holding the capability

## Access model

### Profiler retune (C0's first slice — approved 2026-08-31; **BUILT 2026-08-31**, ahead of the Classroom scaffold)

> **Status: done.** Shipped as Profiler v01.75w / v01.30g — `OV_ROLE_CAPS` gained `network` · `coverage` · `study` · `compare`, `handleNewsOp_` got its server-side check, the `#network` and `#compare` hash routes deny on their own, and `scripts/verify-profiler-roles.py` now asserts all ten surfaces per tier plus both deep links. The rest of C0 (Classroom scaffold, cross-links) shipped 2026-09-01 as v04.11r — C0 is complete.

| Profiler surface | Admin | Contributor | Analyst | Viewer |
|---|---|---|---|---|
| Dossiers, roster, search (incl. Policy tab) | ✓ | ✓ | ✓ | ✓ |
| Study guides + concepts tooltips | ✓ | ✓ | ✓ | ✗ *(change)* |
| Compare view | ✓ | ✓ | ✓ | ✗ *(change if currently visible)* |
| Relationships tab + Network explorer | ✓ | ✓ | **✗ (change)** | ✗ |
| Coverage 📰 (Scraper-bound) | ✓ | ✓ | **✗ (change)** | ✗ |
| Export dossier (Word/PDF) | ✓ | ✓ | **✗ (change)** | ✗ |
| Industry Guidance | ✓ | ✓ | ✗ | ✗ |
| Reports · Versions · Field notes · Style 🖋 | ✓ | ✗ | ✗ | ✗ |

Implementation notes for the C0 session:
- New `OV_ROLE_CAPS` capabilities for the surfaces not yet capability-gated (`network`/relationships tab, `coverage`, and the study/compare gating for viewer); flip `export` off for analyst. `?as=<tier>` preview must keep only-subtracting semantics
- **Coverage gets a real server-side role check** in `handleNewsOp_` (`Profiler.gs`) — the proxy currently validates the session only. Relationships/network, study guides, and compare draw on public Pages JSON, so their analyst/viewer gates are app-experience gates (same honest standing as Versions); if that data must ever become truly private, that is a data-relocation decision (the M3 pattern), not a gate tweak
- `GUIDANCE_ROLES` unchanged; "report generation" needs no change — reports are session-authored and the in-app surface is already view-only, admin-only. User management lives in the MasterACL app (admin-only, unchanged)
- Update `scripts/verify-profiler-roles.py` for the new matrix; Profiler page + GAS version bumps per the normal train
- **Budget definition** (for "anything that could meaningfully use up budgets"): anything that triggers AI generation or heavy fetch loops. Nothing a contributor can reach in Profiler today qualifies; Classroom's pipeline is the ecosystem's first real budget surface and its triggers stay admin-only + scheduled

### Classroom matrix (v1)

| Classroom surface | Admin | Contributor | Analyst | Viewer |
|---|---|---|---|---|
| Public-provenance tracks + progress + study-next | ✓ | ✓ | ✓ | — no access |
| Guidance-derived lessons (post-C3) / guidance deep-links (pre-C3) | ✓ | ✓ | ✗ | — |
| Report-derived lessons | ✓ | ✗ | ✗ | — |
| Weekly briefing lessons | ✓ | ✓ | ✗ (public-only edition if trivial, else ✗) | — |
| Pipeline controls, content admin | ✓ | ✗ | ✗ | — |

**Provenance-gating rule (the rule that makes "Everything" safe):** a generated lesson inherits the **strictest gate of its inputs** — public-input lessons can reach analysts; guidance-derived lessons are contributor+; report-derived lessons are admin-only. Content flows *up* the corpus into lessons, never *down* the access ladder. The gate follows the content across apps: a tier that cannot see guidance in Profiler never sees guidance-derived material in Classroom.

**Field notes never become content** — private Drive data under the notes-are-not-sources rule. At most they steer pipeline emphasis (admin-supplied, never quoted, never cited).

## Content & pipeline model

- **Sources**: dossiers (+ v7 policy/project/KPI data), relationship graph, named projects, study guides, concepts registry, guidance modules, reports, and Scraper's corpus (timeline/candidates via the existing token-gated route). Public layers are fetched by the page directly; gated content is served by `Classroom.gs` behind its role checks
- **Storage**: authored lessons and track definitions live in `Classroom.gs` (the guidance content-in-GAS pattern) because tracks reference gated titles; per-account track progress lives in Classroom's own property store (the `gd_progress` pattern), independent of Profiler's ticks
- **The pipeline** (C2): a recurring Routine fires fresh Claude sessions that read what changed since the last run — Scraper corpus deltas, dossier revisions (`lastUpdated` / graph), new guidance and reports — decide what a BESS/AIDC seller now needs to know, and author or refresh lesson modules + a **"This week in BESS/AIDC" briefing lesson**, committed and deployed like guidance modules. Cadence recommendation: weekly, staggered clear of the Monday 06:00 ET Scraper run; the C2 session sets it. Pipeline sessions follow the freshness discipline: every lesson carries a `reviewBy` from its own dated gates, refreshes emit **"what changed since you learned this"** deltas when new material contradicts taught material
- **In-app runtime Q&A ("ask the instructor") stays deliberately deferred** — same standing as the 2026-08-22 guidance decision. `anthropicSummarize_` (Scraper) is the API precedent and the `claude-api` skill is the mandatory pre-read if it is ever revisited; it is a real budget surface and needs its own design pass

## Phase plan

- **C0 — Scaffold + Profiler retune**: `setup-gas-project.sh` → Classroom app (auth template); Classroom access matrix; the Profiler retune above (page + server + role-verifier); masthead cross-links between the apps. No guidance route (decision 6)
- **C1 — Learning core**: track/lesson schema (provenance-gated); first tracks assembled from the existing corpus (public content inline, guidance via deep-links); per-account track progress + rollup; study-next pointer
  > **Status: two of four slices built, and the app is live.** *(1) Schema + provenance stamp* — 2026-09-01, Classroom v01.01g: specified in `CLASSROOM-SCHEMA.md` (single source of truth), implemented in `Classroom.gs` (`CL_PROVENANCE_REF_KINDS`, `clStampKinds_()`, registries, per-tier index filtering, read-only `action=classroom` ops through `clRequireLesson_()`), verified by `scripts/check-classroom-content.py`. *(2) First tracks + renderer* — 2026-09-01, v01.01w / v01.02g: five schema-v1 lessons (four public-stamped from the study guides and concepts registry, one guidance-stamped) across two tracks, `bess-foundations` and `aidc-power-primer`; the guidance engine ported into `Classroom.html` as `cl*` equivalents on a light palette, driving the index and lesson views over `cop=index|lesson` with `#lesson/<id>` routing and a provenance strip that deep-links guidance inputs to Profiler pre-C3.
  >
  > **The backend is deployed and verified end to end** (2026-09-02, v01.02w / v01.03g): `DEPLOYMENT_ID` is recorded, the auto-deploy webhook fires on merge, and an admin session renders both tracks and all five lessons from the live ops. The provenance gate is exercised by real content, not only fixtures — analyst sees `bess-foundations` with two lessons and `withheld: 1`, and never sees `spec-sheet-decoded` in its index at all.
  >
  > **Remaining C1 slices:** per-account track progress + rollup (the `gd_progress` property-store pattern; the nav-tick markup and `clnav-<id>` ids are already in place), and the study-next pointer
- **C2 — Curriculum pipeline**: the scheduled authoring Routine, briefing lessons, freshness gates + deltas. **V1 ends here — verified before C3 starts**
  > **Status: C2a, C2b and C2c done — the pipeline is live; first real run measured against the contract.** *(C2a) The unattended-committer contract* — 2026-09-02, `CLASSROOM-COMMITTER-CONTRACT.md`: what a pipeline run may write (the content region of `Classroom.gs` + versioning/changelog files, nothing else), what it must never touch (AUTH region, gate derivation, stamp vocabulary, existing ids and gates), how it fails closed (`COMMIT` / `STAND-DOWN` / `BLOCKED`, one attempt, nothing committed unless every gate passes), and the thirteen corpus-delta guarantees a lesson change must satisfy. Design only. *(C2b) The machinery* — 2026-09-02: the `// CONTENT START` / `// CONTENT END` fence in `Classroom.gs`, `scripts/check-classroom-pipeline.py` (the twelve diff-aware assertions of the contract's §7, with a positive fixture and one failing fixture each under `--selftest`), `classroom-pipeline-ledger.json` (watermark + gate digest + last committing run), the corpus item key on the Scraper timeline route, the file-commit-date rule for the undated registries, and the weekly Routine (Wednesday 11:00 UTC, fresh session, push + email). *(C2c) The authoring prompt* — 2026-09-02, the "Authoring a pipeline lesson" section of `.claude/rules/classroom-app.md`: how a run decides what to teach, written to the twelve assertions — the G3 contradiction test (a nameable "section teaches X, source now says Y" sentence, or no change at all, pin included), `changed[]` as exactly P8's differs set, the briefing's G9/G11 bar (edition = run date; ≥ 3 qualifying items across ≥ 2 sources, counted after the gate decision), the frozen non-section fields, and the three obligations no checker sees (G2, G7, §5.2). The Routine's C2c pre-flight gate was removed the same day; the next Wednesday run is a real one
- **C3 — Guidance homecoming** (developer-proposed 2026-08-31): migrate Industry Guidance from Profiler to Classroom. Checklist: the module content functions + `guidanceDocs_`/index/doc/search/unified-glossary ops move to `Classroom.gs`; the **Admin lens re-hosts in Classroom** (reads the public `reports/*.report.json` + index from Pages under Classroom's own `reports`-capability check); dossier-side "covered in guidance modules" chips re-point through **one narrow token-gated mentions route** on Classroom that `Profiler.gs` proxies (mirror of the corpus-route pattern); per-account guidance progress ticks migrate via a one-shot admin export/import (developer study history is not resettable state); Scraper's `guidance:<module-id>` seed sources stay valid (module ids never change); the quarterly review Routine re-points to Classroom; `.claude/rules/industry-guidance.md` re-targets authoring at `Classroom.gs` (module JSON vocabulary and renderer contract unchanged)
  > **Slice plan — written 2026-09-13 at the open of session 1 (v05.43r → v05.44r).** The design's checklist is eight items; this is which session each lands in, and the file-layout decision the migration turns on.
  >
  > | # | Checklist item | Session |
  > |---|---|---|
  > | 1 | Module content functions + `guidanceDocs_`/index/doc (and the page-side search + unified glossary they drive) move to Classroom | **1** |
  > | 2 | Admin lens re-hosts in Classroom (public `reports/*.report.json` + index from Pages under Classroom's `reports` capability) | 2 |
  > | 3 | Dossier-side "covered in guidance modules" chips re-point through one narrow token-gated mentions route on Classroom that `Profiler.gs` proxies | 2 |
  > | 4 | Per-account guidance progress ticks migrate via a one-shot admin export/import | 3 |
  > | 5 | Scraper's `guidance:<module-id>` seed sources stay valid — module ids never change | invariant, held in **1** and re-checked each session |
  > | 6 | The quarterly review Routine re-points to Classroom | 3 — **developer-approved before it happens** |
  > | 7 | `.claude/rules/industry-guidance.md` re-targets authoring at `Classroom.gs` | 3 |
  > | 8 | Module JSON vocabulary and renderer contract unchanged | invariant, all sessions |
  >
  > Session 3 also carries the work the checklist implies but does not number: Profiler's guidance deep-links redirect to Classroom, and Profiler's own copy of the nine content functions is deleted — the last step, not the first.
  >
  > **File layout — the module content lands in `Classroom.gs` itself, below `// CONTENT END`, not in a second `.gs`.** `scripts/check-gas-inner-scripts.js` walks `googleAppsScripts/*/*.gs`, so the *checker* tolerates a second file — but the *deployer* does not. `pullAndDeployFromGitHub()` fetches exactly `FILE_PATH` and PUTs `[{name:"Code", type:"SERVER_JS", source:newCode}, manifest]` to `projects/<id>/content`, an API call that **replaces the project's entire file set**: a second `.gs` would either never reach Apps Script or be deleted by the next merge-triggered deploy, taking `guidanceDocs_()` with it and leaving the app calling an undefined function. It also has no `var VERSION` line of its own, and the deployer short-circuits on `"Already up to date"` by diffing exactly that line — so a commit touching only the second file would silently skip its own deploy. Supporting a second file means rewriting the pull-deploy path that all nine projects share, with no staging environment to prove it on; that is a deploy-architecture change, not a migration step. Size is not the binding constraint either way: `Classroom.gs` is 1.37 MB and the guidance block is 326 KB, far under Apps Script's project cap.
  >
  > **What a second file was wanted for, the fence gives instead.** The worry is an unattended C2 pipeline run rewriting guidance content. Placing the nine modules **below `// CONTENT END`** answers it exactly: the pipeline's write set stays what `CLASSROOM-COMMITTER-CONTRACT.md` §3 says it is — the `clLesson*_` / `clTrack*_` literals and the two registries — and `scripts/check-classroom-pipeline.py` (P2) already enforces that a pipeline diff never leaves the fence. Guidance content is outside it and therefore unwritable by an unattended run, using machinery the checkers enforce today rather than machinery the deployer cannot support.
  >
  > **Function names stay byte-identical** (`guidanceDoc*_()`, `guidanceDocs_()`, `guidanceIndex_()`, `guidanceDoc_()`). The move is then a verbatim copy that a parity checker can diff, and item 7 becomes a one-line file re-target rather than a rewrite of the authoring rule.
  >
  > **Sessions 1–2 run with the nine modules duplicated in both `.gs` files, deliberately** — the design requires Profiler's guidance to keep working until its slice is cut over, and `guidanceMentions_()` (item 3, session 2) cannot answer without the content beside it. The named risk is that something edits one copy during the window: the bankability review (`bess-bankability-2026-08`, `reviewBy` 2026-10-01) is scheduled *beside* C3 in `INTEGRATED-REMEDIATION-PLAN.md` §7.3 order 3, and it edits a module. `scripts/check-guidance-parity.py` closes that: it extracts all twelve functions from both files and fails on any byte difference, so a one-sided edit cannot reach `main`. It is deleted in session 3 with Profiler's copy.
  >
  > **Guidance renders read-only in Classroom in session 1 — no reading ticks.** Wiring ticks now would open a *second* progress store for the same material (`cl_progress:` beside Profiler's `gd_progress:`) during the very window item 4 exists to reconcile, turning a one-shot import into a two-source merge. Until session 3, Profiler stays the place guidance is *studied*; Classroom is where it now *lives*. No guidance item enters the drill until item 4 lands, which is decision 6's rule unchanged.
  >
  > **Session 2 — what actually landed (2026-09-13, v05.44r → v05.45r).** Both assigned items, and **nothing moved between sessions 2 and 3**: session 3's list is exactly what the table above says it is — items 4, 6 and 7, plus Profiler's guidance deep-link redirects, the deletion of Profiler's copy of the modules, and the deletion of `scripts/check-guidance-parity.py` with it.
  >
  > *Item 2 — the Admin lens.* `clLoadAdminLens` / `clApplyAdminLens` in `Classroom.html` (v01.11w), gated by `clCan('reports')` — admin only in `CL_ROLE_CAPS`, the same tier `OV_ROLE_CAPS` grants it, so **no access-matrix value changed** and `verify-profiler-roles.py` reports the same 13 × 4 matrix it did before. **No new server route**: the index and the reports are public Pages data under `CL_DATA_BASE`, the same bytes Profiler's `ovFetchJson` reads. Verified in a served Playwright render (real `fetch`, not a stub): admin sees **twelve panels across five modules** — `nvidia-800vdc` 3, `power-infra-aidc` 3, `bess-bankability` 2, `china-policy-stack` 2, `utility-aidc-procurement` 2 — **zero** on the other four, and a contributor sees zero on all nine while issuing **zero** report-data network requests, so the capability check gates the fetch and not just the paint. The two doubly-anchored sections (`bess-bankability/counterparty`, `power-infra-aidc/markets`) each carry both panels: insertion **appends**, it never replaces. Zero page errors on every surface.
  >
  > Three behaviours are worth naming because a future reader will diff the two implementations and find them. **(a)** Guidance is read-only here, so a section has no `.cl-done-btn`; the lookup is kept but a miss appends to the section rather than dropping the panel — the failure the brief warned about. **(b)** The stale-anchor path was proved with a synthetic overlay pointing at a deleted section id: the panel renders with its italic note and *both* content paragraphs, immediately before `#cl-glossary`. **(c)** Company names in panel prose are **not** linked to dossiers. Profiler's `gdLinkCompanies` walks the registry and rewrites text nodes; Classroom has no such helper, and adding one would be a page-wide feature rather than a lens port. Term tooltips, conversely, *are* bound on the panel — Profiler binds them before the lens resolves, so a `{{term}}` in a future overlay would render unbound there. No overlay uses one today.
  >
  > *Item 3 — the mentions route.* `guidanceMentions_()` moved to `Classroom.gs` **byte-identical** (verified by extraction and diff) and is exposed on `?action=guidancepeer&t=…&gpop=mentions`, the mirror of `scHandleCorpus_`. `Profiler.gs` no longer computes it: `handleGuidanceOp_`'s `gop=mentions` branch is now `guidanceMentionsProxy_()`, a server-to-server `UrlFetchApp` call on the shape `handleNewsOp_` already uses for Scraper's corpus route. `ovGuidanceMentionsLine` and `ovGuideApi('mentions')` are **untouched**, which was the test. Proved in Node per tier: viewer and analyst get `ROLE_DENIED` with **zero** upstream calls (`guidanceAllowed_` is still the only browser-facing boundary and it short-circuits before the proxy); contributor and admin relay in one call; every upstream failure — 403, unreachable, non-JSON — returns `success:false` rather than throwing, and the page's existing failure path renders nothing. On the Classroom side all six token-boundary cases (property unset, wrong token, absent token, empty token, sub-16-char property) return a flat `denied`, an unknown `gpop` returns `unknown_gpop`, and only the authorised call reaches the registry fetch. The scan's output is identical to what Profiler returned at `HEAD` — same 29 companies, same hits.
  >
  > **The secret is new and deliberately not `CORPUS_TOKEN`.** `GUIDANCE_PEER_TOKEN` must be set to the *same* random 16+ character value in **both** projects' Script Properties. Until it is, Profiler answers `not_configured`, Classroom refuses `denied`, and the dossier's `✦ Covered in guidance modules` line renders nothing — no error, no chips. This is the one step of C3 that cannot be done in the repo, and it is the only thing standing between this commit and the chips working again. A different pair of peers rotates on its own schedule, and a leak of the corpus token must not open the guidance route.
  >
  > **One thing session 3 inherits differently:** Profiler's copy is now twelve functions, not thirteen — `guidanceMentions_()` has already gone. `check-guidance-parity.py` covers exactly those twelve and is unaffected.
  >
  > **Session 3 — what actually landed, and C3 is CLOSED (2026-09-13, v05.45r → v05.48r).** All three assigned items, both unnumbered ones, and the Routine. **Industry Guidance now lives, is served, is studied and is drilled in exactly one app.**
  >
  > *Item 4 — the progress ticks.* A one-shot admin operation in six no-underscore functions (Apps Script hides `_`-suffixed ones from the editor's Run dropdown, so the names had to change shape): Profiler's `exportGuidanceProgress()` filters `gd_progress:<email>` to the nine module ids over a **frozen `GD_MIGRATED_MODULE_IDS` literal** — not a walk of `guidanceDocs_()`, which is being deleted in the same commit, and the literal is the honest source anyway because module ids are permanent. Classroom's `previewGuidanceProgressImport` / `applyGuidanceProgressImport` / `verifyGuidanceProgressImport` read it from a `GUIDANCE_PROGRESS_IMPORT` Script Property (an editor-run function takes no arguments) into `cl_progress:<email>`; Profiler's `previewGuidanceProgressPrune` / `applyGuidanceProgressPrune` drop the migrated ids afterwards and are **optional, last, and must not run before the verifier is clean** — study history is not resettable state.
  >
  > **The merge rule is "an existing Classroom tick always stands", and it is correct in both directions.** The brief required only that a newer Classroom tick not be overwritten by an older Profiler one; max-date-wins would satisfy that too and is *wrong*, because when Profiler's date is the newer of the two, Classroom's earlier date is the true first completion and moving it forward fabricates history and suppresses every delta in between. Never overwriting also makes the import **idempotent by construction** and matches the "an existing value stands" rule both progress stores already state.
  >
  > **The done-when is met.** `scripts/check-guidance-migration.js` (new) runs the six real functions, lifted out of both `.gs` files, in two isolated VM contexts with one Script Property store each — the isolation the two projects have in production — against a fixture built from the real registry: `study-`/`dossier-` ticks sharing the property, a legacy `true`, an unreadable date, a ghost section id, an account with no module ticks, an unrelated property, and a Classroom tick newer than Profiler's. **44 assertions pass**: the export mutates nothing and is deterministic, the dry run writes nothing, the verifier accounts for every exported tick per account with zero missing, the second import writes **0** and leaves the store byte-identical, and the prune leaves study and dossier ticks and unrelated properties untouched and is itself idempotent.
  >
  > *The read-only window closed with it.* `clProgressValid_` admits every registered module behind `clCan_(sess,'guidance')` — **one map, not two**, because the invariant is one: progress is never a weaker gate than reading. Modules render with mark-as-understood buttons and filling nav ticks (`clRenderSection` with no `opts` at all, the same call a lesson makes), the library cards carry a completion readout, and `clDrillGuidanceItems_` puts **139 guidance items** into the C4 pool as `gc:`/`gq:` ids beside the 216 lesson items. **Decision 6 is at its target state.**
  >
  > *Items 6 and 7 — the deep links and the deletion.* `#ov-guide-btn` became an `<a href="Classroom.html#guidance">` and the dossier mention chips `<a href="Classroom.html#guidance/<id>">` — same id, same `ovCan('guidance')` gate, same masthead slot, so `verify-profiler-roles.py` reports the identical 13 × 4 matrix and **no access-matrix row moved**. Then the twelve functions, the `gop=index|doc` branches, ten page-side functions and five dead CSS blocks went, with `scripts/check-guidance-parity.py`. `Profiler.gs` 760 KB → 449 KB; `Profiler.html` 8,099 → 7,818 lines.
  >
  > **What deliberately did NOT go, as the checklist asked be recorded:** the `gd-*` renderer. Guidance was never its only caller — `gdRenderDoc` draws the public study guides, the report view builds on `gdTable`/`gdTimeline`/`gdBars`/`gdProsCons`, and the sign-in log uses `gdTable` and `gdCloseBtn`. Deleting it would have been deleting three live surfaces to tidy up after a fourth. The progress store stayed too, and stayed named `gd_progress:` — renaming it would orphan every study and dossier tick to buy a tidier label.
  >
  > *Item 7 (rules) and item 6 (Routine).* `.claude/rules/industry-guidance.md` re-targeted at `Classroom.gs` — frontmatter, the authoring file and its `// CONTENT END` fence, the access model (`CL_ROLE_CAPS` + `clRequire_`, and `gop=mentions` named as the token-gated peer route it is rather than a third browser op), the versioning targets, the render recipe and the Routine paragraph. A file-target and op-name change, not a rewrite — which is exactly what session 1 kept the function names byte-identical for. The **quarterly review Routine** (`trig_01CrhxzfBV6uKQNKpUXLLMSZ`) was updated **in place after the developer was asked in-session and said yes**: same trigger id, same cron, still enabled, run history intact, now walking `guidanceDocs_()` in `Classroom.gs`.
  >
  > **One thing a future session should know:** `scripts/check-classroom-content.py` asserted `tickable == sorted(readable lessons)` per tier, so admitting modules broke it by design. The checker moved with the code in the same commit and the new assertions are *stronger* — the guidance drill pool is gated per tier (a tier without the capability draws nothing, not even an id), every served guidance id must satisfy `CL_DRILL_ID_RE`, and **lesson ids and module ids must be disjoint**, since they share the progress and drill namespaces. 134 → 142 gate cases. `clProgressValid_` is a `GATE_SYMBOLS` member, so `gateDigest` was refreshed in the same commit.
- **C4 — Retention**: spaced-repetition drill queue across all flashcards/quiz items, per-item history; needs its own storage design (drill history outgrows the 9KB-per-account property pattern — expect a sheet-backed store)
  > **Status: built — 2026-09-02, Classroom v01.06w / v01.09g.** Specified first in `CLASSROOM-SCHEMA.md` → "Drill items and history" (item identity, the content hash, the SM-2 grade table, the two tab layouts, the caps), then implemented: `ClassroomDrill` (state, the queue) and `ClassroomDrillLog` (append-only, one row per grade) on the Classroom spreadsheet; `cop=drill` and `cop=grade` behind the app door with lesson items enumerated through `clLessonVisible_` and study items validated by containment against the public registry; the `#drill` route, landing card, reveal-then-grade card and source links in `Classroom.html`. The storage prediction in this line held — the sheet-backed store is what the item count required.
  >
  > **Order note: C4 was pulled ahead of C3**, on the argument in `IMPROVEMENT-PLAN.md` §2 P1 and §6 — the C3 → C4 sequence was a build order, not a dependency, and C4 v1 needs no guidance content. Retention is the stated purpose of the app; C3 relocates material that already works. **Decision 6 is untouched**: no guidance item enters the drill until C3 moves guidance into Classroom, so no interim Profiler→Classroom guidance route exists. The drill's pool today is lesson flashcards, lesson quiz items, and public study-guide flashcards.
  >
  > **Prerequisite that landed first** (v04.27r, Session B): both progress stores moved from booleans to completion dates. The drill did not strictly need it, but the "changed since you read it" delta did, and the two share the same account-scoping and the same never-a-weaker-gate rule.
- **C5 — Sales simulations**: objection/discovery scenario modules built from dossier facts, relationships, named projects, and policy exposure
- **C6 — Team layer** (gated on a team actually existing): instructor dashboard, placement quiz, certification checkpoints

Each phase runs as its own session train with the standard Pre-Commit/Pre-Push discipline, exactly as Phases 0–5 did.

## Verification expectations

Per build session: `node --check` on `.gs` copies + `scripts/check-gas-inner-scripts.js`; Playwright render + screenshot read of every new surface; `scripts/verify-profiler-roles.py` after any access-matrix change (C0 extends it); the C3 migration is complete only when guidance renders in Classroom, the lens and mention chips work, and migrated progress ticks verify against a pre-migration export.

## After Phase 6

C6 closes the original 7-phase plan. Beyond it, candidate future decisions already named and deliberately deferred: in-app runtime Q&A, note-steered emphasis, audio/commute mode.

Developed by: LightAISolutions
