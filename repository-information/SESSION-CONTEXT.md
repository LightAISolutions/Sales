# Previous Session Context

Claude writes to this file when the developer says **"Remember Session"** — capturing enough context for a future session to pick up the train of thought quickly. This is separate from "Reminders for Developer" (REMINDERS.md), which is the developer's own notes.

> **Note on stale-context auto-reconstruction** — when a session starts and this file's `Repo version:` doesn't match the current repo version, Claude reconstructs the missing entry from CHANGELOG.md and commits it **without pushing**. The commit rides along with the session's first user-task commit on the next push. If a session ends before any user-task push happens, the reconstructed entry stays **local-only** and the next session will just re-reconstruct from CHANGELOG if still stale. This is intentional — pushing a dedicated reconstruction commit on its own would force every subsequent user push in the same session to wait for the auto-merge workflow to finish before it could push too (push-once enforcement). The reconstructed entry is a convenience hint, not load-bearing state, so the small persistence risk is a fair trade.
## Latest Session

**Date:** 2026-09-16 03:00:50 AM EST
**Repo version:** v06.10r — two pushes (v06.09r the lesson, v06.10r this handover)
**Branch:** `claude/epic-gates-ynmejl` — restarted from `origin/main` before the first push

### What was done

- **Phase 4 row 15 — the `string-versus-central` lesson** (v06.09r). The **forty-fifth** lesson and **the one that completes `electrical-foundations`**: *One Big Box or Many Small Ones*, id `string-versus-central`, group `Technology Foundations`, `updated` 2026-09-16, `reviewBy` **2027-03-16**, authored **inside** the `// CONTENT START` … `// CONTENT END` fence and registered at the **end** of `clLessons_()`
- **Inserted at §4 position 2 of the track's `lessons[]`**, between `four-machines` and `the-transformer-and-the-substation` — **the first Phase 4 row whose correct write is a mid-array insert**. The track is now whole at five of five
- **Eight sections, not §3.2's seven**: `the-fork` (prose) · **`three-answers-not-two` (proscons, inserted)** · `solar-taught-it` (prose) · `same-argument-three-rooms` (table, 6 × 4) · `the-hidden-engineering` (callout) · `where-it-fails` (callout, 6 failure modes) · `drill` (7 cards) · `check-yourself` (5 items). Four tiles, a six-entry local glossary
- **Nine public stamps**, every date read off the fetched document and every ref resolved by matching its own `slug`; the fold to `tracks` **verified through the real serving path** — an analyst answered `cop=lesson` with all eight sections and `cop=index` with the track in §4 order and `withheld` 0, a viewer got `ROLE_DENIED` on both
- Classroom GAS **v01.50g → v01.51g**, one generic public changelog line; `Scraper.gs` untouched at v02.11g; the guidance-module assertion left at **19**; no segment regenerated
- **§7.3's Phase 4 row and order-6 cell flipped 14 → 15 of 26**, §6's Phase 4 row likewise, curriculum §7 row 15 flipped to built with the full record, §4's `electrical-foundations` table flipped at position 2 and re-worded at 3, 4 and 5 now the track is whole. **§7.37 written in the same commit**, both halves
- **v06.10r (the handover)** — the measured deploy counters recorded and `SESSION-CONTEXT.md` written, in the same commit as the repo-version bump, per the v06.02r rule

### Where we left off

Everything is merged and live. **Phase 4 is 15 of 26; S2 is 10 of ~19**, so the interleave stands at 15:10 and hands the turn to **S2 session 11, `hyperscalers-and-ai-labs`** — its brief is **§7.37**, written inside the v06.09r work commit. GAS live at Classroom **v01.51g**, Scraper **v02.11g**. The content-checker baseline is **0 errors / 0 warnings at 45 lessons / 8 tracks / 142 gate cases**, with the guidance-module assertion at **19** — session 11 moves it to 20. `--check` reports **10 due**.

### Key decisions made

- **THE §8 ITEM 6 CONTRADICTION WENT THE OPPOSITE WAY FROM ITS BRIEF'S PREFERENCE, AND THE EVIDENCE IS WHY.** §7.36 preferred converting the second `prose` to `bars` or `table` **if** the corpus states a harvest delta or an availability figure. **It states neither.** Huawei says granularity *"harvests more"* and that a module swaps *"in minutes"* against *"hours or days"*; Sungrow says *"more independent MPPT channels = less energy lost"* — relationships without figures, which §8 item 6 itself routes away from `bars`. The only percentages nearby belong to other arguments (Fluence's `>99%` is a transmission-grade BESS spec; Liteon's 98/99.5 % is board yield). Route (a), and the insert is a **`proscons`, not a table**, because the record names **three** shapes and not two — Sungrow states the modular middle outright, and that middle term is what `where-it-fails` later turns into *"a monolith in disguise"*
- **P5 FIRED AS FORECAST AND IT IS THE CONTRACT WORKING; P8 WAS FORECAST AND DID NOT FIRE.** The mid-array insert fails `head[:len(base)] == base` by construction, which binds the unattended committer and not a developer session. But §7.36's *"expect P1, P5 and P8"* over-predicted: **P8 iterates only lessons present at both base and head *and touched*** — read out of `p8_changed_names_what_moved` — so a row that adds a lesson and opens no existing one passes it trivially. **The measured signature is P1 and P5 alone.** Correction recorded in both the CHANGELOG and curriculum §7
- **§10.6 (t) RAN THE UNUSUAL WAY ROUND: THREE *DOWNSTREAM* LESSONS ALREADY OWN THE SPECIFICS.** This lesson is read **before** them in the walk (track 2 against track 3) and yet all three were built first — `the-ups-room` owns the module/frame/unit/system/path ladder, `inside-the-rack` owns the ORing/precharge/current-sharing mechanics, `redundancy-by-the-numbers` owns N/N+1/2N/catcher/nines. None restated. The boundary written into the lesson: **those answer "how many of them"; this one answers "how big is one of them"**
- **A SECOND INSTANCE OF SESSION 10's UNBUILT-HANDOFF SHAPE, NAMED NOT FILLED.** `the-800-vdc-shift` (built, public) hands the **DC arc taxonomy** to *"the engineering lesson at the stricter gate"* — `dc-fault-engineering`, §7 row 20, **unbuilt** — while `breakers-relays-and-faults` carries the principle and explicitly not the taxonomy. Nobody holds it. Conversely, **nothing pre-declares a handoff *to* `string-versus-central`** (searched all twenty-six hand-authored lessons for its id, title and phrase): this row inherited no debt and created none
- **BOTH HALVES OF THE DUE COUNT CALLED EXACTLY, AND THE THREE-PART TEST RUN PROGRAMMATICALLY.** 9 → 10, the new entry `power-conversion-and-rack-power-silicon`, `sections differing: read-next` alone, staying at 10. The deepening half was **no** because **neither `string` nor `central` appears in any of that segment's six `buyingCriteria`** — printed and read, not assumed — even though the lesson **is** in `CRITERION_LEXICON` and **is** in exactly one `READ_NEXT`
- **THE MICRO-MARKUP CHECK EARNED ITS KEEP FOR THE FIFTH SESSION.** `where-it-fails` shipped `**"Modular" … only *manufactured* that way.**` — a nested italic inside a bold that `clFmt`'s `[^*]` class cannot match. `node --check` passed, the schema was valid, every term resolved, and literal asterisks would have reached the reader
- **NO ROTATION, AND §7.36's ESCAPE CLAUSE FIRED RATHER THAN ITS CONCLUSION.** EST read 2026-09-16 02:55 against UTC's 06:55 and the push landed **on** 2026-09-16 EST — the exact condition §7.36 named. `Classroomgs.changelog.md` closes at **51 raw / 48 non-exempt against a cap of 50**; `CHANGELOG.md` at **98 raw / 93 non-exempt**

### Active context

- **Repo version** v06.10r · **`CHANGELOG.md` 98 raw**, five sections dated 2026-09-16 EST, counter `Sections: 98/100`. **98 non-exempt once EST rolls past 2026-09-16** — two clear of the trigger, so the next session is **probably not** the repo rotator, but its own section takes the file to 99 and the one after arms it. Next group in line: **2026-09-08 (10 sections)**
- **THE NEXT SESSION IS THE GAS ROTATOR AND ALMOST CERTAINLY CANNOT ESCAPE IT.** `Classroomgs.changelog.md` closed at **51 raw / 48 non-exempt against a cap of 50**, three sections dated 2026-09-16 EST; the moment EST rolls past that date it stands at **51 non-exempt**, which IS the trigger. Oldest whole date group **2026-09-02 (11 sections)**: 51 − 11 = 40, plus the new one = 41, under the cap in one pass. `Scrapergs.changelog.md` **32/50** (moves only on an S2 seed); `Profilerhtml.changelog.md` **49/50**, closest of the three to its own trigger and untouched by anything S2 or Phase 4 does
- **GAS versions:** Classroom **v01.51g**, Scraper **v02.11g**, Profiler v01.39g. **Page versions:** Classroom v01.14w, Profiler v01.90w, Scraper v01.72w — no page bump this session
- **Content-checker baseline is 0 errors / 0 warnings at 45 lessons / 8 tracks / 142 gate cases** — protect it. **The gate-case count did not move** (a public `tracks` lesson adds no new fixture shape). **The guidance-module assertion is 19 and S2 session 11 takes it to 20**
- **Deployment counters: **Classroom `deployment 63 | 62/200`, Scraper `156/200` — MEASURED at v06.10r, and the two Classroom figures DIVERGED for the first time.** One `.gs` in the merge diff fired exactly one step and logged exactly one line, so that half of the forecast held — but the line is `Classroom deploy confirmed (POLL 1)`, the **first non-first-GET confirmation in nineteen uses** of the §7.19 order. The GET leg fired a deploy that did not confirm, the POST fallback fired another, and only the poll read a confirmation, so **two deployments were consumed and one was tallied** — the abstract warning in the counter rule, now observed. **Treat the deployment number as authoritative and the tally as under-reporting by one: Classroom's real headroom is 200 − 63 = 137.** Pages and the job log agreed and **no `?op=deploy` probe was made**; Pages lagged at `|v01.50g|` only while the Pages job was still running. The counter rule, with the new instance: `Updated to` consumes one; a step that never fires consumes none and logs nothing; **a leg that fires and does not confirm still consumes one and the tally does not see it**; `Already up to date` is silent on consumption and must never be forecast as zero**
- **`--check` reports 10 due** — `cells-and-chemistry`, **`power-conversion-and-rack-power-silicon`**, `compute-and-the-rack`, `epc-and-construction`, `storage-developers-and-ipps`, **`hyperscalers-and-ai-labs`**, `neoclouds`, `capital`, `software-and-optimization`, `insurance-and-risk-transfer`. **Session 11's own segment is already in the set**, so expect **10 → 10 → 9** with `read-next` alone at every step
- **THE NEXT SEGMENT IS THE SMALLEST S2 HAS AUTHORED AND ONE OF ONLY TWO *CLOSED* SEGMENTS — all measured at v06.09r.** `hyperscalers-and-ai-labs` holds **8 members: 5 incumbents · 3 challengers · 0 adjacents** (`amazon`, `google`, `meta`, `microsoft`, `oracle` against `anthropic`, `openai`, `xai`), every one with a dossier and a guide. The previous smallest landscape was utilities at 14. Three segments carry zero adjacents; only two are **closed** — zero adjacents *and* no member in any other segment — and this is one, at **8 of 8 pure plays**. **Session 10's adjacency instrument cannot be run here at all**, because it is computed over adjacents and there are none; what the closure means instead is that the segment is the chain's **terminus**
- **`check-classroom-curriculum.py --strict`** reports no structural findings, **28 stale pins** (unchanged — this session's nine pins are all current) and **2 items due for review** — `landscape-utilities-2026-09` and `landscape-in-hall-power-2026-09`, both 2026-10-01 and both by design. All exit 0. **Do not "fix" either**
- **Toggles:** `START_OF_RESPONSE_BLOCK` On · `CHAT_BOOKENDS` Off · `TIMING_ESTIMATES` On · `END_OF_RESPONSE_BLOCK` On · `MULTI_SESSION_MODE` Off
- **Where the programme stands, measured 2026-09-16 at v06.10r.** Done: Phases 0, 1, 2a, 2b, 3, 5, 6; S0, K1, S1, S3, G6, C3, the bankability review, the one-off segment regeneration. Open: **Phase 4 at 15 of 26**, **S2 at 10 of ~19**, **K2** (1–2 sessions, runnable any time), **C5** (~6 sessions; its gate of ~6 landscapes is **MET**), the **Q plan clock** (next due ~2026-12). Deferred: C6. **About 27 sessions remain**
- **Open, recorded, not taken.** The `.cl-tbl` `min-width` question now has **twenty-one** clean data points (a six-row four-column table clean at 1400px); the generated `read-next` link is a `Classroom.html` renderer feature (`clFmt` has no link token) — **both want the same session, one that edits `Classroom.html`, plus a developer `--all` run**. **`the-800-vdc-shift`'s stale `three-chains` intro is now fifteen inserts stale** and row 15 was the nearest miss yet. **§10.6's session-4 bullet block still sits out of chronological order** — cosmetic. **A stale `lesson_ref` can make a segment due on its own, and row 15 was the THIRD consecutive Phase 4 row to leave one** — three in a row makes it systematic rather than incidental, which is the evidence that open item was waiting for and a developer decision rather than another session's discovery. **NEW: the DC-arc-taxonomy gap above** — a built public lesson declining material to an unbuilt one, for the second time in the corpus
- **Render harness — the recipe, confirmed again and unchanged.** Run the PROJECT region of `Classroom.gs` under Node in a `vm` context with GAS shims; shim **only** `validateSessionForData` (flat `{email, role, permissions}`, permissions an **ARRAY**) plus no-op `clProgressRead_` / `clProgressWrite_` / `clStudyNext_` / `dataAuditLog` / `auditLog`. **Route the op: `payload.action === 'guidance'` → `ctx.handleGuidanceOp_`, everything else → `ctx.handleClassroomOp_`** — through the vm context object, never bare. Pass the op as `{ parameter: payload }`. **Serve the scratch copy at `/Classroom.html`, NOT at the site root.** `_e = ''` and `AUTO_REFRESH = false`; copy `profiler-data/`, `sounds/`, `images/` and `favicon.ico` in. Seed `sessionStorage` **after** load (`Classroom_gas_session_token` · `_gas_user_email` · `_gas_user_role` · `_gas_user_permissions`), token **≥ 32 characters**; override `_gasPost` **after** load and return a **Promise**. **Point `_gasPost` at a same-origin path (`/__gas`) and fulfil it with Playwright's `page.route()`** — `connect-src` is `'self'` plus Google hosts and a cross-port fetch is refused outright. **Hide** `#auth-wall`, `#signing-out-wall`, `#auth-timers`, `#gas-layer-toggle` plus `#cl-app{display:block !important}`. Entrypoints: `clShowLesson(id)`, `clShowGuidanceDoc(id)`. Section nodes are `#cl-<sectionId>`. Launch with `executable_path` from `glob('/opt/pw-browsers/chromium-*/chrome-linux/chrome')`; `pip install playwright` is needed (the browser is pre-installed, the Python package is not). **This session: analyst 8/8 sections, 32,028 characters, zero page errors, zero surviving `{{…}}` and zero literal asterisks; viewer `ROLE_DENIED` on both `cop=lesson` and `cop=index` from `clRequire_`'s own boundary**
- **The two console errors in every render are environmental and pre-existing**, both `ERR_CERT_AUTHORITY_INVALID` because the sandbox proxy's CA is untrusted by Chromium. Neither is an asset and neither raises a page error

### Recommendation for next session

- Run **S2 session 11 — the `hyperscalers-and-ai-labs` landscape module** on Opus 5 xhigh. Its brief is **`INTEGRATED-REMEDIATION-PLAN.md` §7.37**, written inside the v06.09r work commit with its deploy counters measured at v06.10r. It carries three things worth the attention: **a segment shape no landscape has met** — eight members, zero adjacents, and one of only two *closed* segments in the taxonomy, so session 10's adjacency instrument cannot be run and the closure has to be measured and read on its own terms; **a certain changelog rotation**, because `Classroomgs.changelog.md` sits at 51 raw against a cap of 50 with only 2026-09-16 EST protecting it; and **a segment already in the due set**, so the count runs 10 → 10 → 9 rather than opening clean.

**To continue:** type `run S2 session 11`

## Previous Sessions

### Previous Session — v06.08r

**Date:** 2026-09-16 01:52:24 AM EST
**Repo version:** v06.08r — two pushes (v06.07r the module, v06.08r this handover)
**Branch:** `claude/gallant-wright-he04cb` — restarted from `origin/main` before the second push

#### What was done

- **S2 session 10 — the `clean-firm-and-nuclear` landscape module** (v06.07r). The **nineteenth guidance module** and the **tenth landscape**, authored as `guidanceDocLandscapeCleanFirmAndNuclear_()` **below** the `// CONTENT END` fence and registered at the **end** of `guidanceDocs_()`'s The Value Chain lane. Id `landscape-clean-firm-and-nuclear-2026-09`, tier **contributor**, `reviewBy` **2026-12-31**
- **Nine §10.6 sections in its order**, a **five-row** `each-players-bet` table (the shortest in the corpus), a **nine-row** `the-indicators` table, a **47-row** claims ledger, four tiles and an eight-entry glossary
- **The segment re-measured on the day at 16 members (3 · 2 · 11)**, confirming §7.35 member for member — the **tenth consecutive** session to do so — with all sixteen carrying a dossier
- **The guidance-module assertion moved 18 → 19** in `scripts/check-classroom-content.py` — the one edit outside the fence a landscape session makes
- **One segment regenerated** with `--segment clean-firm-and-nuclear --today 2026-09-16` and nothing else. **Scraper seed `topic-landscape-clean-firm-and-nuclear` added** (thirteen terms) after scoring against all 431 existing terms. Classroom GAS **v01.49g → v01.50g**, Scraper GAS **v02.10g → v02.11g**, one generic public changelog line each. No page bump; `Profiler.gs` and `Profiler.html` untouched
- **§7.3's S2 row flipped 8 → 10** with **both** the missing session 9 record and session 10's — closing the one-session gap §7.35 item (iv) asked for; §6's S2 row 9 → 10; §10.6's progress note 9 → 10 of 19, dated; the session-10 findings block **(aa1)–(aa5)** added; **§7.36 written in the same work commit**, both halves
- **v06.08r (the handover)** — the measured deploy counters recorded in the v06.07r Notes and in §7.36's deploy paragraph, and `SESSION-CONTEXT.md` written. **Carried in the same commit as the repo-version bump**, per the v06.02r rule

#### Where we left off

Everything is merged and live. **Phase 4 is 14 of 26; S2 is 10 of ~19**, so the interleave stands at 14:10 and hands the turn to **Phase 4 row 15, `string-versus-central`** — the lesson that completes `electrical-foundations`. GAS live at Classroom **v01.50g**, Scraper **v02.11g**. The content-checker baseline is **0 errors / 0 warnings at 44 lessons / 8 tracks / 142 gate cases**, with the guidance-module assertion now at **19** — row 15 leaves it there. `--check` reports **9 due** and row 15's registration will take it to **10** without clearing it.

#### Key decisions made

- **THE FINDING OF THE SESSION: the split was drawn by a lesson that does not exist yet, and that is a shape no previous landscape has met.** `contracts-and-revenue` — Phase 4 row 11, **public**, built, stamped on all three of this segment's incumbents — carries a section `clean-firm-and-the-restart` whose **opening sentence hands the physics off by name**: *"The physics … belongs to Clean Firm Power in the campus track, and is deliberately not repeated here."* **`clean-firm-power` is §7 row 16 and unbuilt.** So a built public lesson pre-declared a handoff **to a lesson that does not exist**, and the declined material is held by nobody. Session 6's (s) gains a **third posture** — say the test is unowned and decline to own it — which is hardest to obey when the missing lesson is the segment's own subject. §7.23's test held on the **uprate attached to a supply agreement**. **Twenty-two omissions across eight neighbours** enumerated
- **THE EMPTY `notes` WAS ANSWERED BY MEASUREMENT, AND THE MEASUREMENT IS A NEW INSTRUMENT.** §10.6 (l) and (y4) both need a registry note; this segment has none. Cross-referencing all nineteen segments: **eleven of the eleven adjacents are ranked incumbent or challenger in another segment, eleven of eleven with no exception**, and **the two challengers are the segment's only pure plays** — the only two of the sixteen belonging to no other segment. 19 shared memberships across seven neighbours, **14 inversions (74 %), the highest rate measured** — and the **direction is uniform**, not position-dependent (session 8's (y1)) or bimodal (session 9's (z3)). **The rule: adjacents 100 % ranked elsewhere means the segment is a PRODUCT other industries make, not a LAYER they sell into**
- **TWO §7.35 MEASUREMENTS WERE FALSE AND THE STANDING INSTRUCTION TO VERIFY CAUGHT BOTH — the third consecutive brief to carry one.** (1) The brief said the segment was last regenerated 2026-09-16 EST and would report P7; **the literal read `updated: 2026-09-14`**, so P7 could not fire and did not. (2) The brief said four of six buying criteria have no built owner; the generator prints a **dash against five** and the sixth points at an unbuilt lesson, so **five of six have no owner at all and six of six have no *built* owner**. **The first draft inherited the brief's four into a `sales` line and contradicted its own record section two sections later — caught ONLY by reading the rendered screenshots**, which nothing in the toolchain does
- **`reviewBy` produced a SIXTH distinct failure mode: both standing rejection classes intersecting to exhaust the field.** 67 policy entries, 49 dated, **exactly one in the future** — a Virginia large-load rate class that is another lane's subject *and* already four modules' `reviewBy`. Every on-subject gate is then either month/quarter-level (the November 2026 final safety evaluation, itself a *tell* rather than a gate; the Q1 2027 construction permit) or lands on a day another module holds (2026-11-30, 2027-03-31). **So 2026-12-31 is the THIRD module to carry that day**, taken on a four-way convergence inside `fermi-america`'s `policyExposure[0].exposure`, an entry with **no `effectiveDate` at all**. **Eight** rejections in writing; the module ships outside its own 30-day horizon so the checker still reports 2 items due
- **BOTH HALVES OF THE DUE COUNT CALLED CORRECTLY, AND THE NETTING WAS TRACED TO ITS CAUSE FOR THE FIRST TIME.** `--check` **10 → 10 → 9**, `read-next` alone at every step, the section half **derived** from the generator before the write. But the segment was already due for a reason unrelated to this module: **`the-campus-as-a-power-project` was built AFTER that segment's last regeneration, so `lesson_ref` had been rendering a BUILT lesson as `(planned)`.** Every Phase 4 row that lands a lesson named in some segment's `READ_NEXT` leaves exactly this behind and nobody owns clearing it — recorded as open item (v). Row 14's three-part test was run programmatically and **passed cleanly**, which sharpens the rule: **it governs a LESSON registration and is structurally irrelevant to a LANDSCAPE**
- **A HARNESS DEFECT FOUND AND FIXED THAT NO CHECKER WOULD HAVE CAUGHT.** Splicing the new function after the previous one's `};` nested it **inside** that function — valid JavaScript, so `node --check` passed, while `guidanceDocLandscapeCleanFirmAndNuclear_` could never have resolved at global scope. **Caught by inspecting the splice point**, not by any checker. When appending a function to `Classroom.gs`, the anchor is the function's closing `}` on its own line, not the literal's `};`
- **The Scraper-seed prior resolved YES and decisively.** A `seg-nuclear` seed exists but carries **four generic words**; against all **431** terms the segment's licensing, fuel and contracting vocabulary scored **zero across the board** — criteria 1, 2 and 4 entire. Thirteen seeded; **three dropped on split grounds** per (v), including `front-of-the-meter`, whose mirror already sits in `seg-bess-utility`
- **The deploy forecast held exactly: two `.gs` in the diff → two steps → two lines, both on the first GET.** `Scraper … Updated to v02.11g (deployment 156) | 156/200` and `Classroom … Updated to v01.50g (deployment 61) | 61/200` — the **eighteenth consecutive clean use** of the §7.19 order, no POLL and no probe, with Pages agreeing before the log was closed

#### Active context

- **Repo version** v06.08r · **CHANGELOG 97 raw**, four sections dated 2026-09-16 EST, counter `Sections: 97/100`. **93 non-exempt once EST rolls past 2026-09-16** — the next session is **NOT** the repo rotator; the next group in line is **2026-09-08 (10 sections)** and it does not arm until the file is back at 100
- **Standing changelog state — and the NEXT session IS the GAS rotator.** `Classroomgs.changelog.md` closed at **50 raw / 48 non-exempt against a cap of 50**, two sections dated 2026-09-16 EST; **the moment EST rolls past that date it stands at 50 non-exempt, which IS the trigger**, so unless the next push also lands on 2026-09-16 EST it rotates — oldest whole date group **2026-09-02 (11 sections)**. `Scrapergs.changelog.md` **32/50**. `Profilerhtml.changelog.md` **49/50**, closest of the three to its own trigger, and nothing a Phase 4 row does touches Profiler
- **GAS versions:** Classroom **v01.50g**, Scraper **v02.11g**, Profiler v01.39g. **Page versions:** Classroom v01.14w, Profiler v01.90w, Scraper v01.72w — no page bump this session
- **Content-checker baseline is 0 errors / 0 warnings at 44 lessons / 8 tracks / 142 gate cases** — protect it. The gate-case count did not move. **The guidance-module assertion is now 19 and a Phase 4 row leaves it there**
- **Deployment counters: Classroom 61/200 and Scraper 156/200, both MEASURED at v06.08r against the job log.** Two `.gs` files in the merge diff fired exactly two steps and logged exactly two lines, both on the first GET leg, the **eighteenth consecutive clean use** of the §7.19 order. **The counter rule, unchanged**: `Updated to` consumes one; a step that never fires consumes none and logs nothing; `Already up to date` is silent on consumption and must never be forecast as zero
- **`--check` reports 9 due** — `cells-and-chemistry`, `compute-and-the-rack`, `epc-and-construction`, `storage-developers-and-ipps`, `hyperscalers-and-ai-labs`, `neoclouds`, `capital`, `software-and-optimization`, `insurance-and-risk-transfer`. **Row 15's forecast is measured, not guessed: 9 → 10, `read-next` alone, STAYING at 10** — `string-versus-central` is in `CRITERION_LEXICON` (keys `string`, `central`) and in exactly one segment's `READ_NEXT` (`power-conversion-and-rack-power-silicon`), but **neither keyword hits any of that segment's six criteria**, so the deepening half is no; the membership half is yes via the `lesson_ref` flip, and a Phase 4 row regenerates nothing
- **ROW 15 WILL FIRE P5, AND IT IS NOT A DEFECT.** `electrical-foundations`'s `lessons[]` holds §4 positions **1, 3, 4, 5**; row 15 is position **2**, so the correct write is a **mid-array insert** and P5 asserts `head[:len(base)] == base` on every track's `lessons[]`. P5 binds the unattended committer, which may only append; a developer session places a lesson at its planned position. **Expect P1, P5 and P8. Do NOT "fix" it by appending** — that would put the lesson at position 5 and contradict §4
- **`check-classroom-curriculum.py --strict`** reports no structural findings, **28 stale pins** (unchanged — a guidance module carries no `provenance`, so this session added none) and **2 items due for review** — `landscape-utilities-2026-09` and `landscape-in-hall-power-2026-09`, both at 2026-10-01 and both by design. All exit 0. **Do not "fix" either**
- **Toggles:** `START_OF_RESPONSE_BLOCK` On · `CHAT_BOOKENDS` Off · `TIMING_ESTIMATES` On · `END_OF_RESPONSE_BLOCK` On · `MULTI_SESSION_MODE` Off
- **Where the programme stands, measured 2026-09-16 at v06.08r.** Done: Phases 0, 1, 2a, 2b, 3, 5, 6; S0, K1, S1, S3, G6, C3, the bankability review, the one-off segment regeneration. Open: **Phase 4 at 14 of 26**, **S2 at 10 of ~19**, **K2** (1–2 sessions, runnable any time), **C5** (~6 sessions; its gate of ~6 landscapes is **MET**), the **Q plan clock** (next due ~2026-12). Deferred: C6. **About 28 sessions remain**
- **Open, recorded, not taken.** The `.cl-tbl` `min-width` question now has **twenty** clean data points (a three-column five-row table and a three-column nine-row table, both clean at 1400px); the generated `read-next` link is a `Classroom.html` renderer feature (`clFmt` has no link token) — **both want the same session, one that edits `Classroom.html`, plus a developer `--all` run**. **`the-800-vdc-shift`'s stale `three-chains` intro is now fourteen inserts stale**, and **row 15 is the nearest miss yet** — `string-versus-central` is the modularity argument that lesson's rack-power half rests on. **§10.6's session-4 bullet block still sits out of chronological order** — cosmetic. **NEW: a stale `lesson_ref` can make a segment due on its own** (see the netting finding above) and nobody owns clearing it — worth a developer decision rather than another session's incidental discovery
- **Render harness — the recipe, unchanged and confirmed twice this session.** Run the PROJECT region of `Classroom.gs` under Node in a `vm` context with GAS shims; shim **only** `validateSessionForData` (flat `{email, role, permissions}`, permissions an **ARRAY**) plus no-op `clProgressRead_` / `clProgressWrite_` / `clStudyNext_` / `dataAuditLog` / `auditLog`. **Route the op: `payload.action === 'guidance'` → `ctx.handleGuidanceOp_`, everything else → `ctx.handleClassroomOp_`** — through the vm context object, never bare. Pass the op as `{ parameter: payload }`. **Serve the scratch copy at `/Classroom.html`, NOT at the site root.** `_e = ''` and `AUTO_REFRESH = false`; copy `profiler-data/`, `sounds/`, `images/` and `favicon.ico` in. Seed `sessionStorage` **after** load (`Classroom_gas_session_token` · `_gas_user_email` · `_gas_user_role` · `_gas_user_permissions`), token **≥ 32 characters**; override `_gasPost` **after** load and return a **Promise**. **Point `_gasPost` at a same-origin path (`/__gas`) and fulfil it with Playwright's `page.route()`** — `connect-src` is `'self'` plus Google hosts and a cross-port fetch is refused outright. **Hide** `#auth-wall`, `#signing-out-wall`, `#auth-timers`, `#gas-layer-toggle` plus `#cl-app{display:block !important}`. Entrypoints: `clShowLesson(id)`, `clShowGuidanceDoc(id)`. Section nodes are `#cl-<sectionId>`. Launch with `executable_path` from `glob('/opt/pw-browsers/chromium-*/chrome-linux/chrome')`; `pip install playwright` is needed (the browser is pre-installed, the Python package is not). **Contributor: 9/9 sections, zero page errors, zero surviving `{{…}}` and zero literal asterisks across 42,495 characters. Analyst: 0/9 and 187 body characters**, with the server-side probe returning `ROLE_DENIED` from `handleGuidanceOp_`'s own `clRequire_` boundary
- **The two console errors in every render are environmental and pre-existing**, both `ERR_CERT_AUTHORITY_INVALID` because the sandbox proxy's CA is untrusted by Chromium. Neither is an asset and neither raises a page error

#### Recommendation for next session

- Run **Phase 4 row 15 — the `string-versus-central` lesson** on Opus 5 xhigh. Its brief is **`INTEGRATED-REMEDIATION-PLAN.md` §7.36**, written inside the v06.07r work commit with its deploy counters measured at v06.08r. It carries three things worth the attention: **a pipeline signature no Phase 4 row has seen** — the lesson belongs at §4 position 2 of a track whose `lessons[]` already holds positions 1, 3, 4 and 5, so the correct write is a mid-array insert and **P5 will fire, correctly**; **a §8 item 6 contradiction to resolve**, because §3.2's outline opens two `prose` sections back to back and row 14's precedent says check whether the corpus states the numbers before reaching for `bars`; and **a due count that can be forecast exactly for the first time** — 9 → 10 on the `lesson_ref` flip, staying at 10, with the deepening half measured as no.

**To continue:** type `run Phase 4 row 15`
