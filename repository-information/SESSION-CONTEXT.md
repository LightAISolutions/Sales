# Previous Session Context

Claude writes to this file when the developer says **"Remember Session"** — capturing enough context for a future session to pick up the train of thought quickly. This is separate from "Reminders for Developer" (REMINDERS.md), which is the developer's own notes.

> **Note on stale-context auto-reconstruction** — when a session starts and this file's `Repo version:` doesn't match the current repo version, Claude reconstructs the missing entry from CHANGELOG.md and commits it **without pushing**. The commit rides along with the session's first user-task commit on the next push. If a session ends before any user-task push happens, the reconstructed entry stays **local-only** and the next session will just re-reconstruct from CHANGELOG if still stale. This is intentional — pushing a dedicated reconstruction commit on its own would force every subsequent user push in the same session to wait for the auto-merge workflow to finish before it could push too (push-once enforcement). The reconstructed entry is a convenience hint, not load-bearing state, so the small persistence risk is a fair trade.
## Latest Session

**Date:** 2026-09-15 02:53:26 AM EST
**Repo version:** v05.84r — two pushes (v05.83r the module, v05.84r the counter refresh)
**Branch:** `claude/exciting-wozniak-zekkm3` (restarted from `origin/main` before each push)

### What was done

- **S2 session 5 — the `power-conversion-and-rack-power-silicon` landscape module** (v05.83r). `landscape-power-conversion-and-rack-power-silicon-2026-09`: the **fifteenth guidance module** and the **fifth landscape**, authored **below the `// CONTENT END` fence** at §10.6's nine section ids in its order — `who-dominates-and-on-what-basis` (prose, written **stage by stage** rather than as a league table) · `who-threatens` (prose) · `each-players-bet` (table, **twelve rows**) · `the-indicators` (table, ten rows) · `the-sellers-play` (callout) · `claims-ledger` (ledger, **60 rows**) · `what-the-record-does-not-say` (callout) · `drill` (nine cards) · `check-yourself` (five items) — four tiles, six-term local glossary, `reviewBy` **2026-11-27**, registered in `guidanceDocs_()`
- **The analysis file is 316 lines, the longest in the landscape corpus** (previous high: utilities at 308), and its **§2 — the split — was written before any other section** per the rule §7.23 established
- **The content checker's guidance-module assertion moved 13 → 14** with an extended comment (the one edit outside the fence), **one Scraper interest seed added** (`topic-power-semiconductors`), **one `--segment` regeneration** (`changed: read-next`, no pin moved), and a new **"Deepen the clone"** bullet in `CLAUDE.md`'s Session Start Checklist Always Run section
- **`Scrapergs.changelog.md` rotated** — 26 sections dated 2026-08-28 moved to the archive, all 26 SHA-resolved
- **v05.84r (the second push)** — §7.26's deployment counters refreshed from forecast to measurement **in both halves**, plus the first firing of the v05.59r poll recorded in §7.3's order-5 row and §6's S2 row

### Where we left off

Everything is merged and live. **Phase 4 is 9 of 26; S2 is 5 of ~19.** GAS live at Classroom **v01.40g** and Scraper **v02.06g**, both confirmed on Pages and in the job log. The content-checker baseline is **0 errors / 0 warnings at 39 lessons / 8 tracks / 142 gate cases**, with the module assertion now at **14**. `build-classroom-segments.py --check` reports **7 due** — none of them this session's, and all seven belong to their own S2 sessions under §10.9.

### Key decisions made

- **THE FINDING OF THE SESSION, and it corrects a model four consecutive briefs carried: a landscape whose own segment is ALREADY due cannot raise the due count.** §7.25 predicted **8 due → 9 due → 8 due**, inheriting sessions 1–4's arithmetic. The measurement was **8 → 8 → 7**. In sessions 1–4 the session's own `segment-*` lesson was *not* in the standing due set, so registering the module moved it in and the count rose; here `power-conversion-and-rack-power-silicon` was **already the first of the eight standing due**, so registration changed its `read-next` without changing its membership, and regenerating it then **cleared** it. §7.25's own prose said as much in a different paragraph ("this session clears its own residue rather than adding to a backlog") — **the two halves of the brief disagreed and the measurement settled it.** The rule now written into §7.3 and §10.6 (p): **check whether your own segment is already due before predicting; if it is, the arithmetic is N → N → N−1**
- **The Scraper-seed question answered YES against the brief's stated prior of no, and running the check properly is the whole reason.** §7.25 expected no because "existing terms already score this layer". Run the way §10.6 (i) requires — against **every term rather than every seed label**, all **390 distinct terms across both arrays** — the prior does not survive: `topic-800vdc-power` owns the architecture, `seg-power-electronics` the box, `seg-semiconductors` compute silicon, `topic-china-policy` the trade instrument, and **none scores the wide-bandgap power-semiconductor layer or the last centimetre**. `silicon carbide`, `gallium nitride`, `wide-bandgap`, `point of load` and `vertical power delivery` each returned **zero**. Those are two of the five sub-layers the segment's own definition names, one of its nine incumbents lives entirely there, two of its six buying criteria sit there, and the module's own `reviewBy` gate is a gallium date. **Re-scoped once before landing** per session 3's (i): `bus converter` and `power semiconductor` dropped as duplicates of `converter` and `semiconductor`; `MOSFET` and `IGBT` clear of all 390 but left out deliberately. **The transferable lesson: a brief's prior is a prior, not an answer**
- **The split rule fired a second time and produced a total rather than partial overlap.** `nvidia-800vdc-2026-08` covers the **architecture of the exact layer** the segment is defined as — the segment's one-sentence definition reads as that module's table of contents. The line taken: **that module owns the architecture and is deliberately vendor-blind (it says so in its own guardrails section); this one owns the parties.** §7.23's test held on **~2029, the next-generation SST launch** — a *roadmap gate* there, a *position clock* here; **4.8 MW** behaves the same way (design standard there, entry barrier here). **Six things that module owns are enumerated as deliberate omissions** in the function's header comment and §2 of the analysis file, so a later revision cannot quietly import them. With `eo14420-bulk-power-2026-08` and `china-policy-stack-2026-08` owning the manufacturing-location criterion, this is the **first landscape with three modules bearing on its segment**
- **`reviewBy` 2026-11-27 — read, not sorted, for the fourth consecutive S2 session, and a third distinct failure mode for the sort.** The roster's 20 members carry **40 `policyExposure[]` entries, 17 dated, and exactly ONE of those dates is in the future** (2027-04). The nearer gate was read out of `profile:infineon`'s policy **prose** — China's export ban on gallium suspended in favour of licensing **only until 27 November 2026** — while that entry's `effectiveDate` holds the 2023 first step. **Four candidates rejected in writing**, and one rejection is worth copying: **2026-12-24** (the DOE rulemaking) is a *genuine* gate only 27 days later, rejected **because it is already `eo14420-bulk-power-2026-08`'s own `reviewBy`** — taking it would duplicate that module's clock rather than set this one's. A gate a module watches but does not own belongs in `the-indicators`
- **THE FIRST FIRING OF THE v05.59r DEPLOY POLL, and it was needed.** Seven consecutive briefs recorded it as armed and never used. The Scraper leg failed both confirmation legs and resolved only on the second re-read: `neither leg confirmed v02.06g — re-reading in 5s (poll 1/5)` → `… in 15s (poll 2/5)` → **`Scraper deploy confirmed (POLL 2): Already up to date (v02.06g)`**. Without it that leg would have reported a failure on a deploy that had succeeded. **And it corrected my own counter forecast in the safe direction**: `Already up to date` is **not** `Updated to` — **no deployment was consumed**, so Scraper stayed at **150/200** against a 151/200 forecast. **Forecast a counter from whether the running bundle is behind, not from whether the file changed**
- **`Scrapergs.changelog.md` was already OVER its cap before this session touched anything, and the EST roll is what did it.** Three consecutive briefs called it "one short"; that figure was true only on the pre-roll count. When EST rolled to 2026-09-15 the three sections dated 2026-09-14 stopped being exempt, taking it to **52 non-exempt against a cap of 50**. **An armed changelog arms further overnight while nobody is looking — re-count from the file, never from a brief**
- **The SHA enrichment failed silently on the first attempt and only the mandatory post-rotation grep caught it.** A `\s*$` in the substitution pattern consumed each header's trailing newline, so every SHA landed on its own line and ate the blank line before `### Fixed`. Repaired and re-verified at **79 archive headers, 0 without a SHA**. That grep is documented as the most commonly skipped rotation step and it earned the description
- **C8 was honoured explicitly rather than tacitly.** The nine incumbents' leadership claims rest on non-comparable evidence — four published rankings, three trade-press-reported qualification sets, one **absence** of a ranking stated as an absence, one trade-commission determination — and the module says so rather than flattening them. A tile reads **"0 sourced rankings of the rack-shelf fight"**: three dossiers carry the Megmeet-displaces-LITEON report and **all three qualify it**, so the module states there is no sourced number two and declines to supply one

### Active context

- **Repo version** v05.84r · **CHANGELOG 86 raw / 80 non-exempt**, six sections dated 2026-09-15, oldest remaining group thirteen dated 2026-09-07. **The next session will not rotate**; re-count on the EST clock anyway
- **The EST/UTC trap did NOT fire — both clocks read 2026-09-15 again.** Still a convergence, not a fix; it diverges for any session running between 20:00 EST and midnight UTC
- **Standing changelog state:** `Scrapergs.changelog.md` has **just rotated and is no longer armed** — 27 raw / 26 non-exempt. `Classroomgs.changelog.md` 40 raw / 39 non-exempt against 50. `Profilerhtml.changelog.md` sits at 49/50
- **GAS versions:** Classroom **v01.40g**, Scraper **v02.06g**, Profiler v01.39g. **Page versions:** Classroom v01.14w, Profiler v01.90w, Scraper v01.72w — no page bump this session
- **Content-checker baseline is 0 errors / 0 warnings at 39 lessons / 8 tracks / 142 gate cases** — protect it. **Its guidance-module assertion is now 14 and a Phase 4 row must NOT touch it** — that number rises only on an S2 session
- **Deployment counters: Classroom 51/200, Scraper 150/200, MEASURED.** Classroom confirmed on the **first GET leg with no POLL line and no `?op=deploy` probe** — the eighth consecutive clean use of the §7.19 order. Scraper needed the poll (see Key decisions) and **consumed no deployment**
- **`--check` reports 7 due** — `in-hall-power`, `clean-firm-and-nuclear`, `compute-and-the-rack`, `epc-and-construction`, `aidc-developers-and-landlords`, `hyperscalers-and-ai-labs`, `neoclouds`. **None is the next session's**; each belongs to its own S2 session under §10.9
- **`check-classroom-curriculum.py --strict`** reports no structural findings, **28 stale pins** (pre-existing) and **1 item due for review** — `landscape-utilities-2026-09` at 2026-10-01, by design. **A second joins it on 28 October** when this session's module (`reviewBy` 2026-11-27) enters the 30-day horizon; both exit 0, because review dates never call `strict()`. Both belong to the quarterly guidance Routine
- **Toggles:** `START_OF_RESPONSE_BLOCK` On · `CHAT_BOOKENDS` Off · `TIMING_ESTIMATES` On · `END_OF_RESPONSE_BLOCK` On · `MULTI_SESSION_MODE` Off
- **Where the programme stands, measured 2026-09-15 at v05.84r.** Done: Phases 0, 1, 2a, 2b, 3, 5, 6; S0, K1, S1, S3, G6, C3, the bankability review, the one-off segment regeneration. Open: **Phase 4 at 9 of 26**, **S2 at 5 of ~19**, **K2** (1–2 sessions, runnable any time), **C5** (~6 sessions, gated on ~6 landscapes — **five now exist**), the **Q plan clock** (next due ~2026-12). Deferred: C6. **About 38 sessions remain**
- **Open, recorded, not taken.** The `.cl-tbl` `min-width` question now has **eleven** clean data points — this session's twelve-row × three-column bets table and sixty-row ledger both rendered clean at 1400px, adding a row-count extreme to row 9's six-column one — and should be **closed**; the generated `read-next` link is a `Classroom.html` renderer feature (`clFmt` has no link token) — **both want the same session, one that edits `Classroom.html`, plus a developer `--all` regeneration run**. `the-800-vdc-shift`'s stale `three-chains` intro stays **reassigned away from the Phase 4 queue** (§7.25); row 10 does not acquire a claim either, since it inserts into `market-access` while that lesson sits in `aidc-grid-to-chip`
- **§10.6's session-4 bullet block sits out of chronological order** (session 4's appears before session 3's). Session 5's was appended at the end rather than silently reordering another session's text — a future session can fix it deliberately
- **Render harness — the recipe, now confirmed at contributor as well as analyst.** Run the PROJECT region of `Classroom.gs` under Node with GAS shims; shim **only** `validateSessionForData` plus no-op `clProgressRead_` / `dataAuditLog` / `auditLog`, and let `handleGuidanceOp_` / `handleClassroomOp_` answer for real. Serve a scratch copy over `http://127.0.0.1` with `_e = ''` and `AUTO_REFRESH = false`; copy `profiler-data/`, `sounds/`, `images/` **and `favicon.ico`** in. Seed `sessionStorage` **after** load (`Classroom_gas_session_token` · `_gas_user_email` · `_gas_user_role` · `_gas_user_permissions`); override `_gasPost` **after** load and return a **Promise**. **Hide** `#auth-wall`, `#signing-out-wall`, `#auth-timers`, `#gas-layer-toggle` plus `#cl-app{display:block !important}` — never `remove()`. Entrypoints: `clShowLesson(id)` for lessons, `clShowGuidanceDoc(id)` for guidance. Section nodes are `#cl-<sectionId>`. `pip install playwright`; launch with `executable_path` from `glob('/opt/pw-browsers/chromium-*/chrome-linux/chrome')`. **`sess.permissions` must be an ARRAY** — `clRoleOf_` calls `.indexOf` on it. **NEW this session: the session token must be at least 32 characters** or `handleGuidanceOp_`'s own guard returns `SESSION_EXPIRED` before any shim is consulted — that is the real guard working, not a harness fault
- **The two console errors in every render are environmental and pre-existing**, both `ERR_CERT_AUTHORITY_INVALID` because the sandbox proxy's CA is untrusted by Chromium. Neither is a module asset and neither raises a page error

### Recommendation for next session

- Run **Phase 4 row 10 — the `how-a-storage-project-happens` lesson** on Opus 5 xhigh. Its brief is **`INTEGRATED-REMEDIATION-PLAN.md` §7.26**, written at the close of S2 session 5 and refreshed in the same session's handover commit so **both halves already carry measured deployment counters** — nothing in it is a forecast. It restores the §7.4 item 6 interleave at 9:5, its **sixteen-input roster is already derived and cross-checked** against both §3.5 and §7 row 10 (the arithmetic row 9's brief got one short), its due-count prediction (**7 → 8**) is stated with the netting rule this session's measurement produced, and **twelve of the guides row 9 deliberately did not pin are now this row's input list** because §3.5 assigns that material here.

**To continue:** type `run Phase 4 row 10`

## Previous Sessions

### Session — 2026-09-15 01:58:46 AM EST (v05.81r)

**Date:** 2026-09-15 01:58:46 AM EST
**Repo version:** v05.81r — two pushes (v05.80r the lesson, v05.81r the counter refresh)
**Branch:** `claude/campus-power-project-lesson-xh95ue` (restarted from `origin/main` before each push)

### What was done

- **Phase 4 row 9 — the `the-campus-as-a-power-project` lesson** (v05.80r). *Reading the Named Projects*: the **ninth mechanism lesson** and the **fifth mid-track insertion**, authored inside the content fence at §3.4's seven section ids in its order — `from-land-to-fit-out` (timeline, twelve items across three lanes) · `the-named-projects` (table, **6 × 9**) · `who-is-in-the-room` (prose) · `reading-an-announcement` (callout) · `where-it-fails` (callout) · `drill` (eight cards) · `check-yourself` (five items) — with four tiles and a six-term local glossary
- **Thirty inputs, all public**, so the stamp folds to `tracks` and the lesson is analyst-visible: nine `project:` slugs, `graph:profiler-graph`, `concepts:profiler-concepts` and **nineteen** `study:` guides. Every `study:` ref resolved by opening the file and matching its own `slug`, every pin read off the document fetched that run
- **The instructed rename was taken** — section `the-named-projects`, title dropping the count, lesson id unchanged — and the project registry was re-read on the day at **nine** projects
- Inserted at `aidc-campus` `lessons[]` **between `heat-is-the-constraint` and `where-bess-plugs-in`** (the §4 position 3), track `updated` 2026-09-13 → 2026-09-15 with a new clause in its `short`, registered in `clLessons_()`
- **v05.81r (the second push)** — §7.25's deployment counters refreshed from forecast to measurement once the workflow reported, and (in this handover) the §7.25 **prompt block** synced to match, which the v05.81r edit had missed

### Where we left off

Everything is merged and live. **Phase 4 is 9 of 26; S2 is 4 of ~19.** GAS live at Classroom **v01.39g**, Pages serving `|v01.39g|` and `|v01.14w|`, both confirmed. The content-checker baseline is **0 errors / 0 warnings at 39 lessons / 8 tracks / 142 gate cases**. `build-classroom-segments.py --check` reports **8 due** — one of which (`power-conversion-and-rack-power-silicon`) is S2 session 5's own to clear.

### Key decisions made

- **THE FINDING OF THE SESSION, and it is a new class: a shallow clone silently falsifies `git log` pins.** `--check` opened at **19 due** against the brief's measured 5, every extra one reporting `inputs moved: concepts:profiler-concepts 2026-09-13→2026-09-14` with `sections differing: none`. The cause was not the corpus: **the fresh container's clone was shallow (51 commits) and its boundary commit `75a4314` appears to introduce every file**, so `git log -1 --format=%cs -- <path>` stops there and returns the boundary's date. `git fetch --unshallow origin main` restored the true pin (`5eab070`, **2026-09-13**) and `--check` fell straight back to 5. **The due count was only the alarm.** §8 item 2 and G2 make that command the pin source for `project:` and `concepts:` refs, so authoring off the shallow clone would have written `concepts:profiler-concepts@2026-09-14` into all thirty stamps — invisible to every checker, and a false baseline for the next C2 refresh. **Unshallow before any pin, not just before a CHANGELOG rotation**
- **The brief's input count was one short and §3.4's own arithmetic settled it.** The base list carries **eight** `study:` refs and the v05.07r amendment adds **eleven**, so the roster is **nineteen guides and thirty inputs**, not eighteen and twenty-nine. All thirty were drawn on and pinned — none dropped
- **The omission record therefore moved one level down, with a single structural reason.** Nothing was fetched-and-not-pinned at the input level; what was fetched and not drawn on is a large set of *sections*, overwhelmingly **capital structure and accounting** (ABS and master trusts, converts and capped calls, REIT metrics, money-ladder bars, sponsor and GP-fund structures, collateral tables, consolidation treatments), which §3.5 assigns to rows 11 and 25. Three named carve-outs beside it: **cooling** → row 18; **turbine and reactor machinery** → rows 3 and 16; **bitcoin-mining economics** → no lesson at all
- **A recorded item was investigated and reassigned rather than deferred, and the reassignment is the useful half.** §7.24 offered `the-800-vdc-shift`'s three-inserts-stale `three-chains` intro on the grounds it "sits in Phase 4 row 9's own track". **It does not** — it is in `aidc-grid-to-chip` while this lesson went into `aidc-campus`, so this insertion moved its predecessor by nothing. Worse for the offer: reading §4 forward, **no remaining Phase 4 row acquires a claim either** (row 26 inserts at position 2 ahead of `bridge-power`; rows 20 and 17 append after it). The boundary was last moved by rows 7 and 8 and will not move again, so it belongs to whichever session opens that lesson for its own reason. §7.25 records it that way so it stops being re-offered
- **The due-count prediction was measured and held exactly: 5 due → 8 due.** Newly due: `clean-firm-and-nuclear`, `epc-and-construction`, `hyperscalers-and-ai-labs`; `aidc-developers-and-landlords` and `neoclouds` gained a second reason. Seven of the eight differ in `read-next` alone — row 8's bound (|`READ_NEXT`| + prior residue) held for a second consecutive row — and the predicted `PLANNED_TITLES` mismatch appeared as described
- **`reviewBy` took the six-month default (2027-03-15) with three candidates rejected in writing**: a lease-closing condition (30 September 2026), a lender's covenant date (November 2026), and two project delivery schedules — the last being the class the lesson's own claim predicts will slip, so a date moving *confirms* the claim rather than contradicting it
- **C8 was honoured explicitly rather than tacitly.** A tile reads "0 league tables" and the table's `note` argues these developers solve *different constraints* rather than the same one better or worse — each picked the fastest lawful route to firm megawatts, and the routes differ because the constraint differs by site
- **The 6 × 9 table is the widest any mechanism lesson carries by column count** and rendered clean at 1400px — a **tenth** data point against the open `.cl-tbl` question and the **first at six columns**, which excludes column count about as firmly as evidence can
- **Two pushes, and the second exposed an editing trap worth remembering: refreshing a brief's body paragraph does not refresh its prompt block.** v05.81r corrected §7.25's counters in the prose and left the quoted prompt — the half that actually gets pasted — still saying "EXPECTED". Caught and fixed in this handover. **When refreshing any §7.x brief, edit both halves**

### Active context

- **Repo version** v05.81r · **CHANGELOG 83 raw / 80 non-exempt**, three sections dated 2026-09-15, oldest remaining group thirteen dated 2026-09-07. **The next session will not rotate**; re-count on the EST clock anyway
- **The EST/UTC trap did NOT fire — for the first time in four sessions.** Both clocks read 2026-09-15. **This is a convergence, not a fix**: it diverges again for any session running between 20:00 EST and midnight UTC
- **Standing changelog traps:** `Scrapergs.changelog.md` at **52 raw / 49 non-exempt** is **still armed** and stayed one short for a third session running — and unlike a Phase 4 row, **an S2 session can fire it** if it adds a seed and lands on a later EST day (oldest group: twenty-six dated 2026-08-28). `Profilerhtml.changelog.md` sits at 49/50. `Classroomgs.changelog.md` 39 raw / 38 non-exempt against 50
- **GAS versions:** Classroom **v01.39g**, Scraper v02.05g, Profiler v01.39g. **Page versions:** Classroom **v01.14w**, Profiler v01.90w, Scraper v01.72w
- **Content-checker baseline is 0 errors / 0 warnings at 39 lessons / 8 tracks / 142 gate cases** — protect it. Its **guidance-module assertion is 13 and S2 session 5 must move it to 14**, which is that session's one edit outside the content fence and the one that stops it dead if met cold
- **Deployment counters:** Classroom **50/200** (measured — `Classroom deploy confirmed (GET): Updated to v01.39g (deployment 50) | 50/200`, first GET leg, no `POLL` line, no probe issued), Scraper 150/200. **Seventh consecutive clean use of the §7.19 order.** The v05.59r poll is armed and has still never been needed
- **`--check` reports 8 due** — `power-conversion-and-rack-power-silicon`, `in-hall-power`, `clean-firm-and-nuclear`, `compute-and-the-rack`, `epc-and-construction`, `aidc-developers-and-landlords`, `hyperscalers-and-ai-labs`, `neoclouds`. The first is S2 session 5's own; the other seven are their own sessions' under §10.9
- **`check-classroom-curriculum.py --strict`** reports no structural findings, **28 stale pins** (pre-existing; row 9 added none) and **1 item due for review** — `landscape-utilities-2026-09` at 2026-10-01, by design. **From 2 October it reads "⚠ review due" rather than "due soon"** and still exits 0; it belongs to the quarterly guidance Routine
- **S2 session 5 faces the split rule for the second time, and the overlap is sharper than session 4's.** `nvidia-800vdc-2026-08` covers the architecture of the very layer `power-conversion-and-rack-power-silicon` is defined as — the MV-to-800 VDC sidecar, the power shelf, the bus converter. `eo14420-bulk-power-2026-08` and `china-policy-stack-2026-08` touch its manufacturing-location buying criterion. **The split must be written before the module**, per §7.23
- **Segment measured 2026-09-15:** `power-conversion-and-rack-power-silicon` holds **20 members — 9 incumbent · 3 challenger · 8 adjacent**. Re-measure on the day
- **Toggles:** `START_OF_RESPONSE_BLOCK` On · `CHAT_BOOKENDS` Off · `TIMING_ESTIMATES` On · `END_OF_RESPONSE_BLOCK` On · `MULTI_SESSION_MODE` Off
- **Where the programme stands, measured 2026-09-15 at v05.81r.** Done: Phases 0, 1, 2a, 2b, 3, 5, 6; S0, K1, S1, S3, G6, C3, the bankability review, the one-off segment regeneration. Open: **Phase 4 at 9 of 26**, **S2 at 4 of ~19**, **K2** (1–2 sessions, runnable any time), **C5** (~6 sessions, gated on ~6 landscapes — **four now exist**), the **Q plan clock** (next due ~2026-12). Deferred: C6. **About 39 sessions remain**
- **Open, recorded, not taken.** The `.cl-tbl` `min-width` question has **ten** clean data points including its first at six columns and should be **closed**; the generated `read-next` link is a `Classroom.html` renderer feature (`clFmt` has no link token) — **both want the same session, one that edits `Classroom.html`, plus a developer `--all` regeneration run**. `the-800-vdc-shift`'s stale `three-chains` intro is **reassigned away from the Phase 4 queue entirely** (see Key decisions)
- **Render harness — the recipe, confirmed again at analyst.** Run the PROJECT region of `Classroom.gs` (lines 329–48467) under Node with GAS shims; shim **only** `validateSessionForData` plus no-op `clProgressRead_` / `dataAuditLog` / `auditLog`, and let `handleClassroomOp_` answer for real. Serve a scratch copy over `http://127.0.0.1` with `_e = ''` and `AUTO_REFRESH = false`; copy `profiler-data/`, `sounds/`, `images/` **and `favicon.ico`** in. Seed `sessionStorage` **after** load (`Classroom_gas_session_token` · `_gas_user_email` · `_gas_user_role` · `_gas_user_permissions`); override `_gasPost` **after** load and return a **Promise**. **Hide** `#auth-wall`, `#signing-out-wall`, `#auth-timers`, `#gas-layer-toggle` plus `#cl-app{display:block !important}` — never `remove()`. Entrypoints: `clShowLesson(id)` for lessons, `clShowGuidanceDoc(id)` for guidance. Section nodes are `#cl-<sectionId>`. `pip install playwright`; launch with `executable_path` from `glob('/opt/pw-browsers/chromium-*/chrome-linux/chrome')`, never run `playwright install`. **`sess.permissions` must be an ARRAY** — `clRoleOf_` calls `.indexOf` on it
- **The two console errors in every render are environmental and pre-existing**, identified by URL: `accounts.google.com/gsi/client` and an external logo, both `ERR_CERT_AUTHORITY_INVALID` because the sandbox proxy's CA is untrusted by Chromium. Neither is a lesson asset and neither raises a page error

### Recommendation for next session

- Run **S2 session 5 — the `power-conversion-and-rack-power-silicon` landscape module** on Opus 5 xhigh. Its brief is **`INTEGRATED-REMEDIATION-PLAN.md` §7.25**, written at the close of Phase 4 row 9 and refreshed twice since — once at v05.81r for the deployment counters, and again in this handover to sync its **prompt block**, which that refresh had missed. It restores the §7.4 item 6 interleave at 9:4, the segment is measured at **20 members (9 · 3 · 8)**, it **clears its own `segment-*` residue** from the standing 8-due, it is the session that must move the content checker's guidance-module assertion **13 → 14**, and it faces the **sharpest module split yet** — `nvidia-800vdc-2026-08` covers the architecture of the exact layer this segment is defined as, so the split has to be written before the module.

**To continue:** type `run S2 session 5`
