# Previous Session Context

Claude writes to this file when the developer says **"Remember Session"** — capturing enough context for a future session to pick up the train of thought quickly. This is separate from "Reminders for Developer" (REMINDERS.md), which is the developer's own notes.

> **Note on stale-context auto-reconstruction** — when a session starts and this file's `Repo version:` doesn't match the current repo version, Claude reconstructs the missing entry from CHANGELOG.md and commits it **without pushing**. The commit rides along with the session's first user-task commit on the next push. If a session ends before any user-task push happens, the reconstructed entry stays **local-only** and the next session will just re-reconstruct from CHANGELOG if still stale. This is intentional — pushing a dedicated reconstruction commit on its own would force every subsequent user push in the same session to wait for the auto-merge workflow to finish before it could push too (push-once enforcement). The reconstructed entry is a convenience hint, not load-bearing state, so the small persistence risk is a fair trade.

## Latest Session

**Date:** 2026-09-07 02:10 AM EST
**Repo version:** v05.00r — **one** push commit this session (`a3427d1`), merged to `main` as part of `bbeab60`, plus this housekeeping commit
**Branch:** `claude/guide-backfill-session-5-zojq8v` (restarted from `origin/main` after the merge — the remote branch was already deleted by the workflow, which is what freed push-once for this second push)
**Model:** Opus 5 xhigh — **guide-backfill session 5 of 6**, the four IPP portfolio owners. Guides only. **This closed the §8 backfill ledger.**

### What was done

**v05.00r — four schema-v2 study guides plus lesson plans, no dossier touched.** `plus-power` (11 sections), `nextera-energy-resources` (11), `lightsource-bp` (11), `terra-gen` (11) — **44 sections**. No `profileVersion` moved, nothing archived, `archive-index.json` untouched, `profiler-companies.json` never opened, and `profiler-graph.json` came back **byte-identical** — asserted against the specific expected digest rather than against "same as before" (sha256 `563ae281f00ce164075ded5c21315c67769bbf50bcb8a8d046c80b882c2168d6`, now unchanged across **four** consecutive guides-only sessions). **The corpus reaches 154 companies and 154 guides — every dossier now has one, and §8 has zero outstanding rows in both its tables.**

**The brief's premise about the collision field was too narrow, and finding that out changed the syllabus before any authoring began.** The brief named five session-1 guides to read; reading them was necessary but not sufficient. A scan of **all 150** existing guides found **fourteen more owner-side guides** already holding the ground the brief proposed for its portfolio-financing topic — `engie-north-america` owns *Capital recycling: how you fund the next battery when the last one earns less*, `clearway-energy` owns the yieldco, CAFD and tax-equity/HLBV, `recurrent-energy`, `spearmint-energy`, `strata-clean-energy`, `available-power` and `aes-clean-energy` all own develop-and-sell in some form, `aypa-power` and `gridstor` own the financing stack and the sponsor exit, `invenergy` owns who-owns-a-private-developer, `esvolta` owns the mixed book, and **`rwe-clean-energy` owns repowering**. So of the brief's five candidate topics, **one was already shipped and one partly shipped**. Portfolio financing was dropped as a standalone topic and its single genuinely unclaimed part — the regulated-utility affiliate — was merged into the scale guide; repowering was dropped entirely. **The lesson generalises: reading the five guides a brief names is not the same as checking the corpus.**

**The organising claim of the course: at portfolio scale the unit of production is a process, and every practice is a way of managing an obligation that outlives every contract that created it.** Each guide owns one consequence:
- `plus-power` — *the process, not the plant*: a **reference design as frozen decisions**, with the rule that separates what a fleet may freeze from what it may not — *decisions freeze, measurements do not*; why repetition is worth more in **permitting, lending and insurance** than in procurement, because those examine an argument rather than buy a thing; the same block on five networks (island grid-forming with black start, weak-point damping and operator model validation, winter duty, desert derating, dense-urban fire footprint) and the observation that **none of it is a different battery**; the serial build curve, why it flattens well above zero and why it **resets at a border**; the owner's build-organisation decision; **concurrency rather than capital** as the growth cap, with warranty clocks that start at delivery so a finished unenergised plant spends guarantee life it never earns against; a pipeline as options whose discipline is killing them early; and the **replicated defect**, deliberately distinguished from the contracting guide's supplier-diversity argument because one is about a company and the other about a fault
- `nextera-energy-resources` — *the factory's calendar*: built on the observation that **two rules the first course states as laws are facts about being small** — your counterparty is the integrator, and you cannot use your own tax credit. Self-integration with access rather than margin as the motive; the **six seams**; forward booking as the purchase of a calendar, converting procurement into a position with inventory and obsolescence cost, and the odd result that a long owner sells to its competitors; the long-lead list, where the slowest items are ordinary grid equipment contested by every other connection; the **regulated affiliate**, what it buys and the three costs usually omitted (enforced separation, inherited regulatory risk, competing for capital against a rate base); the **tax-appetite inversion**; one battery under three owners, explicitly distinguished from the dispatch guide's version because that one is skill and this one is discount rate
- `lightsource-bp` — *what crosses a border*: the corpus's **first international guide** and first non-US project developer. Six support instruments read as what each fixes and leaves floating, with the consequence that **policy design rather than engineering sets much of what a country's electricity costs**; the national auction as an institution, with prequalification spend committed before the outcome, delivery security, **indexation often worth more than the headline price**, and the winner's curse as a property of the format; the currency problem, asymmetric in time and unhedgeable at tenor; what a network rulebook decides, including loss factors that move revenue administratively; consent risk as **variance rather than difficulty**; the portable/non-portable split — capital-like against time-like, which is why entry is usually an acquisition; and diversification as **effective against politics, weak against finance and supply chains, useless against a design fault**, including that a portfolio of fixed-price contracts is *more* rate-exposed than a merchant one because a strike price is a bond
- `terra-gen` — *the twenty years after the ribbon*, and the **close of the buyer-side course**: asset management as commercial custody, the layer that scales worst because a contractor can be paid to perform an obligation but never made liable for one; **four things called availability**, with the excluded-event list as where the negotiation lives and a fleet average as a reporting convenience rather than exposure; **enforcing** a guarantee — a demonstration costing a day out of the market on a schedule the contract rather than the equipment sets, records without which the supplier's first defence stands, notice deadlines as the commonest way a sound claim is lost, and a remedy ladder whose escalation mechanism beats its rungs; augmentation as a **date** designed in years ahead; the operating bill for a mixed-vendor fleet; telemetry from the owner's side, where twenty-nine normal sites turn an anomaly into **evidence rather than an argument**; and the closing claim that every other party sells something with a term, so the owner holds what is left — which is why assets change hands after COD, and is usually the system working

**Seven concepts registered** (1,203 → 1,210), all from `lightsource-bp` — `bid-bond`, `contract-for-difference`, `corporate-ppa`, `feed-in-premium`, `feed-in-tariff`, `green-certificate`, `marginal-loss-factor`. The other three guides needed **zero** new concepts, which measures how deep the registry now is on project-finance, contracting and grid vocabulary: 44 of the 51 markers used already resolved.

**The CHANGELOG did not rotate, and the brief was wrong that it would.** Details in Key decisions below — this is the single most important thing for the next session to read before touching the changelog.

**README tree audit exact on all three listings** — 312 profiler-data files, 106 study-prep directories, 131 study-prep files, nothing listed that is not on disk, no drift beyond this session's own four additions in each.

**Two post-push research turns answered plan questions and produced no commits.** The first established that **X3 is next and now unblocked**; the second worked the model question for X3 and Phase D and found two things the plan does not record (below).

### Where we left off

v05.00r merged (`bbeab60`). All six checkers exit 0; roster/calendar bijection clean at 154; 154 guides and 1,210 concepts validate with zero errors and zero warnings. The developer asked for a paste-in prompt for **X3** and this context write. **The next unit of work is X3 — the Phase X integrity close-out — on Opus 5 xhigh.**

### Key decisions and findings

- **The CHANGELOG rotation did NOT fire, and the reason will recur every time a session pushes twice in one EST day.** The brief asserted rotation was "unavoidable" from 99 sections plus one reaching the 100 trigger. The counter was verified against the real count as instructed (`grep -c '^## \[v[0-9]'` returned 99, matching `Sections: 99/100`) — but that arithmetic omits the **current-day exemption**. `v04.99r` is dated 2026-09-07, the same EST day as this push, so the file now holds **100 sections of which two are today's, leaving 98 non-exempt**. Five of the six worked scenarios in `CHANGELOG-archive.md` state the threshold against the non-exempt count; Scenario D is decisive — *"97 < 100 → no rotation needed **despite 102 total**"*.
- **A second, residual contradiction was found in that same document — recorded, not fixed.** The v04.98r reconciliation harmonised the *threshold* (99-or-fewer stops, exactly 100 rotates) but left the *basis* inconsistent: **steps 1–2 count all sections** and **step 4** then mandates at least one rotation once that check trips, while **Scenarios A and D** say a total at or above 100 does not trigger when non-exempt is below it. Here the two readings disagreed by thirteen sections. The scenarios were followed — five consistent worked examples against one shorthand, and the only place the exemption's interaction with the threshold is actually exercised. **The failure modes are opposite:** v04.98r's bug *skipped* rotations, this one fires them *early*. Suggested fix, not done: change step 1 to count **non-exempt** sections, and align step 2 and the quick rule. It was left alone because editing rotation logic on a guides push is scope widening.
- **Rotation is genuinely due on the next push and the counter alone will not say so.** It now reads `Sections: 100/100`, which looks like a skipped rotation. It is not. Next push, on a later EST day: total 101, one exempt, **100 non-exempt — at the trigger**. Oldest whole date group is **thirteen sections dated 2026-09-01** (`v04.01r`–`v04.13r`). `git fetch --unshallow` **must** run before any SHA lookup; it was run this session (51 → 1,005 commits) so the rotation would have enriched correctly had it fired, but a fresh clone starts shallow again.
- **X3's biggest stated component is already empty, and this is the finding most likely to be missed.** `check-profiler-crossrefs.py` over the completed 154-company corpus reports **0 candidates**, 8 suppressed, 10 accept entries. §9.3's "re-run over the completed corpus and adjudicate what it surfaces" therefore has nothing to adjudicate — *except* the **21 scopes it never examined**, over its 900-character cap, which it prints with its own caveat that "a clean run does not cover these." That is ~25,000 characters of the corpus's longest prose and is exactly the categorical-mischaracterisation class §9.2 says no comparator can reach. **A session briefed from §9.3's wording alone will re-run a clean checker, see zero, and conclude the component is done.**
- **The report pins are two different jobs, not one.** 38 pins, **mean gap 2.9 versions, max 6** (`vertiv` v3→v9, `schneider-electric` v3→v9, `eaton` v3→v8). **17 pins sit at a 1–2 version gap** — comparison of two records the repo holds, which is §2's definition of Medium work. **21 sit at 3–6** — six revisions can restructure a section, split a figure or turn "contracted" into "pipeline", so re-verifying those is reading.
- **§2 and §9 look like they disagree about X3's model, and both are half-right.** §2 says "§9's integrity items are assigned to Medium" and names report re-pinning as a Medium example; §9.3 assigns X3 to **Opus 5 xhigh** and — unlike its X1 and X2 rows — gives **no reason**. Resolution: X3 is two jobs stapled together. Checkers, the §6 register rows and the 17 short-gap pins are Medium-shaped; the 21 over-cap scopes and the 21 wide-gap pins are Opus-shaped, and they are the ones that decide whether X3 meets its "done when". **Recommendation given: run it whole on Opus**, because the reading half is what the session is for and a handoff mid-pass buys a Medium session's savings at the cost of continuity. **§9.3's X3 row should gain a "why Opus" note** — its absence is what makes the two sections look contradictory.
- **Fable 5.1 High has no role in X3 or Phase D.** §2 assigns High to private or opaque subjects and to utilities — inference from a thin public record. Every input to X3 and Phase D is a record this repo already holds.
- **Phase D's model is not a judgment call.** `CLASSROOM-CURRICULUM-PLAN.md` §"Model and session shape" cites the standing decision (SESSION-CONTEXT, 2026-09-02): *planning on Fable 5.1, **authoring on Opus 5***, **one lesson per session**, because "a session that authors two is a session that copies a section between them."
- **X3 step 4 and Phase D step 1 are the same action** — both re-run and date the `CLASSROOM-CURRICULUM-PLAN.md` §6 register checks, and X3's "done when" includes the fresh date. The recommendation given was that X3 absorbs it, so Phase D starts at authoring.
- **Two abbreviations were deliberately left unregistered, with the reason recorded so it is not re-derived.** Concept matching is exact-lowercase, so registering `CfD` would also claim **CFD** (computational fluid dynamics) in a corpus holding `aligned`, `coolit` and `canadian-solar`; and `FIT` would claim **failures in time**, the reliability unit, in a corpus holding `infineon`, `vicor` and `delta-electronics`. Neither is used by any guide today. Whoever needs one should take it knowingly.
- **`bars` item labels are marker-safe, verified by reading the renderer rather than by precedent.** `gdBars` (`Profiler.html:7238`) passes both `label` and `sub` through `gdFmt` and uses `gdPlain` for the bar's hover title, so a term renders its tooltip and is not a `<button>` — the trap that disqualifies quiz choices does not apply. Only one prior guide (`infineon`) had used them.
- **The v04.99r quiz rule was encoded correctly in this session's authoring helper** — markers refused in `title`, `read`, `timeline.lanes.*` and quiz **choices**, permitted in quiz `q` and `why`. All four guides passed first time.
- **A second push in one session remains legitimate once the first has merged**, and this session used it for the context write: `bbeab60` carried v05.00r into `main` and the workflow deleted the remote branch, so the branch was restarted from `origin/main` before any edit.

### Active context

- **Branch:** `claude/guide-backfill-session-5-zojq8v` · **repo version:** v05.00r · **Profiler page:** v01.83w (unchanged since v04.85r — every push since has been data-only)
- **Corpus:** 154 companies · 154 profiles · **154 study guides — the backfill ledger is closed** · 374 archived dossier versions · **1,210 concepts** · 106 study-prep directories · 106 lesson plans · 131 study-prep files · 312 profiler-data files
- **Toggles:** `START_OF_RESPONSE_BLOCK` On · `CHAT_BOOKENDS` Off · `TIMING_ESTIMATES` On · `END_OF_RESPONSE_BLOCK` On · `MULTI_SESSION_MODE` Off · `IS_TEMPLATE_REPO` No · `TEMPLATE_DEPLOY` Off
- **CHANGELOG: 100/100 and rotation is DUE on the next push** — see Key decisions for why it did not fire this time and why the counter alone is misleading. Budget ~10 extra minutes and run `git fetch --unshallow` first.
- **§8 is fully closed** — both tables, zero outstanding rows. **§9.4's X3 row is the only item left before Phase D**, and it still reads "Not started".
- **Phase X state carried into X3:** `check-profiler-crossrefs.py` **0 candidates / 21 scopes not examined**; `check-profiler-reports.py` **0 errors / 38 aged pins**; `check-profiler-relationships.py` **0 findings**, accept list stands at **10** and did not grow.
- **G6 is independent of everything above and is blocked on the developer, not a model** — it wants a guidance module, which needs a developer-supplied industry document run through the `industry guidance:` command. It can be handed over at any point without disturbing the run order.
- **No dossier errors were found to hand forward.** The four profiles were read for `summary` and `productsAndServices` only; nothing read contradicted itself or the corpus.
- **Standing, unassigned:** the README *archive* listing is still well behind (the non-archive listing is exact at 312/106/131); `archive/nvidia.profile.v2.json` is missing and cannot be reconstructed; OSHA IMIS and SEC EDGAR are both network-blocked from this environment. Pre-existing README tree cosmetics recorded and left: ordering breaks in the `study-prep/` listing, a `rolls-royce-power-systems` plan nested under `megmeet/`, and raw `&` in existing `Samsung C&T` entries. The X3-versus-Phase-D-step-1 overlap and the missing "why Opus" note on §9.3's X3 row are both new to this list.
- **Closed off this session and removed from the standing list:** the §8 guide-backfill ledger itself, which was the largest standing item in the program.

### Recommendation for next session

- Run **X3 — the Phase X integrity close-out — whole, on Opus 5 xhigh**, in the order checkers → the 21 over-cap crossref scopes → all 38 report pins → the `CLASSROOM-CURRICULUM-PLAN.md` §6 register re-check and dating, briefed explicitly on the over-cap list rather than on §9.3's wording, because a session that reads §9.3 alone will re-run a clean crossrefs checker and wrongly conclude that component is finished. It is the last item before Phase D.

**To continue:** type `run X3`

## Previous Sessions

### Session — Guide-backfill session 4, the four EPC study guides (Opus 5 xhigh)

**Date:** 2026-09-07 12:58 AM EST
**Repo version:** v04.99r — **two** push commits this session (`20a8889` v04.98r, merged to `main` as `9302d97`; `9d038b5` v04.99r, pushed and awaiting merge), plus this housekeeping commit
**Branch:** `claude/guide-backfill-session-4-epc-fsazns` (restarted from `origin/main` after the v04.98r merge — the workflow had already deleted the remote branch, which is what freed push-once for the second push)
**Model:** Opus 5 xhigh — **guide-backfill session 4 of 6**, the four EPCs. Guides only.

#### What was done

**v04.98r — four schema-v2 study guides plus lesson plans, no dossier touched.** `blattner` (11 sections), `mastec` (10), `solv-energy` (11), `samsung-ct` (11) — 43 sections. No `profileVersion` moved, nothing archived, `archive-index.json` untouched, `profiler-companies.json` never opened, and `profiler-graph.json` came back **byte-identical** after the rebuild (sha256 `563ae281f00ce164075ded5c21315c67769bbf50bcb8a8d046c80b882c2168d6`, unchanged since v04.96r) and correctly absent from the diff.

**The four were designed as one non-overlapping course against THREE shipped courses rather than one**, which made the design problem finding the material none of them owns. The answer came from the buyer-side course itself: its schedule chart compresses this entire subject into **a single bar of roughly fourteen months**, with a note saying a slipped completion date is usually a queue problem rather than a construction one. True from outside; from inside, construction is the one activity with a fixed price, a fixed date and money payable per day of lateness. **This course is inside that bar.** Organising claim: **an EPC does not sell construction, it sells a date and a price**, and each guide owns one consequence:
- `blattner` — *the price of a date*: the course entry point; a fixed price as the purchase of **certainty** rather than labour, so ambiguity costs more than difficulty and the change-order count measures design completeness at award; three delivery models and why storage separates the battery purchase for commercial not technical reasons; how work is won through standing relationships rather than tenders, which makes market share invisible; **scope seams as the real failure mode** with three breakages — gap, overlap, timing mismatch; critical path, float and the fact that it is consumed by whoever uses it first; submittal approval rather than the purchase order releasing a factory slot; weather as a priced allowance and the non-linearity that makes a two-month slip cost six; and the accept-or-accelerate binary
- `mastec` — *the people who build it*: craft labour as **the one input that cannot be procured**, so a shortage is a **schedule** risk not a cost line — the hours are not available at the higher price either; the trades, with the scarcest needed latest so shortage and compression arrive together by construction; travelling crews, hiring models, apprentice hours as a compliance input, and **supervision ratio as the true cap on a ramp**; unit rates, the productivity factor as the earliest honest signal, and the five destroyers; then **safety as a production system** — the recordable-rate formula, its **instability at small scale**, the insurance modifier that prices a past several years old, rising near-miss reports as good news that looks bad, and a low injury rate being compatible with a lethal exposure profile
- `solv-energy` — *the ground, the wire and the handover*: built on the observation that **almost every commissioning defect was created during installation**, so useful checks happen when work becomes invisible; borings as point samples and the differing-site-conditions clause explained through its counter-intuitive purpose (it exists to make bids **cheaper**, so thinning the geotech buys a claim); three foundation methods chosen by the ground rather than by preference; torque, compression dies, pulling tension and bend radius as operations that look identical done right or wrong; what first energisation actually finds; the commissioning sequence read **by lane**, showing the contractor does not employ most of the parties in its last third; two completions; and the handover records a twenty-year guarantee is measured against
- `samsung-ct` — *who can hold the risk*: the closing guide, and it closes **all four courses**. A contractor funds the work before it is funded for it, so the balance sheet is production equipment; retainage as interest-free profit held to the milestone with least schedule pressure; roughly two and a half months of turnover financed at all times as a percentage of **revenue** not margin, so growth consumes cash faster than profit generates it; change against claim and notice as the commonest way entitlement is lost; caps as **where the risk transfer stops**; six security instruments; and the payoff that a surety line makes **capacity a stock rather than a flow** — the same conclusion the labour guide reaches from an entirely independent constraint

**28 concepts registered** (1,175 → 1,203) from **216 candidates probed**, 55 already present. Contracting and schedule (`lump-sum-turnkey`, `multi-contract`, `owner-furnished-equipment`, `master-service-agreement`, `limited-notice-to-proceed`, `constructability`, `baseline-schedule`, `float`, `concurrent-delay`, `extension-of-time`, `schedule-compression`, `weather-day`, `force-majeure`); people and safety (`apprenticeship`, `prevailing-wage`, `unit-rate`, `productivity-factor`, `trade-stacking`, `recordable-incident`, `total-recordable-incident-rate`, `dart-rate`, `experience-modification-rate`); the site (`differing-site-conditions`, `soil-resistivity`, `pile-refusal`, `punch-list`, `as-built`); the money (`retainage`).

**v04.99r — the three-session-old quiz-marker question was settled, and the answer reversed the assumption.** Markers in quiz items do **not** render as literal braces: `gdQuiz` (`Profiler.html:7274`) passes the question, **every choice** and `why` through `gdFmt` (`:6714`), and `gdBindTermTips` (`:7307`) binds by delegation on the shell (`:7098`). The blanket "quiz items must be marker-free" convention was stricter than the renderer requires, and the real scope was three markers, not a class of breakage.

**The genuine trap is narrower and was measured, not inferred.** In headless Chromium: hovering a term in an enabled choice works; **clicking** it logs `ANSWERED` before `TIPCLICK`, because the span is a child of the button — so on touch there is no way to read the definition without committing that answer; and after `gdQuiz` disables the button, hovering returns **nothing**, because disabled controls suppress mouse events on descendants. `scripts/check-profiler-study.py` now errors on `{{…}}` inside `quiz items[].c[]` and deliberately leaves `q` and `why` unguarded. Verified both directions — exactly **one** error across 150 guides before the fix, zero after. `blackstone.study.json` moved `{{fee-earning AUM}}` from the choice into that item's `why`; `macquarie` was not modified.

**The CHANGELOG rotation-rule contradiction was reconciled at v04.98r.** The quick rule said "100 triggers" while step 2 said "stop at 100 or fewer". Precedent runs the other way consistently (v04.83r and v04.97r both rotated at exactly 100/100) and the counter's own notation reads as at-capacity, so the whole document was harmonised on **rotate-at-100** — header prose, quick rule, step 2 (with the reason recorded inline), step 3, step 7, and Scenarios A/C/D/E/F re-worked so each still teaches its original point. Two stale `Pre-Commit #7` cross-references corrected to `[PC-CHANGELOG] #6`. Verbatim prompt blockquotes in the archive were left untouched.

**README tree audit found no orphans** — the only gaps were this session's own four additions, in each of the three listings.

#### Where we left off

v04.98r merged (`9302d97`); v04.99r pushed and awaiting the auto-merge workflow. All six checkers exit 0; roster/calendar bijection clean at 154; **150 guides and 1,203 concepts** validate with zero errors and zero warnings. The developer asked for a paste-in prompt for backfill session 5 and this context write.

#### Key decisions and findings

- **The plural-aware collision sweep is now the settled method and it found nothing new to fix.** A normalised map over 1,175 concepts (3,296 distinct keys from 4,600 terms, slugs and aliases) folded hyphens, spaces, punctuation and case, then tested each candidate against singular/plural variants of its last word. **All 53 proposed terms and aliases came back free.** The sweep did surface **nine pre-existing plural-aware collisions in the registry itself** — `cp`/`cps` between `coincident-peak` and `conditions-precedent`, `ld`/`lds` between `liquidated-damages` and `long-duration-storage`, plus the `certificate-of-need`, `kw-month` and `power-first` pairs. The checker matches exact-lowercase so none is an error today; **recorded, not changed.**
- **Reading existing definitions before reuse mattered more this session than in any prior one — five traps.** `turnkey` is registered with a **manufacturing** meaning (a builder buying its own materials, the opposite of consignment), which would have contradicted every sentence around it, so `lump-sum-turnkey` was registered instead. `letter of credit` is an alias of `credit-support`, defined around a utility demanding collateral from a new customer. `duty cycle` is an alias of `pwm`. `capacity test` is defined for stationary-battery load-bank discharge rather than a contractual demonstration. `learning curve`'s definition argues for factories **over** on-site repetition, the opposite of a construction productivity curve. The last two were handled by writing prose without a marker rather than by registering near-duplicates.
- **The checker-as-ledger method was inverted this session, and the inversion is better.** Sessions 1–3 drafted definitions and discarded the unused ones (19 dropped in session 3). This session wrote **no definition until the checker had proved the term was referenced**, so the 24-term ledger and the 28 registered entries differ only by four deliberate additions — `trade stacking`, `punch list`, `retention` and `pile refusal`, each marked where one guide uses a term another guide defines, which is what a shared registry is for. **Recommend keeping this order.**
- **Marker placement must be asserted programmatically, and the assertion itself needs to encode the right rule.** A shared helper refused to write any guide with `{{…}}` in `title`, `read` or `timeline.lanes.*`; all four passed first time. But the same helper encoded the **old** blanket quiz rule and therefore rejected the *corrected* `blackstone` file at v04.99r — the legitimate `why` marker tripped it. That is the cleanest possible demonstration that the blanket convention was wrong: a tool built from it produces false positives. Narrowed to choices before the edit was applied.
- **The new quiz rule, stated for future authoring:** markers are **fine and now encouraged** in quiz `q` and `why`; they are **banned in `c[]` (choice text)** and the checker enforces it. No existing guide was rewritten to add markers — the rule is a guard, not a prescription — and the four EPC guides keep all quiz items marker-free, which is conservative but valid.
- **A second push in one session is legitimate when the first has already merged**, and this session used that path deliberately: the workflow had merged v04.98r and deleted the remote branch, so the branch was restarted from `origin/main` before any v04.99r edit, and push-once was clean. Worth remembering rather than assuming one push per session is a hard cap.
- **Diff proportionality held on both pushes.** v04.98r: concepts 214/0 (pure insertion, 28 alphabetical entries, no existing entry moved — asserted by comparing the serialised before-list against the filtered after-list), README 13/1, CHANGELOG 64/1, archive 14/14, rules 2/2, plan 4/4, version 1/1, plus 8 new files. v04.99r: checker 21/0, blackstone 3/3, CHANGELOG 24/1, context 1/1, README 1/1, version 1/1. `profiler-graph.json` correctly absent from both.
- **The graph sha256 has now been unchanged across three consecutive guides-only sessions**, so a future session can assert the specific expected hash rather than only "identical to before".
- **A recorded-figure correction.** Session 3's context said "110 lesson plans"; the measured figure was 110 **markdown files**, of which 98 were lesson plans and 12 were other artefacts. Neither listing was ever wrong — the label was.

#### Active context

- **Branch:** `claude/guide-backfill-session-4-epc-fsazns` · **repo version:** v04.99r · **Profiler page:** v01.83w (unchanged since v04.85r — every push since has been data-only)
- **Corpus:** 154 companies · 154 profiles · **150 study guides (4 backfills outstanding)** · 374 archived dossier versions · **1,203 concepts (3,489 term+alias keys)** · 102 study-prep directories · 102 lesson plans · 127 study-prep files · 308 profiler-data files
- **Toggles:** `START_OF_RESPONSE_BLOCK` On · `CHAT_BOOKENDS` Off · `TIMING_ESTIMATES` On · `END_OF_RESPONSE_BLOCK` On · `MULTI_SESSION_MODE` Off · `IS_TEMPLATE_REPO` No · `TEMPLATE_DEPLOY` Off
- **CHANGELOG: 99/100**, counter verified against the real section count both times it moved. **Rotation FIRES on the next push — it is now unavoidable, not merely likely**, and it will be the first live exercise of the rotate-at-100 threshold reconciled at v04.98r. The oldest whole date group is **thirteen sections dated 2026-09-01** (`v04.01r` through `v04.13r`). The working clone arrives **shallow at ~50 commits**, and `git fetch --unshallow` must run **before any SHA lookup** or every moved header archives as `[SHA unavailable]` permanently, since the archive is never re-enriched. Budget ~10 minutes.
- **The 4 remaining backfills, all IPP buyer-side:** `lightsource-bp`, `nextera-energy-resources`, `plus-power`, `terra-gen`. That is session 5; session 6 has no backfill rows left, so the §8 backfill ledger closes at session 5.
- **No dossier errors were found to hand forward.** The four profiles were read for `summary`, `productsAndServices` and `technicalSpecs` only; nothing read contradicted itself or the corpus.
- **Standing, unassigned:** the README *archive* listing is still well behind (the non-archive listing is exact at 308/102/127); `archive/nvidia.profile.v2.json` is missing and cannot be reconstructed; OSHA IMIS and SEC EDGAR are both network-blocked from this environment. Two pre-existing README tree cosmetics were recorded and left: ordering breaks in the `study-prep/` listing (`blackstone` before `aypa-power`, `dnv` before `cyrusone`) and a `rolls-royce-power-systems` lesson-plan entry nested under the `megmeet/` node; plus raw `&` where `[PC-README-TREE]` requires `&amp;` in existing `Samsung C&T` entries (this session's four new entries follow the rule).
- **Two items were closed off this session** and removed from the standing list: the `blackstone`/`macquarie` quiz-marker question (v04.99r) and the CHANGELOG rotation-rule contradiction (v04.98r).

#### Recommendation for next session

- Run **guide-backfill session 5** on the four IPP buyer-side companies — `lightsource-bp`, `nextera-energy-resources`, `plus-power`, `terra-gen` — on Opus 5 xhigh, guides only. It is the **last** cluster in the §8 backfill ledger, and its difficulty is the inverse of session 4's. Session 4 had to find material three shipped courses did not own; session 5's four companies sit **inside** an existing course — session 1 already taught the buyer's economics through `apex-clean-energy`, `arevon`, `eolian`, `jupiter-power` and `key-capture-energy`. So the syllabus question is not "what does this cluster own" but **"what does an owner-operator at portfolio scale do that a single-project developer does not"** — fleet construction and standardisation across many sites at once, portfolio-level financing and capital recycling, the regulated-utility-affiliate model, and international origination. Decide that division of labour before authoring, as all four prior sessions did, and read session 1's five guides first to fix the boundary.

**To continue:** type `run guide backfill session 5`
