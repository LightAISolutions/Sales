# Previous Session Context

Claude writes to this file when the developer says **"Remember Session"** — capturing enough context for a future session to pick up the train of thought quickly. This is separate from "Reminders for Developer" (REMINDERS.md), which is the developer's own notes.

> **Note on stale-context auto-reconstruction** — when a session starts and this file's `Repo version:` doesn't match the current repo version, Claude reconstructs the missing entry from CHANGELOG.md and commits it **without pushing**. The commit rides along with the session's first user-task commit on the next push. If a session ends before any user-task push happens, the reconstructed entry stays **local-only** and the next session will just re-reconstruct from CHANGELOG if still stale. This is intentional — pushing a dedicated reconstruction commit on its own would force every subsequent user push in the same session to wait for the auto-merge workflow to finish before it could push too (push-once enforcement). The reconstructed entry is a convenience hint, not load-bearing state, so the small persistence risk is a fair trade.

## Latest Session

**Date:** 2026-09-16 08:29:09 PM EST
**Repo version:** v06.25r — two pushes (v06.24r the module, v06.25r this handover)
**Branch:** `claude/dazzling-meitner-3f4qh8` — restarted from `origin/main` before each push

### What was done

- **S2 session 13 — the `capital` landscape module** (v06.24r). The **thirteenth** landscape and the **twenty-second** guidance module: `guidanceDocLandscapeCapital_()`, id `landscape-capital-2026-09`, lane The Value Chain, tier **contributor**, authored **below** the `// CONTENT END` fence and registered at the **END** of `guidanceDocs_()`'s Value Chain lane. Nine sections at §10.6's ids and order, four tiles, a nine-entry local glossary, a **42-row** claims ledger over **8** member dossiers, `reviewBy` **2026-11-06**
- **`repository-information/industry-guidance/landscape-capital-analysis.md`** — 783 lines, the source of truth, with §2 (the split) drafted before any other section per (j), §10 (`reviewBy`, seven rejections), §11 (the Scraper seed) and §12 (verification)
- **`segment-capital` regenerated** — exactly one segment, `--today 2026-09-16`, `changed: ["read-next", "what-is-bought-and-on-what"]`, `no pin moved`
- **`topic-landscape-capital` seeded** in `Scraper.gs` with ten terms; Scraper GAS **v02.13g → v02.14g**, Classroom **v01.56g → v01.57g**
- **§7.3's S2 row, §6's S2 row and §10.6's progress note flipped 12 → 13 of ~19**, `capital` struck through, the **(dd1)–(dd7)** findings block written, and **§7.42 written in the same commit** — the brief for **Phase 4 row 18, `the-cooling-plant-and-water`** — both halves
- **v06.25r (this handover)** — §7.42's deploy counters refreshed in both halves against the measured v06.24r job log, and `SESSION-CONTEXT.md` written, in the same commit as the repo-version bump

### Where we left off

Everything is merged and live. **Phase 4 is 17 of 26; S2 is 13 of ~19**, so the interleave stands at 17:13 and hands the turn to **Phase 4 row 18, `the-cooling-plant-and-water`** — briefed in **§7.42**, written this session. GAS live at Classroom **v01.57g** and Scraper **v02.14g**. Content checker **0/0 at 47 lessons / 8 tracks / 142 gate cases**, module assertion **22**. `--check` reports **7 due**.

### Key decisions made

- **THE FOUR INCUMBENTS DO NOT SHARE A BASIS, SO THE MODULE DECLINED THE RANKING AND TESTED THE RIVALRY AGAINST THE GRAPH INSTEAD.** 13 edges among the eight, **8 curated carrying 13 typings** — `competitor` 6, `partner` 5, `other` 1, `portfolio` 1. The six competitor typings are **three pairs typed in both directions**, a complete triangle among the three asset managers; **the sovereign incumbent is typed `competitor` with nobody** (one historical transaction counterparty, one active portfolio holding, **no curated typing at all** to the other two incumbents), and so is the single challenger. Published judgment: **three competitors and a counterparty, not four rivals** — and **two of the three competitor edges are the same transaction seen from both sides**, one firm buying the platform another was obliged to sell
- **THE CUT IS STATED INSIDE THE CORPUS BY TWO MEMBERS ABOUT EACH OTHER, INDEPENDENTLY, ON THE SAME DAY.** `profile:blackstone` `strategyRead[0]`: *"Same asset class, opposite structural obligation."* `profile:macquarie` `strategyRead[0]`: *"Same asset class, opposite structural clock."* **Neither cites the other; both carry `lastUpdated` 2026-09-06.** The first corpus-internal cross-attestation S2 has found, and the reason the module sorts on **the clock — when must this money leave** — rather than on size, which no publisher ranks and on which two published rankings disagree
- **ALL FOUR INCUMBENTS ARE PURE PLAYS AND ALL THREE ADJACENTS INVERT, FROM ONE SOURCE SEGMENT.** 3 of 4 shared members inverted (**75 %**), bimodal at 3/3 and 0/1, with only **two** of the twelve built landscapes sharing any member. The adjacents are **operators that learned to sell capital**, not investors that bought operations — so session 10's (aa2) direction with the arrow reversed, the threat aimed at the **fee** rather than the asset, and priced at about **375 bp** for issuing with no sponsor behind an unrated tenant
- **FOUR MECHANISMS IN FIVE CRITERION ROWS, AND THE BRIEF'S OPEN QUESTION ANSWERED AGAINST IT.** Criterion 1 mapped-and-built; 2 mapped-and-unbuilt; 3 and 4 hitting **no** lexicon key; 5 a built lesson outside `READ_NEXT`. **Mapped-but-unbuilt is a VARIANT OF THE FIRST mechanism, not a fourth**, because both rows pass the same `lid in eligible` guard and reach the same `lesson_ref()`, differing only at the registry lookup that appends `(planned)` — **a dash row is what the reader sees when the INTERSECTION fails, and those two are not dash rows**. The row-2 false positive (`engine` matching *engineer*) **never reaches the page**: the eligibility guard drops it
- **`READ_NEXT` IS TWO-THIRDS UNBUILT, VERIFIED BY ID RATHER THAN ASSERTED, SO (bb1) DOES NOT BIND.** Session 10's third posture — say the test is unowned and decline to own it — applies and is correct: §7 row 24 is held by §8 item 3 until the bankability module's quarterly review lands, and row 25 is already specified on `profile:galaxy-digital`, an adjacent here
- **`reviewBy` PRODUCED AN EIGHTH FAILURE MODE — A TAUTOLOGY.** The sort's single future answer (2027-12-31) is rejected three ways, the third being **(bb4) on the same `policyExposure` entry the generator used** to set `segment-capital`'s own clock. The sort and the generator read one field, so the mechanical route cannot produce a second opinion at all
- **A NAME COLLISION IS A NEIGHBOUR RELATION NO CHECKER CAN SEE.** `reading-the-graph` names none of the eight, so (cc4)'s instrument returned nothing — but `nvidia-800vdc-2026-08` carries a glossary entry defining **MGX** as NVIDIA's rack form factor while one of this segment's incumbents **is** MGX. `{{MGX}}` is never used as a term and the module names the collision once
- **THE SCREENSHOTS CAUGHT THREE DEFECTS EVERY CHECKER PASSED, for a fourth consecutive session** — an **unverified corpus-wide superlative**, and **two arithmetic slips**, one of them a sentence shape carried over from the previous module and never recounted against its own table (*five of eight rows* against six)

### Active context

- **Repo version** v06.25r · **`CHANGELOG.md` 114 raw / 93 non-exempt** against a 100 trigger, twenty-one sections dated 2026-09-16 EST. **NO ROTATION FOR A TENTH CONSECUTIVE SESSION**, for exactly the clause every brief since §7.36 has recorded: **the deferral is a function of the day of the push, not of the section count.** `TZ=America/New_York date` read **2026-09-16** at both pushes while the harness banner read 2026-09-17 UTC. It lapses on the first push dated **2026-09-17 EST or later**
- **`Classroomgs.changelog.md` 57 raw / 48 non-exempt** against a cap of 50, nine same-day sections — same deferral, same lapse. Re-counted from the files: `CHANGELOG.md` clears in **ONE** group (2026-09-08, 10 sections); `Classroomgs.changelog.md` needs **TWO** (2026-09-01's 2, then 2026-09-02's 11). `Scrapergs.changelog.md` **35/50**; `Profilerhtml.changelog.md` **49/50**, closest to its own trigger
- **GAS versions:** Classroom **v01.57g**, Scraper **v02.14g**, Profiler v01.39g. **Page versions:** Classroom v01.14w — no page bump
- **Content-checker baseline 0 errors / 0 warnings at 47 lessons / 8 tracks / 142 gate cases**, module assertion **22**. A Phase 4 row takes the lesson count to **48** and leaves the assertion at 22
- **`--check` reports 7 due** — `cells-and-chemistry`, `power-conversion-and-rack-power-silicon`, `compute-and-the-rack`, `epc-and-construction`, `storage-developers-and-ipps`, `software-and-optimization`, `insurance-and-risk-transfer`. **`capital` cleared.** All three numbers were called in advance: **8 → 8 → 7**, and this was the **first S2 session to clear its own already-due segment**
- **Row 18's deepening test PASSES ON ALL THREE PARTS and it is the strongest pass yet** — `the-cooling-plant-and-water` is in `CRITERION_LEXICON` on **nine** keys, in exactly one segment's `READ_NEXT` (`cooling`), and those keys hit **all six** of that segment's own criteria. `cooling` is **not** in the due set and its lesson was last generated 2026-09-14, so forecast **7 → 8 → 8** with `cooling` entering and differing in **two** sections
- **`check-classroom-curriculum.py --strict`** — no structural findings, **28 stale pins**, **3 items due for review**: `landscape-utilities-2026-09` and `landscape-in-hall-power-2026-09` at 2026-10-01, `landscape-neoclouds-2026-09` at **2026-09-30**. All three by design — **do not "fix" any**. `landscape-capital-2026-09` is at **2026-11-06**, outside the horizon by choice
- **Deployment counters, MEASURED at v06.24r against that push's own job log: `Classroom deploy confirmed (GET): Updated to v01.57g (deployment 69) | 69/200` and `Scraper deploy confirmed (GET): Updated to v02.14g (deployment 159) | 159/200`.** Exactly **two** steps fired (Scraper 10 s, Classroom 15 s) and logged exactly two lines; every other `Deploy <Project>` step exited 0 silently in zero seconds on its own `git diff` guard. **Both legs of the forecast were right.** Classroom's **fifth consecutive equal cycle** (69 = 69), headroom **131**; **Scraper headroom 41**, and only an S2 seed moves it. Pages read `|v01.56g|`/`|v02.13g|` at 08:27:01 PM and `|v01.57g|`/`|v02.14g|` at 08:27:18 PM — **re-read rather than assumed**, a lag of about seventeen seconds — and the log agreed. **No `?op=deploy` probe was made**; the §7.19 order resolved on its two reads for the **twenty-fourth** time
- **Toggles:** `START_OF_RESPONSE_BLOCK` On · `CHAT_BOOKENDS` Off · `TIMING_ESTIMATES` On · `END_OF_RESPONSE_BLOCK` On · `MULTI_SESSION_MODE` Off
- **Where the programme stands.** Done: Phases 0, 1, 2a, 2b, 3, 5, 6; S0, K1, S1, S3, G6, C3, the bankability review, the one-off segment regeneration. Open: **Phase 4 at 17 of 26**, **S2 at 13 of ~19**, **K2** (1–2 sessions, runnable any time), **C5** (~6 sessions; its gate is met), the **Q plan clock** (next due ~2026-12). Deferred: C6. **About 22 sessions remain**
- **Open, recorded, not taken.** (i) The `.cl-tbl` `min-width` question still wants a session that edits `Classroom.html`. (ii) The generated `read-next` names the landscape module without linking to it. (iii) **`the-800-vdc-shift`'s stale `three-chains` intro is now twenty registrations stale.** (iv) §10.6's session-4 bullet block is out of chronological order. (v) **A stale `lesson_ref` can make a segment due on its own** — now traced at four separate segments. (vi) The DC-arc taxonomy gap stays with §7 row 20. (vii) **`restart` scores zero across all 472 Scraper terms** — unseeded for a **fourth** session. (viii) **A SECOND standing reservation debt**: six terms session 12 assigned "on split grounds to `topic-aidc-landlords`" are **in no seed and still score zero**, because an S2 session may only add `topic-landscape-<segment>` and that seed has not been edited since session 6. **A term assigned to another seed's territory is not a term seeded there.** The GPU-residual-value gap is unchanged
- **Render harness — unchanged, with one trap sharpened.** PROJECT region of `Classroom.gs` (line 1 to `// TEMPLATE START` — **recompute that line number**; it was **58762** after this session) in a Node `vm`. Shim **only** `validateSessionForData` (permissions an **ARRAY**) plus no-op `clProgressRead_`/`clProgressWrite_`/`clStudyNext_`/`dataAuditLog`/`auditLog`, **after** the module loads. Route `payload.action === 'guidance'` → `ctx.handleGuidanceOp_`, else `ctx.handleClassroomOp_`, **through the vm context object**; pass the op as `{ parameter: payload }`. **Serve the static files and the `/__gas` route from ONE Node process on ONE port**, at **`/Classroom.html`**, not the site root. `_e = ''`; patch the `var AUTO_REFRESH = true;` **declaration**; copy `profiler-data/`, `sounds/`, `images/` and `favicon.ico` in. Seed `sessionStorage` **after** load, token **≥ 32 characters**; override `window._gasPost` **after** load and return a **Promise**. Hide `#auth-wall` / `#signing-out-wall` / `#auth-timers` / `#gas-layer-toggle` plus `#cl-app{display:block !important}`. Module entrypoint `clShowGuidanceDoc(id)`, lesson entrypoint `clShowLesson(id)`; section nodes are `#cl-<sectionId>`
- **THE SHELL TRAP, SHARPENED.** §7.41 recommends `ps -eo pid,args | grep -E "node .*/shim"` in place of `pgrep`. **That pattern misses the process when the shim is launched from its own directory**, because the arguments are then literally `node shim.js` with no slash: a re-render after three fixes returned a **byte-identical 47,810 characters** and none of the corrected strings, because the old `vm` was still serving. **Match on `node .*shim`, and treat an unchanged character count after an edit as evidence of a stale process rather than of an unchanged page.**
- **This session's render: contributor 9/9 sections, 47,821 characters, zero page errors**, zero unresolved `{{…}}`, zero literal backticks, zero stray markup, every section's screenshot read individually. Through the real serving path: **analyst → `ROLE_DENIED`** with 0 of 9 section nodes and 66 characters, rendering the page's own line; **token < 32 chars → `SESSION_EXPIRED`**. The two `ERR_CERT_AUTHORITY_INVALID` console errors are environmental

### Recommendation for next session

- Run **Phase 4 row 18 — the `the-cooling-plant-and-water` lesson** on Opus 5 xhigh. Its brief is **`INTEGRATED-REMEDIATION-PLAN.md` §7.42**, written and counter-corrected this session. Four things carry the attention: the **three-part deepening test passes on all three parts and on six of six criteria**, the strongest pass any row has had, so forecast **7 → 8 → 8** with `cooling` **entering** the due set rather than clearing; it is a **mid-array insert at position 2** of `aidc-campus`, so expect **P5** where the last two rows had none; **§2.3's one-lesson cap is the constraint** and a ninth section is the tell that it has been crossed; and the push may be **the rotator** — test `TZ=America/New_York date` rather than forecasting, because ten consecutive sessions have been deferred by the day of the push alone.

**To continue:** type `run Phase 4 row 18`

## Previous Sessions

### Previous Session — v06.23r

**Date:** 2026-09-16 07:00:25 PM EST
**Repo version:** v06.23r — two pushes (v06.22r the lesson, v06.23r this handover)
**Branch:** `claude/brave-brown-ubhmo4` — restarted from `origin/main` before each push

#### What was done

- **Phase 4 row 17 — the `where-the-chain-breaks` lesson** (v06.22r). The **forty-seventh** lesson and the **capstone of `aidc-grid-to-chip`**: *The Failure-Point Map*, gate `tracks`, authored **inside** the `// CONTENT START` … `// CONTENT END` fence and registered at the **END** of `clLessons_()`. Six sections at §3.3's ids and order in **six distinct kinds** — `one-chain-twelve-weak-points` (table, 5 × 14) · `the-physics-behind-them` (callout) · `who-owns-the-failure` (prose) · `a-fault-walks-the-chain` (timeline, 11 rungs) · `drill` (14 cards) · `check-yourself` (6 scenario items) — plus four tiles and a five-entry local glossary
- **THIRTY public stamps, the widest in the corpus**, every date read off the document fetched this run and every ref resolved by opening the file and matching its own `slug`. All `study:` and `concepts:`, so the fold is **`tracks`** — analyst-visible
- **APPENDED at position 9 of 9** in `clTrackAidcGridToChip_()`'s `lessons[]`, which **broke the two-row mid-array-insert run**: the signature came back **P1 alone** — no P5, no P3, no P8 — exactly as §7.40 forecast. That track's `updated` moved and its running `short` gained the closer
- **§7.3's Phase 4 row and §6's Phase 4 row flipped 16 → 17 of 26**, curriculum **§7 row 17 flipped to built** with a six-paragraph record, and **§4's `aidc-grid-to-chip` table flipped at position 11**. **§7.41 written in the same commit** — the brief for **S2 session 13, `capital`** — both halves
- **v06.23r (this handover)** — §7.41's deploy counters corrected in both halves against the measured v06.22r job log, and `SESSION-CONTEXT.md` written, in the same commit as the repo-version bump

#### Where we left off

Everything is merged and live. **Phase 4 is 17 of 26; S2 is 12 of ~19**, so the interleave stands at 17:12 and hands the turn to **S2 session 13, `capital`** — briefed in **§7.41**, written this session. GAS live at Classroom **v01.56g**. Content checker **0/0 at 47 lessons / 8 tracks / 142 gate cases**, module assertion **21**. `--check` reports **8 due**.

#### Key decisions made

- **THE §5 MAP IS THE SPEC AND IT HAS FOURTEEN ROWS WHILE THE SECTION ID SAYS TWELVE — DECIDED, NOT INHERITED.** §3.3 fixes the id as `one-chain-twelve-weak-points`; §5's table has fourteen rows and resolves the discrepancy in its own closing line. **The id is kept, all fourteen rows carried, and the split written into the table's `note`.** It is also **corpus-visible rather than plan-only**: `study:vertiv` and `study:schneider-electric` cite rows 13 and 14 **by these numbers** in their own `where-it-fails` sections and `study:siemens-energy` cites rows 1, 2, 3, 11 and 12 — so a renumbering would have broken three shipped guides
- **THE FOUR CAUSES WERE COUNTED, NOT ASSERTED, AND THE COUNT IS THE LESSON'S SHARPEST FACT.** Hands 6 (rows 2, 3, 5, 6, 8, 14) · current squared 5 (3, 6, 7, 9, 10) · no zero crossing 1 (11) · heat in series 1 (13) · **none of the four 3** (1, 4, 12); rows 3 and 6 carry two. So **the commonest cause on the map is not physics at all**, and the most-discussed hazard in the chain — the DC arc — is **its rarest**. No fifth cause was invented: the three unassigned rows are marked as such and share a stated property — in each the equipment works exactly as specified and the failure is in the system around it
- **THE OWNERSHIP COLUMN WAS COUNTED THE SAME WAY.** Operator **6 of 14** — more than any other party, and it buys almost none of the equipment in those rows; electrical contractor 5; OEM 5; developer, utility and commissioning agent 2 each. **Exactly one row has a single owner** (row 10, the forty-eight volt bus), and it is the only stage of the chain entirely inside one vendor's box — which is why the map reads as a list of **seams** rather than a list of broken parts. The two rows naming the commissioning agent are the two whose failure is that something was never proven
- **THE DC-ARC TAXONOMY WAS DECLINED RATHER THAN QUIETLY OWNED** — session 10's **(aa1)** posture applied to a lesson. Row 11 states the failure and names the handoff to `dc-fault-engineering` (§7 row 20, guidance-gated, **unbuilt**) in the row cell, the drill card **and** the function header, so the debt is now explicit in shipped content rather than only in a plan
- **TWO GUIDES §5 DOES NOT NAME WERE PINNED, AND ONE §5 SENTENCE WOULD OTHERWISE HAVE BEEN UNSOURCED.** §5 row 1 ends *"an audit that pauses a batch"* and **neither guide §5 names for that row carries the clause** — checked by grep before it was written. `study:aep` is the only guide in the corpus that does; `study:dominion-energy` states row 1 almost verbatim. Both pinned with the reason in the stamp's `note`. **§5.2's rule applied to a planning document rather than to a corpus source.** Three guides §5 *does* name (`mortenson`, `wartsila`, `vantage`) were **dropped as unused** — the first row to exercise §3.3's "pin only what you used" in both directions in one commit
- **(bb1) WAS MEASURED ON ITS FIRST OUTING AND DID NOT BIND.** All 21 modules grepped for the id, the title and any planned/unbuilt claim: **zero hits on all three**. The one `(planned)` occurrence in the file is inside the **generated** `clLessonSegmentEpcAndConstruction_()` — (bb1)'s mechanical detector working. **No landscape covers `epc-and-construction` at all**, so there was no prose to revise and the assertion stayed at 21
- **THE SCREENSHOTS CAUGHT ONE DEFECT EVERY CHECKER PASSED.** The timeline's second rung read *"This is row 3 and row 7 arriving together"* — but row 7 is harmonic heating and nothing in it is failing at that instant; the shared thing is the **cause**, not the row. Corrected to *"This is row 3's clock starting"*. The micro-markup check caught one genuine nested emphasis **before** the write (an italic lesson title inside a bold sentence in drill card 11) and nothing after
- **§7.40's DEPLOY-COUNTER FORECAST WAS WRONG BY ONE AND THE REASON IS GENERALISABLE.** It forecast Classroom at 67; the log read **68**. Its counters were measured at v06.16r (66/66) and it did not account for **v06.20r's own Classroom `.gs` change** — the module revision — which consumed 67. **A counter forecast must add every intervening `.gs` push, not just the forecasting session's own.** §7.41 records the correction

#### Active context

- **Repo version** v06.23r · **`CHANGELOG.md` 111 raw / 93 non-exempt** against a 100 trigger, eighteen sections dated 2026-09-16 EST, counter `Sections: 111/100`. **NO ROTATION FOR A NINTH CONSECUTIVE SESSION** — §7.40 called this session "very likely the rotator" and it was not, for exactly the reason §7.40's own changelog paragraph stated: **the deferral is a function of the day of the push, not of the section count.** It lapses on the first push dated **2026-09-17 EST or later**
- **`Classroomgs.changelog.md` 56 raw / 48 non-exempt** against a cap of 50, eight same-day sections — same deferral, same lapse. Re-counted from the files: `CHANGELOG.md` clears in **ONE** group (2026-09-08, 10 sections); `Classroomgs.changelog.md` needs **TWO** (2026-09-01's 2, then 2026-09-02's 11). `Scrapergs.changelog.md` **34/50**; `Profilergs.changelog.md` 39/50; `Profilerhtml.changelog.md` **49/50**, closest to its own trigger
- **GAS versions:** Classroom **v01.56g**, Scraper v02.13g, Profiler v01.39g. **Page versions:** Classroom v01.14w — no page bump
- **Content-checker baseline 0 errors / 0 warnings at 47 lessons / 8 tracks / 142 gate cases**, module assertion **21**. An S2 session takes the assertion to **22** and leaves the lesson count where the generator puts it. Drill pools moved by this row's 14 cards + 6 quiz items
- **`--check` reports 8 due** — `cells-and-chemistry`, `power-conversion-and-rack-power-silicon`, `compute-and-the-rack`, `epc-and-construction`, `storage-developers-and-ipps`, **`capital`**, `software-and-optimization`, `insurance-and-risk-transfer`. **Both halves called exactly: 8 → 8 → 8.** The three-part deepening test was run programmatically and **fails at part one** — `where-the-chain-breaks` is in **zero** `CRITERION_LEXICON` entries — while its one `READ_NEXT` membership (`epc-and-construction`) nets against a segment already due and already differing in both sections. **Fifth consecutive Phase 4 row to leave a stale `lesson_ref` behind**
- **`check-classroom-curriculum.py --strict`** — no structural findings, **28 stale pins**, **3 items due for review**: `landscape-utilities-2026-09` and `landscape-in-hall-power-2026-09` at 2026-10-01, `landscape-neoclouds-2026-09` at **2026-09-30**. All three by design — **do not "fix" any**. The neoclouds one passes on 30 September and will then read overdue until a session revises that module
- **Deployment counters, MEASURED at v06.23r against the v06.22r job log: Classroom `Updated to v01.56g (deployment 68) | 68/200`, Scraper unchanged at 158/200.** Exactly **one** deploy step fired and logged exactly one line; every other `Deploy <Project>` step exited 0 silently in zero seconds on its own `git diff` guard. Classroom's **fourth consecutive equal cycle** (68 = 68), headroom **132**; **Scraper headroom 42 and only an S2 seed moves it**. Pages read `|v01.55g|` at 06:57:26 PM and `|v01.56g|` at 06:57:47 PM — **re-read rather than assumed**; **no `?op=deploy` probe was made** and the §7.19 order resolved on its two reads for the **twenty-third** time
- **Toggles:** `START_OF_RESPONSE_BLOCK` On · `CHAT_BOOKENDS` Off · `TIMING_ESTIMATES` On · `END_OF_RESPONSE_BLOCK` On · `MULTI_SESSION_MODE` Off
- **Where the programme stands.** Done: Phases 0, 1, 2a, 2b, 3, 5, 6; S0, K1, S1, S3, G6, C3, the bankability review, the one-off segment regeneration. Open: **Phase 4 at 17 of 26**, **S2 at 12 of ~19**, **K2** (1–2 sessions, runnable any time), **C5** (~6 sessions; its gate is met), the **Q plan clock** (next due ~2026-12). Deferred: C6. **About 23 sessions remain**
- **`capital` is measured and briefed.** Segment position 16, **eight members: 4 incumbents · 1 challenger · 3 adjacents** — the inverse of session 12's 1 · 6 · 0. Its `READ_NEXT` is **two-thirds unbuilt** (`what-bankable-means` §7 row 24, `reading-the-numbers` §7 row 25; only `contracts-and-revenue` is built), and its five buying criteria produce **all three known dash mechanisms at once plus a candidate fourth** — one criterion maps to a lesson that is in `READ_NEXT` **and unbuilt**. **It is already in the due set**, so the next session **clears** its own segment rather than entering it: forecast **8 → 8 → 7**
- **Open, recorded, not taken.** (i) The `.cl-tbl` `min-width` question now has **twenty-six** data points — this session added a **five-column, fourteen-row** table clean at 1400 px, the widest the corpus has rendered. (ii) The generated `read-next` names the module without linking to it. (iii) **`the-800-vdc-shift`'s stale `three-chains` intro is now nineteen registrations stale — and it now sits immediately before the lesson this session built**, so the two are adjacent on the analyst walk. (iv) §10.6's session-4 bullet block is out of chronological order. (v) A stale `lesson_ref` can make a segment due on its own. (vi) **The DC-arc taxonomy gap stays with §7 row 20** and is now explicit in shipped content. (vii) **`restart` scores zero across all 454 Scraper terms** — unseeded for three sessions. (viii) The GPU-residual-value gap blocked in both directions still wants a developer decision on the scorer
- **Render harness — the recipe, rebuilt from scratch this session in a fresh container and confirmed unchanged.** PROJECT region of `Classroom.gs` (line 1 to `// TEMPLATE START` — **recompute that line number, it moves with every append**; it was 58074 after this row) in a Node `vm`. Shim **only** `validateSessionForData` (permissions an **ARRAY**) plus no-op `clProgressRead_`/`clProgressWrite_`/`clStudyNext_`/`dataAuditLog`/`auditLog`, **after** the module loads. Route `payload.action === 'guidance'` → `ctx.handleGuidanceOp_`, else `ctx.handleClassroomOp_`, **through the vm context object**; pass the op as `{ parameter: payload }`. **Serve the static files and the `/__gas` route from ONE Node process on ONE port** — same-origin, satisfies the page's `connect-src 'self'` CSP. **Serve at `/Classroom.html`, NOT at the site root.** `_e = ''`; patch the `var AUTO_REFRESH = true;` **declaration**; copy `profiler-data/`, `sounds/`, `images/` and `favicon.ico` in. Seed `sessionStorage` **after** load, token **≥ 32 characters**; override `window._gasPost` **after** load and return a **Promise**. Hide `#auth-wall` / `#signing-out-wall` / `#auth-timers` / `#gas-layer-toggle` plus `#cl-app{display:block !important}`. Entrypoint `clShowLesson(id)`; section nodes are `#cl-<sectionId>`. `pip install playwright` is needed; launch with `executable_path` from `glob('/opt/pw-browsers/chromium*/chrome-linux/chrome')`
- **A shell trap the previous session's note did not fully close, and it cost a killed shell.** `pgrep -f "[s]him[.]js"` **still matches the invoking shell**, because the bracket trick only defeats the pattern matching *itself* — the compound command line contains the literal text `shim.js`, which the regex matches. **Use `ps -eo pid,args | grep -E "node .*/shim" | grep -v grep`** instead. Also: `pg.screenshot()` on a hidden element times out at 30 s each, so filter to the section nodes you actually want
- **This session's render: analyst 6/6 sections, 33,814 characters, zero page errors, zero surviving `{{…}}`, zero literal asterisks**, every section's screenshot read individually. Through the real serving path: **viewer → `ROLE_DENIED`** with zero section nodes, **token < 32 chars → `SESSION_EXPIRED`**, both rendering the page's own text. The two `ERR_CERT_AUTHORITY_INVALID` console errors are environmental

#### Recommendation for next session

- Run **S2 session 13 — the `capital` landscape module** on Opus 5 xhigh. Its brief is **`INTEGRATED-REMEDIATION-PLAN.md` §7.41**, written and counter-corrected this session. Four things carry the attention: the roster is **4 · 1 · 3** and **inverts session 12's 1 · 6 · 0**, so `who-dominates-and-on-what-basis` must sort **four different instruments** rather than rank one basis; the segment's `READ_NEXT` is **two-thirds unbuilt**, so (bb1) points the other way and the module may honestly name the gap **if it verifies on the day**; the criterion intersection produces **all three known dash mechanisms at once plus a candidate fourth** (a criterion mapped to a lesson that is in `READ_NEXT` and unbuilt), which the module must name row by row; and **`capital` is already in the due set**, so this session **clears** its own segment — forecast **8 → 8 → 7**, not 8 → 8 → 8.

**To continue:** type `run S2 session 13`
