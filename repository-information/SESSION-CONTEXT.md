# Previous Session Context

Claude writes to this file when the developer says **"Remember Session"** — capturing enough context for a future session to pick up the train of thought quickly. This is separate from "Reminders for Developer" (REMINDERS.md), which is the developer's own notes.

> **Note on stale-context auto-reconstruction** — when a session starts and this file's `Repo version:` doesn't match the current repo version, Claude reconstructs the missing entry from CHANGELOG.md and commits it **without pushing**. The commit rides along with the session's first user-task commit on the next push. If a session ends before any user-task push happens, the reconstructed entry stays **local-only** and the next session will just re-reconstruct from CHANGELOG if still stale. This is intentional — pushing a dedicated reconstruction commit on its own would force every subsequent user push in the same session to wait for the auto-merge workflow to finish before it could push too (push-once enforcement). The reconstructed entry is a convenience hint, not load-bearing state, so the small persistence risk is a fair trade.
## Latest Session

**Date:** 2026-09-15 04:40:06 AM EST
**Repo version:** v05.89r — two pushes (v05.88r the module, v05.89r the counter correction)
**Branch:** `claude/awesome-ritchie-0h4k10` (restarted from `origin/main` before each push)

### What was done

- **S2 session 6 — the `aidc-developers-and-landlords` landscape module** (v05.88r). `landscape-aidc-developers-and-landlords-2026-09`: the **sixteenth guidance module** and the **sixth landscape**, authored **below the `// CONTENT END` fence** at §10.6's nine section ids in its order — `who-dominates-and-on-what-basis` (prose, eight paragraphs, organised by **what each incumbency is made of** rather than as a league table) · `who-threatens` (prose, eight paragraphs) · `each-players-bet` (table, **twenty-two rows**, second-largest in the corpus) · `the-indicators` (table, ten rows) · `the-sellers-play` (callout) · `claims-ledger` (ledger, **75 rows**) · `what-the-record-does-not-say` (callout) · `drill` (eleven cards) · `check-yourself` (five items) — four tiles, six-term local glossary, `reviewBy` **2026-12-15**, registered in `guidanceDocs_()`
- The analysis file is **367 lines**, and its **§2 — the split — was written before any other section** per the rule §7.23 established
- **The content checker's guidance-module assertion moved 14 → 15** (the one edit outside the fence), **one Scraper interest seed added** (`topic-aidc-landlords`, seven terms), **one `--segment` regeneration** (`changed: read-next`, no pin moved)
- **v05.89r (the second push)** — §7.28's deployment counters refreshed from forecast to measurement in both halves, **and the measurement corrected the counter rule**; see Key decisions

### Where we left off

Everything is merged and live. **Phase 4 is 10 of 26; S2 is 6 of ~19**, so the interleave stands at 10:6 and hands the turn back to Phase 4. GAS live at Classroom **v01.42g** and Scraper **v02.07g**, both confirmed on Pages and in the job log. The content-checker baseline is **0 errors / 0 warnings at 40 lessons / 8 tracks / 142 gate cases**, with the module assertion now at **15**. `build-classroom-segments.py --check` reports **7 due** — none of them the next session's to clear.

### Key decisions made

- **THE FINDING OF THE SESSION: a landscape can have to split against a public LESSON, and that split crosses an ACCESS TIER rather than only a subject.** This segment had **four** neighbours against session 5's three — three registered modules plus `the-campus-as-a-power-project`, a Phase 4 row 9 mechanism lesson that is **public** and built on nine of this segment's own members. The line taken is one sentence: **the lesson teaches the test, and this module applies it to named parties.** So the module states where EdgeCore's "1.8 GW delivered or under development" headline actually sits (~62 MW operating in two buildings) without restating the five-rung ladder that makes the reading possible. **It matters more than a module split because the gates differ**: material drifting from the module into the lesson's territory is material an analyst can already read, and material drifting the other way is a gate raised on something already public. Nine omissions across all four neighbours are enumerated in the function header and §2 of the analysis file
- **And the neighbour PRE-DECLARED the handoff, which no earlier split had.** One of that lesson's four tiles reads *0 league tables — no ranking of these landlords exists in this corpus and this lesson does not invent one.* That is layer 4's job by construction (§10.1), so the boundary was stated upstream before this module existed. **Check a public neighbour's tiles and callouts for a declined judgment before drafting** — it is the cleanest possible statement of where the line runs. §7.23's test held on **the pre-leased share falling 64 % → 54 % while capacity underway rose 82 %**: an *arithmetic exercise* in the lesson, a *solitary market gauge* here
- **THE SECOND FINDING, and it corrects a rule three briefs carried: `Already up to date` is silent on deployment consumption.** Scraper's counter measured **152/200** against a 151/200 forecast. Reading back to the last measured anchor — **150/200 at v05.78r**, then v05.83r's poll-resolved `Already up to date` (recorded as consuming none), then v05.85r's step never firing — this push's one `Updated to` accounts for one and **one consumption is unaccounted for**. The only Scraper deploy activity in the interval was v05.83r's, whose **unconfirmed first GET leg had already run `pullAndDeployFromGitHub()`**. So `Already up to date` describes the state at **read** time, not that the call spent nothing. **The transferable lesson is about evidence class**: a rule derived from *what a log says* survives until someone does *arithmetic across two measurements*. Three briefs restated the middle case because each read one run's wording; none subtracted an anchor
- **The module's own argument is that dominance here cannot be ranked, and the record supports the refusal.** `digital-realty`'s dossier calls the category claim *unfalsifiable rather than true or false*; `cyrusone`'s records published rankings of one company disagreeing by **4.5×**; and **eighteen of the thirty members publish no normalized revenue figure — seven of the nine incumbents among them — while eight of the thirteen challengers do**, because the converted-miner cohort arrived already listed. So the measurable half of this segment is its contesting set. Incumbency rests on four **non-comparable** bases (securitisation franchise, standardized product, proprietary engineering, a listed trust's disclosure obligation), and flattening them would misrepresent all four. A tile reads **18 of 30**
- **The second and third judgments.** The threat is **not share loss — the scarce asset moved, from land to an energised interconnection**, which is why seven of thirteen challengers are converted miners competing on a date. And **tenant credit here is manufactured with an observable price**: 6.125 % (parent-guaranteed), 6.192 % (BBB−, backstopped), 7.125 %, 9.875 % (unrated tenant, no enhancement) — a **~375 bp** spread paid for the tenant rather than the building, plus equity at ~14 % and ~5.4 % from two landlords and **nothing from a third on the same template**
- **`reviewBy` read-don't-sort for the sixth consecutive session, and the rejection class fired on the NEAREST candidate — twice over.** Across 30 members: **107 `policyExposure[]` entries, 84 dated, exactly two future dates**, both 2027-12-31 and both the same Section 232 schedule — so a sort would have set the clock on a **tariff expiry**. The genuine nearest gate, **10 December 2026**, is **already the `reviewBy` of two registered modules**, so it went to `the-indicators` as row 1. A second candidate (PJM's 16 September deadline) was rejected twice over: one day after `updated`, *and* a neighbour module's subject. **Five candidates rejected in writing**; the date taken (2026-12-15) was read out of `edgecore`'s policy **prose** while that entry's `effectiveDate` holds the first step
- **The Scraper-seed prior was HALF right — a third outcome the rule had not recorded.** §7.27 named five candidate gaps; run against all **392** terms, **two were already covered** (the co-location statutes and the buildout shape) while the segment's own **commercial** layer scored zero across the board. Seven terms seeded; `special contract` and `energy service agreement` also score zero and were **left out on split grounds rather than duplication grounds**, because they are `utility-aidc-procurement-2026-08`'s instruments
- **The netting rule held on its second run and the deepening did NOT fire.** `--check` went **8 → 8 → 7** exactly as predicted, with this segment already sixth of the eight; it differed in `read-next` alone both before and after. So deepening is a real possibility rather than a regular one — **predict both halves and record which fired**
- **A harness fault worth knowing, because it looks exactly like an access defect.** The first Playwright render served the page at `/` rather than `/Classroom.html`. `Classroom.html` derives its storage-key prefix from the URL path (`_pageName`), so the seeded session landed under `index_gas_*` keys while `clRole()` read `Classroom_gas_*`, and the page showed *"Industry Guidance is available to the admin and contributor tiers"* — indistinguishable from a real denial. **Serve the scratch copy at its own filename, never at the site root**

### Active context

- **Repo version** v05.89r · **CHANGELOG 91 raw / 80 non-exempt**, eleven sections dated 2026-09-15 — **nine from the 100 cap, the closest it has ever been**. The next session will not rotate; **the one after it probably will**. Re-count on the EST clock anyway
- **The EST/UTC trap did NOT fire — both clocks read 2026-09-15 again.** Still a convergence, not a fix; it diverges for any session running between 20:00 EST and midnight UTC
- **Standing changelog state:** `Classroomgs.changelog.md` **42 raw / 38 non-exempt** against 50 — still the closest of the three to arming, about eight sessions of headroom. `Scrapergs.changelog.md` **28 raw / 26 non-exempt**. `Profilerhtml.changelog.md` sits at 49/50
- **GAS versions:** Classroom **v01.42g**, Scraper **v02.07g**, Profiler v01.39g. **Page versions:** Classroom v01.14w, Profiler v01.90w, Scraper v01.72w — no page bump this session
- **Content-checker baseline is 0 errors / 0 warnings at 40 lessons / 8 tracks / 142 gate cases** — protect it. **Its guidance-module assertion is now 15 and a Phase 4 row must NOT touch it**
- **Deployment counters: Classroom 53/200, Scraper 152/200, MEASURED.** Both legs fired and both confirmed on the **first GET leg with no POLL and no probe** — the **tenth consecutive** clean use of the §7.19 order and the first two-line deploy since v05.83r. Scraper came in **one above forecast** (see Key decisions)
- **`--check` reports 7 due** — `in-hall-power`, `clean-firm-and-nuclear`, `compute-and-the-rack`, `epc-and-construction`, `storage-developers-and-ipps`, `hyperscalers-and-ai-labs`, `neoclouds`. **The next session (Phase 4 row 11) regenerates none of them** and should take the count to **10 due**
- **`check-classroom-curriculum.py --strict`** reports no structural findings, **28 stale pins** (pre-existing and unchanged — this session added none) and **1 item due for review** — `landscape-utilities-2026-09` at 2026-10-01, by design. **A second joins it on 28 October** (`landscape-power-conversion-and-rack-power-silicon-2026-09`, `reviewBy` 2026-11-27); **this session's own module at 2026-12-15 does not enter the horizon until 15 November**. All exit 0, because review dates never call `strict()`. All belong to the quarterly guidance Routine
- **Toggles:** `START_OF_RESPONSE_BLOCK` On · `CHAT_BOOKENDS` Off · `TIMING_ESTIMATES` On · `END_OF_RESPONSE_BLOCK` On · `MULTI_SESSION_MODE` Off
- **Where the programme stands, measured 2026-09-15 at v05.89r.** Done: Phases 0, 1, 2a, 2b, 3, 5, 6; S0, K1, S1, S3, G6, C3, the bankability review, the one-off segment regeneration. Open: **Phase 4 at 10 of 26**, **S2 at 6 of ~19**, **K2** (1–2 sessions, runnable any time), **C5** (~6 sessions, gated on ~6 landscapes — **six now exist, so C5's gate is MET**), the **Q plan clock** (next due ~2026-12). Deferred: C6. **About 36 sessions remain**
- **Open, recorded, not taken.** The `.cl-tbl` `min-width` question now has **fourteen** clean data points — this session's twenty-two-row bets table and seventy-five-row ledger added two more row-count extremes — and **should be closed**; the generated `read-next` link is a `Classroom.html` renderer feature (`clFmt` has no link token) — **both want the same session, one that edits `Classroom.html`, plus a developer `--all` regeneration run**. `the-800-vdc-shift`'s stale `three-chains` intro is now **six inserts stale** and stays reassigned away from the Phase 4 queue (§7.25); this session acquired no claim on it either
- **§10.6's session-4 bullet block still sits out of chronological order** (session 4's appears before session 3's). Sessions 5 and 6 both appended at the end rather than silently reordering another session's text. Cosmetic; a future session can fix it deliberately
- **Render harness — the recipe, with one addition this session.** Run the PROJECT region of `Classroom.gs` under Node with GAS shims; shim **only** `validateSessionForData` plus no-op `clProgressRead_` / `dataAuditLog` / `auditLog`, and let `handleGuidanceOp_` / `handleClassroomOp_` answer for real. **Serve the scratch copy at `/Classroom.html`, NOT at the site root** — the page derives its sessionStorage key prefix from the URL path. `_e = ''` and `AUTO_REFRESH = false`; copy `profiler-data/`, `sounds/`, `images/` **and `favicon.ico`** in. Seed `sessionStorage` **after** load (`Classroom_gas_session_token` · `_gas_user_email` · `_gas_user_role` · `_gas_user_permissions`); override `_gasPost` **after** load and return a **Promise**. **Hide** `#auth-wall`, `#signing-out-wall`, `#auth-timers`, `#gas-layer-toggle` plus `#cl-app{display:block !important}` — never `remove()`. Entrypoints: `clShowLesson(id)` for lessons, `clShowGuidanceDoc(id)` for guidance. Section nodes are `#cl-<sectionId>`. `pip install playwright`; launch with `executable_path` from `glob('/opt/pw-browsers/chromium-*/chrome-linux/chrome')`. **`sess.permissions` must be an ARRAY** and **the session token at least 32 characters**, or the real guard returns `SESSION_EXPIRED`
- **The two console errors in every render are environmental and pre-existing**, both `ERR_CERT_AUTHORITY_INVALID` because the sandbox proxy's CA is untrusted by Chromium. Neither is a module asset and neither raises a page error

### Recommendation for next session

- Run **Phase 4 row 11 — the `contracts-and-revenue` lesson** on Opus 5 xhigh. Its brief is **`INTEGRATED-REMEDIATION-PLAN.md` §7.28**, written at the close of S2 session 6 and refreshed twice in the same session, so **both halves carry measured deployment counters and a corrected counter rule** — nothing in it is a forecast. It restores the §7.4 item 6 interleave at 10:6, its **input arithmetic has a trap worth the attention** (the v05.07r amendment *overlaps* the base list, so the roster is **fourteen**, not fifteen), and its due-count prediction (**7 → 10**) is stated with per-segment section deltas for all four affected segments — the second real test of the row-10 deepening case, which session 6's own segment did not fire.

**To continue:** type `run Phase 4 row 11`

## Previous Sessions

### Session — 2026-09-15 03:45:30 AM EST (v05.87r)

**Date:** 2026-09-15 03:45:30 AM EST
**Repo version:** v05.87r — three pushes (v05.85r the lesson, v05.86r the counter refresh, v05.87r this handover)
**Branch:** `claude/eloquent-gauss-6kqnar` (restarted from `origin/main` before each push)

#### What was done

- **Phase 4 row 10 — the `how-a-storage-project-happens` lesson** (v05.85r). *From Site Control to Commercial Operation*: the **tenth mechanism lesson** and the **sixth mid-track insertion**, authored inside the content fence at §3.5's seven section ids in its order — `the-lifecycle` (timeline, **nineteen items across three lanes**, anchored on months either side of financial close) · `who-does-what-when` (table, **4 × 8**) · `where-the-seller-enters` (prose, nine paragraphs) · `how-overruns-hide` (callout) · `where-it-fails` (callout) · `drill` (eight cards) · `check-yourself` (five items) — with four tiles and a six-term local glossary
- **Sixteen inputs, all public**, so the stamp folds to `tracks` and the lesson is analyst-visible: thirteen `study:` guides, two `profile:` dossiers and `concepts:profiler-concepts`. Every pin read off the document fetched that run against a deepened clone; every `study:`/`profile:` ref resolved by opening the file and matching its own `slug`
- Inserted at `market-access` `lessons[]` **between `who-buys-storage` and `how-a-utility-buys`** (the §4 position 2), track `updated` 2026-09-14 → 2026-09-15, registered in `clLessons_()`
- **v05.86r (the second push)** — §7.27's deployment counters refreshed from forecast to measurement in both halves, and a **third case of the counter rule** recorded in §7.3's order-6 row
- **v05.87r (this handover)** — §7.27's **changelog arithmetic** corrected in both halves (87 → 88 raw), found while reproducing its prompt block verbatim; see Key decisions for the rule it generalises

#### Where we left off

Everything is merged and live. **Phase 4 is 10 of 26; S2 is 5 of ~19.** GAS live at Classroom **v01.41g**, confirmed on Pages and in the job log. The content-checker baseline is **0 errors / 0 warnings at 40 lessons / 8 tracks / 142 gate cases**, with the guidance-module assertion still at **14**. `build-classroom-segments.py --check` reports **8 due** — none of them this session's to clear.

#### Key decisions made

- **THE FINDING OF THE SESSION: an already-due segment can *deepen* as well as stay put, and no brief has predicted that half.** `--check` went **7 due → 8 due** exactly as §7.26 forecast — `storage-developers-and-ipps` joined, `epc-and-construction` was already among the seven, and v05.83r's netting held. What the forecast did not describe is that `epc-and-construction` **also changed shape**: from differing in `read-next` alone to differing in `read-next` **and** `what-is-bought-and-on-what`, because its buying-criteria lexicon matches this lesson. So row 8's rule — the lexicon is subordinate to `READ_NEXT` and decides only whether a segment differs in one section or two — **applies to segments already in the due set, not only to ones newly entering it.** A prediction stated purely as a membership delta under-describes the diff; predict the membership delta *and* the per-segment section delta
- **A THIRD CASE OF THE DEPLOYMENT-COUNTER RULE, and it is the one that logs nothing.** §7.26 established two: `Updated to` consumes a deployment, `Already up to date` consumes none. v05.85r's Scraper leg produced **neither**, because the step never fired — each project's deploy step opens with `git diff --name-only "$PRE" HEAD | grep -q "$GS" || exit 0`, and `Scraper.gs` was not in the merge diff, so it exited 0 in under a second without contacting the deployment. **Forecasting from "whether the running bundle is behind" gets the number right in all three cases but describes only the middle one** — to predict the log line you will actually read, ask first whether the file is in the diff. Classroom confirmed on the first GET leg at `Updated to v01.41g (deployment 52) | 52/200`, no POLL, no probe: the **ninth consecutive** clean use of the §7.19 order
- **The instruction to re-derive the input roster rather than copy it paid, by passing.** §3.5's base list is eight and the v05.07r amendment adds eight, so the roster is **sixteen** — and §7 row 10 already listed exactly sixteen. The two halves agreed, which is what row 9's did not. **A check that passes is not a wasted check**; row 9's brief was one short on the identical arithmetic and only the lesson's own §3 entry settled it
- **Row 6's omission list became row 10's input list, exactly as §3.5 said it would**, so **nothing was fetched-and-not-pinned at the input level**. Six of the twelve guides row 6 deliberately dropped teach a lifecycle stage rather than a buyer class, and this is the row that material is assigned to. The omission record therefore sits one level down at the *section* level, with the same shared reason row 9 found: capital structure and revenue instruments belong to rows 11 and 25
- **A track's `short` is not automatically extended by an insertion, and the test is what kind of sentence it is.** `market-access`'s `short` is §3.5's verbatim **finished-track outcome** — what a reader can do after all nine lessons — while `aidc-campus`'s is a running "So far: …" enumeration. Row 9 extended the latter; row 6, the other insertion into this track, correctly left the former alone. This row left it alone too and **recorded the reasoning** rather than repeating the judgement call, so the next insertion into either track has a rule instead of a precedent
- **`reviewBy` took the six-month default (2027-03-15) with FOUR candidates rejected in writing**, the read-don't-sort discipline applied for a fifth consecutive lesson: a counterparty's forced-exit right (31 May 2029) is a contractual option on one developer's balance sheet rather than a rule taking effect; a build-and-transfer ownership handover (March 2029) is a project delivery schedule, the class row 9 rejected because a date that moves *confirms* the lesson's claim; a commission decision "expected end-2026" is a window rather than a date; and a state co-location exemption dated 1 January 2025 has already passed
- **The render was the POSITIVE test, not the negative one, and that is the shape of every all-public lesson.** The real `handleClassroomOp_` answered `cop=lesson` with `success: true` at **analyst**, the index showed the lesson among 38 visible cards, and `market-access` returned the walk `who-buys-storage > how-a-storage-project-happens > how-a-utility-buys` with `withheld: 0`. Seven sections rendered at 1400px with **zero page errors**; every section's screenshot was read
- **The "edit BOTH halves" rule has a second edge, found by falling over it.** §7.27's counters were refreshed at v05.86r — but that same handover commit added a CHANGELOG section, which made §7.27's *changelog arithmetic* paragraph stale by one in both halves. Caught and corrected in this handover. **A handover commit that moves a number the brief quotes must refresh that number too, not only the counters it was opened to fix**

#### Active context

- **Repo version** v05.87r · **CHANGELOG 89 raw / 80 non-exempt**, nine sections dated 2026-09-15, well under 100. **The next session will not rotate**; re-count on the EST clock anyway
- **The EST/UTC trap did NOT fire — both clocks read 2026-09-15 again.** Still a convergence, not a fix; it diverges for any session running between 20:00 EST and midnight UTC
- **Standing changelog state:** `Classroomgs.changelog.md` is now **the closest of the three to arming** — 41 raw / 38 non-exempt against 50, and **41 non-exempt the moment EST rolls past 2026-09-15**, about eight sessions of headroom. `Scrapergs.changelog.md` rotated at v05.83r and sits at 27 raw / 26 non-exempt, no longer armed. `Profilerhtml.changelog.md` sits at 49/50
- **GAS versions:** Classroom **v01.41g**, Scraper v02.06g, Profiler v01.39g. **Page versions:** Classroom v01.14w, Profiler v01.90w, Scraper v01.72w — no page bump this session
- **Content-checker baseline is 0 errors / 0 warnings at 40 lessons / 8 tracks / 142 gate cases** — protect it. **Its guidance-module assertion is still 14 and rises to 15 on the next S2 session**; a Phase 4 row must never touch it
- **Deployment counters: Classroom 52/200, Scraper 150/200, MEASURED.** Classroom confirmed on the first GET leg with no POLL and no `?op=deploy` probe — the ninth consecutive clean use of the §7.19 order. The Scraper step never fired at all (see Key decisions)
- **`--check` reports 8 due** — `in-hall-power`, `clean-firm-and-nuclear`, `compute-and-the-rack`, `epc-and-construction`, `storage-developers-and-ipps`, `aidc-developers-and-landlords`, `hyperscalers-and-ai-labs`, `neoclouds`. **The next session's own segment is the sixth of those**, which is why its arithmetic is 8 → 8 → 7 rather than a rise
- **`check-classroom-curriculum.py --strict`** reports no structural findings, **28 stale pins** (pre-existing and unchanged — all sixteen of this lesson's pins are fresh) and **1 item due for review** — `landscape-utilities-2026-09` at 2026-10-01, by design. **A second joins it on 28 October** when `landscape-power-conversion-and-rack-power-silicon-2026-09` (`reviewBy` 2026-11-27) enters the 30-day horizon; both exit 0, because review dates never call `strict()`. Both belong to the quarterly guidance Routine
- **Toggles:** `START_OF_RESPONSE_BLOCK` On · `CHAT_BOOKENDS` Off · `TIMING_ESTIMATES` On · `END_OF_RESPONSE_BLOCK` On · `MULTI_SESSION_MODE` Off
- **Where the programme stands, measured 2026-09-15 at v05.86r.** Done: Phases 0, 1, 2a, 2b, 3, 5, 6; S0, K1, S1, S3, G6, C3, the bankability review, the one-off segment regeneration. Open: **Phase 4 at 10 of 26**, **S2 at 5 of ~19**, **K2** (1–2 sessions, runnable any time), **C5** (~6 sessions, gated on ~6 landscapes — **five now exist**), the **Q plan clock** (next due ~2026-12). Deferred: C6. **About 37 sessions remain**
- **Open, recorded, not taken.** The `.cl-tbl` `min-width` question now has **twelve** clean data points — this session's 4 × 8 table added the **first case of a `{{term}}` span in the first column**, which wraps at word boundaries like any other cell, so column count, row count and tooltip spans are all excluded and only the v05.57r single-long-unbreakable-word case stands. It should be **closed**; the generated `read-next` link is a `Classroom.html` renderer feature (`clFmt` has no link token) — **both want the same session, one that edits `Classroom.html`, plus a developer `--all` regeneration run**. `the-800-vdc-shift`'s stale `three-chains` intro is now **five inserts stale** and stays reassigned away from the Phase 4 queue (§7.25); row 10 acquired no claim on it either, since it inserted into `market-access` while that lesson sits in `aidc-grid-to-chip`
- **§10.6's session-4 bullet block still sits out of chronological order** (session 4's appears before session 3's). Cosmetic; a future session can fix it deliberately
- **Render harness — the recipe, now confirmed at analyst as well as contributor.** Run the PROJECT region of `Classroom.gs` under Node with GAS shims; shim **only** `validateSessionForData` plus no-op `clProgressRead_` / `dataAuditLog` / `auditLog`, and let `handleGuidanceOp_` / `handleClassroomOp_` answer for real. Serve a scratch copy over `http://127.0.0.1` with `_e = ''` and `AUTO_REFRESH = false`; copy `profiler-data/`, `sounds/`, `images/` **and `favicon.ico`** in. Seed `sessionStorage` **after** load (`Classroom_gas_session_token` · `_gas_user_email` · `_gas_user_role` · `_gas_user_permissions`); override `_gasPost` **after** load and return a **Promise**. **Hide** `#auth-wall`, `#signing-out-wall`, `#auth-timers`, `#gas-layer-toggle` plus `#cl-app{display:block !important}` — never `remove()`. Entrypoints: `clShowLesson(id)` for lessons, `clShowGuidanceDoc(id)` for guidance. Section nodes are `#cl-<sectionId>`. `pip install playwright`; launch with `executable_path` from `glob('/opt/pw-browsers/chromium-*/chrome-linux/chrome')`. **`sess.permissions` must be an ARRAY** and **the session token at least 32 characters**, or the real guard returns `SESSION_EXPIRED` before any shim is consulted
- **The two console errors in every render are environmental and pre-existing**, both `ERR_CERT_AUTHORITY_INVALID` because the sandbox proxy's CA is untrusted by Chromium. Neither is a module asset and neither raises a page error

#### Recommendation for next session

- Run **S2 session 6 — the `aidc-developers-and-landlords` landscape module** on Opus 5 xhigh. Its brief is **`INTEGRATED-REMEDIATION-PLAN.md` §7.27**, written at the close of Phase 4 row 10 and refreshed twice in the same session, so **both halves carry measured deployment counters and a corrected changelog count** — nothing in it is a forecast. It restores the §7.4 item 6 interleave at 10:5, its segment is already measured (**30 members: 9 · 13 · 8**), its due-count prediction (**8 → 8 → 7**) is the second run of the netting rule S2 session 5 discovered, and its hardest part is genuinely new: this is the first landscape that must split against a **public mechanism lesson** as well as against three guidance modules.

**To continue:** type `run S2 session 6`
