# Previous Session Context

Claude writes to this file when the developer says **"Remember Session"** — capturing enough context for a future session to pick up the train of thought quickly. This is separate from "Reminders for Developer" (REMINDERS.md), which is the developer's own notes.

> **Note on stale-context auto-reconstruction** — when a session starts and this file's `Repo version:` doesn't match the current repo version, Claude reconstructs the missing entry from CHANGELOG.md and commits it **without pushing**. The commit rides along with the session's first user-task commit on the next push. If a session ends before any user-task push happens, the reconstructed entry stays **local-only** and the next session will just re-reconstruct from CHANGELOG if still stale. This is intentional — pushing a dedicated reconstruction commit on its own would force every subsequent user push in the same session to wait for the auto-merge workflow to finish before it could push too (push-once enforcement). The reconstructed entry is a convenience hint, not load-bearing state, so the small persistence risk is a fair trade.

## Latest Session

**Date:** 2026-09-03 03:54 PM EST
**Repo version:** v04.45r (started at v04.44r — one push commit, `7dec728`, merged to `main` within minutes; plus this context write)
**Branch:** `claude/phase-b1-southern-entergy-xeztnd` — restarted from `origin/main` (`git checkout -B … origin/main`) before this context commit because the workflow had merged and swept it
**Model:** Fable 5.1 High — the first Phase B session, run at the §2 model as planned; **no substitution**

**What we worked on — Phase B1 of `PROFILER-COVERAGE-PLAN.md`, Southern Company (Georgia Power) and Entergy, two `utility` dossiers + two study guides in one data-only commit (v04.45r):**

- **`southern-company.profile.json`** — schema v7, profileVersion 1, intel-briefing: BLUF, seven confidence-tagged judgments, collection-gap line, dated INDICATORS TO WATCH. Eight product lines (regulated service + the large-load contracting framework; the December 2025 all-source certification of 9,885 MW; the utility-owned BESS program + the 500 MW storage RFP; the nuclear fleet and Vogtle; transmission + the $26.54B DOE loan guarantees; Alabama Power and Mississippi Power; Southern Power; Southern Company Gas), seven banded spec groups verbatim from the PSC releases, the Rules and Regulations for Electric Service, the 8-Ks and the decks; FY2024–Q2 2026 vs consensus with the contracted-load ladder as `mw-contracted` (8 → 10 → 11 → 17 GW, the company's definition of "contracted" carried, the 75 GW prospective figure never counted); 51 developments; eight relationships (OpenAI, Tesla, Burns & McDonnell, Wärtsilä, Microsoft, Meta, NextEra Energy Resources, Dominion); five policy exposures; twelve decision makers, twelve headshots; **118 sources at 64% first-party**
- **`entergy.profile.json`** — seven product lines built around the single-customer certificate (Louisiana U-37425 approved 2025-08-20 4–1 and U-37882 filed March 2026 for 5,278 MW / $12.9B with a December 2026 decision; Arkansas Cypress Solar + Jefferson for Google; Mississippi's three pre-certified 754 MW plants for AWS; Texas as the control case; the solar/storage RFP program; the nuclear fleet; the turbine exclusivity agreement raised 15 → 21 → 27 power islands; transmission, resilience and storms), seven spec groups, FY2024–Q2 2026 vs three trackers' divergent consensus (Q2: Zacks $0.94 / MarketBeat $1.01 / Investing.com $1.13), 47 developments, four relationships (Meta, Google, Amazon, Dominion), five policy exposures, eleven decision makers with headshots; **94 sources at 56% first-party**
- **`southern-company.study.json`** (15 sections, 109 `{{term}}` spans) — the elected commission; who decides in Georgia with the missing RTO row; its own grid; the pipeline in tranches (`callout`); certifying a decade; the large-load rule vs GS-5 (`table`); the freeze and the fuel line; Vogtle as a `timeline`; four channels; who picks the battery (`proscons`); gigawatts on the Georgia clock (`bars`); where Southern slots in; `where-it-fails` (§5 row 1); 14 flashcards; 10-item quiz
- **`entergy.study.json`** (15 sections, 111 spans) — one parent, five utilities; four commissions and a council (`table`); the formula rate plan worked once (`callout`); MISO South; buying for one campus; anatomy of an order that names the customer (`table`, U-37425 vs U-37882 clause by clause); who pays when the customer pays (`proscons`); storms and securitization; three states, three campuses; the turbine slot; megawatts on the Entergy clock (`bars`); where Entergy slots in; `where-it-fails`; 14 flashcards; 10-item quiz
- **27 shared concepts** (224 → 251): `AFUDC`, `balancing authority`, `capacity factor`, `CECPN`, `construction monitoring`, `cost cap`, `CWIP`, `demand charge`, `elected commission`, `ESA`, `energy charge`, `formula rate plan`, `fuel clause`, `holding company`, `interruptible`, `load factor`, `MISO`, `MTEP`, `nuclear uprate`, `planning reserve margin`, `PRA`, `rate freeze`, `regulatory lag`, `retail choice`, `securitization`, `SEEM`, `self-build`; `simple cycle` gained the `peaker` aliases
- **Bookkeeping**: two lesson plans (five modules + self-test each, both assuming the Dominion plan as prerequisite); 23 company headshots (188 → 211 across 51 companies); registry entries after Dominion + sync (95 companies; the Utility chip counts 3); graph 540 → 555 edges; calendar rows `southern-company` 2026-10-29 and `entergy` 2026-11-04, both `confirmed: false`, both with a "Chesterton check" watch item; README tree entries; §8 rows → `v1 · v04.45r` / `✓ · v04.45r`; CHANGELOG **98/100**
- **Verification**: registry `--check`, study checker (68 guides / 251 concepts), a local schema-v7 validator and Playwright with the `verify-profiler-roles.py` stub — nine tabs each, 8 + 4 curated relationship cards with resolved sources, every tooltip resolving on hover, zero page errors, zero console errors

**Where we left off:**

- **Phase B1 is on `main` (v04.45r).** Next: **Phase B2 — Oncor, AEP and Xcel Energy — on Fable 5.1 High**, three `utility` dossiers + guides in one commit per §4 and §8. Three regulatory stories now exist publicly and B2 must not repeat them: Dominion (PJM tariff state, the chain itself), Southern (elected commission, no RTO, certifying a decade), Entergy (holding company, formula rates, the single-customer certificate). Oncor is the ERCOT wires-only case (SB 6, the 474 GW queue, no generation to certify), AEP the multi-state RTO holding company with the most corpus mentions, Xcel the Midwest/Mountain IRP state
- **Sequencing during agent waits worked a fifth time**: the 27 concepts, all conceptual guide sections, both `where-it-fails`/flashcards/quiz sets, the Playwright harness and the v7 validator were built while the four agents ran (~10, ~15, ~18 and ~20 minutes); the dossiers were assembled from Python builders (skeleton + appended data blocks, ~200 lines per Edit) after the reports landed. Total 53 minutes against a 150-minute revised estimate — the builders are faster than hand-writing JSON
- **Environment notes for B2**: `pip install playwright pillow` each container (Chromium is pre-installed at `/opt/pw-browsers/chromium`); `git fetch origin main` before the rebase check; **four subagents exhaust the session's 200-call WebSearch budget** — after they return, gap-fills are curl/WebFetch only, so anything the agents must find has to be in their prompts (next-earnings date, headshot URLs, a URL for every relationship claim); the Playwright harness must `page.reload()` between dossiers because the Study Guide overlay stays open across hash navigation and intercepts tab clicks; the tooltip probe reads `#gd-tip` (an id, not a class) and needs ~40 ms after `scroll_into_view` before `mouseover`; the harness-created remote branch is swept by the workflow before the first push, so `ls-remote` returns empty and the push creates it. Blocked hosts this session: selc.org, georgiarecorder.com, datacenterdynamics.com, insideclimatenews.org, wabe.org, alabamareflector.com, lailluminator.com, arkansasonline.com, cnbc.com, mississippitoday.org, businesswire.com, spglobal.com, moodys.com, linkedin.com (999), zacks.com, nasdaq.com (503), interchange.puc.texas.gov (503); psc.ga.gov and lpscpubvalence.lpsc.louisiana.gov PDFs fetched; sec.gov, PR Newswire, s27/s201.q4cdn.com worked. Expect ERCOT/PUCT (interchange.puc.texas.gov) to be flaky for Oncor
- **The model-rule verdict given at B1's close**: research yield (source counts, first-party shares) and schema compliance were inside the A1–A4 range; judgment-level discipline held (a Google relationship declined for want of a source, two unverifiable URLs dropped, three divergent consensus figures carried, the Georgia rule described by date after neither agent could confirm its citation). What read thinner was attributable to the subjects — redacted ESA terms, undisclosed turbine OEMs and PPA counterparties — not the model. **§2 stands: B2 on Fable 5.1 High**
- CHANGELOG at **98/100**; rotation fires on the third push from here (budget ~10 minutes in that session, likely B3 or B4). The §6 register re-check is owed at the Phase B close, not per session. The earnings desk Routine and the weekly C2 pipeline remain live and unreviewed; sessions D and E of `IMPROVEMENT-PLAN.md` not started

**Key decisions and positions taken:**

- **A pipeline figure is carried with the company's own tranche vocabulary and definition, never collapsed** — Southern's contracted / finalizing / late-stage / prospective ladder and Georgia's separate "committed" (signed request for service) are all in the dossier with their definitions; only the contracted rung gets the `mw-contracted` KPI
- **A regulator's own document outranks press, and an intervenor's quotation of a sealed contract outranks nothing** — the U-37425 terms are carried as "per the intervenors' motion" and "per the company's testimony" because the order text was unreachable, and the guide's order-anatomy table says so in its intro
- **A rule citation neither agent could confirm in a primary document is not written** — Georgia's large-load rule is described by adoption date and docket of origin, with a calendar watch item to verify the citation next refresh
- **A relationship needs a source that explicitly states it** — Georgia Power–Google was declined; Entergy–NextEra Energy Resources (a real 2024 JDA per the agents) was dropped for want of a verifiable URL after the search budget ran out
- **Month-dated developments get the first of the month plus a "(month-dated; day not stated)" tag** rather than a fabricated day
- **Study guides for later utilities teach the state's own regulatory story against the Dominion chain** — Southern got the elected commission and the no-RTO case, Entergy the holding company and the one-customer certificate; B2 gets ERCOT wires-only, the multi-state RTO holding company and the Midwest IRP state
- **A data-only change lists the Profiler page as an indirect affect** — no page or GAS file, no version bump

**Active context:** `TEMPLATE_DEPLOY` Off · `MULTI_SESSION_MODE` Off · `CHAT_BOOKENDS` Off · Profiler `v01.81w` / `v01.34g` · Classroom `v01.07w` / `v01.16g` · 95 dossiers · 68 study guides · concepts registry 251 · graph 555 edges · calendar 56 rows · CHANGELOG 98/100 · no active reminders · TODO empty

**Recommendation for next session:**

- **Run Phase B2 — Oncor, AEP and Xcel Energy — on Fable 5.1 High** as a fresh session: `profiler <Company>` + `profiler prep <Company>` for each, all three `utility`, one push commit, data-only, per `PROFILER-COVERAGE-PLAN.md` §4, §7 and the three §8 rows. Six research subagents will exhaust the search budget, so every must-find item (next-earnings dates, headshot URLs, a URL for every relationship) goes in the agent prompts. The guides teach ERCOT wires-only + SB 6 (Oncor), the multi-state RTO holding company (AEP) and the Midwest/Mountain IRP state (Xcel) against the Dominion, Southern and Entergy stories already public.
**To continue:** type `run Phase B2 of PROFILER-COVERAGE-PLAN.md — Oncor, AEP and Xcel Energy — on Fable 5.1 High`

## Previous Sessions

**Date:** 2026-09-03 02:41 PM EST
**Repo version:** v04.44r (started at v04.43r — one push commit, `394c079`, merged to `main` within minutes; plus this context write)
**Branch:** `claude/phase-a4-vicor-profiler-2fmo2w` — restarted from `origin/main` (`git checkout -B … origin/main`) before this context commit because the workflow had merged and swept it
**Model:** Fable 5.1 xhigh — the fourth and last Phase A anchor session; **Phase A is closed**

**What we worked on — Phase A4 of `PROFILER-COVERAGE-PLAN.md`, Vicor, dossier + study guide + the phase-end §6 register re-check in one commit (v04.44r):**

- **`vicor.profile.json`** — schema v7, profileVersion 1, `supplier`, intel-briefing: BLUF, seven confidence-tagged key judgments, a collection-gap statement, a dated INDICATORS TO WATCH bullet. **Ten product lines** with all depth fields (FPA — PRM + VTM and the Sine Amplitude Converter; Power-on-Package and VPD incl. the 2nd-gen VPD that exists first-party only as CEO sentences; BCM 800 V/400 V → 48 V; NBM 48 V ↔ 12 V; DCM; PFM/ZVS bucks; the IP licensing practice; automotive 48 V; A&D/Brick; the ChiP platform + Andover fab), **seven banded spec groups** verbatim from datasheets, financials FY2024–Q2 2026 vs tracker consensus, 34 developments, four relationships (Delta as competitor via the ITC record; NVIDIA, Google, Tesla historical per SemiAnalysis), two policy exposures (Section 337 as complainant; CHIPS §48D), twelve decision makers with eleven company headshots, **136 sources at 57% first-party**
- **The litigation as the tribunals state it**: the 13 Feb 2025 ITC determination (337-TA-1370) read from the Commission's own Federal Register notice — LEO + CDOs on the '481/'761 patents, *and* Delta's redesigns found non-infringing and Foxconn's FII USA/Ingrasys found licensed; both 2023 district cases terminated May 2025 beside a $45M settlement with unnamed parties; the second ITC case (337-TA-1484, one patent issued Aug 2025, 15 respondents incl. MPS, Wistron, Wiwynn, Quanta) instituted 11 Feb 2026 with a final ID due 20 May 2027; MPS/Delta trade-secret counter-suits (July 2026) known only from a lede and a snippet and labeled so
- **`vicor.study.json`** — schema v2, **17 sections**: the last centimetre; amps per kilowatt (`bars`); sixteen-to-one arithmetic (`callout`); the 48 V bus and the 60 V line; two-stage vs direct vs factorized (`table`); current multiplication and the sine-amplitude idea; the PDN at one volt; lateral vs vertical (`proscons`); 800 V → 48 V → chip (`table`); the battery on the DC bus; living on a shared bus (`callout`); where Vicor slots in (`table`); amps per package (`bars`); who buys / who competes / the licence; `where-it-fails` aligned to §5 rows 9–11; 14 flashcards; 11-item quiz. 91 `{{term}}` spans, no local glossary
- **30 shared concepts** registered (193 → 224): `buck converter`, `bus converter`, `conversion efficiency`, `current density`, `current multiplier`, `current sharing`, `design win`, `factorized power`, `galvanic isolation`, `hot-swap`, `I²R loss`, `inrush`, `intermediate bus`, `IP licensing`, `lateral power delivery`, `load transient`, `ORing`, `PDN`, `point of load`, `power density`, `precharge`, `PWM`, `Section 337`, `SELV`, `sine amplitude converter`, `soft switching`, `substrate`, `switching frequency`, `thermal resistance`, `vertical power delivery`, `voltage regulation`
- **`study-prep/vicor/vicor-lesson-plan.md`** — five modules (current squared; factorized power and the multiplier; the last centimetre and vertical delivery; 800 V to the chip and the battery on the bus; Vicor against the chain) plus a ten-question self-test
- **Bookkeeping**: registry entry after Vertiv + sync (136 / 57% / kpiNorm true); graph 528 → 540 edges (12 touch `vicor`); calendar row `nextReport` 2026-10-20 / `confirmed: false` (no company date; trackers split Oct 20 vs 27; matches the 8-K cadence); README tree entries + execs count (188 / 49); §8 row A4 → `v1 · v04.44r` / `✓ · v04.44r`; CHANGELOG **97/100**
- **Phase A register re-check (§6 of `CLASSROOM-CURRICULUM-PLAN.md`)** — every row re-run and dated: **G2, G3, G5, G9 Open → Partial** (each anchor closes one half; the second halves are C1 Cummins/Rolls-Royce, the Vertiv/Schneider UPS revisions (unscheduled), B1–B2 utilities, the Narada backfill); G4, G6, G7, G8, G10 unchanged; G11, G12 as judged; a "where the register stands after Phase A" paragraph added; the next re-run is owed at the Phase B close
- **Verification**: registry `--check`, graph `--check`, study checker (66 guides, 224 concepts) and a local schema-v7 validator all clean; Playwright on localhost with the GAS `nop=whoami` stub as admin and the Google sign-in loader **fulfilled with an empty script rather than aborted** — nine dossier tabs, four relationship chips with resolved sources, the Study Guide with 17 sections and 91 resolving tooltips, **zero page errors and zero console errors**; the eleven headshots checked on a contact sheet
**Where we left off:**

- **Phase A is closed on `main` (A1 Caterpillar v04.41r, A2 Piller v04.42r, A3 Dominion v04.43r, A4 Vicor v04.44r).** Next: **Phase B1 — Southern Company (Georgia Power) and Entergy — on Fable 5.1 High**, two `utility` dossiers + study guides in one commit, per §4 and §8 of `PROFILER-COVERAGE-PLAN.md`. The `utility` category, its compare peer family and the Dominion guide (17 sections) already exist — the B1 guides should teach each utility's own regulatory specifics rather than re-teach the revenue-requirement equation and the IRP → CPCN → RFP chain that `dominion-energy.study.json` carries
- **Sequencing during agent waits worked a fourth time**: the 30 concepts, 11 of 17 guide sections, the where-it-fails/flashcards/quiz, four of five lesson-plan modules, the §6 register re-check, the README/§8 edits, a schema-v7 validator and the Playwright harness were all built while the two agents ran (~24 and ~21 minutes; the reports arrived after the stop hook's first prompt, which was ignored as planned). Post-report work was assembly, validation and the commit cycle (~50 minutes, most of it the dossier builder)
- **Environment notes for B1**: `pip install playwright` **and** `pip install pillow` each container (Pillow is needed to resize headshots; Chromium is pre-installed, never run `playwright install`); `git fetch origin main` before the rebase check (the local ref was stale again); the stop hook fires on the uncommitted tree while agents run — ignore its first prompt; for the Playwright check mirror `scripts/verify-profiler-roles.py` exactly — the GAS stub must answer `nop=whoami` with `{success, role, isAdmin}`, the seed key is `ov_note_session` (48 chars) with `ov_note_role` removed, wait for `ov_note_role` to appear, and **fulfill** `accounts.google.com` with an empty JS body instead of aborting it or the page throws two sandbox-only `gis_load_failed` errors; the Study Guide opens from `#ov-study-btn` on the dossier, tabs are `#ov-tab-<key>` (`rels` for Relationships), tooltips are `.gd-term`. Blocked hosts this session: Vicor's IR host (gcs-web, 403/503), usitc.gov, federalregister.gov HTML (the API worked), Justia, Nasdaq, Zacks, statesman.com; govinfo, sec.gov, CourtListener-via-curl and PacerMonitor worked. Expect utility IR sites and state-PSC dockets to be JS-rendered or slow — search located the decks on A3
- **The xhigh test, fourth data point and the verdict for the developer**: across A1–A4 the visible xhigh contribution was guide design (physics before products, the arithmetic and table sections, failure sections aligned to §5) and judgment-level source discipline (on A4: the ITC order carried with its two limiting findings, Megmeet/LITEON declined as relationships for want of a source, the 2nd-gen VPD carried as CEO sentences, the TDK-Lambda false graph edge caught). Research yield and schema compliance were indistinguishable from High on all four — the agents inherit the model and their sweeps would be the same. §2 already assigns Phase B to Fable 5.1 High; the recommendation given at A4's close was to run it there as written and fall back to Opus 5 xhigh only if the weekly cap binds, recording any substitution in §8
- CHANGELOG at **97/100**; archive rotation is mandatory above 100 and will fire on roughly the fourth push from here — inside Phase B (budget ~10 minutes in that session). The earnings desk Routine and the weekly C2 pipeline remain live and unreviewed; sessions D and E of `IMPROVEMENT-PLAN.md` not started

**Key decisions and positions taken:**

- **A company absent from an ecosystem's published partner lists gets that absence stated as a fact in the summary, a development per list, and a moderate-confidence judgment** — never inferred into a relationship; the covered partners on those lists (Delta, LITEON, Megmeet) become `competitor` links only where a source connects them to the subject, so Megmeet and LITEON were declined
- **A product that exists first-party only in executive sentences is carried as such** — a product line whose description says so, a spec group whose notes say so, no invented part numbers; trade-press transcript figures are labeled as relayed
- **A tribunal's own document outranks the company's release about it** — the ITC determination is cited from govinfo's Federal Register text; Vicor's release is a development source; Vicor's filings, which never describe the ITC cases, are cited for what they do say (the $45M settlement, SynQor, the class action)
- **Historical customers from a single independent history (SemiAnalysis) are curated as `customer` / `historical` with the single-source caveat in `context`** rather than omitted — the corpus otherwise has no Vicor mention at all, so the inbound side of every edge is empty
- **A derived graph edge from a brand fragment ("TDK-Lambda" → `lambda`) is fixed in the prose, not the build script** — the derivation mirrors the app's and must not diverge
- **A data-only change lists the Profiler page as an indirect affect** — this session was data-only (no page or GAS file, no version bump)

**Active context:** `TEMPLATE_DEPLOY` Off · `MULTI_SESSION_MODE` Off · `CHAT_BOOKENDS` Off · Profiler `v01.81w` / `v01.34g` · Classroom `v01.07w` / `v01.16g` · 93 dossiers · 66 study guides · concepts registry 224 · graph 540 edges · calendar 54 rows · CHANGELOG 97/100 · no active reminders · TODO empty

**Recommendation for next session:**

- **Run Phase B1 — Southern Company (Georgia Power) and Entergy — on Fable 5.1 High** as a fresh session: `profiler Southern Company` + `profiler prep Southern Company`, then `profiler Entergy` + `profiler prep Entergy`, both `utility`, one push commit, data-only, per `PROFILER-COVERAGE-PLAN.md` §4, §7 and the two §8 rows. The guides should be each utility's own regulatory story (Georgia's PSC and the 2025 IRP/certification cycle; Entergy's four-jurisdiction footprint and the Louisiana large-load approvals) against the Dominion guide's already-taught chain, so `how-a-utility-buys` gains second and third public sources rather than duplicates.
**To continue:** type `run Phase B1 of PROFILER-COVERAGE-PLAN.md — Southern Company and Entergy — on Fable 5.1 High`

Developed by: LightAISolutions
