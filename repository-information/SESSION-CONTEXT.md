# Previous Session Context

Claude writes to this file when the developer says **"Remember Session"** — capturing enough context for a future session to pick up the train of thought quickly. This is separate from "Reminders for Developer" (REMINDERS.md), which is the developer's own notes.

> **Note on stale-context auto-reconstruction** — when a session starts and this file's `Repo version:` doesn't match the current repo version, Claude reconstructs the missing entry from CHANGELOG.md and commits it **without pushing**. The commit rides along with the session's first user-task commit on the next push. If a session ends before any user-task push happens, the reconstructed entry stays **local-only** and the next session will just re-reconstruct from CHANGELOG if still stale. This is intentional — pushing a dedicated reconstruction commit on its own would force every subsequent user push in the same session to wait for the auto-merge workflow to finish before it could push too (push-once enforcement). The reconstructed entry is a convenience hint, not load-bearing state, so the small persistence risk is a fair trade.

## Latest Session

**Date:** 2026-09-17 09:22:06 PM EST
**Repo version:** v06.46r — one push, merged, then this handover
**Branch:** `claude/phase-4-row-24-bankable-means-c9ife0` — restarted from `origin/main` before the push

### What was done

- **Phase 4 row 24 — the `what-bankable-means` mechanism lesson (v06.46r). THE FIFTY-FOURTH LESSON, THE FIFTH GATED LESSON PHASE 4 HAS AUTHORED AND THE FOURTH IN `market-access`** — the first track in the programme to show an analyst a withheld count of **four**. `clLessonWhatBankableMeans_()`, id `what-bankable-means`, title *The Independent Engineer's Report*, group Market Access & Bankability, authored **inside** the `// CONTENT START` fence, registered at the **end** of `clLessons_()` and **inserted at index 5** of `clTrackMarketAccess_()` — the programme's **fifth mid-array insert**, verified against the live array before the write
- **Seven sections at §3.5's ids and order** — `the-ie-report` (prose, 6 ps) · `the-contract-skeleton` (**table 4×5**) · `the-scorecards` (prose, 6) · `the-rfp-checklist` (**table 3×6**, the module's ten standing requests regrouped into six folders) · `where-it-fails` (callout, 6 items) · `drill` (6 cards) · `check-yourself` (5 items at **0·3·1·2·1**, ranks 2·2·2·3·3, **0 of 5 sole-longest and 0 of 5 sole-shortest**). Four tiles, **eight glossary entries all used**, **95 `{{term}}` uses over 48 distinct, all resolving**. **100 % ASCII**
- **The module assertion stayed at 28** and **nothing was regenerated**. `--check` went **6 → 9 due**, derived from the parsed generator before the write and **exact on both halves**
- **§6's and §7.3's Phase 4 rows flipped to 24 of 26**, curriculum §7 row 24 marked built, §4's table updated at `market-access` position 7 (leaving **position 4 alone**), **findings (pp1)–(pp8)** written, and **§7.55 written in both halves** for Phase 4 row 25
- **Classroom GAS v01.69g → v01.70g**; Scraper and Profiler untouched; page versions unchanged

### Where we left off

Everything is merged and live. **Phase 4 is 24 of 26 and S2 is closed**, so the recommended next run is **Phase 4 row 25, `reading-the-numbers`**, briefed in **§7.55**, written this session. Content checker **0/0 at 54 lessons / 8 tracks / 142 gate cases**, module assertion **28**. `--check` reports **9 due**.

### Key decisions made

- **THE `(planned)` LEAK IS NOT A PROPERTY OF GATED REGISTRATIONS — (pp1), and the brief's arithmetic was wrong in both halves.** §7.54 said *one segment leaks and row 24 will take it to five*. Grepping every generated `segment-*` literal for `*<title>* (planned)` where the title is **registered** returns **25 false renders across 9 segments over 10 titles** after this commit and **15 across 6** before it. This row contributes **10 renders across 4 segments** for one title — the largest single-title leak the register has seen, **2.5× the next**. The decisive number is the split: **five of the ten leaked titles are gated and five are public**, so the gate is irrelevant and the leak is the standing cost of any registration landing after a segment's last regeneration. **Count renders, not segments.**
- **(oo7) FIRED ONCE AND IT FIRED ON AN EDIT MADE TO FIX SOMETHING ELSE — (pp2).** The `clFmt` transcription passed its four-probe self-test first time, so (oo6) did not recur. What did recur is the contract it found: correcting an inconsistent sibling reference in a table `note` dropped an italic lesson title into a sentence that was **already bold end to end**, which `[^*]+` cannot span. **The edit most likely to break a markup rule is the one made to satisfy a different rule** — re-run the markup pass after every fix round, not only after the draft.
- **A SELF-REFERENTIAL COUNT WAS WRONG TWICE, AND THE SECOND TIME WAS THE CORRECTION — (pp3).** The callout's `note` claimed a taxonomy of its own six items; the recount rewrote it and **the screenshots caught the rewrite too**, because the correction was computed from memory of the list rather than by re-walking it. The real distribution is **one contradiction, two wrong-object, one unsupported assumption, one bid structure, one channel fit**. **A correction is a fresh assertion and inherits none of the first one's checking.**
- **(nn1)'s CHEAPER RULE HELD EXACTLY AND BOTH HALVES COLLAPSED ONTO THE SAME FOUR SEGMENTS — (pp4).** 4 of 19 segments name the lesson in `READ_NEXT`; 3 of 27 `CRITERION_LEXICON` entries target it; **all four members also carry a matching criterion**. One was already due, so **6 → 9**, every entrant differing in both sections and the already-due member's line unchanged. **(aa5) holds for a SEVENTEENTH session.**
- **THE DENIAL CHANNEL IS A TRANSITION, NOT A STATE — (pp5).** Same analyst token, same call: `cop=lesson` answered **`UNKNOWN_LESSON` at base and `ROLE_DENIED` at head**. Every previous gated row recorded only the head state. **Registration publishes the day a gated lesson was created** to anyone probing ids, while the analyst index stays **byte-identical at 42,265 characters**.
- **THE `reviewBy` SORT INVERTED (ii7)/(nn8) — (pp6).** An ISO sweep found **one** future date and a word-form sweep **two**, and the one only the word-form sweep sees — **2026-10-01, PRC-029-1's effective date** — is the **grid** gauntlet this lesson excludes by name. **One rejection in writing**, and the inherited **2027-01-01** is attested three ways inside one module. **A wider sweep is worth running even when it cannot change the answer.**
- **§10.6 (t) FIRED IN TWO DIRECTIONS AT ONCE — (pp7).** `the-certification-stack`'s header comment declared *the MONEY gauntlet … is not previewed here, not even by title* one day earlier and the check confirms it held. But **`duration-and-degradation`, public and two tracks away, already carries the literal example §3.5 names for this lesson's callout** and declares nothing, because it was authored long before this row was scheduled. **A declared handoff protects the neighbour it names; only a corpus-wide grep protects the ones it does not.**

### Active context

- **Repo version v06.46r** · **`CHANGELOG.md` 114 raw / 97 non-exempt** against a 100 trigger with **seventeen** sections dated 2026-09-17 EST; `Classroomgs.changelog.md` **57 raw / 46 non-exempt** against a cap of 50 with **eleven** same-day; `Scrapergs.changelog.md` **41 / 36**; `Profilerhtml.changelog.md` **49**. **No rotation — the EST day had not rolled** (`TZ=America/New_York date` read 2026-09-17 at 09:22 PM), re-counted by hand. **A push landing 18 September or later is the rotator on BOTH `CHANGELOG.md` (114 against 100) and `Classroomgs.changelog.md` (57 against 50)**
- **GAS versions:** Classroom **v01.70g**, Scraper v02.20g, Profiler v01.39g. **Page versions:** Classroom v01.14w — no page bump
- **Content-checker baseline 0 errors / 0 warnings at 54 lessons, 8 tracks, 142 gate cases**, module assertion **28**. **Phase 4 row 25 authors a LESSON and must LEAVE the assertion at 28**
- **`--check` reports 9 due** — `cells-and-chemistry` (both), `storage-integrators-and-containers` (both), `power-conversion-and-rack-power-silicon`, `storage-developers-and-ipps` (both), `hyperscalers-and-ai-labs`, `utilities`, `capital` (both), `assurance` (both), `insurance-and-risk-transfer` (both). **Row 25's forecast is derived and is a HOLD at 9** — its two `READ_NEXT` members are both already due and the `CRITERION_LEXICON` half fires zero times
- **`check-classroom-curriculum.py --strict`** — no structural findings, **28** stale pins, **4** items due, unchanged. This lesson's 2027-01-01 sits 106 days out, outside the 30-day horizon, so it adds none
- **Pipeline signature exactly as a mid-array insert should produce: P1 × 2 on the developer paths and P5 on the track array, and NO P2, NO P3, NO P7, NO P8, NO P12.** Selftest 13 fixtures / 0 failures
- **Deployment counters, MEASURED against v06.46r's own job log — the ONE-leg forecast was right.** `Classroom deploy confirmed (GET): Updated to v01.70g (deployment 82) | 82/200` in **17 s**, with exactly **one** `Deploy <Project>` step firing and the other **seven** exiting 0 silently in zero seconds. The run closed on *All GAS deploys confirmed the merged version*. **Classroom now stands at 82/200 with 118 left; Scraper at 165/200 with 35 — still the tight one.** The §7.19 order resolved on its **two reads** for the **thirty-seventh** time and no `?op=deploy` probe was made. **The post-merge `check-readme-tree.py --fix` reported *already in sync*** — because this session ran it before committing
- **Toggles:** `START_OF_RESPONSE_BLOCK` On · `CHAT_BOOKENDS` Off · `TIMING_ESTIMATES` On · `END_OF_RESPONSE_BLOCK` On · `MULTI_SESSION_MODE` Off
- **Where the programme stands.** Done: Phases 0, 1, 2a, 2b, 3, 5, 6; S0, K1, S1, S2 (19 of 19), S3, G6 *(the register row, not the module)*, C3, the bankability review. Open: **Phase 4 at 24 of 26**, **the G6 module** (row 26 depends on it), **K2** (1–2 sessions), **C5** (~6 sessions), the **Q plan clock** (next due ~2026-12). Deferred: C6. **About 9 sessions remain**
- **`market-access` is 8 of 9 built**, **position 4 alone remaining**. **Row 25 (`reading-the-numbers`) is §4 position 4 and inserts at LIVE INDEX 3** — the array is eight long now. It is **PUBLIC**, so it changes no withheld count and the render test is the **analyst** seeing it, which inverts the last five rows
- **Open, recorded, not taken — seventeen.** (i) `.cl-tbl` `min-width` — **forty-four** data points. (ii) The generated `read-next` names a landscape module by **id** rather than title. (iii) `the-800-vdc-shift`'s stale `three-chains` intro is **thirty-three** registrations stale. (iv) §10.6's session-4 block is out of chronological order. (v) A stale `lesson_ref` can make a segment due alone — five such segments stand. (vi) **CLOSED** by row 20. (vii) `restart` scores zero for a **sixteenth** session. (viii) Session 12's six `topic-aidc-landlords` terms unseeded for a **twelfth**. (ix) (ee4)/(gg7)'s dash rows. (x) (ff4)'s answer-**index** skew and (ll3)/(mm5)/(nn10)'s answer-**length** skew — closed at both extremes on this row. (xi) (ff5)/(hh6)/(jj3)/(ll5)/(nn2)/**(pp1)** — the standing leak is **25 renders across 9 segments and 10 titles, five gated and five public**, owned by no scheduled session. (xii) `Scraper.gs` holds **twelve** `topic-landscape-*` seeds against **nineteen** landscapes — shortfall seven, **frozen**. (xiii) (jj4)'s two timelines. (xiv) (kk8)'s blindness, plural and `-ing` alike. (xv) (ll1)/(mm6)/(nn3)/(oo6) — an `assert walked > 0`, a path assertion and a renderer self-test. (xvi) (nn5)/**(pp5)** — the denial channel's existence confirmation, now known to publish the **creation date** as a base-to-head transition. (xvii) (oo7)/**(pp2)** — `clFmt` cannot render italic inside bold, and the edit most likely to trip it is a correction
- **Render harness — WORKING, rebuilt from §7.53's record and everything held.** Static files + `/__gas` from one Node process on one port; `var _e` rewritten to `c2FnX18v` in the **served** HTML; the PROJECT region loaded into a Node `vm` with twelve stubs; `handleClassroomOp_({parameter: p})`; `p.session` ≥ 32 chars with the role in the prefix; the four `sessionStorage` keys seeded **after** load per (nn4); `section.cl-sec` with id `cl-<sectionId>` for screenshots
- **This session's render: contributor 29,450 characters, 7 section nodes plus the glossary, ZERO uncaught page errors**, 95 of 95 tooltips resolving against a 4,208-key map, zero unresolved `{{…}}`, zero literal asterisks, zero backticks; analyst **denied at the server** with **55 characters** and **0 of 7** headings. PROJECT region **3,848,938 → 3,892,311 bytes** and the render **29,365 → 29,450 characters** across the splice and one fix round, so both meters were demanded and both moved
- **Defect tally: (kk7) at a SEVENTEENTH consecutive session — TWENTY-FOUR and NINE.** Seven pre-splice simulations with printed denominators (113 `clFmt` fields, 44 plain-text fields, 95 term uses over 48 distinct, 8 glossary entries, 5 quiz items, ~38,800 ASCII characters, the structural check) returned **one** failure, which was (pp2). The recount then caught **twenty-four** — sharpest, an opening paragraph claiming the report judges forecasts while the paragraph three below says the engineer does not opine on price — and the screenshots **nine more** with near-zero overlap, sharpest a table `note` promising six folders ordered by who asks and then naming the asker for **five**

### Recommendation for next session

- Run **Phase 4 row 25 — the `reading-the-numbers` mechanism lesson** on Opus 5 xhigh. Its brief is **`INTEGRATED-REMEDIATION-PLAN.md` §7.55**, written this session. Four things carry the attention: **the module assertion must STAY at 28**; **the insert index is 3** against an array that is now eight long; **`the-overlay` is specified against `PROFILER-SCHEMA.md`'s Normalized KPI fields**, so read the schema and then the actual `profile:` files whose rows the table prints; and **the render test inverts** — this row is public, so the analyst is the tier that must SEE it.

**To continue:** type `run Phase 4 row 25`

## Previous Sessions

### Previous Session — v06.45r
**Date:** 2026-09-17 06:05:00 PM EST
**Repo version:** v06.45r — one push, merged, then this handover
**Branch:** `claude/assurance-landscape-s2-19-u8n929` — restarted from `origin/main` before each push

#### What was done

- **S2 session 19 — the `assurance` landscape module (v06.45r). THE NINETEENTH AND LAST LANDSCAPE, AND S2 IS NOW CLOSED** — the first phase to end since S3 closed on 2026-09-13. `guidanceDocLandscapeAssurance_()`, id `landscape-assurance-2026-09`, lane The Value Chain, tier contributor, authored **below** the `// CONTENT END` fence and registered at the **end** of its lane — the **twenty-eighth** guidance module
- **Seven members at 4 · 1 · 2**, chain position 17 of 19. **Nine sections at §10.6's ids and order** — `who-dominates-and-on-what-basis` (prose, 11) · `who-threatens` (prose, 8) · `each-players-bet` (**table 5×3**) · `the-indicators` (**table 8×3**) · `the-sellers-play` (callout, 8) · `claims-ledger` (**ledger, 32 rows**) · `what-the-record-does-not-say` (callout, 9) · `drill` (7 cards) · `check-yourself` (5 items at **2 · 0 · 3 · 1 · 0**). Four tiles, **eight glossary entries all used**, **101 `{{term}}` uses over 17 distinct, all resolving**. **100 % ASCII**
- **The module assertion moved 27 → 28** and stops rising with S2. **`segment-assurance` regenerated** (one segment, `--today 2026-09-17`), closing the `(planned)` leak row 23 opened here. **`topic-landscape-assurance` seeded** in `Scraper.gs`, twelve terms
- **§6's and §7.3's S2 rows flipped to `CLOSED — 19 of 19`**, all nineteen struck, §10.6's progress note flipped, **findings (oo1)–(oo10)** written, and **§7.54 written in both halves** for Phase 4 row 24
- **Classroom GAS v01.68g → v01.69g; Scraper v02.19g → v02.20g**; page versions unchanged

#### Where we left off

Everything is merged and live. **S2 is CLOSED at 19 of 19 and Phase 4 is 23 of 26**, so the interleave has nothing left to alternate with: the recommended next run is **Phase 4 row 24, `what-bankable-means`**, briefed in **§7.54**, written this session. Content checker **0/0 at 53 lessons / 8 tracks / 142 gate cases**, module assertion **28**. `--check` reports **6 due**.

#### Key decisions made

- **THE COVERAGE BOUNDARY WAS A TABLE OF INSTRUMENTS, NOT A CAVEAT — (oo1), and the shape generalises.** Four instruments touch this roster and **the largest covers four of seven**: a battery-storage TIC assessment naming six majors of which four are members; a global TIC revenue league table ranking **two** (5th at USD 4,619m, 8th at USD 3,106m) and omitting three more by name; the ENR design lists covering **three others**; and, for the fourth incumbent, **nothing at all**. The two revenue-based instruments **share no company**. The sub-market claim is true and small — its USD 0.66bn is out-booked **4.6× and 7.0×** by the two firms it calls leaders. (ii1) measured a covered fraction of one ranking; this measured that **no ranking of the whole exists**, which is the commoner case.
- **THE FIRST WHOLLY COMPETITIVE ROSTER — (oo2), and the MISSING edges were the finding.** 15 of 21 pairs carry an edge; 12 curated carry **15 typings and all fifteen read `competitor`** — no partner, customer or supplier edge anywhere inside the roster, where `cooling` reached 36 of 39 and `insurance` ran 1 to 2. The six absent pairs are **exactly** the three engineering firms against the two pure certification bodies, and the two members joined to all six others are the two holding **both** kinds of authority. **The graph partitions the segment and names the two companies in both halves, without being asked** — the same boundary the league tables draw, from an artefact that does not cite them.
- **THE SEGMENT SELLS AUTHORITY IN FOUR SEPARABLE PIECES AND NOBODY HOLDS ALL FOUR — (oo3).** Write the standard · run the laboratory · issue the mark · sign the lender's report. Five members, **four distinct combinations**, and the party issuing most of its market's listings holds the **fewest**: a sister non-profit publishes the three storage standards, holds ~61.6 % of the equity and 94.1 % of the votes, and is paid **USD 22m a year** for library access out of cost of revenue while the mark goes back royalty-free.
- **THE BRIEF NAMED TWO CHANGES OF CONTROL AND THE RECORD CARRIED THREE — (oo4).** The third is an incumbent **buying** an independent NRTL and its mark for ~EUR 575m, closing the **same quarter** as the other two. **Re-deriving a brief's arithmetic ADDED a finding rather than correcting one**, which is a new outcome for (kk1)/(jj1).
- **(nn8) IN A FOURTH SHAPE — BOTH DAY-LEVEL SCANS WERE COMPLETE AND BLIND — (oo5).** The fence gives nothing (**40 entries, 17 dated, ZERO future**). The ISO sweep returned **3** distinct future days in the whole segment and the word-form sweep **1** the ISO sweep already had — and **not one of the four is any of the three deals**, because every deal is stated as a **quarter**. `reviewBy` **2026-10-31** off a month-only members' vote, taken at month end per (y5); **nine rejections**, including **2026-12-31 rejected as the downstream landscape's own gate**.
- **(mm6)/(nn3) FIRED IN BOTH DIRECTIONS ON ONE CHECK, AND THE SECOND HALF WAS THE FIX FOR THE FIRST — (oo6).** A hand-rolled asterisk heuristic reported **five** failures, four phantom. Replacing it with **the renderer's own `clFmt`** was right and introduced a new fault: `\\1` where `\1` was needed, which **silently swallowed every bold span's contents and hid a real backtick the discredited heuristic had caught**. A four-probe self-test on the transcription now runs with the simulation. **Replacing a bad instrument with a better one is exactly when to test the better one.**
- **A RENDERER CONTRACT FELL OUT OF IT — (oo7), new open item (xvii).** `clFmt`'s bold pattern is `/\*\*([^*]+)\*\*/`, whose `[^*]+` cannot span an inner asterisk, so **italic nested inside bold breaks both** and prints two literal asterisks with emphasis on the wrong words. **(mm7) covered fields that never reach `clFmt`; this is a field that does and still prints literally.**
- **(nn7) AT A SECOND FIRING, AND THE TWO PASSES DIVIDE THE LABOUR — (oo8).** Two over-broad claims replicated: *the largest incumbent* used four times for a company the section above ranks **eighth of ten**; and the module's best fact, *the second source held the scope first* — **false**, because the challenger's NRTL scope list is its **current** scope, not the 2021 renewal's (the same dossier records a 2026 grant inside that list), so the record gives **no grant date** for that member. **The recount caught both primaries; the screenshots then found the second still standing in FIVE trailing positions.** The corrected version — one party's dates are published and the other's are not — is a better finding than the false one.
- **A SEED TERM REJECTED ON PUNCTUATION, AND (nn9) IN A SECOND INSTRUMENT — (oo10).** `owner's engineer` scores zero and is the segment's second seat, but it would have been **the only apostrophe-bearing term in all 532** and `scTermsHit_` matches by exact `indexOf` — traced on the real path, the ASCII form returns **false** against the typographic apostrophe news text carries. And `accreditation`, the third buying criterion's own first word, was rejected because the registry resolves it to **ELCC** — **the same collision that would tooltip the wrong definition would score the wrong article.**

#### Active context

- **Repo version v06.45r** · **`CHANGELOG.md` 113 raw / 97 non-exempt** against a 100 trigger with **sixteen** sections dated 2026-09-17 EST; `Classroomgs.changelog.md` **56 raw / 46 non-exempt** against a cap of 50 with **ten** same-day; `Scrapergs.changelog.md` **41 / 36**; `Profilerhtml.changelog.md` **49**. **No rotation fired — the EST day had not rolled**, re-counted by hand. **A push landing 18 September or later is the rotator on `CHANGELOG.md` (113 against 100) AND past the boundary on `Classroomgs.changelog.md` (56 against 50)**
- **GAS versions:** Classroom **v01.69g**, Scraper **v02.20g**, Profiler v01.39g. **Page versions:** Classroom v01.14w — no page bump
- **Content-checker baseline 0 errors / 0 warnings at 53 lessons, 8 tracks, 142 gate cases**, module assertion **28**. **Phase 4 row 24 authors a LESSON and must LEAVE the assertion at 28**
- **`--check` reports 6 due** — `cells-and-chemistry` (both sections), `storage-integrators-and-containers` (both), `power-conversion-and-rack-power-silicon`, `storage-developers-and-ipps` (both), `hyperscalers-and-ai-labs`, `utilities`. Forecast was **7 → 7 → 6** and was exact — **(aa5) at a SIXTEENTH session**
- **`check-classroom-curriculum.py --strict`** — no structural findings, **28** stale pins, **4** items due, unchanged. This module's **2026-10-31 sits 44 days out, outside the 30-day horizon**, so it adds none
- **Pipeline signature exactly as §10.6 requires for a below-the-fence module: P1 × 4 and P2, and NO P3, NO P5, NO P7, NO P8, NO P12.** Selftest 13 fixtures / 0 failures
- **Deployment counters, MEASURED against v06.45r's own job log — the TWO-leg forecast was right.** `Scraper deploy confirmed (GET): Updated to v02.20g (deployment 165) | 165/200` in **11 s** and `Classroom deploy confirmed (GET): Updated to v01.69g (deployment 81) | 81/200` in **17 s**, with exactly **two** `Deploy <Project>` steps firing and the other **six** exiting 0 silently in zero seconds. The run closed on *All GAS deploys confirmed the merged version*. **Classroom now stands at 81/200 with 119 left; Scraper at 165/200 with 35 — still the tight one.** The §7.19 order resolved on its **two reads** for the **thirty-sixth** time and no `?op=deploy` probe was made. **The post-merge `check-readme-tree.py --fix` reported *already in sync*** — because this session ran it itself after the checker caught both GAS displays drifting by one bump
- **Toggles:** `START_OF_RESPONSE_BLOCK` On · `CHAT_BOOKENDS` Off · `TIMING_ESTIMATES` On · `END_OF_RESPONSE_BLOCK` On · `MULTI_SESSION_MODE` Off
- **Where the programme stands.** Done: Phases 0, 1, 2a, 2b, 3, 5, 6; S0, K1, S1, **S2 (19 of 19)**, S3, G6, C3, the bankability review. Open: **Phase 4 at 23 of 26**, **K2** (1–2 sessions), **C5** (~6 sessions), the **Q plan clock** (next due ~2026-12). Deferred: C6. **About 10 sessions remain**
- **`market-access` is 7 of 9 built**, positions 4 and 7 remaining. **Row 24 (`what-bankable-means`) is §4 position 7 and inserts at LIVE INDEX 5** — measured against `clTrackMarketAccess_()`'s actual array, which is **not** §4's numbering because §4's position 4 (`reading-the-numbers`, row 25) is unbuilt. Another **mid-array insert**; P5 will fire. It shares row 23's gate module and inherits **2027-01-01**, and would take the withheld count **3 → 4**
- **A README-tree gap closed that was not this session's.** The directory audit found **two** `industry-guidance/` analysis files missing from the tree — this session's and **`landscape-insurance-and-risk-transfer-analysis.md`, created at v06.43r and never listed**. `check-readme-tree.py` reads page and GAS version displays only and is **structurally blind to a missing file row**; audit the directory against the tree by hand
- **Open, recorded, not taken — seventeen.** (i) `.cl-tbl` `min-width` — **forty-two** data points. (ii) The generated `read-next` names a landscape module by **id** rather than title. (iii) `the-800-vdc-shift`'s stale `three-chains` intro is **thirty-two** registrations stale. (iv) §10.6's session-4 block is out of chronological order. (v) A stale `lesson_ref` can make a segment due alone — five such segments stand. (vi) **CLOSED** by row 20. (vii) `restart` scores zero for a **fifteenth** session. (viii) Session 12's six `topic-aidc-landlords` terms unseeded for an **eleventh**. (ix) (ee4)/(gg7)'s dash rows. (x) (ff4)'s answer-**index** skew and (ll3)/(mm5)/(nn10)'s answer-**length** skew. (xi) (ff5)/(hh6)/(jj3)/(ll5)/(nn2) — **one segment leaks `(planned)` as of this commit and row 24 will take it to five**, none owned by a scheduled session. (xii) `Scraper.gs` holds **twelve** `topic-landscape-*` seeds against **nineteen** landscapes — shortfall seven, now **frozen**. (xiii) (jj4)'s two timelines. (xiv) (kk8)'s blindness, now known to cover the **`-ing`** form as well as the plural. (xv) (ll1)/(mm6)/(nn3)/**(oo6)** — an `assert walked > 0`, a path assertion **and a self-test on any transcribed renderer function**. (xvi) (nn5)'s denial-channel asymmetry. **(xvii) NEW — (oo7)**: `clFmt` cannot render italic inside bold and prints literal asterisks; a one-line pattern change would fix it and no checker reports it
- **Render harness — WORKING, and one new fact.** Everything §7.53 recorded held, plus: **section containers are `section.cl-sec` with id `cl-<sectionId>`** — `[data-sec]` matches the stale-badge and done-button nodes instead, one of which is invisible, which costs a 30-second screenshot timeout. Seed the four `sessionStorage` keys **after** load per (nn4); rewrite `var _e` to `c2FnX18v` in the **served HTML**; `handleGuidanceOp_` takes the GAS event object and reads `p.session`; restart the shim after every `.gs` edit
- **This session's render: contributor 9/9 section headings plus title and glossary, 53,584 characters, ZERO uncaught page errors**, 101 of 101 tooltips resolving against a 4,208-key map, zero unresolved `{{…}}`, zero literal asterisks, zero backticks; analyst **denied at the server** with **280 characters** and **0 of 9** headings. PROJECT region **3,777,118 → 3,848,937** bytes and the render **52,132 → 53,584** characters across the splice and three fix rounds, so both meters were demanded and both moved
- **Defect tally: seven pre-splice simulations with printed denominators (197 formatted fields, 48 plain-text fields, 101 term uses, 8 glossary entries, 5 quiz items, ~60,000 ASCII characters, the structural check) all returned 0 failures; the recount then caught EIGHTEEN and the screenshots EIGHTEEN more with near-zero overlap — (kk7) at a SIXTEENTH consecutive session.** The screenshots' sharpest was an indicators table whose intro claimed rows ordered by how soon they resolve while a two-quarter-wide row sat above a single-quarter one; the recount's sharpest was the NRTL scope-date claim that the same dossier refutes two fields away

#### Recommendation for next session

- Run **Phase 4 row 24 — the `what-bankable-means` mechanism lesson** on Opus 5 xhigh. Its brief is **`INTEGRATED-REMEDIATION-PLAN.md` §7.54**, written this session. Four things carry the attention: **the module assertion must STAY at 28** (you author a lesson, not a module); **the insert index is 5 and must be verified against `clTrackMarketAccess_()`'s live array**, which is not §4's numbering because §4's position 4 is unbuilt; **the landscape module that ranks your subject's sellers was built yesterday**, so read `landscape-assurance-2026-09`'s tiles, callouts and ledger per §10.6 (t) and be careful with its `reliance` treatment; and **(oo6)** — replacing a bad instrument with a better one is exactly when to test the better one, because a mis-transcribed `clFmt` hid a real defect the heuristic it replaced had caught.

**To continue:** type `run Phase 4 row 24`

