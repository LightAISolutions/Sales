# Previous Session Context

Claude writes to this file when the developer says **"Remember Session"** — capturing enough context for a future session to pick up the train of thought quickly. This is separate from "Reminders for Developer" (REMINDERS.md), which is the developer's own notes.

> **Note on stale-context auto-reconstruction** — when a session starts and this file's `Repo version:` doesn't match the current repo version, Claude reconstructs the missing entry from CHANGELOG.md and commits it **without pushing**. The commit rides along with the session's first user-task commit on the next push. If a session ends before any user-task push happens, the reconstructed entry stays **local-only** and the next session will just re-reconstruct from CHANGELOG if still stale. This is intentional — pushing a dedicated reconstruction commit on its own would force every subsequent user push in the same session to wait for the auto-merge workflow to finish before it could push too (push-once enforcement). The reconstructed entry is a convenience hint, not load-bearing state, so the small persistence risk is a fair trade.
## Latest Session

**Date:** 2026-09-16 12:38:06 AM EST
**Repo version:** v06.06r — two pushes (v06.05r the lesson, v06.06r this handover)
**Branch:** `claude/nifty-euler-21yj7m` — restarted from `origin/main` before the second push

### What was done

- **Phase 4 row 14 — the `grid-stability-and-the-generator` lesson, *Inertia, Reactive Power, and Teaching Electronics to Lead*** (v06.05r). The **forty-fourth lesson** and **the one that closes the `electrical-foundations` floor track**, authored as `clLessonGridStabilityAndTheGenerator_()` **inside** the `// CONTENT START` … `// CONTENT END` fence, registered at the **end** of `clLessons_()` and **appended** to that track's `lessons[]` at §4 position 5 (position 2, `string-versus-central`, is row 15 and unbuilt)
- **EIGHT sections, not §3.2's seven** — `the-spinning-machine` (prose) · **`two-knobs` (table, 4 × 3 — the inserted one)** · `frequency-is-a-meter` (prose) · `what-retires-with-coal` (callout) · `following-versus-forming` (proscons, 2 cards) · `where-it-fails` (callout) · `drill` (8 cards) · `check-yourself` (5 items). Six distinct kinds, no two prose adjacent, four tiles, a **nine-entry** glossary, `reviewBy` **2027-03-16**
- **Eight public stamps**, each pin read off the document fetched that run and each ref resolved by opening the file and matching its own `slug`: `study:ge-vernova`@2026-08-21 · `study:black-veatch`@2026-08-21 · `study:hitachi-energy`@2026-08-21 · `study:sinexcel`@2026-08-08 · `study:siemens-energy`@2026-09-04 · `study:piller`@2026-09-03 · `study:power-electronics`@2026-09-06 · `concepts:profiler-concepts`@2026-09-13
- **BOTH `segment-grid-equipment` AND `segment-bridge-and-on-site-generation` regenerated** with `--segment … --today 2026-09-16` and nothing else — the first Phase 4 row to regenerate two. Classroom GAS **v01.48g → v01.49g** in both places with one generic public changelog line. No page bump; `Scraper.gs`, `Profiler.gs` and `Profiler.html` untouched
- **The CHANGELOG rotated** — the 2026-09-07 group, 13 sections, all SHA-resolved; §7.3's Phase 4 row, §6's Phase 4 row, curriculum §7 row 14 and §4's track table all flipped and dated; **§7.35 written in the same work commit**, both halves
- **v06.06r (the handover)** — the measured deploy counter recorded in the v06.05r Notes and in §7.35's deploy paragraph, and `SESSION-CONTEXT.md` written. **Carried in the same commit as the repo-version bump**, per the v06.02r rule

### Where we left off

Everything is merged and live. **Phase 4 is 14 of 26; S2 is 9 of ~19**, so the interleave stands at 14:9 and hands the turn to **S2 session 10, `clean-firm-and-nuclear`**. GAS live at Classroom **v01.49g**, Scraper **v02.10g**. The content-checker baseline is **0 errors / 0 warnings at 44 lessons / 8 tracks / 142 gate cases**, with the guidance-module assertion still at **18** — S2 session 10 is the commit that takes it to 19. `--check` reports **10 due** and `clean-firm-and-nuclear` is one of them, so session 10's arithmetic is the **netting** case, 10 → 10 → 9.

### Key decisions made

- **THE FINDING OF THE SESSION: a contradiction between two parts of the plan was RESOLVED rather than obeyed, and the brief's preferred route failed on its own stated condition.** §3.2's outline opens `the-spinning-machine` (prose) and `frequency-is-a-meter` (prose) **back to back**, which **§8 item 6 forbids in as many words**. §7.34 preferred converting the second to `bars` or `timeline` — *only if a source states the numbers*. **None does.** No input carries a frequency-deviation series, and `study:power-electronics` records in terms that the quantification (*no inertia constant, no fault-current contribution, no RoCoF withstand*) is *in no datasheet, no release and no independent test result*. So `bars` could only have been invented, which §8 item 6 bars explicitly. **Route (a) was taken: a `table` inserted between them and the count moved seven → eight.** The insert is load-bearing, not a spacer — `study:ge-vernova` states *one machine, two independent knobs* outright, and `two-knobs`'s **third row is the lesson's hinge** (*what you turn: **nothing*** — the mass is not a control), which is exactly the third, separate loss `what-retires-with-coal` has to be able to name two sections later. **Section ids are permanent, so eight is now the contract**
- **THE BRIEF'S DERIVATION OF THE SECTION HALF WAS FALSE AND THE CORRECTION IS LOAD-BEARING.** §7.34 asserted that `grid-stability-and-the-generator` is **not** in `CRITERION_LEXICON`, "checked entry by entry at v06.03r". **It is** — keyed on `grid-forming`, `inertia`, `reactive`, `stability`, `black start`. The prediction still held (`read-next` alone, no deepening) but **for a different reason**: none of those five keywords appears in either segment's six `buyingCriteria` strings, checked programmatically. **So row 12's gate needs one more turn: the section half is bounded by `READ_NEXT` ∩ `CRITERION_LEXICON` AND THEN by an actual case-insensitive keyword hit against that segment's own `buyingCriteria`** — three tests, not two, and only the third can be run without guessing. **Never take a lexicon-membership claim from a brief; grep the file**
- **The membership half was the programme's first two-segment regeneration and it ran exactly as forecast: 10 → 12 → 10.** Both joined on `read-next` alone and the generator's own line confirmed it verbatim on both (`changed: read-next; no pin moved`)
- **The foreseeable P7 did NOT fire, and reading the literals beforehand is what settled it cheaply.** Both segments carried `"updated": "2026-09-15"`; EST read **2026-09-16**; both advanced. **The append signature is P1 alone** — no P5, no P3 (`gateDigest` deliberately untouched: a lesson inside the fence moves no `GATE_SYMBOLS` member), no P7, no P8. **A smaller finding worth carrying: `--base origin/main` reported ZERO findings on the code-only diff** — not even P1 — because at that point the only two changed paths are both inside the pipeline's own write set. P1 appears only once the plan documents join the diff
- **§10.6 (t) PAID A FIFTH TIME, AND THIS IS THE FIRST LESSON TO PRE-DECLARE IN BOTH A TILE AND A NOTE.** Two landscape modules take this lesson as a `READ_NEXT` entry and both name machines and operators it must not. A tile reads **0 vendors named**; the `what-retires-with-coal` note declines the ranking in terms. **The claim was verified rather than asserted** — all seven study guides were scanned for any sizing or ranking of the grid-stability machine and none carries one, so "no input sizes or ranks that market" is a measurement
- **`reviewBy` 2027-03-16 on the ~6-month default, four rejections in writing, and the near miss is instructive.** The fetched corpus holds exactly three future-dated items. *January 2027* (one vendor's medium-voltage overload ladder becoming available) was rejected **twice over** — a vendor fact in a vendor-blind lesson, and a product-availability date rather than a rule taking effect. *24 December 2026* (EO 14420's DOE deadline) is a manufacturing-origin gate and already `eo14420`'s own clock. The whole **ride-through rulebook** is described as adopted and enforceable with **no phase-in date stated anywhere in the corpus**, and reaching outside it is what §5.2 forbids. Keeps §5's due-for-review count at **2**
- **THE MICRO-MARKUP CHECK EARNED ITSELF A FOURTH TIME, ON ITS OTHER HALF.** The first draft carried **five errors**, every one the `textContent` trap `CLASSROOM-SCHEMA.md` documents: a `proscons` card's `t` and `meta`, and a `glossary[].d`, are printed as text rather than sent to `clFmt`, so a `{{term}}` in any of them reaches the reader as literal braces. Nothing else in the toolchain sees it
- **A RENDER-HARNESS FINDING THAT WILL COST THE NEXT SESSION A CYCLE IF IT IS NOT CARRIED: `Classroom.html`'s CSP blocks a shim server on another port.** `connect-src` is `'self'` plus Google hosts, so `_gasPost` pointed at `http://127.0.0.1:<port>` is **refused outright** — the page renders empty with a CSP console error and no page error. **The fix: point `_gasPost` at a same-origin path (`/__gas`) and fulfil it with Playwright's `page.route()`**, proxying to the Node shim from Python
- **THE EST/UTC TRAP RESOLVED THE OTHER WAY FOR THE FIRST TIME IN FIVE SESSIONS, AND IT MADE THIS SESSION THE ROTATOR.** EST read **2026-09-16 00:07** against UTC's **04:07** — the harness banner said 2026-09-16 and for once agreed — so the **twenty-six** sections dated 2026-09-15 all stopped being exempt overnight and the file stood at **106 raw / 106 non-exempt against a 100 trigger**. §7.33's escape clause could not fire. The 2026-09-07 group (13 sections) moved, **all thirteen SHA-resolved** on a 1,269-commit clone, and the **mandatory post-rotation grep passed on the first attempt**
- **The deploy forecast held exactly: one `.gs` in the diff → one step → one line.** `Classroom deploy confirmed (GET): Updated to v01.49g (deployment 60) | 60/200`, first GET leg, no POLL, no probe — the **seventeenth consecutive clean use** of the §7.19 order — and Pages agreed before the log was closed. Scraper's step never fired and it holds at 155/200

### Active context

- **Repo version** v06.06r · **CHANGELOG 95 raw**, two sections dated 2026-09-16 EST (v06.05r and v06.06r), counter `Sections: 95/100`. **93 non-exempt once EST rolls past 2026-09-16**, well under the trigger — **the next session is NOT the rotator**. The next group in line is **2026-09-08 (10 sections)** and it does not arm until the file is back at 100
- **Standing changelog state:** `Classroomgs.changelog.md` **49 raw / 50 cap** — one section from its trigger, and **session 10's own section is same-day exempt**, so it arms for the session *after* that one; its oldest group is **2026-09-02 (11 sections)**. `Scrapergs.changelog.md` **31/50**. `Profilerhtml.changelog.md` **49/50**, the closest of the three to its own trigger, and nothing an S2 session does touches Profiler
- **GAS versions:** Classroom **v01.49g**, Scraper **v02.10g**, Profiler v01.39g. **Page versions:** Classroom v01.14w, Profiler v01.90w, Scraper v01.72w — no page bump this session
- **Content-checker baseline is 0 errors / 0 warnings at 44 lessons / 8 tracks / 142 gate cases** — protect it. **The gate-case count did NOT move**, because an all-public stamp reuses fixture classes the truth table already covers. **The guidance-module assertion is 18 and S2 session 10 is the commit that takes it to 19**
- **Deployment counters: Classroom 60/200 and Scraper 155/200, both MEASURED at v06.06r against the job log.** One `.gs` in the merge diff fired exactly one step and logged exactly one line, on the first GET leg with no POLL and no `?op=deploy` probe — the **seventeenth consecutive clean use** of the §7.19 order, and the one-line forecast held exactly
- **`--check` reports 10 due** — `cells-and-chemistry`, `clean-firm-and-nuclear`, `compute-and-the-rack`, `epc-and-construction`, `storage-developers-and-ipps`, `hyperscalers-and-ai-labs`, `neoclouds`, `capital`, `software-and-optimization`, `insurance-and-risk-transfer`. **Session 10's own segment is among them**, so its arithmetic is the netting case **10 → 10 → 9**, not 10 → 11 → 10
- **`clean-firm-and-nuclear` measured at v06.05r: 16 members (3 · 2 · 11), all sixteen carrying a dossier.** At **69 % adjacent** it is the second most adjacent-heavy segment behind `software-and-optimization`'s 75 %, and its **two-name challenger bench** is the joint second-smallest. **Its registry `notes` is EMPTY**, so session 8's precedent (a thin bench explained by `notes`) does not transfer — the explanation has to come from the members' dossiers. Its `READ_NEXT` is only **two** entries and **one is unbuilt** (`clean-firm-power`, §7 row 16), so four of six buying criteria have no built owner
- **`check-classroom-curriculum.py --strict`** reports no structural findings, **28 stale pins** (unchanged — row 14's eight pins are all current) and **2 items due for review** — `landscape-utilities-2026-09` and `landscape-in-hall-power-2026-09`, both at 2026-10-01 and both by design. All exit 0. **Do not "fix" either**
- **Toggles:** `START_OF_RESPONSE_BLOCK` On · `CHAT_BOOKENDS` Off · `TIMING_ESTIMATES` On · `END_OF_RESPONSE_BLOCK` On · `MULTI_SESSION_MODE` Off
- **Where the programme stands, measured 2026-09-16 at v06.06r.** Done: Phases 0, 1, 2a, 2b, 3, 5, 6; S0, K1, S1, S3, G6, C3, the bankability review, the one-off segment regeneration. Open: **Phase 4 at 14 of 26**, **S2 at 9 of ~19**, **K2** (1–2 sessions, runnable any time), **C5** (~6 sessions; its gate of ~6 landscapes is **MET**), the **Q plan clock** (next due ~2026-12). Deferred: C6. **About 29 sessions remain**
- **Open, recorded, not taken.** The `.cl-tbl` `min-width` question now has **seventeen** clean data points (row 14's four-column `two-knobs` table rendered clean at 1400px); the generated `read-next` link is a `Classroom.html` renderer feature (`clFmt` has no link token) — **both want the same session, one that edits `Classroom.html`, plus a developer `--all` run**. **`the-800-vdc-shift`'s stale `three-chains` intro is now thirteen inserts stale.** **§7.3's S2 row (order 5) is one session behind** — it records sessions 1–8 and never got session 9; §7.35 asks session 10 to close it as part of its own flip. **§10.6's session-4 bullet block still sits out of chronological order** — cosmetic
- **Render harness — the recipe, with this session's correction.** Run the PROJECT region of `Classroom.gs` under Node in a `vm` context with GAS shims; shim **only** `validateSessionForData` (flat `{email, role, permissions}`, permissions an **ARRAY**) plus no-op `clProgressRead_` / `clProgressWrite_` / `clStudyNext_` / `dataAuditLog` / `auditLog`. **Route the op: `payload.action === 'guidance'` → `ctx.handleGuidanceOp_`, everything else → `ctx.handleClassroomOp_`** — through the vm context object, never bare. Pass the op as `{ parameter: payload }`. **Serve the scratch copy at `/Classroom.html`, NOT at the site root.** `_e = ''` and `AUTO_REFRESH = false`; copy `profiler-data/`, `sounds/`, `images/` and `favicon.ico` in. Seed `sessionStorage` **after** load (`Classroom_gas_session_token` · `_gas_user_email` · `_gas_user_role` · `_gas_user_permissions`), token **≥ 32 characters**; override `_gasPost` **after** load and return a **Promise**. **THE CSP CORRECTION: point `_gasPost` at a same-origin path (`/__gas`) and fulfil it with Playwright's `page.route()`** — `connect-src` is `'self'` plus Google hosts and a cross-port fetch is refused outright. **Hide** `#auth-wall`, `#signing-out-wall`, `#auth-timers`, `#gas-layer-toggle` plus `#cl-app{display:block !important}`. Entrypoints: `clShowLesson(id)`, `clShowGuidanceDoc(id)`. Section nodes are `#cl-<sectionId>`. Launch with `executable_path` from `glob('/opt/pw-browsers/chromium-*/chrome-linux/chrome')`; `pip install playwright` is needed (the browser is pre-installed, the Python package is not). The cheap extra assertion earned itself again: **zero surviving `{{…}}` and zero literal asterisks across 26,208 characters**
- **The two console errors in every render are environmental and pre-existing**, both `ERR_CERT_AUTHORITY_INVALID` because the sandbox proxy's CA is untrusted by Chromium. Neither is an asset and neither raises a page error

### Recommendation for next session

- Run **S2 session 10 — the `clean-firm-and-nuclear` landscape module** on Opus 5 xhigh. Its brief is **`INTEGRATED-REMEDIATION-PLAN.md` §7.35**, written inside the v06.05r work commit with its deploy counters measured at v06.06r. It carries three things worth the attention: **the netting arithmetic** — its own segment is already due, so `--check` runs 10 → 10 → 9 rather than 10 → 11 → 10; **the thinnest mechanism coverage any landscape has faced** — a two-entry `READ_NEXT` with one entry unbuilt, so four of six buying criteria have no built owner and the module must name the gap rather than becoming the nuclear lesson by default; and **a threat section with no registry help** — a two-name challenger bench against eleven adjacents, with the segment's `notes` empty, so session 8's precedent does not transfer.

**To continue:** type `run S2 session 10`

## Previous Sessions

### Session — 2026-09-15 11:39:09 PM EST (v06.04r)

**Date:** 2026-09-15 11:39:09 PM EST
**Repo version:** v06.04r — two pushes (v06.03r the module, v06.04r this handover)
**Branch:** `claude/amazing-pasteur-us8xcs` — restarted from `origin/main` before each push

### What was done

- **S2 session 9 — the `bridge-and-on-site-generation` landscape module** (v06.03r). The **ninth landscape** and the **eighteenth guidance module**, authored as `guidanceDocLandscapeBridgeAndOnSiteGeneration_()` **below** the `// CONTENT END` fence and registered at the **end** of `guidanceDocs_()`'s Value Chain lane. §10.6's nine section ids in §10.6's order; `each-players-bet` is **13 rows** (the highest ranked-to-member ratio of any landscape — 13 of 17), the claims ledger **60 rows**, `what-the-record-does-not-say` eight paragraphs, `drill` ten cards, `check-yourself` five items, four tiles, a **seven-entry** glossary, `updated` 2026-09-15, `reviewBy` **2026-12-31**
- **The analysis markdown** at `repository-information/industry-guidance/landscape-bridge-and-on-site-generation-analysis.md`, twelve sections, with **§2 (the split) written first** per §10.6 (j) — literally the first text in the file, front matter prepended afterwards
- **The guidance-module assertion moved 17 → 18** in `scripts/check-classroom-content.py`, the one edit outside the fence a landscape session makes
- **`segment-bridge-and-on-site-generation` regenerated** with `--segment` and nothing else (`changed: read-next; no pin moved`); **Scraper seed `topic-landscape-bridge-and-on-site-generation`** added (12 terms); §7.3's S2 row and §10.6's progress note flipped to **9**, dated; **§7.34 written in the same commit**, both halves
- **v06.04r (the handover)** — the measured deploy counters recorded in the CHANGELOG and in §7.34's deploy paragraph, and `SESSION-CONTEXT.md` written. **Carried in the same commit as the repo-version bump**, per the v06.02r rule

### Where we left off

Everything is merged and live. **Phase 4 is 13 of 26; S2 is 9 of ~19**, so the interleave stands at 13:9 and hands the turn back to **Phase 4 row 14, `grid-stability-and-the-generator`**. GAS live at Classroom **v01.48g**, Scraper **v02.10g**. The content-checker baseline is **0 errors / 0 warnings at 43 lessons / 8 tracks / 142 gate cases**, with the module assertion now at **18** — a Phase 4 row does **not** move it. `--check` reports **10 due**, none of them row 14's to clear by accident.

### Key decisions made

- **THE FINDING OF THE SESSION: a public lesson's own CLASSIFICATION mapped ONE-FOR-ONE onto the segment's challenger bench.** `bridge-power` says in as many words that there are *exactly three ways out of a factory queue* — change machine class, change where the machine comes from, build a machine nobody queues for — and **names nobody**. The registry's five challengers fill all three: `rolls-royce-power-systems` and `rehlko` on machine class, `proenergy` on provenance, `bloom-energy` and `mainspring-energy` on machines nobody queues for. Session 6's (s) generalises from *the lesson teaches the test and the module applies it to named parties* to **the lesson teaches the CATEGORIES and the module FILLS them**
- **§10.6 (t) FIRED ON FOUR WHOLE SECTIONS OF TWO PUBLIC LESSONS, which is a different order of pre-declaration.** `bridge-power` and `backup-generation` between them teach **five of the six buying criteria** — the four machine classes, the slot-reservation instrument, the standby/prime/continuous ladder, block load and the whole permit ceiling — and name **not one vendor**. Earlier pre-declarations were one tile (session 6) or one callout (session 8). §7.23's test held on **the slot itself**
- **SIX built landscape neighbours is a record, and the inversion rate is BIMODAL.** `in-hall-power` 6 shared / **6 inverted (100 %)**; `grid-equipment` 3 shared / **0 inverted (0 %)**. The extremes sit either side of this segment in the chain, and **the rate measures whether a shared member's business CROSSES the boundary or STRADDLES it** — a finer instrument than session 8's direction, available to any landscape with two or more built neighbours
- **`reviewBy` — session 3's (g) case a THIRD time, but the one future date in the fence is a TRAP rather than an absence.** 60 `policyExposure[]` entries, 24 dated, **exactly one in the future**: `cummins`'s `2027-01`, whose own text says it applies to **on-highway truck platforms**. A mechanical sort returns it and clocks a bridge-generation module on a semi-truck engine. The date taken, **2026-12-31**, came from `caterpillar`'s `strategyRead[5]` and is **corroborated by four other members converging on end-2026** — the first landscape gate resting on a *convergence* rather than one sentence. **Four rejections in writing**, and **the first deliberate acceptance of a calendar day another module already carries**: (r)/(u) bar taking another module's own *gate*, and this is a different bell on the same day; shifting a day would mean fabricating a date the corpus does not state
- **A BRIEF'S OPEN ITEM WAS DECIDED RATHER THAN DEFERRED A THIRD TIME, and the original ground did not survive inspection.** `section 232` scores zero across all 444 Scraper terms. Session 8 left it out as *a tariff regime belonging with the policy seeds* — but **no policy seed owns it**: `topic-china-policy` holds `tariff` and `Section 301` and is China-scoped, while 232 bites Germany, France and Japan, and four members here carry that exposure. It stays out on the **sharpened** ground that it is a **cross-segment** regime and a landscape session may only add `topic-landscape-<segment>`. **Recommendation recorded: it wants its own `topic-trade-tariffs` seed or an addition to `topic-federal-action`** — a developer or policy-session call
- **§8 item 8's `bridge-power` pointer: REPORTED, not fixed.** `bridge-power` was read in full. The debt is **real in the letter and paid in substance** — `where-batteries-stop` states the boundary twice, names `bridge-power` by title both times and declares the non-duplication in both directions; `bridge-power` carries no back-pointer. **Recommendation: close the open half as paid; add the pointer only inside a substantive revision, never as a standalone edit (the G4 change P8 exists to fail)**
- **THE DEFECT OF THE SESSION, and the transferable half: never sweep a date across a file holding eighteen independently dated modules.** Aligning the module's `updated` to the generator's stamp with a blanket `16 September 2026` → `15 September 2026` replace rewrote a **comment-deadline date inside `guidanceDocEo14420_()`** — a module this session must not touch. **The pipeline checker caught it**, in a P2 line quoting the changed string. The fix was to rebuild `Classroom.gs` from `origin/main` with only the four intended changes reapplied and re-run the generator, after which the diff showed exactly five intended deletions. **Scope a replacement to the function being authored**
- **`--base origin/main` on an S2 session reads P1 + P2, not P1 alone, and §7.31/§7.33's wording was imprecise.** A guidance module is authored **below** the fence *by design*, so every line of it is outside the fence to a checker written for the pipeline's write set. **P2 is structural to every S2 session** and is the same class of noise as P1. **No P3, no P5, no P7, no P8** — the segment lesson's `updated` advanced 2026-09-14 → 2026-09-15, so row 13's same-EST-day P7 did not repeat
- **THE EST/UTC TRAP DECIDED THE ROTATION FOR A FOURTH CONSECUTIVE SESSION — and this time the escape clause fired rather than the conclusion.** §7.33 forecast this session as the rotator *"unless your push also lands on 2026-09-15 EST"*. EST read **2026-09-15 23:31** against UTC's **2026-09-16 03:31**, the harness banner said 2026-09-16 again, and the push landed on the 15th — so **no rotation**: 105 raw / 80 non-exempt, counter `Sections: 105/100`
- **One render-harness correction worth carrying: guidance ops go to `handleGuidanceOp_`, not `handleClassroomOp_`.** The page posts `{action:'guidance', gop:'doc', session}` and the classroom handler answers `unknown_cop`. A cycle was lost before that was traced — route on `payload.action === 'guidance'`

### Active context

- **Repo version** v06.04r · **CHANGELOG 105 raw / 80 non-exempt**, **twenty-five sections dated 2026-09-15 EST**, counter `Sections: 105/100` (legitimately above 100 — `CHANGELOG-archive.md` step 3 allows it on a busy day). **The moment EST rolls past 2026-09-15 all twenty-five stop being exempt and the file stands at 105 non-exempt against a 100 trigger, so the NEXT SESSION IS THE ROTATOR AND CANNOT ESCAPE IT** — it must rotate the oldest whole date group, **2026-09-07 (13 sections)**, taking the file to 92 + its own = 93. Deepen the clone first so every rotated header enriches, and run the **mandatory post-rotation grep**
- **Standing changelog state:** `Classroomgs.changelog.md` **48 raw / 48 non-exempt** against 50 — two from its cap and now the closest of the page/GAS three to arming. `Scrapergs.changelog.md` **31/50**. `Profilerhtml.changelog.md` **49/50**, one section from its own trigger, and nothing a Phase 4 row does touches Profiler
- **GAS versions:** Classroom **v01.48g**, Scraper **v02.10g**, Profiler v01.39g. **Page versions:** Classroom v01.14w, Profiler v01.90w, Scraper v01.72w — no page bump this session
- **Content-checker baseline is 0 errors / 0 warnings at 43 lessons / 8 tracks / 142 gate cases** — protect it. **Its guidance-module assertion is now 18 and does NOT move on a Phase 4 row**; row 14 takes the lesson count to 44 and leaves the module count alone
- **Deployment counters: Classroom 59/200 and Scraper 155/200, both MEASURED at v06.03r against the job log.** Two `.gs` files in the merge diff fired exactly two steps and logged exactly two lines, both on the **first GET leg** with no POLL and no `?op=deploy` probe — the **sixteenth consecutive clean use** of the §7.19 order, and the two-line forecast held exactly. Pages agreed before the log was closed
- **`--check` reports 10 due** — `cells-and-chemistry`, `clean-firm-and-nuclear`, `compute-and-the-rack`, `epc-and-construction`, `storage-developers-and-ipps`, `hyperscalers-and-ai-labs`, `neoclouds`, `capital`, `software-and-optimization`, `insurance-and-risk-transfer`. **Row 14's arithmetic is NEW: `grid-stability-and-the-generator` sits in TWO segments' `READ_NEXT`** (`grid-equipment` and `bridge-and-on-site-generation`), so it is **the first Phase 4 row that must regenerate two segments** and its count is **10 → 12 → 10**. The section half is `read-next` alone for both — that lesson id is **not** in `CRITERION_LEXICON`, checked entry by entry
- **A genuine plan-internal conflict is waiting for row 14 and is flagged in §7.34.** §3.1's outline puts `the-spinning-machine` (prose) and `frequency-is-a-meter` (prose) **back to back**, which **§8 item 6 forbids**. Three routes are written out; the brief's preference is to convert the second to `bars` or `timeline` **only if a source states the numbers**. Section ids are permanent once shipped, so this is the one cheap moment to settle it
- **`check-classroom-curriculum.py --strict`** reports no structural findings, **28 stale pins** and **2 items due for review** — `landscape-utilities-2026-09` and `landscape-in-hall-power-2026-09`, both at 2026-10-01 and both by design. All exit 0. **Do not "fix" either**
- **Toggles:** `START_OF_RESPONSE_BLOCK` On · `CHAT_BOOKENDS` Off · `TIMING_ESTIMATES` On · `END_OF_RESPONSE_BLOCK` On · `MULTI_SESSION_MODE` Off
- **Where the programme stands, measured 2026-09-15 at v06.04r.** Done: Phases 0, 1, 2a, 2b, 3, 5, 6; S0, K1, S1, S3, G6, C3, the bankability review, the one-off segment regeneration. Open: **Phase 4 at 13 of 26**, **S2 at 9 of ~19**, **K2** (1–2 sessions, runnable any time), **C5** (~6 sessions; its gate of ~6 landscapes is **MET**), the **Q plan clock** (next due ~2026-12). Deferred: C6. **About 30 sessions remain**
- **Open, recorded, not taken.** The `.cl-tbl` `min-width` question now has **sixteen** clean data points (this session's 13-row bets table and 60-row ledger both rendered clean at 1400px); the generated `read-next` link is a `Classroom.html` renderer feature (`clFmt` has no link token) — **both want the same session, one that edits `Classroom.html`, plus a developer `--all` run**. **`the-800-vdc-shift`'s stale `three-chains` intro is now twelve inserts stale.** **§10.6's session-4 bullet block still sits out of chronological order** — cosmetic
- **Render harness — the recipe, with this session's correction.** Run the PROJECT region of `Classroom.gs` under Node in a `vm` context with GAS shims; shim **only** `validateSessionForData` (flat `{email, role, permissions}`, permissions an **ARRAY**) plus no-op `clProgressRead_` / `clProgressWrite_` / `clStudyNext_` / `dataAuditLog` / `auditLog`. **Route the op: `payload.action === 'guidance'` → `ctx.handleGuidanceOp_`, everything else → `ctx.handleClassroomOp_`** — through the vm context object, never bare. Pass the op as `{ parameter: payload }`. **Serve the scratch copy at `/Classroom.html`, NOT at the site root.** `_e = ''` and `AUTO_REFRESH = false`; copy `profiler-data/`, `sounds/`, `images/` and `favicon.ico` in. Seed `sessionStorage` **after** load (`Classroom_gas_session_token` · `_gas_user_email` · `_gas_user_role` · `_gas_user_permissions`), token **≥ 32 characters**; override `_gasPost` **after** load and return a **Promise**. **Hide** `#auth-wall`, `#signing-out-wall`, `#auth-timers`, `#gas-layer-toggle` plus `#cl-app{display:block !important}`. Entrypoints: `clShowLesson(id)`, `clShowGuidanceDoc(id)`. Section nodes are `#cl-<sectionId>`. Launch with `executable_path` from `glob('/opt/pw-browsers/chromium-*/chrome-linux/chrome')`. The cheap extra assertion earned itself again: **zero surviving `{{…}}` and zero literal asterisks across 62,230 characters**
- **The two console errors in every render are environmental and pre-existing**, both `ERR_CERT_AUTHORITY_INVALID` because the sandbox proxy's CA is untrusted by Chromium. Neither is an asset and neither raises a page error

### Recommendation for next session

- Run **Phase 4 row 14 — the `grid-stability-and-the-generator` lesson** on Opus 5 xhigh. Its brief is **`INTEGRATED-REMEDIATION-PLAN.md` §7.34**, written inside the v06.03r work commit with its deploy counters measured at v06.04r. It carries three things worth the attention: **a genuine plan-internal conflict to resolve** — §3.1's outline puts two `prose` sections back to back and §8 item 6 forbids it, with three routes written out and section ids permanent once shipped; **the first Phase 4 row that must regenerate TWO segments**, because its lesson id sits in both `grid-equipment`'s and `bridge-and-on-site-generation`'s `READ_NEXT`, so the count is 10 → 12 → 10; and **it is the rotator** — the CHANGELOG stands at 105 non-exempt the moment EST passes 2026-09-15, so it must rotate the 2026-09-07 date group and cannot escape it.

**To continue:** type `run Phase 4 row 14`
