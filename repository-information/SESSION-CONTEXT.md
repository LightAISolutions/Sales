# Previous Session Context

Claude writes to this file when the developer says **"Remember Session"** — capturing enough context for a future session to pick up the train of thought quickly. This is separate from "Reminders for Developer" (REMINDERS.md), which is the developer's own notes.

> **Note on stale-context auto-reconstruction** — when a session starts and this file's `Repo version:` doesn't match the current repo version, Claude reconstructs the missing entry from CHANGELOG.md and commits it **without pushing**. The commit rides along with the session's first user-task commit on the next push. If a session ends before any user-task push happens, the reconstructed entry stays **local-only** and the next session will just re-reconstruct from CHANGELOG if still stale. This is intentional — pushing a dedicated reconstruction commit on its own would force every subsequent user push in the same session to wait for the auto-merge workflow to finish before it could push too (push-once enforcement). The reconstructed entry is a convenience hint, not load-bearing state, so the small persistence risk is a fair trade.

## Latest Session

**Date:** 2026-09-17 12:05:58 AM EST
**Repo version:** v06.31r — two pushes (v06.30r the lesson, v06.31r this handover)
**Branch:** `claude/great-fermi-0cbomc` — restarted from `origin/main` before each push

### What was done

- **Phase 4 row 19 — the `how-a-cell-is-made` lesson** (v06.30r). The **forty-ninth** lesson: `clLessonHowACellIsMade_()`, id `how-a-cell-is-made`, title *From Powder to Cell: Manufacturing, Formats, and Yield*, group Technology Foundations, gate **`tracks`** (public), authored **inside** the `// CONTENT START` … `// CONTENT END` fence. Seven sections at §3.1's ids and order — `the-line` (prose) · `three-formats` (proscons) · `yield-is-the-game` (table) · `scaling-physics` (prose) · `factories-are-the-product` (callout) · `drill` (6 cards) · `check-yourself` (5 items) — six distinct kinds, three `sales` lines of seven, four tiles all-ASCII, a nine-entry local glossary. `updated` 2026-09-17, `reviewBy` **2027-03-17**
- **Registered at the END of `clLessons_()` and MID-ARRAY INSERTED at position 5 of `clTrackBessFoundations_()`**, which **completes the track at six of six** — the third of the five mechanism tracks to close, after `electrical-foundations` (row 15) and `aidc-campus` (row 18). The track's `short` moved to the whole-track form §3.1 specifies
- **One sibling corrected in the same commit** — `where-batteries-stop`'s `eight-hour-native` handoff, which named *From Cell to Container* as the home of the formats, the manufacturing and the yield. One `revisions[]` entry, that section id alone, `updated` advanced, no pin moved
- **Ten public inputs, one dropped.** `study:envision-energy` read and dropped under §3.3; the other nine study guides plus `concepts:profiler-concepts` pinned at their own dates
- **§7.3's Phase 4 row and §6's Phase 4 row flipped to 19 of 26**, curriculum §7 row 19 marked built with the full record, §4's table updated at positions 5 and 6, **§10.6's (ff1)–(ff6) findings written**, and **§7.45 written in the same commit** — the brief for **S2 session 15, `cooling`** — both halves
- **Classroom GAS v01.59g → v01.60g**, both files; page version unchanged at v01.14w; content checker's module assertion unchanged at **23**
- **THE TWELVE-SESSION CHANGELOG DEFERRAL ENDED.** The push landed **2026-09-17 EST** and this session was the rotator: `CHANGELOG.md` cleared **two** date groups (2026-09-08's 10, 2026-09-09's 11) and `Classroomgs.changelog.md` cleared **two** (2026-09-01's 2, 2026-09-02's 11)

### Where we left off

Everything is merged and live. **Phase 4 is 19 of 26; S2 is 14 of ~19**, so the interleave stands at 19:14 and hands the turn to **S2 session 15, `cooling`** — briefed in **§7.45**, written this session. Content checker **0/0 at 49 lessons / 8 tracks / 142 gate cases**, module assertion **23**. `--check` reports **7 due**.

### Key decisions made

- **§10.6 (t) FIRED THREE TIMES INSIDE ONE TRACK AND ONE FIRING IS A THIRD CLASS — (ff1).** Not a pre-declared handoff honoured (session 6) and not an undeclared pre-emption (row 18), but a **declared handoff naming the wrong sibling**: `where-batteries-stop` hands *"the formats, the manufacturing, the yield"* to *From Cell to Container*, in which `prismatic`, `pouch`, `cylindrical`, `slurry`, `calender`, `formation`, `slitting` and `yield` each appear **zero** times. **A declared handoff is not self-maintaining**, and (bb1)'s same-commit correction rule was extended from guidance modules to lessons on its own argument — hand-authored prose has no detector, and a lesson's prose has exactly as little
- **A FOURTH OUTCOME FOR THE THREE-PART DEEPENING TEST — (ff2), failure at PART ONE.** All 27 `CRITERION_LEXICON` entries parsed; **none names `how-a-cell-is-made`**, so part three never runs and the `what-is-bought-and-on-what` half cannot fire whatever the segment looks like. Distinct from (ee4)'s lexicon-coverage failure, which passes the guard and misses on keywords. **A Phase 4 row's due-count forecast is therefore fully derivable**: run the lexicon test before writing, and when part one fails the only possible change is `read-next`
- **`reviewBy`'s EMPTY-SET SHAPE REACHED A LESSON — (ff3), and the boundary of "its own material" is what decided it.** The nine stamped guides carry **not one day-level date of any kind**. A genuine future date (**2027-10-01**, the NDAA §154 procurement ban) sits in three members' **dossiers** and was rejected on three grounds, the first general: a lesson's `reviewBy` is the nearest gate in the material it is **stamped** on, not in the corpus behind it
- **THE DRILL'S ANSWER INDEX IS NOT RANDOM — (ff4), measured and left for the developer.** **160 of the 240 quiz items authored before this row sit at index 0** and `Classroom.html` does not shuffle. This row's own five were authored at 0, 2, 1, 3, 0; **no other lesson was touched**, because re-ordering a shipped item's choices changes its content hash and re-introduces it to every account that already learned it
- **A GATED LESSON'S TITLE IS TREATED TWO WAYS — (ff5).** The track view withholds `spec-sheet-decoded`'s title behind `withheld: 1`; the generator's `read-next` publishes it in two shipped **public** `segment-*` lessons. Recorded, nothing changed on either surface
- **THE SCREENSHOTS CAUGHT TWO THINGS FOR A SEVENTH CONSECUTIVE SESSION, AND THE CLASS GENERALISED AGAIN — (ff6).** `three-formats` told the reader to read three cards against **four** named questions and the Prismatic card answered **three**. No checker sees that and no recount finds it, because the claim is about the **completeness** of adjacent content. **The standing instruction takes a third clause: check every enumeration the prose promises the adjacent content will answer**

### Active context

- **Repo version v06.31r** · **`CHANGELOG.md` 98 sections** against a 100 trigger after the session's **two** rotated date groups; `CHANGELOG-archive.md` now holds **432** sections, every rotated header SHA-enriched and the post-rotation grep clean. **`Classroomgs.changelog.md` 47/50** after its own two groups; its archive holds **13** and was empty before. `Scrapergs.changelog.md` **36/50**; `Profilerhtml.changelog.md` **49/50**, closest to its own trigger and untouched
- **Three rotation bugs were found in a DRY RUN and fixed before anything was committed** — a mandatory practice to carry: the archived date groups landed **oldest-first** (the archive is reverse-chronological, so the newest group goes on top); the seam between the rotated block and the pre-existing archive lost its **blank line**; and the source file's trailing **`Developed by:` footer was dragged into the archive**, duplicating it, because the oldest date group sits at the bottom of the file and the slice ran to end-of-file
- **GAS versions:** Classroom **v01.60g**, Scraper v02.15g, Profiler v01.39g. **Page versions:** Classroom v01.14w — no page bump
- **Content-checker baseline 0 errors / 0 warnings at 49 lessons, 8 tracks, 142 gate cases**, module assertion **23**. An S2 session registers the twenty-fourth module and **must edit that assertion to 24 in the same commit**
- **`--check` reports 7 due** — `cells-and-chemistry`, `power-conversion-and-rack-power-silicon`, `cooling`, `compute-and-the-rack`, `storage-developers-and-ipps`, `software-and-optimization`, `insurance-and-risk-transfer`. **Both of this session's numbers were called before the run and both held: 7 → 7**
- **`check-classroom-curriculum.py --strict`** — no structural findings, **28 stale pins**, **3 items due for review** (`landscape-utilities-2026-09` and `landscape-in-hall-power-2026-09` at 2026-10-01, `landscape-neoclouds-2026-09` at **2026-09-30**). All three by design — **do not "fix" any**
- **Pipeline signature exactly as forecast: P1 on the two plan files, P5 on the mid-array insert, no P3, no P8, no P12.** Selftest 13 fixtures / 0 failures
- **Deployment counters.** Pages read `|v01.59g|` at 12:03 AM EST before the push and `|v01.60g|` at 12:04 AM after; the job log confirms **`Classroom deploy confirmed (GET): Updated to v01.60g (deployment 72) | 72/200`** — exactly **one** step firing in 26 s, every other `Deploy <Project>` step exiting 0 silently in zero seconds on its own `git diff` guard. **Classroom is now at 72/200 with 128 of its allowance left; Scraper unchanged at 160/200 with 40.** The §7.19 order resolved on its **two reads** for the **twenty-seventh** time and no `?op=deploy` probe was made. Counter rule unchanged: `Updated to` consumes one; a step that never fires consumes none and logs nothing; a leg that fires and does not confirm still consumes one and the tally does not see it
- **Toggles:** `START_OF_RESPONSE_BLOCK` On · `CHAT_BOOKENDS` Off · `TIMING_ESTIMATES` On · `END_OF_RESPONSE_BLOCK` On · `MULTI_SESSION_MODE` Off
- **Where the programme stands.** Done: Phases 0, 1, 2a, 2b, 3, 5, 6; S0, K1, S1, S3, G6, C3, the bankability review, the one-off segment regeneration. Open: **Phase 4 at 19 of 26**, **S2 at 14 of ~19** (the five remaining: `cooling`, `compute-and-the-rack`, `assurance`, `software-and-optimization`, `insurance-and-risk-transfer`, all five already in the standing due set and all five now at §10.2's floor), **K2** (1–2 sessions, runnable any time), **C5** (~6 sessions; its gate is met), the **Q plan clock** (next due ~2026-12). Deferred: C6. **About 19 sessions remain**
- **Which S2 segment is next needs a sentence of honesty.** The §7.3 sales-value order is **not written out as a list anywhere**. Only positions 18 and 19 are fixed — the v05.62r CHANGELOG record puts `assurance` and `insurance-and-risk-transfer` there. §7.45 takes **`cooling`** for 15 on three re-testable grounds (deepest of the three unranked at eleven members; its mechanism lesson landed one day earlier; chain position 8 right after the seven built supply segments) and says to take the developer's order if they have one
- **Open, recorded, not taken.** (i) The `.cl-tbl` `min-width` question — **twenty-nine** data points, this session's three-column six-row defect table the latest and clean at 1400 px. (ii) The generated `read-next` **names** the landscape module without linking to it. (iii) `the-800-vdc-shift`'s stale `three-chains` intro is **twenty-three** registrations stale. (iv) §10.6's session-4 bullet block is out of chronological order. (v) **A stale `lesson_ref` can make a segment due on its own** — now **seven** segments, `cells-and-chemistry` added by this row. (vi) The DC-arc taxonomy gap stays with §7 row 20. (vii) `restart` scores zero across all Scraper terms — a **sixth** session unseeded. (viii) Session 12's six `topic-aidc-landlords` terms are in no seed. (ix) `CRITERION_LEXICON` has no entry for a criterion an eligible built lesson already teaches ((ee4)). (x) **NEW — (ff4) the answer-index pattern and (ff5) the gated-title asymmetry**, both measured and both the developer's call. (xi) **NEW — `Scraper.gs` holds only SEVEN `topic-landscape-*` seeds against FOURTEEN built landscapes**, so seven S2 sessions seeded nothing; count the quoted literals, because a loose grep returns eight (one seed is split across a line break). (xii) **NEW — `INTEGRATED-REMEDIATION-PLAN.md`'s `Developed by:` footer now sits mid-file**, because §7.44 and §7.45 were appended after it; every future brief will do the same, so the fix is a rule decision rather than a move
- **Render harness — unchanged, and one new fact.** PROJECT region of `Classroom.gs` (line 1 to `// TEMPLATE START` — **recompute it**; it was **3,407,759 bytes** after this session) in a Node `vm`. Shim **only** `validateSessionForData` (permissions an **ARRAY**) plus no-op `clProgressRead_`/`clProgressWrite_`/`clStudyNext_`/`dataAuditLog`/`auditLog`, **after** the module loads. Route `payload.action === 'guidance'` → `ctx.handleGuidanceOp_`, else `ctx.handleClassroomOp_`, **through the vm context object**; pass the op as `{ parameter: payload }`. **The session parameter is `session`** and must be ≥ 32 characters. **Chromium is `/opt/pw-browsers/chromium-1194/chrome-linux/chrome`** — note that `/opt/pw-browsers/chromium/` DOES exist as a directory in this container but holds no binary, so the versioned path is still the one to use. **Serve static files and `/__gas` from ONE Node process on ONE port**, at **`/Classroom.html`**. `_e = ''`; patch the `var AUTO_REFRESH = true;` declaration; copy `profiler-data/`, `sounds/`, `images/` and `favicon.ico` in. Seed `sessionStorage` and override `window._gasPost` (returning a **Promise**) **after** load. Hide `#auth-wall` / `#signing-out-wall` / `#auth-timers` / `#gas-layer-toggle` plus `#cl-app{display:block !important}`. Lesson entrypoint `clShowLesson(id)`, module entrypoint `clShowGuidanceDoc(id)`; section nodes are `#cl-<sectionId>`. **`ps -eo pid,args | grep -E "node .*shim"` is the pattern that works**, and **re-extract the PROJECT region after every edit** — this session's two re-renders moved **25,968 → 26,279 → 26,328**
- **This session's render: analyst 7/7 sections, 26,328 characters, zero page errors**, zero unresolved `{{…}}`, zero literal asterisks, zero backticks, DOM non-ASCII limited to `U+00B0`, `U+00B7`, `U+2014` and the page's own back-arrow. Every section's screenshot read individually. Through the real serving path: **viewer → the page-level denial and 0/7 sections**, **token < 32 chars → `SESSION_EXPIRED`**, **analyst → the full seven-section lesson**, and the analyst's track view returning **five lessons and `withheld: 1`**. The two `ERR_CERT_AUTHORITY_INVALID` console errors are environmental
- **The micro-markup simulation was run BEFORE the splice and caught three defects in one pass** — a nested italic inside a bold, an unresolved `{{battery passport}}`, and two glossary entries defined and never used. **Run it before the splice, not after**

### Recommendation for next session

- Run **S2 session 15 — the `cooling` landscape module** on Opus 5 xhigh. Its brief is **`INTEGRATED-REMEDIATION-PLAN.md` §7.45**, written this session with the roster, the three-number forecast and the split already measured. Four things carry the attention: **which segment is next is a judgment, not a lookup** — only positions 18 and 19 of the sales-value order are on the record, so the brief argues `cooling` for 15 and says to take the developer's order instead if one exists; **the module assertion in `check-classroom-content.py` must move 23 → 24** in the same commit or the checker stops the session; **§10.6 (t) now has three shapes** and both of this segment's `READ_NEXT` neighbours are public lessons, one of them a day old; and **`topic-landscape-cooling` is not seeded** — nor are five other built landscapes, which is an observation for the developer rather than a backlog to clear in one session.

**To continue:** type `run S2 session 15`

## Previous Sessions

### Previous Session — v06.29r

**Date:** 2026-09-16 10:36:02 PM EST
**Repo version:** v06.29r — two pushes (v06.28r the module, v06.29r this handover)
**Branch:** `claude/serene-volta-dn6g6z` — restarted from `origin/main` before the push

#### What was done

- **S2 session 14 — the `epc-and-construction` landscape module** (v06.28r). The **fourteenth** landscape and the **twenty-third** guidance module: `guidanceDocLandscapeEpcAndConstruction_()`, id `landscape-epc-and-construction-2026-09`, lane The Value Chain, tier **contributor**, authored **below** the `// CONTENT END` fence and registered at the END of `guidanceDocs_()`'s Value Chain lane. Nine sections at §10.6's ids and order, four tiles (all `k` in pure ASCII), a **ten-entry** glossary, an **eighteen-row** `each-players-bet` table, a **thirty-three-row** claims ledger, 15 drill cards, 7 quiz items. `updated` 2026-09-16, `reviewBy` **2027-03-16**
- **`landscape-epc-and-construction-analysis.md`** — 839 lines, §2 (the split against **nine** neighbours) drafted before any other section, §2g enumerating **22** omissions
- **Scraper seeded** with `topic-landscape-epc-and-construction` — ten terms audited against all **472** distinct terms
- **Exactly one segment regenerated** (`--segment epc-and-construction --today 2026-09-16`), appending one `revisions[]` entry with `changed: [read-next, what-is-bought-and-on-what]`
- **Classroom GAS v01.58g → v01.59g and Scraper v02.14g → v02.15g**, both files each; page versions unchanged at v01.14w; content checker's module assertion **22 → 23**
- **§7.3's S2 row, §6's S2 row and §10.6's progress note flipped to 14 of ~19**, the §6 row's stale next-segment pointer replaced with the five remaining segments, `epc-and-construction` struck from the S2-needs attach-map list, the **(ee1)–(ee7)** findings written, and **§7.44 written in the same commit** — the brief for **Phase 4 row 19, `how-a-cell-is-made`** — both halves

#### Where we left off

Everything is merged and live. **Phase 4 is 18 of 26; S2 is 14 of ~19**, so the interleave stands at 18:14 and hands the turn to **Phase 4 row 19, `how-a-cell-is-made`** — briefed in **§7.44**, written this session. GAS live at Classroom **v01.59g** and Scraper **v02.15g**. Content checker **0/0 at 48 lessons / 8 tracks / 142 gate cases**, module assertion **23**. `--check` reports **7 due**.

#### Key decisions made

- **THE SIXTEEN-FIRM RANKING WAS DECLINED AND THE ONE THE RECORD SUPPORTS WAS PUBLISHED WITH BOTH CONDITIONS THE CORPUS ATTACHES — (ee1), the inverse of session 13's (dd1) rather than a repeat.** 19 members, **16 · 2 · 1**, no registry `notes`, four *kinds* of basis. The **ENR 2026 Top 400** ranks **13 of 19** (12 of 16 incumbents); **six are absent, including the #1 and #2 US electrical contractors** (#1 at 3.7× the runner-up). Condition one is stated inside the corpus by a member about its own rank — *"ENR publishes several revenue bases across different lists, so 'ENR rank' is meaningless without naming the list"* — and the roster proves it twice more (one member **#1 in Power design eleven years and #35 as a contractor**; another **#1 Hydrogen / #6 Power / #49 contractor**; **two different members both carry '#6'**). Condition two is that the **movement**, not the level, is the signal: the table grew **11.8 %** while its telecom category grew **86.4 %**, so a firm **fell #4 → #5 while adding USD 1.4bn**
- **THE GRAPH SPLIT THE SEGMENT INTO TWO MARKETS AND THE CUT IS A NODE AND AN EDGE — (ee2).** 53 edges among the nineteen, 45 curated, 57 typings (`competitor` **39**, `partner` 16, `investor` 1, `other` 1, **zero untyped curated pairs**), **35 competitor edges across 20.5 %** of all 171 pairs. The competitor subgraph is **one component of 18 that falls into 6 + 10 + 1** when `mortenson` (the only member typed `competitor` on **both** sides — 3 GC-side, 4 power-side) and the single edge `mccarthy`↔`strata-clean-energy` are cut. **The Top 400 order runs ACROSS that boundary** (#1 and #5 in the data-hall market; #2/#4/#6 in the power market), so the one publishable ranking compares firms the graph says do not compete. `blattner` hangs off the second market by one edge because its link to its own parent is typed `investor`/`other`. **`samsung-ct` has ZERO edges of any kind** — the only member with degree zero
- **THE BENCH INVERTS DOWNWARD 6 OF 6 — (ee3), (aa2)'s MIRROR AND THE HIGHEST RATE MEASURED.** 8 of 19 shared, 11 memberships across 6 neighbours (5 with a built landscape), **10 inversions (91 %**, past (aa2)'s 74**)**, 9 down / 1 up / 1 equal, and **every one of the six shared incumbents is `adjacent` in every other segment it appears in**. The instrument: *when a segment's incumbents are demoted everywhere else, the segment is the services layer the rest of the chain borrows a builder from.* The lone upward inversion confirms it — a **developer** with an EPC arm, ranked where it develops and demoted where it builds
- **A FOURTH MECHANISM FOR A DASH ROW — (ee4).** Three dashes, three diagnoses. Criterion 6 is **(y3)** and total (`TRIR`, `recordable`, `incident rate`, `experience modification`, `surety` each score **zero across all 48 lessons**); criterion 5's `open-book` half is **(cc3)** exactly; **criterion 4's material IS taught, by a BUILT lesson that IS in this segment's `READ_NEXT`** (`how-a-storage-project-happens` teaches `percentage-of-completion` and the performance bond), and the row still dashes because `CRITERION_LEXICON`'s entry for that lesson matches **none** of the criterion's words. **The intersection did not fail; the keyword table did** — a *generator* gap, the only one of the four a generator edit could fix
- **`reviewBy` TOOK THE SIX-MONTH DEFAULT FOR THE FIRST TIME IN FOURTEEN LANDSCAPES — (ee5), and the sort returned an EMPTY set rather than a distant one.** The fence holds **21 entries across 19 members (1.1 each, the thinnest measured), 10 members carrying none, 6 dated, ZERO future, 20 `in-effect` against 1 `announced`** — a regime already in force has no future date to ring. All **four** future day-level dates in the corpus rejected (a securities lead-plaintiff deadline; an *estimated* earnings date the dossier itself calls unconfirmed; the OBBBA cliff, on-subject but later than the default and already twice a `reviewBy`, kept as indicators row 6; a buyback expiry), plus six month- or window-level candidates including **the next ENR Top 400 edition — the one thing that genuinely clocks the module, published annually on no announced day**
- **THE SCRAPER ZERO RATE WAS ESSENTIALLY TOTAL — (ee6).** 497 raw / **472 distinct**; **49 candidates scored, 45 zero**, and the four non-zero are not construction terms at all. **The roster has never held a construction seed**, so every drop is editorial. Criterion 1 is covered by the segment one link up (`speed to power` already seeded); **criterion 3 is blocked in both directions for a third time** and is recorded unseeded rather than served badly
- **THE SCREENSHOTS CAUGHT TWO DEFECTS FOR A SIXTH CONSECUTIVE SESSION, AND BOTH ARE THE RECOUNT CLASS — (ee7).** `the-indicators` asserted twice that **two rows carry a day** beside a table where **three** do (four days between them, one still ahead); `what-the-record-does-not-say` said *eleven of the nineteen are private* where the `ownership` field gives **12 private, 5 public, 2 subsidiary**, and its triple-disclosure claim was replaced with the measurable one. Row 18's glyph countermeasure held — **all four `tiles[].k` in pure ASCII**, the DOM carrying only `U+2014`, `U+00B7` and the page's own back-arrow

#### Active context

- **Repo version** v06.29r · **`CHANGELOG.md` 118 raw / 93 non-exempt** against a 100 trigger, twenty-five sections dated 2026-09-16 EST. **NO ROTATION FOR A TWELFTH CONSECUTIVE SESSION**, for the clause every brief since §7.36 has recorded: **the deferral is a function of the day of the push, not of the section count.** `TZ=America/New_York date` read **2026-09-16** at the push while the harness banner read 2026-09-17 UTC. It lapses on the first push dated **2026-09-17 EST or later**
- **`Classroomgs.changelog.md` 59 raw / 48 non-exempt** against a cap of 50, eleven same-day sections — same deferral, same lapse. Re-counted from the files: `CHANGELOG.md` clears in **ONE** group (2026-09-08, **10** sections); `Classroomgs.changelog.md` needs **TWO** (2026-09-01's **2**, then 2026-09-02's **11**). `Scrapergs.changelog.md` **36/50**; `Profilerhtml.changelog.md` **49/50**, closest to its own trigger
- **GAS versions:** Classroom **v01.59g**, Scraper **v02.15g**, Profiler v01.39g. **Page versions:** Classroom v01.14w — no page bump
- **Content-checker baseline 0 errors / 0 warnings at 48 lessons, 8 tracks, 142 gate cases**, module assertion **23**. A Phase 4 row takes the lesson count to **49** and leaves the assertion at 23
- **`--check` reports 7 due** — `cells-and-chemistry`, `power-conversion-and-rack-power-silicon`, `cooling`, `compute-and-the-rack`, `storage-developers-and-ipps`, `software-and-optimization`, `insurance-and-risk-transfer`. **All three of this session's numbers were called before the run and all three held: 8 → 8 → 7**
- **Row 19's numbers are already measured for §7.44.** `--check` **7 → 7** (the HOLD case): `how-a-cell-is-made` sits in exactly one segment's `READ_NEXT` — `cells-and-chemistry`, already due and differing in `read-next` alone — and **no `CRITERION_LEXICON` entry maps to it at all**, so there is **no deepening**. **THE G2 TRAP ON THAT ROW IS TEN OF TEN**: every one of its ten `study:` guides has a `lastUpdated` that differs from its `git log` date (four at 2026-08-21 vs 2026-08-31; `hithium` 2026-08-08 vs 2026-08-31; five at 2026-09-06 vs 2026-09-07). The `concepts:` commit date is **2026-09-13**
- **`check-classroom-curriculum.py --strict`** — no structural findings, **28 stale pins**, **3 items due for review**: `landscape-utilities-2026-09` and `landscape-in-hall-power-2026-09` at 2026-10-01, `landscape-neoclouds-2026-09` at **2026-09-30**. All three by design — **do not "fix" any**. This module's 2027-03-16 sits well outside the horizon
- **Deployment counters.** Pages read `|v01.58g|` and `|v02.14g|` at **10:25 PM EST before the push**; this session moved **both** `.gs` files, so it forecast **two** steps and two logged lines, Classroom **70 → 71** and Scraper **159 → 160**. Read v06.28r's own job log for what fired. Counter rule unchanged: `Updated to` consumes one; a step that never fires consumes none and logs nothing; a leg that fires and does not confirm still consumes one and the tally does not see it; `Already up to date` is silent on consumption. **Scraper headroom is 40 and only an S2 seed moves it**
- **Toggles:** `START_OF_RESPONSE_BLOCK` On · `CHAT_BOOKENDS` Off · `TIMING_ESTIMATES` On · `END_OF_RESPONSE_BLOCK` On · `MULTI_SESSION_MODE` Off
- **Where the programme stands.** Done: Phases 0, 1, 2a, 2b, 3, 5, 6; S0, K1, S1, S3, G6, C3, the bankability review, the one-off segment regeneration. Open: **Phase 4 at 18 of 26**, **S2 at 14 of ~19** (the five remaining: `cooling`, `compute-and-the-rack`, `assurance`, `software-and-optimization`, `insurance-and-risk-transfer`, **all five already in the standing due set**), **K2** (1–2 sessions, runnable any time), **C5** (~6 sessions; its gate is met), the **Q plan clock** (next due ~2026-12). Deferred: C6. **About 20 sessions remain**
- **Open, recorded, not taken.** (i) The `.cl-tbl` `min-width` question wants a session that edits `Classroom.html` — **twenty-eight** data points, this session's four-column eighteen-row bet table the latest and clean at 1400 px. (ii) The generated `read-next` **names** the landscape module without linking to it. (iii) **`the-800-vdc-shift`'s stale `three-chains` intro is now twenty-two registrations stale.** (iv) §10.6's session-4 bullet block is out of chronological order. (v) **A stale `lesson_ref` can make a segment due on its own** — six segments, and row 19 makes `cells-and-chemistry` the seventh. (vi) The DC-arc taxonomy gap stays with §7 row 20. (vii) **`restart` scores zero across all 472 Scraper terms** — a **fifth** session unseeded. (viii) The second standing reservation debt: session 12's six terms assigned to `topic-aidc-landlords` are in no seed and still score zero, re-confirmed. (ix) **NEW — `CRITERION_LEXICON` has no entry matching a criterion whose material an eligible built lesson already teaches** ((ee4)), so a dash row can be a *generator* gap; the only one of the four mechanisms a generator edit could fix, and it wants a developer decision
- **Render harness — unchanged, and two new facts.** PROJECT region of `Classroom.gs` (line 1 to `// TEMPLATE START` — **recompute it**; it was **60006** after this session) in a Node `vm`. Shim **only** `validateSessionForData` (permissions an **ARRAY**) plus no-op `clProgressRead_`/`clProgressWrite_`/`clStudyNext_`/`dataAuditLog`/`auditLog`, **after** the module loads. Route `payload.action === 'guidance'` → `ctx.handleGuidanceOp_`, else `ctx.handleClassroomOp_`, **through the vm context object**; pass the op as `{ parameter: payload }`. **The session parameter is `session`, not `t`** — `handleGuidanceOp_` reads `p.session` and returns `SESSION_EXPIRED` under 32 characters. **The Chromium binary is `/opt/pw-browsers/chromium-1194/chrome-linux/chrome`** — the unversioned `chromium/` path does not exist. **Serve the static files and `/__gas` from ONE Node process on ONE port**, at **`/Classroom.html`**, not the site root. `_e = ''`; patch the `var AUTO_REFRESH = true;` **declaration**; copy `profiler-data/`, `sounds/`, `images/` and `favicon.ico` in. Seed `sessionStorage` **after** load; override `window._gasPost` **after** load and return a **Promise**. Hide `#auth-wall` / `#signing-out-wall` / `#auth-timers` / `#gas-layer-toggle` plus `#cl-app{display:block !important}`. Module entrypoint `clShowGuidanceDoc(id)`, lesson entrypoint `clShowLesson(id)`; section nodes are `#cl-<sectionId>`. **`ps -eo pid,args | grep -E "node .*shim"` is the pattern that works**, and **re-extract the PROJECT region after every edit** — this session's re-render moved **62,894 → 63,051**, which is the evidence to demand
- **This session's render: contributor 9/9 sections, 63,051 characters, zero page errors**, zero unresolved `{{…}}`, zero literal asterisks, zero backticks, every section's screenshot read individually. Through the real serving path: **analyst → `ROLE_DENIED`** (and 0/9 sections with *"Industry Guidance is available to the admin and contributor tiers"* at the page level), **viewer → `ROLE_DENIED`**, **token < 32 chars → `SESSION_EXPIRED`**, **admin and contributor → the full nine-section module**. The two `ERR_CERT_AUTHORITY_INVALID` console errors are environmental
- **Pipeline signature 4 × P1 and 9 × P2 exactly as forecast** — P2 is what a developer diff below the content fence reports; **no P3** (no gate symbol moved, so `gateDigest` is untouched), **no P5** (the module appends and the segment lesson is replaced in place), **no P7** (the base literal was dated 2026-09-14, not the same day), no P8, no P10, no P12

#### Recommendation for next session

- Run **Phase 4 row 19 — the `how-a-cell-is-made` lesson** on Opus 5 xhigh. Its brief is **`INTEGRATED-REMEDIATION-PLAN.md` §7.44**, written this session with every number already measured. Three things carry the attention: the **G2 pin trap is ten of ten** — every one of the row's ten `study:` guides has a `lastUpdated` that differs from its `git log` date, so a session taking the commit date writes ten wrong pins and no checker sees one; the due count is the **hold** case (**7 → 7**) with **no deepening**, because no `CRITERION_LEXICON` entry maps to the lesson at all; and §10.6 **(t)** will fire on **two lessons in its own track** (`cell-to-container` at position 1 and `spec-sheet-decoded` at position 3), so grep both plus `where-batteries-stop` before writing a section.

**To continue:** type `run Phase 4 row 19`
