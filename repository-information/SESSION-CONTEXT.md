# Previous Session Context

Claude writes to this file when the developer says **"Remember Session"** — capturing enough context for a future session to pick up the train of thought quickly. This is separate from "Reminders for Developer" (REMINDERS.md), which is the developer's own notes.

> **Note on stale-context auto-reconstruction** — when a session starts and this file's `Repo version:` doesn't match the current repo version, Claude reconstructs the missing entry from CHANGELOG.md and commits it **without pushing**. The commit rides along with the session's first user-task commit on the next push. If a session ends before any user-task push happens, the reconstructed entry stays **local-only** and the next session will just re-reconstruct from CHANGELOG if still stale. This is intentional — pushing a dedicated reconstruction commit on its own would force every subsequent user push in the same session to wait for the auto-merge workflow to finish before it could push too (push-once enforcement). The reconstructed entry is a convenience hint, not load-bearing state, so the small persistence risk is a fair trade.
## Latest Session

**Date:** 2026-09-14 10:30:54 PM EST
**Repo version:** v05.77r — two pushes (v05.76r the lesson, v05.77r this handover)
**Branch:** `claude/practical-ramanujan-dr9g6d` (restarted from `origin/main` before each push)

### What was done

- **Phase 4 row 8 — the `redundancy-by-the-numbers` mechanism lesson, titled *N+1, 2N, and Six Nines*** (v05.76r). The **eighth** mechanism lesson and the **fourth mid-track insertion**: eight sections at the ids curriculum plan §3.3 fixes, in its order, inside the `// CONTENT START … // CONTENT END` fence — `the-vocabulary` (table, 4 × 6) · `pricing-a-nine` (**bars**) · `the-tiers` (prose) · `commissioning` (**timeline**, 9 items across 3 lanes) · `the-vanishing-ups` (callout) · `where-it-fails` (**table**, 4 × 5) · `drill` (6 cards) · `check-yourself` (5 items). Four tiles, a six-term glossary, `short` at 160 against the 160 budget, `sales` on 4 of 8, **seven distinct section kinds**
- **The first Phase 4 row to carry a `bars` section and the first to render `where-it-fails` as a table** rather than the callout every predecessor used. Both rendered clean at 1400px
- **Inserted at `aidc-grid-to-chip` `lessons[]` index 5, immediately before `inside-the-rack`** — §4's position 7 — with the track's running `short` given its missing clause. **P1 + P5, no P3, no P8**, the signature rows 3, 4, 6 and 7 set
- **All eleven stamps pinned**, each date read off a document fetched that run and identity-matched on `slug`; nothing was dropped, because §3.3 names exactly eleven and every one carries a named section
- **Rendered at analyst AND contributor with the real serving functions — 8/8 sections, zero page errors at both**, and every section screenshot was read, `pricing-a-nine` first and `where-it-fails` second
- **v05.77r (this push)** — §7.23's deployment counter refreshed from forecast to measurement, its CHANGELOG figures moved to the post-push state, and this handover

### Where we left off

Everything is merged and live. **Phase 4 is 8 of 26; S2 is 3 of ~19.** GAS live at Classroom **v01.37g**, Pages serving `|v01.37g|` and `|v01.14w|`, both confirmed. The content-checker baseline is **0 errors / 0 warnings at 38 lessons / 8 tracks / 142 gate cases** and was held across the whole session. `build-classroom-segments.py --check` reports **5 due** — two are row 7's residue, three are row 8's, and all five belong to S2.

### Key decisions made

- **The brief's due-count prediction was right in range and wrong in mechanism, and the mechanism is the transferable half.** §7.22 described **two independent channels** by which registering a lesson makes segments due — `READ_NEXT` membership and the buying-criteria lexicon — and predicted 5 to 7. The count came in at **5**, and the reason is that the channels are not independent: `sec_what_is_bought` in `scripts/build-classroom-segments.py` guards its lexicon match with **`if lid in eligible`**, where `eligible = set(READ_NEXT.get(seg["id"], []))`. **The lexicon is subordinate to `READ_NEXT`.** `grid-equipment` and `hyperscalers-and-ai-labs` each carry a criterion containing the word "availability" and **neither could ever have gone due**, because `redundancy-by-the-numbers` is not in their `READ_NEXT` lists. **The bound for every future Phase 4 row is |READ_NEXT membership| + prior residue** — a checkable number rather than a range
- **The five due, and why one of them differs in two sections.** `in-hall-power` (`read-next` **and** `what-is-bought-and-on-what`, because its criterion reads "installed MW per protected MW under the chosen redundancy topology" and hits `"redundan"`), `aidc-developers-and-landlords` and `neoclouds` (`read-next` only), plus `power-conversion-and-rack-power-silicon` and `compute-and-the-rack` carried from row 7. `PLANNED_TITLES["redundancy-by-the-numbers"]` was already byte-identical to the authored title, so the whole diff on each is a `" (planned)"` marker disappearing. **Nothing regenerated**
- **`reviewBy` took the six-month default (2027-03-14) with both candidates rejected in writing.** A scan of all ten study guides for future-dated material returned exactly two hits. A nuclear deployment milestone stated as *"targeted for late 2027"* — a **window rather than a date**, and decisively **on a topic no section of the lesson teaches** (session 1's Cummins test and S2 session 2's "late 2026" test applied together). And one campus's construction clock, whose only forward entries are a 2027 workforce peak and a 2028 completion — **a project schedule, not a rule taking effect**
- **A source contradiction was taught rather than smoothed over.** One fetched guide reports, inside a single document, an industry expectation of roughly ninety-six hours of on-site ride-through at the top certified Uptime rung **and** the self-declaring operator's own framing of that same rung as a twelve-hour benchmark its hundred-hour standard beats. Rather than pick a figure or drop both, `the-tiers` teaches the gap as the point — *the benchmark a self-declared standard beats is chosen by the party declaring it* — which makes the guide's own stated lesson ("a self-declared standard can change definitions") concrete
- **`bars` needed a written answer to §8 item 6's "numbers the source actually states".** Two rungs come straight off the sources (three nines ≈ nine hours and four nines < one hour from one guide; six nines ≈ thirty-one seconds from another); five nines and the exact three-nines figure are **the same division of the same year**, and the section's `note` says so in as many words. On a linear axis the five- and six-nines bars both sit at the renderer's `min-width: 3px` floor while the value column carries the discrimination — the section's own argument rendering as itself, not a defect
- **The `.cl-tbl` `min-width` question is now diagnosed rather than open.** `where-it-fails` is a **four**-column table — the same column count as the v05.57r defect — and rendered clean at 1400px, as did the 4 × 6 vocabulary table. Seven clean data points. The difference is the first column's content: the defect's held one long unbreakable word, these hold short wrapping phrases. Closing it formally needs a `Classroom.html` edit and a page bump
- **Two render-harness gaps found and closed, both of which would have wasted a future session.** The overlay to hide is **`#auth-wall`** (plus `#signing-out-wall`, `#auth-timers`, `#gas-layer-toggle`) — the recorded recipe's generic selector list misses it, and without it `el.screenshot()` **silently captures the sign-in wall instead of the section**, which is exactly what the first `pricing-a-nine` shot showed. And the scratch site root needs **`favicon.ico`** copied in beside `profiler-data/`, `sounds/` and `images/`, or the render reports a 404 that belongs to the harness. Both identified **by URL / by DOM**, not assumed
- **The EST/UTC trap fired for the whole session and was navigated correctly.** `TZ=America/New_York date` read **2026-09-14** while the harness clock read **2026-09-15 UTC** from the first tool call onward. Reading the wrong one would have forced a repo-CHANGELOG rotation that was not due
- **The deploy cost nothing for the fifth time running.** Pages and the job log agreed, so **no `?op=deploy` probe was issued at all**: `Classroom deploy confirmed (GET): Updated to v01.37g (deployment 48) | 48/200`, on the first GET leg with no `POLL` line. Scraper stayed at 149/200
- **The stop-hook false positive did not recur, because the inoculation was applied.** `git fetch --prune origin` after the merge — rather than `git fetch origin main` — cleared the stale remote-tracking ref, so the hook saw 0 unpushed commits instead of counting the workflow's own `[skip ci]` SHA-tracker commit

### Active context

- **Repo version** v05.77r · **CHANGELOG** 117 raw / **98 non-exempt** (19 sections dated 2026-09-14). **The next push landing 2026-09-15 or later rotates** — that group stops being exempt, the count is 117, and the oldest whole group is **eighteen sections dated 2026-09-05 (`v04.61r`–`v04.78r`)**, leaving **99**. **Re-count on the EST clock** (`TZ=America/New_York date`), and match the **standalone** counter line — that string also appears inside older entries
- **Standing changelog traps, neither of them S2 session 4's by default:** `Scrapergs.changelog.md` at **51 raw / 49 non-exempt** is **still armed** — the next session to touch `Scraper.gs` on a later EST day fires rotation (oldest group: twenty-six sections dated 2026-08-28), **and S2 session 4 must ask the Scraper-seed question, so it may well be that session**. `Profilerhtml.changelog.md` sits at 49/50. `Classroomgs.changelog.md` 37/50 with 29 non-exempt
- **GAS versions:** Classroom **v01.37g**, Scraper **v02.04g**, Profiler v01.39g. **Page versions:** Classroom **v01.14w**, Profiler v01.90w, Scraper v01.72w
- **Content-checker baseline is 0 errors / 0 warnings at 38 lessons / 8 tracks / 142 gate cases** — protect it; nothing inherited remains. Its **guidance-module count assertion is 12**, and **S2 session 4 must move it to 13** in the same commit — the one edit outside the fence and the one that stops a session dead if met cold
- **Deployment counters:** Classroom **48/200**, Scraper **149/200**. **The v05.59r poll is armed and has still never been needed**
- **`--check` reports 5 due** — `power-conversion-and-rack-power-silicon`, `in-hall-power`, `compute-and-the-rack`, `aidc-developers-and-landlords`, `neoclouds`. **None is `utilities`.** Registering `landscape-utilities-2026-09` will make `segment-utilities` go due on `read-next` (the generator renders "**No landscape module yet.**" until a `landscape-<segment-id>-YYYY-MM` id exists in `guidanceDocs_()`), so S2 session 4 should expect **5 → 6 → back to 5** after regenerating its own segment, and leave the other five alone
- **The `utilities` segment, measured 2026-09-14: 14 members, 6 · 2 · 6** — six incumbents (`dominion-energy`, `southern-company`, `entergy`, `oncor`, `aep`, `xcel-energy`), two challengers (`nrg-energy`, `vistra` — both competitive **retail** suppliers, not wires companies) and six adjacents (`engie-north-america`, `tesla`, `nextera-energy-resources`, `invenergy`, `grid-united`, `pattern-energy`). **Re-measure on the day.** The structural warning: `utility-aidc-procurement-2026-08` already exists and owns the **procurement playbook**; the landscape owns the **market-structure judgment**, and the two must state their split explicitly
- **Toggles:** `START_OF_RESPONSE_BLOCK` On · `CHAT_BOOKENDS` Off · `TIMING_ESTIMATES` On · `END_OF_RESPONSE_BLOCK` On · `MULTI_SESSION_MODE` Off
- **Where the programme stands, measured 2026-09-14 at v05.77r.** Done: Phases 0, 1, 2a, 2b, 3, 5, 6; S0, K1, S1, S3 (10 of 10), G6, C3, the bankability review, the one-off segment regeneration. Open: **Phase 4 at 8 of 26**, **S2 at 3 of ~19**, **K2** (1–2 sessions, runnable any time), **C5** (~6 sessions, gated on ~6 landscapes — **three now exist**), the **Q plan clock** (Fable 5.1 xhigh, next due ~2026-12). Deferred: C6. **About 41 sessions remain**, roughly 40 of them Opus 5 xhigh
- **No Phase 4 row is gated by a review date today.** `check-classroom-curriculum.py --strict` reports **0 items due for review**; the nearest is `large-load-interconnection-2026-09` at 2026-11-16
- **Open, recorded, not taken:** the `.cl-tbl` `min-width` question is now **diagnosed** (a first column holding one long unbreakable word) on seven clean data points and should be **closed** the next time a session has reason to open `Classroom.html`; the generated segment lesson's `read-next` still **names** the landscape module without linking to it — **S2 session 4 has the best claim on it**, since it is a `sec_read_next` template question and that session runs the generator anyway; and **`the-800-vdc-shift`'s `three-chains` intro is now three inserts stale** — "The previous lesson walked a megawatt from the service entrance to the rack" has been untrue since row 7 and is further off since row 8
- **Render harness — the recipe with this session's two corrections folded in.** Run the PROJECT region of `Classroom.gs` (**lines 329–47213**) under Node with GAS shims; shim **only** `validateSessionForData` plus no-op `clProgressRead_` / `dataAuditLog` / `auditLog`, and let `handleClassroomOp_` answer `index`, `track` and `lesson` for real. Serve a scratch copy of the page over `http://127.0.0.1` with `_e = ''` and `AUTO_REFRESH = false`; copy `profiler-data/`, `sounds/`, `images/` **and `favicon.ico`** in. Seed `sessionStorage` **after** load (`Classroom_gas_session_token` · `_gas_user_email` · `_gas_user_role` · `_gas_user_permissions`); override `_gasPost` **after** load and return a **Promise**, not a callback. **Hide** overlays with CSS — **`#auth-wall`, `#signing-out-wall`, `#auth-timers`, `#gas-layer-toggle`** plus `#cl-app{display:block !important}` — never `remove()`. Entrypoints: `clShowLesson(id)` for lessons, `clShowGuidanceDoc(id)` for guidance. Section nodes are `#cl-<sectionId>`. `pip install playwright` is needed; Chromium is pre-installed — launch with `executable_path='/opt/pw-browsers/chromium-1194/chrome-linux/chrome'`, never run `playwright install`
- **The two console errors in every render are environmental and pre-existing** — `ERR_CERT_AUTHORITY_INVALID` on `accounts.google.com/gsi/client` and on an external logo, both blocked because the sandbox proxy's CA is untrusted by Chromium. Identified **by URL**. Neither is a lesson asset and neither raises a page error

### Recommendation for next session

- Run **S2 session 4 — the `utilities` landscape module** — on Opus 5 xhigh. Its brief is **`INTEGRATED-REMEDIATION-PLAN.md` §7.23**, written at the close of Phase 4 row 8 and refreshed in this handover so its deployment counter is a measurement and its CHANGELOG figures describe the state the session will actually find. It restores the §7.4 item 6 interleave at 8:4, takes S2 to 4 of ~19, is the session that must move the content checker's guidance-module assertion from 12 to 13, and is the **first landscape that has to state its split against an existing guidance module** — `utility-aidc-procurement-2026-08` owns the procurement playbook, the landscape owns the market-structure judgment.

**To continue:** type `run S2 session 4`

## Previous Sessions

### Session — 2026-09-14 09:32:46 PM EST (v05.75r)

**Date:** 2026-09-14 09:32:46 PM EST
**Repo version:** v05.75r — two pushes (v05.74r the landscape module, v05.75r this handover)
**Branch:** `claude/hopeful-gates-1lzznw` (restarted from `origin/main` before each push)

### What was done

- **S2 session 3 — the `storage-developers-and-ipps` landscape module** (v05.74r). `landscape-storage-developers-and-ipps-2026-09`: the **twelfth guidance module and the third landscape**, authored below the `// CONTENT END` fence, nine §10.6 sections in its order, four tiles, a six-term glossary, and the **full twenty-seven-row `each-players-bet` table** — the longest the guidance corpus carries — with none trimmed
- **The analysis file first** — `repository-information/industry-guidance/landscape-storage-developers-and-ipps-analysis.md`, 267 lines, eleven sections, a **58-row claims ledger** in which every load-bearing claim names a dossier field at its `profileVersion`. Corpus synthesis over the 34 member dossiers and `profiler-segments.json` only; no ingested document, no web research
- **The segment re-measured on the day: 34 members, 19 · 8 · 7** — §7.21's figure confirmed exactly, and for once with nothing to correct: it genuinely is the deepest segment of the nineteen, and it holds **the largest incumbent set in the registry** (next is `epc-and-construction` at 16)
- **A Scraper seed WAS added** — `topic-storage-owners` — the first since session 1, after the first draft had to be **cut back against all 144 existing terms**
- **The content checker's module-count assertion moved 11 → 12** with its comment extended; one `--segment storage-developers-and-ipps` regeneration, `--check` **3 due → 2 due** exactly as predicted
- **v05.75r (this push)** — §7.22's deployment counter refreshed from forecast to measurement, its CHANGELOG figures moved to the post-push state, and this handover

### Where we left off

Everything is merged and live. **Phase 4 is 7 of 26; S2 is 3 of ~19.** GAS live at Classroom **v01.36g** and Scraper **v02.04g**, Pages serving `|v01.36g|`, `|v02.04g|` and `|v01.14w|`, all confirmed. The content-checker baseline is **0 errors / 0 warnings at 37 lessons / 8 tracks / 142 gate cases** and was held across the whole session. `build-classroom-segments.py --check` reports **2 due** — `power-conversion-and-rack-power-silicon` and `compute-and-the-rack`, both Phase 4 row 7's residue and both S2's to clear.

### Key decisions made

- **`reviewBy` — the sort returns NOTHING, which is a stronger finding than session 2's rather than a repeat of it.** In `cells-and-chemistry` a sort of the fence returned a *wrong* date while the prose held the right one. Here there is **no answer to sort at all**: the 34 members carry **142 `policyExposure[]` entries, 96 with an `effectiveDate`, and not one of those 96 is in the future** — the latest is 2026-09-01. A generator implementing "earliest future `effectiveDate`, else `updated` + 6 months" falls through to the default, which is exactly what the public segment lesson's own 2027-03-14 is. The taken value, **2027-01-01**, is the **prohibited-foreign-entity material-assistance cost ratio** stepping to its next calendar-year value: one member's policy prose puts it at 55% in 2026 rising toward 75% in 2030, another's records that the permissible percentage **varies by calendar year**, and neither puts it in a date field
- **Three nearer candidates rejected in writing.** The **16 September 2026** comment deadline in the co-location docket — *a procedural step in a docket, not a rule taking effect*, and the tariff it leads to carries no date anywhere; **5 October 2026**, a federal scoping step on an *adjacent* member the module leans no claim on (session 1's Cummins test); and the ERCOT verification *"running to December 2026"* — **a window rather than a date**, and a partial one the record never resolves to a day. A fourth, 31 December 2027, was rejected for governing **wind and solar** rather than storage
- **All three landscapes now carry `reviewBy` 2027-01-01 by three entirely different routes**, and the analysis file says so explicitly so the fourth session tests its own gate instead of assuming the answer
- **The Scraper-seed answer was yes — and the first draft was wrong, which is the transferable half.** The draft proposed `tolling agreement` and `resource adequacy` as the owner layer's nouns. Checking every term against **all 144 already in the array** showed `topic-storage-offtake` (labelled *Storage EPC, offtake & tolling*) already carries both, and `topic-capacity-markets` owns the queue and the auction — **the contract layer is already scored.** What survived is narrower and better founded: **ownership** — who owns the fleet, where it ranks, who is buying or selling the platform, scored by none of the 28 existing seeds. The justification is session 1's own: there is a **separate ranking table** for it (an owner table from government filings, against Wood Mackenzie's integrator table). **No `tv` marker was needed** — `tv` guards edits to an *existing* seed's terms and lives on `SCRAPER_SEGMENT_SEEDS`; this is a new key with no sheet row
- **The module's thesis: depth is not concentration.** Nineteen incumbents fall into four groups that do not compete for the same dollar — three that appear on the only independent owner table, eight standalone-storage franchises where procurement actually happens, five for whom storage is an attachment, and three merchant generators with the largest fleet numbers and the smallest battery budgets. And **the eight challengers attack the contract form, not the incumbents** — eight different answers to "how does a battery get paid for". A threat section padded to the last module's proportions would have missed it
- **The best sentence the segment produces: the buyer class that "decides which integrator's product gets installed" is mostly not the party that decides.** Four members say so in their own strategy reads — the tax-equity banks, the minority co-owners, five financing syndicates, an independent engineer, an infrastructure fund's diligence
- **UTC rolled to 2026-09-15 mid-session while EST was still 2026-09-14, and the distinction decided three rotations.** `changelogs.md` counts on the **EST** clock. Reading the harness clock would have forced rotation in the repo CHANGELOG *and* fired the armed Scraper file. All three stayed under their line. The clone was unshallowed early anyway as insurance — the precondition, not the remedy
- **The pipeline signature is confirmed three times for this lane:** **P1 ×4, P2 ×9, P7 ×1, no P3** — identical for S2 sessions 1, 2 and 3
- **The deploy cost nothing for the fourth time running.** Pages and the job log agreed, so **no `?op=deploy` probe was issued at all**: `Classroom deploy confirmed (GET): Updated to v01.36g (deployment 47) | 47/200` and Scraper at 149/200, both on the first GET leg with no `POLL` line
- **One of my own counts was caught and corrected before it propagated** — "ninety-nine dated entries" was wrong; the fence holds 142 entries of which 96 carry a date. Fixed in all three places it appeared
- **The stop hook fired a false positive at the close, and the cause is structural.** It resolved `upstream` to `origin/claude/hopeful-gates-1lzznw`, a **stale local remote-tracking ref** that `git fetch origin main` does not prune (refspec-limited). The "1 unpushed commit" was **the workflow's own `[skip ci]` SHA-tracker commit**, already on `origin/main`, pulled in by `git pull --ff-only origin main`. **`git fetch --prune origin` clears it.** Re-pushing would have been the wrong fix — it would queue a second workflow run on already-merged commits

### Active context

- **Repo version** v05.75r · **CHANGELOG** 115 raw / **98 non-exempt** (17 sections dated 2026-09-14). **The next push landing 2026-09-15 or later rotates** — that group stops being exempt, the count is 115, and the oldest whole group is **eighteen sections dated 2026-09-05 (`v04.61r`–`v04.78r`)**, leaving **97**. **Re-count on the EST clock** (`TZ=America/New_York date`), and match the **standalone** counter line — that string also appears inside older entries
- **Standing changelog traps:** `Scrapergs.changelog.md` at **51 raw / 49 non-exempt** is **still armed** — this session added a seed and stayed under only because the push landed on the EST day it was written; the next session to touch `Scraper.gs` on a later EST day fires rotation (oldest group: twenty-six sections dated 2026-08-28). `Profilerhtml.changelog.md` sits at 49/50. `Classroomgs.changelog.md` 36/50 with 29 non-exempt. **A Phase 4 row touches neither Scraper nor Profiler**
- **GAS versions:** Classroom **v01.36g**, Scraper **v02.04g**, Profiler v01.39g. **Page versions:** Classroom **v01.14w**, Profiler v01.90w, Scraper v01.72w
- **Content-checker baseline is 0 errors / 0 warnings at 37 lessons / 8 tracks / 142 gate cases** — protect it; nothing inherited remains. Its **guidance-module count assertion is now 12**, and **a Phase 4 row does not touch it**
- **Deployment counters:** Classroom **47/200**, Scraper **149/200**. **The v05.59r poll is armed and has still never been needed**
- **`--check` reports 2 due** — `power-conversion-and-rack-power-silicon` and `compute-and-the-rack`, row 7's residue. **Phase 4 row 8 will push this up through two channels**: `READ_NEXT` names `redundancy-by-the-numbers` in `in-hall-power`, `aidc-developers-and-landlords` and `neoclouds`, and the buying-criteria lexicon can add `grid-equipment` and `hyperscalers-and-ai-labs`. `PLANNED_TITLES` already holds the exact title, so the whole diff on each is a `" (planned)"` marker disappearing. **Expect roughly 5–7 due, not 0**
- **Toggles:** `START_OF_RESPONSE_BLOCK` On · `CHAT_BOOKENDS` Off · `TIMING_ESTIMATES` On · `END_OF_RESPONSE_BLOCK` On · `MULTI_SESSION_MODE` Off
- **Where the programme stands, measured 2026-09-14 at v05.75r.** Done: Phases 0, 1, 2a, 2b, 3, 5, 6; S0, K1, S1, S3 (10 of 10), G6, C3, the bankability review, the one-off segment regeneration. Open: **Phase 4 at 7 of 26**, **S2 at 3 of ~19**, **K2** (1–2 sessions, runnable any time), **C5** (~6 sessions, gated on ~6 landscapes — **three now exist**), the **Q plan clock** (Fable 5.1 xhigh, next due ~2026-12). Deferred: C6. **About 42 sessions remain**, roughly 41 of them Opus 5 xhigh
- **No Phase 4 row is gated by a review date today.** `check-classroom-curriculum.py --strict` reports **0 items due for review**; the nearest is `large-load-interconnection-2026-09` at 2026-11-16
- **Open, recorded, not taken:** the `.cl-tbl` `min-width` question now has a **fifth** clean data point (this session's 27-row × 4-column table at 1400px), so the v05.57r defect is settled as a first column holding one long word — row 8's `where-it-fails` **table** is the next test; the generated segment lesson's `read-next` still **names** the landscape module without linking to it (§10.4 forbids hand-editing a generated literal); and **`the-800-vdc-shift`'s `three-chains` intro is now two inserts stale** — it says "The previous lesson walked a megawatt from the service entrance to the rack", untrue since row 7, and row 8 moves that boundary again
- **Render harness — two corrections to the recorded recipe, both hard-won.** **`_gasPost` returns a Promise, not a callback** (`clApi`/`clGuidanceApi` do `_gasPost(qp).then(...)`), and the session keys are **page-name-prefixed**: `Classroom_gas_session_token` · `Classroom_gas_user_email` · `Classroom_gas_user_role` · `Classroom_gas_user_permissions`. The guidance entrypoint is **`clShowGuidanceDoc(id)`** (not `clShowGuidance`); lessons use `clShowLesson(id)`. Otherwise unchanged: run the PROJECT region (lines 329–46756) under Node with GAS shims, shim **only** `validateSessionForData` plus no-op `clProgressRead_`/`dataAuditLog`, serve a scratch copy over `http://127.0.0.1` with `_e = ''` and `AUTO_REFRESH = false`, seed `sessionStorage` **after** load, override `_gasPost` **after** load, **hide** overlays with CSS including `#cl-app{display:block !important}`, never `remove()`. Section nodes are `#cl-<sectionId>`. `pip install playwright` is needed; Chromium is pre-installed — launch with `executable_path='/opt/pw-browsers/chromium-1194/chrome-linux/chrome'`, never run `playwright install`
- **The two console errors in every render are environmental and pre-existing** — `ERR_CERT_AUTHORITY_INVALID` on `accounts.google.com/gsi/client` and on an external logo, both blocked because the sandbox proxy's CA is untrusted by Chromium. Identified **by URL**, not assumed. Neither is a lesson asset and neither raises a page error

### Recommendation for next session

- Run **Phase 4 row 8 — `redundancy-by-the-numbers`, *N+1, 2N, and Six Nines*** — on Opus 5 xhigh. Its brief is **`INTEGRATED-REMEDIATION-PLAN.md` §7.22**, written at the close of S2 session 3 and refreshed in this handover so its deployment counter is a measurement and its CHANGELOG figures describe the state the session will actually find. It restores the §7.4 item 6 interleave at 7:3, takes Phase 4 to 8 of 26, and is a **mid-track insertion at `aidc-grid-to-chip` index 5, immediately before `inside-the-rack`** — expect the **P1 + P5** signature, no P3 and no P8. It is also the first row in the queue whose §3.3 outline calls for a **`bars`** section and specifies `where-it-fails` as a **table** rather than the callout every predecessor used.

**To continue:** type `run Phase 4 row 8`

Developed by: LightAISolutions
