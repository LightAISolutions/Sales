# Previous Session Context

Claude writes to this file when the developer says **"Remember Session"** — capturing enough context for a future session to pick up the train of thought quickly. This is separate from "Reminders for Developer" (REMINDERS.md), which is the developer's own notes.

> **Note on stale-context auto-reconstruction** — when a session starts and this file's `Repo version:` doesn't match the current repo version, Claude reconstructs the missing entry from CHANGELOG.md and commits it **without pushing**. The commit rides along with the session's first user-task commit on the next push. If a session ends before any user-task push happens, the reconstructed entry stays **local-only** and the next session will just re-reconstruct from CHANGELOG if still stale. This is intentional — pushing a dedicated reconstruction commit on its own would force every subsequent user push in the same session to wait for the auto-merge workflow to finish before it could push too (push-once enforcement). The reconstructed entry is a convenience hint, not load-bearing state, so the small persistence risk is a fair trade.

## Latest Session

**Date:** 2026-09-17 04:12:42 AM EST
**Repo version:** v06.38r — one push, then this handover
**Branch:** `claude/inspiring-volta-1858gl` — restarted from `origin/main` before each push

### What was done

- **Phase 4 row 21 — the `utility-procurement-meets-ai-load` lesson** (v06.38r). The **fifty-first** lesson, the **second gated lesson Phase 4 has authored**, and the first guidance lesson `market-access` has ever carried: `clLessonUtilityProcurementMeetsAiLoad_()`, title *The Gatekeeper*, group Market Access & Bankability, authored **inside** the `// CONTENT START` fence, registered at the **end** of `clLessons_()` and **APPENDED** to `clTrackMarketAccess_()`
- **Eight sections at §3.5's ids and order** — `the-gatekeeper` (prose) · `the-four-moves` (table 4×4) · `five-channels` (**proscons**, 5 cards) · `who-signs-the-po` (callout) · `ercot-versus-regulated` (table 3×5) · `the-dated-gates` (**timeline**, 3 lanes / 7 items) · `drill` (6 cards) · `check-yourself` (5 items at **1·3·0·2·3**). Four tiles, a **seven-entry** glossary with every entry used, 28 distinct `{{term}}` uses all resolvable. **`reviewBy` 2026-12-10, inherited.** The literal is **100 % ASCII**
- **Eight stamps, every pin read off the document fetched this run** — `guidance:utility-aidc-procurement-2026-08@2026-08-24`, the six utility guides all `@2026-09-03`, `concepts:profiler-concepts@2026-09-13`
- **The module assertion stayed at 25** — a mechanism lesson registers no module. **Nothing was regenerated.** Scraper untouched
- **§7.3's and §6's Phase 4 rows flipped to 21 of 26**, curriculum §7 row 21 marked built, §4's table at `market-access` position 9 updated, **§8 item 10's standing check corrected**, **§10.6 findings (jj1)–(jj6)** written, and **§7.49 written in both halves**
- **Classroom GAS v01.63g → v01.64g**; page versions unchanged

### Where we left off

Everything is merged and live. **Phase 4 is 21 of 26; S2 is 16 of ~19**, so the interleave stands at 21:16 and hands the turn to **S2 session 17, `software-and-optimization`** — briefed in **§7.49**, written this session. Content checker **0/0 at 51 lessons / 8 tracks / 142 gate cases**, module assertion **25**. `--check` reports **7 due**.

### Key decisions made

- **A BRIEF'S ARITHMETIC ABOUT THE CORPUS IS A CLAIM ABOUT THE CORPUS — (jj1), and it is this session's most important finding.** §7.48 stated **in both halves** that `market-access` "already carries three gated lessons (`the-certification-stack`, `what-bankable-means`, `the-china-policy-stack`)" and therefore that the analyst `withheld` count would move **3 → 4** rather than 0 → 1 — and explicitly called that "a *different* arithmetic and worth calling before you measure it". **All three register ZERO times in `Classroom.gs`**: they are §7 rows 22–24, unbuilt. Measured base against head, the count moves **0 → 1**, identical to row 20's. The companion claim "the analyst index stays at 50 of 51" was wrong on both numbers: **the analyst index holds at 47** while the corpus goes 50 → 51. **The error was invisible to reading and obvious to a one-line grep. Grep for every entity a brief names before planning against it.**
- **A HANDOFF WRITTEN AS A POINTER SURVIVES THE ROW THAT CLOSES IT — (jj2), the first such instance the programme has found.** Row 20's (hh2) was that a declared handoff can be honoured in substance and false in its *status* clause. `where-bess-plugs-in`'s timeline `note` does the same job and **does not break**: it declines "the utility procurement lane — certification orders, storage RFP cycles, new large-load tariffs taking effect" as belonging "to the market-access material rather than here". It names a **destination**, not a state, so nothing in it became false. **Write the destination.**
- **THE GENERATOR PUBLISHES THE TITLES OF GATED LESSONS THAT DO NOT EXIST YET — (jj3).** Open item (xi) is answered and widened: `PLANNED_TITLES` renders *The Gatekeeper* **(planned)** in the `read-next` of `segment-hyperscalers-and-ai-labs` and `segment-utilities`, **both analyst-visible**, and it was doing so **before this lesson was written**. `segment-utilities` leaks **two** unbuilt guidance lessons in one callout. **Registration does not create the leak — it only removes the word that made it honest**
- **THE TIMELINE RENDERER HAS TWO UNDOCUMENTED CONTRACTS — (jj4), both found pre-splice and both used.** `CL_LANE_COLORS` is keyed on the **literal** lane names `gen`/`deploy`/`eco` and falls back to `var(--cl-faint)`, so a semantically-named lane loses the CVD trio entirely — `where-bess-plugs-in`'s `rules`/`tx` lanes are grey today. And `clTimeline` prints `Math.floor(it.x)` in the left gutter as a **year label**, so on a calendar timeline `x` should carry the real year: at 2025.52 … 2027.0 the gutter reads **2025 / 2026 / 2027**. Neither is in `CLASSROOM-SCHEMA.md` and no checker can see a violation
- **(ii7) REPEATED AND THE WORD-FORM SCAN WON FIVE TO ONE — (jj5).** Across the module and six guides, an **ISO regex finds one** future day-level date (the module's own `reviewBy`, a tautology) and a **word-form scan finds five**, **two earlier than the inherited date**. First Phase 4 sort to surface a candidate that would have beaten the inheritance; both were rejected on **scope** and **subject**, not lateness, which is stronger than an empty set. **Five rejections in writing**
- **THE RECOUNT PASS AND THE SCREENSHOT PASS CATCH DIFFERENT DEFECTS — (jj6).** Six pre-splice simulations returned **0/0**; a manual recount then caught **six** contradictions *inside* single fields, and the screenshots caught **four more** *between adjacent fields*, which are only visible once laid out together — an **eleventh** consecutive session. **Run both, in that order, every time**

### Active context

- **Repo version v06.38r** · **`CHANGELOG.md` 106 raw / 97 non-exempt** against a 100 trigger with **9** sections dated 2026-09-17 EST; `Classroomgs.changelog.md` **51 raw / 46 non-exempt** against a cap of 50; `Scrapergs.changelog.md` **38 / 36**; `Profilerhtml.changelog.md` **49**, untouched. **No rotation fired — the EST day had not rolled.** **A push landing 18 September or later is the rotator on `CHANGELOG.md` (106 against 100) AND past the boundary on `Classroomgs.changelog.md` (51 against 50)** — re-count the second by hand
- **GAS versions:** Classroom **v01.64g**, Scraper v02.17g, Profiler v01.39g. **Page versions:** Classroom v01.14w — no page bump
- **Content-checker baseline 0 errors / 0 warnings at 51 lessons, 8 tracks, 142 gate cases**, module assertion **25**. **S2 session 17 authors a MODULE and must move the assertion 25 → 26**
- **`--check` reports 7 due** — `cells-and-chemistry`, `power-conversion-and-rack-power-silicon`, `storage-developers-and-ipps`, **`hyperscalers-and-ai-labs`**, **`utilities`**, `software-and-optimization`, `insurance-and-risk-transfer`. **The two bolded are this row's residue**, left unregenerated by instruction. **5 → 7 was derived from the parsed generator before the write and was exact on both halves; (aa5) has now held for eleven sessions.** Next session's segment is **already in the list**, so its forecast is a **decrement** plus whatever its module adds
- **`check-classroom-curriculum.py --strict`** — no structural findings, **28** stale pins, **4** items due for review: `landscape-cooling-2026-09` at 2026-09-28 (overdue), `landscape-neoclouds-2026-09` at 2026-09-30, `landscape-utilities-2026-09` and `landscape-in-hall-power-2026-09` at 2026-10-01. **All four by design — do not "fix" any.** This lesson's 2026-12-10 is outside the 30-day horizon and adds none
- **Pipeline signature exactly as forecast for an APPEND: P1 × 2 on the plan files alone** — no P2, no P3, no P5, no P7, no P8, no P12. Selftest 13 fixtures / 0 failures
- **Deployment counters, MEASURED against v06.38r's own job log — the ONE-leg forecast was right.** `Classroom deploy confirmed (GET): Updated to v01.64g (deployment 76) | 76/200` in **16 s**, with exactly **one** `Deploy <Project>` step firing and the other **seven** exiting 0 silently in zero seconds on their `git diff` guard. The run closed on *All GAS deploys confirmed the merged version*. **Classroom is now at 76/200 with 124 left; Scraper at 162/200 with 38 — the tight one.** Pages read `|v01.63g|` at 04:17:03 AM EST and `|v01.64g|` at 04:18:22 AM, so the §7.19 order resolved on its **two reads** for the **thirty-first** time and no `?op=deploy` probe was made. The post-merge `check-readme-tree.py --fix` reported *already in sync* — because this session ran `--fix` itself after catching the GAS display drift the rules warn always gets missed
- **Toggles:** `START_OF_RESPONSE_BLOCK` On · `CHAT_BOOKENDS` Off · `TIMING_ESTIMATES` On · `END_OF_RESPONSE_BLOCK` On · `MULTI_SESSION_MODE` Off
- **Where the programme stands.** Done: Phases 0, 1, 2a, 2b, 3, 5, 6; S0, K1, S1, S3, G6, C3, the bankability review. Open: **Phase 4 at 21 of 26**, **S2 at 16 of ~19** (three remaining: `software-and-optimization`, then `insurance-and-risk-transfer` and `assurance`, fixed as 18th and 19th by the v05.62r record), **K2** (1–2 sessions), **C5** (~6 sessions; gate met), the **Q plan clock** (next due ~2026-12). Deferred: C6. **About 15 sessions remain**
- **`market-access` is 5 of 9 built** and is now the **fourth** track showing an analyst withheld count — the first in the market walk. Rows 22–24 add three more gated lessons to it; **row 22 (`the-china-policy-stack`) inherits from TWO modules (2026-12-31 and 2026-12-24) and must take the earlier**
- **Open, recorded, not taken — thirteen.** (i) `.cl-tbl` `min-width` — **thirty-four** data points, this row's 4×4 and 3×5 clean at 1400 px. (ii) The generated `read-next` names a landscape module without linking to it. (iii) `the-800-vdc-shift`'s stale `three-chains` intro is **twenty-seven** registrations stale. (iv) §10.6's session-4 block is out of chronological order. (v) A stale `lesson_ref` can make a segment due alone — this row left **two** such segments. (vi) **CLOSED** by row 20. (vii) `restart` scores zero for a **tenth** session. (viii) Session 12's six `topic-aidc-landlords` terms unseeded for a **sixth**. (ix) (ee4)/(gg7)'s dash rows. (x) (ff4)'s answer-index skew. (xi) **ANSWERED AND WIDENED by (jj3)** — the leak predates the lesson and `segment-utilities` carries two. (xii) `Scraper.gs` holds **nine** `topic-landscape-*` seeds against **sixteen** landscapes; shortfall seven, the developer's. (xiii) **NEW — (jj4)**: two built timelines violate undocumented renderer contracts and both make the instrument say something false
- **Render harness — WORKING, with two corrections this session found the hard way.** **(1) Do NOT blank `var _e`.** Blanking it means the page never creates `#gas-app`, so `_gasPost` has no `dataset.baseUrl` and the page renders *"Classroom content is not connected yet"*. Set `_e` to **`c2FnX18v`** (= base64 of `/__gas` reversed) so the transport points same-origin, and hide `#gas-app` along with the other overlays. **(2) `_gasPost` puts every parameter in the QUERY STRING, not the body** — the shim must read `u.searchParams`. Role plumbing that works: carry the role in the **session-token prefix** (`contributor_` + 32 chars) and have `validateSessionForData` return `role`, `permissions` (an ARRAY) and `email` at the **TOP LEVEL**. Overlays: `#auth-wall` / `#signing-out-wall` / `#auth-timers` / `#gas-layer-toggle` / `#gas-app` / `#tab-takeover-wall` plus `#cl-app{display:block !important}`. Seed sessionStorage **after** load. One Node process, one port, at `/Classroom.html`. **Do NOT put `pkill -f "node .*shim"` in the same Bash call that starts the shim — the pattern matches the enclosing shell's own command line and kills it; use `fuser -k -n tcp <port>`.** Start with `(setsid … &)` in a subshell. PROJECT region is line 1 to `// TEMPLATE START`
- **This session's render: contributor 8/8 sections, 25,030 characters, zero page errors**, zero unresolved `{{…}}`, zero literal asterisks, zero backticks; analyst **denied at the server** (`ROLE_DENIED`) with **0 of 8** section nodes at 60 characters and the title never in the DOM. PROJECT region moved **3,559,117 → 3,599,178 bytes** across the fix round and the render moved with it, so both meters were demanded and both moved. The DOM's only non-ASCII characters are the page's own `·`, `—` and `←`

### Recommendation for next session

- Run **S2 session 17 — the `software-and-optimization` landscape module** on Opus 5 xhigh. Its brief is **`INTEGRATED-REMEDIATION-PLAN.md` §7.49**, written this session. Four things carry the attention: the **module assertion must move 25 → 26** (it is a hard assertion and sessions forget); the roster is the **most adjacent-heavy in the taxonomy at 75 %** and both incumbents reach the segment from an adjacent business, which is the opening problem rather than a footnote; the **ranking vacuum is documented in the registry's own notes** — the only named third party omits one of the two incumbents, which is (ii1)'s shape with a sharper edge; and **the `--check` forecast is a DECREMENT**, because the segment is already among the seven due. Above all, **(jj1) applies to §7.49 itself**: grep for every entity it names before planning against it.

**To continue:** type `run S2 session 17`

## Previous Sessions

### Previous Session — v06.36r

**Date:** 2026-09-17 03:17:33 AM EST
**Repo version:** v06.36r — one push, then this handover
**Branch:** `claude/pensive-wright-23dc9n` — restarted from `origin/main` before each push

### What was done

- **S2 session 16 — the `compute-and-the-rack` landscape module** (v06.36r). The **twenty-fifth** guidance module and the **sixteenth** landscape: `guidanceDocLandscapeComputeAndTheRack_()`, id `landscape-compute-and-the-rack-2026-09`, lane The Value Chain, tier contributor, authored **below** the `// CONTENT END` fence and registered at the **end** of `guidanceDocs_()`'s Value Chain lane
- **Nine sections at §10.6's ids and order** — four tiles, an **eight-entry glossary with every entry used**, a **35-row** claims ledger, 13 drill cards and 7 quiz items at indices **3, 1, 2, 0, 3, 1, 2**. `reviewBy` **2027-03-17**. **The literal is 100 % ASCII**
- **The module assertion in `scripts/check-classroom-content.py` moved 24 → 25** in the same commit — the edit sessions forget, and §7.47 said so
- **Scraper seed `topic-landscape-compute-and-the-rack`**, twelve terms scored against all **494** distinct terms in both arrays, eight drops each with its ground
- **One segment regenerated** — `--check` **6 → 5**, called before the run
- **§7.3's S2 row, §6's S2 row and §10.6's progress note flipped to 16 of ~19**, `compute` struck, and **§7.3's order-table row 5 corrected from 14 to 16** — it was two sessions stale, the exact failure that row's own note warns about. **§10.6 findings `(ii1)`–`(ii10)`** written, and **§7.48 written in both halves**
- **Classroom GAS v01.62g → v01.63g, Scraper v02.16g → v02.17g**; page versions unchanged

### Where we left off

Everything is merged and live. **Phase 4 is 20 of 26; S2 is 16 of ~19**, so the interleave stands at 20:16 and hands the turn to **Phase 4 row 21, `utility-procurement-meets-ai-load`** — briefed in **§7.48**, written this session. Content checker **0/0 at 50 lessons / 8 tracks / 142 gate cases**, module assertion **25**. `--check` reports **5 due**.

### Key decisions made

- **THE COVERAGE BOUNDARY IS THE SEGMENT'S ONLY RANKING, SO IT IS THE OPENING SECTION — (ii1).** Four members, exactly the floor. The segment has **no named third-party ranking of itself**; the one named series it has measures the adjacent **server** market and names **four parties of which the corpus covers ONE** — ODM Direct 50.2 %, Dell 16.5, Supermicro 7.6, HPE 3.0, so **7.6 of the 77.3 points named**. Every figure is read out of the one covered member's own file, so **the segment's own member is the source for the fact that the segment is mostly uncovered**
- **THE GAP IS VISIBLE IN THE CURRICULUM'S OWN VOCABULARY — (ii2).** The reverse-(t) sweep returned empty on ODM, Foxconn, Wiwynn, Sanmina, Wistron, Inventec, HPE, rack OEM and contract manufacturer — and found two homonyms. **`Dell` appears ten times and NINE are `Dell'Oro`**, the research firm; **`Quanta` appears forty-two times and ALL forty-two are `Quanta Services`**, the covered EPC contractor. (hh3)'s collision class fired twice and pointed at the boundary rather than at a teaching hazard
- **FIVE OF SIX PAIRS AND ONE `competitor` TYPING — (ii3).** The densest roster ratio S2 has measured (83 % against cooling's 47 %) and the least rivalrous: **one of seven curated typings** reads competitor, against cooling's 36 of 39. The one missing pair is Flex ↔ Supermicro — the two members that both build racks to other people's designs, the two most alike, and the only pair the corpus does not connect
- **EVERY EXTERNAL MEMBERSHIP POINTS UPSTREAM — (ii4).** Six memberships at positions 3, 4, 5 and 8 against this segment's 9, **none in the ten segments downstream**. (aa2) and (ee3) fire together for the **second** time and **complete** rather than merely dominant
- **THE CROSS-ATTESTATION INSTRUMENT RETURNED ITS FIRST NEGATIVE — (ii5).** `nvidia` @ v10 says 80–85 % *per IDC-cited estimates*, marking 92 % as a 2023 figure; `amd` @ v1 says 75–92 %. Neither cites the other. **The typing survives; the sentence a seller may say does not.** Run an instrument for its negative result
- **`reviewBy` — THE EMPTY SET WAS TOTAL, AND TWO SCANS DISAGREED — (ii7)/(ii8).** 19 `policyExposure` entries, 10 dated, **none future**. An ISO-date regex finds **nothing** in any of the four files; a word-form scan finds **four**. The only in-window one, **2026-11-10**, was rejected on **PROVENANCE** — it lives only in a `sources[]` label. Seven rejections. And a **third tautology**: regenerating the segment on the same day moved its own generated `reviewBy` to **2027-03-17 as well**, which is unavoidable by construction because both are the same arithmetic on the same date
- **THE SCRAPER ROSTER IS BIASED IN THE DIRECTION OF THE MARKET IT OBSERVES — (ii9).** `blackwell`, `rubin`, `nvlink` are exact held terms; `instinct`, `helios`, `rocm`, `epyc` all score **zero**. A digest on that set bands the incumbent's news and is blind to the challenger's — **it would keep confirming the incumbency it exists to help measure.** A class beyond (gg9)'s half-seeded pair
- **(gg7) WAS RE-MEASURED AND IS OFF BY ONE — (ii6).** The `READ_NEXT` ∩ `CRITERION_LEXICON` intersection returns **two** segments resolving none, not three, and neither the generator nor the registry has moved since v05.41r. Its other half holds. **Do not trust a recorded split — re-run it**

### Active context

- **Repo version v06.36r** · **`CHANGELOG.md` 104 raw / 97 non-exempt** against a 100 trigger with **7** sections dated 2026-09-17 EST; `Classroomgs.changelog.md` **50 raw / 46 non-exempt** against a cap of 50; `Scrapergs.changelog.md` **38 / 36**; `Profilerhtml.changelog.md` **49**, untouched. **No rotation fired.** **A push landing on 18 September or later is the rotator on `CHANGELOG.md` (104 against 100) AND at the boundary on `Classroomgs.changelog.md` (50 against 50)** — re-count the second by hand rather than inferring it
- **GAS versions:** Classroom **v01.63g**, Scraper **v02.17g**, Profiler v01.39g. **Page versions:** Classroom v01.14w — no page bump
- **Content-checker baseline 0 errors / 0 warnings at 50 lessons, 8 tracks, 142 gate cases**, module assertion **25**. **Phase 4 row 21 authors a LESSON and registers no module, so the assertion STAYS at 25**
- **`--check` reports 5 due** — `cells-and-chemistry`, `power-conversion-and-rack-power-silicon`, `storage-developers-and-ipps`, `software-and-optimization`, `insurance-and-risk-transfer`. **6 → 5 was called before the run and held; (aa5) has now held for ten sessions.** **Row 21's forecast is derived and exact: 5 → 7** — its lesson sits in the `READ_NEXT` of `hyperscalers-and-ai-labs` and `utilities`, neither currently due, and it matches **none** of the 27 `CRITERION_LEXICON` entries
- **`check-classroom-curriculum.py --strict`** — no structural findings, **28** stale pins, **4** items due for review: `landscape-cooling-2026-09` at **2026-09-28** (overdue from 29 September), `landscape-neoclouds-2026-09` at 2026-09-30, `landscape-utilities-2026-09` and `landscape-in-hall-power-2026-09` at 2026-10-01. **All four by design — do not "fix" any.** This module at 2027-03-17 adds none
- **Pipeline signature exactly as forecast: P1 × 7 and P2 × 9, no P3, no P5, no P7, no P8, no P12.** Selftest 13 fixtures / 0 failures
- **Deployment counters, MEASURED against v06.36r's own job log — the TWO-leg forecast was right on both legs.** `Scraper deploy confirmed (GET): Updated to v02.17g (deployment 162) | 162/200` in **16 s** and `Classroom deploy confirmed (GET): Updated to v01.63g (deployment 75) | 75/200` in **17 s**; exactly two `Deploy <Project>` steps fired and every other exited 0 silently in zero seconds. The run closed on *All GAS deploys confirmed the merged version*. **Classroom is now at 75/200 with 125 left; Scraper at 162/200 with 38 — the tight one.** Pages read the old pair at 03:12:17 AM EST and the new pair at 03:13 AM, so the §7.19 order resolved on its **two reads** for the **thirtieth** time and no `?op=deploy` probe was made. The post-merge `check-readme-tree.py --fix` reported *already in sync*, so no correction commit landed behind the push
- **Toggles:** `START_OF_RESPONSE_BLOCK` On · `CHAT_BOOKENDS` Off · `TIMING_ESTIMATES` On · `END_OF_RESPONSE_BLOCK` On · `MULTI_SESSION_MODE` Off
- **Where the programme stands.** Done: Phases 0, 1, 2a, 2b, 3, 5, 6; S0, K1, S1, S3, G6, C3, the bankability review, the one-off segment regeneration. Open: **Phase 4 at 20 of 26**, **S2 at 16 of ~19** (three remaining: `software-and-optimization`, `insurance-and-risk-transfer`, `assurance` — and the order is now settled, the last two being 18th and 19th of 19 by the v05.62r record, so **`software-and-optimization` is S2 session 17**), **K2** (1–2 sessions, runnable any time), **C5** (~6 sessions; gate met), the **Q plan clock** (next due ~2026-12). Deferred: C6. **About 16 sessions remain**
- **Open, recorded, not taken — thirteen.** (i) The `.cl-tbl` `min-width` question — **thirty-two** data points. (ii) The generated `read-next` names a landscape module without linking to it, and prints its **id** rather than its title. (iii) `the-800-vdc-shift`'s stale `three-chains` intro is **twenty-six** registrations stale. (iv) §10.6's session-4 bullet block is out of chronological order. (v) A stale `lesson_ref` can make a segment due on its own — `compute-and-the-rack` was the **ninth**. (vi) **CLOSED** — the DC-arc taxonomy gap, by row 20. (vii) `restart` scores zero for a **ninth** session. (viii) Session 12's six `topic-aidc-landlords` terms are unseeded for a **fifth**. (ix) (ee4) now has **both** dash rows of one segment on it, plus the (gg7) correction. (x) (ff4)'s answer-index skew. (xi) (ff5)/(hh6)'s gated-title asymmetry — **row 21 will add a third lesson to the permissive side**. (xii) `Scraper.gs` holds **nine** `topic-landscape-*` seeds against **sixteen** built landscapes; the shortfall is unchanged at seven and is the developer's. (xiii) **NEW — (ii9)**: whether the scraper roster's incumbent-side tilt exists on other segments is unmeasured
- **Render harness — unchanged, with ONE correction this session found the hard way.** PROJECT region of `Classroom.gs` (line 1 to `// TEMPLATE START` — **recompute it**; it was **3,560,190 bytes** after this session) in a Node `vm`. **CORRECTION: the shim's `validateSessionForData` must return `role`, `permissions` (an ARRAY) and `email` at the TOP LEVEL** — `clRoleOf_` reads `sess.role` directly, so a `{ ok: true, session: {...} }` shape silently resolves to `viewer` and denies everything, which cost a round. Shim also no-ops `clProgressRead_`/`clProgressWrite_`/`clStudyNext_`/`dataAuditLog`/`auditLog`, **after** the module loads. Route `payload.action === 'guidance'` → `ctx.handleGuidanceOp_`, else `ctx.handleClassroomOp_`, **through the vm context object**; pass the op as `{ parameter: payload }`. The session parameter is **`session`** and must be ≥ 32 characters. **`pip install playwright` first**; Chromium is `/opt/pw-browsers/chromium-1194/chrome-linux/chrome`. **Serve static files and `/__gas` from ONE Node process on ONE port**, at **`/Classroom.html`**. Seed `sessionStorage` **after** load. Blank `var _e` with the regex `var _e = '[^']*';`. **Start the shim as `(setsid … &)` inside a subshell** — a bare `setsid … &` gets killed by a later `pkill` in the same shell
- **This session's render: contributor 9/9 sections, 43,782 characters, zero page errors**, zero unresolved `{{…}}`, zero literal asterisks, zero backticks; analyst **denied at the server** on both ops and **0 of 9** section nodes in the DOM at 66 characters. **The literal is 100 % ASCII**, so the rendered DOM's only non-ASCII characters are the page's own `·`, `—` and back-arrow. **All six pre-splice simulations ran BEFORE the splice and returned clean** — section ids, micro-markup, glossary usage, ASCII, strict JSON, quiz indices. **THE SCREENSHOTS THEN CAUGHT SEVEN DEFECTS, for a TENTH consecutive session**, and **five of the seven were the prose contradicting the content beside it**: two paragraphs calling both share ranges "unattributed" while the adjacent paragraph and ledger row 5 say one is sourced to IDC; "the three parties doing the threatening" including the one shrinking from 6.8 % to 3.0; a sales line promising three bets and listing four; a table intro claiming four rows are the spec's shape when the spec gives three; a sales line claiming two rows carry a diary date when **none** of the seven does; an unsupported "under a fifth of the market" against a measured 27.1 against 50.2; and "fourteen months" for four year-ends **six months** apart

### Recommendation for next session

- Run **Phase 4 row 21 — the `utility-procurement-meets-ai-load` lesson** on Opus 5 xhigh. Its brief is **`INTEGRATED-REMEDIATION-PLAN.md` §7.48**, written this session. Four things carry the attention: it is the **second gated lesson** Phase 4 has authored, so **row 20's denial test is the acceptance check** — but the arithmetic differs, because `market-access` already carries three gated lessons and its analyst `withheld` count moves **3 → 4** rather than 0 → 1; the **`--check` forecast is derivable exactly at 5 → 7** and should be called before the run; the **module assertion stays at 25** because a mechanism lesson registers no module; and the **split with `how-a-utility-buys` is already decided in §2.2 but its status clause expired at v05.54r**, which is exactly row 20's **(hh2)** and the trap this row is most exposed to.

**To continue:** type `run Phase 4 row 21`
